---
phase: 29-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hp41-math1-functions.json
  - hp41-cli/src/help_data.rs
  - hp41-cli/src/keys.rs
  - hp41-cli/src/app.rs
  - hp41-cli/tests/phase29_help_data_math1.rs
  - hp41-cli/tests/function_matrix_parity.rs
  - hp41-cli/tests/phase25_xeq_by_name.rs
  - hp41-cli/tests/phase25_pending_input.rs
  - hp41-cli/tests/key_coverage.rs
autonomous: true
requirements: [CLI-01, CLI-02]

must_haves:
  truths:
    - "`xeq_by_name_local_resolve(\"SINH\", 0b1)` returns `Some(Op::Sinh)`; `xeq_by_name_local_resolve(\"SINH\", 0b0)` returns `None`"
    - "`xeq_by_name_local_resolve` fires `xrom_resolve` ONLY as the final fallback after the 8 conditional-test mnemonic arms (D-25.6 / C-28.4 ordering preserved)"
    - "`hp41-cli/src/help_data.rs::help_entries_math1()` returns ≥ 47 unique `op_variant` rows on first call; subsequent calls return the cached slice (D-25.16 / D-25.17 hard-build-blocker pattern)"
    - "`help_entries_all()` chains both pools and is the new single source of truth for `?` overlay rows; legacy `help_entries()` is retained for the 130-target smoke test only"
    - "Every Math Pac I JSON entry's `xrom.module_id == 7` AND `display_name` is resolvable via `xrom_resolve` (bidirectional parity)"
  artifacts:
    - path: "docs/hp41-math1-functions.json"
      provides: "Canonical JSON source-of-truth for ~47 Math Pac I XEQ entry points (schema mirrors hp41cv-functions.json plus optional `xrom: { module, module_id, function_id }` object per D-29.1 / C-28.3)"
      contains: "47 unique op_variant rows; every entry carries xrom.module_id == 7"
    - path: "hp41-cli/src/help_data.rs"
      provides: "Second `OnceLock<Vec<HelpEntry>>` over the new JSON; new `help_entries_math1()` and `help_entries_all()` accessors per D-29.2"
      contains: "static MATH1_HELP_ENTRIES; pub fn help_entries_math1; pub fn help_entries_all; XromEntry struct on HelpEntry"
    - path: "hp41-cli/src/keys.rs"
      provides: "`xeq_by_name_local_resolve` widened signature with `xrom_resolve` final-fallback per CLI-01"
      contains: "pub fn xeq_by_name_local_resolve(name: &str, xrom_modules: u8) -> Option<Op>"
    - path: "hp41-cli/tests/phase29_help_data_math1.rs"
      provides: "Wave-0 smoke test mirror of phase25_help_data.rs against help_entries_math1()"
      contains: "math1_help_entries_loads_at_runtime; math1_help_entries_count_meets_47_target; math1_help_entries_all_xrom_module_id_is_7"
  key_links:
    - from: "hp41-cli/src/keys.rs::xeq_by_name_local_resolve"
      to: "hp41-core/src/ops/math1/xrom::xrom_resolve"
      via: "final-fallback arm replacing `_ => None`"
      pattern: "hp41_core::ops::math1::xrom::xrom_resolve\\(name, xrom_modules\\)"
    - from: "hp41-cli/src/help_data.rs::help_entries_math1"
      to: "docs/hp41-math1-functions.json"
      via: "include_str! + OnceLock + serde_json::from_str + .expect(\"hp41-math1-functions.json is malformed — fix the JSON\")"
      pattern: "include_str!\\(\"\\.\\./\\.\\./docs/hp41-math1-functions.json\"\\)"
    - from: "hp41-cli/tests/function_matrix_parity.rs"
      to: "hp41-core/src/ops/math1/xrom::xrom_resolve"
      via: "bidirectional parity assertion (every JSON entry resolvable; every MATH1_OP_VARIANT_NAMES entry has a JSON row)"
      pattern: "xrom_resolve\\(entry\\.display_name\\.as_str\\(\\), 0b0000_0001\\)"
---

<objective>
Wire `hp41-cli` to Phase 28's `xrom_resolve` and ship the canonical JSON pipeline mirror for Math Pac I. After this plan: typing `XEQ "SINH"` in the CLI resolves to `Op::Sinh` via the same chain `op_xeq` and `run_program::execute_op` use (closing the 3rd call site per `28-01-SUMMARY.md:173`); `docs/hp41-math1-functions.json` exists with ~47 entries; `help_entries_math1()` and `help_entries_all()` accessors are live; bidirectional parity is guarded by 3 new tests in `function_matrix_parity.rs`.

Purpose: Closes CLI-01 and CLI-02 in one plan because the resolver extension and the JSON file are independent (no shared call sites apart from the parity test). Both must land Wave 1 because plan 02 depends on `help_entries_all()` and plan 03 depends on the widened `xeq_by_name_local_resolve` signature.

Output: 1 new JSON data file (47 entries), 1 new smoke test file, 3 new tests appended to `function_matrix_parity.rs`, signature widening for `xeq_by_name_local_resolve` rippled through all call sites (production + 5 test files), `HelpEntry` schema extension with optional `xrom: Option<XromEntry>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cli-integration/29-CONTEXT.md
@.planning/phases/29-cli-integration/29-RESEARCH.md
@.planning/phases/29-cli-integration/29-PATTERNS.md
@.planning/phases/29-cli-integration/29-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key types and signatures the executor needs from Phase 28's shipped public surface. -->
<!-- Source-of-truth files: hp41-core/src/ops/math1/xrom.rs, hp41-cli/src/help_data.rs. -->

From hp41-core/src/ops/math1/xrom.rs:43-127 (Phase 28 shipped):
  pub struct XromModule { id: u8, name: &'static str, ops: &'static [(&'static str, Op)] }
  pub const MATH_1: XromModule = XromModule { id: 7, name: "MATH 1A", ops: &[ /* 52 entries */ ] };
  pub fn xrom_resolve(name: &str, modules: u8) -> Option<Op>
  // Gated by `modules & 0b0000_0001`. The 52-entry table includes 5 ASCII aliases
  // (C*, C/, Z^N, Z^1/N, E^Z, A^Z, Z^W) that map to the same Op as their Unicode primaries.

From hp41-cli/src/help_data.rs:45-77 (v2.2 shipped — the pattern to mirror):
  pub struct HelpEntry {
      pub op_variant: String,
      pub display_name: String,
      pub category: String,
      pub status: String,
      pub phase: Option<String>,
      pub key_path: Option<String>,
      pub description: String,
      #[serde(default)] pub divergences: Vec<String>,
  }
  const FUNCTIONS_JSON: &str = include_str!("../../docs/hp41cv-functions.json");
  static HELP_ENTRIES: OnceLock<Vec<HelpEntry>> = OnceLock::new();
  pub fn help_entries() -> &'static [HelpEntry]  // narrow accessor — RETAINED for 130-target test

From hp41-cli/src/keys.rs:347-370 (v2.1 shipped — extension point):
  pub fn xeq_by_name_local_resolve(name: &str) -> Option<Op>
  // Current body: 8 conditional-test arms (X<>Y?, X≠Y?, …) followed by `_ => None` at line 368.

From hp41-cli/src/app.rs:113 (PendingInput enum — DO NOT MODIFY in this plan; plan 03 owns the struct-variant migration):
  XeqByName(String),  // tuple variant; plan 03 widens to struct-variant
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author docs/hp41-math1-functions.json (47 entries) + Wave-0 smoke test</name>
  <files>docs/hp41-math1-functions.json, hp41-cli/tests/phase29_help_data_math1.rs, hp41-cli/src/help_data.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §3.2 (lines ~197–288), §8 Open-Q-4 to Open-Q-6, §7.5 (panic-message wording)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "docs/hp41-math1-functions.json" + "phase29_help_data_math1.rs" sections
    - .planning/phases/29-cli-integration/29-CONTEXT.md decisions D-29.1, D-29.2; canonical_refs JSON pipeline section
    - docs/hp41cv-functions.json (lines 1–60) — schema source to mirror verbatim
    - hp41-cli/src/help_data.rs:45-77 — HelpEntry struct + OnceLock pattern
    - hp41-cli/tests/phase25_help_data.rs (entire file, 131 lines) — exact template to mirror
    - hp41-core/src/ops/math1/xrom.rs:43-118 — MATH_1.ops canonical pull-list (52 entries, 47 unique Op variants after dedup of 5 ASCII aliases)
  </read_first>
  <behavior>
    - Test 1 (`math1_help_entries_loads_at_runtime`): `help_entries_math1()` returns non-empty slice; hard-build-blocker fires on malformed JSON per D-25.17.
    - Test 2 (`math1_help_entries_count_meets_47_target`): `help_entries_math1().len() >= 47` (matches deduplicated `MATH_1.ops` cardinality).
    - Test 3 (`math1_help_entries_has_no_duplicate_op_variants`): HashSet insertion confirms unique op_variant strings.
    - Test 4 (`math1_help_entries_all_have_non_empty_description`): every entry's `description` is non-empty and ≤ 200 chars.
    - Test 5 (`math1_help_entries_status_is_closed_enum`): every status ∈ {"implemented", "deferred-v3", "na"} (Phase 29 entries should be "implemented").
    - Test 6 (`math1_help_entries_all_xrom_module_id_is_7`): every entry's `xrom.module_id == 7` (C-28.3 invariant).
    - Test 7 (`math1_help_entries_categories_prefix_with_math1`): every category starts with `"Math1 "`.
    - Test 8 (`math1_help_entries_xrom_function_ids_are_dense`): function_ids form `1..=N` with no gaps and no duplicates.
    - Test 9 (`math1_help_entries_all_key_path_is_xeq_form`): every entry has `key_path == Some("XEQ \"<MNEMONIC>\"")` per D-28.6.
  </behavior>
  <action>
    1. Extend `HelpEntry` in `hp41-cli/src/help_data.rs` with a new `XromEntry` struct and an optional field (per RESEARCH §3.2 schema-extension snippet):

       - Add `#[derive(Debug, Clone, Deserialize)] #[allow(dead_code)] pub struct XromEntry { pub module: String, pub module_id: u8, pub function_id: u16 }`
       - Append `#[serde(default)] pub xrom: Option<XromEntry>,` to `HelpEntry`. `#[serde(default)]` means v2.2 entries (no `xrom` key) parse unchanged.

    2. Add the second `OnceLock` block in `help_data.rs` mirroring lines 62–77 verbatim except path + panic message wording (D-29.2 / §7.5):

       - `const MATH1_FUNCTIONS_JSON: &str = include_str!("../../docs/hp41-math1-functions.json");`
       - `static MATH1_HELP_ENTRIES: OnceLock<Vec<HelpEntry>> = OnceLock::new();`
       - `pub fn help_entries_math1() -> &'static [HelpEntry]` with `.expect("hp41-math1-functions.json is malformed — fix the JSON")` (distinct from v2.2 wording).
       - `pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry>` chaining `help_entries().iter().chain(help_entries_math1().iter())`.

    3. Author `docs/hp41-math1-functions.json` as a JSON array of 47 entries. Pull from `hp41-core/src/ops/math1/xrom.rs:43-118` `MATH_1.ops` table; for each unique `Op` variant, emit one entry. Categories per RESEARCH §8 Open-Q-6:
       - 6 entries `"Math1 Hyperbolics"` (Sinh, Cosh, Tanh, Asinh, Acosh, Atanh) — function_id 1..=6
       - 5 entries `"Math1 Complex Arithmetic"` (CPlus, CMinus, CTimes, CDiv, Real) — function_id 7..=11
       - 12 entries `"Math1 Complex Functions"` (Magz, Cinv, ZpowN, Zpow1N, ExpZ, LnZ, SinZ, CosZ, TanZ, ApowZ, LogZ, ZpowW) — function_id 12..=23
       - 2 entries `"Math1 Polynomial"` (PolyWorkflow, Roots) — function_id 24..=25
       - 8 entries `"Math1 Matrix"` (MatrixWorkflow, MatSize, MatVmat, MatEdit, MatDet, MatInv, MatSimeq, MatVcol) — function_id 26..=33
       - 1 entry `"Math1 Integration"` (Integ) — function_id 34
       - 2 entries `"Math1 Root Solver"` (Solve, Sol) — function_id 35..=36
       - 1 entry `"Math1 Differential Eq"` (Difeq) — function_id 37
       - 1 entry `"Math1 Fourier"` (Four) — function_id 38
       - 5 entries `"Math1 Triangle Solvers"` (TriSss, TriAsa, TriSaa, TriSas, TriSsa) — function_id 39..=43
       - 2 entries `"Math1 Coordinate Transform"` (Trans2d, Trans3d) — function_id 44..=45
       - Add Roots/Sol AT NEW function_ids if not already in MATH_1.ops alias group — verify against `MATH_1.ops` at plan-execute time.
       Each entry shape (per `29-PATTERNS.md` "docs/hp41-math1-functions.json" section):
       - `op_variant`: PascalCase Rust enum name (e.g. "Sinh", "MatrixWorkflow")
       - `display_name`: HP-41 mnemonic exactly as in `MATH_1.ops` (e.g. "SINH", "MATRIX", "C+", "Z↑N")
       - `category`: per-program category from list above
       - `status`: `"implemented"` (Phase 28 shipped all 47)
       - `phase`: `"28"`
       - `key_path`: `"XEQ \"<DISPLAY_NAME>\""` (D-28.6 — XEQ-by-name only)
       - `description`: short OM-cited single-sentence (e.g. `"Hyperbolic sine: X <- sinh(X)"`) — RESEARCH §8 Open-Q-5
       - `xrom`: `{ "module": "Math 1", "module_id": 7, "function_id": <n> }` (1-indexed per HP convention per RESEARCH §8 Open-Q-4)
       JSON contains 47 entries (1:1 with `Op` variants); the 5 ASCII aliases `C*`, `C/`, `Z^N`, `Z^1/N`, `E^Z`, `A^Z`, `Z^W` resolve via `xrom_resolve` to the same `Op` variants without separate JSON entries (Phase 30 docs-matrix surfaces aliases as decorations). This reconciles the "~55" count referenced in CONTEXT.md / ROADMAP.md / VALIDATION.md (Unicode primaries + 5 ASCII alias rows in `MATH_1.ops`) with the 47 dedup-by-Op JSON-row count this plan ships.

    4. Create `hp41-cli/tests/phase29_help_data_math1.rs` mirroring `phase25_help_data.rs` verbatim (header comment, `#![allow(clippy::unwrap_used)]`, `use hp41_cli::help_data::{help_entries_math1, help_entries_all};`). Implement the 9 tests listed in `<behavior>`. Each test name prefixed `math1_`. Per-test failure message includes the literal filename `hp41-math1-functions.json` so failures are routable.

    5. Run `cargo test -p hp41-cli --test phase29_help_data_math1` and confirm all 9 tests pass.

    Per D-29.1, D-29.2, D-25.16, D-25.17, C-28.3, D-28.6. Schema extension to `HelpEntry` is additive and `#[serde(default)]`-guarded so v2.2 JSON continues to parse unchanged.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase29_help_data_math1 --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hp41-math1-functions.json` exists and is non-empty (verify: `test -s docs/hp41-math1-functions.json`)
    - JSON validates: `python3 -c "import json; json.load(open('docs/hp41-math1-functions.json'))"` exits 0
    - `cargo test -p hp41-cli --test phase29_help_data_math1` reports `9 passed; 0 failed`
    - Source assertion: `grep -c "\"xrom\":" docs/hp41-math1-functions.json` outputs `47` (every entry has xrom block)
    - Source assertion: `grep -c "\"module_id\": 7" docs/hp41-math1-functions.json` outputs `47`
    - Alias-resolution acceptance: `xrom_resolve("C*", 0b1) == Some(Op::CTimes) && xrom_resolve("C/", 0b1) == Some(Op::CDiv) && xrom_resolve("Z^N", 0b1) == Some(Op::ZpowN) && xrom_resolve("Z^1/N", 0b1) == Some(Op::Zpow1N) && xrom_resolve("E^Z", 0b1) == Some(Op::ExpZ) && xrom_resolve("A^Z", 0b1) == Some(Op::ApowZ) && xrom_resolve("Z^W", 0b1) == Some(Op::ZpowW)` — all 7 ASCII aliases from `MATH_1.ops` resolve to their Unicode-primary `Op` variants (no separate JSON entries needed; alias coverage is the responsibility of `xrom_resolve`)
    - Source assertion: `grep -c "static MATH1_HELP_ENTRIES" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "pub fn help_entries_math1" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "pub fn help_entries_all" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "hp41-math1-functions.json is malformed — fix the JSON" hp41-cli/src/help_data.rs` outputs `1` (D-25.17 hard-build-blocker)
    - Source assertion: `grep -c "pub xrom: Option<XromEntry>" hp41-cli/src/help_data.rs` outputs `1`
    - Existing `phase25_help_data.rs::help_entries_count_meets_130_target` test still passes (narrow accessor preserved): `cargo test -p hp41-cli --test phase25_help_data help_entries_count_meets_130_target` exits 0
  </acceptance_criteria>
  <done>JSON file authored with 47 entries; 9 new smoke tests green; `help_entries_math1()` and `help_entries_all()` callable; `XromEntry` schema extension is `#[serde(default)]`-guarded; v2.2 130-target smoke test continues to pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen xeq_by_name_local_resolve with xrom_resolve fallback + ripple signature change to all call sites</name>
  <files>hp41-cli/src/keys.rs, hp41-cli/src/app.rs, hp41-cli/tests/phase25_xeq_by_name.rs, hp41-cli/tests/phase25_pending_input.rs, hp41-cli/tests/key_coverage.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §3.1 (resolver-chain extension), §3.0 (CLI-03 already-shipped verification)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "hp41-cli/src/keys.rs::xeq_by_name_local_resolve" section (lines ~340–405)
    - .planning/phases/29-cli-integration/29-CONTEXT.md decisions C-28.4, D-25.6
    - hp41-cli/src/keys.rs:347-370 — current function body (8 conditional-test arms + `_ => None`)
    - hp41-cli/src/app.rs:1423 — single production call site
    - hp41-core/src/ops/program.rs:67-87 — `op_xeq` resolver-chain pattern (3rd call site mirrors this)
    - hp41-cli/tests/phase25_xeq_by_name.rs:369-415 — `cli_resolver_matches_core_resolver` extension point
    - hp41-cli/tests/phase25_xeq_by_name.rs:94,99,105,108,113,122,133,200,214,225,243,322,344,376 — every existing call site (signature ripple)
    - hp41-cli/tests/key_coverage.rs (entire file) — grep for `xeq_by_name_local_resolve` call sites
  </read_first>
  <behavior>
    - With `xrom_modules == 0b0000_0001` (default Math 1 loaded): `xeq_by_name_local_resolve("SINH", 0b1)` returns `Some(Op::Sinh)`; same for MATRIX → MatrixWorkflow, DET → MatDet, C+ → CPlus, INTG → Integ, SOLVE → Solve.
    - With `xrom_modules == 0b0000_0000` (no XROM loaded): same calls return `None` — Math Pac I names fall through to `Op::Xeq` per existing fallback chain.
    - 8 existing conditional-test mnemonic arms (X<>Y?, X≠Y?, X#Y?, X<Y?, X>Y?, X<=Y?, X>=Y?, X#0?) continue to resolve correctly regardless of `xrom_modules` value (built-ins win — C-28.4 ordering).
    - `cli_resolver_matches_core_resolver` test in `phase25_xeq_by_name.rs` continues to pass for v2.1 card-reader names AND new Math Pac I names; with module unloaded, Math Pac I names return None.
  </behavior>
  <action>
    1. In `hp41-cli/src/keys.rs:347`, widen the signature from `pub fn xeq_by_name_local_resolve(name: &str) -> Option<Op>` to `pub fn xeq_by_name_local_resolve(name: &str, xrom_modules: u8) -> Option<Op>`. Replace the `_ => None` arm at line 368 with `_ => hp41_core::ops::math1::xrom::xrom_resolve(name, xrom_modules),` per CLI-01 / C-28.4.

    2. In `hp41-cli/src/app.rs:1423`, update the production call site: `keys::xeq_by_name_local_resolve(&acc)` → `keys::xeq_by_name_local_resolve(&acc, self.state.xrom_modules)`.

    3. Ripple the signature change through test files. Each call must thread an explicit `0b0000_0001` (default Math-1-loaded bitfield):
       - `hp41-cli/tests/phase25_xeq_by_name.rs` — 14 sites at lines 94, 99, 105, 108, 113, 122, 133, 200, 214, 225, 243, 322, 344, 376 (per RESEARCH §3.1)
       - `hp41-cli/tests/phase25_pending_input.rs` — any call sites (grep before editing)
       - `hp41-cli/tests/key_coverage.rs` — any call sites (NOT the local enum `KeyPath::XeqByName(String)` — different type per RESEARCH §3.6 "NOT affected" table)

    4. Extend `phase25_xeq_by_name.rs::cli_resolver_matches_core_resolver` (after line 415) with a Math Pac I canonical table:
       - 10 positive cases: ("SINH", Op::Sinh), ("ASINH", Op::Asinh), ("MATRIX", Op::MatrixWorkflow), ("DET", Op::MatDet), ("INV", Op::MatInv), ("C+", Op::CPlus), ("REAL", Op::Real), ("INTG", Op::Integ), ("SOLVE", Op::Solve), ("DIFEQ", Op::Difeq)
       - For each: assert `xeq_by_name_local_resolve(name, 0b0000_0001) == Some(expected_op.clone())`
       - Negative loop: for each name, assert `xeq_by_name_local_resolve(name, 0b0000_0000) == None`

    5. Run `cargo check -p hp41-cli --tests` first to surface every missed ripple site (Rust compiler enforces). Fix until clean.

    6. Run `cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver` and confirm green.

    Per CLI-01, C-28.4 (xrom fires LAST), D-25.6 (CLI-local resolver mirrors core `op_xeq` chain), FN-CLI-04 (no `_ =>` catch-all — `xrom_resolve` is the explicit fallthrough). The signature widening is mandatory because tests must be able to simulate XROM-unloaded state (`0b0000_0000`) — a module-level constant would lie to those tests.
  </action>
  <verify>
    <automated>cargo check -p hp41-cli --tests && cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-cli --tests` exits 0 (every signature ripple site updated)
    - `cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver` reports `1 passed; 0 failed`
    - Source assertion: `grep -c "fn xeq_by_name_local_resolve(name: &str, xrom_modules: u8) -> Option<Op>" hp41-cli/src/keys.rs` outputs `1`
    - Source assertion: `grep -c "hp41_core::ops::math1::xrom::xrom_resolve(name, xrom_modules)" hp41-cli/src/keys.rs` outputs `1`
    - Source assertion: `grep -c "xeq_by_name_local_resolve(&acc, self.state.xrom_modules)" hp41-cli/src/app.rs` outputs `1` (exact replacement at line 1423)
    - Source assertion: `grep -c "Op::Sinh" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
    - Source assertion: `grep -c "Op::MatrixWorkflow" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
    - Source assertion: `grep -c "0b0000_0000" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1` (negative-test bitfield used)
    - No `_ =>` arm remains in `xeq_by_name_local_resolve`: `grep -A 30 "fn xeq_by_name_local_resolve" hp41-cli/src/keys.rs | grep -c "_ => None"` outputs `0`
    - All Phase 25 XEQ-by-name tests still green: `cargo test -p hp41-cli --test phase25_xeq_by_name` exits 0
  </acceptance_criteria>
  <done>Resolver signature widened; xrom_resolve wired as final fallback after all 8 conditional-test arms; signature ripple complete across production + 5 test files; cli_resolver_matches_core_resolver extended with 10 Math Pac I positive cases + 10 negative cases (module-unloaded); all existing Phase 25 tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend function_matrix_parity.rs with bidirectional Math1 parity tests</name>
  <files>hp41-cli/tests/function_matrix_parity.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §5 (Test Architecture — function_matrix_parity.rs extension), §6 (Phase Requirements → Test Map)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "function_matrix_parity.rs extension" section (lines ~750–820)
    - .planning/phases/29-cli-integration/29-CONTEXT.md Claude's Discretion bullet on parity-test extension
    - hp41-cli/tests/function_matrix_parity.rs:200-267 — 4 existing tests (canonical pool patterns)
    - hp41-core/src/ops/math1/xrom.rs:43-118 — MATH_1.ops as source-of-truth for MATH1_OP_VARIANT_NAMES
  </read_first>
  <behavior>
    - Test 1 (`test_math1_op_inventory_count`): `MATH1_OP_VARIANT_NAMES.len() == 47` — drift detector for the constant.
    - Test 2 (`test_every_math1_rom_op_has_math1_json_entry`): forward parity — every `MATH1_OP_VARIANT_NAMES` entry has a matching `op_variant` row in `help_entries_math1()`. Failure message lists missing variants.
    - Test 3 (`test_every_math1_json_entry_has_xrom_resolver_match`): reverse parity — every JSON entry's `display_name` resolves via `xrom_resolve(display_name, 0b0000_0001)` to `Some(_)`. Failure message names the orphan entry.
    - Existing 4 v2.2 parity tests (lines 200–267) continue to pass against the narrow `help_entries()` accessor.
  </behavior>
  <action>
    1. Append to `hp41-cli/tests/function_matrix_parity.rs` (after line 267, after the 4 existing tests):

       - Declare `const MATH1_OP_VARIANT_NAMES: &[&str] = &[ /* 47 entries */ ];` covering all Math Pac I Op variants per RESEARCH §5:
         "Sinh", "Cosh", "Tanh", "Asinh", "Acosh", "Atanh",
         "CPlus", "CMinus", "CTimes", "CDiv", "Real",
         "Magz", "Cinv", "ZpowN", "Zpow1N", "ExpZ", "LnZ", "SinZ", "CosZ", "TanZ", "ApowZ", "LogZ", "ZpowW",
         "PolyWorkflow", "Roots",
         "MatrixWorkflow", "MatSize", "MatVmat", "MatEdit", "MatDet", "MatInv", "MatSimeq", "MatVcol",
         "Integ", "Solve", "Sol", "Difeq",
         "Four", "TriSss", "TriAsa", "TriSaa", "TriSas", "TriSsa",
         "Trans2d", "Trans3d"
         Verify against `MATH_1.ops` at plan-execute time — total = 47.

       - Test 1 — `test_math1_op_inventory_count`:
         `assert_eq!(MATH1_OP_VARIANT_NAMES.len(), 47, "MATH1 inventory drift");`

       - Test 2 — `test_every_math1_rom_op_has_math1_json_entry`:
         Use `help_entries_math1()` (narrow accessor — Math1 pool only). Build `HashSet<&str>` of `op_variant`s, collect missing names, assert empty.

       - Test 3 — `test_every_math1_json_entry_has_xrom_resolver_match`:
         For each entry in `help_entries_math1()`, call `hp41_core::ops::math1::xrom::xrom_resolve(entry.display_name.as_str(), 0b0000_0001)` and assert `.is_some()`. Failure message includes both `entry.op_variant` and `entry.display_name`.

    2. Import `use hp41_cli::help_data::help_entries_math1;` at the top of the file (already imports `help_entries`).

    3. Run `cargo test -p hp41-cli --test function_matrix_parity` and confirm 7 passing tests (4 existing v2.2 + 3 new Math1).

    Per CLI-02, D-29.2 (merged accessor enforces single source of truth), D-25.16 (JSON-canonical pipeline parity), C-28.4 (xrom_resolve table is the canonical name→Op mapping). Tests are SURGICAL — v2.2 pool stays untouched (4 existing tests continue using `help_entries()`); Math1 pool gets its own 3 tests. Per-pool partition prevents future v3.1 Stat Pac additions from breaking v2.2 assertions.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test function_matrix_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test function_matrix_parity` reports `7 passed; 0 failed` (4 existing + 3 new)
    - Source assertion: `grep -c "MATH1_OP_VARIANT_NAMES" hp41-cli/tests/function_matrix_parity.rs` outputs at least `3` (1 declaration + 2 references)
    - Source assertion: `grep -c "test_math1_op_inventory_count" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion: `grep -c "test_every_math1_rom_op_has_math1_json_entry" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion: `grep -c "test_every_math1_json_entry_has_xrom_resolver_match" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion: `grep -c "help_entries_math1" hp41-cli/tests/function_matrix_parity.rs` outputs at least `2`
    - Source assertion: `grep -c "xrom_resolve(entry.display_name.as_str(), 0b0000_0001)" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Existing v2.2 narrow-pool tests still green: `cargo test -p hp41-cli --test function_matrix_parity test_op_inventory_count_matches_enum` exits 0
  </acceptance_criteria>
  <done>3 new Math1 parity tests append to `function_matrix_parity.rs`; `MATH1_OP_VARIANT_NAMES` const is the inventory drift sentinel; forward + reverse parity proven; v2.2 pool tests preserved unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard → CLI App | Math Pac I mnemonics enter via XeqByName modal accumulator (already-existing v2.1 alpha-collection UX; trusted-but-untyped string) |
| JSON file → OnceLock | `docs/hp41-math1-functions.json` is compile-time-embedded via `include_str!`; parsed at first access (D-25.17 hard-build-blocker) |
| CLI resolver → hp41-core | `xeq_by_name_local_resolve` calls `xrom_resolve` with a `&str` name and `u8` bitfield — read-only, no state mutation in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | `docs/hp41-math1-functions.json` (untracked malformed edits) | mitigate | D-25.17 hard-build-blocker: `.expect("hp41-math1-functions.json is malformed — fix the JSON")` panics on first access; smoke test `phase29_help_data_math1::math1_help_entries_loads_at_runtime` is the CI gate |
| T-29-01-02 | Tampering | Math Pac I JSON entries vs `MATH_1.ops` drift | mitigate | Bidirectional parity tests (`test_every_math1_rom_op_has_math1_json_entry` + `test_every_math1_json_entry_has_xrom_resolver_match`) catch any divergence |
| T-29-01-03 | Spoofing | Math Pac I mnemonic shadows existing built-in (Pitfall 1 reprise) | accept | Phase 28 already shipped `tests/xrom_shadowing.rs`; this plan does NOT add new mnemonics — only wires the existing table. Risk dormant. |
| T-29-01-04 | Tampering | Signature ripple misses a test call site → silent default-bitfield use | mitigate | Compiler-enforced: `cargo check -p hp41-cli --tests` fails until every call site is updated to `(name, xrom_modules)` form |
| T-29-01-05 | Information disclosure | `xrom_resolve` reads `state.xrom_modules` (u8 bitfield, no PII) | accept | No PII; bitfield is local state. |
| T-29-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure source edits + JSON authoring. RESEARCH §"Environment Availability" confirms zero new runtime deps. |
</threat_model>

<verification>
- `cargo test -p hp41-cli --tests` is green for all 3 new test surfaces (phase29_help_data_math1 + extended function_matrix_parity + extended phase25_xeq_by_name)
- `cargo check -p hp41-cli` clean (signature ripple complete; no `_ =>` arm in `xeq_by_name_local_resolve`)
- `just ci` clean at wave-end (lint + test + coverage gate; coverage gate ≥ 95% on hp41-core unchanged because Phase 29 doesn't touch hp41-core in this plan)
- JSON file parses: `python3 -c "import json; json.load(open('docs/hp41-math1-functions.json'))"` exits 0
- 130-target v2.2 invariant preserved: `cargo test -p hp41-cli --test phase25_help_data help_entries_count_meets_130_target` exits 0
</verification>

<success_criteria>
- CLI-01: `xeq_by_name_local_resolve("SINH", 0b1) == Some(Op::Sinh)` AND `xeq_by_name_local_resolve("SINH", 0b0) == None` — proven by `cli_resolver_matches_core_resolver` extension
- CLI-02 (a): second `OnceLock` loads `docs/hp41-math1-functions.json` lazily via `help_entries_math1()`; merged accessor `help_entries_all()` chains both pools — proven by `phase29_help_data_math1` 9-test suite
- CLI-02 (b): bidirectional parity between `MATH_1.ops` ↔ JSON ↔ `Op::*` variants — proven by 3 new tests in `function_matrix_parity.rs`
- Compile-time exhaustiveness preserved: no `_ =>` arm in `xeq_by_name_local_resolve`; `xrom_resolve` is the explicit final fallthrough (FN-CLI-04 spirit)
- Save-file backward compat preserved: no new `CalcState` fields; `HelpEntry::xrom` field is `#[serde(default)]`-guarded
</success_criteria>

<output>
Create `.planning/phases/29-cli-integration/29-01-SUMMARY.md` when done.
</output>