---
phase: 29-cli-integration
plan: 03
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/solve.rs
  - hp41-core/src/ops/math1/integ.rs
  - hp41-core/src/ops/math1/difeq.rs
  - hp41-core/src/ops/math1/matrix.rs
  - hp41-core/src/ops/math1/poly.rs
  - hp41-core/src/ops/math1/four.rs
  - hp41-core/src/ops/math1/trans.rs
  - hp41-cli/src/app.rs
  - hp41-cli/src/ui.rs
  - hp41-cli/src/keys.rs
  - hp41-cli/tests/phase29_modal_flow.rs
  - hp41-cli/tests/phase29_pending_prompt_modal.rs
  - hp41-cli/tests/phase25_pending_input.rs
  - hp41-cli/tests/phase25_xeq_by_name.rs
  - docs/verifying-math-pac-1.md
autonomous: true
requirements: [CLI-05]

must_haves:
  truths:
    - "Dispatching `Op::Solve` (or `Op::Integ` / `Op::Difeq`) interactively (`!state.is_running`) opens the modal at `FunctionNamePrompt`: `state.modal_program == Some(ModalProgram::Solve(SolveInputStep::FunctionNamePrompt))` AND `state.modal_prompt == Some(\"FUNCTION NAME?\")`"
    - "After dispatch, `App::call_dispatch` auto-opens `PendingInput::XeqByName { acc: \"\", mode: XeqByNameMode::CollectForModal }` when `modal_program.requires_alpha_label() == true` (D-29.9)"
    - "Typing 'F' + ENTER in CollectForModal mode calls `submit_modal_with_label(state, \"F\")` which writes `state.alpha_reg == \"F\"` (uppercased, trimmed) AND advances `modal_program` to `Solve(Guess1Prompt)` AND `modal_prompt == Some(\"GUESS 1=?\")` AND `pending_input == None`"
    - "Pressing R/S (`KeyCode::F(5)`) when `state.modal_program.is_some() && pending_input.is_none()` calls `submit_modal(state)` which flushes `entry_buf`, advances the per-program step, and updates `state.modal_prompt`. Example: in Matrix(OrderPrompt) with entry_buf=\"3\", after R/S: `modal_program == Matrix(ElementPrompt(0,0))` AND `modal_prompt == Some(\"A1,1=?\")`"
    - "Pressing Esc when `state.modal_program.is_some() && pending_input.is_none() && !shift_armed` calls `cancel_modal(state)` which clears `modal_program`, `modal_prompt`, `entry_buf`, leaves stack untouched, sets `app.message == Some(\"Cancelled\")`"
    - "`pending_prompt(None, Some(\"ORDER=?\"))` returns `\"ORDER=?\"`; `pending_prompt(Some(&XeqByName{Normal}), None)` returns `\"XEQ \\\"...\\\"\"` (existing v2.2 behavior); precedence well-defined when both Some"
    - "Two-step Esc convention: with f-prefix armed + modal open, first Esc clears `shift_armed`, second Esc cancels modal (ordering #1 pending_input → #2 shift_armed → #3 modal_program preserved per D-07 / §7.2)"
    - "ALL existing v2.2 PendingInput::XeqByName pattern-match sites updated to struct-variant form (10 production + 10 test sites enumerated in RESEARCH §4)"
  artifacts:
    - path: "hp41-core/src/ops/math1/mod.rs"
      provides: "Public surface: `submit_modal`, `cancel_modal`, `submit_modal_with_label` per D-29.5 / D-29.6 / D-29.7"
      contains: "pub fn submit_modal; pub fn cancel_modal; pub fn submit_modal_with_label; pub use modal::ModalProgram"
    - path: "hp41-core/src/ops/math1/modal.rs"
      provides: "`requires_alpha_label()` method on `ModalProgram` per D-29.9"
      contains: "pub fn requires_alpha_label(&self) -> bool"
    - path: "hp41-core/src/ops/math1/solve.rs"
      provides: "`op_solve` interactive branch opens FunctionNamePrompt modal (RESEARCH §7.4 / Open Q1 Option A); new `submit_step` and `submit_label_step` free functions"
      contains: "if !state.is_running { state.modal_program = Some(ModalProgram::Solve(SolveInputStep::FunctionNamePrompt)); state.modal_prompt = Some(\"FUNCTION NAME?\".to_string()); return Ok(()); }; pub fn submit_step; pub fn submit_label_step"
    - path: "hp41-core/src/ops/math1/integ.rs"
      provides: "Same interactive-branch pattern + submit_step + submit_label_step"
      contains: "pub fn submit_step; pub fn submit_label_step"
    - path: "hp41-core/src/ops/math1/difeq.rs"
      provides: "Same interactive-branch pattern + submit_step + submit_label_step"
      contains: "pub fn submit_step; pub fn submit_label_step"
    - path: "hp41-core/src/ops/math1/matrix.rs"
      provides: "New `pub fn submit_step` (additive — `op_matrix_workflow` already opens modal correctly per Phase 28 ship)"
      contains: "pub fn submit_step"
    - path: "hp41-cli/src/app.rs"
      provides: "PendingInput::XeqByName struct-variant + XeqByNameMode enum; R/S interception (line ~638); Esc interception; call_dispatch + call_dispatch_and_drain auto-open hook"
      contains: "pub enum XeqByNameMode { Normal, CollectForModal }; XeqByName { acc: String, mode: XeqByNameMode }; submit_modal; cancel_modal; submit_modal_with_label; maybe_auto_open_collect_for_modal"
    - path: "hp41-cli/src/ui.rs"
      provides: "Widened `pending_prompt` signature (Option A from RESEARCH §3.3); `state.modal_prompt` surfaces in status bar"
      contains: "pub fn pending_prompt(pending: Option<&PendingInput>, modal_prompt: Option<&str>) -> String"
    - path: "hp41-cli/tests/phase29_modal_flow.rs"
      provides: "Wave-0 integration tests covering R/S submit, Esc cancel, auto-open CollectForModal, label submission, shift_armed precedence"
      contains: "matrix_workflow_order_prompt_advances_on_r_s; solve_workflow_auto_opens_collect_for_modal; solve_workflow_label_submission_advances_to_guess1; esc_cancels_open_modal; esc_shift_armed_takes_precedence_over_modal_cancel"
    - path: "hp41-cli/tests/phase29_pending_prompt_modal.rs"
      provides: "Wave-0 unit tests for widened `pending_prompt` signature"
      contains: "pending_prompt_renders_modal_prompt_when_no_pending; pending_prompt_renders_pending_when_no_modal; pending_prompt_modal_wins_when_both_active"
    - path: "docs/verifying-math-pac-1.md"
      provides: "§9 table updated to mark Math Pac I modal flows ✅ on CLI"
      contains: "CLI ✅ for MATRIX/SOLVE/POLY/INTG/DIFEQ/FOUR/TRANS"
  key_links:
    - from: "hp41-cli/src/app.rs::handle_key (R/S interceptor)"
      to: "hp41-core/src/ops/math1::submit_modal"
      via: "F5 keystroke when modal_program.is_some() && pending_input.is_none() — BELOW pending_input route (D-07)"
      pattern: "hp41_core::ops::math1::submit_modal\\(&mut self\\.state\\)"
    - from: "hp41-cli/src/app.rs::handle_key (Esc interceptor)"
      to: "hp41-core/src/ops/math1::cancel_modal"
      via: "Esc keystroke when modal_program.is_some() && pending_input.is_none() && !shift_armed — BELOW shift_armed route (§7.2)"
      pattern: "hp41_core::ops::math1::cancel_modal\\(&mut self\\.state\\)"
    - from: "hp41-cli/src/app.rs::call_dispatch (post-dispatch hook)"
      to: "hp41-cli/src/app.rs::maybe_auto_open_collect_for_modal"
      via: "trailing call after every hp41-core dispatch"
      pattern: "self\\.maybe_auto_open_collect_for_modal\\(\\)"
    - from: "hp41-cli/src/app.rs::handle_xeq_by_name (Enter arm, CollectForModal mode)"
      to: "hp41-core/src/ops/math1::submit_modal_with_label"
      via: "branch on XeqByNameMode::CollectForModal in Enter arm"
      pattern: "submit_modal_with_label\\(&mut self\\.state, &acc\\)"
    - from: "hp41-cli/src/ui.rs::pending_prompt"
      to: "state.modal_prompt"
      via: "widened signature accepts Option<&str> modal_prompt arg from caller"
      pattern: "pub fn pending_prompt\\(\\s*pending: Option<&crate::app::PendingInput>,\\s*modal_prompt: Option<&str>"
---

<objective>
Land the modal-prompt routing for ALL Math Pac I workflows (MATRIX / SOLVE / POLY / INTG / DIFEQ / FOUR / TRANS) on the CLI per CLI-05. Six contracts close in this plan:

1. **`hp41-core` public surface (additive — D-29.5 / D-29.6 / D-29.7)**: new `submit_modal`, `cancel_modal`, `submit_modal_with_label` free functions in `hp41-core/src/ops/math1/mod.rs`; new `requires_alpha_label()` method on `ModalProgram` in `modal.rs`; new per-program `submit_step` and `submit_label_step` helpers in each `math1/<program>.rs` file.

2. **`op_solve` / `op_integ` / `op_difeq` interactive branch (RESEARCH §7.4 / Open Q1 — additive completion of Phase 28 stubs)**: each function gains an `if !state.is_running { open modal; return Ok(()) }` branch mirroring the literal pattern in `op_xeq` at `program.rs:67-87`. The Phase-28 inline comment at `solve.rs:188` explicitly cross-references Phase 29 / CLI-07 as the wiring site — this is documented completion, not a frozen-source violation. Justified as "the additive-public-surface clause covers this — the bodies were stubs designed to be filled in Phase 29".

3. **`PendingInput::XeqByName` struct-variant migration (D-29.8)**: rename tuple variant `XeqByName(String)` to struct variant `XeqByName { acc: String, mode: XeqByNameMode }`. New `XeqByNameMode { Normal, CollectForModal }` enum in `hp41-cli/src/app.rs`. Update 10 production sites + 10 test sites enumerated in RESEARCH §4. Compile-time exhaustive match preserved (FN-CLI-04).

4. **`pending_prompt` signature widening (D-29.3 / RESEARCH §3.3 Option A)**: `pub fn pending_prompt(pending: Option<&PendingInput>, modal_prompt: Option<&str>) -> String`. Render `modal_prompt` when `pending.is_none()`; precedence rule when both Some (modal_prompt wins for CollectForModal mode).

5. **R/S and Esc interception in `handle_key` (D-29.5 / D-29.6)**: insert two new top-level handlers BELOW the `pending_input.is_some()` route (D-07 invariant preserved) and below the `shift_armed` route (§7.2). R/S calls `submit_modal`; Esc calls `cancel_modal`.

6. **`call_dispatch` post-dispatch auto-open hook (D-29.9)**: new `maybe_auto_open_collect_for_modal()` helper invoked at the tail of `call_dispatch` AND `call_dispatch_and_drain`. Auto-opens `PendingInput::XeqByName { mode: CollectForModal }` when `state.modal_program.requires_alpha_label() && pending_input.is_none()`.

Purpose: Closes CLI-05 — every Math Pac I modal flow (MATRIX `ORDER=?`, SOLVE `FUNCTION NAME?` → `GUESS 1=?` → …, INTG, DIFEQ, FOUR, TRANS) is reachable end-to-end from the CLI. Wires Phase 28's dormant `ModalProgram` state machine to actual keystroke routing. CLI ↔ GUI parity (D-25.6) preserved: every behavior added here routes through shared `hp41-core` code — Phase 31 will reuse the SAME `submit_modal` / `cancel_modal` / `submit_modal_with_label` / `requires_alpha_label` functions identically.

Output: 3 new free functions + 1 new method in `hp41-core`; 1 new enum + 1 widened enum variant + 3 new handlers + 1 widened function signature + 1 new helper method on App in `hp41-cli`; 2 new Wave-0 test files (1 integration + 1 unit); signature-ripple sweep across 4 existing test files; 1 documentation-only update to `docs/verifying-math-pac-1.md §9`.

**Frozen-source policy compliance.** CONTEXT.md `<domain>` says "the entire family is FROZEN as of Phase 28 ship except for the additive `submit_modal` / `cancel_modal` / `requires_alpha_label` public surface". This plan honors that by:
- Adding ONLY new `pub fn` / `pub method` items in `math1/*.rs`
- NOT modifying any existing function body except the three `op_solve` / `op_integ` / `op_difeq` stubs, where the modification is explicitly anticipated by the Phase 28 inline comment at `solve.rs:188` (Phase 29 / CLI-07 cross-reference). RESEARCH §7.4 / Open Q1 Option A. The change is structurally identical to `op_xeq`'s existing `if !state.is_running { … }` branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cli-integration/29-CONTEXT.md
@.planning/phases/29-cli-integration/29-RESEARCH.md
@.planning/phases/29-cli-integration/29-PATTERNS.md
@.planning/phases/29-cli-integration/29-VALIDATION.md
@.planning/phases/29-cli-integration/29-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Phase 28 public surface this plan consumes + extends. Source-of-truth: hp41-core/src/ops/math1/. -->

From hp41-core/src/ops/math1/modal.rs:24-63 (Phase 28 shipped):
  pub enum ModalProgram {
      Matrix(MatrixInputStep),
      Solve(SolveInputStep),
      Poly(PolyInputStep),
      Integ(IntegInputStep),
      Difeq(DifeqInputStep),
      Four(FourInputStep),
      Trans(TransInputStep),
  }
  // Each per-program step enum has its own variants — search file for FunctionNamePrompt:
  // SolveInputStep::FunctionNamePrompt   (line 121)
  // DifeqInputStep::FunctionNamePrompt   (line 220)
  // IntegInputStep::FunctionNamePrompt   (line 268)
  // MatrixInputStep::OrderPrompt; MatrixInputStep::ElementPrompt(usize, usize); etc.
  // Each `*InputStep` has a current_prompt() -> Option<String> method.
  impl ModalProgram {
      pub fn current_prompt(&self) -> Option<String>;
  }

From hp41-core/src/state.rs:160-235 (Phase 28 shipped):
  pub struct CalcState {
      // … existing v2.2 fields …
      pub xrom_modules: u8,                            // line 164, serde default = "default_xrom_modules"
      pub complex_mode: bool,                          // serde default
      pub matrix_dim: Option<(u8, u8)>,                // serde default
      pub matrix_active_reg: Option<u8>,               // serde default
      pub modal_program: Option<ModalProgram>,         // line 186, serde default+skip
      pub modal_prompt: Option<String>,                // line 194, serde default+skip
      pub integ_state: Option<IntegState>,             // serde skip
      pub solve_state: Option<SolveState>,             // serde skip
      pub difeq_state: Option<DifeqState>,             // serde skip
      pub cancel_requested: Arc<AtomicBool>,           // serde default+skip; PHASE 29 DOES NOT TOUCH
      // … remaining v2.2 fields …
      pub alpha_reg: String,                           // v1.x existing — used for FUNCTION NAME? label staging
      pub entry_buf: String,                           // v1.x existing — submit_modal flushes this
      pub is_running: bool,                            // v1.x existing — interactive vs run_loop gate
  }

From hp41-core/src/ops/math1/matrix.rs:323-331 (Phase 28 shipped — analog for op_solve modification):
  pub fn op_matrix_workflow(state: &mut CalcState) -> Result<(), HpError> {
      state.matrix_active_reg = Some(DEFAULT_MATRIX_BASE_REG);
      state.modal_program = Some(ModalProgram::Matrix(MatrixInputStep::OrderPrompt));
      state.modal_prompt = Some("ORDER=?".to_string());
      apply_lift_effect(state, LiftEffect::Neutral);
      Ok(())
  }

From hp41-core/src/ops/math1/solve.rs:117-190 (Phase 28 shipped — stub designed for Phase 29 completion):
  pub fn op_solve(_state: &mut CalcState) -> Result<(), HpError> {
      // Phase 28 stub — Phase 29 fills the interactive branch per the inline
      // cross-reference comment at line 188.
      Err(HpError::InvalidOp)
  }
  // Comment at line 188: "// CROSS-REFERENCE: see Plan 28-07 op_integ_run_loop for the
  // symmetric pattern. Phase 29 / CLI-07 wires the full FunctionNamePrompt/Guess1Prompt/
  // Guess2Prompt modal flow that stages these into the same registers before calling run_loop."

From hp41-core/src/ops/program.rs:67-87 (v2.1+v3.0 shipped — pattern to mirror):
  pub fn op_xeq(state: &mut CalcState, label: &str) -> Result<(), HpError> {
      if !state.is_running {
          if let Some(card_op) = builtin_card_op(label) {
              return crate::ops::dispatch(state, card_op);
          }
          if let Some(xrom_op) = crate::ops::math1::xrom::xrom_resolve(label, state.xrom_modules) {
              return crate::ops::dispatch(state, xrom_op);
          }
          return Err(HpError::InvalidOp);
      }
      Err(HpError::InvalidOp)
  }

From hp41-core/src/ops/mod.rs:820 (v1.x shipped):
  pub fn flush_entry_buf(state: &mut CalcState) -> Result<(), HpError>;

From hp41-cli/src/app.rs (v2.2 shipped — sites to extend or update per plan 01):
  Line 113:    XeqByName(String),                         // tuple variant — widen in this plan
  Line 309:    Some(PendingInput::XeqByName(_)) | Some(PendingInput::ClpLabel(_))  // or-pattern in '?' handler
  Line 327:    if self.pending_input.is_some() { … return; }  // D-07 — Phase 29 interceptors go BELOW this
  Line 438-451: if self.shift_armed { … return; }              // §7.2 — Phase 29 Esc goes BELOW this
  Line 638:    if key.code == KeyCode::F(5) { /* run_program("A") */ }  // Phase 29 R/S interceptor goes IMMEDIATELY BEFORE this
  Line 1072:   Some(PendingInput::XeqByName(acc)) => { self.handle_xeq_by_name(key, acc); }
  Line 1407:   fn handle_xeq_by_name(&mut self, key: KeyEvent, acc: String)  // widen signature: add mode
  Line 1412-1430: Enter arm — branch on mode
  Line 1640:   fn call_dispatch(&mut self, op: Op) { … }       // Phase 29 inserts trailing maybe_auto_open call
  Line 1651:   fn call_dispatch_and_drain(...) { … }            // same trailing call

From hp41-cli/src/keys.rs:319 (v2.1 shipped):
  app.pending_input = Some(PendingInput::XeqByName(String::new()));  // f-N opener — update to struct form, Normal mode

From hp41-cli/src/ui.rs:225-258 (v2.1+v2.2 shipped):
  fn render_status(app: &App, frame: &mut Frame, area: Rect)              // line 225 — caller of pending_prompt
  pub fn pending_prompt(pending: &crate::app::PendingInput) -> String      // line 258 — widen signature
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test stubs + hp41-core additive public surface (submit_modal, cancel_modal, submit_modal_with_label, requires_alpha_label, per-program submit_step + op_solve/op_integ/op_difeq interactive branch)</name>
  <files>hp41-core/src/ops/math1/mod.rs, hp41-core/src/ops/math1/modal.rs, hp41-core/src/ops/math1/solve.rs, hp41-core/src/ops/math1/integ.rs, hp41-core/src/ops/math1/difeq.rs, hp41-core/src/ops/math1/matrix.rs, hp41-core/src/ops/math1/poly.rs, hp41-core/src/ops/math1/four.rs, hp41-core/src/ops/math1/trans.rs, hp41-cli/tests/phase29_modal_flow.rs, hp41-cli/tests/phase29_pending_prompt_modal.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §3.4 (submit_modal shape), §3.5 (cancel_modal shape), §3.7 (requires_alpha_label), §7.4 (Open Q1 — op_solve interactive arm), §7.8 (recursive auto-open hook bound), §8 Open Q2-Q3 (per-program submit_step and submit_modal_with_label API shape)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "hp41-core/src/ops/math1/modal.rs::requires_alpha_label" + "hp41-core/src/ops/math1/mod.rs::submit_modal, cancel_modal, submit_modal_with_label" + "hp41-core/src/ops/math1/{solve,integ,difeq}.rs::op_solve/op_integ/op_difeq (Open Q1)" sections
    - .planning/phases/29-cli-integration/29-CONTEXT.md decisions D-29.5, D-29.6, D-29.7, D-29.8, D-29.9; <domain> frozen-source caveat
    - hp41-core/src/ops/math1/modal.rs (entire file) — verify exact `*InputStep::FunctionNamePrompt` variant names
    - hp41-core/src/ops/math1/solve.rs:117-200 — current op_solve stub + inline cross-reference comment at line 188
    - hp41-core/src/ops/math1/integ.rs:150 — current op_integ stub
    - hp41-core/src/ops/math1/difeq.rs:126 — current op_difeq stub
    - hp41-core/src/ops/math1/matrix.rs:323-331 — op_matrix_workflow shape (modal-open analog for op_solve/integ/difeq fixes)
    - hp41-core/src/ops/program.rs:67-87 — op_xeq's `if !state.is_running` pattern (literal mirror target)
    - hp41-core/src/ops/mod.rs:820 — flush_entry_buf signature
    - hp41-cli/tests/phase25_xeq_by_name.rs:32-87 — test scaffolding (App harness, key/raw_key helpers, make_app)
  </read_first>
  <behavior>
    Test file `phase29_modal_flow.rs` (5 tests covering CLI-05 modal-routing):
    - `matrix_workflow_order_prompt_advances_on_r_s`: dispatch `Op::MatrixWorkflow` → assert modal opens at OrderPrompt with prompt "ORDER=?" → push '3' through entry_buf → press F5 (R/S) → assert modal advanced to `Matrix(ElementPrompt(0,0))` and prompt is "A1,1=?"
    - `solve_workflow_auto_opens_collect_for_modal`: dispatch `Op::Solve` → assert modal opens at `Solve(FunctionNamePrompt)` with prompt "FUNCTION NAME?" → assert `app.pending_input == Some(XeqByName { acc: "", mode: CollectForModal })` after `call_dispatch` returns (auto-open hook fired)
    - `solve_workflow_label_submission_advances_to_guess1`: with Solve modal in CollectForModal mode → simulate typing 'F' + ENTER → assert `state.alpha_reg == "F"` AND `state.modal_program == Some(Solve(SolveInputStep::Guess1Prompt))` AND `state.modal_prompt == Some("GUESS 1=?".to_string())` AND `app.pending_input == None`
    - `esc_cancels_open_modal`: open Matrix modal → press Esc → assert `modal_program.is_none() && modal_prompt.is_none() && app.message == Some("Cancelled".to_string())`; stack untouched
    - `esc_shift_armed_takes_precedence_over_modal_cancel`: open Matrix modal → set `app.shift_armed = true` → press Esc → assert `shift_armed == false && modal_program.is_some()` (modal STILL open — shift-Esc consumed the keystroke)

    Test file `phase29_pending_prompt_modal.rs` (3 tests covering widened signature):
    - `pending_prompt_renders_modal_prompt_when_no_pending`: `pending_prompt(None, Some("ORDER=?"))` returns `"ORDER=?"`
    - `pending_prompt_renders_pending_when_no_modal`: `pending_prompt(Some(&XeqByName { acc: "F".to_string(), mode: Normal }), None)` returns the existing `"XEQ \"F\"_"` shape
    - `pending_prompt_modal_wins_when_both_active`: `pending_prompt(Some(&XeqByName { acc: "F", mode: CollectForModal }), Some("FUNCTION NAME?"))` — output contains "FUNCTION NAME"

    Tests COMPILE but FAIL initially (test-stubs first; hp41-core surface incomplete; widened sig not yet active). Steps 2–7 below close the gaps in order.

    Behaviors guaranteed by hp41-core additions:
    - `submit_modal(state)`: calls `flush_entry_buf` first; matches on `state.modal_program.clone()`; dispatches to per-program `submit_step`. Returns `Err(HpError::InvalidOp)` if `modal_program.is_none()`.
    - `cancel_modal(state)`: clears `modal_program`, `modal_prompt`, `entry_buf`; stack untouched; no error path.
    - `submit_modal_with_label(state, label)`: trims+uppercases label; writes to `state.alpha_reg`; matches on `modal_program` and dispatches to per-program `submit_label_step` (only for the 3 FunctionNamePrompt variants).
    - `ModalProgram::requires_alpha_label()`: returns true ONLY for `Integ(FunctionNamePrompt)`, `Solve(FunctionNamePrompt)`, `Difeq(FunctionNamePrompt)`.
    - `op_solve` / `op_integ` / `op_difeq`: if `!state.is_running` → open modal at FunctionNamePrompt and return Ok(); else → fall through to existing run_loop arm.
    - Per-program `submit_step`: advances `modal_program` to next step + sets `modal_prompt` accordingly. For Matrix(OrderPrompt) reading X as the order; for Solve(Guess1Prompt) reading X as the first guess; etc. Each program's transitions follow OM-cited prompt sequences from Phase 28 already in `modal.rs`.
    - Per-program `submit_label_step`: reads `alpha_reg` (already staged by `submit_modal_with_label`), advances to next step (e.g. Solve FunctionNamePrompt → Guess1Prompt).
  </behavior>
  <action>
    1. **Create Wave-0 test stubs FIRST** (RED phase per TDD):
       - `hp41-cli/tests/phase29_modal_flow.rs`: copy the test scaffolding pattern from `phase25_xeq_by_name.rs:32-57` (header, `#![allow(clippy::unwrap_used)]`, `key`, `raw_key`, `make_app` helpers — adjust the state_path filename to `phase29-modal-flow-test-state.json`). Implement the 5 tests listed in `<behavior>`. Each test:
         - Uses `make_app()` then dispatches an `Op` via `app.call_dispatch(op)`
         - Asserts on `app.state.modal_program`, `app.state.modal_prompt`, `app.state.alpha_reg`, `app.pending_input`, `app.message` as appropriate
         - For R/S simulation: `app.handle_key(raw_key(KeyCode::F(5)));`
         - For Esc simulation: `app.handle_key(raw_key(KeyCode::Esc));`
         - For digit entry: push chars into `app.state.entry_buf` directly (NOT via key events — bypasses the modal interceptor) OR via `app.handle_key(key('3'))` (lets the existing digit-handler push into entry_buf, then R/S submits)
         - Add `// Catches: <bug class>` doc comment per D-27.1 — e.g. `// Catches: R/S not flushing entry_buf before submit_modal` or `// Catches: cancel_modal leaving modal_prompt set`
       - `hp41-cli/tests/phase29_pending_prompt_modal.rs`: implement the 3 tests listed in `<behavior>` using the widened `pending_prompt(pending: Option<&PendingInput>, modal_prompt: Option<&str>) -> String` signature. Each test calls `pending_prompt(...)` directly and asserts on the returned `String`. Add `// Catches: pending_prompt regression` comments.

    2. **Add `requires_alpha_label()` method** to `ModalProgram` in `hp41-core/src/ops/math1/modal.rs`:

       Per RESEARCH §3.7 / 29-PATTERNS.md — positive-form `matches!` macro returning true ONLY for the three FunctionNamePrompt variants. The exact step names are `IntegInputStep::FunctionNamePrompt`, `SolveInputStep::FunctionNamePrompt`, `DifeqInputStep::FunctionNamePrompt`. Use a single `matches!(self, ...)` expression. The method docs comment includes the D-29.7 / D-29.9 / D-25.6 references.

    3. **Add per-program `submit_step` and (for the 3 callback programs) `submit_label_step` free functions** to each `hp41-core/src/ops/math1/<program>.rs` file. These are NEW additive `pub fn` items per RESEARCH §8 Open Q2:

       - `matrix.rs`: `pub fn submit_step(state: &mut CalcState, step: MatrixInputStep) -> Result<(), HpError>` — match on step; advance OrderPrompt → ElementPrompt(0,0) reading X as N; advance ElementPrompt(i,j) → ElementPrompt(i,j+1) or next-col or Ready; write `modal_prompt` accordingly. Reuse existing matrix helpers from `op_matrix_workflow` for register addressing.
       - `solve.rs`: `pub fn submit_step(state, step: SolveInputStep)` — Guess1Prompt → reads X into R00, advance to Guess2Prompt with "GUESS 2=?"; Guess2Prompt → reads X into R01, advance to Ready (set `state.is_running = true` + invoke the existing op_solve_run_loop path); etc. `pub fn submit_label_step(state, label: &str)` — only valid for FunctionNamePrompt; advances to Guess1Prompt with "GUESS 1=?".
       - `integ.rs`: same shape — `submit_step` for the numeric steps; `submit_label_step` for FunctionNamePrompt → next-step.
       - `difeq.rs`: same shape — submit_step + submit_label_step.
       - `poly.rs`, `four.rs`, `trans.rs`: submit_step ONLY (no FunctionNamePrompt variants — verified by `modal.rs` inspection).

       Each `submit_step` MAY also dispatch the final computation (e.g. matrix DET, polynomial root-finding) when its last step completes; or it may set a flag for subsequent `Op::*` dispatch. Planner inspects existing run_loop callers and matches the convention.

       **NO existing function body modified beyond adding new free functions** per frozen-source policy. Each new function carries a `// Phase 29 / CLI-05 additive public surface — D-29.5` doc comment.

    4. **Modify `op_solve` / `op_integ` / `op_difeq` to add the interactive branch** per RESEARCH §7.4 / Open Q1 Option A:

       For each function — `solve.rs:117`, `integ.rs:150`, `difeq.rs:126` — replace the current stub with:
       - Top of function: `if !state.is_running { state.modal_program = Some(ModalProgram::<Variant>(<VariantStep>InputStep::FunctionNamePrompt)); state.modal_prompt = Some("FUNCTION NAME?".to_string()); return Ok(()); }`
       - Existing `Err(HpError::InvalidOp)` stays as the run_loop arm (programmatic XEQ inside running program still rejected — the actual work is in `op_solve_run_loop` at solve.rs:150+).
       - Function parameter renamed from `_state` to `state` (was underscore-prefixed because unused; now actively mutates).
       - Add a one-line `// Phase 29 / CLI-07 — additive completion of Phase 28 stub per the inline cross-reference comment` doc comment above the new `if` branch.

       This is the documented exception to the frozen-source policy. The Phase 28 inline comment at `solve.rs:188` explicitly cross-references Phase 29 / CLI-07 as the wiring site; RESEARCH §7.4 Option A justifies it as "the additive-public-surface clause covers this — the bodies were stubs designed to be filled in Phase 29". Plan-checker will examine this carefully; this is the agreed Option A per CONTEXT.md Decisions section.

    5. **Add the three new free functions** to `hp41-core/src/ops/math1/mod.rs`:

       - `pub use modal::ModalProgram;` (re-export — verify it's not already present)
       - `pub fn submit_modal(state: &mut CalcState) -> Result<(), HpError>`: per RESEARCH §3.4 shape. First call `crate::ops::flush_entry_buf(state)?`. Then `let Some(modal) = state.modal_program.clone() else { return Err(HpError::InvalidOp); };`. Match on `modal` — each arm dispatches to the corresponding `<program>::submit_step(state, step)`. NO `_ =>` arm — match is exhaustive over the 7 `ModalProgram` variants.
       - `pub fn cancel_modal(state: &mut CalcState)`: per RESEARCH §3.5 shape. Clear `modal_program`, `modal_prompt`, `entry_buf`. Leave stack and matrix_dim/matrix_active_reg untouched. No error path. No return value.
       - `pub fn submit_modal_with_label(state: &mut CalcState, label: &str) -> Result<(), HpError>`: per RESEARCH §3.4 shape. Compute `let upper = label.trim().to_ascii_uppercase();`. Write `state.alpha_reg = upper.clone();`. Match `state.modal_program.clone()` — call the matching `submit_label_step(state, &upper)` for `Integ(FunctionNamePrompt)` / `Solve(FunctionNamePrompt)` / `Difeq(FunctionNamePrompt)`; ANY other variant returns `Err(HpError::InvalidOp)`. Add doc comment "unreachable in well-formed flow per D-29.9 gate".

       All three functions carry `// Phase 29 / CLI-05 additive public surface — D-29.5 / D-29.6 / D-29.7` doc comments.

    6. **Compile gate**: run `cargo check -p hp41-core` — must succeed with no warnings beyond pre-existing. The exhaustive match in `submit_modal` and the positive-form `matches!` in `requires_alpha_label` are compile-time-validated.

    7. **Unit-test the hp41-core additions** by running `cargo test -p hp41-core` (existing Phase 28 tests must continue to pass; this plan does NOT modify those test files). Coverage gate (≥ 95% lines on hp41-core) is sensitive — verify post-task via `just coverage` if coverage shows a drop.

    Per D-29.5, D-29.6, D-29.7, D-29.9, RESEARCH §3.4, §3.5, §3.7, §7.4 Option A, §8 Open Q2/Q3. The hp41-core surface is purely additive (4 new public items + 3 stub-completion edits). CLI ↔ GUI parity (D-25.6) reduced to: both frontends call the SAME functions. SC-4 trivially holds — all math1 logic stays in `hp41-core/src/ops/math1/`.
  </action>
  <verify>
    <automated>cargo check -p hp41-core && cargo test -p hp41-core --tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-core` exits 0
    - `cargo test -p hp41-core --tests` exits 0 (all Phase 28 tests still green)
    - Source assertion: `grep -c "pub fn submit_modal" hp41-core/src/ops/math1/mod.rs` outputs `1`
    - Source assertion: `grep -c "pub fn cancel_modal" hp41-core/src/ops/math1/mod.rs` outputs `1`
    - Source assertion: `grep -c "pub fn submit_modal_with_label" hp41-core/src/ops/math1/mod.rs` outputs `1`
    - Source assertion: `grep -c "pub fn requires_alpha_label" hp41-core/src/ops/math1/modal.rs` outputs `1`
    - Source assertion: `grep -c "pub fn submit_step" hp41-core/src/ops/math1/matrix.rs hp41-core/src/ops/math1/solve.rs hp41-core/src/ops/math1/integ.rs hp41-core/src/ops/math1/difeq.rs hp41-core/src/ops/math1/poly.rs hp41-core/src/ops/math1/four.rs hp41-core/src/ops/math1/trans.rs` outputs at least `7`
    - Source assertion: `grep -c "pub fn submit_label_step" hp41-core/src/ops/math1/solve.rs hp41-core/src/ops/math1/integ.rs hp41-core/src/ops/math1/difeq.rs` outputs `3`
    - Source assertion: `grep -c "if !state.is_running" hp41-core/src/ops/math1/solve.rs hp41-core/src/ops/math1/integ.rs hp41-core/src/ops/math1/difeq.rs` outputs `3` (one interactive-branch in each function)
    - Source assertion: `grep -c "ModalProgram::Solve(SolveInputStep::FunctionNamePrompt)" hp41-core/src/ops/math1/solve.rs` outputs at least `1`
    - Source assertion: `grep -c "FUNCTION NAME?" hp41-core/src/ops/math1/solve.rs hp41-core/src/ops/math1/integ.rs hp41-core/src/ops/math1/difeq.rs` outputs at least `3`
    - Source assertion: no `_ =>` catch-all in `submit_modal`: `grep -A 12 "pub fn submit_modal\\(state" hp41-core/src/ops/math1/mod.rs | grep -c "_ =>"` outputs `0` (CollectForModal's `_ =>` for unreachable arm is in `submit_modal_with_label`, not `submit_modal`)
    - Test files exist: `test -f hp41-cli/tests/phase29_modal_flow.rs && test -f hp41-cli/tests/phase29_pending_prompt_modal.rs` succeeds
    - `#![deny(clippy::unwrap_used)]` still holds in hp41-core: `cargo clippy -p hp41-core -- -D warnings` exits 0 (no `.unwrap()` introduced in production code)
  </acceptance_criteria>
  <done>hp41-core additive public surface complete: `submit_modal`, `cancel_modal`, `submit_modal_with_label`, `requires_alpha_label` shipped; 7 per-program `submit_step` + 3 `submit_label_step` helpers shipped; `op_solve` / `op_integ` / `op_difeq` interactive branches added per RESEARCH §7.4 Option A; Wave-0 test files exist (compile but fail until CLI wiring lands); existing Phase 28 tests green; `#![deny(clippy::unwrap_used)]` preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hp41-cli wiring — PendingInput::XeqByName struct-variant migration, pending_prompt widening, R/S + Esc interception, post-dispatch auto-open hook</name>
  <files>hp41-cli/src/app.rs, hp41-cli/src/ui.rs, hp41-cli/src/keys.rs, hp41-cli/tests/phase25_pending_input.rs, hp41-cli/tests/phase25_xeq_by_name.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §3.3 (pending_prompt widening Option A), §3.4 (R/S interception), §3.5 (Esc interception), §3.6 (XeqByName mode extension), §3.7 (auto-open hook), §4 (PendingInput match-arm inventory), §7.1 (D-07 ordering), §7.2 (Esc precedence), §7.3 (modal_program vs pending_input render precedence), §7.8 (recursive auto-open bound)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "PendingInput::XeqByName" + "handle_key (R/S + Esc interception + post-dispatch hook)" + "pending_prompt" sections
    - .planning/phases/29-cli-integration/29-CONTEXT.md decisions D-29.3, D-29.4, D-29.5, D-29.6, D-29.7, D-29.8, D-29.9; <code_context> integration points
    - hp41-cli/src/app.rs (entire file) — locations: 113 (enum decl), 309 (or-pattern guard), 327 (D-07 pending_input route), 438-451 (shift_armed Esc), 638 (F5 R/S handler — Phase 29 inserts BEFORE this), 1072 (handle_pending_input dispatch arm), 1407-1447 (handle_xeq_by_name body), 1640 (call_dispatch), 1651 (call_dispatch_and_drain)
    - hp41-cli/src/ui.rs:225-331 — render_status + pending_prompt
    - hp41-cli/src/keys.rs:319 — f-N opener (single XeqByName constructor in keys.rs)
    - hp41-cli/tests/phase25_pending_input.rs:74,95,216,442,447,477 — XeqByName match-arm sites
    - hp41-cli/tests/phase25_xeq_by_name.rs:66,72,249,255 — XeqByName match-arm sites
    - hp41-cli/tests/key_coverage.rs:65,75,191 — LOCAL `KeyPath::XeqByName(String)` enum (UNRELATED — no change needed)
  </read_first>
  <behavior>
    - After this task, `cargo check -p hp41-cli --tests` is clean — all 10 production + 10 test pattern-match sites updated to struct-variant form.
    - `pending_prompt(None, Some("ORDER=?"))` returns `"ORDER=?"`; `pending_prompt(Some(&XeqByName{acc:"F",mode:Normal}), None)` returns `"XEQ \"F\"_"` (preserved v2.2 behavior).
    - The 5 `phase29_modal_flow.rs` tests from task 1 turn from RED to GREEN.
    - The 3 `phase29_pending_prompt_modal.rs` tests from task 1 turn from RED to GREEN.
    - All existing Phase 25 tests (`phase25_pending_input.rs`, `phase25_xeq_by_name.rs`) continue to pass after the struct-variant ripple.
    - R/S keystroke in MATRIX(OrderPrompt) with `entry_buf="3"` advances to ElementPrompt(0,0); Esc cancels cleanly; shift_armed Esc precedence preserved (§7.2 two-step convention).
    - Auto-open hook is depth-bounded: only `FunctionNamePrompt` variants trigger it; no chain of FunctionName steps (RESEARCH §7.8 verified).
  </behavior>
  <action>
    1. **Add `XeqByNameMode` enum + migrate `PendingInput::XeqByName` to struct variant** in `hp41-cli/src/app.rs`:

       - Above `PendingInput` enum, add:
         `pub enum XeqByNameMode { Normal, CollectForModal }` with `#[derive(Debug, Clone, Copy, PartialEq, Eq)]`. Doc-comment per RESEARCH §3.6.
       - In `PendingInput` enum at line 113, change `XeqByName(String)` to `XeqByName { acc: String, mode: XeqByNameMode }`.

    2. **Ripple struct-variant pattern across 10 production + 10 test sites** per RESEARCH §4. The Rust compiler enforces this — start with `cargo check -p hp41-cli --tests` and fix errors iteratively until clean. The 20 sites enumerated:

       Production:
       - `app.rs:113`     enum decl                                       [step 1 above]
       - `app.rs:309`     or-pattern guard `Some(PendingInput::XeqByName(_))` → `Some(PendingInput::XeqByName { .. })`
       - `app.rs:1072`    dispatch arm — destructure `{ acc, mode }` and pass both to `handle_xeq_by_name`
       - `app.rs:1407`    `handle_xeq_by_name` signature → `fn handle_xeq_by_name(&mut self, key: KeyEvent, acc: String, mode: XeqByNameMode)`
       - `app.rs:1412-1430` Enter arm — branch on `mode`: `Normal` → existing `xeq_by_name_local_resolve(&acc, self.state.xrom_modules)` flow (note: signature already widened by plan 29-01); `CollectForModal` → call `hp41_core::ops::math1::submit_modal_with_label(&mut self.state, &acc)`, set `self.message` from `Ok(()) => None, Err(e) => Some(format!("{e}"))`
       - `app.rs:1434, 1441, 1444` — three Backspace/Char/fallback re-stores: each must construct `PendingInput::XeqByName { acc: new_acc, mode }` passing mode through
       - `keys.rs:319` f-N opener: `Some(PendingInput::XeqByName(String::new()))` → `Some(PendingInput::XeqByName { acc: String::new(), mode: XeqByNameMode::Normal })`
       - `ui.rs:329` pending_prompt arm: replace single arm with TWO explicit arms (per RESEARCH §3.6 + FN-CLI-04):
         - `PendingInput::XeqByName { acc, mode: XeqByNameMode::Normal } => format!("XEQ \"{acc}\"_")`
         - `PendingInput::XeqByName { acc, mode: XeqByNameMode::CollectForModal } => format!("NAME: {acc}_")` (planner picks readable form; the function falls through to modal_prompt-priority in the wrapper layer per step 3 below)

       Test:
       - `phase25_pending_input.rs:74,95,216,442,447,477` (6 sites): every `PendingInput::XeqByName(...)` becomes `PendingInput::XeqByName { acc: ..., mode: XeqByNameMode::Normal }`
       - `phase25_xeq_by_name.rs:66,72,249,255` (4 sites): same

       SKIP: `key_coverage.rs:65,75,191` — these reference a LOCAL `KeyPath::XeqByName(String)` enum in the test file, NOT `PendingInput::XeqByName`. Verify via Read before editing — do NOT touch this file unless the planner picks the mirror-for-symmetry option (recommend skipping to keep blast radius small).

    3. **Widen `pending_prompt` signature in `hp41-cli/src/ui.rs`** per D-29.3 + RESEARCH §3.3 Option A:

       - Change signature line 258 from `pub fn pending_prompt(pending: &crate::app::PendingInput) -> String` to `pub fn pending_prompt(pending: Option<&crate::app::PendingInput>, modal_prompt: Option<&str>) -> String`
       - At function top, implement the precedence rules (RESEARCH §3.3 + §7.3):
         - If `pending.is_none() && modal_prompt.is_some()` → return `modal_prompt.unwrap().to_string()` (early return)
         - If `pending.is_some() && modal_prompt.is_some()` AND the pending is `XeqByName { mode: CollectForModal, .. }` → return `modal_prompt.unwrap().to_string()` (modal wins for label-collection step per §7.3)
         - Otherwise, match on `pending` (existing 18-arm exhaustive match wrapped in `match pending { None => String::new(), Some(p) => match p { ... existing arms ... } }`). All 18 arms preserved; XeqByName grows from 1 arm to 2 arms per step 2 above.
       - Update the single production call site at `ui.rs:225-234` (`render_status`):
         - Change the `if let Some(ref pending) = app.pending_input` block to: `if app.pending_input.is_some() || app.state.modal_prompt.is_some() { pending_prompt(app.pending_input.as_ref(), app.state.modal_prompt.as_deref()) } else if app.state.alpha_mode { ... } else { app.message.as_deref().unwrap_or("Ready").to_string() }`
       - Update all test call sites in `phase25_pending_input.rs:103+` from `pending_prompt(&p)` to `pending_prompt(Some(&p), None)`.

       FN-CLI-04 invariant preserved — no `_ =>` arm; exhaustive over PendingInput AND XeqByNameMode.

    4. **Insert R/S interception** in `hp41-cli/src/app.rs::handle_key` per D-29.5 + RESEARCH §3.4 + §7.1:

       Location: IMMEDIATELY BEFORE the existing F5 handler at line 638 (which runs `run_program("A")`). The new handler MUST be below the `pending_input.is_some()` route at line 327 (D-07 invariant) and below the `shift_armed` block at line 438-451 (§7.2 precedence). Verify ordering by reading the file before inserting.

       New block:
       - Guard: `if key.code == KeyCode::F(5) && self.state.modal_program.is_some()` (the explicit `pending_input.is_none()` check is implicit because the pending_input route returns at line 327; add the explicit guard if the planner prefers self-documenting safety per §7.1)
       - Call `hp41_core::ops::math1::submit_modal(&mut self.state)`
       - On `Ok(())`: clear `self.message`; call `self.drain_and_show_print_output(None)` per RESEARCH §7.7 (modal advancement may push to print_buffer)
       - On `Err(e)`: `self.message = Some(format!("{e}"))`
       - `return;`

       Add `// Phase 29 (D-29.5): R/S submits modal numeric input` doc comment.

    5. **Insert Esc interception** in `hp41-cli/src/app.rs::handle_key` per D-29.6 + RESEARCH §3.5 + §7.2:

       Location: same block as the R/S interception (immediately before F5 line 638), AFTER the shift_armed block. Guard:
       - `if key.code == KeyCode::Esc && self.state.modal_program.is_some() && self.pending_input.is_none()` (the `&& self.pending_input.is_none()` defensive guard prevents a future code-shape change from breaking the D-07 invariant — even though the pending_input.is_some() route already returned at line 327)
       - Call `hp41_core::ops::math1::cancel_modal(&mut self.state)`
       - Set `self.message = Some("Cancelled".to_string())`
       - `return;`

       Add `// Phase 29 (D-29.6): Esc cancels open math1 modal (no pending_input active).` doc comment.

       **Verify shift_armed Esc precedence still works** — the existing `if self.shift_armed { if key.code == KeyCode::Esc { self.shift_armed = false; return; } ... }` block at line 438-451 fires FIRST. Phase 29's new Esc block only fires when `!self.shift_armed`. Two-step convention preserved.

    6. **Insert auto-open hook in `call_dispatch` AND `call_dispatch_and_drain`** per D-29.9 + RESEARCH §3.7:

       - Add new private method `maybe_auto_open_collect_for_modal(&mut self)` to App. Body per RESEARCH §3.7 / 29-PATTERNS.md:
         - `if self.pending_input.is_some() { return; }`
         - `let Some(ref mp) = self.state.modal_program else { return; };`
         - `if mp.requires_alpha_label() { self.pending_input = Some(PendingInput::XeqByName { acc: String::new(), mode: XeqByNameMode::CollectForModal }); }`

       - At the tail of `call_dispatch` (line ~1640 — after the existing `match dispatch(...) { ... }` block), add a single line: `self.maybe_auto_open_collect_for_modal();`
       - Same for `call_dispatch_and_drain` (line ~1651) — single-line append.

       Add `// Phase 29 (D-29.9): post-dispatch auto-open CollectForModal modal when modal needs alpha label.` doc comment above the new method.

       **Recursive-bound verification per RESEARCH §7.8**: `submit_modal_with_label` advances from FunctionNamePrompt to a numeric prompt (Guess1Prompt / StepSizePrompt / etc.) — `requires_alpha_label()` returns false for those. So the auto-open fires at most ONCE per `Op` dispatch. Bounded.

    7. **Update `docs/verifying-math-pac-1.md §9`** per CONTEXT.md `<specifics>` bullet + RESEARCH §8 Open Q8:

       - Read the file to find §9 (the modal-flow verification table)
       - Update the CLI column for MATRIX / SOLVE / POLY / INTG / DIFEQ / FOUR / TRANS rows: mark each as ✅ (available on CLI)
       - GUI column remains ❌ until Phase 31 ships
       - Low-effort doc edit — single Edit pass per row

    8. **Run the Wave-0 tests**: `cargo test -p hp41-cli --test phase29_modal_flow && cargo test -p hp41-cli --test phase29_pending_prompt_modal`. All 8 tests must be green.

    9. **Run the full test suite**: `cargo test -p hp41-cli --tests`. All existing tests + new Phase 29 tests green. Per RESEARCH §6 sampling rate: per-task quick run is `~25 seconds`.

    Per CLI-05, D-29.3..D-29.9, D-07 (pending_input route above all modal interceptors), §7.2 (shift_armed Esc precedence), §7.3 (modal wins for CollectForModal render), §7.8 (recursive-auto-open bound = 1), FN-CLI-04 (no `_ =>` catch-all in `pending_prompt`'s match over PendingInput AND XeqByNameMode), D-25.6 (CLI ↔ GUI parity — same hp41-core surface).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-cli --tests` exits 0 (struct-variant ripple complete; 20 sites updated)
    - `cargo test -p hp41-cli --test phase29_modal_flow` reports `5 passed; 0 failed`
    - `cargo test -p hp41-cli --test phase29_pending_prompt_modal` reports `3 passed; 0 failed`
    - `cargo test -p hp41-cli --tests` exits 0 (full suite — including unchanged Phase 25 tests)
    - Source assertion: `grep -c "pub enum XeqByNameMode" hp41-cli/src/app.rs` outputs `1`
    - Source assertion: `grep -c "XeqByName { acc: String, mode: XeqByNameMode }" hp41-cli/src/app.rs` outputs `1` (enum decl)
    - Source assertion: `grep -c "XeqByNameMode::Normal" hp41-cli/src/app.rs hp41-cli/src/keys.rs hp41-cli/src/ui.rs` outputs at least `4` (constructors + match arms)
    - Source assertion: `grep -c "XeqByNameMode::CollectForModal" hp41-cli/src/app.rs hp41-cli/src/ui.rs` outputs at least `3`
    - Source assertion: `grep -c "hp41_core::ops::math1::submit_modal" hp41-cli/src/app.rs` outputs at least `1`
    - Source assertion: `grep -c "hp41_core::ops::math1::cancel_modal" hp41-cli/src/app.rs` outputs at least `1`
    - Source assertion: `grep -c "submit_modal_with_label" hp41-cli/src/app.rs` outputs at least `1`
    - Source assertion: `grep -c "maybe_auto_open_collect_for_modal" hp41-cli/src/app.rs` outputs at least `3` (1 method def + 2 call sites in call_dispatch + call_dispatch_and_drain)
    - Source assertion: `grep -c "fn pending_prompt(\\s*pending: Option<&crate::app::PendingInput>,\\s*modal_prompt: Option<&str>" hp41-cli/src/ui.rs` outputs `1` (widened signature)
    - Source assertion: `grep -c "key\\.code == KeyCode::F(5) && self\\.state\\.modal_program\\.is_some()" hp41-cli/src/app.rs` outputs `1` (R/S interceptor present)
    - Source assertion: `grep -c "key\\.code == KeyCode::Esc.*modal_program\\.is_some" hp41-cli/src/app.rs` outputs at least `1` (Esc interceptor present)
    - D-07 ordering check: the pending_input.is_some() route appears BEFORE the new R/S interceptor: verify via Read that line 327 (or its current location) returns before the new F5 block
    - §7.2 ordering check: the shift_armed Esc block appears BEFORE the new Esc interceptor: verify via Read
    - No `_ =>` arm in `pending_prompt`'s PendingInput match: `awk '/fn pending_prompt/,/^}/' hp41-cli/src/ui.rs | grep -c "^\\s*_ =>"` outputs `0`
    - `docs/verifying-math-pac-1.md §9` updated: `grep -c "MATRIX.*✅" docs/verifying-math-pac-1.md` outputs at least `1`
    - SC-4 grep still clean (Phase 29 does not touch hp41-gui/src-tauri/src/): `grep -rn "fn op_(add\\|sub\\|mul\\|div\\|sin\\|cos\\|tan\\|sto\\|rcl\\|flush_entry\\|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing
    - MSRV unchanged: `grep -c "rust-version = \"1.88\"" Cargo.toml` outputs at least `1`
  </acceptance_criteria>
  <done>PendingInput::XeqByName migrated to struct-variant with XeqByNameMode discriminator; 20 pattern-match sites updated; pending_prompt widened with modal_prompt rendering + CollectForModal precedence; R/S and Esc interceptors inserted with D-07 + §7.2 ordering preserved; call_dispatch and call_dispatch_and_drain post-dispatch hook added with bounded recursion; 5 phase29_modal_flow tests green; 3 phase29_pending_prompt_modal tests green; full hp41-cli suite green; SC-4 invariant preserved; MSRV unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard → CLI App | All Math Pac I modal interactions enter here; pending_input route (D-07) is the safety gate |
| CLI App → hp41-core::math1 | `submit_modal` / `cancel_modal` / `submit_modal_with_label` are the only entry points; hp41-core remains UI-agnostic |
| `entry_buf` → numeric value on X register | `flush_entry_buf` is the established v1.x pipeline; `submit_modal` reuses it |
| `state.alpha_reg` → user-program LBL lookup | `submit_modal_with_label` writes the label per HP-41 convention (trimmed + uppercased); existing `op_solve_run_loop` reads `state.alpha_reg.clone()` at solve.rs:190 |
| `state.modal_program` ↔ `pending_input::XeqByName{CollectForModal}` | Auto-open hook (D-29.9) is the bridge; depth bounded to 1 per RESEARCH §7.8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Spoofing | R/S keystroke routed to `submit_modal` while pending_input modal is open (D-07 violation) | mitigate | Defensive `&& self.pending_input.is_none()` guard on the R/S interceptor; pending_input route at line 327 already returns first; tested by `esc_shift_armed_takes_precedence_over_modal_cancel` and inversely by the f-prefix-then-Esc scenario |
| T-29-03-02 | Tampering | Esc inside f-armed modal accidentally cancels modal (shift_armed Esc precedence violation per §7.2) | mitigate | shift_armed block fires BEFORE the new Esc handler; test `esc_shift_armed_takes_precedence_over_modal_cancel` is the gate |
| T-29-03-03 | Denial of service | Infinite-recursion in `maybe_auto_open_collect_for_modal` if a FunctionName step advances to ANOTHER FunctionName step | mitigate | RESEARCH §7.8 verified: each program has exactly ONE FunctionNamePrompt; transitions to Guess1/StepSize/etc. — `requires_alpha_label() == false` after first advance. Bound = 1. Test `solve_workflow_label_submission_advances_to_guess1` confirms post-advance state is not a FunctionName step. |
| T-29-03-04 | Tampering | `op_solve` / `op_integ` / `op_difeq` interactive-branch modification breaks the run_loop path | mitigate | Existing `Err(HpError::InvalidOp)` body stays — only the `if !state.is_running { ... return Ok(()); }` branch is added on top. `cargo test -p hp41-core` re-validates the run_loop path. The change is structurally identical to `op_xeq` pattern at program.rs:67-87 (precedent). |
| T-29-03-05 | Tampering | Frozen-source policy violation if hp41-core math1 logic is modified beyond the stubs | mitigate | Source assertions in this plan's acceptance_criteria enforce only ADDITIVE changes (new pub fn / pub method items); diff inspection during /gsd:verify-work is the safety net. The 3 stub-completions are the documented exception per RESEARCH §7.4 Option A. |
| T-29-03-06 | Information disclosure | `state.alpha_reg` writing user LBL label (no PII) | accept | LBL labels are not secrets. |
| T-29-03-07 | Denial of service | Long-running `op_solve_run_loop` / `op_integ_run_loop` after CLI submits FunctionName + 2 numeric prompts → no UI cancellation in v3.0 Phase 29 | accept | Cancellation UI is Phase 31 / GUI-05. CLI users can Ctrl+C to exit. Documented in `docs/verifying-math-pac-1.md` future-work section. |
| T-29-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 29 — pure source edits. RESEARCH §"Environment Availability" confirms zero new runtime deps in hp41-core or hp41-cli. MSRV 1.88 unchanged. |
</threat_model>

<verification>
- `cargo test -p hp41-cli --tests` green (all Phase 29 + existing Phase 25 + earlier tests)
- `cargo test -p hp41-core --tests` green (Phase 28 tests unaffected by the 3 stub completions + new additive surface)
- `cargo clippy -p hp41-core -- -D warnings` clean (`#![deny(clippy::unwrap_used)]` preserved)
- `cargo check -p hp41-cli` clean (struct-variant ripple complete; no compile errors)
- `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` clean (D-25.6 parity — GUI continues to compile against the SAME hp41-core surface)
- `just ci` green at wave-end (full lint + test + coverage gate; coverage gate ≥ 95% on hp41-core — verify with `just coverage` since hp41-core gained new code)
- Manual: `cargo run -p hp41-cli`, type `XEQ "MATRIX"`, then `2 R/S 1 R/S 2 R/S 3 R/S 4 R/S`, then `XEQ "DET"`, X-register reads `-2.0` (per ROADMAP SC-5 + RESEARCH §6 manual verification)
- SC-4 invariant unchanged: `grep -rn "fn op_(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing
</verification>

<success_criteria>
- CLI-05 (whole-phase capstone):
  - Modal-prompt routing for MATRIX / SOLVE / POLY / INTG / DIFEQ / FOUR / TRANS workflows reaches end-to-end: dispatch → modal opens → user types digits → R/S submits → next step → final compute or completion
  - ALPHA-text prompts for INTG / SOLVE / DIFEQ `FUNCTION NAME?` integrate via auto-opened `XeqByName { CollectForModal }` modal; user types LBL name + ENTER → `submit_modal_with_label` advances; subsequent R/S submits numeric prompts
  - Status bar renders `state.modal_prompt` via widened `pending_prompt` (D-29.3); LCD continues to render normal X-register / entry_buf during modal (D-29.4 — Display14Seg untouched)
  - Esc cleanly cancels modal via `cancel_modal` (D-29.6); shift_armed Esc precedence preserved (two-step convention, §7.2)
  - D-07 invariant preserved: `pending_input` route remains ABOVE both new modal interceptors
  - D-25.6 CLI ↔ GUI parity reduced to: both frontends will call the SAME `submit_modal` / `cancel_modal` / `submit_modal_with_label` / `requires_alpha_label` functions in hp41-core. Phase 31 mirrors verbatim.
  - FN-CLI-04 exhaustive-match invariant preserved (no `_ =>` catch-all in `pending_prompt` over PendingInput AND XeqByNameMode)
- Frozen-source policy honored: 4 new public items in hp41-core/src/ops/math1/ + 7 per-program submit_step + 3 per-program submit_label_step + 3 stub-completions per RESEARCH §7.4 Option A (documented exception)
- Save-file backward compat preserved: no new `CalcState` fields in Phase 29; v1.x–v2.2 save files continue to load
- MSRV 1.88 unchanged; zero new runtime dependencies in hp41-core or hp41-cli
- `docs/verifying-math-pac-1.md §9` updated to mark CLI side ✅ for the 7 modal flows
</success_criteria>

<output>
Create `.planning/phases/29-cli-integration/29-03-SUMMARY.md` when done. Include:
- The full diff list of hp41-core additive items (4 new pub items in math1/mod.rs + 1 method in math1/modal.rs + 7 per-program submit_step + 3 per-program submit_label_step + 3 stub-completion branches in solve/integ/difeq.rs)
- Phase 29 capstone summary: every Math Pac I modal flow now reachable end-to-end via CLI; Phase 31 (GUI Integration) builds on the SAME hp41-core surface
- Hand-off note for Phase 30: `docs/hp41-math1-functions.json` was authored in 29-01 (pulled forward per D-29.1); Phase 30 / DOC-02 owns matrix regeneration via `scripts/docs-matrix` two-input extension
</output>