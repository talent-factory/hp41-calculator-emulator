---
phase: 29-cli-integration
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - hp41-cli/src/help_data.rs
  - hp41-cli/src/keys.rs
  - hp41-cli/tests/phase29_key_ref_includes_math1.rs
autonomous: true
requirements: [CLI-03, CLI-04]

must_haves:
  truths:
    - "`help_overlay_rows()` (`?` overlay source) returns rows that include the new Math Pac I categories (Math1 Hyperbolics, Math1 Matrix, etc.) AND the v2.2 categories"
    - "`key_ref_entries()` (right-panel discoverability listing) includes at least one row whose `key_path == \"XEQ \\\"SINH\\\"\"` and `display == \"SINH\"` AFTER migrating from `help_entries()` to `help_entries_all()`"
    - "`hp41-cli/src/prgm_display.rs::op_display_name` continues to compile with the same exhaustive match (no `_ =>` catch-all) — every Phase 28 Math Pac I `Op` variant has a display arm shipped in Phase 28; this plan VERIFIES the contract, no new arms added"
    - "`hp41-gui/src-tauri/src/prgm_display.rs::op_display_name` matches CLI's coverage (both `prgm_display.rs` copies stay in sync per the 4-exhaustive-match invariant) — verified via grep"
  artifacts:
    - path: "hp41-cli/src/help_data.rs"
      provides: "Migration of `help_overlay_rows()` from `help_entries()` to `help_entries_all()` (D-29.2)"
      contains: "let entries: Vec<&HelpEntry> = help_entries_all().collect();"
    - path: "hp41-cli/src/keys.rs"
      provides: "Migration of `key_ref_entries()` from `help_entries()` to `help_entries_all()` (D-29.2 / D-25.18)"
      contains: "for entry in crate::help_data::help_entries_all()"
    - path: "hp41-cli/tests/phase29_key_ref_includes_math1.rs"
      provides: "Wave-0 unit test asserting Math Pac I rows surface in right-panel discoverability listing (CLI-04 acceptance)"
      contains: "key_ref_entries_includes_math1_sinh; key_ref_entries_includes_math1_matrix"
  key_links:
    - from: "hp41-cli/src/keys.rs::key_ref_entries"
      to: "hp41-cli/src/help_data.rs::help_entries_all"
      via: "iteration over merged accessor"
      pattern: "help_entries_all\\(\\)"
    - from: "hp41-cli/src/help_data.rs::help_overlay_rows"
      to: "hp41-cli/src/help_data.rs::help_entries_all"
      via: "iteration over merged accessor"
      pattern: "help_entries_all\\(\\)"
---

<objective>
Migrate the two production consumers of `help_entries()` (`help_overlay_rows` and `key_ref_entries`) to the new `help_entries_all()` merged accessor from plan 29-01 (D-29.2). Add a focused Wave-0 test confirming Math Pac I rows surface in the right-panel discoverability listing per CLI-04 / D-25.18. Verify that CLI-03 (`op_display_name` arms for all Math Pac I `Op` variants) is already satisfied by the Phase 28 ship via a grep audit and a compile-time check — no new code beyond the migration + test. Both `prgm_display.rs` copies (hp41-cli + hp41-gui/src-tauri) are inspected to confirm parity.

Purpose: Closes CLI-03 (verification only — code shipped Phase 28) and CLI-04 (single-line migration per call site). Plan 29-02 unblocks the `?`-overlay and right-panel UI from showing Math Pac I content without code-level surgery on the rendering layer.

Output: 2 single-line migrations to `help_entries_all()` (in `help_data.rs::help_overlay_rows` and `keys.rs::key_ref_entries`); 1 new test file with 2-3 surgical assertions; 1 compile-time + grep audit confirming `op_display_name` exhaustive match covers all 47 Math Pac I `Op` variants in BOTH `prgm_display.rs` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cli-integration/29-CONTEXT.md
@.planning/phases/29-cli-integration/29-RESEARCH.md
@.planning/phases/29-cli-integration/29-PATTERNS.md
@.planning/phases/29-cli-integration/29-VALIDATION.md
@.planning/phases/29-cli-integration/29-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Plan 29-01 ships `help_entries_math1()` and `help_entries_all()`; this plan consumes them. -->

From hp41-cli/src/help_data.rs (after plan 29-01):
  pub fn help_entries() -> &'static [HelpEntry]                            // narrow — RETAINED for 130-target test
  pub fn help_entries_math1() -> &'static [HelpEntry]                      // narrow — Math1 pool only (47 entries)
  pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry>    // MERGED — the new single source of truth

From hp41-cli/src/keys.rs:390-405 (v2.2 shipped — migration target):
  pub fn key_ref_entries() -> Vec<(String, String)>
  // Currently: for entry in crate::help_data::help_entries() { … }
  // After: for entry in crate::help_data::help_entries_all() { … }

From hp41-cli/src/prgm_display.rs (Phase 28 shipped — DO NOT MODIFY; verify only):
  fn op_display_name(op: &Op) -> String
  // Exhaustive match over Op enum; ~40 Math Pac I arms (Sinh, MatrixWorkflow, Integ, Solve, Difeq, Four, etc.)
  // No `_ =>` catch-all — compile-time exhaustive.

From hp41-gui/src-tauri/src/prgm_display.rs (Phase 28 shipped — DO NOT MODIFY; verify only):
  fn op_display_name(op: &Op) -> String
  // Mirrors hp41-cli copy; D-25.6 parity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test phase29_key_ref_includes_math1.rs + migrate key_ref_entries() to help_entries_all()</name>
  <files>hp41-cli/tests/phase29_key_ref_includes_math1.rs, hp41-cli/src/keys.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §3.2 (migration of 3 call sites — table at lines 271–278)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "phase29_key_ref_includes_math1.rs" section (lines ~305–335) + "hp41-cli/src/keys.rs::xeq_by_name_local_resolve" sibling-fn notes
    - .planning/phases/29-cli-integration/29-CONTEXT.md decisions D-29.2, D-25.18
    - hp41-cli/src/keys.rs (lines 380–410 — `key_ref_entries` current body that filters by non-null `key_path`)
    - hp41-cli/tests/phase25_help_data.rs (lines 88–130 — established test idioms for HelpEntry iteration)
  </read_first>
  <behavior>
    - Test 1 (`key_ref_entries_includes_math1_sinh`): `key_ref_entries()` returns a Vec containing the tuple `("XEQ \"SINH\"", "SINH")` after migration to `help_entries_all()`.
    - Test 2 (`key_ref_entries_includes_math1_matrix`): same shape for MATRIX (`("XEQ \"MATRIX\"", "MATRIX")`) — confirms SC-4 of ROADMAP (Math Pac I in right-panel).
    - Test 3 (`key_ref_entries_preserves_v22_entries`): negative-regression — `key_ref_entries()` still contains v2.2 entries like `("+", "+")` (Add op) so the migration doesn't drop the existing pool.
  </behavior>
  <action>
    1. Create `hp41-cli/tests/phase29_key_ref_includes_math1.rs` with file-scope `#![allow(clippy::unwrap_used)]` and `use hp41_cli::keys::key_ref_entries;`. Implement the 3 tests from `<behavior>`.

       For each Math Pac I assertion, iterate over `key_ref_entries()` and use `.any(|(key_path, display)| key_path == "XEQ \"SINH\"" && display == "SINH")` — assertion message must reference D-29.2 / CLI-04 / SC-4 of ROADMAP and call out the help_entries_all migration. The third test uses the same pattern with the v2.2 row `("+", "+")` (the `Add` Op).

       Note: at this point in plan execution the test FILE exists but tests fail because `key_ref_entries()` still reads from `help_entries()` (Math Pac I rows not yet in the listing). The TDD "red" state — proceed to step 2.

    2. In `hp41-cli/src/keys.rs::key_ref_entries` (around line 392 — verify exact line via Read), replace `for entry in crate::help_data::help_entries()` with `for entry in crate::help_data::help_entries_all()`. This is a single-line change per RESEARCH §3.2.

    3. Run `cargo test -p hp41-cli --test phase29_key_ref_includes_math1` — confirm all 3 tests pass.

    Per CLI-04, D-29.2, D-25.18 (KEY_REF_TABLE derives from JSON via key_ref_entries filtered by non-null `key_path`; Math Pac I entries with `key_path: "XEQ \"NAME\""` automatically surface). NO code-shape change to `key_ref_entries` beyond the accessor migration — the filter-by-non-null-`key_path` logic stays unchanged. NO hand-curated parallel table.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase29_key_ref_includes_math1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase29_key_ref_includes_math1` reports `3 passed; 0 failed`
    - Source assertion: `grep -c "help_entries_all" hp41-cli/src/keys.rs` outputs at least `1`
    - Source assertion: `grep -n "for entry in crate::help_data::help_entries()" hp41-cli/src/keys.rs` returns nothing (the v2.2 call was replaced, not duplicated)
    - Source assertion: `grep -c "for entry in crate::help_data::help_entries_all()" hp41-cli/src/keys.rs` outputs `1`
    - Source assertion: `test -f hp41-cli/tests/phase29_key_ref_includes_math1.rs` succeeds
    - Existing v2.2 narrow-pool tests still green: `cargo test -p hp41-cli --test phase25_help_data help_entries_count_meets_130_target` exits 0
  </acceptance_criteria>
  <done>`key_ref_entries()` migrated to merged accessor; 3 new tests prove Math Pac I rows surface AND v2.2 rows preserved; no parallel hand-curated table; D-25.18 invariant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate help_overlay_rows() to help_entries_all() + verify CLI-03 already-shipped op_display_name coverage</name>
  <files>hp41-cli/src/help_data.rs</files>
  <read_first>
    - .planning/phases/29-cli-integration/29-RESEARCH.md §3.0 (CLI-03 verification — grep audit), §3.2 (migration table for help_overlay_rows)
    - .planning/phases/29-cli-integration/29-PATTERNS.md "hp41-cli/src/help_data.rs (second OnceLock)" section
    - .planning/phases/29-cli-integration/29-CONTEXT.md "Already locked" bullet on op_display_name shipped in Phase 28
    - hp41-cli/src/help_data.rs:95-121 — `help_overlay_rows` current body that calls `help_entries()`
    - hp41-cli/src/prgm_display.rs (entire file) — verify all 47 Math Pac I Op variants have arms
    - hp41-gui/src-tauri/src/prgm_display.rs (entire file) — verify parity with CLI copy
  </read_first>
  <behavior>
    - `help_overlay_rows()` returns rows for BOTH v2.2 categories (Arithmetic, Stack, Math, …) AND Math1 categories (Math1 Hyperbolics, Math1 Matrix, …) after migration.
    - Empty/short slices excluded by existing logic (no new filter needed).
    - `cargo check -p hp41-cli` continues to compile cleanly — the exhaustive match in `op_display_name` covers all 47 Math Pac I `Op` variants per Phase 28 ship.
    - `cargo check -p hp41-gui --manifest-path hp41-gui/src-tauri/Cargo.toml` continues to compile cleanly — mirrors CLI's coverage per D-25.6.
  </behavior>
  <action>
    1. In `hp41-cli/src/help_data.rs::help_overlay_rows` (around line 95–121 — verify exact range via Read), replace `let entries = help_entries();` with `let entries: Vec<&HelpEntry> = help_entries_all().collect();`. The `.collect()` materializes the chained iterator into a Vec so subsequent group-by-category logic (which iterates twice or indexes) keeps working unchanged. Inspect the function body to confirm no behavior change beyond the entry source.

       Reference RESEARCH §3.2 migration table (line 275): "help_overlay_rows clones into owned Strings — refactor cost is minimal".

    2. **CLI-03 verification step — `op_display_name` exhaustive-match audit.** Run the verification command from RESEARCH §3.0:

       `grep -c "Op::\(Sinh\|Cosh\|Tanh\|CPlus\|CMinus\|CTimes\|CDiv\|Real\|Magz\|Cinv\|ZpowN\|ExpZ\|LnZ\|SinZ\|CosZ\|TanZ\|LogZ\|ZpowW\|Apow\\sZ\|Zpow1N\|PolyWorkflow\|Roots\|MatrixWorkflow\|MatSize\|MatVmat\|MatEdit\|MatDet\|MatInv\|MatSimeq\|MatVcol\|Integ\|Solve\|Sol\|Difeq\|Four\|TriSss\|TriAsa\|TriSaa\|TriSas\|TriSsa\|Trans2d\|Trans3d\)" hp41-cli/src/prgm_display.rs hp41-gui/src-tauri/src/prgm_display.rs`

       Expected output: at least 42 distinct match arms in each file (some Op variants like ApowZ may use distinct spellings). Confirm the count is identical (within ±0) in both files. Document the audit result inline in `29-02-SUMMARY.md`'s "CLI-03 verification" section.

    3. **Compile-time verification.** Run `cargo check -p hp41-cli` and `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml`. Both must succeed without errors — proves the exhaustive match in `op_display_name` covers every `Op` variant including all Math Pac I additions. The Rust compiler is the gate; if any Math Pac I variant lacks an arm, the build fails with "non-exhaustive patterns" (FN-CLI-04).

    4. (No new code required.) Document in `29-02-SUMMARY.md` that CLI-03 is satisfied by the Phase 28 ship per CONTEXT "Already locked" bullet — Phase 29 plan 02's role is **verification**, not new code. Adding the grep audit command + the two `cargo check` exit codes to the SUMMARY is the verification artifact.

    Per CLI-03 (verification only — code shipped Phase 28), CLI-04 (`help_overlay_rows` migration to merged accessor), D-29.2, D-25.6 (both `prgm_display.rs` copies stay in sync — 4-exhaustive-match invariant), SC-4 (no calculator logic in hp41-gui — `op_display_name` is the documented display-formatter exception).
  </action>
  <verify>
    <automated>cargo check -p hp41-cli && cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-cli` exits 0 (CLI `op_display_name` exhaustive over all Math Pac I Op variants)
    - `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` exits 0 (GUI `op_display_name` exhaustive)
    - Source assertion: `grep -c "help_entries_all" hp41-cli/src/help_data.rs` outputs at least `2` (the `help_entries_all` definition from plan 01 + at least one new caller in `help_overlay_rows`)
    - Source assertion: `grep -n "let entries = help_entries();" hp41-cli/src/help_data.rs` does not find the legacy call inside `help_overlay_rows` body (it should now read from `help_entries_all`)
    - Audit assertion: `grep -c "Op::Sinh =>" hp41-cli/src/prgm_display.rs` outputs `1` AND `grep -c "Op::Sinh =>" hp41-gui/src-tauri/src/prgm_display.rs` outputs `1` (parity per D-25.6)
    - Audit assertion: `grep -c "Op::MatrixWorkflow =>" hp41-cli/src/prgm_display.rs` outputs `1` AND `grep -c "Op::MatrixWorkflow =>" hp41-gui/src-tauri/src/prgm_display.rs` outputs `1`
    - Audit assertion: `grep -c "Op::Integ =>" hp41-cli/src/prgm_display.rs` outputs `1` AND `grep -c "Op::Integ =>" hp41-gui/src-tauri/src/prgm_display.rs` outputs `1`
    - Audit assertion: no `_ =>` catch-all in `op_display_name`: `grep -A 200 "fn op_display_name" hp41-cli/src/prgm_display.rs | head -200 | grep -c "^\\s*_ =>"` outputs `0`
    - Existing v2.2 130-target test still green: `cargo test -p hp41-cli --test phase25_help_data help_entries_count_meets_130_target` exits 0
  </acceptance_criteria>
  <done>`help_overlay_rows` migrated to `help_entries_all()`; CLI-03 verified via grep audit + two `cargo check` exit codes; no `_ =>` catch-all in either `op_display_name`; CLI ↔ GUI parity preserved per D-25.6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `help_entries()` (narrow) ↔ `help_entries_all()` (merged) | Callers must pick the correct accessor: narrow for v2.2-specific tests, merged for production consumers — drift risk |
| `prgm_display.rs` (CLI copy) ↔ `prgm_display.rs` (GUI copy) | Two files that must stay in sync per D-25.6 — manual-discipline-only invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | `help_overlay_rows` accidentally migrates back to `help_entries()` (Math Pac I rows disappear from `?` overlay) | mitigate | grep-audit in `29-02-SUMMARY.md`; future CI gate (Phase 32 / QUAL-04) covers this; manual: `key_ref_entries_includes_math1_*` tests assert the right-panel surface, which uses the same merged accessor |
| T-29-02-02 | Tampering | CLI `op_display_name` arm added but GUI copy missed (D-25.6 parity drift) | mitigate | Two `cargo check` exit codes are CI-enforced for every PR touching `Op` enum; manual: this plan's audit greps both files for known Math Pac I variants |
| T-29-02-03 | Spoofing | New `_ =>` catch-all sneaks into `op_display_name` | mitigate | This plan's acceptance_criteria asserts `grep -c "^\\s*_ =>"` outputs `0` in CLI copy |
| T-29-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure source migration + grep audit + 1 new test file. RESEARCH §"Environment Availability" confirms zero new runtime deps. |
</threat_model>

<verification>
- `cargo test -p hp41-cli --tests` green
- `cargo check -p hp41-cli` clean (CLI exhaustive match)
- `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` clean (GUI exhaustive match per D-25.6)
- `cargo test -p hp41-cli --test phase29_key_ref_includes_math1` reports 3 passed
- Existing 130-target v2.2 invariant preserved
- `just ci` green at wave-end
</verification>

<success_criteria>
- CLI-03: every Math Pac I `Op` variant has an exhaustive-match arm in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` — proven by `cargo check` × 2 exit codes (Phase 28 ship verified, no new code added)
- CLI-04: `key_ref_entries()` (the right-panel `KEY_REF_TABLE` source) surfaces Math Pac I rows via the merged `help_entries_all()` accessor; no parallel hand-curated table (D-25.18 preserved) — proven by `phase29_key_ref_includes_math1` 3-test suite
- `?` overlay (`help_overlay_rows`) reads from merged accessor — Math Pac I categories appear in the overlay
- 4-exhaustive-match invariant (dispatch / execute_op / prgm_display × 2) preserved; D-25.6 CLI ↔ GUI parity preserved
</success_criteria>

<output>
Create `.planning/phases/29-cli-integration/29-02-SUMMARY.md` when done. Include the grep-audit output from Task 2 step 2 in the "CLI-03 verification" section.
</output>