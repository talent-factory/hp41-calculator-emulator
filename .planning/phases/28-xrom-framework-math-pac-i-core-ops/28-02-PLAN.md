---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - hp41-core/src/ops/math1/hyperbolics.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - HYP-01
  - HYP-02
  - HYP-03
  - HYP-04
  - HYP-05
  - HYP-06

must_haves:
  truths:
    - "Six new Op variants land in Op enum + dispatch() + execute_op() arms: Op::Sinh, Op::Cosh, Op::Tanh, Op::Asinh, Op::Acosh, Op::Atanh"
    - "Each op consumes X, writes the hyperbolic of X back to X, declares LiftEffect::Disable (matches v2.2 op_sin pattern)"
    - "Domain errors hold: Op::Acosh with X<1 returns HpError::Domain; Op::Atanh with |X|>=1 returns HpError::Domain"
    - "All six ops are angle-mode-INDEPENDENT (no to_radians_f64 / from_radians_f64 calls — different from op_sin/op_cos)"
    - "xrom_resolve registers all six mnemonics: SINH, COSH, TANH, ASINH, ACOSH, ATANH map to the corresponding Op variants; MATH_1.ops static slice now contains 6 entries"
    - "Each op has >= 5 tests per Pitfall 16 (positive case + domain edge + 2 OM-cited reference values + 1 stack-lift assertion)"
    - "Each op has >= 3 cases in numerical_accuracy.rs with OM page citations (per D-27.7 precedent: // Source: HP 00041-90034 p.XX, ex.YY + // Free42 v3.0.5: <value> — agrees with OM)"
    - "Proof-of-pattern complete: hyperbolics demonstrates end-to-end pipeline (Op variant + dispatch + execute_op + xrom registration + tests + xrom_shadowing.rs passes) — validates Plan 28-01 framework for downstream Plans 28-03..28-10"
    - "D-28.6 honored: NO keyboard binding in hp41-cli/src/keys.rs or hp41-gui/src/Keyboard.tsx (XEQ-by-name only); zero edits outside hp41-core"
  nyquist_dimensions:
    - "HYP-01..06 unit — cargo test -p hp41-core --lib math1::hyperbolics::tests (>= 5 cases per op = >= 30 tests)"
    - "Pitfall 16 meta — cargo test -p hp41-core --test math1_op_test_count"
    - "Pitfall 1 — cargo test -p hp41-core --test xrom_shadowing (6 new mnemonics confirmed non-shadowing)"
    - "Numerical accuracy — cargo test -p hp41-core --test numerical_accuracy hyperbolic (cases with OM cite)"
  key_links:
    - from: hp41-core/src/ops/mod.rs
      to: hp41-core/src/ops/math1/hyperbolics.rs
      via: "dispatch arms call math1::hyperbolics::op_sinh(state) etc."
      pattern: "Op::Sinh => math1::hyperbolics::op_sinh"
    - from: hp41-core/src/ops/math1/xrom.rs
      to: hp41-core/src/ops/math1/hyperbolics.rs
      via: "MATH_1.ops slice maps 'SINH' to Op::Sinh"
      pattern: "\\(\"SINH\", Op::Sinh\\)"
---

<objective>
Land the 6 hyperbolic functions as the **proof-of-pattern** for every downstream Math Pac I plan. Hyperbolics are the lowest-cost, highest-confidence target: pure unary stack-acting ops, no modal state, no user callback, no complex stack — just f64-bridge transcendentals with two domain guards.

Purpose: end-to-end validation that Plan 28-01's framework wires correctly through dispatch + execute_op + xrom_resolve + xrom_shadowing.rs CI gate + numerical_accuracy.rs accuracy harness. If hyperbolics ship cleanly, every subsequent Math Pac I plan inherits the proven pipeline.

Output: `hp41-core/src/ops/math1/hyperbolics.rs` with 6 op fns, 6 new `Op` variants in `hp41-core/src/ops/mod.rs` (Op enum + dispatch arms), 6 new `execute_op` arms in `hp41-core/src/ops/program.rs`, 6 new entries in `MATH_1.ops` in `hp41-core/src/ops/math1/xrom.rs`, ≥ 30 unit tests, ≥ 18 numerical accuracy cases with OM citations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-VALIDATION.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-01-SUMMARY.md
@hp41-core/src/ops/math.rs
@hp41-core/src/ops/math1/xrom.rs
@hp41-core/src/ops/mod.rs

<interfaces>
<!-- From `hp41-core/src/ops/math.rs:218-232` — `op_asin` domain-guard pattern (mirror for Acosh/Atanh). -->

```
pub fn op_asin(state: &mut CalcState) -> Result<(), HpError> {
    let v = state.stack.x.inner().to_f64().ok_or(HpError::Overflow)?;
    if !(-1.0..=1.0).contains(&v) {
        return Err(HpError::Domain);
    }
    let rad = v.asin();
    let angle = f64_from_radians(rad, state.angle_mode);
    let result = Decimal::from_f64(angle)
        .map(HpNum::rounded)
        .ok_or(HpError::Overflow)?;
    unary_result(state, result);
    Ok(())
}
```

<!-- From `hp41-core/src/ops/math.rs:158-168` — `op_sin` f64-bridge unary (mirror for Sinh/Cosh/Tanh — STRIP `to_radians_f64` since hyperbolics are angle-mode-independent). -->

```
pub fn op_sin(state: &mut CalcState) -> Result<(), HpError> {
    let v = state.stack.x.inner().to_f64().ok_or(HpError::Overflow)?;
    let rad = to_radians_f64(v, state.angle_mode);
    let result = Decimal::from_f64(rad.sin())
        .map(HpNum::rounded)
        .ok_or(HpError::Domain)?;
    unary_result(state, result);
    Ok(())
}
```

<!-- From `hp41-core/src/ops/mod.rs:678-697` — dispatch arm shape. -->

```
Op::Sin => op_sin(state),
Op::Cos => op_cos(state),
Op::Asin => op_asin(state),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hyperbolics module (6 op fns + inline unit tests)</name>
  <files>hp41-core/src/ops/math1/hyperbolics.rs</files>
  <read_first>
    - hp41-core/src/ops/math.rs (lines 158-247 — op_sin, op_cos, op_tan, op_asin, op_acos, op_atan as f64-bridge unary analog)
    - hp41-core/src/ops/math1/mod.rs (verify it declares submodules; add `pub mod hyperbolics;`)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Hyperbolics (Plan 28-02) — proof-of-pattern", lines 474-485 — domain check + LiftEffect table)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-02 — Hyperbolics", lines 324-367)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-28.6: XEQ-by-name only)
  </read_first>
  <behavior>
    Each function consumes X, writes the hyperbolic value back, declares LiftEffect::Disable.
    - `op_sinh`: X = 0 → 0; X = 1 → 1.1752; X = -1 → -1.1752; very large X → Overflow.
    - `op_cosh`: X = 0 → 1; X = 1 → 1.5431; very large X → Overflow.
    - `op_tanh`: X = 0 → 0; X = 1 → 0.7616; X = -1 → -0.7616; X >= 18 → 1.0000 (saturation); X <= -18 → -1.0000.
    - `op_asinh`: X = 0 → 0; X = 1 → 0.8814; X = -1 → -0.8814. No domain restriction.
    - `op_acosh`: X = 1 → 0; X = 2 → 1.3170; **X = 0.5 → HpError::Domain** (X < 1 forbidden); X = 0 → Domain.
    - `op_atanh`: X = 0 → 0; X = 0.5 → 0.5493; **X = 1 → HpError::Domain** (|X| ≥ 1 forbidden); X = -1 → Domain; X = 1.5 → Domain.
    - Stack-lift: after a successful op, `state.stack.lift_enabled` is `false` (LiftEffect::Disable; matches v2.2 transcendental pattern, never Enable like binary arithmetic).
    - Five tests per op minimum (Pitfall 16): two positive numerical cases (with `assert_relative_eq!(max_relative = 1e-7)` per Pitfall 14), one zero/identity case, one stack-lift assertion, and for Acosh/Atanh one domain-violation case (= 5 each). For Sinh/Cosh/Tanh/Asinh which have no domain limit, add a fifth case asserting behavior at extreme magnitude (overflow or saturation).
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/hyperbolics.rs`. File-top header:
    ```
    // Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979);
    // Free42 source consulted only as sanity-check oracle, not copied.
    ```

    Implement six public functions, each with signature `pub fn op_<name>(state: &mut CalcState) -> Result<(), HpError>`:
    - `op_sinh` — pattern from `op_sin` BUT strip the `to_radians_f64` call (hyperbolics are angle-mode-independent). Use `v.sinh()` on the f64 bridge value. Map back via `Decimal::from_f64(...).map(HpNum::rounded).ok_or(HpError::Overflow)?` (Overflow, not Domain — matches RESEARCH §Hyperbolics table semantics for very large arguments).
    - `op_cosh` — same shape, `v.cosh()`.
    - `op_tanh` — same shape, `v.tanh()`. Note: tanh saturates to ±1 at large magnitudes — that is correct behavior, not an error.
    - `op_asinh` — same shape, `v.asinh()`. No domain check.
    - `op_acosh` — copy `op_asin` shape; replace `(-1.0..=1.0).contains(&v)` check with `v >= 1.0` check (return `HpError::Domain` if `v < 1.0`); use `v.acosh()`.
    - `op_atanh` — copy `op_asin` shape; replace check with `v.abs() < 1.0` (return `Domain` if `|v| >= 1.0`); use `v.atanh()`.

    Apply `unary_result(state, result)` for all 6 (declares `LiftEffect::Disable` — confirm by reading the helper's body in `hp41-core/src/ops/math.rs`).

    `unary_result` is currently `pub(crate) fn` per v2.2 — verify. If it's narrower, either widen to `pub(crate)` (preferred — keeps internal-only) or factor into `ops/helpers.rs`. PATTERNS line 366 suggests re-exporting from `crate::ops::math::unary_result`; do that.

    Add `pub mod hyperbolics;` to `hp41-core/src/ops/math1/mod.rs`.

    Inline `#[cfg(test)] mod tests` block at bottom of `hyperbolics.rs`:
    - 6 functions × 5 tests = 30 tests minimum.
    - Use `approx::assert_relative_eq!(actual_decimal_as_f64, expected_f64, max_relative = 1e-7)` for numerical comparisons (Pitfall 14 discipline from Plan 28-01).
    - Add file-scope `#[allow(clippy::unwrap_used)]` ONLY on the `#[cfg(test)] mod tests` (not file-wide — production code stays under `#[deny(clippy::unwrap_used)]`).
    - Tests for Acosh/Atanh must include `assert_eq!(result, Err(HpError::Domain))` for the documented out-of-domain inputs.
    - At least one test per op asserts `state.stack.lift_enabled == false` after the op succeeds (Disable semantics).
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib math1::hyperbolics::tests</automated>
  </verify>
  <done>6 op fns compile; all 30+ tests pass; domain guards verified; LiftEffect::Disable asserted; per-file Free42 header present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Op enum + dispatch + execute_op + xrom_resolve registration</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
  </files>
  <read_first>
    - hp41-core/src/ops/mod.rs (Op enum lines 104-201; dispatch lines 652-694 — find the v2.2 pattern Op::Sin => op_sin(state))
    - hp41-core/src/ops/program.rs (execute_op match arms — likely around line 623 onward; trace pattern from how Op::Sin is handled there)
    - hp41-core/src/ops/math1/xrom.rs (created in Plan 28-01 Task 5; MATH_1.ops slice currently empty; grow it here)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-01 — `hp41-core/src/ops/mod.rs`", lines 156-184; §Pattern 3 4-place exhaustive-match, lines 579-609)
  </read_first>
  <behavior>
    - Test 1: `dispatch(&mut state, Op::Sinh)` with X=1.0 mutates X to ~1.1752 (post-condition; reuses Task 1 unit tests, but exercised via dispatch).
    - Test 2: `execute_op` invoked with `Op::Sinh` (via run_loop with a single-step program) produces the same result.
    - Test 3 (xrom_chain_order.rs — extend existing scaffold from Plan 28-01): `Op::Xeq("SINH".into())` in run_loop dispatches to `Op::Sinh`. Add a test case asserting this positive-resolution path.
    - Test 4 (xrom_shadowing.rs — extend): with 6 new entries in MATH_1.ops, the iteration in `math1_names_do_not_shadow_builtins` runs over 6 names and confirms none collide with builtin_card_op.
    - Test 5: `xrom_resolve("ASINH", 0b0000_0001)` returns `Some(Op::Asinh)` (positive path).
    - Test 6: `xrom_resolve("ASINH", 0)` returns `None` (module not loaded — bit 0 clear).
  </behavior>
  <action>
    In `hp41-core/src/ops/mod.rs`:
    1. Add 6 variants to the `Op` enum (one per op fn from Task 1), each with a doc comment `/// SINH — hyperbolic sine. LiftEffect: Disable.` etc. Group header: `// ── Phase 28: Hyperbolics (Plan 28-02) ────────`.
    2. Add `use crate::ops::math1::hyperbolics::*;` near the existing `use math::*;` lines.
    3. Add 6 dispatch arms in the `dispatch` function: `Op::Sinh => op_sinh(state),` and the other five.

    In `hp41-core/src/ops/program.rs`:
    4. Add 6 arms to `execute_op` (the central match block that delegates to `dispatch` for most ops). Each arm is identical structure to `Op::Sin` — likely `Op::Sinh => crate::ops::dispatch(state, Op::Sinh),` per v2.2 idiom; verify by reading the existing `Op::Sin` arm in `execute_op`.

    In `hp41-core/src/ops/math1/xrom.rs`:
    5. Grow `pub const MATH_1: XromModule { ... ops: &[ ... ] };` to include 6 entries: `("SINH", Op::Sinh), ("COSH", Op::Cosh), ("TANH", Op::Tanh), ("ASINH", Op::Asinh), ("ACOSH", Op::Acosh), ("ATANH", Op::Atanh)`.
    6. Grow the `math1_resolve` private fn match block to map these 6 names to the same `Op::*` variants. Pattern from RESEARCH lines 163-186.

    Update `xrom_chain_order.rs` (Plan 28-01 scaffold) to add the positive-resolution test case for `SINH` per Test 3.

    `prgm_display.rs` edits for the 6 new variants land in this plan's Task 4 (added per ROADMAP Success Criterion #5 + CLAUDE.md SC-4-literal carve-out — display formatters are not calculator/math logic).

    Run `cargo build -p hp41-core` to verify exhaustive-match coverage. If `dispatch` is `match op { ... }` without `_ =>`, the compiler will refuse to build until all 6 arms are added — that is the desired structural enforcement.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test xrom_chain_order &amp;&amp; cargo test -p hp41-core --lib math1::xrom::tests</automated>
  </verify>
  <done>6 Op variants land in enum + dispatch + execute_op + xrom_resolve; xrom_shadowing iterates and passes; xrom_chain_order positive case for SINH passes; xrom_resolve("ASINH", 0b1) returns Some(Op::Asinh); workspace builds cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ≥ 18 numerical accuracy cases with OM citations</name>
  <files>hp41-core/tests/numerical_accuracy.rs</files>
  <read_first>
    - hp41-core/tests/numerical_accuracy.rs (full file — preserve the v1.x 503-case baseline floor per D-27.6; understand `case!` macro shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-VALIDATION.md (§"Free42 Cross-Check Protocol" lines 119-127)
    - HP-41C Math Pac Owner's Manual — Chapter on Hyperbolic Functions (~p.44 per RESEARCH §Hyperbolics table)
  </read_first>
  <behavior>
    - At least 3 cases per op = 18 total; each case carries a `// Source: HP 00041-90034 p.<n>, ex.<m>` doc-comment AND a `// Free42 v3.0.5: <value> — agrees with OM` cross-check note.
    - Cases cover: zero-input (where defined), positive reference, negative reference (where defined), domain edge (for Acosh/Atanh), and one "stress" case (large magnitude).
    - Combined pass rate including all v2.2 baseline + new hyperbolics cases >= 98%; v1.x 503-case baseline floor 498/503 (D-27.6) UNCHANGED — assert with the existing independent floor check.
  </behavior>
  <action>
    In `hp41-core/tests/numerical_accuracy.rs`, add a new section (or test function) `hyperbolic_cases` adding `case!` invocations for each of the 6 hyperbolic ops. Use the established `case!(name, input, expected, tolerance, op_fn, "OM citation", "Free42 cross-check")` macro shape from the existing v2.2 cases — verify by reading the v2.2 PHASE-27-cited cases for guidance.

    Each `case!` doc-comment carries (a) the OM page+example reference and (b) the Free42 v3.0.5 oracle value. Per D-27.7 the citations are mandatory.

    Cases minimum:
    - SINH: 3+ (zero, sinh(1) = 1.1752, sinh(-1) = -1.1752; optional: sinh(2) = 3.6269)
    - COSH: 3+ (cosh(0) = 1, cosh(1) = 1.5431, cosh(2) = 3.7622)
    - TANH: 3+ (tanh(0) = 0, tanh(1) = 0.7616, tanh(-1) = -0.7616; optional: tanh(18) ≈ 1)
    - ASINH: 3+ (asinh(0) = 0, asinh(1) = 0.8814, asinh(-1) = -0.8814)
    - ACOSH: 3+ (acosh(1) = 0, acosh(2) = 1.3170, acosh(0.5) = Domain error)
    - ATANH: 3+ (atanh(0) = 0, atanh(0.5) = 0.5493, atanh(1) = Domain error)

    Total: 18+ cases. Use `approx::assert_relative_eq!(max_relative = 1e-7)` (Pitfall 14).

    Run `numerical_accuracy.rs` to confirm new cases pass AND v1.x baseline still hits >= 498/503.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test numerical_accuracy</automated>
  </verify>
  <done>>= 18 new hyperbolic cases land with OM + Free42 citations; numerical_accuracy.rs passes; v1.x 503-case baseline floor preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Add op_display_name arms to both prgm_display.rs copies</name>
  <files>
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (full file — confirm `op_display_name` match is exhaustive with NO `_ =>` arm)
    - hp41-gui/src-tauri/src/prgm_display.rs (mirror copy — same exhaustive shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (analog excerpt §"Pattern 3 / op_display_name 4-place exhaustive match" — line shape to mirror)
    - CLAUDE.md (SC-4-literal carve-out: "op_display_name(...) in prgm_display.rs is a display formatter, not calculator logic — duplicated in both CLI and GUI copies; every new Op variant must be added in BOTH files")
  </read_first>
  <action>
    Add the 6 new Op variant arm(s) introduced by this plan to the `op_display_name` exhaustive `match` in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs`. Place each new arm under a `// ── Phase 28: Hyperbolics (Plan 28-02) ────────` section comment.

    Variants to add (matching the xrom_resolve mnemonic registered by Task 2 of this plan): Op::Sinh => "SINH", Op::Cosh => "COSH", Op::Tanh => "TANH", Op::Asinh => "ASINH", Op::Acosh => "ACOSH", Op::Atanh => "ATANH".

    Preserve the existing exhaustive match shape — `op_display_name` has NO `_ =>` arm by design (FN-CLI-04 invariant from Phase 25 / CLAUDE.md v2.2 additions). The Rust compiler enforces every Op variant is named; a missing arm causes a workspace build failure in either `hp41-cli` or `hp41-gui/src-tauri` and that is the desired structural enforcement. Both copies stay in sync — the CLAUDE.md comment "every new Op variant must be added in both copies" is the canonical instruction.

    No production logic added here; this task is purely the SC-4-literal carve-out display formatting. ROADMAP Success Criterion #5 (every new Op variant appears in BOTH prgm_display copies) is satisfied for the 6 variant(s) introduced by Plan 28-02.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>6 new arm(s) appear in both prgm_display.rs files in the same order under the Phase 28 section header; `cargo build --workspace` passes (compile-time exhaustiveness confirms no Op variant is missing); no production code paths touched outside the two display files.</done>
</task>


</tasks>

<verification>
- `cargo build -p hp41-core` (zero clippy::unwrap_used violations).
- `cargo test -p hp41-core` (full hp41-core test suite green; new tests + W0 scaffolds + accuracy harness).
- `cargo test -p hp41-core --test xrom_shadowing` — 6 new mnemonics confirmed non-shadowing.
- `cargo test -p hp41-core --test math1_op_test_count` — meta-test confirms each of the 6 new variants has >= 5 tests.
- `cargo bench --bench dispatch_bench` (advisory) — `bench_dispatch_math1` shows hyperbolic ops < 200 ns (Pitfall 10).
- SC-4 invariant: no `hp41-gui/src-tauri/src/` source changes.
</verification>

<success_criteria>
- Six hyperbolic Op variants reach users (post-Phase 29) via `XEQ "SINH"` etc. — in Phase 28 isolation, demonstrate via direct dispatch + run_loop tests.
- Domain errors return `HpError::Domain` for `Op::Acosh(X<1)` and `Op::Atanh(|X|>=1)`.
- ≥ 5 tests per op (≥ 30 unit tests + ≥ 18 numerical_accuracy cases = ≥ 48 hyperbolic-flavored tests).
- xrom_shadowing.rs (Plan 28-01 scaffold) passes — no SINH/COSH/etc. shadow v2.2 mnemonics.
- xrom_chain_order.rs gets a real positive case (SINH resolves via xrom).
- Proof-of-pattern: Plans 28-03..28-10 inherit this exact pipeline without re-design.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-SUMMARY.md` recording:
- 6 hyperbolic op variants added (Op enum + dispatch + execute_op + xrom registration).
- ≥ 30 unit tests + ≥ 18 numerical_accuracy cases.
- Proof-of-pattern validated end-to-end: framework from Plan 28-01 wires correctly through every layer.
- Bench result (advisory): per-op dispatch median in ns.
- Unblocks Plans 28-03..28-10 by demonstrating the assembly line works.
</output>
