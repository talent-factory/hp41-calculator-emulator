---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 04
type: execute
wave: 4
depends_on:
  - 28-01
  - 28-03
files_modified:
  - hp41-core/src/ops/math1/complex.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/math1_complex_edge_cases.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - CMPLX-06
  - CMPLX-07
  - CMPLX-08
  - CMPLX-09
  - CMPLX-10
  - CMPLX-11
  - CMPLX-12
  - CMPLX-13
  - CMPLX-14
  - CMPLX-15
  - CMPLX-16
  - CMPLX-17

must_haves:
  truths:
    - "12 new Op variants land in Op enum + dispatch() + execute_op() + MATH_1.ops registry: Magz, Cinv, ZpowN, Zpow1N, ExpZ, LnZ, SinZ, CosZ, TanZ, ApowZ, LogZ, ZpowW"
    - "Unary complex ops (Magz, Cinv, ExpZ, LnZ, SinZ, CosZ, TanZ, LogZ) consume ζ, write to ζ, leave τ unchanged, declare LiftEffect::Disable"
    - "Binary complex ops (ApowZ = a^z, ZpowW = z^w) consume ζ AND τ (4 levels), write to ζ, T-replicate, LiftEffect::Enable"
    - "ZpowN / Zpow1N: integer-exponent variants — N read from real-axis X (real-valued register); for ZpowN use repeated complex multiply (no f64 bridge); for Zpow1N use polar form r^(1/n) · cis(θ/n) via complex_atan2 + sqrt"
    - "Branch-cut policy (Pitfall 6 mitigations): LnZ(0+0i) returns HpError::Domain; ZpowW(0+0i, w) with Re(w) ≤ 0 returns Domain; CDiv-like guards on Cinv (re=0 && im=0)"
    - "complex_atan2 first-arm (0,0)→0 used internally by Magz / Cinv / LnZ / Zpow1N / Z^N polar conversions"
    - "TanZ domain check: cos(re)=0 && sinh(im)=0 → Domain (computed via hyperbolic identity decomposition)"
    - "Op::Real (added in Plan 28-03) unchanged; complex_mode auto-on for every op in this plan"
    - "math1_complex_edge_cases.rs (Plan 28-01 scaffold) is fully filled: 4 placeholder cases all green (the 2 from Plan 28-03 + ln_z_zero_returns_domain + z_pow_w_zero_neg_exp_returns_domain)"
    - "Per-Op test count >= 5 (Pitfall 16): 12 ops × 5 tests = >= 60 unit tests"
  nyquist_dimensions:
    - "CMPLX-06..17 unit — cargo test -p hp41-core --lib math1::complex::tests (>= 60 tests for the 12 functions)"
    - "CMPLX-11 branch cut — cargo test -p hp41-core --test math1_complex_edge_cases ln_z_zero_returns_domain"
    - "CMPLX-17 branch cut — cargo test -p hp41-core --test math1_complex_edge_cases z_pow_w_zero_neg_exp_returns_domain"
    - "Pitfall 6 full — cargo test -p hp41-core --test math1_complex_edge_cases (all 4 cases green)"
    - "Numerical accuracy — cargo test -p hp41-core --test numerical_accuracy complex_functions (>= 36 cases)"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
  key_links:
    - from: hp41-core/src/ops/math1/complex.rs
      to: hp41-core/src/ops/math1/complex.rs
      via: "Z^W and A^Z reduce to complex multiplication + ExpZ + LnZ — built on Plan 28-03 primitives"
      pattern: "op_exp_z\\|op_ln_z"
    - from: hp41-core/src/ops/math1/xrom.rs
      to: hp41-core/src/ops/math1/complex.rs
      via: "MATH_1.ops grows by 12 entries with Unicode + ASCII aliases"
      pattern: "\\(\"MAGZ\", Op::Magz\\)"
---

<objective>
Land 12 transcendental complex functions on top of Plan 28-03's complex-arithmetic primitives. Five unary (Magz, Cinv, ExpZ, LnZ, LogZ) and four near-unary (SinZ, CosZ, TanZ via hyperbolic-identity decomposition); two integer-exponent variants (ZpowN, Zpow1N); two binary (ApowZ, ZpowW).

Purpose: complete the CMPLX domain (CMPLX-06..17 = 12 reqs); deliver branch-cut policy per Pitfall 6 (LnZ(0,0), ZpowW(0,_) with Re(w) ≤ 0, Cinv(0,0)); finalize `math1_complex_edge_cases.rs` from Plan 28-01.

Output: ~600 lines of `complex.rs` growth (mostly unary ops + tests); MATH_1.ops registry grows by 12; numerical_accuracy.rs grows by ~36 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@hp41-core/src/ops/math1/complex.rs
@hp41-core/src/num.rs

<interfaces>
<!-- From RESEARCH §"Complex Functions" lines 498-509 — algorithm table -->
```
| Op::Magz (MAGZ)    | sqrt(re² + im²) via f64 bridge       | none
| Op::Cinv (CINV)    | 1/(re+im·i) — DivideByZero on (0,0)  | (0,0)
| Op::ZpowN (Z↑N)    | Integer-exponent power via repeated  | none
| Op::Zpow1N (Z↑1/N) | Polar form: r^(1/n) · cis(θ/n)        | (0,0) returns 0
| Op::ExpZ (E↑Z)     | Euler: e^re · (cos(im) + i sin(im))   | none
| Op::LnZ (LNZ)      | ln(r) + i·θ via complex_atan2         | (0,0) → Domain (CMPLX-11)
| Op::SinZ/CosZ/TanZ | hyperbolic identities                  | TanZ: cos(re)=0 && sinh(im)=0 → Domain
| Op::ApowZ (A↑Z)    | exp(z · ln(a))                        | a=0+0i: Domain
| Op::LogZ (LOGZ)    | LnZ / ln(10)                          | (0,0) → Domain
| Op::ZpowW (Z↑W)    | exp(w · LnZ)                          | (0,0)^w with Re(w) ≤ 0: Domain
```

<!-- From Plan 28-03 — complex_atan2 helper (now available, internal-only) -->
```
pub(super) fn complex_atan2(im: HpNum, re: HpNum) -> HpNum
```

<!-- From Plan 28-03 — complex multiplication formula (reuse for ZpowN integer power) -->
```
(X+iY)(Z+iT) = (XZ-YT) + i(XT+YZ)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unary complex functions (Magz, Cinv, ExpZ, LnZ, LogZ)</name>
  <files>hp41-core/src/ops/math1/complex.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/complex.rs (post-Plan 28-03 — has 5 arithmetic ops + complex_atan2; understand existing patterns)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Complex Functions (Plan 28-04)" algorithm table lines 498-509; §"Branch-cut policy for LnZ / ZpowW" lines 440-444)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-03 + 28-04 — Complex stack + functions" pattern remarks lines 397-410)
    - hp41-core/src/num.rs (HpNum::sqrt API — verify existence; if missing, use f64 bridge `v.sqrt()`)
  </read_first>
  <behavior>
    - `op_magz`: result = sqrt(X² + Y²); writes to X; Y stays unchanged (per OM convention "magnitude goes to X, leave Y as imag part for visualization"); τ unchanged; LiftEffect::Disable; complex_mode auto-on. Test: (3,4) → 5; (0,0) → 0; (1,1) → √2; negative components → magnitude (positive).
    - `op_cinv`: 1/(X+iY) = (X-iY)/(X²+Y²); guard (X=0 && Y=0) → HpError::DivideByZero (NOT Domain — symmetric with Plan 28-03's op_c_div). Write to X, Y. LiftEffect::Disable. Tests: (1,0) → (1,0); (0,1) → (0,-1); (1,1) → (0.5,-0.5); (0,0) → DivideByZero.
    - `op_exp_z`: e^(X+iY) = e^X · (cos(Y) + i sin(Y)). Use f64 bridge for `exp` / `cos` / `sin`. No domain. Tests: (0,0) → (1,0); (0, π) → (-1, ~0); (1,0) → (e, 0); LiftEffect::Disable.
    - `op_ln_z`: ln(X+iY) = ln(|X+iY|) + i·complex_atan2(Y, X). **Guard (X=0 && Y=0) → HpError::Domain** (CMPLX-11, Pitfall 6). Use `magz` magnitude internally then take ln of it. Tests: (1,0) → (0,0); (e, 0) → (1, 0); (0, 1) → (0, π/2 in current angle_mode); (-1, 0) → (0, π); **(0, 0) → Domain**.
    - `op_log_z`: LnZ / ln(10). Internally compute LnZ then divide both parts by ln(10) ≈ 2.302585. Inherits LnZ's Domain on (0,0). Tests: (10, 0) → (1, 0); (100, 0) → (2, 0); (0,0) → Domain.

    All ops set `state.complex_mode = true` BEFORE computation; declare `LiftEffect::Disable`; τ (Z, T) untouched.
  </behavior>
  <action>
    In `hp41-core/src/ops/math1/complex.rs`, add 5 unary functions. Each follows the unary-stack-acting pattern (consume X+iY, write X+iY, leave Z+iT alone). Use f64 bridge for transcendentals not natively supported by `rust_decimal` (sqrt, exp, ln, cos, sin). Reuse `complex_atan2` (added in Plan 28-03) for LnZ's imaginary part. For LnZ angle output: respect `state.angle_mode` (DEG / RAD / GRAD) per HP-41 convention — convert atan2 result from radians to current mode. Verify by reading v2.2's `op_asin` for the `f64_from_radians` conversion helper.

    All five functions set `state.complex_mode = true;` as first mutation.
    All five apply `LiftEffect::Disable` (matches v2.2 transcendental pattern; matches Plan 28-03's unary-op rule).

    Add inline tests under existing `#[cfg(test)] mod tests` block (≥ 5 per op = ≥ 25 new tests).

    Document each fn with: (a) one-line description, (b) OM page reference (placeholder if unknown — use `// OM p.~25` style), (c) f64 bridge rationale (e.g., `// rust_decimal has no sqrt; use f64 bridge`).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::complex::tests::magz math1::complex::tests::cinv math1::complex::tests::exp_z math1::complex::tests::ln_z math1::complex::tests::log_z</automated>
  </verify>
  <done>5 unary complex functions land with branch-cut guards; ≥ 25 tests pass; complex_mode auto-on; LiftEffect::Disable verified; LnZ/LogZ (0,0) → Domain confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trig complex functions (SinZ, CosZ, TanZ) + power variants (ZpowN, Zpow1N, ApowZ, ZpowW)</name>
  <files>hp41-core/src/ops/math1/complex.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/complex.rs (post-Task 1 — has 5 unary ops landed)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Complex Functions" table lines 498-509 — algorithms for sin/cos/tan/Z^N/Z^(1/N)/A^Z/Z^W)
    - hp41-core/src/format.rs (for angle_mode handling if needed)
  </read_first>
  <behavior>
    Trig (hyperbolic-identity decomposition):
    - `op_sin_z`: sin(X+iY) = sin(X)·cosh(Y) + i cos(X)·sinh(Y). No domain. Tests: (0,0) → (0,0); (π/2, 0) → (1, 0); (0, 1) → (0, sinh(1) = 1.1752); LiftEffect::Disable.
    - `op_cos_z`: cos(X+iY) = cos(X)·cosh(Y) - i sin(X)·sinh(Y). No domain. Tests: (0,0) → (1,0); (π, 0) → (-1, 0); (0, 1) → (cosh(1) = 1.5431, 0).
    - `op_tan_z`: tan = sin/cos using SinZ and CosZ values. **Guard: if (cos(X)=0 AND sinh(Y)=0) → Domain** — this is tan's singularity (CMPLX-13 Domain branch per RESEARCH line 505). Tests: (0,0) → (0,0); (π/4, 0) → (1, 0); (π/2, 0) → Domain (cos = 0).

    Power variants:
    - `op_z_pow_n`: integer N read from X (must be real; ζ in this op is τ = Z+iT, exponent N = X). RESEARCH says "Integer-exponent power via repeated multiply (no f64)". So: convert X to integer (error if non-integer? — OM-faithful: truncate or check). Use Plan 28-03's complex-multiply pattern in a loop n times. Tests: ((1,1), 2) → (0, 2); ((2,0), 3) → (8, 0); ((1,0), 0) → (1, 0); negative N → 1/Z^|N| via Cinv composition.
    - `op_z_pow_1_n`: Z^(1/N) via polar form `r^(1/n) · cis(θ/n)`. Use complex_atan2 + sqrt (or f64 bridge to `powf(1.0/n)`). (0,0) → (0,0) per RESEARCH line 502. Tests: ((1,0), 2) → (1,0); ((-1,0), 2) → (0, 1) — principal sqrt of -1 = i; ((0,0), 5) → (0,0).
    - `op_a_pow_z`: a^z where a is τ (Z+iT, a possibly complex too) and z is ζ. Formula: `exp(z · ln(a))`. Internally reuse `op_ln_z`-style computation on a, then complex-multiply by z, then `op_exp_z`-style computation. **Guard: a = (0,0) → Domain**. Binary op: consume ζ + τ (4 levels), write to ζ, T-replicate. LiftEffect::Enable.
    - `op_z_pow_w`: z^w = exp(w · LnZ). Binary. **Guards: (0,0)^w with Re(w) ≤ 0 → Domain (CMPLX-17 / Pitfall 6 / RESEARCH line 502)**. Otherwise compute via composition of LnZ + complex-multiply + ExpZ. Tests: ((2,0), (3,0)) → (8, 0); ((0,0), (1,0)) → (0, 0) (Re(w)=1>0 OK); **((0,0), (-1,0)) → Domain (Re(w)=-1 ≤ 0)**; ((0,0), (0,0)) → Domain (Re(w)=0 ≤ 0).

    Per-Op test count ≥ 5 (Pitfall 16): 7 ops × 5 tests = ≥ 35 new unit tests.
  </behavior>
  <action>
    Add 7 op fns to `hp41-core/src/ops/math1/complex.rs`:
    - `op_sin_z`, `op_cos_z`, `op_tan_z` — unary complex; hyperbolic-identity decomposition. TanZ has the cos/sinh-zero domain guard.
    - `op_z_pow_n` — read N from a stack register (per OM: N is real integer in X, ζ is τ — verify against OM convention; if unclear, document the assumption in the doc-comment and add a fallback). Iterate complex-multiply.
    - `op_z_pow_1_n` — polar form via complex_atan2 + sqrt; (0,0)→(0,0) first arm.
    - `op_a_pow_z` — binary; composition `exp(z · ln(a))`. Domain on a=(0,0).
    - `op_z_pow_w` — binary; composition `exp(w · LnZ)`. Domain on (0,0)^w with Re(w) ≤ 0.

    All set `state.complex_mode = true;` first.
    Unary ops: LiftEffect::Disable. Binary ApowZ / ZpowW: LiftEffect::Enable.

    Add inline tests under `#[cfg(test)] mod tests` block (≥ 5 per op = ≥ 35 tests).

    Use `approx::assert_relative_eq!(max_relative = 1e-7)`.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::complex::tests</automated>
  </verify>
  <done>7 op fns land; ≥ 35 tests pass; TanZ singularity Domain; ZpowW (0,0)^(-1,0) → Domain; ApowZ (0,0)^_ → Domain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve (12 new variants); fill math1_complex_edge_cases.rs</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/math1_complex_edge_cases.rs
    hp41-core/tests/numerical_accuracy.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/xrom.rs (current MATH_1.ops post-Plan 28-03 — 11 entries; grow to 23)
    - hp41-core/tests/math1_complex_edge_cases.rs (post-Plan 28-03 — 2 of 4 cases filled)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Resolver-Chain Conflict Map" complex names — all clean per line 624)
  </read_first>
  <behavior>
    - 12 new Op variants in dispatch + execute_op + xrom_resolve match arms.
    - xrom_resolve mnemonic registration (with Unicode + ASCII aliases per OM 1979 convention):
      - `"MAGZ"` → Op::Magz; `"CINV"` → Op::Cinv
      - `"Z↑N"` and `"Z^N"` (ASCII alias) → Op::ZpowN
      - `"Z↑1/N"` and `"Z^1/N"` → Op::Zpow1N
      - `"E↑Z"` and `"E^Z"` → Op::ExpZ
      - `"LNZ"` → Op::LnZ
      - `"SINZ"` → Op::SinZ; `"COSZ"` → Op::CosZ; `"TANZ"` → Op::TanZ
      - `"A↑Z"` and `"A^Z"` → Op::ApowZ
      - `"LOGZ"` → Op::LogZ
      - `"Z↑W"` and `"Z^W"` → Op::ZpowW
    - math1_complex_edge_cases.rs: `ln_z_zero_returns_domain` and `z_pow_w_zero_neg_exp_returns_domain` cases fill in (remove `#[ignore]`).
    - numerical_accuracy.rs grows by ≥ 36 cases (3 per op × 12 ops).
  </behavior>
  <action>
    1. Add 12 variants to `Op` enum in `hp41-core/src/ops/mod.rs` under header `// ── Phase 28: Complex Functions (Plan 28-04) ────────`. Order: Magz, Cinv, ZpowN, Zpow1N, ExpZ, LnZ, SinZ, CosZ, TanZ, ApowZ, LogZ, ZpowW.
    2. Add 12 dispatch arms.
    3. Add 12 `execute_op` arms in `hp41-core/src/ops/program.rs`.
    4. Grow `MATH_1.ops` in `hp41-core/src/ops/math1/xrom.rs` with 12 entries (≥ 18 mnemonic strings counting aliases) and the matching `math1_resolve` match-block arms.
    5. In `hp41-core/tests/math1_complex_edge_cases.rs`, remove `#[ignore]` from the two remaining placeholders and fill bodies:
       - `ln_z_zero_returns_domain`: dispatch `Op::LnZ` on stack (0,0,_,_); assert `Err(HpError::Domain)`; assert stack unchanged.
       - `z_pow_w_zero_neg_exp_returns_domain`: set stack to ζ=(0,0), τ=(-1,0); dispatch `Op::ZpowW`; assert `Err(HpError::Domain)`.
    6. In `hp41-core/tests/numerical_accuracy.rs`, add ≥ 3 cases per op (≥ 36 total) with `// Source: HP 00041-90034 p.XX` + `// Free42 v3.0.5: <value>` citations. OM Chapter on Complex Functions ~pp.24-26.
    7. Re-run xrom_shadowing.rs — confirm 23 entries (6 hyperbolic + 5 arith + 12 functions) all non-shadowing.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib --test xrom_shadowing --test math1_complex_edge_cases --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>12 Op variants land in 4-place exhaustive match (Op enum + dispatch + execute_op in this task; both prgm_display.rs arms in Task 4); xrom_resolve registers 12 (with aliases); math1_complex_edge_cases.rs fully green (4 of 4 cases passing); numerical_accuracy.rs +36 cases.</done>
</task>

<task type="auto">
  <name>Task 4: Add op_display_name arms to both prgm_display.rs copies</name>
  <files>
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (full file — confirm `op_display_name` match is exhaustive with NO `_ =>` arm)
    - hp41-gui/src-tauri/src/prgm_display.rs (mirror copy — same exhaustive shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (analog excerpt §"Pattern 3 / op_display_name 4-place exhaustive match" — line shape to mirror)
    - CLAUDE.md (SC-4-literal carve-out: "op_display_name(...) in prgm_display.rs is a display formatter, not calculator logic — duplicated in both CLI and GUI copies; every new Op variant must be added in BOTH files")
  </read_first>
  <action>
    Add the 12 new Op variant arm(s) introduced by this plan to the `op_display_name` exhaustive `match` in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs`. Place each new arm under a `// ── Phase 28: Complex Functions (Plan 28-04) ────────` section comment.

    Variants to add (matching the xrom_resolve mnemonic registered by Task 2 of this plan): Op::Magz => "MAGZ", Op::Cinv => "CINV", Op::ZpowN => "Z↑N", Op::Zpow1N => "Z↑1/N", Op::ExpZ => "E↑Z", Op::LnZ => "LNZ", Op::SinZ => "SINZ", Op::CosZ => "COSZ", Op::TanZ => "TANZ", Op::ApowZ => "A↑Z", Op::LogZ => "LOGZ", Op::ZpowW => "Z↑W".

    Preserve the existing exhaustive match shape — `op_display_name` has NO `_ =>` arm by design (FN-CLI-04 invariant from Phase 25 / CLAUDE.md v2.2 additions). The Rust compiler enforces every Op variant is named; a missing arm causes a workspace build failure in either `hp41-cli` or `hp41-gui/src-tauri` and that is the desired structural enforcement. Both copies stay in sync — the CLAUDE.md comment "every new Op variant must be added in both copies" is the canonical instruction.

    No production logic added here; this task is purely the SC-4-literal carve-out display formatting. ROADMAP Success Criterion #5 (every new Op variant appears in BOTH prgm_display copies) is satisfied for the 12 variant(s) introduced by Plan 28-04.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>12 new arm(s) appear in both prgm_display.rs files in the same order under the Phase 28 section header; `cargo build --workspace` passes (compile-time exhaustiveness confirms no Op variant is missing); no production code paths touched outside the two display files.</done>
</task>


</tasks>

<verification>
- `cargo test -p hp41-core` — workspace green.
- `cargo test -p hp41-core --test math1_complex_edge_cases` — all 4 cases pass with NO `#[ignore]` remaining.
- `cargo test -p hp41-core --test math1_op_test_count` — meta-test confirms 12 new Ops each have ≥ 5 tests (≥ 60 tests in math1::complex::tests).
- `cargo test -p hp41-core --test xrom_shadowing` — 23 entries (6+5+12) clear non-collision.
- SC-4 invariant: no `hp41-gui/src-tauri/src/` changes.
- Branch-cut policy verified: LnZ(0,0)/LogZ(0,0)/Cinv(0,0)/ApowZ(0,_)/ZpowW(0,0,Re(w)≤0)/TanZ(π/2, 0) all return their documented error variants (Domain vs DivideByZero).
</verification>

<success_criteria>
- 12 complex transcendental Op variants integrate cleanly through the 3 Phase-28 exhaustive matches (Op enum + dispatch + execute_op).
- Branch-cut policy implemented per OM convention: LnZ/LogZ/ApowZ/ZpowW domain on zero (or zero with non-positive Re for ZpowW); Cinv divide-by-zero on (0,0); TanZ domain on singularity.
- `complex_atan2(0,0) = 0` first arm used by Magz, Cinv, LnZ, Zpow1N — verified by the `math1_complex_edge_cases.rs` test.
- CMPLX-06..17 closed (12 requirements satisfied; CMPLX-05 closed in Plan 28-03).
- Plan 28-05's POLY can now compute complex roots using the full complex stack + functions.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-04-SUMMARY.md`:
- 12 Op variants added (Magz..ZpowW).
- Branch-cut policy locked: LnZ/LogZ/ApowZ/ZpowW Domain on (0,0)-related cases; Cinv DivideByZero on (0,0); TanZ Domain at singularity.
- math1_complex_edge_cases.rs fully filled — Plan 28-01 scaffold complete.
- 23 entries in MATH_1.ops (6 hyperbolic + 5 arith + 12 functions); xrom_shadowing.rs confirms no shadowing.
- Plan 28-05 POLY can build on complex stack.
</output>
