---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 07
type: execute
wave: 7
depends_on:
  - 28-01
  - 28-06
files_modified:
  - hp41-core/src/ops/math1/integ.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/math1_user_callback.rs
  - hp41-core/tests/numerical_accuracy.rs
  - docs/hp41-math1-divergences.md
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04
  - INTG-05
  - INTG-06
  - INTG-07
  - INTG-08
  - XROM-08

must_haves:
  truths:
    - "integ_threshold helper transcribed from OM via Plan 28-01 Task 2 ADR-004 is consumed by op_integ (Pitfall 2 / INTG-08 / D-4 irreversible) — formula tied to DisplayMode"
    - "Op::Integ (XEQ \"INTG\") master entry: opens modal_program = ModalProgram::Integ(IntegInputStep::ModeChoice); modal_prompt = \"INTG MODE?\" or per OM convention"
    - "Two modes per INTG-02/INTG-03: Discrete (A=h, B=f(xⱼ) sample input, C=trapezoidal, D=Simpson; even-n check returns N NOT EVEN modal_prompt) and Explicit (A=(a,b), B=n, FUNCTION NAME? for user LBL)"
    - "Re-entry uses run_loop (NOT run_program) per C-28.5; preserves outer program clone; mirrors Op::XeqInd precedent at program.rs:476-491"
    - "Pre-mutation 4-deep call_stack cap (Pitfall 4 mitigation): state.call_stack.len() >= 4 → HpError::CallDepth BEFORE any mutation; mirrors Op::XeqInd:479 EXACTLY"
    - "XROM-08 strict-reject nested INTG/SOLVE/DIFEQ at op entry: state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() → HpError::InvalidOp BEFORE mutation"
    - "Per-64-samples cancellation check (D-28.8): sample_idx & 0x3F == 0 → state.cancel_requested.load(Ordering::Relaxed); if true, clears integ_state and returns HpError::Canceled (D-28.9). Wiring of request_cancel lands Phase 31 — Plan 28-07 ships the check stub"
    - "INTG-07 subdivision cap 2^15 = 32768: requested n > 32768 returns HpError::Domain"
    - "INTG-08 convergence threshold tied to DisplayMode (Pitfall 2): test asserts SAME integral produces different precision under Fix(4) vs Fix(9) — if threshold formula is wrong, BOTH tests pass with same tolerance and bug is invisible"
    - "math1_user_callback.rs Plan 28-01 scaffold gets the INTG branches filled: nested_integ_inside_integ_rejected, user_fn_stops_aborts_integ (other branches by Plan 28-08/09)"
    - "Per-program scratch register clobber (RESEARCH Open Q6): user-program STO to R00-R07 during INTG corrupts integration — documented as user-responsibility per OM; ships integration test user_fn_stores_to_scratch_corrupts_integ asserting documented behavior + docs/hp41-math1-divergences.md entry"
    - "Op::Integ dispatch arm returns Err(InvalidOp) when called OUTSIDE a run_loop context; real implementation runs ONLY in the run_loop match arm (mirrors Op::XeqInd precedent at program.rs:839+)"
    - "Per-Op test count >= 5 (Pitfall 16): Op::Integ has >= 5 tests (discrete mode trapezoidal, discrete mode Simpson, explicit mode known integral, cancellation, nested rejection)"
  nyquist_dimensions:
    - "INTG-01..07 unit — cargo test -p hp41-core --lib math1::integ::tests"
    - "INTG-08 unit (2 cases at Fix(4) vs Fix(9)) — cargo test -p hp41-core --lib math1::integ::tests::threshold_fix4 math1::integ::tests::threshold_fix9"
    - "XROM-08 integration — cargo test -p hp41-core --test math1_user_callback nested_integ_inside_integ_rejected"
    - "Pitfall 4 — cargo test -p hp41-core --test math1_user_callback (5-case suite filled)"
    - "Pitfall 11 plumbing — cargo test -p hp41-core --test math1_user_callback cancel_aborts_integ"
    - "Cancellation cadence — cargo test -p hp41-core --lib math1::integ::tests::cancel_per_64_samples"
    - "Numerical accuracy — cargo test -p hp41-core --test numerical_accuracy integ_cases"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
  key_links:
    - from: hp41-core/src/ops/math1/integ.rs
      to: docs/adr/v3.0-004-intg-threshold.md
      via: "integ_threshold(mode) helper sourced from this ADR per Plan 28-01 Task 2 OM transcription"
      pattern: "integ_threshold"
    - from: hp41-core/src/ops/math1/integ.rs
      to: hp41-core/src/ops/program.rs
      via: "run_loop re-entry from inside op_integ for each sample point (NOT run_program)"
      pattern: "run_loop\\(state, program"
    - from: hp41-core/src/state.rs
      to: hp41-core/src/ops/math1/integ.rs
      via: "state.integ_state Option<IntegState> field landed in Plan 28-01; Plan 28-07 grows IntegState struct fields"
      pattern: "state\\.integ_state"
---

<objective>
Land INTG — the FIRST user-callback program in v3.0. `Op::Integ` re-enters `run_loop` for each Simpson-rule sample point, evaluating a user-defined LBL function `f(x)`. The architectural novelty (vs hyperbolics / complex / POLY / MATRIX which were all pure-data ops): user-callback re-entrancy with strict-reject of nested INTG/SOLVE/DIFEQ per ADR-002 / D-28.7.

Purpose: validates the `run_loop` re-entry pattern that Plans 28-08 (SOLVE) and 28-09 (DIFEQ) will inherit verbatim. Consumes the OM-transcribed convergence threshold from Plan 28-01 ADR-004. Ships the 5-case user-callback regression suite (filling math1_user_callback.rs scaffold from Plan 28-01) and the cancellation-per-64-samples plumbing (D-28.7 / D-28.8).

Output: `hp41-core/src/ops/math1/integ.rs` containing `op_integ` + full `IntegState` struct + `integ_threshold` helper consumed from ADR-004; extends Plan 28-01's compile-only `math1_user_callback.rs` scaffold with the INTG branches; adds the user-responsibility divergence entry to `docs/hp41-math1-divergences.md` (NEW file in Phase 28 per RESEARCH Open Q6 recommendation — full document landed in Phase 30 / DOC-04 but the first entry ships here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@docs/adr/v3.0-004-intg-threshold.md
@hp41-core/src/ops/program.rs
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/state.rs

<interfaces>
<!-- From PATTERNS §"Plan 28-07 — INTG" lines 466-521 — Op::XeqInd precedent + op_integ template -->
```
// program.rs:476-491 — Op::XeqInd pre-mutation cap (REFERENCE)
Op::XeqInd(reg) => {
    if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }
    let i = crate::ops::indirect::resolve_indirect_decimal(state, reg)?;
    let label_str = i.to_string();
    let target = find_in_program(program, &label_str)?;
    state.call_stack.push(state.pc);
    state.pc = target + 1;
}

// Plan 28-07 op_integ pattern (RESEARCH lines 293-330)
pub fn op_integ(state: &mut CalcState, program: &[Op]) -> Result<(), HpError> {
    if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() {
        return Err(HpError::InvalidOp);    // XROM-08
    }
    if state.call_stack.len() >= 4 {
        return Err(HpError::CallDepth);     // Pitfall 4
    }
    state.cancel_requested.store(false, Ordering::Relaxed);
    state.integ_state = Some(IntegState::new(...));

    for sample_idx in 0..n_samples {
        if sample_idx & 0x3F == 0 && state.cancel_requested.load(Ordering::Relaxed) {
            state.integ_state = None;
            return Err(HpError::Canceled);
        }
        // compute x_k, push to stack, find user_label, run_loop re-entry
    }
    state.integ_state = None;
    Ok(())
}
```

<!-- From RESEARCH §"`integ_state` / `solve_state` struct shape" lines 357-369 -->
```
#[derive(Debug, Clone, Default)]
pub struct IntegState {
    pub user_label: String,
    pub a: HpNum,
    pub b: HpNum,
    pub n: u16,           // subdivisions, cap 32768 per INTG-07
    pub accumulator: HpNum,
    pub mode: IntegMode,  // Discrete vs Explicit
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend IntegInputStep + ModalProgram::current_prompt for INTG workflow</name>
  <files>hp41-core/src/ops/math1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Plan 28-06 — IntegInputStep stubs from Plan 28-01)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"INTG" Op table lines 547-559)
  </read_first>
  <behavior>
    - `IntegInputStep` variants (extend Plan 28-01's stubs): ModeChoice, FunctionNamePrompt, IntervalPrompt, SubdivisionPrompt, Ready (NEW: add Ready arm to clear modal after completion).
    - current_prompt arms:
      - Integ(ModeChoice) → "INTG MODE?" (or whatever OM convention; document the choice)
      - Integ(FunctionNamePrompt) → "FUNCTION NAME?"
      - Integ(IntervalPrompt) → "(A,B)=?"
      - Integ(SubdivisionPrompt) → "N=?"
      - Integ(Ready) → None
  </behavior>
  <action>
    Extend `IntegInputStep` enum if needed: add `Ready` variant. Fill the `ModalProgram::current_prompt()` exhaustive match's Integ arm with all 5 sub-variants per behavior.

    Update existing modal.rs tests to cover the 5 prompts. Use the owned-String signature from Plan 28-06 Task 1.

    Preserve exhaustive match (no `_ =>`).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::modal::tests</automated>
  </verify>
  <done>5 IntegInputStep variants return correct prompts via current_prompt(); tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement integ.rs (IntegState struct + integ_threshold helper + op_integ with run_loop re-entry)</name>
  <files>
    hp41-core/src/ops/math1/integ.rs
    hp41-core/src/ops/program.rs
  </files>
  <read_first>
    - hp41-core/src/ops/program.rs (full file — especially `run_loop` signature and the `Op::XeqInd` re-entry at lines 476-491 + the `Op::XeqInd` execute_op arm at line 839+)
    - docs/adr/v3.0-004-intg-threshold.md (Plan 28-01 Task 2 — verbatim integ_threshold helper body)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"User-Callback Infrastructure" lines 283-353; §"INTG" Op table)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-07 — INTG (user-callback infrastructure)" lines 466-521)
    - hp41-core/src/format.rs (DisplayMode shape)
    - hp41-core/src/ops/math1/integ.rs (current empty `IntegState` stub from Plan 28-01 Task 5)
  </read_first>
  <behavior>
    - `IntegState` (replace Plan 28-01's empty stub):
      - `pub user_label: String`
      - `pub a: HpNum` (interval lower bound — explicit mode)
      - `pub b: HpNum` (interval upper bound — explicit mode)
      - `pub n: u16` (subdivisions, cap 32768 per INTG-07)
      - `pub accumulator: HpNum` (running Simpson sum)
      - `pub mode: IntegMode` (NEW enum: `Discrete | Explicit`)
      - `#[derive(Debug, Clone, Default)]` per RESEARCH lines 361-369
    - `pub fn integ_threshold(mode: DisplayMode) -> HpNum` — body verbatim from ADR-004 (Plan 28-01 Task 2). Tied to DisplayMode::Fix(n) / Sci(n) / Eng(n).
    - `pub fn op_integ_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` — the REAL implementation; runs from inside run_loop's match arm. Per PATTERNS line 520 "Plan 28-07 ships BOTH the dispatch stub AND the run_loop body".
    - `pub fn op_integ(state: &mut CalcState) -> Result<(), HpError>` — dispatch arm version; returns `Err(HpError::InvalidOp)` because INTG can only run inside run_loop. Mirrors Op::XeqInd precedent at program.rs:839+ where the dispatch arm rejects with InvalidOp.

    Op::Integ flow inside run_loop (per RESEARCH lines 293-330 and PATTERNS lines 488-513):
    1. Strict-reject: `if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() { return Err(HpError::InvalidOp); }` BEFORE any mutation.
    2. Pre-mutation call_stack cap: `if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }` BEFORE any mutation.
    3. Reset cancellation flag: `state.cancel_requested.store(false, Ordering::Relaxed);`.
    4. Read mode + parameters from state (the modal flow set these prior — read from `state.integ_state` if it was partially staged or from the modal_program completion state; the exact read path depends on how Plan 28-07 chose to stage the parameters during the modal flow — document the choice).
    5. Cap check: `if n_samples > 32_768 { return Err(HpError::Domain); }` (INTG-07).
    6. Mode-dispatch:
       - Discrete trapezoidal (C entry): trapezoidal rule over user-provided f(xⱼ) samples.
       - Discrete Simpson (D entry): Simpson rule over user-provided f(xⱼ) samples; check `n_samples is even` else state.modal_prompt = Some("N NOT EVEN".into()) and return Ok(()).
       - Explicit: Simpson rule over (a,b) with n subdivisions, where each f(x_k) is computed by re-entering run_loop with the user-LBL program.
    7. For Explicit mode, the sample loop:
       ```
       for sample_idx in 0..=n_samples {
           if sample_idx & 0x3F == 0 && state.cancel_requested.load(Ordering::Relaxed) {
               state.integ_state = None;
               return Err(HpError::Canceled);
           }
           let x_k = a + (b - a) * (sample_idx as HpNum) / (n_samples as HpNum);
           state.stack.push_lift(x_k);
           state.call_stack.push(state.pc);
           state.pc = find_in_program(program, &user_label)? + 1;
           run_loop(state, program, MAX_STEPS_REMAINING)?;  // re-entrant; outer clone reused
           // After return, X = f(x_k)
           accumulate_simpson_term(&mut accumulator, state.stack.x().clone(), sample_idx, n_samples);
       }
       ```
    8. Compute final Simpson result; apply threshold check from `integ_threshold(state.display_mode)` to decide convergence (per OM); push result to X via stack-lift Enable; clear integ_state.
    9. The 5 user-callback regression tests from math1_user_callback.rs land via this implementation.

    Per RESEARCH Open Q6 recommendation (user-responsibility for scratch register clobber): document in code comment that R00-R07 are scratch and the user must not write to them inside the callback; ship integration test `user_fn_stores_to_scratch_corrupts_integ` that asserts the documented (wrong-answer) behavior — NO snapshot/restore (matches real Math Pac I OM).
  </behavior>
  <action>
    Replace Plan 28-01's empty `IntegState` stub in `hp41-core/src/ops/math1/integ.rs` with the full struct per behavior. Add `pub enum IntegMode { Discrete, Explicit }` (with `#[derive(Debug, Clone, Default)]`; Default = Explicit).

    Implement `pub fn integ_threshold(mode: DisplayMode) -> HpNum` with body from ADR-004 verbatim.

    Implement `pub(super) fn op_integ_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` — full body per behavior.

    Implement `pub fn op_integ(state: &mut CalcState) -> Result<(), HpError>` — dispatch arm: returns `Err(HpError::InvalidOp)` with a doc comment "Op::Integ can only run inside run_loop; real implementation in op_integ_run_loop".

    Also implement `pub fn op_integ_workflow_open(state: &mut CalcState) -> Result<(), HpError>` if needed for the modal opener path — OR fold modal opening into op_integ. Document the choice.

    In `hp41-core/src/ops/program.rs`, add the `Op::Integ` arm to `run_loop`'s match block, calling `crate::ops::math1::integ::op_integ_run_loop(state, program)?;`. Place it next to `Op::XeqInd` for pattern symmetry.

    Per-file Free42-contamination header.

    Inline `#[cfg(test)] mod tests` (file-scope `#![allow(clippy::unwrap_used)]` on test mod):
    - Discrete trapezoidal: integrate samples [0, 1, 2, 1, 0] with h=1 → expected = trapezoidal sum.
    - Discrete Simpson: 5 samples (n=4 even) → Simpson sum; assert.
    - Discrete Simpson n-odd: 4 samples (n=3 odd) → `state.modal_prompt == Some("N NOT EVEN")` and Ok(()).
    - Explicit known integral: ∫₀¹ x² dx with n=10 subdivisions → ~0.333; assert via `approx::assert_relative_eq!(max_relative = 1e-3)`.
    - threshold_fix4 / threshold_fix9: same integral under Fix(4) vs Fix(9); assert different precision (or assert the helper returns different thresholds for the two modes).
    - subdivision_cap: n=32769 → Err(Domain).
    - cancel_per_64_samples: pre-set state.cancel_requested.store(true); run an INTG that would take > 64 samples; assert Err(Canceled) after the first per-64 check.
    - nested_rejection: pre-set state.integ_state = Some(IntegState::default()); call op_integ_run_loop again → Err(InvalidOp).
    - call_stack_full: pre-fill state.call_stack with 4 entries; call op_integ_run_loop → Err(CallDepth) BEFORE mutation; assert state.call_stack still has 4 entries (no leak).
    - Plus several Simpson-rule correctness cases against OM-cited integrals.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::integ::tests</automated>
  </verify>
  <done>IntegState full struct compiled; integ_threshold returns ADR-004 values; op_integ_run_loop integrates Simpson rule correctly; XROM-08 strict-reject works; Pitfall 4 call_stack cap works pre-mutation; cancellation per-64-samples fires; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve; fill math1_user_callback.rs INTG branches; create divergences doc entry</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/math1_user_callback.rs
    hp41-core/tests/numerical_accuracy.rs
    docs/hp41-math1-divergences.md
  </files>
  <read_first>
    - hp41-core/tests/math1_user_callback.rs (Plan 28-01 scaffold with 5 #[ignore]'d placeholder functions)
    - hp41-core/src/ops/math1/xrom.rs (post-Plan 28-06 — 33 entries; grow to 34)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§Open Q6 — scratch register clobber as user-responsibility lines 922-925)
  </read_first>
  <behavior>
    - Op::Integ integrated: Op enum + dispatch arm (returns InvalidOp) + execute_op arm (calls op_integ_run_loop via dispatch indirectly OR via direct call from execute_op — choose pattern matching how Op::XeqInd is handled in execute_op).
    - xrom_resolve("INTG", 0b1) → Some(Op::Integ).
    - math1_user_callback.rs: 2 of 5 #[ignore]d placeholders filled by Plan 28-07:
      - `nested_integ_inside_integ_rejected`: program with `LBL F / XEQ "INTG" / RTN`; outer XEQ INTG with F as callback → inner Op::Integ returns InvalidOp; assert.
      - `user_fn_stops_aborts_integ`: program with `LBL F / STOP / RTN`; outer XEQ INTG → STOP propagates; integ_state cleared; assert.
      - Other 3 placeholders (nested_solve_inside_integ_rejected, nested_integ_inside_solve_rejected, nested_difeq_inside_integ_rejected) remain #[ignore] for Plans 28-08 / 28-09.
    - Plus NEW test `user_fn_stores_to_scratch_corrupts_integ`: program with `LBL F / X / STO 03 / 1 / + / RTN` (clobbers R03 which is scratch); run outer INTG; assert the integration result is WRONG (does not match the analytical answer for x²); docstring documents the user-responsibility convention per RESEARCH Open Q6.
    - numerical_accuracy.rs grows with ≥ 4 cases (∫₀^π sin = 2; ∫₀¹ x² = 1/3; ∫₁^e 1/x = 1; an OM Chapter 4 worked example).
    - docs/hp41-math1-divergences.md created with the user-scratch-register entry (Phase 30 / DOC-04 expands this file with full divergences; Plan 28-07 ships first entry).
    - xrom_shadowing.rs runs over 34 entries non-collision.
  </behavior>
  <action>
    1. Add `Op::Integ` variant to `Op` enum in `hp41-core/src/ops/mod.rs` under header `// ── Phase 28: INTG (Plan 28-07) ────────`. Add the dispatch arm returning `Err(HpError::InvalidOp)` per the "Op variants that run only in run_loop" pattern (mirrors Op::XeqInd dispatch arm).
    2. Add `Op::Integ` arm to `execute_op` in `program.rs` — same `Err(InvalidOp)` per RESEARCH §"execute_op vs run_loop split for callback ops" lines 769-775.
    3. Confirm the `Op::Integ` arm in `run_loop`'s match block from Task 2 is wired correctly.
    4. Grow `MATH_1.ops` and `math1_resolve` in xrom.rs: `("INTG", Op::Integ)`.
    5. In `hp41-core/tests/math1_user_callback.rs`, remove `#[ignore]` from `nested_integ_inside_integ_rejected` and `user_fn_stops_aborts_integ`; fill bodies per behavior. Keep the other 3 #[ignore]d. Add new test `user_fn_stores_to_scratch_corrupts_integ` (un-ignored) — verifies the documented user-responsibility behavior.
    6. Add ≥ 4 cases to `hp41-core/tests/numerical_accuracy.rs` for INTG with OM citations.
    7. Create `docs/hp41-math1-divergences.md` (NEW file) with one entry:
       - **Title:** "User-program scratch register clobber during INTG/SOLVE/DIFEQ"
       - **Behavior:** "Math Pac I uses R00–R07 as scratch during integration. If the user-provided LBL function executes STO to any of these registers, the solver's internal state is corrupted and the result is wrong. The emulator faithfully reproduces this hardware-faithful behavior — NO snapshot/restore. OM 1979 page X documents: 'do not use registers R00–R07 in your user function while INTG is active.'"
       - **Citation:** OM page reference.
       - **Test:** `tests/math1_user_callback.rs::user_fn_stores_to_scratch_corrupts_integ`
    8. Re-run xrom_shadowing.rs — 34/34 entries clear.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test math1_user_callback --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>Op::Integ landed (1 new variant); 2 of 5 user-callback regression cases filled + 1 new user-responsibility test; numerical_accuracy.rs +4 cases; docs/hp41-math1-divergences.md created with first entry; xrom_shadowing.rs 34/34.</done>
</task>

<task type="auto">
  <name>Task 4: Add op_display_name arms to both prgm_display.rs copies</name>
  <files>
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (full file — confirm `op_display_name` match is exhaustive with NO `_ =>` arm)
    - hp41-gui/src-tauri/src/prgm_display.rs (mirror copy — same exhaustive shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (analog excerpt §"Pattern 3 / op_display_name 4-place exhaustive match" — line shape to mirror)
    - CLAUDE.md (SC-4-literal carve-out: "op_display_name(...) in prgm_display.rs is a display formatter, not calculator logic — duplicated in both CLI and GUI copies; every new Op variant must be added in BOTH files")
  </read_first>
  <action>
    Add the 1 new Op variant arm(s) introduced by this plan to the `op_display_name` exhaustive `match` in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs`. Place each new arm under a `// ── Phase 28: INTG (Plan 28-07) ────────` section comment.

    Variants to add (matching the xrom_resolve mnemonic registered by Task 2 of this plan): Op::Integ => "INTG".

    Preserve the existing exhaustive match shape — `op_display_name` has NO `_ =>` arm by design (FN-CLI-04 invariant from Phase 25 / CLAUDE.md v2.2 additions). The Rust compiler enforces every Op variant is named; a missing arm causes a workspace build failure in either `hp41-cli` or `hp41-gui/src-tauri` and that is the desired structural enforcement. Both copies stay in sync — the CLAUDE.md comment "every new Op variant must be added in both copies" is the canonical instruction.

    No production logic added here; this task is purely the SC-4-literal carve-out display formatting. ROADMAP Success Criterion #5 (every new Op variant appears in BOTH prgm_display copies) is satisfied for the 1 variant(s) introduced by Plan 28-07.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>1 new arm(s) appear in both prgm_display.rs files in the same order under the Phase 28 section header; `cargo build --workspace` passes (compile-time exhaustiveness confirms no Op variant is missing); no production code paths touched outside the two display files.</done>
</task>


</tasks>

<verification>
- `cargo test -p hp41-core` — full green.
- `cargo test -p hp41-core --test math1_user_callback` — 3 of 5 cases pass; 2 remain #[ignore]d (clearly labeled "filled by Plan 28-08/09").
- `cargo test -p hp41-core --lib math1::integ::tests::call_stack_full` — pre-mutation cap verified; no leak.
- `cargo test -p hp41-core --lib math1::integ::tests::cancel_per_64_samples` — cancellation cadence verified.
- Documented per-program scratch register clobber test passes (asserts wrong answer, NOT an error — user-responsibility).
- SC-4 invariant preserved.
- `docs/hp41-math1-divergences.md` exists with one entry (Phase 30 will extend; Phase 28 plants the first entry).
</verification>

<success_criteria>
- Op::Integ integrated through 3-place exhaustive match (dispatch + execute_op return InvalidOp; real impl runs in run_loop only).
- `run_loop` re-entrancy works: outer program clone reused; no 30 MB re-clone catastrophe (Pitfall 4 / C-28.5).
- Strict-reject of nested INTG (XROM-08 / ADR-002) works at op entry; state.integ_state.is_some() check fires BEFORE any mutation.
- Pre-mutation call_stack 4-deep cap works (Pitfall 4 / D-22.15 invariant generalized).
- Per-64-samples cancellation check fires correctly (D-28.7 / D-28.8 plumbing; wiring lands Phase 31).
- INTG-08 convergence threshold tied to DisplayMode verified by 2 cases under Fix(4) vs Fix(9).
- INTG-07 subdivision cap 32768 enforced.
- User-program scratch register clobber documented as user-responsibility (RESEARCH Open Q6) — first entry in `docs/hp41-math1-divergences.md`.
- Plans 28-08 (SOLVE) and 28-09 (DIFEQ) can build directly on this user-callback infrastructure.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-SUMMARY.md`:
- integ_threshold value/formula consumed from ADR-004 (cite verbatim).
- Op::Integ + IntegState + IntegMode landed.
- run_loop re-entry pattern proven; 2 of 5 user-callback regression tests filled.
- docs/hp41-math1-divergences.md created with user-scratch-register-clobber entry.
- Cancellation per-64-samples plumbing in place; wiring deferred to Phase 31 / GUI-05.
- 34 entries in MATH_1.ops; xrom_shadowing.rs green.
- Plan 28-08 SOLVE and Plan 28-09 DIFEQ inherit the user-callback infrastructure verbatim.
</output>
