---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 10
type: execute
wave: 8
depends_on:
  - 28-01
  - 28-07
files_modified:
  - hp41-core/src/ops/math1/four.rs
  - hp41-core/src/ops/math1/tri.rs
  - hp41-core/src/ops/math1/trans.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - FOUR-01
  - FOUR-02
  - FOUR-03
  - FOUR-04
  - FOUR-05
  - FOUR-06
  - TRI-01
  - TRI-02
  - TRI-03
  - TRI-04
  - TRI-05
  - TRANS-01
  - TRANS-02
  - TRANS-03
  - TRANS-04
  - TRANS-05

must_haves:
  truths:
    - "8 new Op variants land in Op enum + dispatch() + execute_op() + MATH_1.ops: Op::Four (FOUR master), Op::TriSss, Op::TriAsa, Op::TriSaa, Op::TriSas, Op::TriSsa (5 triangle solvers), Op::Trans2d, Op::Trans3d (2 coordinate transforms)"
    - "FourInputStep extended (Plan 28-01 stubbed NumSamplesPrompt/NumFreqPrompt/FirstCoeffPrompt/SamplePrompt(u8)/Ready; Plan 28-10 adds RectTogglePrompt for FOUR-03 RECT? toggle); ModalProgram::current_prompt() owned-String arms: Four(NumSamplesPrompt) → \"NO. SAMPLES=?\", Four(NumFreqPrompt) → \"NO. FREQ=?\", Four(FirstCoeffPrompt) → \"1ST COEFF=?\", Four(SamplePrompt(idx)) → format!(\"Y{}=?\", idx+1), Four(RectTogglePrompt) → \"RECT?\", Four(Ready) → None"
    - "TransInputStep extended (Plan 28-01 stubbed InitPrompt/ForwardPrompt/InversePrompt; Plan 28-10 splits 2D and 3D modes per TRANS-01/TRANS-03): Trans(Init2dPrompt) → \"X0,Y0,θ?\" (TRANS-01 — 2D A-entry), Trans(Init3dOriginPrompt) → \"ORIGIN?\" (TRANS-03 — 3D A-entry), Trans(Init3dAxisPrompt) → \"AXIS+θ?\" (TRANS-03 — 3D B-entry), Trans(ForwardPrompt) → \"FWD?\", Trans(InversePrompt) → \"INV?\", Trans(Ready) → None"
    - "Op::Four (FOUR-01..06): opens 3-prompt master modal (NO. SAMPLES=? / NO. FREQ=? / 1ST COEFF=?) then iterates Y1=?..YN=? sample input per FOUR-02; on completion runs DFT computing up to 10 (aₙ, bₙ) pairs per FOUR-04; RECT? toggle per FOUR-03 chooses rectangular (aₙ, bₙ) or polar (cₙ, φₙ) coefficient form; uses R00–R26 scratch per FOUR-05; USER-mode E-key per FOUR-06 evaluates the Fourier series at time t after coefficient computation"
    - "FOUR USER-mode E-key (FOUR-06): when state.user_mode == true AND a prior FOUR coefficient computation completed (state.modal_program == Some(ModalProgram::Four(FourInputStep::Ready))), the keyboard E-key in CLI/GUI evaluates the series sum at time t (X register holds t pre-press). Implementation reuses the user-callback infrastructure pattern from Plan 28-07: the E-key dispatch routes to a NEW Op::FourEval variant (or is handled inline in Plan 28-10's op_four module). Plan 28-10 ships the evaluator function; CLI/GUI E-key routing lands in Phases 29/31 per the v2.1 USER-mode E-key precedent (App.tsx user_mode handling per CLAUDE.md v2.1 additions)"
    - "Op::TriSss (TRI-01): three sides (a, b, c) in X/Y/Z stack → all three angles (A, B, C) via Law of Cosines: A = acos((b²+c²-a²)/(2bc)), B = acos((a²+c²-b²)/(2ac)), C = π - A - B; angles in current angle_mode (deg/rad/grad per state.angle_mode); writes 3 lines to print_buffer; LiftEffect::Disable"
    - "Op::TriAsa (TRI-02): Angle-Side-Angle via Law of Sines per OM p.46 / RESEARCH line 602"
    - "Op::TriSaa (TRI-03): Side-Angle-Angle via Law of Sines per OM p.46 / RESEARCH line 603"
    - "Op::TriSas (TRI-04): Side-Angle-Side via Law of Cosines (find third side then Law of Sines for remaining angles) per OM p.46 / RESEARCH line 604"
    - "Op::TriSsa (TRI-05 — AMBIGUOUS CASE per RESEARCH line 605 / PATTERNS line 548): Side-Side-Angle has 0, 1, or 2 solutions; OM-conformes Behandeln means pattern-matching the OM 1979 worked-example display sequence — when discriminant > 1.0 push \"NO SOLUTION\" to print_buffer; when exactly 1 solution push it; when 2 solutions push both via the OM-cited display format (verify the EXACT OM display order during implementation and document it in code comment + test docstring)"
    - "Op::Trans2d (TRANS-01..02): A-entry initializes mode with (x₀, y₀, θ) per TRANS-01; C-entry forward transform per TRANS-02; E-entry inverse transform per TRANS-02; uses R00–R24 scratch per TRANS-05"
    - "Op::Trans3d (TRANS-03..04): A-entry initializes 3D with origin per TRANS-03; B-entry sets rotation axis (a, b, c, θ) per TRANS-03; C-entry forward transform via Rodrigues' rotation formula per TRANS-04 (v' = v·cos(θ) + (k×v)·sin(θ) + k·(k·v)·(1-cos(θ)) where k is the unit rotation axis); E-entry inverse transform per TRANS-04"
    - "Each of the 8 Ops has >= 5 tests per Pitfall 16 / QUAL-04 = >= 40 unit tests minimum; for Op::TriSsa explicitly include the ambiguous-case test per RESEARCH line 605 / TRI-05"
    - "Display arms in both prgm_display.rs files (CLAUDE.md SC-4-literal carve-out per planning context): 8 arms added to op_display_name match in hp41-cli/src/prgm_display.rs AND hp41-gui/src-tauri/src/prgm_display.rs: Op::Four → \"FOUR\", Op::TriSss → \"SSS\", Op::TriAsa → \"ASA\", Op::TriSaa → \"SAA\", Op::TriSas → \"SAS\", Op::TriSsa → \"SSA\", Op::Trans2d → \"TRANS\", Op::Trans3d → \"T3D\" (matching the xrom_resolve mnemonics)"
    - "No new user-callback infrastructure: FOUR's USER-mode E-key (FOUR-06) reuses Plan 28-07's run_loop re-entry pattern AS-IS — same pre-mutation call_stack cap, same XROM-08 strict-reject (now the FINAL 3-state form locked by Plan 28-09). Triangle and TRANS Ops are pure-data unary/multi-ary ops with no callbacks"
  nyquist_dimensions:
    - "FOUR-01..04 unit (modal flow + coefficient computation) — cargo test -p hp41-core --lib math1::four::tests"
    - "FOUR-03 unit (RECT? toggle: rectangular vs polar form) — cargo test -p hp41-core --lib math1::four::tests::rect_toggle_polar_form math1::four::tests::rect_toggle_rectangular_form"
    - "FOUR-05 unit (scratch R00–R26) — cargo test -p hp41-core --lib math1::four::tests::scratch_registers"
    - "FOUR-06 unit (USER-mode E-key Fourier series evaluation at time t) — cargo test -p hp41-core --lib math1::four::tests::user_mode_eval_at_t"
    - "TRI-01..04 unit — cargo test -p hp41-core --lib math1::tri::tests"
    - "TRI-05 unit (AMBIGUOUS SSA case — explicit test per RESEARCH line 605) — cargo test -p hp41-core --lib math1::tri::tests::ssa_ambiguous_two_solutions math1::tri::tests::ssa_no_solution math1::tri::tests::ssa_single_solution"
    - "TRANS-01..02 unit (2D forward + inverse) — cargo test -p hp41-core --lib math1::trans::tests::trans2d_forward math1::trans::tests::trans2d_inverse"
    - "TRANS-03..04 unit (3D Rodrigues forward + inverse) — cargo test -p hp41-core --lib math1::trans::tests::trans3d_rodrigues_forward math1::trans::tests::trans3d_rodrigues_inverse"
    - "TRANS-05 unit (scratch R00–R24) — cargo test -p hp41-core --lib math1::trans::tests::scratch_registers"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
    - "Numerical accuracy (OM-cited cases) — cargo test -p hp41-core --test numerical_accuracy four_cases tri_cases trans_cases"
    - "Pitfall 1 — cargo test -p hp41-core --test xrom_shadowing (FOUR/SSS/ASA/SAA/SAS/SSA/TRANS/T3D entries non-collision)"
  key_links:
    - from: hp41-core/src/ops/math1/four.rs
      to: hp41-core/src/ops/math1/integ.rs
      via: "FOUR-06 USER-mode E-key evaluation reuses Plan 28-07's run_loop re-entry pattern AS-IS (no new infrastructure)"
      pattern: "run_loop\\(state, program"
    - from: hp41-core/src/ops/math1/tri.rs
      to: hp41-core/src/state.rs
      via: "Triangle solvers read state.angle_mode for deg/rad/grad output formatting (same as v2.2 op_sin/op_cos angle-mode handling)"
      pattern: "state\\.angle_mode"
    - from: hp41-core/src/ops/math1/trans.rs
      to: hp41-core/src/state.rs
      via: "TRANS-05 / TRANS-01..04 use R00–R24 scratch via state.regs[0..25]; 2D mode reserves R00–R02, 3D mode reserves R00–R24"
      pattern: "state\\.regs"
    - from: hp41-core/src/ops/math1/tri.rs
      to: hp41-core/src/print.rs
      via: "Triangle solvers push 3 (or 2/4 for SSA ambiguous) output lines to state.print_buffer per OM display convention"
      pattern: "state\\.print_buffer\\.push"
---

<objective>
Land FOUR + Triangle Solutions + TRANS — the LAST Phase 28 plan. 8 new Op variants in 3 newly-created submodules: `four.rs` (1 master Op + USER-mode E-key evaluator per FOUR-06), `tri.rs` (5 triangle solvers including the OM-conforming SSA ambiguous-case handler per TRI-05), `trans.rs` (2D and 3D coordinate transforms with Rodrigues' rotation formula per TRANS-04). No new user-callback infrastructure — FOUR-06 reuses Plan 28-07's run_loop re-entry pattern AS-IS.

Purpose: closes FOUR-01..06, TRI-01..05, TRANS-01..05; brings the Math Pac I behavioral surface to 100% of the 110 v3.0 requirements covered in Phase 28; adds the OM-cited SSA-ambiguous-case explicit test per RESEARCH line 605 / PATTERNS line 548; adds USER-mode E-key Fourier-series evaluation per FOUR-06; locks the Math Pac I behavioral emulation surface for Phases 29 (CLI), 30 (docs), 31 (GUI).

Output: 3 new `hp41-core/src/ops/math1/{four,tri,trans}.rs` modules (~700 LOC combined with tests) + 8 new Op variants integrated through 3-place exhaustive match + 8 xrom_resolve entries + 8 prgm_display arms in BOTH CLI and GUI copies + ≥ 40 unit tests + OM-cited numerical_accuracy cases for each program.

Scope: 3 differentiator programs (FOUR + Triangles + TRANS) bundle into one plan because each is smaller than a single 28-05/06/07 plan and the milestone shape (10 plans) mirrors v2.2 Phase 25. RESEARCH §Suggested Plan Structure endorses this structure; the bundling preserves milestone symmetry and the per-module sub-tasks (Task 2 four.rs, Task 3 tri.rs, Task 4 trans.rs, Task 5 wiring) keep each task within the 10-30% context cost envelope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-PLAN.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-06-PLAN.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-PLAN.md
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/ops/math1/integ.rs
@hp41-core/src/ops/math.rs
@hp41-core/src/state.rs

<interfaces>
<!-- From RESEARCH §"FOUR (Plan 28-10)" lines 586-595 — Op table -->
```
| Op::Four (XEQ "FOUR") | NO. SAMPLES=? / NO. FREQ=? / 1ST COEFF=?
| Sample input          | Y1=? ... YN=?
| RECT? toggle          | Rectangular (aₙ, bₙ) vs polar (cₙ, φₙ) (FOUR-03)
| Pairs cap             | Up to 10 (aₙ, bₙ) pairs
| Scratch               | R00–R26
| USER-mode E-key       | Evaluates Fourier series at time t after coefficient computation (FOUR-06)
```

<!-- From RESEARCH §"Triangles (Plan 28-10)" lines 597-606 — Op table -->
```
| Op::TriSss | Law of Cosines: all three angles from three sides | OM p.46
| Op::TriAsa | Law of Sines | OM p.46
| Op::TriSaa | Law of Sines | OM p.46
| Op::TriSas | Law of Cosines | OM p.46
| Op::TriSsa | AMBIGUOUS CASE — two possible solutions; OM-conformes Behandeln per TRI-05; pattern-match from OM worked examples | OM p.46
```

<!-- From RESEARCH §"TRANS — coordinate transformations (Plan 28-10)" lines 607-614 — Op table -->
```
| Op::Trans2d | A=init (x₀, y₀, θ) → C=forward → E=inverse
| Op::Trans3d | A=(origin) → B=(a, b, c, θ) rotation-axis → C=forward → E=inverse via Rodrigues' rotation formula
| Scratch     | R00–R24
```

<!-- From PATTERNS §"Plan 28-10 — FOUR + Triangles + TRANS" lines 541-550 -->
```
- TRI-05 ambiguous SSA case: pattern-match from OM worked examples; output mirrors OM display sequence
  (CONTEXT.md Claude's Discretion).
- FOUR USER-mode E-key (FOUR-06): post-coefficient-computation evaluation at time t.
  Depends on user-callback infrastructure from Plan 28-07.
- Keep as one plan unless plan-checker flags oversized (RESEARCH §"Revision suggestion" line 866).
```

<!-- Rodrigues' rotation formula (TRANS-04). Given a 3D vector v, a unit rotation axis k, and angle θ:
       v_rotated = v·cos(θ) + (k × v)·sin(θ) + k·(k·v)·(1 - cos(θ))
   The "k·(k·v)" term is the dot-product k·v multiplied by the vector k.
-->

<!-- From PATTERNS Pattern 3 / `hp41-cli/src/prgm_display.rs:27-66` — display-arm template. Plan 28-10 adds 8 arms in BOTH the CLI and GUI copies. -->
```
fn op_display_name(op: &Op) -> String {
    match op {
        // ...
        Op::Four    => "FOUR".to_string(),    // Plan 28-10
        Op::TriSss  => "SSS".to_string(),
        Op::TriAsa  => "ASA".to_string(),
        Op::TriSaa  => "SAA".to_string(),
        Op::TriSas  => "SAS".to_string(),
        Op::TriSsa  => "SSA".to_string(),
        Op::Trans2d => "TRANS".to_string(),
        Op::Trans3d => "T3D".to_string(),
        // ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FourInputStep + TransInputStep + ModalProgram::current_prompt for FOUR + TRANS workflows</name>
  <files>hp41-core/src/ops/math1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Plan 28-09 — owned-String current_prompt signature from Plan 28-06 Task 1; existing FourInputStep stubs from Plan 28-01: NumSamplesPrompt/NumFreqPrompt/FirstCoeffPrompt/SamplePrompt(u8)/Ready; existing TransInputStep stubs: InitPrompt/ForwardPrompt/InversePrompt)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"FOUR" Op table lines 586-595; §"TRANS" Op table lines 607-614)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md (`<decisions>` D-28.4 modal_prompt String type)
    - .planning/REQUIREMENTS.md (FOUR-01..06, TRANS-01..05 — verbatim prompt strings)
  </read_first>
  <behavior>
    - `FourInputStep` extended: NumSamplesPrompt, NumFreqPrompt, FirstCoeffPrompt, RectTogglePrompt (NEW for FOUR-03), SamplePrompt(u8), Ready.
    - `TransInputStep` extended (replaces Plan 28-01's generic InitPrompt/ForwardPrompt/InversePrompt with 2D/3D-specific variants):
      - Init2dPrompt — TRANS-01 A-entry 2D init "(x₀, y₀, θ)"
      - Init3dOriginPrompt — TRANS-03 A-entry 3D origin
      - Init3dAxisPrompt — TRANS-03 B-entry 3D rotation axis "(a, b, c, θ)"
      - ForwardPrompt — TRANS-02/04 C-entry forward transform
      - InversePrompt — TRANS-02/04 E-entry inverse transform
      - Ready (NEW arm)
    - `ModalProgram::current_prompt()` exhaustive match arms (owned-String):
      - Four(NumSamplesPrompt) → Some("NO. SAMPLES=?".to_string())
      - Four(NumFreqPrompt) → Some("NO. FREQ=?".to_string())
      - Four(FirstCoeffPrompt) → Some("1ST COEFF=?".to_string())
      - Four(RectTogglePrompt) → Some("RECT?".to_string())
      - Four(SamplePrompt(idx)) → Some(format!("Y{}=?", idx + 1))
      - Four(Ready) → None
      - Trans(Init2dPrompt) → Some("X0,Y0,θ?".to_string())
      - Trans(Init3dOriginPrompt) → Some("ORIGIN?".to_string())
      - Trans(Init3dAxisPrompt) → Some("AXIS+θ?".to_string())
      - Trans(ForwardPrompt) → Some("FWD?".to_string())
      - Trans(InversePrompt) → Some("INV?".to_string())
      - Trans(Ready) → None
    - Preserves exhaustive match (no `_ =>` per FN-CLI-04 invariant from Phase 25).
  </behavior>
  <action>
    Extend `FourInputStep` enum: add `RectTogglePrompt` arm per FOUR-03. Existing variants (NumSamplesPrompt, NumFreqPrompt, FirstCoeffPrompt, SamplePrompt(u8), Ready) from Plan 28-01 stay.

    Replace `TransInputStep` enum's generic variants with the 6 specific variants per behavior. The Plan 28-01 variants InitPrompt/ForwardPrompt/InversePrompt are replaced by the 2D/3D-split set. Document the schema revision in a module-doc comment AND in this plan's SUMMARY ("TransInputStep schema revised from generic Init/Forward/Inverse to 2D/3D-split per Plan 28-10 TRANS-01..04 prompt requirements; no downstream consumers existed for the placeholder Plan 28-01 variants, so the change is safe").

    All variants derive Debug, Clone, PartialEq.

    Fill the `ModalProgram::current_prompt()` exhaustive match's Four and Trans arms with all sub-variants per behavior. Use owned-String values via `.to_string()` or `format!`.

    Add unit tests to inline `#[cfg(test)] mod tests` covering each new Four and Trans variant's prompt. Mirror the Plan 28-07/28-08/28-09 Task 1 test pattern.

    Do NOT touch other ModalProgram arms (Matrix, Poly, Integ, Solve, Difeq) — they were closed by prior plans.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::modal::tests</automated>
  </verify>
  <done>6 FourInputStep variants + 6 TransInputStep variants return correct prompts via current_prompt(); tests green; exhaustive match preserved; module-doc explains the TransInputStep schema revision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement four.rs (Op::Four master + DFT coefficient computation + USER-mode E-key evaluator)</name>
  <files>
    hp41-core/src/ops/math1/four.rs
    hp41-core/src/ops/math1/mod.rs
    hp41-core/src/ops/program.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Task 1)
    - hp41-core/src/ops/math1/integ.rs (Plan 28-07 — USER-mode E-key evaluator REUSES the run_loop re-entry pattern AS-IS)
    - hp41-core/src/ops/program.rs (full file — `run_loop` signature; the Op::Integ/Solve/Sol/Difeq run_loop arms from Plans 28-07/28-08/28-09)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-PLAN.md (Task 2 — user-callback infrastructure being reused for FOUR-06 USER-mode E-key)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"FOUR" lines 586-595)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-10 — FOUR + Triangles + TRANS" lines 541-550)
    - .planning/REQUIREMENTS.md (FOUR-01..06 verbatim wording)
    - HP-41C Math Pac OM Chapter on Fourier Series (cite OM page during implementation)
    - hp41-core/src/format.rs (`format_hpnum` signature for print_buffer formatting)
  </read_first>
  <behavior>
    - Module-level constants:
      - `pub const MAX_FOURIER_PAIRS: u8 = 10;` (per FOUR-04)
      - `pub const SCRATCH_RANGE: std::ops::Range<usize> = 0..27;` (R00–R26 per FOUR-05)
    - `pub fn op_four(state: &mut CalcState) -> Result<(), HpError>` — master entry; opens modal: sets `state.modal_program = Some(ModalProgram::Four(FourInputStep::NumSamplesPrompt))`, `state.modal_prompt = Some("NO. SAMPLES=?".to_string())`; LiftEffect::Neutral.
    - DFT coefficient computation per FOUR-01..04 / OM: given N samples Y1..YN and L = number of frequencies (NO. FREQ?):
      - aₙ = (2/N) · Σ_{k=1..N} Yₖ · cos(2π·n·k/N) for n=0..L
      - bₙ = (2/N) · Σ_{k=1..N} Yₖ · sin(2π·n·k/N) for n=1..L
      - Store (aₙ, bₙ) pairs in scratch R00..R{2L} per OM convention (verify exact register layout during implementation against OM); cap L at MAX_FOURIER_PAIRS.
    - RECT? toggle per FOUR-03 (state.modal_program transitions to RectTogglePrompt after coefficient computation):
      - Rectangular: leaves (aₙ, bₙ) form in scratch
      - Polar: converts to (cₙ = √(aₙ² + bₙ²), φₙ = atan2(bₙ, aₙ)) per OM convention; writes polar form to the same scratch slots
    - `pub fn op_four_eval_at_t(state: &mut CalcState, program: &[Op], user_label: &str, t: HpNum) -> Result<HpNum, HpError>` — USER-mode E-key evaluator per FOUR-06: given a time t, computes the Fourier-series sum
      - f(t) = a₀/2 + Σ_{n=1..L} (aₙ·cos(2π·n·t/T) + bₙ·sin(2π·n·t/T))
      where T = period (typically N samples × sample-step). Reads (aₙ, bₙ) from R00..R{2L} (post-FOUR computation). Returns the sum.
    - The Phase 28 / Phase 29 boundary for FOUR-06: Plan 28-10 ships the evaluator FUNCTION (`op_four_eval_at_t`); the CLI/GUI E-key dispatch routing lands in Phases 29/31 per the v2.1 USER-mode E-key precedent (App.tsx user_mode handling per CLAUDE.md v2.1 additions). For Plan 28-10 verification, the evaluator is tested directly via unit tests that pre-stage scratch registers and assert the sum value.
    - LiftEffect::Neutral for op_four (it opens a modal, no stack-lift effect); op_four_eval_at_t is a pure function (no side effects on stack-lift; the caller — Phase 29 E-key handler — decides the LiftEffect).
    - DOES NOT reuse Plan 28-07's user-callback infrastructure for the DFT coefficient computation itself — the DFT is over user-PROVIDED Y values (entered via SamplePrompt modal), NOT a user LBL. The only callback connection is FOUR-06's USER-mode E-key, which evaluates the series AT a time t with NO user-LBL involvement after coefficients are computed. So op_four_eval_at_t does NOT need to call run_loop. (Re-read RESEARCH §"FOUR" line 595 + REQUIREMENTS FOUR-06 — the USER-mode E-key evaluates the series at time t given the coefficients; there is no second user-LBL function involved.)
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/four.rs`. File header: Free42-contamination disclaimer.

    Module-level: constants per behavior.

    Implement:
    - `pub fn op_four(state: &mut CalcState) -> Result<(), HpError>` — master entry per behavior.
    - `pub(super) fn compute_dft(state: &mut CalcState, samples: &[HpNum], num_freq: u8) -> Result<Vec<(HpNum, HpNum)>, HpError>` — private DFT helper; iterates the cosine/sine sums; caps num_freq at MAX_FOURIER_PAIRS.
    - `pub(super) fn convert_to_polar(pairs: &[(HpNum, HpNum)]) -> Vec<(HpNum, HpNum)>` — RECT?-toggle polar form per FOUR-03; uses `complex_atan2` from Plan 28-03's complex.rs (consistent atan2 idiom) or recomputes via Rust f64 bridge per consistency with the rest of math1.
    - `pub(super) fn op_four_eval_at_t(state: &CalcState, t: HpNum, period: HpNum, num_freq: u8) -> Result<HpNum, HpError>` — USER-mode E-key evaluator per FOUR-06.

    Add `pub mod four;` to `hp41-core/src/ops/math1/mod.rs`.

    Inline `#[cfg(test)] mod tests` (file-scope `#![allow(clippy::unwrap_used)]`):
    - `master_op_opens_modal`: assert Op::Four sets modal_program = Some(Four(NumSamplesPrompt)) and modal_prompt = Some("NO. SAMPLES=?").
    - `dft_constant_signal`: 4 samples all equal to 5.0; a₀ = 10.0, all other coefficients 0.
    - `dft_pure_sine`: N=8 samples of sin(2π·k/8); a₀ ≈ 0, a₁ ≈ 0, b₁ ≈ 1, all higher ≈ 0; assert `approx::assert_relative_eq!(max_relative = 1e-6)` (Pitfall 14).
    - `dft_pure_cosine`: N=8 samples of cos(2π·k/8); a₁ ≈ 1, b₁ ≈ 0, all higher ≈ 0.
    - `rect_toggle_rectangular_form`: post-DFT scratch reads back as (aₙ, bₙ).
    - `rect_toggle_polar_form`: post-RECT? toggle scratch reads back as (cₙ = √(a²+b²), φₙ = atan2(b, a)).
    - `four_pairs_cap`: requesting NO. FREQ = 11 caps at MAX_FOURIER_PAIRS = 10 per FOUR-04; document whether excess triggers an error or silent cap (verify against OM).
    - `scratch_registers`: assert R00..R26 layout matches FOUR-05 (document the exact mapping in test docstring).
    - `user_mode_eval_at_t`: pre-stage coefficients (a₀, a₁, b₁) = (0, 1, 0), N=8, period=8.0; call op_four_eval_at_t at t=0 → 0.5·a₀ + a₁·cos(0) = 1.0; at t=2 → cos(2π·2/8) = 0; at t=4 → cos(π) = -1; assert each (within Pitfall-14 tolerance).
    - Target: ≥ 5 tests for Op::Four AND ≥ 5 tests for the FOUR-06 evaluator (the user_mode_eval_at_t case plus 4 more variations) = ≥ 10 tests minimum.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::four::tests</automated>
  </verify>
  <done>Op::Four compiles; DFT computation correct for constant/sine/cosine signals; RECT? toggle converts between rectangular and polar form; MAX_FOURIER_PAIRS = 10 cap enforced; scratch R00–R26 layout documented; FOUR-06 USER-mode evaluator returns correct series sum at given t; ≥ 10 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement tri.rs (5 triangle solvers including OM-conforming SSA ambiguous-case)</name>
  <files>
    hp41-core/src/ops/math1/tri.rs
    hp41-core/src/ops/math1/mod.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/four.rs (post-Task 2 — pattern for module-internal helpers)
    - hp41-core/src/ops/math.rs (lines 158-247 — angle_mode handling via to_radians_f64 / from_radians_f64 helpers; TRI solvers REUSE these helpers since their angle outputs follow state.angle_mode per HP-41C convention)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Triangles" lines 597-606 — explicit ambiguous-case note line 605)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-10" line 548 — "TRI-05 ambiguous SSA case: pattern-match from OM worked examples; output mirrors OM display sequence")
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md (Claude's Discretion section — OM display sequence pattern-match policy for ambiguous-case output)
    - .planning/REQUIREMENTS.md (TRI-01..05 verbatim wording)
    - HP-41C Math Pac OM page 46 (RESEARCH §canonical_refs) — Triangle Solutions (Law of Sines, Law of Cosines, SSA ambiguous-case worked examples — TRANSCRIBE the EXACT display sequence into a code comment + test docstring during implementation)
    - hp41-core/src/state.rs (state.angle_mode field, state.print_buffer field)
  </read_first>
  <behavior>
    - All 5 triangle solvers operate on stack values (a, b, c, A, B, C as needed) and write 3 output lines to state.print_buffer per OM display convention. LiftEffect::Disable (consumes stack input; produces print-buffer output, no new stack value).
    - `op_tri_sss(state: &mut CalcState) -> Result<(), HpError>` (TRI-01): three sides (a, b, c) in X/Y/Z stack → all three angles via Law of Cosines:
      - A = acos((b²+c²-a²)/(2bc))
      - B = acos((a²+c²-b²)/(2ac))
      - C = π - A - B (in radians); convert to state.angle_mode for output via from_radians_f64
      - Domain check: if (b²+c²-a²)/(2bc) is outside [-1, 1] → Err(HpError::Domain) per "triangle inequality violation" (a > b+c case)
      - print_buffer pushes: "A=<v>", "B=<v>", "C=<v>" (angles in current angle_mode)
    - `op_tri_asa(state: &mut CalcState) -> Result<(), HpError>` (TRI-02): Angle-Side-Angle (A, c, B) → solve for (b, a, C) via Law of Sines. Domain check: A + B >= π → Domain. Output: "C=<v>", "a=<v>", "b=<v>".
    - `op_tri_saa(state: &mut CalcState) -> Result<(), HpError>` (TRI-03): Side-Angle-Angle (a, A, B) → solve for (b, c, C) via Law of Sines. Domain check same as ASA. Output: "C=<v>", "b=<v>", "c=<v>".
    - `op_tri_sas(state: &mut CalcState) -> Result<(), HpError>` (TRI-04): Side-Angle-Side (b, A, c) → find a via Law of Cosines (a² = b² + c² - 2bc·cos(A)), then B and C via Law of Sines. Output: "a=<v>", "B=<v>", "C=<v>".
    - `op_tri_ssa(state: &mut CalcState) -> Result<(), HpError>` (TRI-05 AMBIGUOUS): Side-Side-Angle (a, b, A) — 0, 1, or 2 solutions:
      - Compute h = b·sin(A) (the height from C to side c)
      - If a < h → NO SOLUTION; push "NO SOLUTION" to print_buffer; return Ok(())
      - If a == h → ONE solution (right triangle); compute B = π/2, C = π/2 - A, c via Law of Sines; push 3 angle/side lines
      - If h < a < b → TWO solutions (ambiguous): compute B₁ = asin(b·sin(A)/a) and B₂ = π - B₁; for each, compute C and c; push 6 lines per OM-conforming display sequence (TRANSCRIBE the exact OM display order during implementation — typically "SOLUTION 1:", "B=<v>", "C=<v>", "c=<v>", "SOLUTION 2:", "B=<v>", "C=<v>", "c=<v>" but VERIFY against OM page 46)
      - If a >= b → ONE solution; compute and push 3 lines
      - LiftEffect::Disable
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/tri.rs`. File header: Free42-contamination disclaimer + module-doc comment naming OM page 46 as the algorithm source.

    Implement the 5 op fns per behavior. Each uses Rust f64 bridge for trig (mirroring v2.2 op_sin/op_asin pattern) with angle-mode conversion via `to_radians_f64` / `from_radians_f64` helpers from `hp41-core/src/ops/math.rs`.

    For Op::TriSsa, document the EXACT OM display sequence in a code comment at the top of `op_tri_ssa` (verify against OM page 46 during implementation; CONTEXT.md Claude's Discretion authorizes the pattern-match-from-OM approach). The choice between "SOLUTION 1:" / "SOLUTION 2:" prefix vs "A=", "B=" labels vs other formatting is governed by what the OM shows — re-implement the OM display order verbatim.

    Add `pub mod tri;` to `hp41-core/src/ops/math1/mod.rs`.

    Inline `#[cfg(test)] mod tests`:
    - `sss_equilateral`: a=b=c=1 → A=B=C=60° (or π/3 rad).
    - `sss_right_triangle`: a=3, b=4, c=5 → A=acos(0.8)≈36.87°, B=acos(0.6)≈53.13°, C=90°.
    - `sss_triangle_inequality_violation`: a=10, b=2, c=2 → Err(HpError::Domain).
    - `asa_basic`: A=60°, c=10, B=60° → equilateral C=60°, a=b=10.
    - `saa_basic`: a=10, A=30°, B=60° → C=90°, b≈17.32, c=20.
    - `sas_basic`: b=3, A=60°, c=4 → a²=9+16-2·12·cos(60°)=25-12=13 → a≈3.606.
    - `ssa_single_solution`: a=10, b=5, A=30° → unique solution (a > b case).
    - `ssa_no_solution`: a=1, b=5, A=30° → "NO SOLUTION" in print_buffer (a < b·sin(A) = 2.5).
    - `ssa_ambiguous_two_solutions`: a=5, b=8, A=30° → h=8·0.5=4, h < a=5 < b=8 → 2 solutions; assert print_buffer length matches OM display sequence (transcribe exact line count + format during implementation).
    - `ssa_right_triangle_edge`: a=4, b=8, A=30° → exactly 1 solution (a == h = 4).
    - Tests for state.angle_mode: same inputs under deg/rad/grad → angles formatted in current mode.
    - Target: ≥ 5 tests per Op = ≥ 25 tests minimum (the test list above clearly exceeds this).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::tri::tests</automated>
  </verify>
  <done>5 triangle solvers compile; SSS Law of Cosines correct (equilateral + 3-4-5 right triangle); ASA/SAA Law of Sines correct; SAS Law of Cosines correct; SSA ambiguous-case handles 0/1/2 solutions per OM display sequence (transcribed verbatim from OM p.46); domain errors fire on triangle inequality violations; angle outputs respect state.angle_mode; ≥ 25 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement trans.rs (2D and 3D coordinate transforms with Rodrigues rotation formula)</name>
  <files>
    hp41-core/src/ops/math1/trans.rs
    hp41-core/src/ops/math1/mod.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/four.rs (post-Task 2)
    - hp41-core/src/ops/math1/tri.rs (post-Task 3)
    - hp41-core/src/ops/math.rs (angle_mode handling — TRANS uses state.angle_mode for θ inputs)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"TRANS" lines 607-614)
    - .planning/REQUIREMENTS.md (TRANS-01..05 verbatim wording)
    - HP-41C Math Pac OM Chapter on Coordinate Transformations (cite OM page during implementation)
    - hp41-core/src/state.rs (state.regs[0..25] for scratch R00–R24 per TRANS-05)
  </read_first>
  <behavior>
    - Module-level constants:
      - `pub const TRANS_2D_SCRATCH_RANGE: std::ops::Range<usize> = 0..3;` (R00–R02 for 2D — x₀, y₀, θ)
      - `pub const TRANS_3D_SCRATCH_RANGE: std::ops::Range<usize> = 0..25;` (R00–R24 for 3D per TRANS-05)
    - `pub fn op_trans2d(state: &mut CalcState) -> Result<(), HpError>` (TRANS-01..02): opens modal with `state.modal_program = Some(ModalProgram::Trans(TransInputStep::Init2dPrompt))`, prompt "X0,Y0,θ?"; subsequent Forward/Inverse routed via modal sub-states.
    - 2D init (A-entry per TRANS-01): user enters (x₀, y₀, θ) via prompt; stored in R00 (x₀), R01 (y₀), R02 (θ in current angle_mode).
    - 2D forward (C-entry per TRANS-02): given input (x, y) in stack X/Y, computes:
      - x' = (x - x₀)·cos(θ) + (y - y₀)·sin(θ)
      - y' = -(x - x₀)·sin(θ) + (y - y₀)·cos(θ)
      - Push (x', y') to stack X/Y; LiftEffect::Enable
    - 2D inverse (E-entry per TRANS-02): given (x', y') in stack, computes:
      - x = x₀ + x'·cos(θ) - y'·sin(θ)
      - y = y₀ + x'·sin(θ) + y'·cos(θ)
      - Push (x, y) to stack X/Y; LiftEffect::Enable
    - `pub fn op_trans3d(state: &mut CalcState) -> Result<(), HpError>` (TRANS-03..04): opens modal with `state.modal_program = Some(ModalProgram::Trans(TransInputStep::Init3dOriginPrompt))`, prompt "ORIGIN?".
    - 3D init (A-entry per TRANS-03): user enters origin (x₀, y₀, z₀); B-entry user enters rotation axis (a, b, c, θ); stored in R00..R06 (origin x₀/y₀/z₀, axis a/b/c, θ).
    - 3D forward (C-entry per TRANS-04 via Rodrigues' rotation formula): given input (x, y, z), let v = (x-x₀, y-y₀, z-z₀); let k = unit-normalize(a, b, c); compute:
      - v_rotated = v·cos(θ) + (k × v)·sin(θ) + k·(k·v)·(1 - cos(θ))
      - Push (v_rotated.x, v_rotated.y, v_rotated.z) to stack X/Y/Z; LiftEffect::Enable
    - 3D inverse (E-entry per TRANS-04): rotate by -θ (Rodrigues with negative angle); add back the origin.
    - Helper functions:
      - `fn cross_product_3d(a: (HpNum, HpNum, HpNum), b: (HpNum, HpNum, HpNum)) -> (HpNum, HpNum, HpNum)` — standard cross product.
      - `fn dot_product_3d(a: (HpNum, HpNum, HpNum), b: (HpNum, HpNum, HpNum)) -> HpNum` — standard dot product.
      - `fn normalize_3d(a: HpNum, b: HpNum, c: HpNum) -> Result<(HpNum, HpNum, HpNum), HpError>` — unit-normalize; if |(a, b, c)| < EPSILON → Err(HpError::Domain) ("zero-length axis").
      - `fn rodrigues_rotate(v: (HpNum, HpNum, HpNum), k: (HpNum, HpNum, HpNum), theta: HpNum) -> (HpNum, HpNum, HpNum)` — implements the Rodrigues formula verbatim.
    - LiftEffect: master entry ops (Trans2d, Trans3d) are Neutral (open modal only); the subsequent forward/inverse sub-ops produce stack output with Enable.
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/trans.rs`. File header: Free42-contamination disclaimer + module-doc comment naming OM Chapter on Coordinate Transformations + the explicit Rodrigues formula derivation per TRANS-04.

    Implement the helper functions (cross_product_3d, dot_product_3d, normalize_3d, rodrigues_rotate) as module-private. Each carries a doc comment naming its mathematical formula.

    Implement `pub fn op_trans2d` and `pub fn op_trans3d` per behavior (master entry; opens modal). The actual forward/inverse computations live in private fns `do_trans2d_forward / do_trans2d_inverse / do_trans3d_forward / do_trans3d_inverse` invoked by the modal-completion handler (Plan 28-10 ships the helpers; CLI/GUI modal routing lands in Phases 29/31 — tested directly via unit tests that stage the scratch registers manually and call the private helpers via a `#[cfg(test)] pub(super) use` re-export).

    Add `pub mod trans;` to `hp41-core/src/ops/math1/mod.rs`.

    Inline `#[cfg(test)] mod tests`:
    - `trans2d_master_opens_modal`: Op::Trans2d sets modal_program correctly.
    - `trans2d_forward`: with (x₀, y₀, θ) = (0, 0, π/2) and input (1, 0) → forward should produce (0, -1). Test the rotation direction matches OM convention (verify during implementation — could be (0, 1) instead; transcribe the OM rotation sense).
    - `trans2d_inverse`: forward followed by inverse round-trips back to the input within 1e-7 tolerance.
    - `trans2d_origin_translation`: with (x₀, y₀, θ) = (5, 5, 0) and input (5, 5) → forward should produce (0, 0).
    - `trans3d_master_opens_modal`: Op::Trans3d sets modal_program correctly.
    - `trans3d_rodrigues_forward`: with origin = (0,0,0), axis = (0, 0, 1) (z-axis), θ = π/2, input = (1, 0, 0) → rotated = (0, 1, 0) (90° rotation about z-axis); assert `approx::assert_relative_eq!(max_relative = 1e-7)`.
    - `trans3d_rodrigues_inverse`: forward followed by inverse round-trips.
    - `trans3d_axis_normalization`: axis = (2, 0, 0) (non-unit) should still rotate correctly because normalize_3d unit-normalizes internally.
    - `trans3d_zero_axis_error`: axis = (0, 0, 0) → Err(HpError::Domain).
    - `trans3d_arbitrary_axis`: rotation about axis (1, 1, 1) normalized, θ = 2π/3, input = (1, 0, 0) → cyclic permutation (0, 1, 0) per Rodrigues identity; assert.
    - `scratch_registers`: assert R00–R24 layout matches TRANS-05 (document the exact mapping in test docstring).
    - Target: ≥ 5 tests per Op = ≥ 10 tests for Trans2d + Trans3d combined; the test list above exceeds this.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::trans::tests</automated>
  </verify>
  <done>Op::Trans2d + Op::Trans3d compile; 2D rotation + translation correct; 3D Rodrigues' formula verified against analytical test cases (z-axis 90° rotation; (1,1,1) axis 2π/3 cyclic permutation); forward/inverse round-trip within 1e-7 tolerance; zero-length axis returns Domain error; scratch R00–R24 layout documented; ≥ 10 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Wire Op enum + dispatch + execute_op + xrom_resolve for all 8 Ops; add OM-cited numerical_accuracy cases; add prgm_display arms (CLI + GUI)</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/numerical_accuracy.rs
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-core/src/ops/mod.rs (post-Plan 28-09 — Op enum + dispatch arms; grow with 8 new variants)
    - hp41-core/src/ops/program.rs (post-Plan 28-09 — execute_op arms; grow with 8 new arms)
    - hp41-core/src/ops/math1/xrom.rs (post-Plan 28-09 — 37 entries; grow to 45 by adding FOUR/SSS/ASA/SAA/SAS/SSA/TRANS/T3D)
    - hp41-cli/src/prgm_display.rs (full file — Plan 28-10 adds 8 arms per CLAUDE.md SC-4-literal carve-out)
    - hp41-gui/src-tauri/src/prgm_display.rs (full file — same as CLI; 8 arms)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-PLAN.md (Task 2 — proof-of-pattern for Op enum + dispatch + execute_op + xrom_resolve quadruple)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Resolver-Chain Conflict Map" lines 619-633 — FOUR/SSS/ASA/SAA/SAS/SSA/TRANS clean, no v2.2 conflicts)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§Pattern 3 lines 579-609 — 4-place exhaustive match)
  </read_first>
  <behavior>
    - 8 new Op variants integrated through Phase 28 3-place exhaustive match (Op enum + dispatch + execute_op):
      - `Op::Four`, `Op::TriSss`, `Op::TriAsa`, `Op::TriSaa`, `Op::TriSas`, `Op::TriSsa`, `Op::Trans2d`, `Op::Trans3d`
    - All 8 dispatch arms delegate to their respective op fns from Tasks 2/3/4 (these are NOT user-callback Ops — they execute fully from dispatch context, unlike Op::Integ/Solve/Sol/Difeq which only run inside run_loop).
    - execute_op arms delegate to dispatch identically (matches v2.2 pattern from PATTERNS Pattern 3 and the hyperbolics from Plan 28-02).
    - xrom_resolve registers 8 mnemonics: `("FOUR", Op::Four)`, `("SSS", Op::TriSss)`, `("ASA", Op::TriAsa)`, `("SAA", Op::TriSaa)`, `("SAS", Op::TriSas)`, `("SSA", Op::TriSsa)`, `("TRANS", Op::Trans2d)`, `("T3D", Op::Trans3d)`. After Plan 28-10, `MATH_1.ops` has 45 entries.
    - The mnemonic `"T3D"` for 3D transforms is a Plan 28-10 disambiguation choice (RESEARCH §"TRANS" line 612 wrote "via XEQ T3D or similar" — Plan 28-10 commits to `"T3D"` because TRANS is already registered for 2D mode). Document this choice in the SUMMARY.
    - numerical_accuracy.rs grows with ≥ 4 cases for FOUR, ≥ 5 cases for triangles (one per solver including SSA ambiguous), ≥ 4 cases for TRANS = ≥ 13 new cases total. Each carries OM citation + Free42 cross-check note per D-27.7 precedent.
    - prgm_display.rs (BOTH CLI and GUI copies per CLAUDE.md SC-4-literal carve-out): 8 arms added to op_display_name match.
    - xrom_shadowing.rs runs over 45 entries non-collision.
  </behavior>
  <action>
    1. Add 8 variants to the `Op` enum in `hp41-core/src/ops/mod.rs` under headers `// ── Phase 28: FOUR (Plan 28-10) ────────`, `// ── Phase 28: Triangle Solvers (Plan 28-10) ────────`, `// ── Phase 28: TRANS (Plan 28-10) ────────`. Add 8 dispatch arms delegating to the op fns from Tasks 2/3/4: `Op::Four => op_four(state)`, `Op::TriSss => op_tri_sss(state)`, ..., `Op::Trans3d => op_trans3d(state)`.

    2. Add 8 `execute_op` arms in `hp41-core/src/ops/program.rs` (delegating to dispatch per v2.2 idiom; mirrors hyperbolics Plan 28-02 Task 2 pattern — NOT the user-callback Plan 28-07/28-08/28-09 pattern). These Ops do NOT need run_loop arms because they are pure-data ops that execute fully from dispatch context.

    3. Grow `MATH_1.ops` and `math1_resolve` in `xrom.rs` with 8 entries per behavior. RESEARCH §"Resolver-Chain Conflict Map" lines 619-633 confirms all 8 mnemonics are clean (no v2.2 collision; in particular SSS/ASA/SAA/SAS/SSA triangle mnemonics are NOT in `xeq_by_name_local_resolve` or `builtin_card_op`).

    4. Add ≥ 13 cases to `hp41-core/tests/numerical_accuracy.rs` with OM citations per D-27.7 precedent. Cases minimum:
       - FOUR (≥ 4): `four_constant_signal` (a₀=2·avg, all others 0), `four_pure_sine` (b₁=1, all others 0), `four_pure_cosine` (a₁=1, all others 0), `four_rect_to_polar` (round-trip), `four_eval_at_t_polynomial` (USER-mode E-key check).
       - TRI (≥ 5, one per solver): `tri_sss_equilateral`, `tri_asa_basic`, `tri_saa_basic`, `tri_sas_basic`, `tri_ssa_ambiguous` (the OM-cited 2-solution example).
       - TRANS (≥ 4): `trans2d_pure_rotation`, `trans2d_pure_translation`, `trans3d_rodrigues_z_axis`, `trans3d_rodrigues_arbitrary_axis`, `trans3d_inverse_round_trip`.
       - Each `case!` doc-comment carries (a) OM page+example + (b) Free42 v3.0.5 oracle value per D-27.7. Pitfall 14 tolerance `max_relative = 1e-7` for analytical cases; `1e-5` for numerical-method cases like DFT.
       - Combined pass rate including v2.2 baseline + ALL Phase 28 plans (28-02..28-10) >= 98%; v1.x 503-case baseline floor 498/503 (D-27.6) UNCHANGED.

    5. Add display arms in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` per CLAUDE.md SC-4-literal carve-out from planning context:
       - In `hp41-cli/src/prgm_display.rs`, add to the `op_display_name` match: `Op::Four => "FOUR".to_string(),`, `Op::TriSss => "SSS".to_string(),`, `Op::TriAsa => "ASA".to_string(),`, `Op::TriSaa => "SAA".to_string(),`, `Op::TriSas => "SAS".to_string(),`, `Op::TriSsa => "SSA".to_string(),`, `Op::Trans2d => "TRANS".to_string(),`, `Op::Trans3d => "T3D".to_string(),`. Place under section headers `// ── Phase 28: FOUR / Triangle Solvers / TRANS (Plan 28-10) ────────`.
       - In `hp41-gui/src-tauri/src/prgm_display.rs`, add the SAME 8 arms in the same format. Per CLAUDE.md v2.2 additions: "Note: op_display_name is duplicated in both hp41-cli/src/prgm_display.rs and hp41-gui/src-tauri/src/prgm_display.rs — every new Op variant must be added in both copies."

    6. Re-run xrom_shadowing.rs — confirm 45 entries clear (37 from Plan 28-09 + 8 new from Plan 28-10). After Plan 28-10, MATH_1.ops covers the complete Math Pac I behavioral surface for Phase 28.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo build -p hp41-cli &amp;&amp; (cd hp41-gui/src-tauri &amp;&amp; cargo build) &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>8 Op variants integrated through 3-place exhaustive match (Op enum + dispatch + execute_op) AND 8 prgm_display arms in BOTH `hp41-cli` and `hp41-gui` copies (per CLAUDE.md SC-4-literal carve-out); 8 xrom_resolve entries (FOUR / SSS / ASA / SAA / SAS / SSA / TRANS / T3D); numerical_accuracy.rs +13 cases with OM citations covering FOUR/TRI/TRANS; xrom_shadowing.rs 45/45; per-Op meta-test confirms ≥ 5 tests per variant for all 8.</done>
</task>

</tasks>

<verification>
- `cargo build -p hp41-core` — full green; zero warnings under `#![deny(clippy::unwrap_used)]`.
- `cargo build -p hp41-cli` — confirms 8 new display arms in `hp41-cli/src/prgm_display.rs` keep the 4-place exhaustive match compiling.
- `(cd hp41-gui/src-tauri && cargo build)` — same for hp41-gui's duplicate copy.
- `cargo test -p hp41-core --lib math1::four::tests math1::tri::tests math1::trans::tests` — ≥ 45 unit tests across the 3 new modules pass (10 four + 25 tri + 10 trans = ≥ 45).
- `cargo test -p hp41-core --lib math1::tri::tests::ssa_ambiguous_two_solutions math1::tri::tests::ssa_no_solution math1::tri::tests::ssa_single_solution` — TRI-05 explicit ambiguous-case suite passes; OM-cited display sequence verified.
- `cargo test -p hp41-core --lib math1::four::tests::user_mode_eval_at_t` — FOUR-06 USER-mode E-key evaluator verified.
- `cargo test -p hp41-core --lib math1::trans::tests::trans3d_rodrigues_forward math1::trans::tests::trans3d_rodrigues_inverse math1::trans::tests::trans3d_zero_axis_error` — Rodrigues formula correctness + zero-axis Domain check verified.
- `cargo test -p hp41-core --test xrom_shadowing` — 45 entries clear (complete Math Pac I XEQ mnemonic surface).
- `cargo test -p hp41-core --test math1_op_test_count` — meta-test confirms each of the 8 new variants has ≥ 5 tests.
- `cargo test -p hp41-core --test numerical_accuracy` — combined pass rate ≥ 98 % including all v2.2 baseline + Phase 28 Plans 28-02..28-10 cases; v1.x 503-case baseline floor 498/503 preserved (D-27.6).
- SC-4 invariant preserved per CLAUDE.md stricter audit: `grep -rn "fn op_(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing (display-arm additions to `op_display_name` are display formatters, not calc/math logic per CLAUDE.md's documented exception).
- Phase 28 complete: 110/110 v3.0 Math Pac I requirements covered.
</verification>

<success_criteria>
- 8 new Op variants integrated through 3-place exhaustive match (Op enum + dispatch + execute_op) AND 8 prgm_display arms in BOTH `hp41-cli` and `hp41-gui` copies (per CLAUDE.md SC-4-literal carve-out from planning context).
- FOUR (FOUR-01..06): master entry opens 3-prompt modal; DFT computes up to 10 (aₙ, bₙ) pairs per FOUR-04; RECT? toggle per FOUR-03; USER-mode E-key evaluator per FOUR-06 returns the series sum at time t.
- Triangle solvers (TRI-01..05): 5 solvers cover SSS / ASA / SAA / SAS / SSA via Law of Sines / Law of Cosines; SSA ambiguous-case handles 0/1/2 solutions per OM display sequence (transcribed verbatim from OM p.46).
- TRANS (TRANS-01..05): 2D rotation+translation correct; 3D Rodrigues' rotation formula verified against analytical test cases; forward/inverse round-trip within 1e-7 tolerance; scratch R00–R24 layout documented per TRANS-05.
- All 8 mnemonics non-shadowing per RESEARCH §"Resolver-Chain Conflict Map" line 619-633.
- Per-Op test count ≥ 5 for all 8 new variants (Pitfall 16 / QUAL-04).
- numerical_accuracy.rs grows by ≥ 13 cases with OM citations; combined pass rate ≥ 98 % preserved.
- Phase 28 complete: 110/110 v3.0 Math Pac I requirements covered across Plans 28-01..28-10. Phase 29 (CLI integration), Phase 30 (docs + JSON pipeline), Phase 31 (GUI integration) inherit the complete `hp41-core` Math Pac I surface AS-IS (no new Op variants land after Phase 28).
- No new user-callback infrastructure introduced: FOUR-06 USER-mode E-key evaluator is a pure function that reads scratch registers and computes the series sum — no run_loop re-entry needed.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-10-SUMMARY.md`:
- 8 new Op variants (Op::Four + 5 triangle solvers + Op::Trans2d + Op::Trans3d) landed through 3-place exhaustive match + 8 prgm_display arms in both CLI and GUI copies.
- 3 new modules: four.rs (DFT + RECT? toggle + USER-mode E-key evaluator per FOUR-06), tri.rs (5 solvers including OM-conforming SSA ambiguous-case per TRI-05), trans.rs (2D + 3D Rodrigues' rotation formula).
- TransInputStep schema revised from generic Init/Forward/Inverse to 2D/3D-split (Init2dPrompt, Init3dOriginPrompt, Init3dAxisPrompt, ForwardPrompt, InversePrompt, Ready) — safe schema revision because the placeholder Plan 28-01 variants had no downstream consumers.
- T3D mnemonic chosen for Op::Trans3d (TRANS is 2D-only per the disambiguation in Plan 28-10).
- ≥ 45 unit tests across the 3 new modules; ≥ 13 OM-cited numerical_accuracy cases.
- 45 entries in MATH_1.ops; xrom_shadowing.rs green.
- Phase 28 complete: 110/110 v3.0 Math Pac I requirements covered. Phase 29 (CLI), Phase 30 (docs), Phase 31 (GUI) inherit the complete hp41-core Math Pac I surface unchanged.
- SC-4 invariant preserved (display-arm additions are display formatters, per CLAUDE.md's documented exception).
</output>
