---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 06
type: execute
wave: 6
depends_on:
  - 28-01
  - 28-05
files_modified:
  - hp41-core/src/ops/math1/matrix.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/math1_matrix_flow.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
  - MAT-05
  - MAT-06
  - MAT-07
  - MAT-08
  - MAT-09
  - MAT-10
  - MAT-11

must_haves:
  truths:
    - "INV_EPSILON const transcribed from OM via Plan 28-01 Task 1 ADR-003 is consumed by op_mat_inv as the singularity threshold (Pitfall 7 / MAT-07 / D-3 irreversible)"
    - "8 new Op variants in Op enum + dispatch() + execute_op() + MATH_1.ops: MatrixWorkflow (master), MatSize, MatVmat, MatEdit, MatDet, MatInv, MatSimeq, MatVcol"
    - "MatrixInputStep sub-enum (extends Plan 28-01's stubs in modal.rs): OrderPrompt, ElementPrompt(u8, u8) — column-major iteration row+col indices, Ready, EditPrompt, SimeqInputPrompt(u8), SimeqDone — exhaustive ModalProgram::current_prompt arms"
    - "Column-major storage (MAT-02): element A(r,c) lives at state.regs[matrix_active_reg + c * rows + r]; matrix_active_reg defaults to 15, matrix_dim stores (rows, cols) — both fields landed in Plan 28-01"
    - "Order N stored in R14 per MAT-02 OM convention"
    - "Max ORDER = 14 per MAT-09 OM-imposed cap (memory-bounded); request for ORDER > 14 returns HpError::OutOfRange (or equivalent existing variant)"
    - "Flag 4 set during input phase (active during ElementPrompt iteration); flag 5 set after MatSimeq column storage — uses crate::ops::flags::set_flag (existing v2.2 helper)"
    - "MatDet uses LU with partial pivoting (column-major access); det = product of pivot diagonal × sign-of-pivot per OM Chapter 4"
    - "MatInv uses Gauss-Jordan on [A | I]; singular detection via |pivot| < INV_EPSILON → state.modal_prompt = Some(\"NO SOLUTION\".to_string()) (NOT an HpError; surfaces in CLI/GUI as a display message)"
    - "MatSimeq solves [A | b] → [I | x]; solution at R(N+1)..R(2N); sets flag 5 after column storage; singular → \"NO SOLUTION\" modal_prompt"
    - "Plan 28-06 must NOT START until Plan 28-01 ADR-003 is committed with the OM-transcribed INV_EPSILON value — this dependency is enforced by depends_on: [28-01]"
    - "Per-Op test count >= 5: 8 Ops × 5 tests = >= 40 unit tests; plus the inv_singular, inv_near_singular, inv_back_sub_overflow trio for MAT-07"
  nyquist_dimensions:
    - "MAT-01..06 unit — cargo test -p hp41-core --lib math1::matrix::tests::basic"
    - "MAT-07 unit (3 cases: singular, near-singular, well-conditioned) — cargo test -p hp41-core --lib math1::matrix::tests::inv_singular math1::matrix::tests::inv_near_singular math1::matrix::tests::inv_well_conditioned"
    - "MAT-08..11 integration — cargo test -p hp41-core --test math1_matrix_flow"
    - "MAT-02 column-major storage — cargo test -p hp41-core --lib math1::matrix::tests::column_major"
    - "MAT-09 ORDER cap — cargo test -p hp41-core --lib math1::matrix::tests::order_cap"
    - "Numerical accuracy — cargo test -p hp41-core --test numerical_accuracy matrix_cases (OM page 14 worked example)"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
  key_links:
    - from: hp41-core/src/ops/math1/matrix.rs
      to: docs/adr/v3.0-003-inv-epsilon.md
      via: "INV_EPSILON const value sourced verbatim from this ADR (Plan 28-01 Task 1 OM transcription)"
      pattern: "INV_EPSILON"
    - from: hp41-core/src/ops/math1/matrix.rs
      to: hp41-core/src/state.rs
      via: "matrix_dim, matrix_active_reg fields read for r/c → register offset; modal_program for workflow state"
      pattern: "matrix_dim\\|matrix_active_reg"
    - from: hp41-core/src/ops/math1/matrix.rs
      to: hp41-core/src/ops/flags.rs
      via: "set_flag(state, 4) during input; set_flag(state, 5) after SIMEQ column storage"
      pattern: "set_flag"
---

<objective>
Land MATRIX — the most complex single Math Pac I program. 8 Op variants spanning workflow (MatrixWorkflow), inspection (MatSize, MatVmat, MatVcol), editing (MatEdit), and the three numerical kernels (MatDet, MatInv, MatSimeq). Consumes the OM-transcribed `INV_EPSILON` from Plan 28-01 ADR-003.

Purpose: validates the column-major matrix storage (MAT-02), the prompt-driven element-entry flow over a 14×14 grid (max), and the Gauss-Jordan inverse with hardware-faithful singularity threshold (MAT-07 / Pitfall 7). Math Pac I MATRIX is the "biggest single workflow" benchmark for the modal layer.

Output: `hp41-core/src/ops/math1/matrix.rs` (~600 LOC including tests) + 8 new Op variants + extended `MatrixInputStep` + `math1_matrix_flow.rs` integration test + ≥ 5 OM-cited numerical_accuracy cases (OM p.14 3×3 det example as the headline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@docs/adr/v3.0-003-inv-epsilon.md
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/ops/flags.rs
@hp41-core/src/state.rs
@hp41-core/src/ops/program.rs

<interfaces>
<!-- From RESEARCH §"MATRIX (Plan 28-06) — most complex single program" lines 523-545 — Op table -->
```
| Op::MatrixWorkflow | 10 | Master: ORDER=? (1..14) → SET SIZE → A1,1=? ... AN,N=? (column-major) → Ready
| Op::MatSize        | 10 | Returns ORDER from R14
| Op::MatVmat        | 10 | Sequentially displays A1,1= ... AN,N= via state.print_buffer
| Op::MatEdit        | 10 | ROW↑COL=? prompt → I ENTER J → Ai,j=? → new value
| Op::MatDet         | 14 | LU with partial pivoting; det = Πpivots × pivot-sign
| Op::MatInv         | 14 | Gauss-Jordan on [A | I]. INV_EPSILON from D-3. Singular → NO SOLUTION
| Op::MatSimeq       | 16 | B1=? ... BN=? → Gauss elimination → solution at R(N+1)..R(2N). Sets flag 5
| Op::MatVcol        | 16 | Displays B1..BN
```

<!-- From PATTERNS §"Plan 28-06 — MATRIX" lines 438-462 — column-major access -->
```
fn matrix_get(state: &CalcState, base: u8, rows: u8, _cols: u8, r: u8, c: u8)
    -> Result<HpNum, HpError>
{
    let idx = base as usize + (c as usize * rows as usize + r as usize);
    if idx >= state.regs.len() { return Err(HpError::InvalidOp); }
    Ok(state.regs[idx].clone())
}
```

<!-- From `hp41-core/src/ops/flags.rs` — Read for set_flag / get_flag idiom (referenced by MatSimeq flag-5 side effect) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MatrixInputStep + ModalProgram::current_prompt for matrix workflow</name>
  <files>hp41-core/src/ops/math1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Plan 28-05 — current Matrix variants)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Modal-State-Machine Layer" / §MATRIX — required prompts)
  </read_first>
  <behavior>
    - MatrixInputStep variants: OrderPrompt, ElementPrompt(u8, u8), Ready, EditPrompt, SimeqInputPrompt(u8), SimeqDone — all from Plan 28-01 already exist; extend `current_prompt` arms.
    - ModalProgram::current_prompt arms:
      - Matrix(OrderPrompt) → "ORDER=?"
      - Matrix(ElementPrompt(r, c)) → "A<r+1>,<c+1>=?" (1-indexed display per OM convention; e.g., (0,0) → "A1,1=?")
      - Matrix(Ready) → None
      - Matrix(EditPrompt) → "ROW↑COL=?"
      - Matrix(SimeqInputPrompt(idx)) → "B<idx+1>=?"
      - Matrix(SimeqDone) → None
  </behavior>
  <action>
    In `hp41-core/src/ops/math1/modal.rs`, fill the `ModalProgram::current_prompt()` exhaustive match's Matrix arm with all 6 sub-variants per behavior. Use `format!` to inject the 1-indexed numbers into "A<r>,<c>=?" and "B<n>=?" strings.

    Note: `current_prompt` currently returns `Option<&'static str>` per Plan 28-01 design. Strings like "A1,1=?" are NOT static when constructed via format!. Two options:
    - (a) Change return type to `Option<String>` (chosen — owned strings) and update Plan 28-01's modal.rs signature accordingly. Update test usages from `assert_eq!(..., Some("FOO"))` to `assert_eq!(..., Some("FOO".to_string()))`.
    - (b) Pre-allocate a const table — rejected (14×14 = 196 strings for ElementPrompt alone).

    Choose (a): broaden signature to `pub fn current_prompt(&self) -> Option<String>`. Update all callers (tests in modal.rs + any tests in poly.rs / state.rs). This is a small Plan 28-01 schema revision documented in this plan's SUMMARY.

    Update existing tests in modal.rs to use owned-string assertions; add new tests for the 6 Matrix variants per behavior.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::modal::tests</automated>
  </verify>
  <done>6 Matrix variants return correct owned-string prompts; signature change rippled correctly through all callers; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement matrix.rs (8 op fns + Gauss-Jordan + LU determinant)</name>
  <files>
    hp41-core/src/ops/math1/matrix.rs
    hp41-core/src/ops/math1/mod.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Task 1)
    - hp41-core/src/ops/flags.rs (full file — set_flag / get_flag signature)
    - hp41-core/src/ops/registers.rs (full file — register access via state.regs[idx])
    - docs/adr/v3.0-003-inv-epsilon.md (Plan 28-01 Task 1 — verbatim OM quote + exact INV_EPSILON value)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"MATRIX" Op table; §"State-Field Changes" matrix_dim/matrix_active_reg lines 681-687)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-06 — MATRIX" lines 438-462)
    - HP-41C Math Pac OM Chapter 4 — MATRIX algorithms (pp. 10-16)
  </read_first>
  <behavior>
    - `op_matrix_workflow`: opens modal (sets modal_program = Matrix(OrderPrompt), modal_prompt = "ORDER=?"); LiftEffect::Neutral.
    - `op_mat_size`: pushes value of R14 (= ORDER) to X via stack-lift Enable; LiftEffect::Enable.
    - `op_mat_vmat`: iterates A(0,0)..A(N-1,N-1) column-major; pushes lines to state.print_buffer in format `format!("A{},{}={}", r+1, c+1, format_hpnum(value))`. LiftEffect::Neutral.
    - `op_mat_edit`: opens modal (sets modal_program = Matrix(EditPrompt), modal_prompt = "ROW↑COL=?"); LiftEffect::Neutral.
    - `op_mat_det`: LU decomposition with partial pivoting on column-major storage; result = product of diagonal pivots × overall pivot-sign; writes to X via stack-lift Enable; LiftEffect::Enable.
    - `op_mat_inv`: Gauss-Jordan on augmented [A | I_N]; result overwrites the matrix in place (column-major); singular detection via `|pivot| < INV_EPSILON` (constant transcribed from ADR-003 per Plan 28-01 Task 1) → `state.modal_prompt = Some("NO SOLUTION".to_string())` AND returns Ok(()) (not an Err per RESEARCH line 531 "Singular → state.modal_prompt = Some(\"NO SOLUTION\".into()) (NOT an HpError)"). LiftEffect::Neutral.
    - `op_mat_simeq`: opens modal (sets modal_program = Matrix(SimeqInputPrompt(0)), modal_prompt = "B1=?"); user enters B vector; on completion runs Gauss elimination on [A | b]; stores solution at R(N+1)..R(2N); sets flag 5 via `crate::ops::flags::set_flag(state, 5)`; singular → "NO SOLUTION" modal_prompt; LiftEffect::Neutral.
    - `op_mat_vcol`: iterates B1..BN (= R(N+1)..R(2N)); pushes lines to state.print_buffer in format `format!("B{}={}", n+1, format_hpnum(value))`. LiftEffect::Neutral.
    - ORDER cap (MAT-09): any op accepting ORDER > 14 returns `HpError::OutOfRange`.
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/matrix.rs`. File header: Free42-contamination disclaimer.

    Module-level:
    - `pub const INV_EPSILON: HpNum = /* verbatim value from docs/adr/v3.0-003-inv-epsilon.md */;` — paste the ADR-003 ready-to-go Rust snippet. Add a doc comment quoting the OM page+paragraph.

    Helpers (module-local — `MatrixView` per PATTERNS Open Q3 (a) recommendation):
    - `fn matrix_get(state: &CalcState, r: u8, c: u8) -> Result<HpNum, HpError>` — column-major access using `state.matrix_active_reg.ok_or(InvalidOp)?` and `state.matrix_dim.ok_or(InvalidOp)?`. Pattern from PATTERNS lines 444-454.
    - `fn matrix_set(state: &mut CalcState, r: u8, c: u8, val: HpNum) -> Result<(), HpError>` — symmetric.
    - `pub(super) struct MatrixView<'a> { regs: &'a mut [HpNum], rows: u8, cols: u8 }` — borrow helper for kernel functions. Methods: `get(r,c) -> HpNum`, `set(r,c,HpNum)`, `det() -> HpNum`, `invert() -> Result<(), HpError>` (singular returns Err which op_mat_inv catches and converts to modal_prompt), `solve(b: &[HpNum]) -> Result<Vec<HpNum>, HpError>`.

    Implement the 8 op fns per behavior. Each fn checks `state.matrix_dim.is_some()` AND `state.matrix_active_reg.is_some()` before computation (returns `InvalidOp` if a matrix isn't declared).

    Add `pub mod matrix;` to `hp41-core/src/ops/math1/mod.rs`.

    Inline `#[cfg(test)] mod tests`:
    - `basic`: tests MatrixWorkflow opens "ORDER=?"; MatSize returns R14; MatVmat populates print_buffer in column-major order.
    - `column_major`: stores [1,2,3,4] as 2×2 matrix; asserts `matrix_get(0,0) == 1`, `(1,0) == 2`, `(0,1) == 3`, `(1,1) == 4` per column-major convention.
    - `order_cap`: setting ORDER=15 returns OutOfRange (MAT-09).
    - `inv_singular`: 2×2 matrix [[1,2],[2,4]] (det = 0) → modal_prompt = Some("NO SOLUTION").
    - `inv_near_singular`: matrix with `|pivot| < INV_EPSILON` → NO SOLUTION.
    - `inv_well_conditioned`: 2×2 [[2,0],[0,2]] (det = 4) → inverse is [[0.5,0],[0,0.5]] stored in place; modal_prompt is None.
    - `inv_back_sub_overflow`: a matrix that triggers back-substitution overflow during inverse → Overflow OR document the OM behavior in the test docstring.
    - `det_3x3`: OM page 14 worked example (write actual 3×3 from OM) → expected determinant from OM (Pitfall 14: `assert_relative_eq!(max_relative = 1e-7)`).
    - `simeq_solves`: 2×2 system [[1,1],[1,-1]] · [x,y] = [3,1] → x=2, y=1; assert flag 5 is set after.
    - `simeq_singular`: singular [A | b] → modal_prompt "NO SOLUTION".
    - LiftEffect assertions per op.

    File-scope `#![allow(clippy::unwrap_used)]` on test mod only.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::matrix::tests</automated>
  </verify>
  <done>8 op fns compile; INV_EPSILON consumed verbatim from ADR-003; ≥ 40 unit tests pass including the inv_singular / inv_near_singular / inv_well_conditioned trio; column-major storage verified; flag 4 / flag 5 side effects verified; LiftEffect declarations verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve; ship math1_matrix_flow.rs integration test + numerical_accuracy cases</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/math1_matrix_flow.rs
    hp41-core/tests/numerical_accuracy.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/xrom.rs (post-Plan 28-05 — 25 entries; grow to 33)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Resolver-Chain Conflict Map" — MATRIX sub-entries non-shadowing per line 626; specifically: `INV` matrix vs `Op::Inv` reciprocal — clean because v2.2 doesn't register `"INV"` as a name; verified)
  </read_first>
  <behavior>
    - 8 Op variants integrated through 3-place exhaustive match.
    - xrom_resolve registrations: "MATRIX" → MatrixWorkflow; "SIZE" → MatSize; "VMAT" → MatVmat; "EDIT" → MatEdit; "DET" → MatDet; "INV" → MatInv; "SIMEQ" → MatSimeq; "VCOL" → MatVcol.
    - math1_matrix_flow.rs integration test: simulates a full end-to-end matrix flow (XEQ MATRIX → 3 → enter 9 elements column-major → XEQ DET → result).
    - numerical_accuracy.rs grows with ≥ 5 cases from OM Chapter 4 (3×3 determinant from p.14, inverse round-trip, SIMEQ solution, singular detection at INV_EPSILON, large 14×14 stress).
    - xrom_shadowing.rs runs over 33 entries, all non-collision.
  </behavior>
  <action>
    1. Add 8 variants to `Op` enum in `hp41-core/src/ops/mod.rs` under header `// ── Phase 28: MATRIX (Plan 28-06) ────────`: MatrixWorkflow, MatSize, MatVmat, MatEdit, MatDet, MatInv, MatSimeq, MatVcol. Add 8 dispatch arms.
    2. Add 8 `execute_op` arms in `hp41-core/src/ops/program.rs`.
    3. Grow `MATH_1.ops` and `math1_resolve` in `xrom.rs` with 8 entries per behavior. Important: register `"INV"` as Op::MatInv — confirmed safe by RESEARCH §"Resolver-Chain Conflict Map" lines 636-638 (v2.2 has Op::Inv reciprocal but doesn't register the `"INV"` STRING in any resolver, so xrom claiming it is clean).
    4. Create `hp41-core/tests/math1_matrix_flow.rs` integration test:
       - File header: Free42-contamination disclaimer + `#![allow(clippy::unwrap_used)]`.
       - Test `matrix_full_flow_3x3_det`: simulate `Op::Xeq("MATRIX".to_string())` → expect modal_program = Matrix(OrderPrompt); push 3 to stack and simulate R/S submit; expect ElementPrompt(0,0); enter 9 values column-major; expect Ready; `Op::Xeq("DET".to_string())` → expect the OM p.14 determinant value.
       - Test `matrix_simeq_flag5`: 2×2 system; after SIMEQ completes, assert `crate::ops::flags::get_flag(&state, 5) == true`.
       - Test `matrix_inv_singular_no_solution`: 2×2 singular [[1,2],[2,4]] → modal_prompt = Some("NO SOLUTION"); no error.
    5. Add ≥ 5 cases to `hp41-core/tests/numerical_accuracy.rs` with `// Source: HP 00041-90034 p.14, ex.1` + `// Free42 v3.0.5: <expected>` citations.
    6. Re-run xrom_shadowing.rs — confirm 33 entries clear.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test math1_matrix_flow --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>8 Op variants integrated; xrom_resolve registers 8 mnemonics; math1_matrix_flow.rs ships 3 integration tests; numerical_accuracy.rs +5 cases with OM citation; xrom_shadowing.rs 33/33; per-Op meta-test ≥ 5 per variant.</done>
</task>

<task type="auto">
  <name>Task 4: Add op_display_name arms to both prgm_display.rs copies</name>
  <files>
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (full file — confirm `op_display_name` match is exhaustive with NO `_ =>` arm)
    - hp41-gui/src-tauri/src/prgm_display.rs (mirror copy — same exhaustive shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (analog excerpt §"Pattern 3 / op_display_name 4-place exhaustive match" — line shape to mirror)
    - CLAUDE.md (SC-4-literal carve-out: "op_display_name(...) in prgm_display.rs is a display formatter, not calculator logic — duplicated in both CLI and GUI copies; every new Op variant must be added in BOTH files")
  </read_first>
  <action>
    Add the 8 new Op variant arm(s) introduced by this plan to the `op_display_name` exhaustive `match` in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs`. Place each new arm under a `// ── Phase 28: MATRIX (Plan 28-06) ────────` section comment.

    Variants to add (matching the xrom_resolve mnemonic registered by Task 2 of this plan): Op::MatrixWorkflow => "MATRIX", Op::MatSize => "SIZE", Op::MatVmat => "VMAT", Op::MatEdit => "EDIT", Op::MatDet => "DET", Op::MatInv => "INV", Op::MatSimeq => "SIMEQ", Op::MatVcol => "VCOL".

    Preserve the existing exhaustive match shape — `op_display_name` has NO `_ =>` arm by design (FN-CLI-04 invariant from Phase 25 / CLAUDE.md v2.2 additions). The Rust compiler enforces every Op variant is named; a missing arm causes a workspace build failure in either `hp41-cli` or `hp41-gui/src-tauri` and that is the desired structural enforcement. Both copies stay in sync — the CLAUDE.md comment "every new Op variant must be added in both copies" is the canonical instruction.

    No production logic added here; this task is purely the SC-4-literal carve-out display formatting. ROADMAP Success Criterion #5 (every new Op variant appears in BOTH prgm_display copies) is satisfied for the 8 variant(s) introduced by Plan 28-06.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>8 new arm(s) appear in both prgm_display.rs files in the same order under the Phase 28 section header; `cargo build --workspace` passes (compile-time exhaustiveness confirms no Op variant is missing); no production code paths touched outside the two display files.</done>
</task>


</tasks>

<verification>
- `cargo test -p hp41-core` — full green.
- `cargo test -p hp41-core --lib math1::matrix::tests::inv_singular` — INV_EPSILON in action; verifies the OM-transcribed value from ADR-003 holds.
- `cargo test -p hp41-core --test math1_matrix_flow` — 3 integration tests pass (full workflow + flag 5 + NO SOLUTION).
- `cargo test -p hp41-core --test xrom_shadowing` — 33 entries non-collision (including the `"INV"` mnemonic confirmed clean).
- SC-4 invariant preserved.
- Column-major storage verified at the byte level (set R15..R23 for 3×3, read back via `matrix_get` confirms column-major mapping).
</verification>

<success_criteria>
- 8 Op variants integrated through Phase 28 3-place exhaustive match.
- INV_EPSILON value flows verbatim from ADR-003 (Plan 28-01 Task 1) into the singularity-detection branch of op_mat_inv.
- MAT-07 closed: 3 inv tests (singular, near-singular, well-conditioned) verify INV_EPSILON behaves correctly at the threshold.
- MAT-09 ORDER cap (≤ 14) enforced.
- MAT-10/MAT-11 flags 4 and 5 set on the correct workflow transitions.
- math1_matrix_flow.rs is the first full end-to-end modal-workflow integration test (master entry + element entry + sub-op execution); template for INTG / SOLVE / DIFEQ / FOUR / TRANS flow tests in later plans.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-06-SUMMARY.md`:
- INV_EPSILON value consumed from ADR-003 (cite verbatim from Plan 28-01).
- 8 Op variants land; MATRIX is the largest single Math Pac I program; modal layer validated.
- Column-major storage convention locked (R15+ from matrix_active_reg).
- Flag 4 / flag 5 side effects verified.
- 33 entries in MATH_1.ops; xrom_shadowing.rs green; `"INV"` mnemonic confirmed non-shadowing.
- math1_matrix_flow.rs is the integration-test pattern Plans 28-07/08/09/10 mirror.
</output>
