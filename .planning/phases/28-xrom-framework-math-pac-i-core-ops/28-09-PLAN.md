---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 09
type: execute
wave: 9
depends_on:
  - 28-01
  - 28-07
  - 28-08
files_modified:
  - hp41-core/src/ops/math1/difeq.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/math1_user_callback.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - DIFEQ-01
  - DIFEQ-02
  - DIFEQ-03
  - DIFEQ-04
  - DIFEQ-05

must_haves:
  truths:
    - "1 new Op variant lands in Op enum + dispatch() + execute_op() + MATH_1.ops: Op::Difeq (XEQ \"DIFEQ\") master with 5- or 6-prompt modal per DIFEQ-01: FUNCTION NAME? / ORDER=? (1 or 2) / STEP SIZE=? / X0=? / Y0=? (+ Y'0=? if ORDER=2)"
    - "DifeqState struct (Plan 28-01 stubbed as empty; Plan 28-09 grows it per PATTERNS line 583 / RESEARCH §DIFEQ table line 583): { user_label: String, order: u8 (1 or 2 — DIFEQ-01), step_size: HpNum, x: HpNum, y: HpNum, y_prime: Option<HpNum> (Some when order=2; None when order=1), step_count: u32 } with #[derive(Debug, Clone, Default)]"
    - "DifeqInputStep extended (Plan 28-01 stubbed FunctionNamePrompt/OrderPrompt/StepSizePrompt/X0Prompt/Y0Prompt/Y1PrimePrompt; Plan 28-09 adds Ready arm and conditional Y'0 prompt routing); ModalProgram::current_prompt() owned-String arms (signature from Plan 28-06): Difeq(FunctionNamePrompt) → \"FUNCTION NAME?\", Difeq(OrderPrompt) → \"ORDER=?\", Difeq(StepSizePrompt) → \"STEP SIZE=?\", Difeq(X0Prompt) → \"X0=?\", Difeq(Y0Prompt) → \"Y0=?\", Difeq(Y1PrimePrompt) → \"Y'0=?\", Difeq(Ready) → None"
    - "4th-order Runge-Kutta per DIFEQ-02 / OM-spec: k1 = h·f(x_n, y_n); k2 = h·f(x_n+h/2, y_n+k1/2); k3 = h·f(x_n+h/2, y_n+k2/2); k4 = h·f(x_n+h, y_n+k3); y_{n+1} = y_n + (k1+2k2+2k3+k4)/6 — for ORDER=1. ORDER=2 uses the system (y' = z, z' = f(x, y, z)) reduced to coupled RK4 per OM Chapter 7"
    - "User callback per DIFEQ-04: ORDER=1 calls f(x, y) → user-LBL receives x in X, y in Y; ORDER=2 calls f(x, y, y') → user-LBL receives x in X, y in Y, y' in Z; user-LBL returns f(x, y[, y']) in X"
    - "Step-by-step output per DIFEQ-05: each RK4 step pushes formatted line to state.print_buffer using format_hpnum: format!(\"X={} Y={}\", format_hpnum(x_n), format_hpnum(y_n)) (and Y'=<v> for ORDER=2)"
    - "Reuses INTG's user-program callback infrastructure: same run_loop re-entry pattern as op_integ_run_loop from Plan 28-07 (NOT run_program); mirrors Op::XeqInd precedent at program.rs:476-491 per PATTERNS line 583 (\"Re-entry: Same pattern as INTG/SOLVE\")"
    - "Pre-mutation 4-deep call_stack cap (Pitfall 4): state.call_stack.len() >= 4 → HpError::CallDepth BEFORE any mutation; identical guard to Plan 28-07's op_integ"
    - "XROM-08 strict-reject grows to its FINAL form per PATTERNS lines 531-534: state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() → HpError::InvalidOp BEFORE mutation. This is the canonical 3-state guard cited in D-28.7. Plan 28-09 is the last plan to grow this guard; subsequent plans inherit it unchanged"
    - "Per-64-steps cancellation check (D-28.8): step_count & 0x3F == 0 → state.cancel_requested.load(Ordering::Relaxed); if true, clears difeq_state and returns HpError::Canceled (D-28.9). Wiring of request_cancel lands Phase 31 / GUI-05"
    - "ORDER validation: only 1 and 2 are accepted per DIFEQ-01; modal entry for any other value writes state.modal_prompt = Some(\"ORDER MUST BE 1 OR 2\".to_string()) and re-prompts (or returns to OrderPrompt sub-state); does NOT return HpError"
    - "Op::Difeq dispatch arm returns Err(InvalidOp) when called OUTSIDE a run_loop context; real implementation runs ONLY in the run_loop match arm (mirrors Op::Integ from Plan 28-07 and Op::Solve from Plan 28-08)"
    - "Scratch R00–R07 per DIFEQ-03: integration uses R00 (x), R01 (y), R02 (y' for order=2), R03 (step_size), R04 (k1), R05 (k2), R06 (k3), R07 (k4) per OM convention; user-program-clobber of these registers is documented user-responsibility per RESEARCH Open Q6 / Plan 28-07's docs/hp41-math1-divergences.md entry"
    - "math1_user_callback.rs Plan 28-01 scaffold gets the FINAL #[ignore]'d placeholder filled (nested_difeq_inside_integ_rejected from Plan 28-07's leftover list); after Plan 28-09 all 5 original Plan 28-01 placeholders + 1 Plan 28-07 NEW user_fn_stores_to_scratch test + 1 Plan 28-08 NEW nested_solve_inside_solve test are filled — math1_user_callback.rs has zero `#[ignore]`'d cases"
    - "Per-Op test count >= 5 (Pitfall 16): Op::Difeq has >= 5 tests (rk4_order1_correctness, rk4_order2_correctness, step_by_step_print_buffer, cancellation, nested_rejection)"
    - "Display arm in both prgm_display.rs files (CLAUDE.md SC-4-literal carve-out per planning context): Op::Difeq → \"DIFEQ\" added to op_display_name match in hp41-cli/src/prgm_display.rs AND hp41-gui/src-tauri/src/prgm_display.rs"
  nyquist_dimensions:
    - "DIFEQ-01 unit (5- or 6-prompt modal flow) — cargo test -p hp41-core --lib math1::difeq::tests::modal_order1_flow math1::difeq::tests::modal_order2_flow"
    - "DIFEQ-02 unit (4th-order RK4 correctness against analytical solution) — cargo test -p hp41-core --lib math1::difeq::tests::rk4_order1_correctness math1::difeq::tests::rk4_order2_correctness"
    - "DIFEQ-03 unit (scratch R00–R07 usage) — cargo test -p hp41-core --lib math1::difeq::tests::scratch_registers"
    - "DIFEQ-04 integration (user callback for f(x,y) and f(x,y,y')) — cargo test -p hp41-core --test math1_user_callback"
    - "DIFEQ-05 unit (step-by-step print_buffer output) — cargo test -p hp41-core --lib math1::difeq::tests::step_by_step_print_buffer"
    - "XROM-08 final form (3-state guard) — cargo test -p hp41-core --test math1_user_callback nested_difeq_inside_integ_rejected"
    - "Pitfall 4 — cargo test -p hp41-core --lib math1::difeq::tests::call_stack_full"
    - "Pitfall 11 plumbing — cargo test -p hp41-core --lib math1::difeq::tests::cancel_per_64_steps"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
    - "Numerical accuracy (OM-cited RK4 reference cases) — cargo test -p hp41-core --test numerical_accuracy difeq_cases"
    - "Pitfall 1 — cargo test -p hp41-core --test xrom_shadowing (DIFEQ entry non-collision)"
  key_links:
    - from: hp41-core/src/ops/math1/difeq.rs
      to: hp41-core/src/ops/math1/integ.rs
      via: "op_difeq_run_loop mirrors op_integ_run_loop verbatim (same run_loop re-entry shape, same strict-reject, same pre-mutation call_stack cap, same per-64 cancellation check)"
      pattern: "op_difeq_run_loop"
    - from: hp41-core/src/ops/math1/difeq.rs
      to: hp41-core/src/ops/program.rs
      via: "run_loop re-entry from inside op_difeq for each RK4 sub-step (k1/k2/k3/k4 evaluations) — NOT run_program"
      pattern: "run_loop\\(state, program"
    - from: hp41-core/src/state.rs
      to: hp41-core/src/ops/math1/difeq.rs
      via: "state.difeq_state Option<DifeqState> field landed in Plan 28-01 alongside integ_state and solve_state (RESEARCH Open Q2 (a) — early commitment); Plan 28-09 grows DifeqState struct fields per PATTERNS line 583"
      pattern: "state\\.difeq_state"
    - from: hp41-core/src/ops/math1/difeq.rs
      to: hp41-core/src/print.rs
      via: "Step-by-step output writes formatted X=<v> Y=<v> [Y'=<v>] lines to state.print_buffer per DIFEQ-05"
      pattern: "state\\.print_buffer\\.push"
---

<objective>
Land DIFEQ — the THIRD and final user-callback program in v3.0. `Op::Difeq` (XEQ "DIFEQ") opens a 5- or 6-prompt modal (FUNCTION NAME? / ORDER=? / STEP SIZE=? / X0=? / Y0=? [+ Y'0=? if ORDER=2]) and runs 4th-order Runge-Kutta integration. Reuses the user-program callback infrastructure from Plan 28-07 verbatim AND grows the XROM-08 strict-reject guard to its FINAL canonical form (3-state: integ_state || solve_state || difeq_state) per PATTERNS lines 531-534.

Purpose: closes DIFEQ-01..05; closes the last `#[ignore]`'d Plan 28-01 user-callback scaffold case (nested_difeq_inside_integ_rejected); locks the 3-state XROM-08 guard for all downstream consumers; ships step-by-step RK4 output via `print_buffer` per DIFEQ-05.

Output: `hp41-core/src/ops/math1/difeq.rs` (~350 LOC including tests) with full `DifeqState` struct + `op_difeq` (dispatch stub) + `op_difeq_run_loop` (real impl) + private RK4 helpers; 1 new Op variant integrated through 3-place exhaustive match + xrom_resolve + 2 prgm_display.rs copies; ≥ 5 OM-cited RK4 reference cases in `numerical_accuracy.rs`; `math1_user_callback.rs` reaches zero `#[ignore]`'d cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-PLAN.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-08-PLAN.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-PLAN.md
@hp41-core/src/ops/math1/integ.rs
@hp41-core/src/ops/math1/solve.rs
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/ops/program.rs
@hp41-core/src/state.rs

<interfaces>
<!-- From Plan 28-07 op_integ_run_loop template (RESEARCH lines 293-330; PATTERNS lines 466-521) AND Plan 28-08 op_solve_run_loop — Plan 28-09 MIRRORS this structure verbatim with RK4-specific inner loop. -->

```
// op_difeq_run_loop skeleton — MIRRORS op_integ_run_loop / op_solve_run_loop
pub(super) fn op_difeq_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError> {
    // 1. Strict-reject (XROM-08 FINAL form — 3-state guard per PATTERNS lines 531-534)
    if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() {
        return Err(HpError::InvalidOp);
    }
    // 2. Pre-mutation 4-deep call_stack cap (Pitfall 4)
    if state.call_stack.len() >= 4 {
        return Err(HpError::CallDepth);
    }
    // 3. Reset cancellation flag (D-28.7)
    state.cancel_requested.store(false, Ordering::Relaxed);
    // 4. Initialize DifeqState from modal flow
    let order: u8 = /* read from modal-staged OrderPrompt — 1 or 2 */;
    state.difeq_state = Some(DifeqState { user_label, order, step_size, x: x0, y: y0, y_prime: if order==2 { Some(y_prime0) } else { None }, step_count: 0 });

    // 5. RK4 iteration loop (no fixed cap; runs until user stops via cancellation or via MAX_STEPS outer budget)
    loop {
        if state.difeq_state.as_ref().unwrap().step_count & 0x3F == 0 && state.cancel_requested.load(Ordering::Relaxed) {
            state.difeq_state = None;
            return Err(HpError::Canceled);
        }
        // Compute k1, k2, k3, k4 by re-entering run_loop on the user LBL (one re-entry per k)
        // For ORDER=1: pass x_n in X, y_n in Y; user LBL returns f(x_n, y_n) in X
        // For ORDER=2: pass x_n in X, y_n in Y, y'_n in Z; user LBL returns f(x_n, y_n, y'_n) in X
        // Update y_{n+1} = y_n + (k1 + 2*k2 + 2*k3 + k4) / 6
        // For ORDER=2 update y'_{n+1} via the coupled RK4 second component
        // Push step line to state.print_buffer per DIFEQ-05
        // Increment step_count
        // Continue until cancellation OR external break (user-driven via R/S in CLI, or outer MAX_STEPS)
    }
}
```

<!-- From PATTERNS lines 524-537: Plan 28-08 / 28-09 — SOLVE / DIFEQ excerpt.
"Strict-reject guard grows in Plan 28-09 to:
    if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some()
 Plan 28-01 ships `difeq_state` field alongside the others (RESEARCH Open Q2 (a) — early commitment for symmetric save-file backward-compat)." -->

<!-- From PATTERNS Pattern 3 / `hp41-cli/src/prgm_display.rs` — display-arm template.
Plan 28-09 adds 1 arm in BOTH the CLI and GUI copies. -->
```
fn op_display_name(op: &Op) -> String {
    match op {
        // ...
        Op::Difeq => "DIFEQ".to_string(),    // Plan 28-09
        // ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DifeqInputStep + ModalProgram::current_prompt for DIFEQ workflow</name>
  <files>hp41-core/src/ops/math1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Plan 28-08 — owned-String current_prompt signature from Plan 28-06 Task 1; existing DifeqInputStep stubs from Plan 28-01: FunctionNamePrompt, OrderPrompt, StepSizePrompt, X0Prompt, Y0Prompt, Y1PrimePrompt)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"DIFEQ" Op table lines 574-584)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md (`<decisions>` D-28.4 modal_prompt String type)
  </read_first>
  <behavior>
    - `DifeqInputStep` variants (extend Plan 28-01's stubs): FunctionNamePrompt, OrderPrompt, StepSizePrompt, X0Prompt, Y0Prompt, Y1PrimePrompt (only reached when modal flow records ORDER=2), Ready (NEW arm; clears modal after submission per established Plan 28-07/28-08 pattern).
    - `ModalProgram::current_prompt()` exhaustive match Difeq arms (owned-String signature post-Plan 28-06):
      - Difeq(FunctionNamePrompt) → Some("FUNCTION NAME?".to_string())
      - Difeq(OrderPrompt) → Some("ORDER=?".to_string())
      - Difeq(StepSizePrompt) → Some("STEP SIZE=?".to_string())
      - Difeq(X0Prompt) → Some("X0=?".to_string())
      - Difeq(Y0Prompt) → Some("Y0=?".to_string())
      - Difeq(Y1PrimePrompt) → Some("Y'0=?".to_string())
      - Difeq(Ready) → None
    - Preserves exhaustive match (no `_ =>` per FN-CLI-04 invariant from Phase 25).
  </behavior>
  <action>
    Extend `DifeqInputStep` enum in `hp41-core/src/ops/math1/modal.rs`: add `Ready` variant. All variants derive Debug, Clone, PartialEq (already established in Plan 28-01).

    Fill the `ModalProgram::current_prompt()` exhaustive match's Difeq arm with all 7 sub-variants per behavior. Use owned-String values via `.to_string()`.

    Add 7 unit tests to inline `#[cfg(test)] mod tests` covering each Difeq variant's prompt; mirror the Plan 28-07/28-08 Task 1 test pattern.

    Document the modal flow in a module-doc comment: "Y1PrimePrompt is only routed when the OrderPrompt user-entered value equals 2; the routing logic lives in the Plan 28-09 op_difeq_run_loop or in a modal-completion handler in Plan 29's CLI integration, depending on architecture. Plan 28-09 stores ORDER=1 vs ORDER=2 in DifeqState.order and skips/visits Y1PrimePrompt accordingly."

    Do NOT touch other ModalProgram arms (Matrix, Poly, Integ, Solve, Four, Trans) — they were closed by prior plans.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::modal::tests</automated>
  </verify>
  <done>7 DifeqInputStep variants (including new Ready) return correct prompts via current_prompt(); tests green; exhaustive match preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement difeq.rs (full DifeqState struct + op_difeq_run_loop with 4th-order RK4 + dispatch stub)</name>
  <files>
    hp41-core/src/ops/math1/difeq.rs
    hp41-core/src/ops/program.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/difeq.rs (current empty `DifeqState` stub from Plan 28-01 Task 5)
    - hp41-core/src/ops/math1/integ.rs (Plan 28-07 — full op_integ_run_loop body; Plan 28-09 MIRRORS its overall structure)
    - hp41-core/src/ops/math1/solve.rs (Plan 28-08 — second example of the user-callback pattern; reuse the run_loop re-entry idiom)
    - hp41-core/src/ops/program.rs (full file — `run_loop` signature, `Op::XeqInd` re-entry at lines 476-491 + dispatch arm at line 839+, and the Op::Integ / Op::Solve / Op::Sol run_loop arms added by Plans 28-07/28-08)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-PLAN.md (Task 2 — verbatim template Plan 28-09 mirrors)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-08-PLAN.md (Task 2 — second-consumer reuse pattern; the helper-factoring idiom)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-08 / 28-09 — SOLVE / DIFEQ" lines 524-537; §"User-Callback Infrastructure" lines 283-353; §"Op::XeqInd precedent" lines 466-521)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"DIFEQ" Op table lines 574-584)
    - HP-41C Math Pac OM Chapter 7 (~p.38 per RESEARCH §canonical_refs) — RK4 algorithm
    - hp41-core/src/format.rs (`format_hpnum` signature for print_buffer formatting per DIFEQ-05)
  </read_first>
  <behavior>
    - `DifeqState` (replace Plan 28-01's empty stub with the full struct per RESEARCH §DIFEQ + PATTERNS line 583):
      - `pub user_label: String`
      - `pub order: u8` (1 or 2; DIFEQ-01)
      - `pub step_size: HpNum` (h, the integration step)
      - `pub x: HpNum` (current x value)
      - `pub y: HpNum` (current y value)
      - `pub y_prime: Option<HpNum>` (Some(y') when order=2; None when order=1)
      - `pub step_count: u32` (cancellation cadence + outer budget anchor)
      - `#[derive(Debug, Clone, Default)]`
    - `pub(super) fn op_difeq_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` — the REAL implementation. Runs from inside `run_loop`'s match arm.
    - `pub fn op_difeq(state: &mut CalcState) -> Result<(), HpError>` — dispatch arm: returns `Err(HpError::InvalidOp)` because DIFEQ can only run inside run_loop. Mirrors Op::Integ from Plan 28-07 and Op::Solve from Plan 28-08.

    Op::Difeq flow inside run_loop (mirrors op_integ_run_loop AND grows the strict-reject to its FINAL canonical form):
    1. Strict-reject (XROM-08 FINAL form per PATTERNS lines 531-534): `if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() { return Err(HpError::InvalidOp); }` BEFORE any mutation. This is the canonical 3-state guard cited in D-28.7.
    2. Pre-mutation call_stack cap (Pitfall 4): `if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }` BEFORE any mutation.
    3. Reset cancellation flag: `state.cancel_requested.store(false, std::sync::atomic::Ordering::Relaxed);`.
    4. Read modal-staged parameters: user_label, order (validate 1 or 2; else write state.modal_prompt = Some("ORDER MUST BE 1 OR 2".to_string()) and return Ok(()) — NOT an HpError), step_size, x0, y0, and y_prime0 (when order=2).
    5. Initialize `state.difeq_state = Some(DifeqState { user_label, order, step_size, x: x0, y: y0, y_prime: if order==2 { Some(y_prime0) } else { None }, step_count: 0 });`.
    6. Push initial step line to print_buffer (DIFEQ-05): `format!("X={} Y={}", format_hpnum(x0), format_hpnum(y0))` (+ ` Y'={}` for order=2).
    7. RK4 iteration loop (no fixed cap; cancellation OR outer MAX_STEPS bounds the run):
       ```
       loop {
           if state.difeq_state.as_ref().expect("set above").step_count & 0x3F == 0
               && state.cancel_requested.load(Ordering::Relaxed) {
               state.difeq_state = None;
               return Err(HpError::Canceled);
           }
           // ORDER=1 RK4: 4 re-entries into run_loop on user_label
           // k1 = h * f(x_n, y_n)             — stack X=x_n, Y=y_n; user returns f in X
           // k2 = h * f(x_n+h/2, y_n+k1/2)
           // k3 = h * f(x_n+h/2, y_n+k2/2)
           // k4 = h * f(x_n+h, y_n+k3)
           // y_{n+1} = y_n + (k1 + 2*k2 + 2*k3 + k4) / 6
           // ORDER=2 uses the coupled system: y'=z, z'=f(x,y,z). Two-component RK4 per OM Chapter 7.

           // Update DifeqState in place; advance x by step_size; increment step_count;
           // Push "X=<v> Y=<v> [Y'=<v>]" to print_buffer per DIFEQ-05.

           // Bound externally: the v2.2 run_loop MAX_STEPS = 1_000_000 budget applies on top
           //   (see hp41-core/src/ops/program.rs run_loop signature).
       }
       ```
    8. Per DIFEQ-04, each k1..k4 evaluation is a separate `run_loop` re-entry on the user LBL: push needed args to stack (X=x, Y=y for order=1; X=x, Y=y, Z=y' for order=2), then `state.call_stack.push(state.pc); state.pc = find_in_program(program, &user_label)? + 1; run_loop(state, program, MAX_STEPS_REMAINING)?;`. After return, the user-LBL result is in X — read into the k variable.
    9. Per DIFEQ-03, scratch R00–R07 may be used for intermediate storage during RK4 computations. Document the user-responsibility convention (user-LBL STO to R00–R07 corrupts integration — same convention as INTG per Plan 28-07's docs/hp41-math1-divergences.md entry; no new entry needed because the divergence policy is already documented for INTG/SOLVE/DIFEQ collectively).

    Helper factoring: extract `fn rk4_step_order1(state, program, label, x, y, h) -> Result<HpNum, HpError>` and `fn rk4_step_order2(state, program, label, x, y, y_prime, h) -> Result<(HpNum, HpNum), HpError>` as private helpers — each owns the 4 user-callback re-entries and returns the new (y) or (y, y') tuple. Mirrors the helper-factoring idiom Plan 28-08 used for `run_secant_loop`.
  </behavior>
  <action>
    Replace Plan 28-01's empty `DifeqState` stub in `hp41-core/src/ops/math1/difeq.rs` with the full struct per behavior (7 fields). Preserve the Free42-contamination header from Plan 28-01.

    Implement the two private RK4 helpers per behavior (rk4_step_order1, rk4_step_order2). Each helper does 4 `run_loop` re-entries on the user LBL and returns the updated state component(s). Document each user-callback re-entry site in code comments with a `// CROSS-REFERENCE: see Plan 28-07 op_integ_run_loop for the symmetric user-callback pattern`.

    For ORDER=2 RK4, reduce the second-order ODE y'' = f(x, y, y') to the coupled first-order system y' = z, z' = f(x, y, z) and apply standard RK4 to both components in lockstep. The four coupled-system slopes are computed pairwise per OM Chapter 7 (TBD — record the OM page number verbatim in the doc comment of `rk4_step_order2` during implementation; PR reviewer rejects without the OM page citation):

       k1y = h · z_n
       k1z = h · f(x_n, y_n, z_n)
       k2y = h · (z_n + k1z/2)
       k2z = h · f(x_n + h/2, y_n + k1y/2, z_n + k1z/2)
       k3y = h · (z_n + k2z/2)
       k3z = h · f(x_n + h/2, y_n + k2y/2, z_n + k2z/2)
       k4y = h · (z_n + k3z)
       k4z = h · f(x_n + h, y_n + k3y, z_n + k3z)
       y_{n+1} = y_n + (k1y + 2·k2y + 2·k3y + k4y) / 6
       z_{n+1} = z_n + (k1z + 2·k2z + 2·k3z + k4z) / 6

    The "z" variable maps directly to `DifeqState.y_prime` (= Some(_) in ORDER=2 mode). Each `k_z` evaluation is one run_loop re-entry on user_label with (X=x_intermediate, Y=y_intermediate, Z=z_intermediate); each `k_y` evaluation is a pure arithmetic step requiring NO user-LBL call.

    Implement `pub(super) fn op_difeq_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` per behavior. Read modal-staged parameters from the same site Plans 28-07/28-08 used (document the choice in code comments).

    Implement `pub fn op_difeq(state: &mut CalcState) -> Result<(), HpError>` — dispatch arm returns `Err(HpError::InvalidOp)` with doc comment "Op::Difeq can only run inside run_loop; real implementation in op_difeq_run_loop".

    In `hp41-core/src/ops/program.rs`, add the `Op::Difeq` arm to `run_loop`'s match block, calling `crate::ops::math1::difeq::op_difeq_run_loop(state, program)?;`. Place it next to `Op::Integ` / `Op::Solve` for pattern symmetry.

    Inline `#[cfg(test)] mod tests` with file-scope `#![allow(clippy::unwrap_used)]` on the test mod:
    - `rk4_order1_correctness`: ORDER=1 RK4 on dy/dx = y (analytical solution y = y0 * e^x) with x0=0, y0=1, h=0.1, 10 steps → y ≈ e ≈ 2.71828; assert `approx::assert_relative_eq!(max_relative = 1e-5)` (Pitfall 14 — RK4 has O(h^4) global error so 1e-5 is appropriate at h=0.1).
    - `rk4_order2_correctness`: ORDER=2 RK4 on y'' = -y (analytical solution y = cos(x)) with x0=0, y0=1, y'0=0, h=0.1, 10 steps → y ≈ cos(1) ≈ 0.5403; assert relative tolerance.
    - `modal_order1_flow`: simulate the 5-prompt modal flow (FUNCTION NAME? / ORDER=? / STEP SIZE=? / X0=? / Y0=?); assert Y1PrimePrompt is NOT visited when ORDER=1.
    - `modal_order2_flow`: simulate the 6-prompt modal flow (adds Y'0=? when ORDER=2); assert Y1PrimePrompt IS visited.
    - `step_by_step_print_buffer`: after 5 RK4 steps, state.print_buffer has 5 (or 6 with initial) lines, each formatted "X=<v> Y=<v>" (+ " Y'=<v>" for order=2).
    - `scratch_registers`: assert R00..R07 register layout matches DIFEQ-03 scratch convention (document the layout in test docstring even if the RK4 implementation uses local Rust variables rather than HP regs — the scratch convention is hardware-faithful but Rust implementation is free to optimize internal layout).
    - `order_validation`: pre-set ORDER=3 → assert state.modal_prompt = Some("ORDER MUST BE 1 OR 2") and Ok(()) (NOT an HpError).
    - `nested_rejection`: pre-set state.integ_state = Some(IntegState::default()); call op_difeq_run_loop → Err(InvalidOp); also pre-set solve_state → Err(InvalidOp); also pre-set difeq_state → Err(InvalidOp). This asserts the FINAL 3-state XROM-08 guard.
    - `call_stack_full`: pre-fill state.call_stack with 4 entries; call op_difeq_run_loop → Err(CallDepth) BEFORE mutation; assert state.call_stack still has 4 entries (no leak; state.difeq_state stays None).
    - `cancel_per_64_steps`: pre-set state.cancel_requested.store(true); run DIFEQ that would take >= 64 steps; assert Err(Canceled) and state.difeq_state cleared after the first per-64 check.
    - Target: ≥ 5 unit tests covering Op::Difeq per Pitfall 16 / QUAL-04. The 10 tests above clearly exceed the floor.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::difeq::tests</automated>
  </verify>
  <done>DifeqState full struct compiled; op_difeq_run_loop implements 4th-order RK4 per DIFEQ-02 for both ORDER=1 and ORDER=2; XROM-08 FINAL 3-state guard works at op entry; Pitfall 4 call_stack cap works pre-mutation; cancellation per-64-steps fires; step-by-step print_buffer output per DIFEQ-05; ORDER validation per DIFEQ-01; ≥ 10 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve; fill last math1_user_callback.rs case; add OM-cited numerical_accuracy cases; add prgm_display arms (CLI + GUI)</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/math1_user_callback.rs
    hp41-core/tests/numerical_accuracy.rs
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-core/src/ops/mod.rs (post-Plan 28-08 — Op enum + dispatch arms; grow with Op::Difeq)
    - hp41-core/src/ops/program.rs (post-Plan 28-08 — execute_op arms including Op::Integ / Op::Solve / Op::Sol all returning InvalidOp; run_loop arms for Op::Integ / Op::Solve / Op::Sol)
    - hp41-core/src/ops/math1/xrom.rs (post-Plan 28-08 — 36 entries; grow to 37 by adding DIFEQ)
    - hp41-core/tests/math1_user_callback.rs (post-Plan 28-08 — 4 of 5 cases filled + 2 NEW Plan 28-07/28-08 cases; 1 #[ignore]'d remains: nested_difeq_inside_integ_rejected — Plan 28-09 un-ignores it)
    - hp41-cli/src/prgm_display.rs (full file — current op_display_name match arms; Plan 28-09 adds Op::Difeq arm per CLAUDE.md SC-4-literal carve-out documented in planning context)
    - hp41-gui/src-tauri/src/prgm_display.rs (full file — same as CLI; SC-4 spirit allows display formatters per CLAUDE.md note on op_display_name)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-PLAN.md (Task 2 — proof-of-pattern for Op enum + dispatch + execute_op + xrom_resolve quadruple)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§Pattern 3 lines 579-609 — 4-place exhaustive match)
  </read_first>
  <behavior>
    - 1 new Op variant integrated through Phase 28 3-place exhaustive match (Op enum + dispatch + execute_op):
      - `Op::Difeq` (master, opens 5- or 6-prompt modal depending on ORDER)
    - Both dispatch and execute_op arms return `Err(HpError::InvalidOp)` per RESEARCH §"execute_op vs run_loop split for callback ops" lines 769-775 (mirrors Op::Integ from Plan 28-07 and Op::Solve from Plan 28-08).
    - run_loop arm calls into the real `op_difeq_run_loop` from Task 2.
    - xrom_resolve registers: `("DIFEQ", Op::Difeq)`. After Plan 28-09, `MATH_1.ops` has 37 entries.
    - `math1_user_callback.rs`: the LAST `#[ignore]`'d placeholder filled by Plan 28-09:
      - `nested_difeq_inside_integ_rejected`: outer XEQ INTG with callback that XEQ "DIFEQ" → inner Op::Difeq returns InvalidOp; assert.
      - After Plan 28-09, math1_user_callback.rs has ZERO `#[ignore]`'d cases. Full regression suite is active.
    - `numerical_accuracy.rs` grows with ≥ 5 DIFEQ cases (OM Chapter 7 worked examples). Each carries OM citation + Free42 cross-check note per D-27.7 precedent.
    - `prgm_display.rs` (BOTH CLI and GUI copies per CLAUDE.md SC-4-literal carve-out from planning context): add `Op::Difeq => "DIFEQ".to_string()` arm to `op_display_name`.
    - `xrom_shadowing.rs` runs over 37 entries non-collision.
  </behavior>
  <action>
    1. Add `Op::Difeq` variant to the `Op` enum in `hp41-core/src/ops/mod.rs` under header `// ── Phase 28: DIFEQ (Plan 28-09) ────────`. Add 1 dispatch arm returning `Err(HpError::InvalidOp)` per the "Op variants that run only in run_loop" pattern from Plan 28-07/28-08 (mirrors Op::Integ / Op::Solve).

    2. Add `Op::Difeq` arm to `execute_op` in `program.rs` — same `Err(InvalidOp)` per RESEARCH lines 769-775 / Plan 28-07/28-08 precedent.

    3. Confirm the `Op::Difeq` arm in `run_loop`'s match block from Task 2 is wired correctly (placed adjacent to `Op::Integ` / `Op::Solve` / `Op::Sol` per pattern symmetry).

    4. Grow `MATH_1.ops` and `math1_resolve` in `xrom.rs`: add `("DIFEQ", Op::Difeq)`. Confirmed safe by RESEARCH §"Resolver-Chain Conflict Map" line 628 — "DIFEQ" does not appear in v2.2 `xeq_by_name_local_resolve` or `builtin_card_op`.

    5. In `hp41-core/tests/math1_user_callback.rs`:
       - Remove `#[ignore]` from `nested_difeq_inside_integ_rejected` (last Plan 28-01 placeholder); fill body per behavior: build an outer INTG with a callback program that contains `Op::Xeq("DIFEQ".to_string())`; assert the inner XEQ resolves to Op::Difeq via xrom_resolve and that op_difeq_run_loop's FINAL 3-state strict-reject returns InvalidOp.
       - Add 2 more NEW symmetric tests for completeness: `nested_difeq_inside_solve_rejected` (outer SOLVE with callback that XEQ "DIFEQ") and `nested_difeq_inside_difeq_rejected` (outer DIFEQ with callback that XEQ "DIFEQ"); both assert Err(InvalidOp).
       - After Plan 28-09, `math1_user_callback.rs` has ZERO `#[ignore]`'d cases: all 5 original Plan 28-01 placeholders + Plan 28-07's user_fn_stops_aborts_integ + Plan 28-07's user_fn_stores_to_scratch_corrupts_integ + Plan 28-08's nested_solve_inside_solve_rejected + Plan 28-09's nested_difeq_inside_solve_rejected and nested_difeq_inside_difeq_rejected are all live tests. Document in module-doc that the suite is now complete.

    6. Add ≥ 5 cases to `hp41-core/tests/numerical_accuracy.rs` for DIFEQ with OM citations per D-27.7 precedent. Cases minimum:
       - `difeq_exp_decay`: dy/dx = -y, y0=1, x0=0, h=0.1, 10 steps → y(1) ≈ 1/e ≈ 0.36788; OM-cited.
       - `difeq_exp_growth`: dy/dx = y, y0=1, x0=0, h=0.1, 10 steps → y(1) ≈ e ≈ 2.71828; OM-cited.
       - `difeq_harmonic_oscillator`: ORDER=2, y''=-y, y0=1, y'0=0, h=0.1, 10 steps → y(1) ≈ cos(1) ≈ 0.5403; OM-cited.
       - `difeq_linear_growth`: dy/dx = x, y0=0, x0=0, h=0.1, 10 steps → y(1) ≈ 0.5; OM-cited (closed form y = x²/2).
       - `difeq_step_size_effect`: same ODE under h=0.1 vs h=0.01 → smaller h yields more accurate result (RK4 has O(h^4) global error; this asserts the convergence rate qualitatively).
       - Each `case!` doc-comment carries (a) the OM page+example reference and (b) the Free42 v3.0.5 oracle value per D-27.7. Pitfall 14 tolerance is `max_relative = 1e-5` (RK4 at h=0.1 has ~h^4 = 1e-4 truncation error per step; over 10 steps cumulative drift ~1e-5 is appropriate).
       - Combined pass rate including all v2.2 baseline + 28-02 hyperbolics + 28-03/04 complex + 28-05 POLY + 28-06 MATRIX + 28-07 INTG + 28-08 SOLVE + 28-09 DIFEQ cases >= 98%; v1.x 503-case baseline floor 498/503 (D-27.6) UNCHANGED — assert with the existing independent floor check.

    7. Add display arm in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` per CLAUDE.md SC-4-literal carve-out from planning context:
       - In `hp41-cli/src/prgm_display.rs`, add to the `op_display_name` match: `Op::Difeq => "DIFEQ".to_string(),`. Place under a `// ── Phase 28: DIFEQ (Plan 28-09) ────────` comment.
       - In `hp41-gui/src-tauri/src/prgm_display.rs`, add the SAME arm in the same format. Per CLAUDE.md v2.2 additions: "Note: op_display_name is duplicated in both hp41-cli/src/prgm_display.rs and hp41-gui/src-tauri/src/prgm_display.rs — every new Op variant must be added in both copies."

    8. Re-run xrom_shadowing.rs — confirm 37 entries clear (36 from Plan 28-08 + 1 new from Plan 28-09).
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo build -p hp41-cli &amp;&amp; (cd hp41-gui/src-tauri &amp;&amp; cargo build) &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test math1_user_callback --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>Op::Difeq landed (1 new variant); LAST `#[ignore]`'d Plan 28-01 user-callback regression case filled + 2 NEW symmetric DIFEQ-inside-SOLVE / DIFEQ-inside-DIFEQ tests; numerical_accuracy.rs +5 DIFEQ cases with OM citations; CLI + GUI prgm_display.rs both carry Op::Difeq arm (workspace builds in BOTH crates); xrom_shadowing.rs 37/37; XROM-08 final 3-state guard locked.</done>
</task>

</tasks>

<verification>
- `cargo build -p hp41-core` — full green; zero warnings under `#![deny(clippy::unwrap_used)]`.
- `cargo build -p hp41-cli` — confirms Op::Difeq display arm in `hp41-cli/src/prgm_display.rs` keeps the 4-place exhaustive match compiling.
- `(cd hp41-gui/src-tauri && cargo build)` — same for hp41-gui's duplicate copy.
- `cargo test -p hp41-core --test math1_user_callback` — ZERO `#[ignore]`'d cases remaining; full regression suite active (5 Plan-01 placeholders + 2 Plan 28-07 NEW + 1 Plan 28-08 NEW + 2 Plan 28-09 NEW symmetric tests).
- `cargo test -p hp41-core --lib math1::difeq::tests::call_stack_full` — pre-mutation cap verified; no leak.
- `cargo test -p hp41-core --lib math1::difeq::tests::cancel_per_64_steps` — cancellation cadence verified.
- `cargo test -p hp41-core --lib math1::difeq::tests::rk4_order1_correctness math1::difeq::tests::rk4_order2_correctness` — RK4 algorithm correctness verified against analytical solutions.
- `cargo test -p hp41-core --test xrom_shadowing` — 37/37 non-collision (DIFEQ clears).
- SC-4 invariant preserved per CLAUDE.md stricter audit: `grep -rn "fn op_(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing (display-arm additions to `op_display_name` are display formatters, not calc/math logic per CLAUDE.md's documented exception).
</verification>

<success_criteria>
- 1 new Op variant integrated through 3-place exhaustive match (Op enum + dispatch + execute_op) AND 1 new prgm_display arm in BOTH `hp41-cli` and `hp41-gui` copies (per CLAUDE.md SC-4-literal carve-out from planning context).
- `op_difeq_run_loop` reuses INTG's user-program callback infrastructure verbatim — same run_loop re-entry, same strict-reject guard (grown to its FINAL 3-state canonical form per PATTERNS lines 531-534), same pre-mutation call_stack cap, same per-64 cancellation check.
- 4th-order Runge-Kutta correctness verified for both ORDER=1 (single-variable) and ORDER=2 (coupled system) per DIFEQ-02 / OM Chapter 7.
- User callback per DIFEQ-04 routes f(x,y) for ORDER=1 and f(x,y,y') for ORDER=2; each k1..k4 evaluation is a separate run_loop re-entry on the user LBL.
- Step-by-step output per DIFEQ-05: each RK4 step pushes formatted "X=<v> Y=<v> [Y'=<v>]" to state.print_buffer.
- ORDER validation per DIFEQ-01: 3 → state.modal_prompt = Some("ORDER MUST BE 1 OR 2") + Ok(()) (NOT an HpError).
- XROM-08 FINAL 3-state guard locked for the rest of v3.x: `state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some()`.
- `math1_user_callback.rs` regression suite is complete — ZERO `#[ignore]`'d cases after Plan 28-09.
- Per-Op test count ≥ 5 for Op::Difeq (Pitfall 16 / QUAL-04).
- Plan 28-10 (FOUR/TRI/TRANS) operates on pure-data Op variants OR uses the FOUR USER-mode E-key callback (FOUR-06) which inherits the same callback infrastructure pattern. The user-callback infrastructure is now FROZEN — no further changes after Plan 28-09.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-09-SUMMARY.md`:
- 1 new Op variant (Op::Difeq) landed through 3-place exhaustive match + 1 prgm_display arm in both CLI and GUI copies.
- DifeqState full struct (7 fields) per PATTERNS line 583 / RESEARCH §DIFEQ.
- 4th-order Runge-Kutta per DIFEQ-02 for both ORDER=1 (single-variable) and ORDER=2 (coupled system).
- Step-by-step output per DIFEQ-05 via state.print_buffer.
- Plan 28-07 user-callback infrastructure reused verbatim (DIFEQ-04); cancellation per-64-steps plumbing inherited.
- XROM-08 strict-reject guard reaches its FINAL canonical 3-state form per PATTERNS lines 531-534; locked for v3.x.
- math1_user_callback.rs scaffold: ZERO `#[ignore]`'d cases remaining; regression suite fully active.
- 37 entries in MATH_1.ops; xrom_shadowing.rs green.
- Plan 28-10 (FOUR + TRI + TRANS) is the last Phase 28 plan; it operates on pure-data Op variants (TRI, TRANS) plus the FOUR USER-mode E-key (FOUR-06) which inherits the callback infrastructure pattern locked here.
</output>
