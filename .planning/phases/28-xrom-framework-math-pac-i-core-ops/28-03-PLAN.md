---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - hp41-core/src/ops/math1/complex.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/math1_complex_edge_cases.rs
  - hp41-core/tests/numerical_accuracy.rs
  - .planning/REQUIREMENTS.md
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - CMPLX-01
  - CMPLX-02
  - CMPLX-03
  - CMPLX-04
  - CMPLX-05

must_haves:
  truths:
    - "REQUIREMENTS.md grows CMPLX-18: Op::Real (XEQ \"REAL\") deactivates complex_mode (D-28.3 derived requirement — added BEFORE this plan's implementation per CONTEXT.md note)"
    - "5 new Op variants: Op::CPlus, Op::CMinus, Op::CTimes, Op::CDiv, Op::Real — each registered in MATH_1.ops as 'C+', 'C-', 'C×' (with Unicode alias), 'C÷' (with Unicode alias), 'REAL'"
    - "ComplexStack location per D-28.1: overlay X/Y/Z/T — ζ = X+iY, τ = Z+iT (zero new HpNum storage on CalcState; the complex_mode flag landed in Plan 28-01)"
    - "Op::CPlus / CMinus / CTimes consume ζ AND τ (4 stack levels), write result to ζ, T-replicate pattern fills τ from old T; LiftEffect::Enable (binary-arithmetic semantics)"
    - "Op::CDiv with (re=0, im=0) divisor returns HpError::DivideByZero BEFORE the division — Pitfall 6 mitigation (CMPLX-05 / RESEARCH lines 442-444)"
    - "complex_atan2 helper lives in hp41-core/src/ops/math1/complex.rs (RESEARCH Open Q5 recommendation); FIRST arm: complex_atan2(HpNum::ZERO, HpNum::ZERO) returns HpNum::ZERO (NOT NaN, NOT DataError) — Pitfall 6 mitigation"
    - "complex_mode auto-on (D-28.2): every binary complex op (CPlus/CMinus/CTimes/CDiv) sets state.complex_mode = true BEFORE the computation"
    - "Op::Real (CMPLX-18 per D-28.3) sets state.complex_mode = false; declares LiftEffect::Neutral; stack untouched"
    - "math1_complex_edge_cases.rs (Plan 28-01 scaffold) is filled with arithmetic-related cases: c_div_zero_returns_divide_by_zero_before_division, complex_atan2_zero_zero_returns_zero (covers the helper directly even though no public Op exposes it yet)"
    - "Per-Op test count >= 5 (Pitfall 16): each of the 5 Op variants has >= 5 tests covering positive case + edge case + LiftEffect assertion + T-replicate assertion + (for CDiv) zero-divisor guard"
  nyquist_dimensions:
    - "CMPLX-01..04 unit — cargo test -p hp41-core --lib math1::complex::tests::arith"
    - "CMPLX-05 unit — cargo test -p hp41-core --lib math1::complex::tests::cdiv_zero_divisor"
    - "Pitfall 6 integration — cargo test -p hp41-core --test math1_complex_edge_cases"
    - "complex_mode lifecycle — cargo test -p hp41-core --lib math1::complex::tests::complex_mode_auto_on"
    - "Op::Real — cargo test -p hp41-core --lib math1::complex::tests::real_clears_flag"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count (>= 25 tests for 5 new Ops)"
  key_links:
    - from: hp41-core/src/ops/math1/complex.rs
      to: hp41-core/src/stack.rs
      via: "binary complex ops read X/Y/Z/T and write back via stack-lift Enable semantics"
      pattern: "state\\.stack\\.[xyzt]"
    - from: hp41-core/src/ops/math1/xrom.rs
      to: hp41-core/src/ops/math1/complex.rs
      via: "MATH_1.ops registers C+/C-/C×/C÷/REAL"
      pattern: "\\(\"C\\+\", Op::CPlus\\)"
---

<objective>
Land the complex-stack arithmetic primitives on top of v2.2's HP stack overlay. Five Op variants: `CPlus`, `CMinus`, `CTimes`, `CDiv`, and the derived `Real` (CMPLX-18 from D-28.3). The complex stack is an OVERLAY — ζ = X+iY, τ = Z+iT — so zero new storage fields on `CalcState` (the `complex_mode: bool` flag landed in Plan 28-01).

Purpose: deliver the building blocks (binary stack ops, `complex_atan2(0,0) = 0` helper, zero-divisor guard) that Plan 28-04's 13 transcendental complex functions depend on.

Output: `hp41-core/src/ops/math1/complex.rs` containing 5 op fns + `complex_atan2` helper; REQUIREMENTS.md grows CMPLX-18 entry; `math1_complex_edge_cases.rs` (Plan 28-01 scaffold) gets the arithmetic edge cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@.planning/REQUIREMENTS.md
@hp41-core/src/stack.rs
@hp41-core/src/ops/arithmetic.rs
@hp41-core/src/ops/mod.rs

<interfaces>
<!-- From `hp41-core/src/ops/arithmetic.rs` — binary stack-acting precedent (op_add consumes X+Y, writes to X, T-replicates, LiftEffect::Enable). Read the actual file for current shape; pattern is: pop X, pop Y, push result, replicate T. -->

<!-- From RESEARCH lines 426-444 — `complex_atan2` (0,0)→0 first arm pattern -->
```
fn complex_atan2(im: HpNum, re: HpNum) -> HpNum {
    if im.is_zero() && re.is_zero() {
        return HpNum::ZERO;
    }
    let result_f64 = im.to_f64().atan2(re.to_f64());
    HpNum::from_f64_rounded(result_f64)
}
```

<!-- From `hp41-core/src/state.rs` (post-Plan 28-01) — complex_mode field already exists; this plan toggles it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CMPLX-18 to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate the CMPLX section and the Traceability table)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-28.3 — full text of the derived requirement)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`:
    1. Append CMPLX-18 to the CMPLX section: `CMPLX-18: Op::Real (XEQ "REAL") — deactivates complex_mode; resets it to false; no other side effects on the stack. Derived from D-28.3 (deviation from OM 1979 for UX). Documented in docs/hp41-math1-divergences.md (Phase 30 / DOC-04).`
    2. Update the Traceability table: add a row mapping `CMPLX-18 → Phase 28 / Plan 28-03`.
    3. Update the Phase 28 requirement count if recorded in the header (was 47; becomes 47 because the original 47 list in the planning_context already included CMPLX-17 max; the new CMPLX-18 ADDS one — verify and adjust the count and the Traceability summary).

    Note: this Task is documentation-only, not a code change. It must land BEFORE Tasks 2-3 of this plan so the `Op::Real` implementation in `complex.rs` references an existing requirement.
  </action>
  <verify>
    <automated>grep -q "^CMPLX-18:" .planning/REQUIREMENTS.md &amp;&amp; grep -q "CMPLX-18.*Phase 28" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>CMPLX-18 entry exists; traceability table updated; requirement count consistent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement complex.rs (arithmetic + complex_atan2 + Op::Real)</name>
  <files>
    hp41-core/src/ops/math1/complex.rs
    hp41-core/src/ops/math1/mod.rs
  </files>
  <read_first>
    - hp41-core/src/ops/arithmetic.rs (full file — binary stack-acting pattern for op_add)
    - hp41-core/src/stack.rs (verify push/pop/T-replicate behavior; understand `lift_enabled` flag)
    - hp41-core/src/num.rs (HpNum API: `is_zero`, `to_f64`, `from_f64_rounded`, `ZERO`, arithmetic operators)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Complex-Stack Layout", lines 408-466; §"Per-Program Implementation Notes" — Complex Stack & Arithmetic, lines 487-493)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-03 + 28-04 — Complex stack + functions", lines 370-410)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-28.1, D-28.2, D-28.3 — overlay model, auto-on/explicit-off, derived REAL)
  </read_first>
  <behavior>
    Stack overlay: ζ = X+iY (X is real part, Y is imaginary part), τ = Z+iT.
    - `op_c_plus`: ζ' = ζ + τ = (X+Z) + i(Y+T). Writes to X = X+Z, Y = Y+T. T-replicate: τ' = old T as both Z and T (per HP-41 T-replicate semantics). LiftEffect::Enable. Sets `state.complex_mode = true`.
    - `op_c_minus`: ζ' = ζ - τ. Same shape; X = X-Z, Y = Y-T.
    - `op_c_times`: ζ' = ζ·τ = (X+iY)(Z+iT) = (XZ-YT) + i(XT+YZ). Use 4 HpNum mults + 2 adds/subs (per RESEARCH §Complex Stack & Arithmetic table).
    - `op_c_div`: divisor τ = Z+iT. **ZeroDivisor check FIRST**: if `Z.is_zero() && T.is_zero()` → `HpError::DivideByZero` (BEFORE mutating stack). Standard formula `(X+iY)/(Z+iT) = ((XZ+YT) + i(YZ-XT)) / (Z² + T²)`.
    - `op_real`: trivial — `state.complex_mode = false; apply_lift_effect(state, LiftEffect::Neutral); Ok(())`. Stack untouched.
    - `complex_atan2(im: HpNum, re: HpNum) -> HpNum`: first arm `(im.is_zero() && re.is_zero())` returns `HpNum::ZERO`; else f64-bridge.

    Per-test expectations (≥ 5 per op):
    - CPlus: (1+2i) + (3+4i) = 4+6i; (0+0i) + (0+0i) = 0+0i; T-replicate after; complex_mode auto-on; lift_enabled true after.
    - CMinus: (3+4i) - (1+2i) = 2+2i; negative-imag case; T-replicate; mode flag; lift.
    - CTimes: (1+1i) * (1+1i) = 0+2i; (3+0i)*(2+0i) = 6+0i (pure-real path); (0+1i)*(0+1i) = -1+0i (i² = -1); mode flag; lift.
    - CDiv: (1+0i)/(0+1i) = 0-1i; **(1+1i)/(0+0i) → Err(DivideByZero)** (Pitfall 6); (4+0i)/(2+0i) = 2+0i; mode flag; lift.
    - Real: complex_mode=true before, complex_mode=false after; stack X/Y/Z/T unchanged.
    - complex_atan2 inline tests: `(0,0)→0`, `(1,0)→π/2 rad`, `(0,1)→0 rad`, `(1,1)→π/4 rad`, `(-1,0)→-π/2 rad`.
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/complex.rs`. File header: Free42-contamination disclaimer.

    Implement:
    1. `pub fn op_c_plus(state: &mut CalcState) -> Result<(), HpError>` — binary complex add per behavior. Set `state.complex_mode = true` BEFORE computation per D-28.2 auto-on. Read X, Y, Z, T; compute new X, Y; T-replicate fills new Z and new T with old T. Call `apply_lift_effect(state, LiftEffect::Enable)`.
    2. `pub fn op_c_minus(state: &mut CalcState) -> Result<(), HpError>` — same shape, subtraction.
    3. `pub fn op_c_times(state: &mut CalcState) -> Result<(), HpError>` — `(X·Z - Y·T) + i(X·T + Y·Z)`.
    4. `pub fn op_c_div(state: &mut CalcState) -> Result<(), HpError>` — divisor zero check FIRST (`state.stack.z.is_zero() && state.stack.t.is_zero()` → `Err(HpError::DivideByZero)`), then standard formula.
    5. `pub fn op_real(state: &mut CalcState) -> Result<(), HpError>` — clears complex_mode; LiftEffect::Neutral.
    6. `pub(super) fn complex_atan2(im: HpNum, re: HpNum) -> HpNum` — `(0,0)→0` first arm; f64 bridge for non-zero. Use `Decimal::from_f64(rad).map(HpNum::rounded).unwrap_or(HpNum::ZERO)` (the `unwrap_or` defaults zero for the catastrophic case where Decimal can't represent the f64 result — but for atan2 the result is bounded in `[-π, π]` so this should never fire; document with a `// safe: atan2 result is bounded` inline comment).

    Add `pub mod complex;` to `hp41-core/src/ops/math1/mod.rs`.

    All public op fns use `state.complex_mode = true;` as the first mutation (auto-on per D-28.2) EXCEPT `op_real` which sets `false`.

    Inline `#[cfg(test)] mod tests`:
    - ≥ 5 tests per Op variant = ≥ 25 tests minimum.
    - Plus ≥ 5 tests for `complex_atan2` helper (the (0,0) test is Pitfall 6 gate).
    - File-scope `#![allow(clippy::unwrap_used)]` on test mod only.
    - Use `approx::assert_relative_eq!(..., max_relative = 1e-7)` for numerical assertions.

    `#![deny(clippy::unwrap_used)]` continues to apply to production code — never use `.unwrap()` outside the test mod.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib math1::complex::tests</automated>
  </verify>
  <done>5 op fns + complex_atan2 compile; ≥ 30 tests pass; DivideByZero fires BEFORE division (verified by ensuring stack unchanged on the error path); complex_mode auto-on; Op::Real clears flag without touching stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve; fill math1_complex_edge_cases.rs</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/math1_complex_edge_cases.rs
    hp41-core/tests/numerical_accuracy.rs
  </files>
  <read_first>
    - hp41-core/src/ops/mod.rs (Op enum + dispatch from prior plans)
    - hp41-core/src/ops/math1/xrom.rs (post-Plan 28-02 — 6 hyperbolic entries; grow with 5 more)
    - hp41-core/tests/math1_complex_edge_cases.rs (Plan 28-01 scaffold with #[ignore] placeholders)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-02 — Hyperbolics" wire-up pattern from prior plan applies here verbatim)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Resolver-Chain Conflict Map" lines 622-633 — confirms C+/C-/C×/C÷ have no v2.2 collision)
  </read_first>
  <behavior>
    - Test 1 (math1_complex_edge_cases.rs filled): `complex_atan2_zero_zero_returns_zero` — call complex_atan2(0,0), assert HpNum::ZERO returned.
    - Test 2: `c_div_zero_returns_divide_by_zero_before_division` — set stack to (1,1,0,0) with X=1, Y=1, Z=0, T=0 (divisor on top of stack); dispatch Op::CDiv; assert Err(DivideByZero); assert stack X/Y/Z/T unchanged from pre-call (no partial mutation — guard FIRES first).
    - Tests 3-4 (`ln_z_zero_returns_domain`, `z_pow_w_zero_neg_exp_returns_domain`) remain `#[ignore]` — Plan 28-04 fills.
    - xrom_resolve("C+", 0b1) → Some(Op::CPlus); "C-" → Some(Op::CMinus); "C×" (Unicode) → Some(Op::CTimes); "C÷" → Some(Op::CDiv); "REAL" → Some(Op::Real).
    - xrom_shadowing.rs still passes — none of the 5 new mnemonics collide with v2.2 builtin_card_op.
  </behavior>
  <action>
    1. In `hp41-core/src/ops/mod.rs`, add 5 variants to the `Op` enum under header `// ── Phase 28: Complex Stack Arithmetic (Plan 28-03) ────────`: `CPlus`, `CMinus`, `CTimes`, `CDiv`, `Real`. Add 5 dispatch arms.
    2. In `hp41-core/src/ops/program.rs`, add 5 `execute_op` arms (delegating to dispatch).
    3. In `hp41-core/src/ops/math1/xrom.rs`, grow `MATH_1.ops` and `math1_resolve` with 5 entries each. Use Unicode aliases per RESEARCH lines 67-68 / 624-633: register both `"C×"` and an ASCII alias `"C*"` for CTimes; both `"C÷"` and `"C/"` for CDiv. (Match the OM 1979 multi-spelling tolerance pattern from v2.2 `builtin_card_op` which accepts `X<>Y?` and Unicode `X\u{2260}Y?` for the same op.)
    4. In `hp41-core/tests/math1_complex_edge_cases.rs`, remove `#[ignore]` from `complex_atan2_zero_zero_returns_zero` and `c_div_zero_returns_divide_by_zero_before_division`; fill the bodies per Test 1-2 behavior. Leave `ln_z_zero_returns_domain` and `z_pow_w_zero_neg_exp_returns_domain` `#[ignore]` for Plan 28-04.
    5. In `hp41-core/tests/numerical_accuracy.rs`, add cases for CPlus/CMinus/CTimes/CDiv covering OM-cited worked examples from Math Pac OM Chapter on Complex Numbers (~pp.24-26 per RESEARCH `<canonical_refs>`). ≥ 3 cases per binary op = 12 new cases minimum, each with `// Source: HP 00041-90034 p.XX` + `// Free42 v3.0.5: <value>`.
    6. Re-run xrom_shadowing.rs — confirm 11 entries (6 hyperbolic + 5 complex) all clear `builtin_card_op` non-collision.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib --test xrom_shadowing --test xrom_chain_order --test math1_complex_edge_cases --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>5 Op variants land in 4-place exhaustive match (Op enum + dispatch + execute_op in this task; both prgm_display.rs arms in Task 4); 5 xrom_resolve entries (with Unicode + ASCII aliases for C× and C÷); math1_complex_edge_cases.rs has 2 of 4 cases filled (the arithmetic-related ones); numerical_accuracy.rs grows by 12+ cases; xrom_shadowing.rs green.</done>
</task>

<task type="auto">
  <name>Task 4: Add op_display_name arms to both prgm_display.rs copies</name>
  <files>
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (full file — confirm `op_display_name` match is exhaustive with NO `_ =>` arm)
    - hp41-gui/src-tauri/src/prgm_display.rs (mirror copy — same exhaustive shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (analog excerpt §"Pattern 3 / op_display_name 4-place exhaustive match" — line shape to mirror)
    - CLAUDE.md (SC-4-literal carve-out: "op_display_name(...) in prgm_display.rs is a display formatter, not calculator logic — duplicated in both CLI and GUI copies; every new Op variant must be added in BOTH files")
  </read_first>
  <action>
    Add the 5 new Op variant arm(s) introduced by this plan to the `op_display_name` exhaustive `match` in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs`. Place each new arm under a `// ── Phase 28: Complex Stack Arithmetic (Plan 28-03) ────────` section comment.

    Variants to add (matching the xrom_resolve mnemonic registered by Task 2 of this plan): Op::CPlus => "C+", Op::CMinus => "C-", Op::CTimes => "C×", Op::CDiv => "C÷", Op::Real => "REAL".

    Preserve the existing exhaustive match shape — `op_display_name` has NO `_ =>` arm by design (FN-CLI-04 invariant from Phase 25 / CLAUDE.md v2.2 additions). The Rust compiler enforces every Op variant is named; a missing arm causes a workspace build failure in either `hp41-cli` or `hp41-gui/src-tauri` and that is the desired structural enforcement. Both copies stay in sync — the CLAUDE.md comment "every new Op variant must be added in both copies" is the canonical instruction.

    No production logic added here; this task is purely the SC-4-literal carve-out display formatting. ROADMAP Success Criterion #5 (every new Op variant appears in BOTH prgm_display copies) is satisfied for the 5 variant(s) introduced by Plan 28-03.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>5 new arm(s) appear in both prgm_display.rs files in the same order under the Phase 28 section header; `cargo build --workspace` passes (compile-time exhaustiveness confirms no Op variant is missing); no production code paths touched outside the two display files.</done>
</task>


</tasks>

<verification>
- `cargo test -p hp41-core` — all tests green.
- `cargo test -p hp41-core --test math1_complex_edge_cases -- --include-ignored` — verify 2 of 4 cases pass, 2 remain ignored (with clear "filled by Plan 28-04" message).
- `grep -c "Op::CPlus\|Op::CMinus\|Op::CTimes\|Op::CDiv\|Op::Real" hp41-core/src/ops/math1/complex.rs` ≥ 5 (one per op fn signature minimum).
- `cargo test -p hp41-core --test math1_op_test_count` — meta-test confirms 5 Ops have ≥ 5 tests each.
- SC-4 invariant: no `hp41-gui/src-tauri/src/` changes.
- `state.complex_mode` flips correctly: false initially → true after first complex op → false after Op::Real → true again after next complex op.
</verification>

<success_criteria>
- 4 complex-arithmetic Op variants (CPlus, CMinus, CTimes, CDiv) + 1 deactivation op (Real) integrate cleanly through dispatch + execute_op + xrom_resolve.
- ζ = X+iY, τ = Z+iT overlay model works: T-replicate semantics match HP-41 hardware (verified by stack-state assertions).
- CMPLX-05 satisfied: `op_c_div` with (0+0i) divisor returns DivideByZero BEFORE division (no stack mutation on the error path).
- `complex_atan2(0,0) = 0` first arm verified (Pitfall 6).
- CMPLX-18 entry exists in REQUIREMENTS.md with traceability to Plan 28-03.
- Plan 28-04 inherits complex_atan2 and the complex-stack contracts; can build 13 transcendental functions on this foundation.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-03-SUMMARY.md`:
- 5 Op variants added (CPlus, CMinus, CTimes, CDiv, Real).
- CMPLX-18 added to REQUIREMENTS.md.
- complex_atan2 helper available for Plan 28-04.
- math1_complex_edge_cases.rs partially filled (2 of 4 cases); Plan 28-04 closes the other 2.
- T-replicate semantics + complex_mode auto-on/off lifecycle verified.
</output>
