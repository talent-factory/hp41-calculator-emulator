---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 05
type: execute
wave: 5
depends_on:
  - 28-01
  - 28-04
files_modified:
  - hp41-core/src/ops/math1/poly.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - POLY-01
  - POLY-02
  - POLY-03
  - POLY-04
  - POLY-05
  - POLY-06
  - POLY-07

must_haves:
  truths:
    - "2 new Op variants: Op::PolyWorkflow (XEQ \"POLY\" — master entry, opens DegreePrompt modal); Op::Roots (XEQ \"ROOTS\" — sub-entry, bypasses DegreePrompt; coefficients assumed in R00–R04)"
    - "PolyInputStep enum sub-states (in modal.rs): DegreePrompt, CoefficientPrompt(degree: u8, current_idx: u8), Ready — exhaustive match in ModalProgram::current_prompt()"
    - "Op::PolyWorkflow dispatch sets state.modal_program = Some(ModalProgram::Poly(PolyInputStep::DegreePrompt)); state.modal_prompt = Some(\"DEGREE=?\"); LiftEffect::Neutral"
    - "POLY-04 output format gate (Pitfall 5): complex root pairs render EXACTLY as four lines per pair: \"U=<u>\", \"V=<v>\", \"U=<u>\", \"-V=-<v>\" — written to state.print_buffer via push, NEVER println! (hp41-core forbids stdout)"
    - "POLY-06 multiplicity-as-cluster: (x-1)^5 returns 5 roots with small imaginary parts (~10⁻³); NO snap-to-zero post-processing — documented as a divergence in docs/hp41-math1-divergences.md (Phase 30 / DOC-04 entry deferred to Phase 30 but BEHAVIOR is locked in this plan via unit test cluster_multiplicity)"
    - "POLY-07 non-convergence: |imag| > 10^9 during real-polynomial iteration returns HpError::Domain (surfaces as 'DATA ERROR' display in CLI/GUI later)"
    - "Closed-form quadratic for degree 2 (RESEARCH §POLY/ROOTS Algorithm row); iterative root-finding (Bairstow-like per OM Chapter 7) for degree 3–5"
    - "PolyInputStep ModalProgram::current_prompt() returns 'DEGREE=?' / 'A=?' / 'B=?' / 'C=?' / 'D=?' / 'E=?' / 'F=?' per Math Pac OM page-cited workflow (~Chapter 7)"
    - "Per-Op test count >= 5 per Pitfall 16: 2 Ops × 5 tests = >= 10 unit tests; plus the POLY-04 output_format test + cluster_multiplicity test + non_convergence test"
  nyquist_dimensions:
    - "POLY-01..03 unit — cargo test -p hp41-core --lib math1::poly::tests::flow"
    - "POLY-04 unit + OM cite — cargo test -p hp41-core --lib math1::poly::tests::output_format (Pitfall 5 fidelity gate)"
    - "POLY-05..07 unit — cargo test -p hp41-core --lib math1::poly::tests::edges (multiplicity-as-cluster + non-convergence Domain)"
    - "Numerical accuracy — cargo test -p hp41-core --test numerical_accuracy poly_cases (OM-cited worked examples from Math Pac OM Chapter 7)"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
    - "Modal state machine — cargo test -p hp41-core --lib math1::modal::tests (poly variants of current_prompt)"
  key_links:
    - from: hp41-core/src/ops/math1/poly.rs
      to: hp41-core/src/state.rs
      via: "Op::PolyWorkflow sets state.modal_program + state.modal_prompt; root output via state.print_buffer"
      pattern: "state\\.modal_program\\|state\\.print_buffer"
    - from: hp41-core/src/ops/math1/poly.rs
      to: hp41-core/src/ops/math1/complex.rs
      via: "complex root pairs computed via complex arithmetic from Plan 28-04"
      pattern: "complex_atan2\\|op_z_pow"
---

<objective>
Land POLY (root-finder) — the first prompt-driven Math Pac I program. Two Op variants (`PolyWorkflow` master + `Roots` sub-entry) plus a `PolyInputStep` modal sub-enum that drives the `DEGREE=?` → `A=?` → ... → `F=?` prompt sequence and the U=u/V=v/U=u/-V=-v output format that's THE Pitfall 5 fidelity gate.

Purpose: Plan 28-05 is the first plan introducing the modal-workflow pattern that Plans 28-06/07/08/09/10 will all build on. POLY validates the modal-state-machine layer end-to-end (state.modal_program transitions, state.modal_prompt updates, state.print_buffer output) before MATRIX (larger workflow) and INTG (user-callback complexity) try the same.

Output: `hp41-core/src/ops/math1/poly.rs` with 2 op fns + closed-form quadratic + Bairstow-like iterative root-finder; `modal.rs` extends `PolyInputStep` with full step states; `numerical_accuracy.rs` grows with OM-cited polynomial cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@hp41-core/src/ops/math1/complex.rs
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/ops/print.rs
@hp41-core/src/state.rs

<interfaces>
<!-- From RESEARCH §"POLY / ROOTS (Plan 28-05) — Pitfall 5 fidelity gate" lines 511-521 -->
```
| Op::PolyWorkflow | Master: opens ModalProgram::Poly(PolyInputStep::DegreePrompt) → 2..5 → CoefficientPrompt(A,B,C,...) per degree → executes
| Op::Roots        | Sub-entry: bypasses DegreePrompt; coefficients assumed in R00–R04
| Output format    | EXACTLY U=u / V=v / U=u / -V=-v for complex pairs (Pitfall 5)
| Multiplicity     | (x-1)^5 returns 5 clustered roots; NO snap-to-zero
| Non-convergence  | |imag| > 10^9 → HpError::Domain
| Algorithm        | Closed-form quadratic for degree 2; iterative (Bairstow-like per OM Chapter 7) for cubic/quintic
```

<!-- From PATTERNS §"Plan 28-05 — POLY / ROOTS" lines 415-434 — modal-opener dispatch pattern -->
```
pub fn op_poly_workflow(state: &mut CalcState) -> Result<(), HpError> {
    state.modal_program = Some(ModalProgram::Poly(PolyInputStep::DegreePrompt));
    state.modal_prompt = Some("DEGREE=?".to_string());
    apply_lift_effect(state, LiftEffect::Neutral);
    Ok(())
}
```

<!-- From `hp41-core/src/ops/print.rs` — print_buffer push pattern (analog for ROOTS output) -->
<!-- Read print.rs to find op_pra / op_prx / op_prstk for exact push idiom -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PolyInputStep enum + ModalProgram::current_prompt arms</name>
  <files>hp41-core/src/ops/math1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Plan 28-01 — has PolyInputStep::{DegreePrompt, CoefficientPrompt(u8,u8), Ready})
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"POLY / ROOTS" algorithm row + workflow)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` Claude's Discretion on ModalProgram shape
  </read_first>
  <behavior>
    - `PolyInputStep::DegreePrompt` → `current_prompt()` returns `Some("DEGREE=?")`.
    - `PolyInputStep::CoefficientPrompt(degree, current_idx)` → returns coefficient prompt: idx 0 → `Some("A=?")`, idx 1 → `Some("B=?")`, ..., idx 5 → `Some("F=?")`. For degree d, only indices 0..=d are valid.
    - `PolyInputStep::Ready` → `current_prompt()` returns `None` (modal complete; awaiting next sub-op like Roots execution).
    - Existing variants from Plan 28-01 (DegreePrompt, CoefficientPrompt(u8,u8), Ready) remain unchanged.
  </behavior>
  <action>
    In `hp41-core/src/ops/math1/modal.rs`:
    1. Verify `PolyInputStep` enum already has `DegreePrompt, CoefficientPrompt(u8, u8), Ready` from Plan 28-01 Task 5.
    2. Extend `ModalProgram::current_prompt()` exhaustive match's `Poly(...)` arm:
       - `Poly(PolyInputStep::DegreePrompt) => Some("DEGREE=?")`
       - `Poly(PolyInputStep::CoefficientPrompt(_d, idx)) => Some(<letter prompt>)` — implement via a `match idx { 0 => "A=?", 1 => "B=?", ..., 5 => "F=?", _ => "?=?" }` mapping. (Degree caps at 5 per Math Pac I OM 1979 Chapter 7; idx > 5 shouldn't fire — defensive default.)
       - `Poly(PolyInputStep::Ready) => None`
    3. Add inline tests for the new prompt strings:
       - `PolyInputStep::CoefficientPrompt(5, 0).into_modal().current_prompt() == Some("A=?")`
       - `PolyInputStep::CoefficientPrompt(5, 5).into_modal().current_prompt() == Some("F=?")`
       - `PolyInputStep::Ready.into_modal().current_prompt() == None`
    4. (Optional helper) Add `impl PolyInputStep { pub fn into_modal(self) -> ModalProgram { ModalProgram::Poly(self) } }` for ergonomic test construction; same for other input-step enums (delete + re-add in a single edit if file already has them).

    Confirm `current_prompt()` remains exhaustive (NO `_ =>` arm) per FN-CLI-04 invariant.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::modal::tests</automated>
  </verify>
  <done>PolyInputStep's three variants all return correct prompts via `current_prompt()`; tests green; exhaustive match preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement poly.rs (PolyWorkflow opener + Roots executor + closed-form quadratic + Bairstow-like iter)</name>
  <files>
    hp41-core/src/ops/math1/poly.rs
    hp41-core/src/ops/math1/mod.rs
  </files>
  <read_first>
    - hp41-core/src/ops/print.rs (full file — `print_buffer` push idiom)
    - hp41-core/src/ops/math1/complex.rs (post-Plan 28-04 — full complex op set; needed for complex-pair root output)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"POLY / ROOTS" full table; §"Per-Program Implementation Notes — POLY/ROOTS" lines 511-521)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-05 — POLY / ROOTS" lines 415-434)
    - HP-41C Math Pac Owner's Manual Chapter 7 "Polynomial Solutions" (~OM section per RESEARCH `<canonical_refs>`)
  </read_first>
  <behavior>
    - `op_poly_workflow`: opens modal (sets `state.modal_program = Some(ModalProgram::Poly(PolyInputStep::DegreePrompt))`, `state.modal_prompt = Some("DEGREE=?".to_string())`, LiftEffect::Neutral).
    - `op_roots`: executes polynomial root-finder using coefficients in R00..R04 (R00 = A, R01 = B, ..., R04 = E or F depending on degree). Determines degree from leading non-zero R-register OR from a separate state field set by the prior PolyWorkflow flow.
      - For degree 2 (quadratic ax² + bx + c): closed-form `(-b ± √(b²-4ac)) / 2a`. Real discriminant → 2 real roots written to print_buffer as `U=<r1>` / `U=<r2>`. Negative discriminant → 1 complex pair: `U=<-b/2a>` / `V=<√(4ac-b²)/2a>` / `U=<-b/2a>` / `-V=-<√...>` (the EXACT POLY-04 / Pitfall 5 format).
      - For degree 3, 4, 5: Bairstow-like iteration deflating quadratic factors. Document the algorithm with OM page citations in code comments.
      - Multiplicity-as-cluster (POLY-06): NO snap-to-zero. `(x-1)^5` returns 5 nearly-equal roots with small imaginary parts (~10⁻³). The test `cluster_multiplicity` asserts the 5 returned roots have real parts within 10⁻² of 1.0 AND non-zero (small) imaginary parts (the divergence from "ideal" multiplicity that we faithfully reproduce).
      - Non-convergence (POLY-07): if any iteration sees `|imag| > 1e9`, return `Err(HpError::Domain)`.
    - Output format gate (POLY-04 / Pitfall 5): `state.print_buffer.push(format!("U={}", format_hpnum(u)))` etc. EXACTLY four lines per complex pair: `U=<u>`, `V=<v>`, `U=<u>`, `-V=-<v>` per RESEARCH line 514 and PATTERNS line 432.
    - Per-Op test count: 2 Ops × ≥ 5 tests + the 3 named extra tests (output_format, cluster_multiplicity, non_convergence) = ≥ 13 unit tests minimum.
  </behavior>
  <action>
    Create `hp41-core/src/ops/math1/poly.rs`. File header: Free42-contamination disclaimer.

    Implement:
    1. `pub fn op_poly_workflow(state: &mut CalcState) -> Result<(), HpError>` — modal opener per PATTERNS pattern. NO computation; just sets `modal_program` + `modal_prompt`. LiftEffect::Neutral.
    2. `pub fn op_roots(state: &mut CalcState) -> Result<(), HpError>` — executor:
       - Reads degree from `state.modal_program` (if Some(Poly(Ready)) → look up degree from stored state; if user invokes XEQ "ROOTS" directly w/o prior PolyWorkflow → infer from leading non-zero coefficient in R00..R04 OR from a stored degree).
       - Reads coefficients from `state.regs[0..=degree]`.
       - For degree 2: closed-form quadratic.
       - For degree 3-5: Bairstow-like iteration (document with OM page citation + Free42 cross-check note).
       - Pushes results to `state.print_buffer` in U=u/V=v/U=u/-V=-v format per Pitfall 5.
       - Convergence guard: `|imag| > 1e9` → `Err(HpError::Domain)`.
       - On success: `state.modal_program = None; state.modal_prompt = None;` (clears modal); LiftEffect::Neutral.
    3. (Optional helper) `fn solve_quadratic(a: HpNum, b: HpNum, c: HpNum) -> [ComplexPair; 2]` — closed-form formula, returns two roots (real or complex pair).
    4. (Optional helper) `fn deflate_and_find(coeffs: &mut Vec<HpNum>) -> Result<Vec<ComplexPair>, HpError>` — Bairstow-like; iterates with cap (per OM-cited max iterations) returning Domain on non-convergence.

    Add `pub mod poly;` to `hp41-core/src/ops/math1/mod.rs`.

    Inline `#[cfg(test)] mod tests`:
    - Test 1 (`flow`): `op_poly_workflow` sets modal_program correctly + emits "DEGREE=?" prompt.
    - Test 2 (`flow`): full state-machine walk — after PolyWorkflow → simulate user entry of degree 2 + 3 coefficients → step transitions correctly through PolyInputStep::CoefficientPrompt(2, 0..2) → Ready.
    - Test 3 (`output_format`): for `x² + 1` (coeffs [1, 0, 1]), assert `state.print_buffer == vec!["U=0.0000", "V=1.0000", "U=0.0000", "-V=-1.0000"]` (Pitfall 5 fidelity gate).
    - Test 4 (`cluster_multiplicity`): for `(x-1)^5 = x⁵ - 5x⁴ + 10x³ - 10x² + 5x - 1` (coeffs [1,-5,10,-10,5,-1]), execute Roots; assert 5 roots returned with real parts within `1e-2` of `1.0`; assert NOT all imaginary parts are exactly zero (multiplicity-as-cluster, not snap-to-zero).
    - Test 5 (`non_convergence`): construct a pathological high-degree polynomial that drives `|imag|` over 1e9 (e.g., very small leading coefficient with poorly-conditioned roots); assert `Err(HpError::Domain)`.
    - Plus 5+ more tests covering: real-roots-only quadratic (x²-3x+2 → 1, 2); equal real roots (x²-2x+1 → 1, 1); single complex pair (x²+x+1); the modal `Ready` exit state; LiftEffect::Neutral verification.

    File-scope `#![allow(clippy::unwrap_used)]` on test mod only; production code stays compliant.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::poly::tests</automated>
  </verify>
  <done>op_poly_workflow + op_roots compile; ≥ 13 unit tests pass; POLY-04 output format verified bit-for-bit; multiplicity-as-cluster verified (5 clustered roots, non-zero imag); non-convergence returns Domain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve + numerical_accuracy cases</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/numerical_accuracy.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/xrom.rs (current MATH_1.ops post-Plan 28-04 — 23 entries; grow to 25)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Resolver-Chain Conflict Map" — POLY/ROOTS confirmed non-shadowing line 627)
  </read_first>
  <behavior>
    - Op::PolyWorkflow + Op::Roots integrated through dispatch + execute_op + xrom_resolve.
    - xrom_resolve("POLY", 0b1) → Some(Op::PolyWorkflow); xrom_resolve("ROOTS", 0b1) → Some(Op::Roots).
    - numerical_accuracy.rs grows with ≥ 6 cases (3 per Op): worked examples from OM Chapter 7 (real-roots quadratic, complex-pair quadratic, cubic, multiplicity cluster, non-convergence DATA ERROR, degree-5 example).
    - xrom_shadowing.rs still green with 25 entries.
  </behavior>
  <action>
    1. Add 2 variants to `Op` enum in `hp41-core/src/ops/mod.rs` under header `// ── Phase 28: POLY / ROOTS (Plan 28-05) ────────`: `PolyWorkflow`, `Roots`. Add 2 dispatch arms.
    2. Add 2 `execute_op` arms in `program.rs`.
    3. Grow `MATH_1.ops` and `math1_resolve` in `hp41-core/src/ops/math1/xrom.rs`: `("POLY", Op::PolyWorkflow), ("ROOTS", Op::Roots)`.
    4. Add ≥ 6 cases to `hp41-core/tests/numerical_accuracy.rs` for POLY (one per OM-cited worked example, each carrying `// Source: HP 00041-90034 p.XX` + `// Free42 v3.0.5: <expected output>` citations).
    5. Confirm xrom_shadowing.rs has 25 entries iterating, all non-shadowing.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>2 Op variants land; xrom_resolve registers POLY + ROOTS; numerical_accuracy.rs +6 cases; xrom_shadowing.rs 25/25 entries non-collision; per-Op meta-test confirms ≥ 5 tests per variant.</done>
</task>

<task type="auto">
  <name>Task 4: Add op_display_name arms to both prgm_display.rs copies</name>
  <files>
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (full file — confirm `op_display_name` match is exhaustive with NO `_ =>` arm)
    - hp41-gui/src-tauri/src/prgm_display.rs (mirror copy — same exhaustive shape)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (analog excerpt §"Pattern 3 / op_display_name 4-place exhaustive match" — line shape to mirror)
    - CLAUDE.md (SC-4-literal carve-out: "op_display_name(...) in prgm_display.rs is a display formatter, not calculator logic — duplicated in both CLI and GUI copies; every new Op variant must be added in BOTH files")
  </read_first>
  <action>
    Add the 2 new Op variant arm(s) introduced by this plan to the `op_display_name` exhaustive `match` in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs`. Place each new arm under a `// ── Phase 28: POLY / ROOTS (Plan 28-05) ────────` section comment.

    Variants to add (matching the xrom_resolve mnemonic registered by Task 2 of this plan): Op::PolyWorkflow => "POLY", Op::Roots => "ROOTS".

    Preserve the existing exhaustive match shape — `op_display_name` has NO `_ =>` arm by design (FN-CLI-04 invariant from Phase 25 / CLAUDE.md v2.2 additions). The Rust compiler enforces every Op variant is named; a missing arm causes a workspace build failure in either `hp41-cli` or `hp41-gui/src-tauri` and that is the desired structural enforcement. Both copies stay in sync — the CLAUDE.md comment "every new Op variant must be added in both copies" is the canonical instruction.

    No production logic added here; this task is purely the SC-4-literal carve-out display formatting. ROADMAP Success Criterion #5 (every new Op variant appears in BOTH prgm_display copies) is satisfied for the 2 variant(s) introduced by Plan 28-05.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>2 new arm(s) appear in both prgm_display.rs files in the same order under the Phase 28 section header; `cargo build --workspace` passes (compile-time exhaustiveness confirms no Op variant is missing); no production code paths touched outside the two display files.</done>
</task>


</tasks>

<verification>
- `cargo test -p hp41-core` — full green; POLY workflow end-to-end verified.
- `cargo test -p hp41-core --lib math1::poly::tests::output_format` — EXACTLY four-line `U=u/V=v/U=u/-V=-v` format asserted; Pitfall 5 fidelity gate.
- `cargo test -p hp41-core --lib math1::poly::tests::cluster_multiplicity` — (x-1)^5 returns 5 clustered roots with small non-zero imaginary parts (multiplicity-as-cluster); NO snap-to-zero.
- SC-4 invariant preserved.
- Modal state machine (PolyInputStep) walks through DegreePrompt → CoefficientPrompt → Ready cleanly; `state.modal_prompt` updates correctly.
</verification>

<success_criteria>
- 2 Op variants (PolyWorkflow, Roots) integrate cleanly through Phase 28 3-place exhaustive match.
- POLY workflow is the first modal Math Pac I program — demonstrates `state.modal_program` + `state.modal_prompt` channel works end-to-end.
- POLY-04 output format gate locked: U=u / V=v / U=u / -V=-v literal four-line format.
- POLY-06 multiplicity-as-cluster behavior verified (divergence from "ideal" multiplicity faithfully reproduced).
- POLY-07 non-convergence returns Domain.
- Plans 28-06..28-10 inherit the proven modal pattern.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-05-SUMMARY.md`:
- 2 Op variants (PolyWorkflow, Roots).
- PolyInputStep enum + current_prompt arms.
- POLY-04 output format gate verified bit-for-bit.
- POLY-06 multiplicity-as-cluster (5 clustered roots with small non-zero imag — documented divergence) — to be written into Phase 30 divergences doc.
- POLY-07 non-convergence Domain.
- 25 entries in MATH_1.ops; xrom_shadowing.rs green.
- Modal state machine validated for Plans 28-06..28-10.
</output>
