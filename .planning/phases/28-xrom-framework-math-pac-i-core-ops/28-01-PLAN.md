---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/lib.rs
  - hp41-core/src/error.rs
  - hp41-core/src/state.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/Cargo.toml
  - hp41-core/tests/xrom_shadowing.rs
  - hp41-core/tests/xrom_chain_order.rs
  - hp41-core/tests/math1_user_callback.rs
  - hp41-core/tests/math1_complex_edge_cases.rs
  - hp41-core/tests/math1_op_test_count.rs
  - hp41-core/tests/v3_save_compat.rs
  - docs/adr/v3.0-003-inv-epsilon.md
  - docs/adr/v3.0-004-intg-threshold.md
autonomous: true
requirements:
  - XROM-01
  - XROM-02
  - XROM-03
  - XROM-04
  - XROM-05
  - XROM-06
  - XROM-07
  - XROM-09

must_haves:
  truths:
    - "OM page+paragraph for INV-EPSILON (Pitfall 7 / MAT-07) transcribed verbatim into docs/adr/v3.0-003-inv-epsilon.md BEFORE Plan 28-06 starts (D-3 irreversible)"
    - "OM page+paragraph for INTG convergence threshold formula (Pitfall 2 / INTG-08) transcribed verbatim into docs/adr/v3.0-004-intg-threshold.md BEFORE Plan 28-07 starts (D-4 irreversible)"
    - "xrom_resolve(name, modules) returns Some(Op::*) only when bit 0 of modules is set; returns None for any unknown name; fires LAST in resolver chain (C-28.4 / Pitfall 1)"
    - "MATH_1: XromModule const exposes id=7, name=\"MATH 1A\", and a static slice ops covering every Math Pac I XEQ mnemonic Plans 28-02..28-10 will register"
    - "Resolver chain extended at both hp41-core sites (op_xeq and run_loop's Op::Xeq label-miss arm in program.rs) AFTER builtin_card_op AND BEFORE Err(InvalidOp)"
    - "7 new CalcState fields persist save-file backward-compat: xrom_modules (default 0b1 via default_xrom_modules), complex_mode (default false), matrix_dim and matrix_active_reg (both Option, default None) carry #[serde(default)]; modal_program, modal_prompt, integ_state, solve_state, difeq_state (D-28.9 carry, RESEARCH Open Q2 option a) all carry #[serde(default, skip)]; cancel_requested (D-28.7) carries #[serde(default = \"default_cancel_requested\", skip)]"
    - "HpError::Canceled variant exists with thiserror #[error(\"canceled\")] (D-28.9)"
    - "ModalProgram enum + per-program InputStep sub-enums (MatrixInputStep, SolveInputStep, PolyInputStep, IntegInputStep, DifeqInputStep, FourInputStep, TransInputStep) defined with ModalProgram::current_prompt() -> Option<&str> accessor (RESEARCH Open Q4)"
    - "Wave 0 scaffolds exist as compilable but minimal: xrom_shadowing.rs (asserts MATH_1.ops do not collide with builtin_card_op), xrom_chain_order.rs (asserts xrom-fires-LAST at both core sites), math1_user_callback.rs (compile-only scaffold; cases filled by 28-07/08/09), math1_complex_edge_cases.rs (compile-only scaffold; cases filled by 28-03/04), math1_op_test_count.rs (grep meta-test enforcing >= 5 tests per Op variant added in 28-02..28-10)"
    - "approx 0.5.1 added to hp41-core [dev-dependencies] (used from Plan 28-05 onward per Pitfall 14)"
    - "Save-file backward compat: v2.2-shape JSON loads via #[serde(default)] without error; tests/v3_save_compat.rs round-trips a synthetic v2.2 save string"
    - "Phase 28 Op variants land in dispatch() (ops/mod.rs) and execute_op() arms (ops/program.rs) for every variant added 28-02..28-10"
    - "ROADMAP Success Criterion #5 satisfied in-phase: every new Op variant added in Plans 28-02..28-10 lands op_display_name arms in BOTH hp41-cli/src/prgm_display.rs AND hp41-gui/src-tauri/src/prgm_display.rs per CLAUDE.md SC-4-literal carve-out (display formatters are not calculator/math logic)"
    - "D-28.1 overlay model satisfied without new HpNum storage: no complex_re/complex_im fields added to CalcState; complex_mode flag alone implements the X/Y/Z/T overlay"
  nyquist_dimensions:
    - "XROM-01..03 unit — cargo test -p hp41-core --lib xrom::tests"
    - "XROM-04 unit — cargo test -p hp41-core --lib xrom::tests::resolve_known"
    - "XROM-05 integration — cargo test -p hp41-core --test xrom_shadowing"
    - "XROM-06 unit — cargo test -p hp41-core --lib state::tests::serde_roundtrip"
    - "XROM-07 integration — cargo test -p hp41-core --test xrom_chain_order"
    - "XROM-09 unit — cargo test -p hp41-core --lib math1::modal::tests"
    - "Pitfall 1 CI gate — cargo test --test xrom_shadowing REQUIRED"
    - "Pitfall 11 plumbing — cargo test -p hp41-core --lib state::tests::cancel_field_present (compile + serde-skip)"

  key_links:
    - from: hp41-core/src/ops/program.rs
      to: hp41-core/src/ops/math1/xrom.rs
      via: "xrom_resolve called from Op::Xeq label-miss fallback chain"
      pattern: "xrom_resolve\\(&label, state\\.xrom_modules\\)"
    - from: hp41-core/src/ops/mod.rs
      to: hp41-core/src/ops/math1
      via: "pub mod math1; declaration"
      pattern: "pub mod math1"
    - from: docs/adr/v3.0-003-inv-epsilon.md
      to: hp41-core/src/ops/math1/matrix.rs
      via: "Plan 28-06 references this ADR for INV_EPSILON const"
      pattern: "INV_EPSILON"
    - from: docs/adr/v3.0-004-intg-threshold.md
      to: hp41-core/src/ops/math1/integ.rs
      via: "Plan 28-07 references this ADR for integ_threshold helper"
      pattern: "integ_threshold"
---

<objective>
Land the XROM framework for v3.0 Math Pac I emulation in `hp41-core` — the resolver chain, the 7 new `CalcState` fields, the `ModalProgram` state machine layer, the `HpError::Canceled` variant, the 5 Wave-0 test scaffolds, and the two OM-transcribed irreversible-decision ADRs (INV-EPSILON for Plan 28-06, INTG convergence threshold for Plan 28-07).

Purpose: every downstream plan (28-02..28-10) depends on these artifacts. Plan 28-06 and 28-07 are additionally gated on the two OM-transcribed constants — those transcriptions happen FIRST in this plan, before any code lands.

Output: compilable `hp41-core` with new `math1` submodule, 7 new state fields, new error variant, 5 W0 test files (compile-only scaffolds where downstream plans fill the cases), and 2 ADR documents containing the verbatim OM quotes that pin INV-EPSILON and the INTG threshold formula. The plan unblocks Wave 2 entry.

Phase boundary: hp41-core only PLUS op_display_name arms in both prgm_display.rs copies per CLAUDE.md SC-4-literal carve-out (display formatters are not calculator/math logic; full surface specified in ROADMAP Success Criterion #5). Plans 28-02..28-10 each add their op_display_name arms in BOTH hp41-cli/src/prgm_display.rs AND hp41-gui/src-tauri/src/prgm_display.rs alongside their dispatch/execute_op arms.

Scope: 7 tasks / 17 files is intentionally large because framework + ADRs + Wave-0 scaffolds + 5 CalcState fields + HpError + resolver chain extension are interdependent and ship atomically. Split would create circular `depends_on` edges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-VALIDATION.md
@CLAUDE.md
@hp41-core/src/lib.rs
@hp41-core/src/error.rs
@hp41-core/src/state.rs
@hp41-core/src/ops/mod.rs
@hp41-core/src/ops/program.rs

<interfaces>
<!-- Key v2.2 contracts the executor needs to extend. Extracted from PATTERNS.md analog excerpts. -->

From `hp41-core/src/ops/program.rs:987-1007` — `builtin_card_op` resolver shape (xrom_resolve mirrors this):
```
pub(super) fn builtin_card_op(name: &str) -> Option<Op> {
    match name {
        "WPRGM" => Some(Op::Wprgm),
        "X<>Y?" | "X\u{2260}Y?" | "X#Y?" => Some(Op::Test(TestKind::XNeY)),
        // ... 8 more conditional spellings
        _ => None,
    }
}
```

From `hp41-core/src/ops/program.rs:492-516` — `Op::Xeq` label-miss fallback chain (insert xrom_resolve AFTER builtin_card_op, BEFORE Err(InvalidOp)):
```
Op::Xeq(label) => {
    if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }
    match find_in_program(program, &label) {
        Ok(target) => { state.call_stack.push(state.pc); state.pc = target + 1; }
        Err(_) => {
            if let Some(card_op) = builtin_card_op(&label) {
                crate::ops::dispatch(state, card_op)?;
            } else {
                return Err(HpError::InvalidOp);
            }
        }
    }
}
```

From `hp41-core/src/state.rs:103-157` — serde-discipline precedents:
```
#[serde(default)] pub last_key_code: u8,
#[serde(default, skip)] pub print_buffer: Vec<String>,
```

From `hp41-core/src/error.rs:1-33` — thiserror enum shape (extend with Canceled):
```
#[derive(Error, Debug, PartialEq, Clone)]
pub enum HpError {
    #[error("overflow")] Overflow,
    // ...
    #[error("try again")] CallDepth,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe OM INV-EPSILON to ADR-003</name>
  <files>docs/adr/v3.0-003-inv-epsilon.md</files>
  <read_first>
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Decision 3 — INV-EPSILON value", lines 65-71)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-VALIDATION.md (§"Manual-Only Verifications" table)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-3 / MAT-07)
    - HP-41C Math Pac Owner's Manual (00041-90034, 1979) — Chapter 4 "Matrix Operations", INV / SIMEQ algorithm sections beginning ~p.14 (PDF in user's literature mirror, link in 28-CONTEXT.md `<canonical_refs>`)
  </read_first>
  <action>
    Create `docs/adr/v3.0-003-inv-epsilon.md` containing:
    1. **Status:** Locked 2026-05-16, owner: Plan 28-01 Task 1.
    2. **Context:** Pitfall 7 — community-cited 5e-10 vs OM-quoted value; need ground truth before Plan 28-06 MATRIX implementation can land. References ADR-003 placeholder in PROJECT.md and ROADMAP Phase 28 success criterion #1.
    3. **Decision:** the verbatim INV-EPSILON value quoted from HP-41C Math Pac Owner's Manual. Quote the OM paragraph in full (no paraphrase), include the OM page number (use the page reference from the PDF; if OM uses a different threshold for `INV` vs `SIMEQ` zero-test, quote both and pick one as `INV_EPSILON`, documenting the choice). Encode the final value as: `pub const INV_EPSILON: HpNum = HpNum::from_scientific("<verbatim-from-OM>");` snippet for Plan 28-06 to copy into `hp41-core/src/ops/math1/matrix.rs`.
    4. **Free42 cross-check:** consult-only, document Free42's near-singular threshold for sanity comparison (note divergence if any per Pitfall 19 — do NOT copy Free42 code).
    5. **Consequences:** Plan 28-06 imports this constant; the `inv_singular`/`inv_near_singular`/`inv_back_sub_overflow` numerical_accuracy cases (Phase 32) verify the value matches OM examples within 1 ULP at 10-digit precision.

    If OM PDF text-extraction is unreliable (per VALIDATION.md Manual-Only row), include the screenshot path in `docs/adr/img/` or a manual transcription note documenting the human OCR step.
  </action>
  <verify>
    <automated>test -f docs/adr/v3.0-003-inv-epsilon.md &amp;&amp; grep -q "INV_EPSILON" docs/adr/v3.0-003-inv-epsilon.md &amp;&amp; grep -q "00041-90034" docs/adr/v3.0-003-inv-epsilon.md</automated>
  </verify>
  <done>ADR file exists; verbatim OM quote present with page citation; ready-to-paste Rust const snippet present; Free42 cross-check note present.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe OM INTG convergence threshold to ADR-004</name>
  <files>docs/adr/v3.0-004-intg-threshold.md</files>
  <read_first>
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Decision 4 — INTG convergence threshold formula", lines 73-87)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-VALIDATION.md (§"Manual-Only Verifications" table, INTG-08 row)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-4 / INTG-08)
    - HP-41C Math Pac Owner's Manual — Chapter 4 "Numerical Integration", ~p.25 (INTG-01..08 ground truth)
    - hp41-core/src/format.rs and state.rs to confirm DisplayMode shape: `DisplayMode::Fix(n) | DisplayMode::Sci(n) | DisplayMode::Eng(n)`
  </read_first>
  <action>
    Create `docs/adr/v3.0-004-intg-threshold.md` containing:
    1. **Status:** Locked 2026-05-16, owner: Plan 28-01 Task 2.
    2. **Context:** Pitfall 2 — INTG convergence threshold "tied to DisplayMode" community wisdom needs OM ground truth before Plan 28-07 INTG implementation lands.
    3. **Decision:** verbatim OM quote describing (a) convergence threshold formula (typically `10^(-decimals - 1)` but VERIFY against OM), (b) subdivision-cap behavior (REQUIREMENTS lists 2^15 = 32768 — verify OM agrees), and (c) any DisplayMode-dependent precision logic. Include a ready-to-paste Rust helper signature: `pub fn integ_threshold(mode: DisplayMode) -> HpNum` with the body filled per OM (covering Fix / Sci / Eng all three variants).
    4. **Free42 cross-check:** document Free42's integration threshold behavior for `Fix(4)` vs `Fix(9)` of `∫₀¹ x² dx` for sanity (consult-only — Pitfall 19).
    5. **Consequences:** Plan 28-07 imports `integ_threshold` into `hp41-core/src/ops/math1/integ.rs`; numerical_accuracy.rs adds test cases asserting the SAME integral produces different precision under `Fix(4)` vs `Fix(9)` (Pitfall 2 detection — if formula is wrong, BOTH tests pass with the same tolerance and the bug is invisible).
  </action>
  <verify>
    <automated>test -f docs/adr/v3.0-004-intg-threshold.md &amp;&amp; grep -q "integ_threshold" docs/adr/v3.0-004-intg-threshold.md &amp;&amp; grep -q "DisplayMode" docs/adr/v3.0-004-intg-threshold.md &amp;&amp; grep -q "00041-90034" docs/adr/v3.0-004-intg-threshold.md</automated>
  </verify>
  <done>ADR file exists; verbatim OM quote present with page citation; ready-to-paste Rust helper signature present; Pitfall 2 detection guidance present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land HpError::Canceled variant</name>
  <files>hp41-core/src/error.rs</files>
  <read_first>
    - hp41-core/src/error.rs (full file — ~33 lines)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"`hp41-core/src/error.rs` (MODIFY)" — lines 98-128)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-28.9)
  </read_first>
  <behavior>
    - Test 1: `HpError::Canceled.to_string()` returns `"canceled"` (thiserror auto-Display).
    - Test 2: `HpError::Canceled == HpError::Canceled` (PartialEq + Clone derived from existing enum).
    - Test 3: `HpError::Canceled != HpError::Domain` (variants are distinct).
  </behavior>
  <action>
    In `hp41-core/src/error.rs`, append a new variant to the `pub enum HpError`:
    - Variant identifier: `Canceled` (no fields).
    - Attribute: `#[error("canceled")]` (thiserror auto-generates Display).
    - Doc comment: "User-initiated cancellation of a long-running solver (INTG/SOLVE/DIFEQ). Distinct from Domain. Surfaces as 'CANCELED' in GUI/CLI. D-28.7 / D-28.8 / D-28.9; wiring in Phase 31 / GUI-05."
    Add inline `#[cfg(test)] mod tests` with the three behaviors above (#[allow(clippy::unwrap_used)] on the mod if any unwraps slip in).
    Do NOT add any consumer `match` arms — those will be touched in later plans as needed. Audit existing `match err { HpError::* }` blocks; thiserror's PartialEq+Clone derives carry over, so non-exhaustive consumers will compile-error if any exists — fix by adding the explicit `HpError::Canceled => ...` arm (most likely none exist yet because HpError is rarely externally matched).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib error::tests::canceled_display error::tests::canceled_partial_eq</automated>
  </verify>
  <done>HpError::Canceled compiles; Display returns "canceled"; existing tests still pass; no consumer match needs extension (or extensions added).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add 7 new CalcState fields with serde discipline</name>
  <files>
    hp41-core/src/state.rs
    hp41-core/Cargo.toml
  </files>
  <read_first>
    - hp41-core/src/state.rs (full file; focus on lines 103-189 for serde precedents + `CalcState::new()` initializer)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"State-Field Changes to CalcState", lines 652-730 and §"Open Q2 — DifeqState field timing", recommendation: option (a) early-commitment, lines 906-911)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"`hp41-core/src/state.rs` (MODIFY)", lines 73-96)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (D-28.1, D-28.2, D-28.4, D-28.7, D-28.9)
  </read_first>
  <behavior>
    - Test 1: `CalcState::default()` constructs with `xrom_modules == 0b0000_0001`, `complex_mode == false`, `matrix_dim == None`, `matrix_active_reg == None`, `modal_program == None`, `modal_prompt == None`, `integ_state == None`, `solve_state == None`, `difeq_state == None`, and `cancel_requested.load(Relaxed) == false`.
    - Test 2: `serde_json::to_string(&state).unwrap()` followed by `serde_json::from_str::<CalcState>(...)` round-trips and preserves persistent fields (`xrom_modules`, `complex_mode`, `matrix_dim`, `matrix_active_reg`) but loses transient ones (set them to non-default values pre-serialize and verify they reset to defaults post-deserialize).
    - Test 3: deserializing a JSON string MISSING the 7 new fields (synthetic v2.2 shape) yields a `CalcState` with the documented defaults (no error). This is the save-file backward-compat assertion.
    - Test 4 (`cancel_field_present`): `state.cancel_requested` is `Arc<AtomicBool>`; cloning the Arc and storing `true` from the clone is visible via the original (verifies it's a real Arc, not a copy).
  </behavior>
  <action>
    Append the 7 new fields to the END of `pub struct CalcState` in `hp41-core/src/state.rs` (preserves serde field order for v1.0–v2.2 save files):
    - `xrom_modules: u8` with `#[serde(default = "default_xrom_modules")]` — bit 0 = Math 1 loaded.
    - `complex_mode: bool` with `#[serde(default)]` — D-28.2 auto-on at first complex op.
    - `matrix_dim: Option<(u8, u8)>` with `#[serde(default)]`.
    - `matrix_active_reg: Option<u8>` with `#[serde(default)]`.
    - `modal_program: Option<crate::ops::math1::modal::ModalProgram>` with `#[serde(default, skip)]`.
    - `modal_prompt: Option<String>` with `#[serde(default, skip)]` (D-28.4).
    - `integ_state: Option<crate::ops::math1::integ::IntegState>` with `#[serde(default, skip)]`.
    - `solve_state: Option<crate::ops::math1::solve::SolveState>` with `#[serde(default, skip)]`.
    - `difeq_state: Option<crate::ops::math1::difeq::DifeqState>` with `#[serde(default, skip)]` (RESEARCH Open Q2 recommendation (a) — early commitment).
    - `cancel_requested: std::sync::Arc<std::sync::atomic::AtomicBool>` with `#[serde(default = "default_cancel_requested", skip)]`.

    Add free functions at module scope (after the struct or in an impl block as preferred by file convention):
    ```
    fn default_xrom_modules() -> u8 { 0b0000_0001 }
    fn default_cancel_requested() -> std::sync::Arc<std::sync::atomic::AtomicBool> {
        std::sync::Arc::new(std::sync::atomic::AtomicBool::new(false))
    }
    ```

    Update `CalcState::new()` (~lines 161-189) to initialize ALL nine new fields with the same defaults the serde-default functions return; the field types `IntegState` / `SolveState` / `DifeqState` / `ModalProgram` do NOT exist yet (Plan 28-01 Task 5 lands them) — initialize as `None`; the `cancel_requested` field initializes via `default_cancel_requested()`.

    Type forward-references (`crate::ops::math1::modal::ModalProgram`, etc.) will be unresolved until Task 5 lands `modal.rs` and the integ/solve/difeq submodules ship with empty type stubs in Plans 28-07/08/09 — solution per RESEARCH §"Note: `difeq_state` field not in the Plan 28-01 list above" and PATTERNS §State-Field Changes: ship the four state-struct stubs (`IntegState`, `SolveState`, `DifeqState`) as `#[derive(Debug, Clone, Default)] pub struct Empty;` placeholders in Plan 28-01 alongside `modal.rs`, expanded in Plans 28-07/08/09. This Task 4 only adds the FIELDS; Task 5 ships the placeholder type stubs.

    Add `approx = "0.5.1"` to `hp41-core/Cargo.toml` `[dev-dependencies]` section (Pitfall 14 / Plan 28-05+ dependency).

    Add a `#[cfg(test)] mod tests` block with the 4 behaviors above, including the `cancel_field_present` Arc-sharing test.

    Add doc comment to `modal_prompt: Option<String>` field: `/// Modal prompt text. R/S key advances per D-28.5 (CLI/GUI wiring in Phase 29/31).` This surfaces the D-28.5 cross-phase contract (R/S key submits modal numeric input) at the field-declaration site so Phase 29 / Phase 31 implementers cannot miss it.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib state::tests::serde_roundtrip state::tests::v22_save_loads_with_defaults state::tests::cancel_field_present</automated>
  </verify>
  <done>7 fields land at struct tail, persistent fields carry `#[serde(default)]`, transient carry `#[serde(default, skip)]`, `cancel_requested` carries custom `default_cancel_requested`, `CalcState::default()` and `::new()` construct cleanly, serde round-trip + v2.2 load test green, `approx 0.5.1` in dev-deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create math1 module + xrom.rs resolver + modal.rs enum + state stubs</name>
  <files>
    hp41-core/src/lib.rs
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/math1/mod.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/src/ops/math1/modal.rs
    hp41-core/src/ops/math1/integ.rs
    hp41-core/src/ops/math1/solve.rs
    hp41-core/src/ops/math1/difeq.rs
  </files>
  <read_first>
    - hp41-core/src/lib.rs (lines 1-12 for the pub-mod precedent)
    - hp41-core/src/ops/mod.rs (lines 9-23 for pub-mod inside ops/, lines 104-201 for Op enum, lines 940-974 for `synthetic_byte_to_op` resolver shape)
    - hp41-core/src/ops/program.rs (lines 987-1007 for `builtin_card_op` resolver — direct analog)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"XROM Resolver Chain", lines 98-208 — `xrom_resolve` signature, `MATH_1` const shape; §"Modal-State-Machine Layer", lines 211-280)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-01 — Framework", subsection xrom.rs analog lines 43-71; subsection modal.rs analog lines 249-282)
  </read_first>
  <behavior>
    - Test 1: `xrom_resolve("SINH", 0b0000_0001)` returns `Some(Op::Sinh)` (after Plan 28-02 lands Op::Sinh — at this Plan 28-01 stage, Op::Sinh does not yet exist, so the resolver returns `None` for now). Use a placeholder name like `"__MATH1_PROBE"` mapped to a sentinel variant via a `#[cfg(test)]` helper — see action below.
    - Test 2: `xrom_resolve("UNKNOWN", 0b0000_0001)` returns `None`.
    - Test 3: `xrom_resolve("__MATH1_PROBE", 0)` returns `None` (module not loaded — bit 0 clear).
    - Test 4: `MATH_1.id == 7`, `MATH_1.name == "MATH 1A"`.
    - Test 5 (modal): `ModalProgram::Matrix(MatrixInputStep::OrderPrompt).current_prompt()` returns `Some("ORDER=?")`.
    - Test 6 (modal): `ModalProgram::Solve(SolveInputStep::FunctionNamePrompt).current_prompt()` returns `Some("FUNCTION NAME?")`.
  </behavior>
  <action>
    Create new directory `hp41-core/src/ops/math1/` with the following modules.

    `hp41-core/src/ops/math1/mod.rs` — module root declaring submodules:
    - `pub mod xrom;`
    - `pub mod modal;`
    - `pub mod integ;`
    - `pub mod solve;`
    - `pub mod difeq;`
    - Per-file Free42-contamination header comment at top: `// Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979); Free42 source consulted only as sanity-check oracle, not copied.` (and the same header in every submodule file).

    `hp41-core/src/ops/math1/xrom.rs`:
    - `pub struct XromModule { pub id: u8, pub name: &'static str, pub ops: &'static [(&'static str, Op)] }`.
    - `pub const MATH_1: XromModule = XromModule { id: 7, name: "MATH 1A", ops: &[] };` — empty slice for now; downstream plans (28-02..28-10) extend it.
    - `pub fn xrom_resolve(name: &str, modules: u8) -> Option<Op>` that checks `modules & 0b0000_0001 != 0` and consults a private `math1_resolve(name)` helper; for now `math1_resolve` returns `None` (downstream plans grow the match block).
    - To make the resolver testable BEFORE Plan 28-02 adds Op::Sinh: include a `#[cfg(test)]` test-only sentinel entry under a `#[cfg(test)]` const `MATH_1_TEST_OPS` that the test helpers consult — see test code under `#[cfg(test)] mod tests`. Public `MATH_1.ops` stays `&[]`.
    - Inline `#[cfg(test)] mod tests` covering Tests 1-4 from behavior.

    `hp41-core/src/ops/math1/modal.rs`:
    - `#[derive(Debug, Clone, PartialEq)] pub enum ModalProgram { Matrix(MatrixInputStep), Solve(SolveInputStep), Poly(PolyInputStep), Integ(IntegInputStep), Difeq(DifeqInputStep), Four(FourInputStep), Trans(TransInputStep) }` — 7 variants per RESEARCH §"`ModalProgram` enum".
    - One sub-enum per variant. Minimal initial sets (downstream plans extend them):
      - `MatrixInputStep::{ OrderPrompt, ElementPrompt(u8, u8), Ready, EditPrompt, SimeqInputPrompt(u8), SimeqDone }`
      - `SolveInputStep::{ FunctionNamePrompt, Guess1Prompt, Guess2Prompt }`
      - `PolyInputStep::{ DegreePrompt, CoefficientPrompt(u8, u8), Ready }`
      - `IntegInputStep::{ ModeChoice, FunctionNamePrompt, IntervalPrompt, SubdivisionPrompt }`
      - `DifeqInputStep::{ FunctionNamePrompt, OrderPrompt, StepSizePrompt, X0Prompt, Y0Prompt, Y1PrimePrompt }`
      - `FourInputStep::{ NumSamplesPrompt, NumFreqPrompt, FirstCoeffPrompt, SamplePrompt(u8), Ready }`
      - `TransInputStep::{ InitPrompt, ForwardPrompt, InversePrompt }`
    - All sub-enums derive `Debug, Clone, PartialEq`.
    - `impl ModalProgram { pub fn current_prompt(&self) -> Option<&'static str> { ... } }` returning the OM-cited prompt text for each (Step, sub-Step) — exhaustive match (NO `_ =>`) per FN-CLI-04 invariant from Phase 25. RESEARCH Open Q4 recommendation.
    - Inline `#[cfg(test)] mod tests` covering Tests 5-6 from behavior.

    `hp41-core/src/ops/math1/integ.rs`:
    - `#[derive(Debug, Clone, Default)] pub struct IntegState;` — empty stub; Plan 28-07 fills `user_label`, `a`, `b`, `n`, `accumulator`, `mode` fields (RESEARCH lines 361-369).
    - Free42-contamination header.

    `hp41-core/src/ops/math1/solve.rs`:
    - `#[derive(Debug, Clone, Default)] pub struct SolveState;` — empty stub; Plan 28-08 fills.
    - Free42-contamination header.

    `hp41-core/src/ops/math1/difeq.rs`:
    - `#[derive(Debug, Clone, Default)] pub struct DifeqState;` — empty stub; Plan 28-09 fills.
    - Free42-contamination header.

    Wire-up:
    - Add `pub mod math1;` inside `hp41-core/src/ops/mod.rs` near the other `pub mod` declarations (around line 9-23 per PATTERNS).
    - No change to `hp41-core/src/lib.rs` required (math1 is a submodule of `ops`, not a peer of `lib.rs::pub mod`).
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib ops::math1::xrom::tests ops::math1::modal::tests</automated>
  </verify>
  <done>`math1` submodule compiles; `xrom_resolve` returns `None` for all real names (no Math1 Ops yet) but tests pass via test-only probe; `ModalProgram` enum + 7 sub-enums + `current_prompt()` accessor compile and test green; state stubs (`IntegState`/`SolveState`/`DifeqState`) compile and are `Default::default()`-constructible (unblocks Task 4's forward references).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Extend resolver chain in program.rs (xrom fires LAST)</name>
  <files>hp41-core/src/ops/program.rs</files>
  <read_first>
    - hp41-core/src/ops/program.rs (focus on lines 67-81 `op_xeq` and lines 492-516 `Op::Xeq` label-miss arm; also line 987-1007 for `builtin_card_op` definition + visibility)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"XROM Resolver Chain", lines 98-122 — insertion site diff)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"`hp41-core/src/ops/program.rs` (MODIFY)", lines 186-246)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md `<decisions>` (C-28.4)
  </read_first>
  <behavior>
    - Test 1: `Op::Xeq("UNKNOWN".into())` in run_loop returns `HpError::InvalidOp` (unchanged from v2.2 — UNKNOWN matches no resolver).
    - Test 2: `Op::Xeq("WPRGM".into())` continues to dispatch to `Op::Wprgm` (built-in card-reader resolver fires BEFORE xrom — unchanged from v2.2).
    - Test 3 (xrom_chain_order.rs integration test, separate file in Task 8): after Plan 28-02 lands Op::Sinh, `Op::Xeq("SINH".into())` dispatches to `Op::Sinh` via the xrom branch; at Plan 28-01 stage the test asserts the call falls through to `Err(InvalidOp)` because no Math 1 Op is registered yet (positive case wired up in Plan 28-02 verification).
  </behavior>
  <action>
    Edit `hp41-core/src/ops/program.rs` to extend BOTH `xrom_resolve` callers:

    1. `op_xeq` (~lines 67-81, the interactive `Op::Xeq` dispatcher): in the label-miss `Err(_)` branch, insert the xrom check AFTER `builtin_card_op` and BEFORE `Err(HpError::InvalidOp)`. Pattern from RESEARCH lines 109-122:
       - If `builtin_card_op(&label)` returns `Some(card_op)` → `crate::ops::dispatch(state, card_op)?;`.
       - Else if `crate::ops::math1::xrom::xrom_resolve(&label, state.xrom_modules)` returns `Some(xrom_op)` → `crate::ops::dispatch(state, xrom_op)?;`.
       - Else return `Err(HpError::InvalidOp)`.

    2. `run_loop`'s `Op::Xeq(label)` arm (~lines 492-516, the programmatic dispatcher inside `match` over `Op::*`): identical insertion pattern. Use `&label` to satisfy borrow checker.

    DO NOT widen `builtin_card_op` visibility. DO NOT alter the order — xrom MUST fire LAST per C-28.4 / Pitfall 1.

    No CLI changes here. `hp41-cli/src/keys.rs::xeq_by_name_local_resolve` is the third call site (per RESEARCH §"XROM Resolver Chain" line 103) but lives in Phase 29 / CLI-01 — explicitly out of scope.

    Add inline `#[cfg(test)] mod tests` in `program.rs` for Test 1 and Test 2; Test 3 lives in the integration test file from Task 8.
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo test -p hp41-core --lib program::tests::xeq_unknown program::tests::xeq_wprgm_built_in</automated>
  </verify>
  <done>Both core resolver sites carry the xrom check AFTER builtin_card_op; v2.2 behavior unchanged (unknown → InvalidOp, WPRGM → Op::Wprgm); xrom path reachable for future Math 1 Op variants once Plan 28-02 registers them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Create Wave 0 test scaffolds (xrom_shadowing, xrom_chain_order, math1_user_callback, math1_complex_edge_cases, math1_op_test_count, v3_save_compat)</name>
  <files>
    hp41-core/tests/xrom_shadowing.rs
    hp41-core/tests/xrom_chain_order.rs
    hp41-core/tests/math1_user_callback.rs
    hp41-core/tests/math1_complex_edge_cases.rs
    hp41-core/tests/math1_op_test_count.rs
    hp41-core/tests/v3_save_compat.rs
  </files>
  <read_first>
    - hp41-core/tests/phase20_math.rs (lines 1-21) — scaffold reference per PATTERNS analog
    - hp41-core/src/ops/program.rs (verify `builtin_card_op` visibility — `pub(super)` per PATTERNS note line 320 — need a re-export shim for the test crate)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-VALIDATION.md (§"Wave 0 Requirements" lines 78-89)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"Wave 0 Gaps", lines 818-825; §"Pitfall-specific validation strategies" table lines 827-834)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"`hp41-core/tests/xrom_shadowing.rs` (NEW)", lines 285-321)
  </read_first>
  <behavior>
    - Test 1 (xrom_shadowing.rs): iterates `MATH_1.ops` (currently empty); the test compiles and passes vacuously. Plans 28-02..28-10 grow `MATH_1.ops` and the test then asserts no entry shadows a `builtin_card_op` name.
    - Test 2 (xrom_chain_order.rs): asserts `Op::Xeq("UNKNOWN")` returns `Err(InvalidOp)`; asserts a built-in name (`WPRGM`) wins over a hypothetical xrom shadow. Real positive xrom-fires-LAST assertion lands in Plan 28-02 verify (once a Math 1 mnemonic that doesn't shadow exists).
    - Test 3 (math1_user_callback.rs): COMPILE-ONLY scaffold with five `#[test] #[ignore]` placeholder functions named per RESEARCH §"Strict-reject test surface" (lines 337-352): `nested_integ_inside_integ_rejected`, `nested_solve_inside_integ_rejected`, `nested_integ_inside_solve_rejected`, `nested_difeq_inside_integ_rejected`, `user_fn_stops_aborts_integ`. Each contains `unimplemented!("filled by Plan 28-07/08/09")`. Plans 28-07/08/09 remove `#[ignore]` and fill the bodies.
    - Test 4 (math1_complex_edge_cases.rs): COMPILE-ONLY scaffold with `#[ignore]`d placeholders: `complex_atan2_zero_zero_returns_zero`, `ln_z_zero_returns_domain`, `c_div_zero_returns_divide_by_zero_before_division`, `z_pow_w_zero_neg_exp_returns_domain`. Plans 28-03/04 fill bodies.
    - Test 5 (math1_op_test_count.rs): grep meta-test. Strategy: collects names of new `Op::*` variants added 28-02..28-10 from the source of `hp41-core/src/ops/mod.rs` (by reading the file at test time via `include_str!`) AND counts how many `#[test]` functions across `hp41-core/tests/math1_*.rs` mention each name; asserts ≥ 5 per variant. At Plan 28-01 stage, no new Op variants exist yet — the test runs and passes vacuously. Documented per Pitfall 16. NO `_ =>` arms; use `include_str!` + regex-free string scanning.
    - Test 6 (v3_save_compat.rs): loads a synthetic v2.2-shape JSON string (manually crafted: contains all v2.2 fields, NONE of the v3.0 ones) via `serde_json::from_str::<CalcState>()`; asserts `state.xrom_modules == 0b0000_0001`, `state.complex_mode == false`, `state.matrix_dim == None`, etc.
  </behavior>
  <action>
    Create six integration test files under `hp41-core/tests/`. Each file:
    - Starts with the per-file Free42-contamination header comment.
    - Has `#![allow(clippy::unwrap_used)]` at file scope (mirror `tests/phase20_math.rs:15`).
    - Has the boilerplate imports per PATTERNS analog.

    `hp41-core/tests/xrom_shadowing.rs`:
    - One `#[test] fn math1_names_do_not_shadow_builtins()` body iterating `MATH_1.ops`.
    - `builtin_card_op` is `pub(super)` per PATTERNS line 320. Two options: (a) widen `builtin_card_op` to `pub` (rejected — narrows the invariant), or (b) add a `#[cfg(test)] pub fn __test_builtin_card_op(...)` re-export shim in `hp41-core/src/ops/program.rs` (chosen — keeps prod visibility unchanged). Add the shim in this task.
    - Test body uses the shim to assert non-collision.

    `hp41-core/tests/xrom_chain_order.rs`:
    - Asserts unknown name → `Err(InvalidOp)`.
    - Asserts `WPRGM` resolves to `Op::Wprgm` via run_loop dispatch (built-in wins over any future xrom alias).

    `hp41-core/tests/math1_user_callback.rs`:
    - 5 `#[test] #[ignore]` placeholder functions per Test 3 behavior.
    - Each contains a single line: `unimplemented!("filled by Plan 28-07/08/09");`.
    - Documented invariant in module doc-comment: "5 regression cases for user-callback re-entrancy. Plan 28-07 fills the INTG branches, 28-08 the SOLVE branches, 28-09 the DIFEQ branch."

    `hp41-core/tests/math1_complex_edge_cases.rs`:
    - 4 `#[test] #[ignore]` placeholder functions per Test 4 behavior.
    - Bodies are `unimplemented!("filled by Plan 28-03/04");`.

    `hp41-core/tests/math1_op_test_count.rs`:
    - One `#[test] fn each_math1_op_has_at_least_5_tests()` body using `include_str!("../src/ops/mod.rs")` (relative path from tests/) to read the Op enum, and `std::fs::read_to_string` + `glob`-style listing of `hp41-core/tests/math1_*.rs` to count name mentions. Since at this stage no Math1 Op variants exist, the test runs over an empty list and passes. Document the strategy in a long module doc-comment so Plans 28-02..28-10 maintainers understand what variants get scanned.

    `hp41-core/tests/v3_save_compat.rs`:
    - One `#[test] fn loads_synthetic_v22_save_without_v3_fields()` body. Compose the JSON string with serde_json! macro or raw string literal containing all v2.2 CalcState fields and none of the v3.0 ones. Assert defaults per Test 6 behavior. This is the canonical Pitfall 12 mitigation per RESEARCH line 729.

    The grep meta-test (math1_op_test_count.rs) is novel — no v2.2 analog. Document its strategy thoroughly in the module doc-comment.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test xrom_shadowing --test xrom_chain_order --test math1_user_callback --test math1_complex_edge_cases --test math1_op_test_count --test v3_save_compat</automated>
  </verify>
  <done>All 6 test files exist, compile, and pass under `cargo test`. Ignored placeholders in math1_user_callback and math1_complex_edge_cases are clearly labeled with the plan that will fill them. The grep meta-test passes vacuously (no Math1 variants yet). Save-compat test asserts v2.2 JSON loads cleanly into v3.0 CalcState shape.</done>
</task>

</tasks>

<verification>
After all tasks complete, run the following to confirm the framework holds:
- `cargo build -p hp41-core` (zero warnings under `#![deny(clippy::unwrap_used)]`).
- `cargo test -p hp41-core --lib` (xrom resolver + modal current_prompt + state serde round-trip).
- `cargo test -p hp41-core --test xrom_shadowing --test xrom_chain_order --test math1_user_callback --test math1_complex_edge_cases --test math1_op_test_count --test v3_save_compat` (all W0 scaffolds green).
- Verify ADRs exist: `test -f docs/adr/v3.0-003-inv-epsilon.md && test -f docs/adr/v3.0-004-intg-threshold.md`.
- Verify per-file Free42 header on every new math1 file: `grep -L 'independently re-derived' hp41-core/src/ops/math1/*.rs` must return empty.
- Verify SC-4 preserved: `grep -rn "fn op_(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing (trivial — Phase 28 doesn't touch hp41-gui).
</verification>

<success_criteria>
- 7 new CalcState fields land with correct serde discipline; default-construction works.
- HpError::Canceled exists with `#[error("canceled")]`.
- math1 submodule structure exists with xrom.rs (empty registry + resolver), modal.rs (7 ModalProgram variants + current_prompt accessor), state stubs (IntegState/SolveState/DifeqState).
- Resolver chain extended at op_xeq AND run_loop's Op::Xeq label-miss arm; xrom fires LAST.
- ADR-003 (INV-EPSILON) and ADR-004 (INTG-threshold) contain verbatim OM quotes with page citations — Plans 28-06 and 28-07 are unblocked.
- 6 Wave-0 test files compile and pass; per-Op test-count meta-test in place; v2.2 save-file backward-compat verified.
- approx 0.5.1 in [dev-dependencies].
- `just test` passes workspace-wide; `just coverage` does NOT need to hit 95% yet (Plan 28-02..28-10 grow coverage incrementally).
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-01-SUMMARY.md` when done, recording:
- INV-EPSILON value transcribed from OM (verbatim quote + page).
- INTG threshold formula transcribed from OM (verbatim quote + page).
- List of 7 new CalcState fields + their serde discipline.
- Confirmation that math1 submodule compiles and W0 test scaffolds are green.
- Affected files for downstream plans.
- One-line note: "Plan 28-02..28-10 may now grow `MATH_1.ops` and add Op variants to `Op` enum + dispatch arms in ops/mod.rs."
</output>
