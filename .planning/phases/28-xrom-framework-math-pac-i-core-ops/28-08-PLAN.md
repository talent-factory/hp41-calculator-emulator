---
phase: 28-xrom-framework-math-pac-i-core-ops
plan: 08
type: execute
wave: 8
depends_on:
  - 28-01
  - 28-07
files_modified:
  - hp41-core/src/ops/math1/solve.rs
  - hp41-core/src/ops/math1/mod.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/math1_user_callback.rs
  - hp41-core/tests/math1_solve_paths.rs
  - hp41-core/tests/numerical_accuracy.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - SOLV-01
  - SOLV-02
  - SOLV-03
  - SOLV-04
  - SOLV-05
  - SOLV-06
  - SOLV-07
  - SOLV-08

must_haves:
  truths:
    - "2 new Op variants land in Op enum + dispatch() + execute_op() + MATH_1.ops: Op::Solve (XEQ \"SOLVE\" master with FUNCTION NAME? / GUESS 1=? / GUESS 2=? prompts) and Op::Sol (XEQ \"SOL\" sub-entry that bypasses the prompts; guesses pre-set in R00/R01 per SOLV-02 / RESEARCH §SOLVE line 566)"
    - "SolveState struct (Plan 28-01 stubbed as empty; Plan 28-08 grows it per PATTERNS line 536 / RESEARCH lines 371-379): { user_label: String, x1: HpNum, x2: HpNum, fx1: HpNum, fx2: HpNum, iteration: u8 } with #[derive(Debug, Clone, Default)]; iteration cap 100 per SOLV-07"
    - "SolveInputStep extended (Plan 28-01 stubbed FunctionNamePrompt/Guess1Prompt/Guess2Prompt; Plan 28-08 adds Ready arm to clear modal after submission); ModalProgram::current_prompt() owned-String arms (signature change landed in Plan 28-06 Task 1): Solve(FunctionNamePrompt) → \"FUNCTION NAME?\", Solve(Guess1Prompt) → \"GUESS 1=?\", Solve(Guess2Prompt) → \"GUESS 2=?\", Solve(Ready) → None"
    - "Modified secant iteration per SOLV-03 / OM Chapter 6 (~p.34 per RESEARCH §canonical_refs); algorithm uses xₙ₊₁ = xₙ − f(xₙ)·(xₙ − xₙ₋₁)/(f(xₙ) − f(xₙ₋₁)) with f computed by re-entering run_loop on the user LBL"
    - "Three termination paths per SOLV-04 / Pitfall 3 / PATTERNS line 537: NO ROOT FOUND (iteration cap reached without convergence), ROOT IS <v> (|f(x)| below convergence threshold), ROOT IS BETWEEN <v1> AND <v2> (sign change detected but iteration could not narrow further) — each pushed to state.print_buffer (NOT modal_prompt per PATTERNS line 537 — these are RESULTS, not prompts)"
    - "Reuses INTG's user-program callback infrastructure per SOLV-06: same run_loop re-entry pattern as op_integ_run_loop from Plan 28-07 (NOT run_program); mirrors Op::XeqInd precedent at program.rs:476-491 per PATTERNS line 528"
    - "Pre-mutation 4-deep call_stack cap (Pitfall 4): state.call_stack.len() >= 4 → HpError::CallDepth BEFORE any mutation; identical guard to Plan 28-07's op_integ at integ.rs"
    - "XROM-08 strict-reject nested INTG/SOLVE/DIFEQ at op entry per D-28.7 / ADR-002 / RESEARCH lines 296-298: state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() → HpError::InvalidOp BEFORE mutation. This satisfies SOLV-08 (Nested INTG-in-SOLVE / SOLVE-in-INTG abgelehnt) AND covers the nested-SOLVE-in-SOLVE case"
    - "Per-64-samples cancellation check (D-28.8): iteration & 0x3F == 0 → state.cancel_requested.load(Ordering::Relaxed); if true, clears solve_state and returns HpError::Canceled (D-28.9). Wiring of request_cancel lands Phase 31 / GUI-05 — Plan 28-08 ships the check stub only"
    - "100-iteration cap (SOLV-07): when SolveState.iteration reaches 100, push \"NO ROOT FOUND\" to print_buffer and return Ok(()) (terminal, not an error per OM convention) — MAX_STEPS = 1_000_000 outer run_loop budget continues to apply on top of the per-SOLVE 100-iteration cap"
    - "Op::Solve and Op::Sol dispatch arms return Err(InvalidOp) when called OUTSIDE a run_loop context; real implementation runs ONLY in the run_loop match arm (mirrors Op::XeqInd at program.rs:839+ and Op::Integ at Plan 28-07 program.rs)"
    - "Op::Sol bypasses the 3-prompt modal (SOLV-02): when invoked, op_solve_run_loop reads user_label from state.solve_state (must be pre-staged), x1 from R00, x2 from R01 (scratch per SOLV-05); if state.solve_state.is_none() returns Err(InvalidOp) with doc comment 'Op::Sol requires a prior Op::Solve setup'"
    - "math1_user_callback.rs Plan 28-01 scaffold gets 2 more #[ignore]'d placeholders filled (nested_solve_inside_integ_rejected from Plan 28-07's leftover list AND nested_solve_inside_solve_rejected NEW); Plan 28-09 fills the remaining nested-DIFEQ branches"
    - "Per-Op test count >= 5 (Pitfall 16): Op::Solve has >= 5 tests (root_found_simple, root_not_found_capped, root_between_sign_change, cancellation, nested_rejection), Op::Sol has >= 5 tests (bypasses_prompts, reads_from_r00_r01, no_setup_rejected, iteration_inherits, cancel_propagates)"
    - "Display arms in both prgm_display.rs files (CLAUDE.md SC-4-literal carve-out per planning context): Op::Solve → \"SOLVE\", Op::Sol → \"SOL\" added to op_display_name match in hp41-cli/src/prgm_display.rs AND hp41-gui/src-tauri/src/prgm_display.rs"
  nyquist_dimensions:
    - "SOLV-01..02 unit (master + sub-entry) — cargo test -p hp41-core --lib math1::solve::tests::master_op math1::solve::tests::sub_entry_op"
    - "SOLV-03 unit (modified secant correctness) — cargo test -p hp41-core --lib math1::solve::tests::secant_root_polynomial"
    - "SOLV-04 integration (3 termination paths) — cargo test -p hp41-core --test math1_solve_paths"
    - "SOLV-05 unit (R00/R01 scratch + SolveState serde-skip) — cargo test -p hp41-core --lib math1::solve::tests::scratch_registers state::tests::serde_roundtrip"
    - "SOLV-06 integration (user-callback re-entry) — cargo test -p hp41-core --test math1_user_callback"
    - "SOLV-07 unit (100-iteration cap) — cargo test -p hp41-core --lib math1::solve::tests::iteration_cap"
    - "SOLV-08 integration (XROM-08 strict-reject) — cargo test -p hp41-core --test math1_user_callback nested_solve_inside_integ_rejected nested_solve_inside_solve_rejected"
    - "Pitfall 4 — cargo test -p hp41-core --lib math1::solve::tests::call_stack_full"
    - "Pitfall 11 plumbing — cargo test -p hp41-core --lib math1::solve::tests::cancel_per_64_iterations"
    - "Per-Op count — cargo test -p hp41-core --test math1_op_test_count"
    - "Numerical accuracy — cargo test -p hp41-core --test numerical_accuracy solve_cases"
    - "Pitfall 1 — cargo test -p hp41-core --test xrom_shadowing (SOLVE/SOL entries non-collision)"
  key_links:
    - from: hp41-core/src/ops/math1/solve.rs
      to: hp41-core/src/ops/math1/integ.rs
      via: "op_solve_run_loop mirrors op_integ_run_loop verbatim (same run_loop re-entry shape, same strict-reject, same pre-mutation call_stack cap, same per-64 cancellation check)"
      pattern: "op_solve_run_loop"
    - from: hp41-core/src/ops/math1/solve.rs
      to: hp41-core/src/ops/program.rs
      via: "run_loop re-entry from inside op_solve for each secant iteration (NOT run_program)"
      pattern: "run_loop\\(state, program"
    - from: hp41-core/src/state.rs
      to: hp41-core/src/ops/math1/solve.rs
      via: "state.solve_state Option<SolveState> field landed in Plan 28-01; Plan 28-08 grows SolveState struct fields per PATTERNS line 536"
      pattern: "state\\.solve_state"
    - from: hp41-core/src/ops/math1/solve.rs
      to: hp41-core/src/print.rs
      via: "Three termination paths push lines to state.print_buffer (PATTERNS line 537 — results not prompts)"
      pattern: "state\\.print_buffer\\.push"
---

<objective>
Land SOLVE — the SECOND user-callback program in v3.0. `Op::Solve` (XEQ "SOLVE") opens a 3-prompt modal (FUNCTION NAME? / GUESS 1=? / GUESS 2=?) and runs modified-secant iteration; `Op::Sol` (XEQ "SOL") is the sub-entry that bypasses the prompts. Both reuse the user-program callback infrastructure from Plan 28-07 verbatim — same `run_loop` re-entry, same strict-reject of nested INTG/SOLVE/DIFEQ per ADR-002 / D-28.7, same pre-mutation 4-deep `call_stack` cap.

Purpose: validates the Plan 28-07 infrastructure under a second consumer; closes SOLV-01..08; ships the 3 OM-cited termination paths (`NO ROOT FOUND` / `ROOT IS <v>` / `ROOT IS BETWEEN <v1> AND <v2>`) via `print_buffer` per PATTERNS line 537 (Pitfall 3 mitigation); fills 2 more `#[ignore]`d cases in `math1_user_callback.rs` (nested-SOLVE-in-INTG and nested-SOLVE-in-SOLVE).

Output: `hp41-core/src/ops/math1/solve.rs` (~400 LOC including tests) with full `SolveState` struct + `op_solve` (dispatch stub) + `op_solve_run_loop` (real impl) + `op_sol_run_loop` (sub-entry); 2 new Op variants integrated through 3-place exhaustive match + xrom_resolve; new `math1_solve_paths.rs` integration test covering the 3 termination paths; ≥ 4 numerical_accuracy cases with OM citations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-PLAN.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-PLAN.md
@hp41-core/src/ops/math1/integ.rs
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/ops/program.rs
@hp41-core/src/state.rs

<interfaces>
<!-- From Plan 28-07 op_integ_run_loop template (RESEARCH lines 293-330; PATTERNS lines 466-521) — Plan 28-08 MIRRORS this structure verbatim. -->

```
// op_solve_run_loop skeleton — MIRRORS op_integ_run_loop from integ.rs
pub(super) fn op_solve_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError> {
    // 1. Strict-reject nested INTG/SOLVE/DIFEQ (XROM-08 / D-28.7)
    if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() {
        return Err(HpError::InvalidOp);
    }
    // 2. Pre-mutation 4-deep call_stack cap (Pitfall 4)
    if state.call_stack.len() >= 4 {
        return Err(HpError::CallDepth);
    }
    // 3. Reset cancellation flag (D-28.7)
    state.cancel_requested.store(false, Ordering::Relaxed);
    // 4. Initialize SolveState from modal flow OR from existing solve_state for Op::Sol sub-entry
    let mut solve_state = /* read modal-staged params */;
    state.solve_state = Some(solve_state.clone());

    // 5. Compute f(x1) and f(x2) via run_loop re-entry on user_label
    // 6. Iterate secant method up to 100 iterations
    for iter in 0..100 {
        if iter & 0x3F == 0 && state.cancel_requested.load(Ordering::Relaxed) {
            state.solve_state = None;
            return Err(HpError::Canceled);
        }
        // compute x_new = x2 - f(x2)*(x2-x1)/(f(x2)-f(x1));
        // push to state.stack; state.call_stack.push(state.pc);
        // state.pc = find_in_program(program, &user_label)? + 1;
        // run_loop(state, program, MAX_STEPS_REMAINING)?;
        // after return: f_new = state.stack.x().clone()
        // check |f_new| < threshold => push "ROOT IS <v>" to print_buffer; clear solve_state; return Ok(())
        // check sign change between f(x1) and f(x2) with no narrowing => "ROOT IS BETWEEN <v1> AND <v2>"; clear; return Ok(())
        // else shift x1 ← x2, x2 ← x_new; continue
    }
    // 7. Iteration cap reached: push "NO ROOT FOUND" to print_buffer; clear solve_state; return Ok(())
    state.print_buffer.push("NO ROOT FOUND".to_string());
    state.solve_state = None;
    Ok(())
}
```

<!-- From PATTERNS lines 530-537: -->
```
- Strict-reject guard for Plan 28-09 (DIFEQ) grows to:
    if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some()
- SolveState (RESEARCH lines 371-379): { user_label, x1, x2, fx1, fx2, iteration: u8 }. Iteration cap 100 per SOLV-07.
- Three termination paths (Pitfall 3 / RESEARCH line 568): NO ROOT FOUND, ROOT IS <v>, ROOT IS BETWEEN <v1> AND <v2>
  — write to state.print_buffer (NOT modal_prompt — these are results, not prompts).
```

<!-- From PATTERNS Pattern 3 / `hp41-cli/src/prgm_display.rs:27-66` — display-arm template. Plan 28-08 adds 2 arms in BOTH the CLI and GUI copies. -->
```
fn op_display_name(op: &Op) -> String {
    match op {
        Op::Add => "+ ".to_string(),
        // ...
        Op::Solve => "SOLVE".to_string(),    // Plan 28-08
        Op::Sol   => "SOL".to_string(),       // Plan 28-08
        // ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SolveInputStep + ModalProgram::current_prompt for SOLVE workflow</name>
  <files>hp41-core/src/ops/math1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (post-Plan 28-07 — owned-String current_prompt signature from Plan 28-06 Task 1; existing SolveInputStep stubs from Plan 28-01: FunctionNamePrompt, Guess1Prompt, Guess2Prompt)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"SOLVE" Op table lines 561-572)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md (`<decisions>` D-28.4 modal_prompt String type)
  </read_first>
  <behavior>
    - `SolveInputStep` variants (extend Plan 28-01's stubs): FunctionNamePrompt, Guess1Prompt, Guess2Prompt, Ready (NEW arm; clears modal after submission per established Plan 28-07 IntegInputStep::Ready pattern).
    - `ModalProgram::current_prompt()` exhaustive match Solve arms (owned-String signature post-Plan 28-06):
      - Solve(FunctionNamePrompt) → Some("FUNCTION NAME?".to_string())
      - Solve(Guess1Prompt) → Some("GUESS 1=?".to_string())
      - Solve(Guess2Prompt) → Some("GUESS 2=?".to_string())
      - Solve(Ready) → None
    - Preserves exhaustive match (no `_ =>` per FN-CLI-04 invariant from Phase 25).
  </behavior>
  <action>
    Extend `SolveInputStep` enum in `hp41-core/src/ops/math1/modal.rs`: add `Ready` variant (mirrors Plan 28-07 IntegInputStep::Ready). All variants derive Debug, Clone, PartialEq (already established in Plan 28-01).

    Fill the `ModalProgram::current_prompt()` exhaustive match's Solve arm with all 4 sub-variants per behavior. Use owned-String values via `.to_string()` since the signature was widened in Plan 28-06 Task 1.

    Add 4 unit tests to the inline `#[cfg(test)] mod tests` covering each Solve variant's prompt; mirror the Plan 28-07 Task 1 test pattern.

    Do NOT touch other ModalProgram arms (Matrix, Poly, Integ, Difeq, Four, Trans) — they were closed by prior plans.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::modal::tests</automated>
  </verify>
  <done>4 SolveInputStep variants (including new Ready) return correct prompts via current_prompt(); tests green; exhaustive match preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement solve.rs (full SolveState struct + op_solve_run_loop + op_sol_run_loop + dispatch stubs)</name>
  <files>
    hp41-core/src/ops/math1/solve.rs
    hp41-core/src/ops/program.rs
  </files>
  <read_first>
    - hp41-core/src/ops/math1/solve.rs (current empty `SolveState` stub from Plan 28-01 Task 5)
    - hp41-core/src/ops/math1/integ.rs (Plan 28-07 — full op_integ_run_loop body; Plan 28-08 MIRRORS its structure)
    - hp41-core/src/ops/program.rs (full file — `run_loop` signature, `Op::XeqInd` re-entry at lines 476-491 + dispatch arm at line 839+, and the Op::Integ run_loop arm added by Plan 28-07)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-07-PLAN.md (Task 2 — verbatim template for the run_loop re-entry shape Plan 28-08 mirrors)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§"Plan 28-08 / 28-09 — SOLVE / DIFEQ" lines 524-537 + §"User-Callback Infrastructure" lines 283-353; §"Op::XeqInd precedent" lines 466-521)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-RESEARCH.md (§"SOLVE" Op table lines 561-572; §"SolveState struct shape" lines 371-379)
    - HP-41C Math Pac OM Chapter 6 (~p.34) — modified secant iteration algorithm
  </read_first>
  <behavior>
    - `SolveState` (replace Plan 28-01's empty stub with the full struct per RESEARCH lines 371-379 + PATTERNS line 536):
      - `pub user_label: String`
      - `pub x1: HpNum` (older guess)
      - `pub x2: HpNum` (newer guess)
      - `pub fx1: HpNum` (f(x1))
      - `pub fx2: HpNum` (f(x2))
      - `pub iteration: u8` (cap 100 per SOLV-07)
      - `#[derive(Debug, Clone, Default)]`
    - `pub(super) fn op_solve_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` — the REAL implementation for `Op::Solve` master entry. Runs from inside `run_loop`'s match arm.
    - `pub(super) fn op_sol_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` — the REAL implementation for `Op::Sol` sub-entry. Bypasses the 3-prompt modal; reads user_label from `state.solve_state` (must be pre-staged from a prior `Op::Solve` setup), x1 from R00, x2 from R01 (scratch per SOLV-05). If `state.solve_state.is_none()` returns `Err(HpError::InvalidOp)` with doc comment "Op::Sol requires a prior Op::Solve setup that staged user_label into solve_state".
    - `pub fn op_solve(state: &mut CalcState) -> Result<(), HpError>` — dispatch arm: returns `Err(HpError::InvalidOp)` because SOLVE can only run inside run_loop. Mirrors Op::XeqInd precedent at program.rs:839+ and Op::Integ from Plan 28-07.
    - `pub fn op_sol(state: &mut CalcState) -> Result<(), HpError>` — same pattern; dispatch returns `Err(InvalidOp)`.

    Op::Solve flow inside run_loop (mirrors op_integ_run_loop):
    1. Strict-reject (XROM-08 / D-28.7 / SOLV-08): `if state.integ_state.is_some() || state.solve_state.is_some() || state.difeq_state.is_some() { return Err(HpError::InvalidOp); }` BEFORE any mutation.
    2. Pre-mutation call_stack cap (Pitfall 4): `if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }` BEFORE any mutation.
    3. Reset cancellation flag: `state.cancel_requested.store(false, std::sync::atomic::Ordering::Relaxed);`.
    4. Read user_label + x1 + x2 from the modal-staged values (the modal flow set these via SolveInputStep::FunctionNamePrompt/Guess1Prompt/Guess2Prompt — the read path is symmetric with Plan 28-07's modal-staging convention; document the exact read sites in code comments).
    5. Initialize `state.solve_state = Some(SolveState { user_label, x1, x2, fx1: HpNum::default(), fx2: HpNum::default(), iteration: 0 });`.
    6. Compute f(x1) by re-entering run_loop: push x1 to stack, call_stack.push(pc), pc = find_in_program(program, &user_label)? + 1, run_loop(state, program, MAX_STEPS_REMAINING)?; read state.stack.x() into fx1; same for f(x2).
    7. Modified-secant loop per SOLV-03 (OM Chapter 6):
       ```
       for iter in 0..100 {
           if iter & 0x3F == 0 && state.cancel_requested.load(Ordering::Relaxed) {
               state.solve_state = None;
               return Err(HpError::Canceled);
           }
           let denom = fx2 - fx1;
           if denom == HpNum::default() {
               // f(x1) == f(x2) — no secant; if sign change exists report BETWEEN, else NO ROOT FOUND
               break;
           }
           let x_new = x2 - fx2 * (x2 - x1) / denom;
           // recompute f(x_new) via run_loop re-entry
           // convergence check: |f(x_new)| < threshold => push "ROOT IS <format_hpnum(x_new)>" to print_buffer; clear; Ok
           // sign-change check (no narrowing): if fx_old and fx_new have opposite signs AND |x_new - x_old| stuck => push "ROOT IS BETWEEN <v1> AND <v2>"; clear; Ok
           // else shift: x1 ← x2, fx1 ← fx2, x2 ← x_new, fx2 ← f(x_new); iteration += 1
       }
       // iter cap reached
       state.print_buffer.push("NO ROOT FOUND".to_string());
       state.solve_state = None;
       Ok(())
       ```
    8. Termination paths per SOLV-04 / Pitfall 3 / PATTERNS line 537: each written to `state.print_buffer` (NOT modal_prompt — these are results not prompts):
       - "ROOT IS <v>" when |f(x)| below convergence threshold
       - "ROOT IS BETWEEN <v1> AND <v2>" when sign change detected with no further narrowing
       - "NO ROOT FOUND" when iteration cap (100) reached without convergence

    Op::Sol flow (SOLV-02 — sub-entry bypassing prompts):
    1. Same strict-reject, same call_stack cap, same cancel-flag reset.
    2. Read user_label from `state.solve_state.as_ref().ok_or(HpError::InvalidOp)?.user_label.clone()` — error if no prior SOLVE setup.
    3. Read x1 from `state.regs[0]` (R00), x2 from `state.regs[1]` (R01) per SOLV-05 scratch convention.
    4. Same secant loop body as op_solve_run_loop (factor into a shared private helper `fn run_secant_loop(state, program, label, x1, x2) -> Result<(), HpError>` to avoid duplication).
  </behavior>
  <action>
    Replace Plan 28-01's empty `SolveState` stub in `hp41-core/src/ops/math1/solve.rs` with the full struct per behavior (6 fields). Preserve the Free42-contamination header from Plan 28-01.

    Implement the private helper `fn run_secant_loop(state: &mut CalcState, program: &[Op], user_label: &str, x1: HpNum, x2: HpNum) -> Result<(), HpError>` that owns the 100-iteration loop body, the per-64 cancellation check, the convergence/sign-change/cap-reached branches, and the print_buffer writes. Both op_solve_run_loop and op_sol_run_loop call into this helper after setting up their parameters.

    Implement `pub(super) fn op_solve_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` per behavior. Read modal-staged parameters from the same site Plan 28-07 used (document the exact site choice in a code comment alongside a `// CROSS-REFERENCE: see Plan 28-07 op_integ_run_loop for the symmetric pattern`).

    Implement `pub(super) fn op_sol_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` per behavior.

    Implement `pub fn op_solve(state: &mut CalcState) -> Result<(), HpError>` and `pub fn op_sol(state: &mut CalcState) -> Result<(), HpError>` — both dispatch arms return `Err(HpError::InvalidOp)` with doc comments "Op::Solve/Sol can only run inside run_loop; real implementation in op_solve_run_loop / op_sol_run_loop".

    In `hp41-core/src/ops/program.rs`, add the `Op::Solve` and `Op::Sol` arms to `run_loop`'s match block, calling `crate::ops::math1::solve::op_solve_run_loop(state, program)?;` and `crate::ops::math1::solve::op_sol_run_loop(state, program)?;` respectively. Place them next to `Op::Integ` for pattern symmetry.

    Inline `#[cfg(test)] mod tests` with file-scope `#![allow(clippy::unwrap_used)]` on the test mod:
    - `master_op`: tests Op::Solve opens "FUNCTION NAME?" modal and registers solve_state.
    - `sub_entry_op`: tests Op::Sol bypasses prompts and uses R00/R01.
    - `secant_root_polynomial`: SOLVE on f(x) = x² - 2 with guesses 1 and 2 → root ≈ 1.4142; assert `approx::assert_relative_eq!(max_relative = 1e-7)` (Pitfall 14).
    - `root_found_simple`: SOLVE on f(x) = x with guesses -1 and 1 → "ROOT IS 0" in print_buffer.
    - `root_not_found_capped`: SOLVE on a non-converging f → after 100 iterations push "NO ROOT FOUND" to print_buffer; assert state.solve_state is None.
    - `root_between_sign_change`: SOLVE on a function with sign change that can't be narrowed → "ROOT IS BETWEEN <v1> AND <v2>".
    - `iteration_cap`: assert the secant loop terminates after exactly 100 iterations on a non-converging case.
    - `scratch_registers`: Op::Sol reads x1 from R00, x2 from R01.
    - `bypasses_prompts`: Op::Sol does NOT open a modal (state.modal_program stays None).
    - `no_setup_rejected`: Op::Sol with state.solve_state.is_none() returns Err(InvalidOp).
    - `nested_rejection`: pre-set state.integ_state = Some(IntegState::default()); call op_solve_run_loop → Err(InvalidOp); also assert pre-set state.solve_state = Some(...) → Err(InvalidOp); also pre-set state.difeq_state = Some(...) → Err(InvalidOp).
    - `call_stack_full`: pre-fill state.call_stack with 4 entries; call op_solve_run_loop → Err(CallDepth) BEFORE mutation; assert state.call_stack still has 4 entries (no leak; state.solve_state stays None).
    - `cancel_per_64_iterations`: pre-set state.cancel_requested.store(true); run SOLVE that would take >= 64 iterations; assert Err(Canceled) and state.solve_state cleared after the first per-64 check.
    - LiftEffect declarations verified.

    Target: ≥ 10 unit tests covering both Ops per Pitfall 16 (≥ 5 per Op × 2 Ops = ≥ 10).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib math1::solve::tests</automated>
  </verify>
  <done>SolveState full struct compiled; op_solve_run_loop and op_sol_run_loop implement modified secant per SOLV-03; XROM-08 strict-reject works at op entry; Pitfall 4 call_stack cap works pre-mutation; cancellation per-64-iterations fires; iteration cap 100 fires; 3 termination paths each push correct print_buffer line; ≥ 10 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Op enum + dispatch + execute_op + xrom_resolve; fill math1_user_callback.rs SOLVE branches; ship math1_solve_paths.rs + numerical_accuracy cases; add prgm_display arms (CLI + GUI)</name>
  <files>
    hp41-core/src/ops/mod.rs
    hp41-core/src/ops/program.rs
    hp41-core/src/ops/math1/xrom.rs
    hp41-core/tests/math1_user_callback.rs
    hp41-core/tests/math1_solve_paths.rs
    hp41-core/tests/numerical_accuracy.rs
    hp41-cli/src/prgm_display.rs
    hp41-gui/src-tauri/src/prgm_display.rs
  </files>
  <read_first>
    - hp41-core/src/ops/mod.rs (post-Plan 28-07 — Op enum + dispatch arms)
    - hp41-core/src/ops/program.rs (post-Plan 28-07 — execute_op arms including Op::Integ that returns InvalidOp; run_loop arm for Op::Integ)
    - hp41-core/src/ops/math1/xrom.rs (post-Plan 28-07 — 34 entries; grow to 36 by adding SOLVE + SOL)
    - hp41-core/tests/math1_user_callback.rs (post-Plan 28-07 — 2 of 5 cases filled; 3 #[ignore]'d remain: nested_solve_inside_integ_rejected, nested_integ_inside_solve_rejected, nested_difeq_inside_integ_rejected — Plan 28-08 un-ignores the first TWO that involve SOLVE)
    - hp41-cli/src/prgm_display.rs (full file — current op_display_name match arms; Plan 28-08 adds Op::Solve / Op::Sol arms per CLAUDE.md SC-4-literal carve-out documented in planning context)
    - hp41-gui/src-tauri/src/prgm_display.rs (full file — same as CLI; SC-4 spirit allows display formatters per CLAUDE.md note on op_display_name)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-02-PLAN.md (Task 2 — proof-of-pattern for Op enum + dispatch + execute_op + xrom_resolve quadruple)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-PATTERNS.md (§Pattern 3 lines 579-609 — 4-place exhaustive match)
  </read_first>
  <behavior>
    - 2 new Op variants integrated through Phase 28 3-place exhaustive match (Op enum + dispatch + execute_op):
      - `Op::Solve` (master, opens 3-prompt modal)
      - `Op::Sol` (sub-entry, bypasses prompts)
    - Both dispatch and execute_op arms return `Err(HpError::InvalidOp)` per RESEARCH §"execute_op vs run_loop split for callback ops" lines 769-775 (mirrors Op::Integ from Plan 28-07).
    - run_loop arms call into the real `op_solve_run_loop` / `op_sol_run_loop` from Task 2.
    - xrom_resolve registers: `("SOLVE", Op::Solve)` AND `("SOL", Op::Sol)`. After Plan 28-08, `MATH_1.ops` has 36 entries.
    - `math1_user_callback.rs`: 2 of the 3 remaining `#[ignore]`'d placeholders filled by Plan 28-08:
      - `nested_solve_inside_integ_rejected`: outer XEQ INTG with a callback that XEQ "SOLVE" → inner Op::Solve returns InvalidOp; assert.
      - NEW test `nested_solve_inside_solve_rejected`: outer XEQ SOLVE with a callback that XEQ "SOLVE" → inner returns InvalidOp; assert.
      - `nested_integ_inside_solve_rejected`: outer XEQ SOLVE with a callback that XEQ "INTG" → inner Op::Integ returns InvalidOp; assert.
      - Last remaining `#[ignore]`d placeholder (`nested_difeq_inside_integ_rejected`) stays for Plan 28-09.
    - `math1_solve_paths.rs` NEW integration test file with 3 tests, one per OM termination path (SOLV-04 / Pitfall 3):
      - `solve_root_found`: SOLVE on f(x) = x² - 2 with guesses 1 and 2 → print_buffer contains "ROOT IS 1.4142" (or matching OM precision).
      - `solve_no_root_found`: SOLVE on a non-converging f → print_buffer contains "NO ROOT FOUND".
      - `solve_root_between`: SOLVE on a function with sign change that can't narrow → print_buffer contains "ROOT IS BETWEEN <v1> AND <v2>".
    - `numerical_accuracy.rs` grows with ≥ 4 SOLV cases (OM Chapter 6 worked examples: polynomial root, transcendental root, sign-change case, capped case). Each carries OM citation + Free42 cross-check note per D-27.7 precedent.
    - `prgm_display.rs` (BOTH CLI and GUI copies per CLAUDE.md SC-4-literal carve-out from planning context): add `Op::Solve => "SOLVE".to_string()` and `Op::Sol => "SOL".to_string()` arms to `op_display_name`.
    - `xrom_shadowing.rs` runs over 36 entries non-collision.
  </behavior>
  <action>
    1. Add `Op::Solve` and `Op::Sol` variants to the `Op` enum in `hp41-core/src/ops/mod.rs` under header `// ── Phase 28: SOLVE (Plan 28-08) ────────`. Add 2 dispatch arms returning `Err(HpError::InvalidOp)` per the "Op variants that run only in run_loop" pattern from Plan 28-07 (mirrors Op::Integ).

    2. Add `Op::Solve` and `Op::Sol` arms to `execute_op` in `program.rs` — same `Err(InvalidOp)` per RESEARCH lines 769-775 / Plan 28-07 precedent.

    3. Confirm the `Op::Solve` and `Op::Sol` arms in `run_loop`'s match block from Task 2 are wired correctly (placed adjacent to `Op::Integ` per pattern symmetry).

    4. Grow `MATH_1.ops` and `math1_resolve` in `xrom.rs`: add `("SOLVE", Op::Solve)` and `("SOL", Op::Sol)`. Confirmed safe by RESEARCH §"Resolver-Chain Conflict Map" line 628 — neither "SOLVE" nor "SOL" appears in v2.2 `xeq_by_name_local_resolve` or `builtin_card_op`.

    5. In `hp41-core/tests/math1_user_callback.rs`:
       - Remove `#[ignore]` from `nested_solve_inside_integ_rejected` (existing Plan 28-01 placeholder); fill body per behavior: build an outer INTG with a callback program that contains `Op::Xeq("SOLVE".to_string())`; assert the inner XEQ resolves to Op::Solve via xrom_resolve and that op_solve_run_loop's strict-reject returns InvalidOp.
       - Remove `#[ignore]` from `nested_integ_inside_solve_rejected`; fill body: outer SOLVE with callback that XEQ "INTG"; assert Err(InvalidOp).
       - Add NEW `#[test] fn nested_solve_inside_solve_rejected()`: outer SOLVE with callback that XEQ "SOLVE"; assert Err(InvalidOp).
       - Leave `nested_difeq_inside_integ_rejected` `#[ignore]` for Plan 28-09 (with comment "filled by Plan 28-09").

    6. Create `hp41-core/tests/math1_solve_paths.rs` (NEW file):
       - File header: Free42-contamination disclaimer + `#![allow(clippy::unwrap_used)]`.
       - 3 integration tests per SOLV-04 termination paths per behavior. Each test simulates `Op::Xeq("SOLVE".to_string())` → modal flow (manually stage by setting state.modal_program / state.solve_state directly OR by using the modal-completion path) → invoke run_loop → assert `state.print_buffer` contains the expected termination string verbatim.
       - Citations: each test has a `// Source: HP 00041-90034 p.<n>, ex.<m>` doc-comment + `// Free42 v3.0.5: <expected behavior>` cross-check note.

    7. Add ≥ 4 cases to `hp41-core/tests/numerical_accuracy.rs` for SOLVE with OM citations per D-27.7 precedent. Cases minimum:
       - `solve_polynomial_root`: f(x) = x² - 2 with guesses 1 and 2 → root ≈ 1.41421356 (OM Chapter 6 polynomial example).
       - `solve_transcendental_root`: f(x) = sin(x) with guesses 3 and 4 → root ≈ π (OM-cited).
       - `solve_no_convergence`: a non-converging f → assert "NO ROOT FOUND" path via OM-cited example.
       - `solve_sign_change_no_narrowing`: f with sign change but flat slope → "ROOT IS BETWEEN" path.
       - Combined pass rate including all v2.2 baseline + 28-02 hyperbolics + 28-03/04 complex + 28-05 POLY + 28-06 MATRIX + 28-07 INTG + 28-08 SOLVE cases >= 98%; v1.x 503-case baseline floor 498/503 (D-27.6) UNCHANGED — assert with the existing independent floor check.

    8. Add display arms in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` per CLAUDE.md SC-4-literal carve-out from planning context:
       - In `hp41-cli/src/prgm_display.rs`, add to the `op_display_name` match: `Op::Solve => "SOLVE".to_string(),` and `Op::Sol => "SOL".to_string(),`. Place under a `// ── Phase 28: SOLVE (Plan 28-08) ────────` comment.
       - In `hp41-gui/src-tauri/src/prgm_display.rs`, add the SAME two arms in the same format. The two files are deliberately duplicated per CLAUDE.md v2.2 additions: "Note: op_display_name is duplicated in both hp41-cli/src/prgm_display.rs and hp41-gui/src-tauri/src/prgm_display.rs — every new Op variant must be added in both copies."

    9. Re-run xrom_shadowing.rs — confirm 36 entries clear (34 from Plan 28-07 + 2 new from Plan 28-08).
  </action>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo build -p hp41-cli &amp;&amp; (cd hp41-gui/src-tauri &amp;&amp; cargo build) &amp;&amp; cargo test -p hp41-core --test xrom_shadowing --test math1_user_callback --test math1_solve_paths --test math1_op_test_count --test numerical_accuracy</automated>
  </verify>
  <done>Op::Solve + Op::Sol landed (2 new variants); 2 of 3 remaining user-callback regression cases filled + 1 NEW nested-SOLVE-in-SOLVE test; math1_solve_paths.rs ships 3 OM-cited termination-path tests; numerical_accuracy.rs +4 SOLV cases; CLI + GUI prgm_display.rs both carry Op::Solve / Op::Sol arms (workspace builds in BOTH crates); xrom_shadowing.rs 36/36; Plan 28-09 inherits the same callback infrastructure.</done>
</task>

</tasks>

<verification>
- `cargo build -p hp41-core` — full green; zero warnings under `#![deny(clippy::unwrap_used)]`.
- `cargo build -p hp41-cli` — confirms Op::Solve / Op::Sol display arms in `hp41-cli/src/prgm_display.rs` keep the 4-place exhaustive match compiling.
- `(cd hp41-gui/src-tauri && cargo build)` — same for hp41-gui's duplicate copy.
- `cargo test -p hp41-core --test math1_user_callback` — 4 of 5 cases pass (3 from Plan 28-07 + 28-08's two newly un-ignored: nested_solve_inside_integ_rejected, nested_integ_inside_solve_rejected, nested_solve_inside_solve_rejected) + 1 NEW user_fn_stores_to_scratch_corrupts_integ from 28-07; only nested_difeq_inside_integ_rejected remains `#[ignore]`d for Plan 28-09.
- `cargo test -p hp41-core --test math1_solve_paths` — 3 termination-path tests pass.
- `cargo test -p hp41-core --lib math1::solve::tests::call_stack_full` — pre-mutation cap verified; no leak.
- `cargo test -p hp41-core --lib math1::solve::tests::cancel_per_64_iterations` — cancellation cadence verified.
- `cargo test -p hp41-core --lib math1::solve::tests::iteration_cap` — 100-iter cap fires.
- `cargo test -p hp41-core --test xrom_shadowing` — 36/36 non-collision (SOLVE + SOL clear).
- SC-4 invariant preserved per CLAUDE.md stricter audit: `grep -rn "fn op_(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing (display-arm additions to `op_display_name` are display formatters, not calc/math logic per CLAUDE.md's documented exception).
</verification>

<success_criteria>
- 2 new Op variants integrated through 3-place exhaustive match (Op enum + dispatch + execute_op) AND 2 new prgm_display arms in BOTH `hp41-cli` and `hp41-gui` copies (per CLAUDE.md SC-4-literal carve-out from planning context).
- `op_solve_run_loop` / `op_sol_run_loop` reuse INTG's user-program callback infrastructure verbatim — same run_loop re-entry, same strict-reject guard, same pre-mutation call_stack cap, same per-64 cancellation check.
- 3 OM-cited termination paths land via `state.print_buffer` per PATTERNS line 537 (results not prompts): "ROOT IS <v>", "ROOT IS BETWEEN <v1> AND <v2>", "NO ROOT FOUND".
- 100-iteration cap (SOLV-07) enforced via `for iter in 0..100`.
- XROM-08 strict-reject (SOLV-08) verified at op entry; `state.solve_state.is_some() || state.integ_state.is_some() || state.difeq_state.is_some()` check fires BEFORE any mutation.
- `Op::Sol` sub-entry (SOLV-02) bypasses the 3-prompt modal; reads x1 from R00, x2 from R01 per SOLV-05.
- 2 more `math1_user_callback.rs` cases filled (Plan 28-08 closes the SOLVE branches); 1 remains for Plan 28-09 (DIFEQ).
- Per-Op test count ≥ 5 for both Op::Solve and Op::Sol (Pitfall 16 / QUAL-04).
- Plan 28-09 (DIFEQ) inherits the same user-callback infrastructure — Plan 28-09 grows the XROM-08 guard to include `state.difeq_state.is_some()` and ships the last `#[ignore]`d `math1_user_callback.rs` case.
</success_criteria>

<output>
Create `.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-08-SUMMARY.md`:
- 2 new Op variants (Op::Solve master + Op::Sol sub-entry) landed through 3-place exhaustive match + 2 prgm_display arms in both CLI and GUI copies.
- SolveState full struct (6 fields) per RESEARCH lines 371-379 / PATTERNS line 536.
- Modified secant iteration per SOLV-03; 100-iteration cap per SOLV-07; 3 termination paths per SOLV-04 / Pitfall 3.
- Plan 28-07 user-callback infrastructure reused verbatim (SOLV-06); cancellation per-64-iterations plumbing inherited.
- math1_user_callback.rs scaffold: 4 of 5 cases now filled; only nested_difeq_inside_integ_rejected remains for Plan 28-09.
- math1_solve_paths.rs NEW: 3 OM-cited integration tests covering the termination-path triad.
- 36 entries in MATH_1.ops; xrom_shadowing.rs green.
- Plan 28-09 DIFEQ inherits the same callback infrastructure verbatim AND grows the XROM-08 strict-reject guard to include `state.difeq_state.is_some()`.
</output>
