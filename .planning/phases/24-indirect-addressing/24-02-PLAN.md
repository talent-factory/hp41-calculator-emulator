---
phase: 24-indirect-addressing
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - hp41-core/src/ops/indirect.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
  - hp41-core/tests/phase24_ind_variants.rs
autonomous: true
requirements:
  - FN-IND-01
  - FN-IND-02
tags:
  - rust
  - hp41-core
  - hp41-cli
  - hp41-gui
  - indirect-addressing
  - op-variants

must_haves:
  truths:
    - "11 new `Op::*Ind` variants exist on the `Op` enum: `StoInd(u8)`, `RclInd(u8)`, `StoArithInd(u8, StoArithKind)`, `IsgInd(u8)`, `DseInd(u8)`, `SfFlagInd(u8)`, `CfFlagInd(u8)`, `FlagTestInd { kind: FlagTestKind, ind_reg: u8 }`, `ArclInd(u8)`, `AstoInd(u8)`, `ViewInd(u8)`"
    - "Every new variant has a dispatch arm in `hp41-core/src/ops/mod.rs::dispatch()`, an execute_op arm in `hp41-core/src/ops/program.rs`, AND `op_display_name` arms in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` (4-place landing rule, D-22.21 / D-23.12)"
    - "`Op::IsgInd`, `Op::DseInd`, and `Op::FlagTestInd` have run_loop arms with skip-next-step semantics mirroring their direct-form counterparts (Pitfall 1 mitigated)"
    - "Every new variant has 3+ integration tests: happy path, non-integer pointer rejection (returns InvalidOp), and out-of-bounds pointer rejection (returns InvalidOp via the delegated direct op's bounds check)"
    - "`STO IND` inherits the D-23.4 text_regs sidecar clear via delegation to op_sto (verified by a bonus test); `RCL IND` inherits Enable lift via op_rcl (verified by lift_enabled assertion)"
    - "`VIEW IND 05` with R05=12 and R12=42 sets `display_override` to the formatted value of R12 (NOT the value of R05) — i.e. shows the resolved register's VALUE, not the pointer (R9 mitigation)"
    - "`Op::FlagTestInd` interactive dispatch is a Neutral no-op (mirrors `Op::FlagTest`); skip semantics fire only inside `run_loop`"
    - "All ~33+ integration tests in `hp41-core/tests/phase24_ind_variants.rs` pass; `cargo test -p hp41-core` and `cargo test -p hp41-cli` and `just gui-check` pass"
    - "`just coverage` reports `hp41-core` line coverage ≥ 92.5% (no regression from current 92.68% baseline)"
  artifacts:
    - path: "hp41-core/src/ops/indirect.rs"
      provides: "11 new pub(crate) op_*_ind shim functions delegating to direct-form ops via resolve_indirect"
      min_lines: 200
      contains: "pub(crate) fn op_sto_ind"
    - path: "hp41-core/src/ops/mod.rs"
      provides: "11 new Op enum variants in a `// ── Phase 24: Indirect Addressing ─────` section + 11 dispatch arms (StoInd/RclInd/StoArithInd/IsgInd/DseInd/SfFlagInd/CfFlagInd/FlagTestInd/ArclInd/AstoInd/ViewInd)"
      contains: "Op::StoInd"
    - path: "hp41-core/src/ops/program.rs"
      provides: "execute_op arms for all 11 new variants + run_loop arms for IsgInd/DseInd/FlagTestInd + extended programming-ops `|`-pattern catch-all (Pitfall 6 mitigation)"
      contains: "Op::IsgInd(reg)"
    - path: "hp41-cli/src/prgm_display.rs"
      provides: "11 new op_display_name match arms producing `<MNEMONIC> IND <reg:02>` strings (or kind-table-then-format for FlagTestInd / StoArithInd)"
      contains: "STO IND"
    - path: "hp41-gui/src-tauri/src/prgm_display.rs"
      provides: "Byte-for-byte mirror of the CLI prgm_display additions (SC-4 invariant)"
      contains: "STO IND"
    - path: "hp41-core/tests/phase24_ind_variants.rs"
      provides: "33+ integration tests: 3 classes (happy/non-integer/out-of-bounds) per variant + sub-tests for FlagTestKind / StoArithKind reuse + sidecar/lift inheritance assertions"
      min_lines: 300
      contains: "sto_ind_happy"
  key_links:
    - from: "hp41-core/src/ops/mod.rs::dispatch() Op::*Ind arms"
      to: "hp41-core/src/ops/indirect.rs::op_*_ind shim functions"
      via: "qualified call `indirect::op_<name>_ind(state, reg)` or `.map(|_| ())` for IsgInd/DseInd"
      pattern: "indirect::op_"
    - from: "hp41-core/src/ops/indirect.rs op_*_ind shims"
      to: "hp41-core/src/ops/{registers,flags,alpha,display_ops,program}.rs direct-form ops"
      via: "delegation: resolve address, then call op_sto/op_rcl/op_sf/op_arcl/op_view/op_isg/op_dse"
      pattern: "resolve_indirect.*op_"
    - from: "hp41-core/src/ops/program.rs::run_loop Op::IsgInd / Op::DseInd / Op::FlagTestInd arms"
      to: "hp41-core/src/ops/indirect.rs::op_isg_ind / op_dse_ind + crate::ops::indirect::resolve_indirect"
      via: "skip-next-step semantic preserved (Pitfall 1)"
      pattern: "state.pc \\+= 1"
    - from: "hp41-cli/src/prgm_display.rs op_display_name new IND arms"
      to: "hp41-gui/src-tauri/src/prgm_display.rs op_display_name new IND arms"
      via: "byte-for-byte mirror (SC-4 invariant — every CLI arm has identical GUI counterpart)"
      pattern: "IND \\{.*:02\\}"
---

<objective>
Phase 24 Plan 02 — Variants. Add 11 new `Op::*Ind` enum variants to `hp41-core::ops::Op` (`StoInd`, `RclInd`, `StoArithInd(u8, StoArithKind)`, `IsgInd`, `DseInd`, `SfFlagInd`, `CfFlagInd`, `FlagTestInd { kind: FlagTestKind, ind_reg: u8 }`, `ArclInd`, `AstoInd`, `ViewInd`), each delegating to its direct-form counterpart via the Plan 24-01 `resolve_indirect` helper. Land each variant in all 4 places (D-22.21 / D-23.12): dispatch arm in `ops/mod.rs`, execute_op (and where required, run_loop) arm in `ops/program.rs`, and identical `op_display_name` arms in BOTH `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` (SC-4 mirror rule). Lock down per-variant behavior with ~33+ integration tests in a new `hp41-core/tests/phase24_ind_variants.rs` covering happy path, non-integer rejection, and out-of-bounds rejection per variant.

Purpose: Deliver FN-IND-01 (all addressable ops support IND addressing) and FN-IND-02 (non-integer pointers reject with InvalidOp) end-to-end across `hp41-core`, with 4-place landing enforced compile-time and runtime-verified per variant.

Output: 11 new Op variants reachable from interactive dispatch and program execution, with hardware-faithful skip semantics for IsgInd/DseInd/FlagTestInd, sidecar/atomicity/lift inheritance via delegation, and identical listing display on both frontends. No new CalcState fields, no new HpError variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-indirect-addressing/24-CONTEXT.md
@.planning/phases/24-indirect-addressing/24-RESEARCH.md
@.planning/phases/24-indirect-addressing/24-PATTERNS.md
@.planning/phases/24-indirect-addressing/24-VALIDATION.md
@.planning/phases/24-indirect-addressing/24-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from RESEARCH.md / PATTERNS.md / existing source. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From hp41-core/src/ops/mod.rs (existing — Phase 21/22/23 precedents):
  pub enum FlagTestKind { IsSet, IsClear, IsSetThenClear, IsClearThenClear }     // mod.rs:55-71
  pub enum StoArithKind { Add, Sub, Mul, Div }                                    // existing
  pub enum StackReg { X, Y, Z, T, LastX }                                         // existing
  pub enum Op {
      // ...
      StoReg(u8),                                                                 // mod.rs:204
      RclReg(u8),                                                                 // mod.rs:206
      StoArith { reg: u8, kind: StoArithKind },                                   // mod.rs:208-211
      Isg(u8),                                                                    // existing
      Dse(u8),                                                                    // existing
      SfFlag(u8),                                                                 // mod.rs:319-323
      CfFlag(u8),                                                                 // mod.rs:319-323
      FlagTest { kind: FlagTestKind, flag: u8 },                                  // mod.rs:326-329
      Arcl(u8),                                                                   // Phase 23
      Asto(u8),                                                                   // Phase 23
      View(u8),                                                                   // Phase 21
      GtoInd(u8),                                                                 // Phase 22 (already shipped — refactored in 24-01)
      XeqInd(u8),                                                                 // Phase 22 (already shipped — refactored in 24-01)
      // ... Phase 24 NEW variants land in a new `// ── Phase 24: Indirect Addressing ─────` section near the end
  }

From hp41-core/src/ops/registers.rs (delegation targets):
  pub fn op_sto(state: &mut CalcState, reg: u8) -> Result<(), HpError>;          // does bounds + sidecar + Neutral lift
  pub fn op_rcl(state: &mut CalcState, reg: u8) -> Result<(), HpError>;          // does bounds + Enable lift
  pub fn op_sto_arith(state: &mut CalcState, reg: u8, kind: StoArithKind) -> Result<(), HpError>;

From hp41-core/src/ops/flags.rs (delegation targets):
  pub fn op_sf(state: &mut CalcState, n: u8) -> Result<(), HpError>;             // bounds: n > 55 → InvalidOp
  pub fn op_cf(state: &mut CalcState, n: u8) -> Result<(), HpError>;             // bounds: n > 55 → InvalidOp
  pub fn flag_get(flags: u64, n: u8) -> bool;                                    // defensive: n > 55 → false
  pub fn flag_clear(flags: u64, n: u8) -> u64;                                   // defensive: n > 55 → no-op

From hp41-core/src/ops/program.rs (delegation targets):
  pub fn op_isg(state: &mut CalcState, reg: u8) -> Result<bool, HpError>;        // bool = "loop exit, skip next"
  pub fn op_dse(state: &mut CalcState, reg: u8) -> Result<bool, HpError>;        // bool = "loop exit, skip next"

From hp41-core/src/ops/alpha.rs (delegation targets):
  pub fn op_arcl(state: &mut CalcState, reg: u8) -> Result<(), HpError>;         // bounds + text_regs sidecar + Neutral lift
  pub fn op_asto(state: &mut CalcState, reg: u8) -> Result<(), HpError>;         // bounds + text_regs sidecar + Neutral lift

From hp41-core/src/ops/display_ops.rs (delegation targets):
  pub fn op_view(state: &mut CalcState, reg: u8) -> Result<(), HpError>;         // sets display_override to formatted regs[reg]

From hp41-core/src/ops/indirect.rs (provided by Plan 24-01):
  pub(crate) fn resolve_indirect_decimal(state: &CalcState, reg: u8) -> Result<Decimal, HpError>;
  pub fn resolve_indirect(state: &CalcState, reg: u8) -> Result<u8, HpError>;

Phase 24 NEW Op variants (this plan creates — exact shapes locked by D-24.6 + D-24.7 + RESEARCH.md §"Variant Set"):
  Op::StoInd(u8),
  Op::RclInd(u8),
  Op::StoArithInd(u8, StoArithKind),
  Op::IsgInd(u8),
  Op::DseInd(u8),
  Op::SfFlagInd(u8),
  Op::CfFlagInd(u8),
  Op::FlagTestInd { kind: FlagTestKind, ind_reg: u8 },     // STRUCT variant — mirrors Op::FlagTest shape (D-24.6)
  Op::ArclInd(u8),
  Op::AstoInd(u8),
  Op::ViewInd(u8),

Phase 24 NEW shim functions (this plan creates in hp41-core/src/ops/indirect.rs — appended below the resolver helpers from Plan 24-01):
  pub(crate) fn op_sto_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  pub(crate) fn op_rcl_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  pub(crate) fn op_sto_arith_ind(state: &mut CalcState, reg: u8, kind: StoArithKind) -> Result<(), HpError>;
  pub(crate) fn op_isg_ind(state: &mut CalcState, reg: u8) -> Result<bool, HpError>;     // returns Result<bool, _> — skip signal
  pub(crate) fn op_dse_ind(state: &mut CalcState, reg: u8) -> Result<bool, HpError>;     // returns Result<bool, _> — skip signal
  pub(crate) fn op_sf_flag_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  pub(crate) fn op_cf_flag_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  pub(crate) fn op_arcl_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  pub(crate) fn op_asto_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  pub(crate) fn op_view_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>;
  // (No op_flag_test_ind — FlagTestInd's behavior lives inline in run_loop, mirrors Op::FlagTest precedent)

Display-string conventions (per RESEARCH §"Open Questions" #3 — space-separated `<MNEMONIC> IND <r:02>`, mirrors Phase-22 GTO IND precedent):
  Op::StoInd(r)            → format!("STO IND {r:02}")
  Op::RclInd(r)            → format!("RCL IND {r:02}")
  Op::StoArithInd(r, kind) → kind-table-then-format("STO{op_sym} IND {r:02}") — op_sym ∈ {"+", "-", "\u{00D7}", "\u{00F7}"}
  Op::IsgInd(r)            → format!("ISG IND {r:02}")
  Op::DseInd(r)            → format!("DSE IND {r:02}")
  Op::SfFlagInd(r)         → format!("SF IND {r:02}")
  Op::CfFlagInd(r)         → format!("CF IND {r:02}")
  Op::FlagTestInd { kind, ind_reg } → kind-table-then-format("{mnemonic} IND {ind_reg:02}") — mnemonic ∈ {"FS?", "FC?", "FS?C", "FC?C"}
  Op::ArclInd(r)           → format!("ARCL IND {r:02}")
  Op::AstoInd(r)           → format!("ASTO IND {r:02}")
  Op::ViewInd(r)           → format!("VIEW IND {r:02}")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 11 op_*_ind shim functions to indirect.rs and 11 Op variants + dispatch arms to ops/mod.rs</name>
  <files>hp41-core/src/ops/indirect.rs (append 11 shim functions below the resolver helpers from Plan 24-01), hp41-core/src/ops/mod.rs (add 11 enum variants + 11 dispatch arms)</files>
  <read_first>
    - hp41-core/src/ops/indirect.rs (the helpers `resolve_indirect_decimal` + `resolve_indirect` from Plan 24-01 — Task 1 appends shims below them)
    - hp41-core/src/ops/registers.rs lines 14-100 (analog: op_sto / op_rcl / op_sto_arith — exact-match delegation targets; PATTERNS.md §"Sidecar / Atomicity / Lift-effect Inheritance via Delegation" lines 677-690 confirms the 2-line shim pattern)
    - hp41-core/src/ops/flags.rs lines 39-56 (analog: op_sf / op_cf — note the `< 56` bounds enforcement that the IND shim inherits gratis)
    - hp41-core/src/ops/alpha.rs (analog: op_arcl / op_asto — direct-form bounds + text_regs sidecar that the IND shim inherits via delegation, D-24.4)
    - hp41-core/src/ops/display_ops.rs (analog: op_view — sets display_override; ViewInd inherits this verbatim)
    - hp41-core/src/ops/program.rs (locate `pub fn op_isg` and `pub fn op_dse` — confirm they return `Result<bool, HpError>`; the IsgInd/DseInd shims preserve this Result<bool> shape)
    - hp41-core/src/ops/mod.rs lines 200-330 (existing Op enum entries — analog patterns for new variant declarations: `StoReg(u8)` at line 204, `StoArith { reg, kind }` at lines 208-211, `FlagTest { kind, flag }` at lines 326-329, `GtoInd(u8)` at lines 372-379)
    - hp41-core/src/ops/mod.rs lines 660-790 (existing dispatch arms — analog for new dispatch arms: `Op::StoReg(r) => op_sto(state, r)` at lines 664-667, `Op::Isg(reg) => program::op_isg(state, reg).map(|_| ())` at lines 687-695, `Op::FlagTest { .. } => { apply_lift_effect(state, LiftEffect::Neutral); Ok(()) }` at lines 745-748, `Op::GtoInd(_) | Op::XeqInd(_) => Err(HpError::InvalidOp)` catch-all at line 780)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Pattern 2: 2-Line Delegation Shim" lines 240-298 (full reference shim signatures)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Variant Set (Recount of D-24.7 estimate)" lines 408-430 (the 11-variant table with direct counterparts and run_loop status)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"4-Place Landing Checklist" lines 432-450 (per-variant routing table)
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"hp41-core/src/ops/mod.rs (MODIFIED)" lines 315-427 (full module-declaration, enum-variant, and dispatch-arm patterns)
    - .planning/phases/24-indirect-addressing/24-CONTEXT.md §D-24.6, §D-24.7 (FlagTestInd struct shape; <Name>Ind(u8) family naming pattern)
  </read_first>
  <behavior>
    - The Op enum gains 11 new variants exactly: `StoInd(u8)`, `RclInd(u8)`, `StoArithInd(u8, StoArithKind)`, `IsgInd(u8)`, `DseInd(u8)`, `SfFlagInd(u8)`, `CfFlagInd(u8)`, `FlagTestInd { kind: FlagTestKind, ind_reg: u8 }`, `ArclInd(u8)`, `AstoInd(u8)`, `ViewInd(u8)`.
    - For 8 simple variants (StoInd, RclInd, StoArithInd, SfFlagInd, CfFlagInd, ArclInd, AstoInd, ViewInd): `dispatch(&mut state, Op::<Name>Ind(reg))` resolves the indirect address and delegates to the corresponding direct-form op.
    - For IsgInd / DseInd: `dispatch()` calls the op_*_ind shim and discards the bool via `.map(|_| ())` (mirrors `Op::Isg(reg) => program::op_isg(state, reg).map(|_| ())` at mod.rs:687-691); skip semantics live in run_loop only (added in Task 2).
    - For FlagTestInd: `dispatch()` is a Neutral no-op `apply_lift_effect(state, LiftEffect::Neutral); Ok(())` — mirrors `Op::FlagTest { .. }` at mod.rs:745-748; skip semantics live in run_loop only (added in Task 2).
    - All 11 op_*_ind shim functions in indirect.rs are exactly 2 lines of body each (3 for StoArithInd because of the kind passthrough): resolve via `let addr = resolve_indirect(state, reg)?;` then call the direct-form op with addr.
    - `cargo build -p hp41-core` succeeds with all 11 new variants wired into dispatch (no exhaustive-match-failure because dispatch's match is exhaustive and gains all 11 new arms).
    - Bonus dispatch sanity (verified via integration tests in Task 4): calling `dispatch(&mut state, Op::StoInd(5))` with state.regs[5]=HpNum::from(12i32) and stack X=7 results in state.regs[12]==HpNum::from(7i32).
  </behavior>
  <action>
    Step A — Append shim functions to `hp41-core/src/ops/indirect.rs` (BELOW the `resolve_indirect_decimal` and `resolve_indirect` helpers, ABOVE the `#[cfg(test)] mod tests` block):

    1. Add imports at the top of the file alongside the existing Plan 24-01 imports:
       - `use crate::ops::alpha::{op_arcl, op_asto};`
       - `use crate::ops::display_ops::op_view;`
       - `use crate::ops::flags::{op_cf, op_sf};`
       - `use crate::ops::program::{op_dse, op_isg};`
       - `use crate::ops::registers::{op_rcl, op_sto, op_sto_arith};`
       - `use crate::ops::StoArithKind;`

    2. Append 11 shim functions per RESEARCH §"Inner helper (full body)" lines 626-678, in this exact order:

       - `pub(crate) fn op_sto_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_sto(state, addr)`. Rustdoc: "STO IND nn — store X into register pointed to by R[nn]'s integer part. Delegates to op_sto. Inherits Neutral lift, D-23.4 sidecar-clearing on text_regs, and D-22.11.1 bounds check via the direct op."

       - `pub(crate) fn op_rcl_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_rcl(state, addr)`. Rustdoc: "RCL IND nn — recall regs[R[nn].int_part] into X. Delegates to op_rcl. Inherits Enable lift."

       - `pub(crate) fn op_sto_arith_ind(state: &mut CalcState, reg: u8, kind: StoArithKind) -> Result<(), HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_sto_arith(state, addr, kind)`. Rustdoc: "STO+/-/×/÷ IND nn — arithmetic via R[nn]'s integer-part address. Single-variant kind reuse mirrors Op::StoArith shape."

       - `pub(crate) fn op_isg_ind(state: &mut CalcState, reg: u8) -> Result<bool, HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_isg(state, addr)`. Rustdoc: "ISG IND nn — increment register pointed to by R[nn]; returns true on counter exit (skip-next signal). Returns Result<bool, _> like op_isg so run_loop can act on the skip."

       - `pub(crate) fn op_dse_ind(state: &mut CalcState, reg: u8) -> Result<bool, HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_dse(state, addr)`. Rustdoc: "DSE IND nn — decrement and skip on exit; returns Result<bool, _> like op_dse."

       - `pub(crate) fn op_sf_flag_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let flag = resolve_indirect(state, reg)?; op_sf(state, flag)`. Rustdoc: "SF IND nn — set flag at R[nn]'s integer part. Inherits the `< 56` bounds check from op_sf."

       - `pub(crate) fn op_cf_flag_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let flag = resolve_indirect(state, reg)?; op_cf(state, flag)`. Rustdoc: "CF IND nn — clear flag at R[nn]. Inherits `< 56` bounds via op_cf."

       - `pub(crate) fn op_arcl_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_arcl(state, addr)`. Rustdoc: "ARCL IND nn — append regs[R[nn].int_part]'s formatted value to ALPHA. Delegates to op_arcl; inherits text_regs sidecar handling."

       - `pub(crate) fn op_asto_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_asto(state, addr)`. Rustdoc: "ASTO IND nn — pack first 6 ALPHA chars into regs[R[nn]]. Inherits text_regs sidecar setup and atomicity."

       - `pub(crate) fn op_view_ind(state: &mut CalcState, reg: u8) -> Result<(), HpError>` — body: `let addr = resolve_indirect(state, reg)?; op_view(state, addr)`. Rustdoc: "VIEW IND nn — display the VALUE of regs[R[nn].int_part] (NOT the pointer value). R9 mitigation: this delegates to op_view(state, addr), which formats state.regs[addr], so the display shows the resolved register's contents."

       NOTE: NO `op_flag_test_ind` shim — `Op::FlagTestInd`'s behavior lives inline in run_loop (Task 2), mirroring the existing `Op::FlagTest` precedent at program.rs:563-582.

    Step B — Add 11 new Op variants to `hp41-core/src/ops/mod.rs`:

    1. Locate the existing Op enum body (currently mod.rs lines 200-450 region, after Phase 23's `Op::Posa` entry — use `grep -n "Op::Posa" hp41-core/src/ops/mod.rs` to find the exact location). Add a new section divider comment block at the bottom of the enum body, BEFORE the closing `}`:
       - `// ── Phase 24: Indirect Addressing (FN-IND-01, FN-IND-02) ─────────────`
       - `// Every variant delegates to its direct-form counterpart via`
       - `// `crate::ops::indirect::resolve_indirect`. Family naming pattern: `<Name>Ind(u8)`.`
       - `// Inherits sidecar (D-23.4), atomicity (D-22.x), and lift-effect from`
       - `// the delegated direct op (D-24.4 — no replication).`

    2. Add the 11 new variants in this order, each with a doc comment in the same format as `Op::GtoInd(u8)` at mod.rs:366-372 — `/// <NAME> IND nn — <description>. LiftEffect: <X> (inherited from <op_name>). Phase 24 (FN-IND-01).`:
       - `StoInd(u8),` — Neutral lift via op_sto
       - `RclInd(u8),` — Enable lift via op_rcl
       - `StoArithInd(u8, StoArithKind),` — Neutral lift via op_sto_arith
       - `IsgInd(u8),` — Neutral lift via op_isg
       - `DseInd(u8),` — Neutral lift via op_dse
       - `SfFlagInd(u8),` — Neutral lift via op_sf
       - `CfFlagInd(u8),` — Neutral lift via op_cf
       - `FlagTestInd { kind: FlagTestKind, ind_reg: u8 },` — Neutral interactive no-op; skip semantics in run_loop. STRUCT variant per D-24.6.
       - `ArclInd(u8),` — Neutral lift via op_arcl
       - `AstoInd(u8),` — Neutral lift via op_asto
       - `ViewInd(u8),` — Neutral lift via op_view (delegates to op_view which sets display_override)

    Step C — Add 11 dispatch arms to `dispatch()` in `hp41-core/src/ops/mod.rs`:

    1. Locate the existing `dispatch()` match block (currently mod.rs lines 567-844 region; the `Op::GtoInd(_) | Op::XeqInd(_) => Err(HpError::InvalidOp)` catch-all is at ~line 780; the FlagTest Neutral no-op is at ~lines 745-748). Add 11 new arms in a contiguous `// ── Phase 24: Indirect Addressing dispatch ─────` section, ideally after the existing Phase-23 ALPHA dispatch arms.

    2. The 11 arms follow these patterns:
       - `Op::StoInd(reg) => indirect::op_sto_ind(state, reg),`
       - `Op::RclInd(reg) => indirect::op_rcl_ind(state, reg),`
       - `Op::StoArithInd(reg, kind) => indirect::op_sto_arith_ind(state, reg, kind),`
       - `Op::IsgInd(reg) => indirect::op_isg_ind(state, reg).map(|_| ()),`     // discard bool, mirrors Op::Isg pattern at mod.rs:687-691
       - `Op::DseInd(reg) => indirect::op_dse_ind(state, reg).map(|_| ()),`     // discard bool, mirrors Op::Dse pattern
       - `Op::SfFlagInd(reg) => indirect::op_sf_flag_ind(state, reg),`
       - `Op::CfFlagInd(reg) => indirect::op_cf_flag_ind(state, reg),`
       - `Op::FlagTestInd { .. } => { apply_lift_effect(state, LiftEffect::Neutral); Ok(()) },`     // Neutral no-op, mirrors Op::FlagTest at mod.rs:745-748
       - `Op::ArclInd(reg) => indirect::op_arcl_ind(state, reg),`
       - `Op::AstoInd(reg) => indirect::op_asto_ind(state, reg),`
       - `Op::ViewInd(reg) => indirect::op_view_ind(state, reg),`

    3. Use the qualified path `indirect::op_*_ind` rather than expanding the import block — this matches RESEARCH §"Module wiring" lines 116-126 acceptable approach and keeps the import block stable.

    Anti-patterns to avoid:
    - Do NOT use struct-variant syntax for `Op::StoArithInd` — D-24.7 says `<Name>Ind(u8)` is the family pattern; tuple variant `(u8, StoArithKind)` matches PATTERNS.md §mod.rs lines 422-423.
    - Do NOT use tuple-variant syntax for `Op::FlagTestInd` — D-24.6 explicitly specifies struct variant `{ kind, ind_reg }`.
    - Do NOT expand the IsgInd/DseInd dispatch arms beyond `.map(|_| ())` — skip semantics live in run_loop, NOT dispatch (mirrors existing Op::Isg/Op::Dse pattern at mod.rs:687-695).
    - Do NOT add any execute_op or prgm_display work in this task — those land in Tasks 2 and 3 respectively. Compile failures from missing exhaustive-match arms in those files are EXPECTED at the end of this task and will be resolved by Tasks 2 and 3.
  </action>
  <verify>
    <automated>cargo check -p hp41-core 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(crate) fn op_sto_ind\|pub(crate) fn op_rcl_ind\|pub(crate) fn op_sto_arith_ind\|pub(crate) fn op_isg_ind\|pub(crate) fn op_dse_ind\|pub(crate) fn op_sf_flag_ind\|pub(crate) fn op_cf_flag_ind\|pub(crate) fn op_arcl_ind\|pub(crate) fn op_asto_ind\|pub(crate) fn op_view_ind" hp41-core/src/ops/indirect.rs` returns exactly 10 (10 shim functions; FlagTestInd has no shim).
    - `grep -c "StoInd(u8)\|RclInd(u8)\|StoArithInd(u8, StoArithKind)\|IsgInd(u8)\|DseInd(u8)\|SfFlagInd(u8)\|CfFlagInd(u8)\|ArclInd(u8)\|AstoInd(u8)\|ViewInd(u8)" hp41-core/src/ops/mod.rs` returns at least 10 (the 10 tuple variants are declared).
    - `grep -c "FlagTestInd { kind: FlagTestKind, ind_reg: u8 }\|FlagTestInd {" hp41-core/src/ops/mod.rs` returns at least 1 (the struct variant is declared per D-24.6).
    - `grep -c "Op::StoInd(reg) => indirect::op_sto_ind\|Op::RclInd(reg) => indirect::op_rcl_ind\|Op::StoArithInd(reg, kind) => indirect::op_sto_arith_ind\|Op::SfFlagInd(reg) => indirect::op_sf_flag_ind\|Op::CfFlagInd(reg) => indirect::op_cf_flag_ind\|Op::ArclInd(reg) => indirect::op_arcl_ind\|Op::AstoInd(reg) => indirect::op_asto_ind\|Op::ViewInd(reg) => indirect::op_view_ind" hp41-core/src/ops/mod.rs` returns at least 8 (the 8 simple-delegate dispatch arms).
    - `grep -c "Op::IsgInd(reg) => indirect::op_isg_ind(state, reg).map(|_| ())\|Op::DseInd(reg) => indirect::op_dse_ind(state, reg).map(|_| ())" hp41-core/src/ops/mod.rs` returns at least 2 (the Isg/Dse `.map(|_| ())` arms).
    - `grep -c "Op::FlagTestInd { .. } => {" hp41-core/src/ops/mod.rs` returns at least 1 (FlagTestInd Neutral no-op arm).
    - `cargo check -p hp41-core` is expected to FAIL with non-exhaustive match errors in `hp41-core/src/ops/program.rs::execute_op` AND in both prgm_display.rs files — that is the expected compile state because Tasks 2 and 3 add those arms. Specifically: the failure messages must mention `Op::StoInd`, `Op::FlagTestInd`, etc. as missing match arms, NOT undefined-variant errors. (If undefined-variant errors appear, the enum declaration itself is wrong.)
    - `cargo build -p hp41-core --lib --tests --no-default-features 2>&1 | grep -c "non-exhaustive patterns"` is at least 1 (proves the enum variants were added — exhaustiveness gate fires correctly).
    - `grep -E '\.unwrap\(\)|\bas u8\b' hp41-core/src/ops/indirect.rs | grep -v '^\s*//' | grep -vc 'mod tests'` returns 0 (zero-panic invariant preserved across all new shim code).
  </acceptance_criteria>
  <done>
    All 11 new Op variants exist in the enum (10 tuple `(u8)` or `(u8, StoArithKind)` + 1 struct `{ kind, ind_reg }`). All 11 dispatch arms route correctly. All 10 shim functions exist in `indirect.rs` (FlagTestInd has no shim by design). `cargo check -p hp41-core` fails ONLY with non-exhaustive match errors in `execute_op` and `prgm_display.rs` — the expected post-Task-1 state. Production code remains `.unwrap()`-free and `as u8`-free.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add execute_op + run_loop arms (with skip semantics for IsgInd/DseInd/FlagTestInd) and extend the programming-ops catch-all</name>
  <files>hp41-core/src/ops/program.rs (add 11 execute_op arms + 3 run_loop arms + extend `|`-pattern catch-all)</files>
  <read_first>
    - hp41-core/src/ops/program.rs (current file — locate `execute_op()` body around lines 610-841 and `run_loop` body around lines 439-602; use `grep -n "fn execute_op\|fn run_loop\|Op::Isg(reg)\|Op::Dse(reg)\|Op::FlagTest { kind, flag }\|Programming ops handled by run_loop" hp41-core/src/ops/program.rs` to locate the exact lines, which may have shifted slightly post-24-01)
    - hp41-core/src/ops/indirect.rs (the shims added in Task 1 — confirm `pub(crate) fn op_sto_ind` etc. are reachable as `crate::ops::indirect::op_sto_ind`)
    - hp41-core/src/ops/mod.rs (the new variants added in Task 1 — confirm `Op::StoInd`, `Op::FlagTestInd { kind, ind_reg }`, etc. exist)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"`Op::IsgInd` run_loop arm (program.rs)" lines 763-778 (verbatim arm bodies for IsgInd and DseInd)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"`Op::FlagTestInd` run_loop arm (program.rs)" lines 780-808 (verbatim arm body for FlagTestInd, including the `flag_get`/`flag_clear` always-clear semantics for IsSetThenClear / IsClearThenClear)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Common Pitfalls" Pitfall 1 (lines 497-505) and Pitfall 6 (lines 561-569) — explicit warnings about forgetting run_loop arms and forgetting catch-all entries
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"hp41-core/src/ops/program.rs (MODIFIED)" lines 430-575 (analog patterns for execute_op delegation arms, run_loop skip-semantic arms, and catch-all extension)
  </read_first>
  <behavior>
    - For 8 simple variants (StoInd, RclInd, StoArithInd, SfFlagInd, CfFlagInd, ArclInd, AstoInd, ViewInd): `execute_op` calls the corresponding `crate::ops::indirect::op_*_ind` shim and returns its `Result<(), HpError>`.
    - For IsgInd / DseInd in execute_op (defense-in-depth, even though their primary path is run_loop): the execute_op arm calls the shim and discards the bool via `.map(|_| ())`, matching the existing `Op::Isg(reg) => super::program::op_isg(state, reg).map(|_| ())` pattern (or its equivalent in execute_op).
    - For FlagTestInd in execute_op: NOT an explicit arm — joins the `|`-pattern catch-all with `Op::Isg(_) | Op::Dse(_) | Op::FlagTest { .. } | Op::IsgInd(_) | Op::DseInd(_) | Op::FlagTestInd { .. } => Err(HpError::InvalidOp)` (defense-in-depth — these primarily run via run_loop and should never reach execute_op directly).
    - run_loop gains 3 new arms with skip-next-step semantics:
      - `Op::IsgInd(reg) => { if crate::ops::indirect::op_isg_ind(state, reg)? { state.pc += 1; } }`
      - `Op::DseInd(reg) => { if crate::ops::indirect::op_dse_ind(state, reg)? { state.pc += 1; } }`
      - `Op::FlagTestInd { kind, ind_reg } => { /* resolve flag via resolve_indirect, then re-use the existing kind-match block from Op::FlagTest verbatim */ }`
    - The programming-ops `|`-pattern catch-all at the bottom of execute_op (currently the `Op::Lbl(_) | Op::Gto(_) | Op::Xeq(_) | Op::Rtn | ... | Op::GtoInd(_) | Op::XeqInd(_) | ... | Op::Ins => Err(HpError::InvalidOp)` block at lines 825-840) gains `| Op::IsgInd(_) | Op::DseInd(_) | Op::FlagTestInd { .. }` entries before the `=> Err(HpError::InvalidOp)` (Pitfall 6 mitigation).
    - `cargo build -p hp41-core` succeeds (all exhaustive-match failures from Task 1 are resolved).
  </behavior>
  <action>
    Step A — Add execute_op arms in `hp41-core/src/ops/program.rs`:

    1. Locate `fn execute_op` (currently around line 610). Find a logical insertion point — ideally a new `// ── Phase 24: Indirect Addressing arms ─────` section after the existing Phase-23 ALPHA arms. Add the following arms:

       - `Op::StoInd(reg) => crate::ops::indirect::op_sto_ind(state, reg),`
       - `Op::RclInd(reg) => crate::ops::indirect::op_rcl_ind(state, reg),`
       - `Op::StoArithInd(reg, kind) => crate::ops::indirect::op_sto_arith_ind(state, reg, kind),`
       - `Op::SfFlagInd(reg) => crate::ops::indirect::op_sf_flag_ind(state, reg),`
       - `Op::CfFlagInd(reg) => crate::ops::indirect::op_cf_flag_ind(state, reg),`
       - `Op::ArclInd(reg) => crate::ops::indirect::op_arcl_ind(state, reg),`
       - `Op::AstoInd(reg) => crate::ops::indirect::op_asto_ind(state, reg),`
       - `Op::ViewInd(reg) => crate::ops::indirect::op_view_ind(state, reg),`
       - `Op::IsgInd(reg) => crate::ops::indirect::op_isg_ind(state, reg).map(|_| ()),`     // discard bool — defense-in-depth; primary path is run_loop
       - `Op::DseInd(reg) => crate::ops::indirect::op_dse_ind(state, reg).map(|_| ()),`     // discard bool — defense-in-depth
       - (NO `Op::FlagTestInd` arm here — joins the `|`-pattern catch-all in Step C)

    Step B — Add run_loop arms in `hp41-core/src/ops/program.rs`:

    1. Locate `fn run_loop` (currently around line 439). Find the existing `Op::Isg(reg)` and `Op::Dse(reg)` arms (lines 549-558 region). Append the IND counterparts immediately AFTER the Op::Dse arm (per RESEARCH §"`Op::IsgInd` run_loop arm" lines 763-778):

       - `Op::IsgInd(reg) => {`
       - `    if crate::ops::indirect::op_isg_ind(state, reg)? {`
       - `        state.pc += 1; // loop exit: skip next (mirrors Op::Isg)`
       - `    }`
       - `}`
       - `Op::DseInd(reg) => {`
       - `    if crate::ops::indirect::op_dse_ind(state, reg)? {`
       - `        state.pc += 1; // loop exit: skip next (mirrors Op::Dse)`
       - `    }`
       - `}`

    2. Locate the existing `Op::FlagTest { kind, flag }` arm (lines 559-582 region). Append the FlagTestInd counterpart immediately AFTER it, per RESEARCH §"`Op::FlagTestInd` run_loop arm (program.rs)" lines 780-808. The arm resolves the indirect register first, then reuses the EXACT same kind-match block. Use this structure:

       - `Op::FlagTestInd { kind, ind_reg } => {`
       - `    use crate::ops::flags::{flag_clear, flag_get};`
       - `    use crate::ops::FlagTestKind;`
       - `    let flag = crate::ops::indirect::resolve_indirect(state, ind_reg)?;`
       - `    let is_set = flag_get(state.flags, flag);`
       - `    let should_skip = match kind {`
       - `        FlagTestKind::IsSet => !is_set,`
       - `        FlagTestKind::IsClear => is_set,`
       - `        FlagTestKind::IsSetThenClear => {`
       - `            state.flags = flag_clear(state.flags, flag);`
       - `            !is_set`
       - `        }`
       - `        FlagTestKind::IsClearThenClear => {`
       - `            state.flags = flag_clear(state.flags, flag);`
       - `            is_set`
       - `        }`
       - `    };`
       - `    if should_skip {`
       - `        state.pc += 1;`
       - `    }`
       - `}`

       The `use` statements may already be in scope from the existing Op::FlagTest arm — keep them on the new arm anyway for textual symmetry (Rust permits redundant `use` inside a match arm without warning).

    Step C — Extend the programming-ops `|`-pattern catch-all in `hp41-core/src/ops/program.rs`:

    1. Locate the existing catch-all (currently lines 825-840 region — search for `// Programming ops handled by run_loop directly`). The current `|`-pattern lists: `Op::Lbl(_) | Op::Gto(_) | Op::Xeq(_) | Op::Rtn | Op::PrgmMode | Op::Test(_) | Op::Isg(_) | Op::Dse(_) | Op::FlagTest { .. } | Op::Prompt | Op::Stop | Op::GtoInd(_) | Op::XeqInd(_) | Op::Clp(_) | Op::Del(_) | Op::Ins => Err(HpError::InvalidOp),`

    2. Insert `| Op::IsgInd(_) | Op::DseInd(_) | Op::FlagTestInd { .. }` BEFORE the `=> Err(HpError::InvalidOp)`. Pitfall 6 mitigation: this is text-additive defense-in-depth — the explicit execute_op arms above for IsgInd/DseInd cover the exhaustive-match requirement, so this catch-all entry is not required for compilation, but it documents intent and protects against future arm-removal regressions.

    3. NOTE on FlagTestInd: it has NO explicit execute_op arm in Step A, so this catch-all entry IS required for exhaustive-match (mirrors the existing `Op::FlagTest { .. }` entry in the catch-all).

    Anti-patterns to avoid (per RESEARCH §"Common Pitfalls"):
    - Pitfall 1: Do NOT skip the run_loop arms for IsgInd/DseInd/FlagTestInd. If they exist only in execute_op via `.map(|_| ())`, the skip semantic is silently lost inside running programs — sentinel test `isg_ind_inside_run_loop` (Task 4) will catch this.
    - Pitfall 6: Do NOT skip the catch-all extension. While it's defense-in-depth for IsgInd/DseInd (which have explicit execute_op arms), it IS required for FlagTestInd (no explicit arm).
    - Do NOT add an `op_flag_test_ind` shim in indirect.rs — `Op::FlagTestInd`'s behavior lives inline in run_loop, not as a function call (matches existing `Op::FlagTest` precedent at program.rs:563-582).
    - Do NOT alter the existing `Op::Isg(reg)` / `Op::Dse(reg)` / `Op::FlagTest { kind, flag }` arms — Phase-22/21 behavior must remain bit-identical.
  </action>
  <verify>
    <automated>cargo build -p hp41-core 2>&amp;1 | tail -20 &amp;&amp; cargo test -p hp41-core --lib 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p hp41-core` exits 0 (all execute_op exhaustive-match failures from Task 1 are resolved).
    - `grep -c "Op::StoInd(reg) => crate::ops::indirect::op_sto_ind\|Op::RclInd(reg) => crate::ops::indirect::op_rcl_ind\|Op::StoArithInd(reg, kind) => crate::ops::indirect::op_sto_arith_ind\|Op::SfFlagInd(reg) => crate::ops::indirect::op_sf_flag_ind\|Op::CfFlagInd(reg) => crate::ops::indirect::op_cf_flag_ind\|Op::ArclInd(reg) => crate::ops::indirect::op_arcl_ind\|Op::AstoInd(reg) => crate::ops::indirect::op_asto_ind\|Op::ViewInd(reg) => crate::ops::indirect::op_view_ind" hp41-core/src/ops/program.rs` returns at least 8 (8 simple execute_op arms).
    - `grep -c "Op::IsgInd(reg) => crate::ops::indirect::op_isg_ind(state, reg).map(|_| ())\|Op::DseInd(reg) => crate::ops::indirect::op_dse_ind(state, reg).map(|_| ())" hp41-core/src/ops/program.rs` returns at least 2 (defense-in-depth execute_op arms with .map for IsgInd/DseInd).
    - `grep -c "Op::IsgInd(reg) => {" hp41-core/src/ops/program.rs` returns at least 2 (one in execute_op, one in run_loop).
    - `grep -B1 -A4 "Op::IsgInd(reg) => {" hp41-core/src/ops/program.rs | grep -c "state.pc += 1"` is at least 1 (the run_loop arm contains the skip-next-step `state.pc += 1`).
    - `grep -c "Op::FlagTestInd { kind, ind_reg } => {" hp41-core/src/ops/program.rs` is at least 1 (run_loop arm exists).
    - `grep -B0 -A20 "Op::FlagTestInd { kind, ind_reg } => {" hp41-core/src/ops/program.rs | grep -c "resolve_indirect(state, ind_reg)"` is at least 1 (the FlagTestInd run_loop arm calls the resolver).
    - `grep -B0 -A25 "Op::FlagTestInd { kind, ind_reg } => {" hp41-core/src/ops/program.rs | grep -c "FlagTestKind::IsSetThenClear\|FlagTestKind::IsClearThenClear"` is at least 2 (the kind-match block reuses the always-clear semantics for FS?C/FC?C).
    - `grep -c "| Op::IsgInd(_) | Op::DseInd(_) | Op::FlagTestInd { .. }" hp41-core/src/ops/program.rs` returns at least 1 (the catch-all extension is in place — Pitfall 6).
    - `cargo test -p hp41-core --lib indirect::tests` exits 0 (the 7 inline unit tests from Plan 24-01 still pass).
    - `cargo test -p hp41-core --test phase22_program_control` exits 0 (Phase-22 GTO/XEQ-IND tests + 4 sentinel tests from Plan 24-01 still pass — refactor is still behavior-preserving).
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    `cargo build -p hp41-core` is green. All 11 execute_op arms exist (8 simple + 2 IsgInd/DseInd defense-in-depth + FlagTestInd via catch-all). 3 run_loop arms exist with correct skip semantics. The programming-ops `|`-pattern catch-all includes IsgInd/DseInd/FlagTestInd (Pitfall 6 mitigated). All Plan-24-01 tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 11 op_display_name arms to BOTH prgm_display.rs copies (CLI + GUI mirror, SC-4 invariant)</name>
  <files>hp41-cli/src/prgm_display.rs (add 11 match arms), hp41-gui/src-tauri/src/prgm_display.rs (add identical 11 match arms — byte-for-byte mirror per SC-4)</files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (current file — locate `op_display_name` function and the existing analog arms: `Op::GtoInd(r) => format!("GTO IND {r:02}")` and `Op::XeqInd(r) => format!("XEQ IND {r:02}")` at ~lines 178-181, `Op::FlagTest { kind, flag } => { let mnemonic = match kind { ... } }` at ~lines 158-166, `Op::StoArith { reg, kind } => { let op_sym = match kind { ... } }` at ~lines 82-90)
    - hp41-gui/src-tauri/src/prgm_display.rs (current file — verify the same op_display_name function exists with the same analog arms; the GUI file is a deliberate byte-for-byte mirror per the SC-4 invariant; existing comment at PATTERNS.md §gui-prgm_display lines 222-226 documents this rule)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"`Op::StoArithInd` Arity Decision" lines 461-473 (single-variant op-symbol-table pattern for StoArithInd display)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Common Pitfalls" Pitfall 4 lines 527-549 (FlagTestInd kind-table-then-format pattern — verbatim block to copy)
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"hp41-cli/src/prgm_display.rs" lines 580-633 (full analog patterns for bare-mnemonic, kind-table, and op-symbol-table arms)
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"hp41-gui/src-tauri/src/prgm_display.rs" lines 636-672 (GUI mirror precedent — same arms in same order)
    - CLAUDE.md §"v2.0 additions (Tauri GUI)" SC-4 invariant: `op_display_name` is the ONLY documented exception to the no-duplication rule between hp41-core/cli/gui — every new variant added to the CLI copy MUST have an identical arm in the GUI copy.
  </read_first>
  <behavior>
    - `op_display_name(&Op::StoInd(5))` returns `"STO IND 05"` in both CLI and GUI copies.
    - `op_display_name(&Op::RclInd(7))` returns `"RCL IND 07"`.
    - `op_display_name(&Op::StoArithInd(12, StoArithKind::Add))` returns `"STO+ IND 12"`.
    - `op_display_name(&Op::StoArithInd(12, StoArithKind::Sub))` returns `"STO- IND 12"`.
    - `op_display_name(&Op::StoArithInd(12, StoArithKind::Mul))` returns `"STO\u{00D7} IND 12"`.
    - `op_display_name(&Op::StoArithInd(12, StoArithKind::Div))` returns `"STO\u{00F7} IND 12"`.
    - `op_display_name(&Op::IsgInd(5))` returns `"ISG IND 05"`.
    - `op_display_name(&Op::DseInd(5))` returns `"DSE IND 05"`.
    - `op_display_name(&Op::SfFlagInd(12))` returns `"SF IND 12"`.
    - `op_display_name(&Op::CfFlagInd(12))` returns `"CF IND 12"`.
    - `op_display_name(&Op::FlagTestInd { kind: FlagTestKind::IsSet, ind_reg: 5 })` returns `"FS? IND 05"`.
    - `op_display_name(&Op::FlagTestInd { kind: FlagTestKind::IsClear, ind_reg: 5 })` returns `"FC? IND 05"`.
    - `op_display_name(&Op::FlagTestInd { kind: FlagTestKind::IsSetThenClear, ind_reg: 5 })` returns `"FS?C IND 05"`.
    - `op_display_name(&Op::FlagTestInd { kind: FlagTestKind::IsClearThenClear, ind_reg: 5 })` returns `"FC?C IND 05"`.
    - `op_display_name(&Op::ArclInd(12))` returns `"ARCL IND 12"`.
    - `op_display_name(&Op::AstoInd(12))` returns `"ASTO IND 12"`.
    - `op_display_name(&Op::ViewInd(5))` returns `"VIEW IND 05"`.
    - `cargo build -p hp41-cli` and `cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml` both succeed (exhaustive-match satisfied in both prgm_display.rs files).
    - The 11 arms are byte-for-byte identical in both files (SC-4 mirror invariant).
  </behavior>
  <action>
    Step A — Add 11 arms to `hp41-cli/src/prgm_display.rs`:

    1. Locate `fn op_display_name` (currently around lines 27-209). Find a logical insertion point after the existing Phase-23 ALPHA arms (e.g., after `Op::Posa => "POSA".to_string(),` — use `grep -n "Op::Posa" hp41-cli/src/prgm_display.rs` to find the exact line). Add a new `// Phase 24: Indirect Addressing — see ROADMAP Phase 24` section divider comment.

    2. Add the 11 arms in this order (the 8 bare-mnemonic patterns mirror PATTERNS.md §cli-prgm_display lines 591-597):
       - `Op::StoInd(r) => format!("STO IND {r:02}"),`
       - `Op::RclInd(r) => format!("RCL IND {r:02}"),`
       - `Op::IsgInd(r) => format!("ISG IND {r:02}"),`
       - `Op::DseInd(r) => format!("DSE IND {r:02}"),`
       - `Op::SfFlagInd(r) => format!("SF IND {r:02}"),`
       - `Op::CfFlagInd(r) => format!("CF IND {r:02}"),`
       - `Op::ArclInd(r) => format!("ARCL IND {r:02}"),`
       - `Op::AstoInd(r) => format!("ASTO IND {r:02}"),`
       - `Op::ViewInd(r) => format!("VIEW IND {r:02}"),`

    3. Add the StoArithInd arm using the kind-table pattern (mirrors PATTERNS.md §cli-prgm_display lines 615-625):
       - `Op::StoArithInd(reg, kind) => {`
       - `    let op_sym = match kind {`
       - `        StoArithKind::Add => "+",`
       - `        StoArithKind::Sub => "-",`
       - `        StoArithKind::Mul => "\u{00D7}",`
       - `        StoArithKind::Div => "\u{00F7}",`
       - `    };`
       - `    format!("STO{op_sym} IND {reg:02}")`
       - `}`

    4. Add the FlagTestInd arm using the kind-table pattern from RESEARCH §Pitfall 4 lines 535-545:
       - `Op::FlagTestInd { kind, ind_reg } => {`
       - `    let mnemonic = match kind {`
       - `        FlagTestKind::IsSet => "FS?",`
       - `        FlagTestKind::IsClear => "FC?",`
       - `        FlagTestKind::IsSetThenClear => "FS?C",`
       - `        FlagTestKind::IsClearThenClear => "FC?C",`
       - `    };`
       - `    format!("{mnemonic} IND {ind_reg:02}")`
       - `}`

    5. Verify imports already cover `FlagTestKind` and `StoArithKind` (check existing line `use hp41_core::ops::{FlagTestKind, Op, StackReg, StoArithKind};` — should not need changes since the existing `Op::FlagTest` and `Op::StoArith` arms compile against these types).

    Step B — Add the SAME 11 arms to `hp41-gui/src-tauri/src/prgm_display.rs` (byte-for-byte mirror):

    1. Locate the matching `fn op_display_name` in the GUI file (currently around lines 47-230). Use `grep -n "Op::Posa\|fn op_display_name" hp41-gui/src-tauri/src/prgm_display.rs` to find the analogous insertion point.

    2. Add the SAME 11 arms in the SAME order, with a section divider comment per PATTERNS.md lines 670-671: `// Phase 24: Indirect Addressing — mirror of the hp41-cli copy — SC-4 invariant requires identical display listing on both frontends.`

    3. The 11 match arms must be character-identical to the CLI copy (a `diff` of just the new arm bodies must show zero differences).

    4. Verify imports already cover `FlagTestKind` and `StoArithKind` — they do, since the existing `Op::FlagTest` and `Op::StoArith` arms in the same file compile against these types.

    Step C — Add display tests (extend existing `tests` module in BOTH files):

    1. In `hp41-cli/src/prgm_display.rs`, locate the existing `#[cfg(test)] mod tests { ... }` block (or `mod tests` near the bottom — use `grep -n "^mod tests\|^#\[cfg(test)\]" hp41-cli/src/prgm_display.rs`). Append a new test function `test_display_phase24_ind_op_labels` containing one assertion per new variant. Use the verbatim outputs listed in `<behavior>` above (e.g., `assert_eq!(op_display_name(&Op::StoInd(5)), "STO IND 05");`). For Op::FlagTestInd assert all 4 sub-kinds (IsSet/IsClear/IsSetThenClear/IsClearThenClear). For Op::StoArithInd assert all 4 sub-kinds (Add/Sub/Mul/Div). 11 base assertions + 3 extra FlagTestInd + 3 extra StoArithInd = 17 assertions total in this test.

    2. In `hp41-gui/src-tauri/src/prgm_display.rs`, append the SAME `test_display_phase24_ind_op_labels` test (same name, same 17 assertions). The two test bodies must be byte-for-byte identical.

    Anti-patterns to avoid:
    - Do NOT add the IND arms to only one of the two prgm_display.rs files — SC-4 mirror invariant requires both. The compile error from one file's exhaustive-match will not appear if you forget the other (both files compile independently as part of different crates).
    - Do NOT use `format!("{mnemonic}IND{ind_reg:02}")` (no space) — Phase-22 precedent (`format!("GTO IND {r:02}")`) uses space-separated form, locked by RESEARCH §"Open Questions" #3.
    - Do NOT use `format!("{kind} IND {ind_reg:02}")` — `FlagTestKind` does NOT impl Display; Pitfall 4 explicitly warns about this. Always use the explicit kind-to-mnemonic mapping.
    - Do NOT touch the existing `Op::GtoInd(r)`, `Op::XeqInd(r)`, `Op::FlagTest { kind, flag }`, or `Op::StoArith { reg, kind }` arms — Phase-22/23 display behavior must remain unchanged.
  </action>
  <verify>
    <automated>cargo build -p hp41-cli 2>&amp;1 | tail -10 &amp;&amp; cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&amp;1 | tail -10 &amp;&amp; cargo test -p hp41-cli prgm_display::tests::test_display_phase24_ind_op_labels 2>&amp;1 | tail -15 &amp;&amp; cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml prgm_display::tests::test_display_phase24_ind_op_labels 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p hp41-cli` exits 0 (CLI prgm_display exhaustive-match satisfied).
    - `cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml` exits 0 (GUI prgm_display exhaustive-match satisfied; SC-4 mirror complete).
    - `grep -c "STO IND \|RCL IND \|ISG IND \|DSE IND \|SF IND \|CF IND \|ARCL IND \|ASTO IND \|VIEW IND " hp41-cli/src/prgm_display.rs` returns at least 9 (the 9 bare-mnemonic patterns are present).
    - `grep -c "STO IND \|RCL IND \|ISG IND \|DSE IND \|SF IND \|CF IND \|ARCL IND \|ASTO IND \|VIEW IND " hp41-gui/src-tauri/src/prgm_display.rs` returns at least 9 (GUI mirror has the same).
    - `grep -c "Op::StoArithInd(reg, kind) => {" hp41-cli/src/prgm_display.rs` is at least 1 AND same for the GUI copy (StoArithInd kind-table arm exists in both).
    - `grep -c "Op::FlagTestInd { kind, ind_reg } => {" hp41-cli/src/prgm_display.rs` is at least 1 AND same for the GUI copy (FlagTestInd kind-table arm exists in both).
    - `grep -c "FlagTestKind::IsSetThenClear => \"FS?C\"" hp41-cli/src/prgm_display.rs` is at least 1 AND same for the GUI copy (the always-clear mnemonics are correctly mapped).
    - `cargo test -p hp41-cli prgm_display::tests::test_display_phase24_ind_op_labels` exits 0.
    - `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml prgm_display::tests::test_display_phase24_ind_op_labels` exits 0.
    - `cargo build --workspace` exits 0 (every workspace member compiles together post-mirror).
    - `cargo clippy -p hp41-cli -- -D warnings` exits 0 AND `cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml -- -D warnings` exits 0.
    - The SC-4 spirit-check from CLAUDE.md (`grep -rn "fn op_(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum)" hp41-gui/src-tauri/src/`) returns nothing new (this task only adds match arms inside `op_display_name`, the documented exception).
  </acceptance_criteria>
  <done>
    Both `hp41-cli/src/prgm_display.rs` and `hp41-gui/src-tauri/src/prgm_display.rs` compile with all 11 new IND arms in identical byte-for-byte form. The `test_display_phase24_ind_op_labels` test passes in both files asserting all 17 expected display strings. SC-4 invariant holds — the only new code in hp41-gui/src-tauri is inside the documented `op_display_name` exception. `cargo build --workspace` is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Author phase24_ind_variants.rs integration test suite (~33+ tests covering happy / non-integer / out-of-bounds per variant + sidecar/lift inheritance bonuses) and verify coverage</name>
  <files>hp41-core/tests/phase24_ind_variants.rs (new)</files>
  <read_first>
    - hp41-core/tests/phase23_arcl_asto.rs (analog: per-Op-variant integration tests for a sidecar-aware register-addressed family — exact-match pattern per PATTERNS.md §phase24_ind_variants.rs analog lines 224-311; mirror the file header, imports, setup-then-dispatch-then-assert pattern, and sidecar-inheritance assertion pattern)
    - hp41-core/tests/phase22_program_control.rs (analog: GTO/XEQ-IND tests with `state.regs[n] = HpNum::from(...)` setup, `Decimal::from_str + HpNum::rounded` for non-integer cases, and `run_program` driver for skip-semantic tests)
    - hp41-core/src/ops/registers.rs (op_sto bounds check at lines 14-29 — confirms `if idx >= state.regs.len() { return Err(InvalidOp); }`; default state.regs.len() == 100 — verified at phase22_program_control.rs:264)
    - hp41-core/src/ops/flags.rs (op_sf bounds check at lines 39-46 — confirms `if n > 55 { return Err(InvalidOp); }`)
    - hp41-core/src/ops/display_ops.rs (op_view sets `display_override` to formatted regs[reg] — confirm this so `view_ind_happy` asserts on display_override field, not on stack)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Per-Variant Test Coverage Matrix (24-02)" lines 840-874 (full 33+ test name list with exact inputs and expected outcomes — table is the test matrix to copy-paste)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Common Pitfalls" Pitfall 1 (drives IsgInd/DseInd/FlagTestInd happy-path tests through `run_program`, NOT `dispatch`)
    - .planning/phases/24-indirect-addressing/24-VALIDATION.md §"Per-Task Verification Map" + §"Phase Requirements → Test Map" (the test names used in the plan map directly to the table rows)
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"Sidecar / Atomicity / Lift-effect Inheritance via Delegation" lines 677-690 (anti-pattern: do NOT replicate bounds/sidecar in shims — the IND tests verify these behaviors are inherited via delegation)
  </read_first>
  <behavior>
    - All 33+ integration tests in `hp41-core/tests/phase24_ind_variants.rs` pass via `cargo test -p hp41-core --test phase24_ind_variants`.
    - For each of the 11 new variants, three core test classes pass: happy path, non-integer pointer rejection, out-of-bounds pointer rejection (33 tests).
    - For Op::StoArithInd: 4 happy-path sub-tests covering Add/Sub/Mul/Div kinds (kind reuse coverage; +3 above the StoArithInd happy-path test in the 33 base = 36 total).
    - For Op::FlagTestInd: 4 happy-path sub-tests covering IsSet/IsClear/IsSetThenClear/IsClearThenClear kinds (+3 above the FlagTestInd happy-path test = 39 total).
    - Bonus inheritance tests: `sto_ind_clears_text_regs_sidecar` (D-23.4 inheritance for StoInd), `rcl_ind_lift_enable_inheritance` (Enable lift via op_rcl), `view_ind_shows_resolved_register_value` (R9 mitigation — display_override holds value of resolved register, NOT pointer register).
    - For Op::IsgInd / Op::DseInd / Op::FlagTestInd happy paths: tests drive through `run_program` (NOT `dispatch`) per Pitfall 1 — proves the run_loop arms wire skip semantics correctly.
    - For Op::FlagTestInd interactive dispatch: a separate test `flag_test_ind_interactive_is_neutral_no_op` asserts that `dispatch(state, Op::FlagTestInd { kind: IsSet, ind_reg: 5 })` returns `Ok(())` and does not mutate `state.pc` or `state.flags` (defends against accidentally adding skip semantics in dispatch).
    - `cargo test --workspace` exits 0 (full workspace test suite green).
    - `just coverage` reports `hp41-core` line coverage ≥ 92.5% (no regression vs current 92.68% baseline; expected delta is 0 to +0.5% per RESEARCH §"Coverage Estimate" lines 904-932).
  </behavior>
  <action>
    Step A — Create `hp41-core/tests/phase24_ind_variants.rs` with file header + imports per PATTERNS.md §phase24_ind_variants.rs analog lines 230-252:

    1. File header:
       - `//! Phase 24 Plan 02 (FN-IND-01 + FN-IND-02) integration tests for the 11 new`
       - `//! `Op::*Ind` variants. Three test classes per variant: happy / non-integer / out-of-bounds.`
       - `//!`
       - `//! Plus inheritance bonuses (D-23.4 sidecar via op_sto_ind, Enable lift via op_rcl_ind,`
       - `//! display_override semantics via op_view_ind, kind reuse for StoArithInd / FlagTestInd).`
       - `//!`
       - `//! Test modules allow unwrap (CLAUDE.md "Zero panics" applies to production code only).`
       - `#![allow(clippy::unwrap_used)]`

    2. Imports (mirror phase23_arcl_asto.rs lines 14-25, plus `run_program` for run_loop tests per Pitfall 1):
       - `use hp41_core::format::format_hpnum;` (for ARCL display-format assertions)
       - `use hp41_core::num::HpNum;`
       - `use hp41_core::ops::program::run_program;` (for IsgInd/DseInd/FlagTestInd happy-path tests — Pitfall 1)
       - `use hp41_core::ops::{dispatch, FlagTestKind, Op, StoArithKind};`
       - `use hp41_core::state::{CalcState, DisplayMode};`
       - `use hp41_core::HpError;`
       - `use rust_decimal::Decimal;`
       - `use std::str::FromStr;`

    Step B — Add the 33 base tests + ~6 sub-tests + ~3 bonus tests, organized by variant. For each variant, group the 3 tests under a `// ── <Op::*Ind> ─────` section divider. Use the test names from RESEARCH §"Per-Variant Test Coverage Matrix" verbatim.

    For StoInd:
    - `sto_ind_happy`: state.regs[5] = HpNum::from(12i32); state.stack.x = HpNum::from(7i32); dispatch(&mut state, Op::StoInd(5)).unwrap(); assert_eq!(state.regs[12], HpNum::from(7i32));
    - `sto_ind_non_integer`: state.regs[5] = HpNum::rounded(Decimal::from_str("12.5").unwrap()); let result = dispatch(&mut state, Op::StoInd(5)); assert!(matches!(result, Err(HpError::InvalidOp)));
    - `sto_ind_out_of_regs_len`: state.regs[5] = HpNum::from(200i32); let result = dispatch(&mut state, Op::StoInd(5)); assert!(matches!(result, Err(HpError::InvalidOp)));   // 200 > default regs.len() of 100, op_sto's bounds check fires
    - `sto_ind_clears_text_regs_sidecar` (BONUS — D-23.4 inheritance): state.regs[5] = HpNum::from(12i32); state.text_regs.insert(12, "ABC".to_string()); state.stack.x = HpNum::from(7i32); dispatch(&mut state, Op::StoInd(5)).unwrap(); assert_eq!(state.text_regs.get(&12), None);   // op_sto cleared the sidecar via D-23.4

    For RclInd:
    - `rcl_ind_happy`: state.regs[5] = HpNum::from(12i32); state.regs[12] = HpNum::from(99i32); dispatch(&mut state, Op::RclInd(5)).unwrap(); assert_eq!(state.stack.x, HpNum::from(99i32));
    - `rcl_ind_non_integer`: same shape as sto_ind_non_integer; expect InvalidOp
    - `rcl_ind_out_of_regs_len`: same shape as sto_ind_out_of_regs_len; expect InvalidOp
    - `rcl_ind_lift_enable_inheritance` (BONUS): same setup as rcl_ind_happy, then assert_eq!(state.stack.lift_enabled, true);   // Enable lift inherited from op_rcl

    For StoArithInd (1 happy + 3 sub-kind-happy + non-integer + out-of-bounds):
    - `sto_arith_ind_add_happy`: state.regs[5] = HpNum::from(12i32); state.regs[12] = HpNum::from(10i32); state.stack.x = HpNum::from(3i32); dispatch(&mut state, Op::StoArithInd(5, StoArithKind::Add)).unwrap(); assert_eq!(state.regs[12], HpNum::from(13i32));
    - `sto_arith_ind_sub_happy`: similar setup; expected regs[12] == 7 after Sub
    - `sto_arith_ind_mul_happy`: similar setup; expected regs[12] == 30 after Mul
    - `sto_arith_ind_div_happy`: state.regs[12] = HpNum::from(12); X=3; expected regs[12] == 4 after Div
    - `sto_arith_ind_non_integer`: pointer R05=12.5; expect InvalidOp on any kind (test with Add)
    - `sto_arith_ind_out_of_regs_len`: pointer R05=200; expect InvalidOp on any kind

    For IsgInd (use `run_program` per Pitfall 1):
    - `isg_ind_inside_run_loop`: state.regs[5] = HpNum::from(12i32); state.regs[12] = HpNum::rounded(Decimal::from_str("0.005").unwrap());   // ISG counter "0.005" — start=0, end=5, increment=increment-after-+1
      program = [Op::Lbl("A".to_string()), Op::IsgInd(5), Op::Gto("A".to_string()), Op::Lbl("END".to_string())]; run_program(&mut state, "A").unwrap(); assert that regs[12] reached the exit value (>= end + 0.001 epsilon per ISG semantics — match the existing test_isg pattern in `hp41-core/tests/` for the exact assertion).
    - `isg_ind_non_integer`: state.regs[5]=12.5; program = [Op::Lbl("A".to_string()), Op::IsgInd(5)]; let result = run_program(&mut state, "A"); assert!(matches!(result, Err(HpError::InvalidOp)));
    - `isg_ind_out_of_regs_len`: state.regs[5]=200; same shape; expect InvalidOp

    For DseInd: 3 mirror tests of the IsgInd shape with `Op::DseInd` substituted.

    For SfFlagInd:
    - `sf_flag_ind_happy`: state.regs[5] = HpNum::from(12i32); dispatch(&mut state, Op::SfFlagInd(5)).unwrap(); assert flag 12 is set via `crate::ops::flags::flag_get(state.flags, 12) == true` OR via `state.flags & (1u64 << 12) != 0`.
    - `sf_flag_ind_non_integer`: pointer = 12.5; expect InvalidOp
    - `sf_flag_ind_out_of_flag_range`: state.regs[5] = HpNum::from(60i32); expect InvalidOp from op_sf's `n > 55` bounds check (NOTE: this fires BEFORE the indirect bounds — op_sf returns InvalidOp because the resolved address is 60).

    For CfFlagInd:
    - `cf_flag_ind_happy`: state.regs[5] = HpNum::from(12i32); state.flags = 1u64 << 12; dispatch(&mut state, Op::CfFlagInd(5)).unwrap(); assert flag 12 is now clear.
    - `cf_flag_ind_non_integer`: pointer = 12.5; expect InvalidOp
    - `cf_flag_ind_out_of_flag_range`: state.regs[5] = HpNum::from(60i32); expect InvalidOp from op_cf's `n > 55` bounds.

    For FlagTestInd (1 happy + 3 sub-kind-happy + non-integer + interactive-no-op + high-flag-no-panic) — drive happy via `run_program` per Pitfall 1:
    - `flag_test_ind_is_set_happy_inside_run_loop`: state.regs[5]=12; state.flags = 1u64 << 12; program = [Op::Lbl("A".to_string()), Op::FlagTestInd { kind: FlagTestKind::IsSet, ind_reg: 5 }, Op::PushNum(HpNum::from(1i32)), Op::PushNum(HpNum::from(2i32))]; run_program(&mut state, "A").unwrap(); assert_eq!(state.stack.x, HpNum::from(2i32));   // flag was set, IsSet does NOT skip, both PushNums execute, X==2
    - `flag_test_ind_is_clear_happy_inside_run_loop`: same setup but flag is clear; with kind=IsClear, again no skip, X==2
    - `flag_test_ind_is_set_then_clear_happy_inside_run_loop`: state.regs[5]=12; state.flags = 1u64 << 12; program with FlagTestInd { kind: IsSetThenClear, ind_reg: 5 }; assert flag 12 is now CLEARED (always-clear semantic) AND skip-or-no-skip behavior matches IsSet (was set → no skip → X==2)
    - `flag_test_ind_is_clear_then_clear_happy_inside_run_loop`: state.regs[5]=12; state.flags = 0; program with FlagTestInd { kind: IsClearThenClear, ind_reg: 5 }; assert flag 12 is still clear (always-clear is no-op when already clear) AND no-skip behavior (was clear → no skip → X==2)
    - `flag_test_ind_non_integer`: state.regs[5]=12.5; program with FlagTestInd { kind: IsSet, ind_reg: 5 }; expect run_program to return Err(InvalidOp)
    - `flag_test_ind_high_flag_no_panic` (defensive — RESEARCH §"Op::FlagTestInd run_loop arm" note about `flag_get`/`flag_clear` defensive behavior for n > 55): state.regs[5]=100; program runs without panic; assert no error returned for the flag-test branch (flag_get returns false defensively for n=100; ISG-equivalent semantics apply).
    - `flag_test_ind_interactive_is_neutral_no_op` (BONUS — defends against accidentally adding skip semantics to dispatch): state.regs[5]=12; state.flags = 1u64 << 12; let pc_before = state.pc; let flags_before = state.flags; dispatch(&mut state, Op::FlagTestInd { kind: FlagTestKind::IsSetThenClear, ind_reg: 5 }).unwrap(); assert_eq!(state.pc, pc_before); assert_eq!(state.flags, flags_before);   // dispatch is Neutral no-op; pc and flags unchanged

    For ArclInd:
    - `arcl_ind_happy`: state.regs[5] = HpNum::from(12i32); state.regs[12] = HpNum::from(42i32); state.alpha_reg = String::new(); state.display_mode = DisplayMode::Fix(4); dispatch(&mut state, Op::ArclInd(5)).unwrap(); assert state.alpha_reg == format_hpnum(&state.regs[12], &state.display_mode);   // "42.0000" or whatever Fix(4) renders
    - `arcl_ind_non_integer`: pointer=12.5; expect InvalidOp
    - `arcl_ind_out_of_regs_len`: pointer=200; expect InvalidOp from op_arcl

    For AstoInd:
    - `asto_ind_happy`: state.regs[5] = HpNum::from(12i32); state.alpha_reg = "HELLO".to_string(); dispatch(&mut state, Op::AstoInd(5)).unwrap(); assert_eq!(state.text_regs.get(&12), Some(&"HELLO".to_string())); assert_eq!(state.regs[12], HpNum::zero());   // no-drift invariant: ASTO zeroes the numeric slot per phase23_arcl_asto.rs:81
    - `asto_ind_non_integer`: pointer=12.5; expect InvalidOp
    - `asto_ind_out_of_regs_len`: pointer=200; expect InvalidOp

    For ViewInd (R9 mitigation — display_override holds the RESOLVED register's value, NOT the pointer):
    - `view_ind_happy`: state.regs[5] = HpNum::from(12i32); state.regs[12] = HpNum::from(42i32); state.display_mode = DisplayMode::Fix(4); dispatch(&mut state, Op::ViewInd(5)).unwrap(); assert state.display_override == Some(format_hpnum(&HpNum::from(42i32), &state.display_mode));   // shows "42.0000", the value of R12 — NOT the value of R05 (which is 12)
    - `view_ind_shows_resolved_register_value` (BONUS — explicit R9 sentinel): same setup as view_ind_happy, but explicitly assert state.display_override does NOT contain "12" or any formatting of HpNum::from(12) — proves the pointer's value is never displayed.
    - `view_ind_non_integer`: pointer=12.5; expect InvalidOp
    - `view_ind_out_of_regs_len`: pointer=200; expect InvalidOp from op_view

    Step C — Run coverage gate:

    1. After Tasks 1-4 are committed and `cargo test --workspace` is green, run `just coverage` to produce the coverage report.
    2. The report must show `hp41-core` line coverage ≥ 92.5% (current baseline 92.68% per STATE.md). The expected delta is 0 to +0.5% per RESEARCH §"Coverage Estimate".
    3. If coverage regresses below 92.5%, identify the uncovered branch via the `cargo-llvm-cov` HTML report and add a targeted test (most likely candidates per RESEARCH "Hard-to-cover branches": `to_i64().ok_or(InvalidOp)?` failure for >i64 pointers — already covered in Plan 24-01 inline tests; `u8::try_from` failure for negative pointers — already covered; `Op::FlagTestInd` interactive dispatch no-op — covered by `flag_test_ind_interactive_is_neutral_no_op` bonus test in this task).

    Anti-patterns to avoid:
    - Do NOT use `dispatch()` for IsgInd/DseInd/FlagTestInd happy-path tests — Pitfall 1 says skip semantic only fires inside `run_loop`. Use `run_program` for those.
    - Do NOT skip the bonus inheritance tests (sidecar, lift, R9 view-shows-resolved-value) — they verify the D-24.4 delegation invariant and Pitfall R9 mitigation explicitly.
    - Do NOT skip the FlagTestInd interactive-no-op test — it defends against future regressions where someone might mistakenly add skip semantics to dispatch.
    - Do NOT use `state.regs[200] = ...` to set up the out-of-bounds tests — the array literal has only 100 slots by default. Set the POINTER register (e.g. state.regs[5]) to the value 200; the resolved address 200 then exceeds regs.len() inside the delegated direct op.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test phase24_ind_variants 2>&amp;1 | tail -20 &amp;&amp; cargo test --workspace 2>&amp;1 | tail -10 &amp;&amp; just coverage 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `hp41-core/tests/phase24_ind_variants.rs` exists.
    - `grep -c "^fn sto_ind_\|^fn rcl_ind_\|^fn sto_arith_ind_\|^fn isg_ind_\|^fn dse_ind_\|^fn sf_flag_ind_\|^fn cf_flag_ind_\|^fn flag_test_ind_\|^fn arcl_ind_\|^fn asto_ind_\|^fn view_ind_" hp41-core/tests/phase24_ind_variants.rs` returns at least 33 (covering 11 variants × 3 base classes).
    - `grep -c "^fn .*_ind_" hp41-core/tests/phase24_ind_variants.rs` returns at least 36 (33 base + 3+ bonus inheritance tests).
    - `cargo test -p hp41-core --test phase24_ind_variants` exits 0 with all tests passing (e.g., output line "test result: ok. 36 passed" or higher).
    - `cargo test -p hp41-core --test phase24_ind_variants sto_ind_happy` exits 0.
    - `cargo test -p hp41-core --test phase24_ind_variants sto_ind_non_integer` exits 0.
    - `cargo test -p hp41-core --test phase24_ind_variants sto_ind_out_of_regs_len` exits 0.
    - `cargo test -p hp41-core --test phase24_ind_variants sto_ind_clears_text_regs_sidecar` exits 0 (D-23.4 inheritance bonus).
    - `cargo test -p hp41-core --test phase24_ind_variants rcl_ind_lift_enable_inheritance` exits 0.
    - `cargo test -p hp41-core --test phase24_ind_variants sto_arith_ind_add_happy sto_arith_ind_sub_happy sto_arith_ind_mul_happy sto_arith_ind_div_happy` exits 0 (StoArithKind reuse coverage).
    - `cargo test -p hp41-core --test phase24_ind_variants isg_ind_inside_run_loop` exits 0 (Pitfall 1 sentinel — proves skip semantic via run_loop).
    - `cargo test -p hp41-core --test phase24_ind_variants dse_ind_inside_run_loop` exits 0.
    - `cargo test -p hp41-core --test phase24_ind_variants flag_test_ind_is_set_happy_inside_run_loop flag_test_ind_is_clear_happy_inside_run_loop flag_test_ind_is_set_then_clear_happy_inside_run_loop flag_test_ind_is_clear_then_clear_happy_inside_run_loop` exits 0 (FlagTestKind reuse + always-clear coverage).
    - `cargo test -p hp41-core --test phase24_ind_variants flag_test_ind_interactive_is_neutral_no_op` exits 0 (defends against accidentally adding skip semantics to dispatch).
    - `cargo test -p hp41-core --test phase24_ind_variants view_ind_happy view_ind_shows_resolved_register_value` exits 0 (R9 mitigation — VIEW IND shows resolved register's value).
    - `cargo test --workspace` exits 0 (every workspace member's tests pass).
    - `just test` exits 0 (justfile-driven equivalent — confirms `just`-only invariant from CLAUDE.md still holds).
    - `just coverage` reports `hp41-core` line coverage ≥ 92.5% (parse the output for the line coverage percentage; do not fail if the report's exact line is `lines: 92.65%` or higher).
    - `cargo clippy --workspace -- -D warnings` exits 0 (no new clippy warnings across any crate).
  </acceptance_criteria>
  <done>
    `hp41-core/tests/phase24_ind_variants.rs` ships with 36+ tests covering all 11 new variants × 3 test classes plus inheritance bonuses (D-23.4 sidecar, Enable lift, R9 view-resolved-value, kind reuse for StoArithInd / FlagTestInd, interactive-no-op defense). All tests pass via `cargo test -p hp41-core --test phase24_ind_variants`. `cargo test --workspace`, `just test`, and `cargo clippy --workspace -- -D warnings` are green. `just coverage` shows `hp41-core` line coverage ≥ 92.5% (no regression from baseline). All Plan 24-01 tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save-file → `Op` enum deserialization | `serde_json` reads program steps from `~/.hp41/autosave.json`; the 11 new `Op::*Ind` variants now appear in `state.program: Vec<Op>` and pass through `run_loop`/`execute_op`/`dispatch`. Malformed payloads (e.g. `Op::StoArithInd(255, StoArithKind::Div)` with no register at index 255) flow into `state.program` and are dispatched on next program run. |
| `state.regs[reg]` content → `resolve_indirect` arithmetic | Each new `Op::*Ind` variant feeds a register pointer through `resolve_indirect`. A poisoned register (e.g. magnitude > i64::MAX from a tampered save file) must collapse to `InvalidOp` without panicking. |
| `state.regs[resolved_addr]` content → delegated direct op | After resolution, the direct op (`op_sto`/`op_sf`/`op_view`/etc.) reads/writes `state.regs[addr]`. The direct op carries its own bounds check (D-22.11.1); the IND shim inherits it via delegation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | malformed save-file `Op::StoInd(reg)` with reg pointing into out-of-bounds register, OR `Op::FlagTestInd { kind, ind_reg }` with ind_reg > regs.len() | mitigate | The Plan 24-01 `resolve_indirect` helper enforces `state.regs.get(reg as usize).ok_or(InvalidOp)?` so a `Op::*Ind` variant referencing an out-of-bounds pointer-register collapses to InvalidOp without panic. The resolved address itself is then checked again by the direct-form op (`op_sto`/`op_sf`/etc.) — defense-in-depth at the resolution boundary AND at the write boundary. Verified by `*_out_of_regs_len` and `*_out_of_flag_range` tests in Task 4. |
| T-24-02-02 | Denial of service | `unwrap()`/`expect()` panic on a corrupt pointer crashing the calculator while running a save-loaded program | mitigate | `#![deny(clippy::unwrap_used)]` at `hp41-core/src/lib.rs` enforced workspace-wide. All 10 new shim functions in `indirect.rs` use only `?` propagation. Acceptance criterion in Task 1 grep proves no `.unwrap()` or `as u8` outside the `mod tests` block. |
| T-24-02-03 | Tampering | `Op::IsgInd`/`Op::DseInd`/`Op::FlagTestInd` skip semantics silently lost (Pitfall 1) — saved program no longer terminates as designed, infinite-loops or skips wrong steps | mitigate | Each of the 3 skip-semantic variants has a dedicated `*_inside_run_loop` test in Task 4 that drives through `run_program` (NOT `dispatch`). Pitfall 1 documented in RESEARCH; the run_loop arms in Task 2 use the `Result<bool, _>` shape directly to preserve the skip signal. Pitfall 6 catch-all extension also documents intent. |
| T-24-02-04 | Information disclosure | `Op::ViewInd 05` accidentally displays the value of R05 (the pointer) instead of the value of the resolved register R12 — leaks pointer-internal state to the user via the display | mitigate | R9 mitigation (per RESEARCH Risk Register lines 1067): `op_view_ind` delegates to `op_view(state, addr)`, which formats `state.regs[addr]` — i.e. the RESOLVED register, NOT the pointer. Dedicated test `view_ind_shows_resolved_register_value` in Task 4 explicitly asserts `display_override` contains the formatted value of R12 (not R05). |
| T-24-02-05 | Tampering | New `Op::*Ind` variant lands in dispatch but not in execute_op (or vice versa), causing inconsistent behavior between interactive use and saved-program playback | mitigate | The exhaustive-match enforcement in BOTH dispatch and execute_op fail-compile if any of the 11 new variants is missed in either site. The 4-place landing rule (D-22.21 / D-23.12) is enforced by the same exhaustive-match in both prgm_display.rs files. Task 1 acceptance criterion specifically expects the post-Task-1 build state to FAIL with non-exhaustive errors in execute_op + prgm_display, proving the gates fire correctly; Tasks 2 and 3 resolve them. |
| T-24-02-06 | Tampering | hp41-gui/src-tauri prgm_display drift (`op_display_name` mirror diverges from CLI copy) — same program shows different listings on CLI vs GUI, undermines feature parity | mitigate | Task 3 specifies byte-for-byte mirror with explicit acceptance criteria comparing the two files' new arm bodies. SC-4 invariant from CLAUDE.md is documented; the `op_display_name` exception is the only legitimate cross-crate display-formatter duplication. The `test_display_phase24_ind_op_labels` test runs in BOTH crates with identical assertions. |
| T-24-02-07 | Information disclosure | low-value (offline library, no network, no PII) | accept | `hp41-core` is a pure offline calculator emulator — no network, no logging, no PII. No exfiltration vector exists for the IND family or any other Op. |
| T-24-02-08 | Elevation of privilege | `Op::*Ind` allowing unauthorized writes to system flags (5x..55) or to register addresses outside the user's allotted range | accept | The HP-41 hardware permits access to all 56 user flags and all 100 default registers via direct ops; IND variants merely automate address selection. There is no privilege boundary inside the calculator state — all flags and registers are equally accessible by direct ops already shipped. The IND family does not cross any new privilege boundary. |

(Phase 24 / Plan 02 ships the user-facing variant set; coverage gate `just coverage` ≥ 92.5% guarantees the test suite catches both happy-path and rejection-path regressions for every new variant.)
</threat_model>

<verification>
- `cargo build --workspace` exits 0
- `cargo test --workspace` exits 0 (all hp41-core / hp41-cli / hp41-gui tests green; includes Plan 24-01 tests + 36+ new tests in this plan)
- `cargo clippy --workspace -- -D warnings` exits 0
- `just test` exits 0 (justfile-driven equivalent — preserves CLAUDE.md `just`-only invariant)
- `just coverage` reports `hp41-core` line coverage ≥ 92.5% (no regression vs current 92.68% baseline)
- 4-place landing verified by exhaustive-match compilation: every new variant compiles in `dispatch()` (mod.rs), `execute_op()` and/or `run_loop` (program.rs), `op_display_name` in `hp41-cli/src/prgm_display.rs`, and `op_display_name` in `hp41-gui/src-tauri/src/prgm_display.rs`
- SC-4 spirit-check: `grep -rn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/` returns nothing new (only `op_display_name` is added in Task 3, which is the documented exception)
- Save-file backward compatibility: a Phase-23-era autosave.json (no `Op::*Ind` variants) loads and runs in this build — verified at run time when the executor runs `just gui-dev` and confirms the pre-existing autosave loads cleanly
</verification>

<success_criteria>
- 11 new `Op::*Ind` variants (10 tuple + 1 struct) declared in `hp41-core::ops::Op` enum
- 10 new `pub(crate) fn op_*_ind` shim functions in `hp41-core/src/ops/indirect.rs` (FlagTestInd has no shim by design)
- Every new variant lands in 4 places: `dispatch()` (mod.rs), `execute_op()` (program.rs), `op_display_name` (cli prgm_display.rs), `op_display_name` (gui prgm_display.rs)
- 3 run_loop arms (`Op::IsgInd`, `Op::DseInd`, `Op::FlagTestInd`) preserve skip-next-step semantics inside running programs (Pitfall 1 mitigated)
- The programming-ops `|`-pattern catch-all in execute_op extends to include `Op::IsgInd(_) | Op::DseInd(_) | Op::FlagTestInd { .. }` (Pitfall 6 mitigated)
- `op_sto_ind` inherits the D-23.4 text_regs sidecar clear via delegation; `op_rcl_ind` inherits Enable lift; `op_view_ind` displays the resolved register's value (R9 mitigated)
- 36+ integration tests in `hp41-core/tests/phase24_ind_variants.rs` pass: 33 base (11 variants × happy/non-integer/out-of-bounds) + 3 StoArithKind sub-tests + 3 FlagTestKind sub-tests + 3+ inheritance bonuses
- `op_display_name` arms produce hardware-faithful listings: `STO IND 05`, `STO+ IND 12`, `FS?C IND 05`, etc. — verified by `test_display_phase24_ind_op_labels` in BOTH cli + gui prgm_display copies (SC-4 mirror)
- `cargo test --workspace`, `just test`, and `cargo clippy --workspace -- -D warnings` are all green
- `just coverage` reports `hp41-core` line coverage ≥ 92.5% (no regression from current 92.68% baseline)
- No new `CalcState` fields, no new `HpError` variants, no save-file migration needed (Phase-23 autosave.json loads unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/24-indirect-addressing/24-02-SUMMARY.md` documenting:
- The 11 new `Op::*Ind` variants and their direct-form delegation targets
- The 3 run_loop arms with skip semantics (IsgInd/DseInd/FlagTestInd) and confirmation that Pitfalls 1 and 6 were exercised by integration tests
- Confirmation that all 4-place landings hold (dispatch + execute_op + cli prgm_display + gui prgm_display) — SC-4 mirror invariant verified
- Final test count (36+) and pass/fail status from `cargo test -p hp41-core --test phase24_ind_variants`
- Final `hp41-core` coverage delta from the `just coverage` run (baseline 92.68% → post-Phase-24 actual)
- Save-file backward compatibility evidence (Phase-23 autosave.json loads in `just gui-dev` without error)
- Hand-off note to Phase 25 (CLI Integration) and Phase 26 (GUI Polish): the 11 new Op variants and their string-IDs (`sto_ind`, `rcl_ind`, `sto_arith_ind`, `isg_ind`, `dse_ind`, `sf_flag_ind`, `cf_flag_ind`, `flag_test_ind`, `arcl_ind`, `asto_ind`, `view_ind`) await keyboard wiring (Phase 25) and key_map.rs registration (Phase 26)
</output>
