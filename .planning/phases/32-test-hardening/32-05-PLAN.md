---
phase: 32
plan: "32-05"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/math1_trans_error_branches.rs
  - hp41-core/tests/math1_four_error_branches.rs
autonomous: true
requirements:
  - QUAL-01
gap_closure: true
tags:
  - gap-closure
  - coverage
  - trans
  - four
  - error-branches
must_haves:
  truths:
    - "After this plan ships, `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/trans.rs` at >= 90 % line coverage (currently 81.17 %) AND `ops/math1/four.rs` at >= 90 % line coverage (currently 81.29 %)."
    - "All new tests carry the verbatim Free42 disclaim header on lines 1–2 per ADR-002."
    - "Every new `#[test]` carries a `// Catches: <bug class>` doc comment per D-27.1."
    - "Tests use `approx::assert_relative_eq!(..., max_relative = 1e-7)` for any decimal comparisons (Pitfall 14 / 17)."
    - "`cargo test -p hp41-core --test math1_trans_error_branches` AND `--test math1_four_error_branches` both report all-passing."
  artifacts:
    - path: "hp41-core/tests/math1_trans_error_branches.rs"
      provides: "Error-branch coverage for ops/math1/trans.rs uncovered arms (Rodrigues degenerate axis + submit_step Ready arms)"
      contains: "#[test]"
    - path: "hp41-core/tests/math1_four_error_branches.rs"
      provides: "Error-branch coverage for ops/math1/four.rs uncovered arms (no-valid-period guard + submit_step Ready arms)"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/math1_trans_error_branches.rs"
      to: "hp41-core/src/ops/math1/trans.rs"
      via: "dispatch(&mut s, Op::Trans2d/Trans3d) + submit_step direct calls"
      pattern: "dispatch\\(.*Op::Trans|TransInputStep::"
    - from: "hp41-core/tests/math1_four_error_branches.rs"
      to: "hp41-core/src/ops/math1/four.rs"
      via: "dispatch(&mut s, Op::Four) + submit_step direct calls"
      pattern: "dispatch\\(.*Op::Four|FourInputStep::"
---

<objective>
Close two adjacent coverage gaps in tandem: `ops/math1/trans.rs` (81.17 % -> >= 90 %, +100 missed lines / 531 -> cover >= 478) AND `ops/math1/four.rs` (81.29 % -> >= 90 %, +64 missed lines / 342 -> cover >= 308). Both files share the same error-branch shape — `submit_step` returning `HpError::InvalidOp` from `Ready` and other terminal arms, plus computational guards (`HpError::Domain` for degenerate-axis on TRANS, no-valid-period on FOUR).

Purpose: Plan 32-05 closes the next two largest math1 gaps after POLY. Together with 32-04 these three plans cover ~263 missed lines (the bulk of the workspace shortfall).

Output: TWO new test files (`math1_trans_error_branches.rs` and `math1_four_error_branches.rs`), each ~6–10 tests / ~150–250 lines. Test-only. No `hp41-core/src/` modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md
@.planning/phases/32-test-hardening/32-CONTEXT.md

# Source files with the error branches being targeted
@hp41-core/src/ops/math1/trans.rs
@hp41-core/src/ops/math1/four.rs

# Analog test file (combined four/tri/trans tests — for patterns + helpers)
@hp41-core/tests/math1_four_tri_trans.rs

# Lint that will judge new test files
@hp41-core/tests/lint_math1_assertions.rs

<interfaces>
<!-- Key public surface from trans.rs and four.rs. Extracted from source via grep. -->

From `hp41-core/src/ops/math1/trans.rs`:
- `pub fn op_trans_2d(...) / op_trans_3d(...) / op_trans_forward(...) / op_trans_inverse(...)` (verify via grep `^pub fn` in source — list all `pub fn` entries before authoring tests)
- `pub fn submit_step(state: &mut CalcState, step: TransInputStep) -> Result<(), HpError>` (location: trans.rs around line 455+)
- `TransInputStep` variants (from `hp41-core/src/ops/math1/modal.rs`): see existing file

From `hp41-core/src/ops/math1/four.rs`:
- `pub fn op_four(...) / op_four_workflow(...)` (verify via grep)
- `pub fn submit_step(state: &mut CalcState, step: FourInputStep) -> Result<(), HpError>` (location: four.rs around line 340+)
- `FourInputStep` variants (from `hp41-core/src/ops/math1/modal.rs`)

Error variants used by these files:
- `HpError::Domain` (unit variant)
- `HpError::InvalidOp` (unit variant)

Import pattern for both new files:
- `use hp41_core::ops::math1::modal::{ModalProgram, TransInputStep, FourInputStep};`
- `use hp41_core::ops::math1::trans::submit_step as trans_submit_step;` (rename for the trans file)
- `use hp41_core::ops::math1::four::submit_step as four_submit_step;` (rename for the four file)
- `use hp41_core::ops::{dispatch, Op};`
- `use hp41_core::{CalcState, HpError, HpNum};`
</interfaces>

<error_branches_target_list>
<!-- Verified via grep on trans.rs and four.rs (see CLAUDE.md context tables). -->

### trans.rs

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| trans.rs:87 | `.ok_or(HpError::Domain)` — Decimal-to-f64 NaN bridge | `decimal_nan_bridges_to_domain` (set X to a poisoned HpNum to trigger; OR document unreachable) |
| trans.rs:117 | Rodrigues axis with zero length -> `Err(HpError::Domain)` | `rodrigues_zero_axis_returns_domain` |
| trans.rs:469 | `submit_step` arm 1 -> `Err(HpError::InvalidOp)` | `submit_step_invalid_step_1` |
| trans.rs:484 | `submit_step` arm 2 -> `Err(HpError::InvalidOp)` | `submit_step_invalid_step_2` |
| trans.rs:503 | `submit_step` arm 3 -> `Err(HpError::InvalidOp)` | `submit_step_invalid_step_3` |
| trans.rs:569 | `TransInputStep::Ready` -> `Err(HpError::InvalidOp)` | `submit_step_ready_returns_invalid_op` |

Plus 1–2 tests covering the 22 missed `Functions` lines (`Missed Functions = 22` in trans.rs per coverage table — exercise a handful of `op_trans_*` entrypoints that aren't yet called by existing `math1_four_tri_trans.rs` tests, such as the inverse-3D and the inverse-2D paths if not covered).

### four.rs

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| four.rs:87 | `.ok_or(HpError::Domain)` — Decimal NaN bridge | `decimal_nan_bridges_to_domain` (document unreachable if so) |
| four.rs:139 | computational guard -> `Err(HpError::Domain)` | `four_invalid_state_returns_domain` |
| four.rs:285 | "no valid period" guard -> `Err(HpError::Domain)` | `four_no_valid_period_returns_domain` |
| four.rs:349 | `submit_step` arm -> `Err(HpError::InvalidOp)` | `submit_step_arm_349` |
| four.rs:365 | `submit_step` arm -> `Err(HpError::InvalidOp)` | `submit_step_arm_365` |
| four.rs:375 | `submit_step` arm -> `Err(HpError::InvalidOp)` | `submit_step_arm_375` |
| four.rs:386 | `submit_step` arm -> `Err(HpError::InvalidOp)` | `submit_step_arm_386` |
| four.rs:408 | `submit_step` arm -> `Err(HpError::InvalidOp)` | `submit_step_arm_408` |
| four.rs:423 | `FourInputStep::Ready` -> `Err(HpError::InvalidOp)` | `submit_step_ready_returns_invalid_op` |

</error_branches_target_list>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconnaissance — grep pub-fn entries and confirm step variants in trans.rs and four.rs</name>
  <read_first>
    - hp41-core/src/ops/math1/trans.rs (focus: lines 80–125 for Rodrigues guards, lines 455–569 for submit_step arms)
    - hp41-core/src/ops/math1/four.rs (focus: lines 80–145 for guards, lines 280–290 for no-valid-period, lines 340–425 for submit_step)
    - hp41-core/src/ops/math1/modal.rs (search for `pub enum TransInputStep` and `pub enum FourInputStep` — list all variants)
    - hp41-core/tests/math1_four_tri_trans.rs (header + helpers — copy the make_state pattern, the print_buffer assertion style)
  </read_first>
  <files>(read-only)</files>
  <action>
    Run `grep -nE "^pub fn|HpError::Domain|HpError::InvalidOp" hp41-core/src/ops/math1/trans.rs` and `grep -nE "^pub fn|HpError::Domain|HpError::InvalidOp" hp41-core/src/ops/math1/four.rs` to enumerate the exact public-surface entries and error-branch line numbers. Cross-check against the `<error_branches_target_list>` table above; adjust line numbers if they have drifted.

    For each `submit_step` arm that returns `Err(HpError::InvalidOp)`, identify which `TransInputStep` / `FourInputStep` variant it corresponds to (the match arm pattern). Each test will call `submit_step(&mut s, <variant>)` with the right state setup to reach that arm.

    For trans.rs:117 (Rodrigues zero-axis): determine which `pub fn` callable triggers `find_axis_normalized` or the equivalent — likely `op_trans_3d` (3D mode A-entry) after setting axis components (a, b, c) all to zero. Document the exact dispatch sequence.

    For four.rs:285 (no-valid-period): determine which `submit_step` path or `op_four_*` entry reaches this. Likely after the user has entered no samples or the period is zero.

    **Arm reachability classification (per the 32-04 quadratic_zero_leading_returns_domain pattern):** for each target arm, classify it as REACHABLE (a normal state-machine path lands on it; test will be authored) or UNREACHABLE (the arm is a defensive `_ =>` that the type system prevents from being hit in normal state). Unreachable arms are documented in the SUMMARY and removed from the test target count — they are NOT covered by `#[allow(dead_code)]` test stubs.
  </action>
  <verify>
    <automated>grep -c "^pub fn" hp41-core/src/ops/math1/trans.rs</automated>
    <automated>grep -c "^pub fn" hp41-core/src/ops/math1/four.rs</automated>
  </verify>
  <done>Line numbers and variant identifiers confirmed. Each target arm classified REACHABLE or UNREACHABLE. Task 2 can now author the test file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author hp41-core/tests/math1_trans_error_branches.rs (6–10 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/trans.rs (specifically the lines named in the trans target table)
    - hp41-core/tests/math1_four_tri_trans.rs (existing TRANS section — for state-setup patterns)
    - hp41-core/tests/math1_mod_entry_points.rs (mirror file-header doc-comment style)
    - hp41-core/tests/lint_math1_assertions.rs (verify the new file does not trip Pitfall 14/17)
  </read_first>
  <files>hp41-core/tests/math1_trans_error_branches.rs</files>
  <action>
    Create new file `hp41-core/tests/math1_trans_error_branches.rs` with structure:

    1. **Lines 1–2** (verbatim ADR-002 disclaim):
       Line 1: `// Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979);`
       Line 2: `// Free42 source consulted only as sanity-check oracle, not copied.`
    2. **File-doc** (`//!`): "Plan 32-05 surgical gap-closure for `hp41-core/src/ops/math1/trans.rs` — closes the 81.17 % per-file coverage gap by exercising the Rodrigues zero-axis guard (line 117), the four `submit_step` Err-arms (lines 469, 484, 503, 569), and 1–2 untested `op_trans_*` entries surfaced by Task 1 reconnaissance."
    3. **File-scope clippy-allow**: `#![allow(clippy::unwrap_used)]`
    4. **Imports** per `<interfaces>` block.
    5. **Helpers**: `make_state()` -> CalcState::new(); `set_stack_xyz(state, x, y, z)` for 3D axis tests; `set_reg(state, idx, val)`.
    6. **Tests** — 6 mandatory + up to 4 optional (only REACHABLE arms per Task 1 classification):

       a) `rodrigues_zero_axis_returns_domain` — set up TRANS 3D state with axis (0, 0, 0), invoke the `op_trans_3d` flow (or whichever pub fn reaches the normalize step), assert `Err(HpError::Domain)`. Covers trans.rs:117.

       b) `submit_step_arm_469_returns_invalid_op` — call `trans_submit_step(&mut s, <variant>)` where `<variant>` is the `TransInputStep` variant hitting line 469 (identified in Task 1). Assert `Err(HpError::InvalidOp)`.

       c) `submit_step_arm_484_returns_invalid_op` — same for line 484.

       d) `submit_step_arm_503_returns_invalid_op` — same for line 503.

       e) `submit_step_ready_returns_invalid_op` — `trans_submit_step(&mut s, TransInputStep::Ready)`; assert `Err(HpError::InvalidOp)`. Covers trans.rs:569.

       f) `op_trans_2d_inverse_round_trip` (or similar) — exercise one of the 22 missed `Functions` paths (per `Missed Functions = 22` coverage marker on trans.rs). Use a 2D translate-rotate-then-inverse round-trip and assert that the result returns to the original X/Y within `max_relative = 1e-7`. Use `approx::assert_relative_eq!` for the decimal compare.

       Optional g–j) — additional `op_trans_3d_*` entries surfaced in Task 1, OR additional Rodrigues edge cases (axis with one non-zero component, axis with rotation angle 0 / 2*pi). Each carries `// Catches:` and a specific bug-class.

    **Assertion discipline**: same rules as Plan 32-04 — `approx::assert_relative_eq!` for decimals; `matches!(r, Err(HpError::Domain))` for error matching; no `assert_eq!(decimal, decimal)`.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_trans_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_trans_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_trans_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 6 tests passing for REACHABLE error arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count. Disclaim header present, lint continues to pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author hp41-core/tests/math1_four_error_branches.rs (8–10 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/four.rs (focus on the 9 line numbers in the FOUR target table)
    - hp41-core/tests/math1_four_tri_trans.rs (FOUR section)
    - hp41-core/tests/lint_math1_assertions.rs
  </read_first>
  <files>hp41-core/tests/math1_four_error_branches.rs</files>
  <action>
    Same shape as Task 2 but for `four.rs`. Mandatory tests cover lines 87 (if reachable), 139, 285, 349, 365, 375, 386, 408, 423. Each `submit_step` arm test calls `four_submit_step(&mut s, <variant>)` with state setup that lands on that specific arm (identified in Task 1).

    For four.rs:285 (no-valid-period): the most likely setup is `op_four_workflow` with sample period zero — verify the exact path in Task 1.

    For four.rs:139: identify the guard semantics (likely "no samples entered" or "no FREQ set") and craft state accordingly.

    **Assertion discipline**: same rules. No decimal `assert_eq!`. Use `matches!(r, Err(HpError::Domain | HpError::InvalidOp))` for error matching.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_four_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_four_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_four_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 8 tests passing for REACHABLE error arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count. Disclaim header present, lint continues to pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Measure per-file coverage delta on trans.rs and four.rs</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md (per-file coverage table)
  </read_first>
  <files>(no file writes — measurement only)</files>
  <action>
    Run `cargo llvm-cov --package hp41-core --summary-only` and inspect the `ops/math1/trans.rs` and `ops/math1/four.rs` lines. Verify both are >= 90.0 % lines. If either is still below 90 %, identify the unreached sub-branches via `cargo llvm-cov --html -p hp41-core` and add 1–3 more targeted tests to that file.
  </action>
  <verify>
    <automated>cargo llvm-cov --package hp41-core --summary-only | grep -E "ops/math1/(trans|four)\.rs"</automated>
  </verify>
  <done>Both `ops/math1/trans.rs` and `ops/math1/four.rs` line coverage >= 90.0 %.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-01 | Tampering | New test files' assertion discipline | mitigate | Every `// Catches:` names a specific bug class; assertions use `matches!(r, Err(HpError::*))` or `approx::assert_relative_eq!`. Lint `lint_math1_assertions.rs` continues to gate. |
| T-32-05-02 | Information Disclosure | Free42 GPL contamination | mitigate | Verbatim disclaim header on lines 1–2 of both new files per ADR-002. |
| T-32-05-03 | Repudiation | Coverage padding without regression signal | mitigate | Each test targets a line number named in the `<error_branches_target_list>` table; tests assert specific error variants or state transitions. Unreachable arms are not padded with `#[allow(dead_code)]` stubs — they are surfaced in the SUMMARY's reach-confirmed test count. |
</threat_model>

<verification>
- `cargo test -p hp41-core --test math1_trans_error_branches` passes with >= 6 reach-confirmed tests
- `cargo test -p hp41-core --test math1_four_error_branches` passes with >= 8 reach-confirmed tests
- `cargo test -p hp41-core --test lint_math1_assertions` continues to pass
- `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/trans.rs` >= 90.0 % AND `ops/math1/four.rs` >= 90.0 %
- Both new files carry the disclaim header on lines 1–2
- `// Catches:` count >= 6 in trans file, >= 8 in four file (or whatever the reach-confirmed count is — documented in SUMMARY)
</verification>

<success_criteria>
- `ops/math1/trans.rs` line coverage >= 90.0 % (up from 81.17 %).
- `ops/math1/four.rs` line coverage >= 90.0 % (up from 81.29 %).
- Two new test files exist with all-passing tests.
- Reach-confirmed test count documented in SUMMARY (REACHABLE arms covered, UNREACHABLE arms catalogued).
- All frozen invariants preserved (no `hp41-core/src/` changes, no new deps, MSRV unchanged).
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-05-SUMMARY.md` when done with per-task completion, final coverage percentages for trans.rs and four.rs, total `// Catches:` count per file, the REACHABLE-vs-UNREACHABLE arm classification table, and any additional tests beyond mandatory minimum.
</output>
</content>
</invoke>