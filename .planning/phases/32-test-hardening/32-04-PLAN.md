---
phase: 32
plan: "32-04"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/math1_poly_error_branches.rs
autonomous: true
requirements:
  - QUAL-01
gap_closure: true
tags:
  - gap-closure
  - coverage
  - poly
  - error-branches
must_haves:
  truths:
    - "After this plan ships, `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/poly.rs` at ≥ 90 % line coverage (currently 76.37 % → +57 lines covered out of 99 currently missed)."
    - "All new tests carry the verbatim Free42 disclaim header on lines 1–2 per ADR-002."
    - "Every new `#[test]` carries a `// Catches: <bug class>` doc comment per D-27.1."
    - "Tests use `approx::assert_relative_eq!(..., max_relative = 1e-7)` for any decimal comparisons (not `assert_eq!(decimal, decimal)` per Pitfall 17)."
    - "`cargo test -p hp41-core --test math1_poly_error_branches` reports >= 10 passing tests."
  artifacts:
    - path: "hp41-core/tests/math1_poly_error_branches.rs"
      provides: "Error-branch coverage for ops/math1/poly.rs uncovered arms"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/math1_poly_error_branches.rs"
      to: "hp41-core/src/ops/math1/poly.rs"
      via: "dispatch(&mut s, Op::Roots) / dispatch(Op::PolyWorkflow) / submit_step(state, PolyInputStep::*)"
      pattern: "dispatch\\(.*Op::(Roots|PolyWorkflow)|PolyInputStep::"
---

<objective>
Close the POLY coverage gap (76.37 % -> >= 90 % on `hp41-core/src/ops/math1/poly.rs`) with risk-weighted error-branch tests. POLY is the single largest contributor to the QUAL-01 / ROADMAP SC-1 coverage shortfall — closing it lifts the workspace total by ~0.5 pts.

Purpose: This is a gap-closure plan that empirically closes the v3.0.1-deferred QUAL-01 gate (per the user's explicit reversal of the Plan 32-03 Rule 4 deferral). It adds tests that exercise the documented-but-untested error arms in `bairstow_deflate` (POLY-07 non-convergence), `solve_quadratic` (degenerate `a ~= 0`), `infer_degree` (degenerate all-zero polynomial), and `submit_step` (`PolyInputStep::Ready` rejection, `CoefficientPrompt` out-of-range index).

Output: One new test file `hp41-core/tests/math1_poly_error_branches.rs` (~10–14 tests, ~180–250 lines). Test-only changes. No `hp41-core/src/` modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md
@.planning/phases/32-test-hardening/32-CONTEXT.md

# Source file with the error branches being targeted
@hp41-core/src/ops/math1/poly.rs

# Existing POLY test patterns to mirror (assert pattern, doc-comment shape, helpers)
@hp41-core/tests/math1_poly.rs

# Lint that will judge new test file's assertion discipline (Pitfall 14 / 17)
@hp41-core/tests/lint_math1_assertions.rs

# Canonical XEQ name source
@docs/hp41-math1-functions.json

<interfaces>
<!-- Key public surface from hp41-core/src/ops/math1/poly.rs the executor will call. Extracted from source. -->

From hp41-core/src/ops/math1/poly.rs (verbatim signatures):
- `pub fn op_poly_workflow(state: &mut CalcState) -> Result<(), HpError>`
- `pub fn op_roots(state: &mut CalcState) -> Result<(), HpError>`
- `pub fn submit_step(state: &mut CalcState, step: PolyInputStep) -> Result<(), HpError>`

`PolyInputStep` variants (from `hp41-core/src/ops/math1/modal.rs`):
- `DegreePrompt`
- `CoefficientPrompt(u8 degree, u8 idx)`
- `Ready`

Error variants (`hp41-core/src/error.rs`):
- `HpError::Domain` — unit variant; surfaces as "DATA ERROR" on the HP-41 display
- `HpError::InvalidOp` — unit variant
- `HpError::OutOfRange` — unit variant

Imports the new test file will need:
- `use hp41_core::ops::math1::modal::{ModalProgram, PolyInputStep};`
- `use hp41_core::ops::math1::poly::submit_step;`
- `use hp41_core::ops::{dispatch, Op};`
- `use hp41_core::{CalcState, HpError, HpNum};`
- `use rust_decimal::Decimal;`
- `use rust_decimal::prelude::FromPrimitive;`
</interfaces>

<error_branches_target_list>
<!-- These are the specific source lines the new tests must reach. Verified via grep -n on poly.rs. -->

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| poly.rs:218 | `infer_degree`: all-zero registers -> `Err(HpError::Domain)` | `infer_degree_all_zero_returns_domain` |
| poly.rs:232 | `find_roots(n=0)`: constant polynomial -> `Err(HpError::Domain)` | `find_roots_constant_poly_returns_domain` (via dispatch+R00=0..R5=0) |
| poly.rs:239–240 | `find_roots(n=1)`: `a.abs() < 1e-300` -> `Err(HpError::Domain)` | `linear_zero_leading_returns_domain` |
| poly.rs:261–262 | `solve_quadratic(a=0)`: degenerate quadratic -> `Err(HpError::Domain)` | `quadratic_zero_leading_returns_domain` |
| poly.rs:329–330 | `bairstow_deflate`: `p\|q > 1e9` non-convergence -> `Err(HpError::Domain)` (POLY-07) | `bairstow_poly07_p_overflow` |
| poly.rs:362–363 | `bairstow_deflate`: residuals `r\|s > 1e9` -> `Err(HpError::Domain)` | `bairstow_poly07_residual_overflow` |
| poly.rs:475 | `submit_step(CoefficientPrompt{idx=200})`: register out-of-range -> `Err(HpError::InvalidOp)` | `submit_step_coefficient_idx_out_of_range` |
| poly.rs:526 | `submit_step(PolyInputStep::Ready)` -> `Err(HpError::InvalidOp)` | `submit_step_ready_returns_invalid_op` |

Additional underconstrained arms (currently exercised by happy-path tests but with weak assertions):
- `solve_quadratic` complex-pair branch (lines 277–286) — exercised but the `v.abs()` sign-flip path can be reinforced with `x^2 + 2x + 5 = 0` (complex roots `-1 +/- 2i`).
- `submit_step DegreePrompt` clamp paths (lines 472–477) — `clamp(2,5)` to upper and lower bounds.

</error_branches_target_list>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconnaissance — read poly.rs error arms and confirm helper signatures</name>
  <read_first>
    - hp41-core/src/ops/math1/poly.rs (already loaded in context; re-read lines 200–530 specifically: `infer_degree`, `find_roots`, `solve_quadratic`, `bairstow_deflate`, `submit_step`)
    - hp41-core/src/ops/math1/modal.rs (read the `PolyInputStep` enum definition — confirm variants are `DegreePrompt`, `CoefficientPrompt(u8, u8)`, `Ready`)
    - hp41-core/tests/math1_poly.rs (read header + `make_state` / `set_reg` helpers — re-use bit-for-bit in new file)
    - hp41-core/src/error.rs (confirm `HpError::Domain` is a unit variant — needed for `matches!(r, Err(HpError::Domain))` shape)
  </read_first>
  <files>(read-only — no writes in this task)</files>
  <action>Verify the 7 error-branch line numbers in the target table by re-reading the relevant lines of poly.rs. Confirm that `solve_quadratic` is a private `fn` (not `pub`) and therefore must be exercised indirectly via `Op::Roots` dispatch with degree-2 coefficients R00=0, R01=1, R02=2 (which routes through `find_roots(n=1)` after `infer_degree` returns 1). Confirm `bairstow_deflate` is private and must be exercised via `Op::Roots` with degree-3+ coefficients. Confirm `submit_step` is `pub fn` and can be called directly. Confirm `PolyInputStep` is `pub` and importable as `hp41_core::ops::math1::modal::PolyInputStep`. Confirm the existing `numerical_accuracy.rs:6135` POLY-cluster block can call `dispatch(&mut s, Op::Roots)` directly — so our test file can mirror that pattern.</action>
  <verify>
    <automated>grep -c "pub fn submit_step\|pub fn op_roots\|pub fn op_poly_workflow" hp41-core/src/ops/math1/poly.rs</automated>
  </verify>
  <done>Reconnaissance written into a scratch note in the next task's `<action>` if any signature differs from the interfaces block above. Otherwise proceed to Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author hp41-core/tests/math1_poly_error_branches.rs with 10–14 error-branch tests</name>
  <read_first>
    - hp41-core/src/ops/math1/poly.rs (re-read — for assertion-target wording)
    - hp41-core/tests/math1_poly.rs (the analog test file — copy `make_state` + `set_reg` helpers verbatim; mirror file-header shape; mirror `// Catches:` discipline)
    - hp41-core/tests/math1_mod_entry_points.rs (mirror the `mod.rs`-style surgical-gap-closure header doc-comment; reference D-27.1 + D-32.x)
    - hp41-core/tests/lint_math1_assertions.rs (read the `line_is_forbidden_assert_eq` and `line_is_forbidden_manual_tolerance` heuristics — the new file MUST NOT trip either lint; use `approx::assert_relative_eq!` for any decimal comparisons)
  </read_first>
  <files>hp41-core/tests/math1_poly_error_branches.rs</files>
  <action>
    Create new file `hp41-core/tests/math1_poly_error_branches.rs` with this structure:

    1. **Lines 1–2** (verbatim — required by ADR-002 and the Free42 contamination guard allowlist):
       Line 1: `// Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979);`
       Line 2: `// Free42 source consulted only as sanity-check oracle, not copied.`
       Line 3: `//`
    2. **File-doc** (`//!` block, ~10–15 lines) referencing the QUAL-01 gap-closure scope, the v3.0.1-deferral reversal, and the targeted error-branch list. Cite D-27.1 (risk-weighted `// Catches:` discipline) and D-32.x. Wording template: "Plan 32-04 surgical gap-closure for `hp41-core/src/ops/math1/poly.rs` — closes the 76.37 % per-file coverage gap by exercising the documented-but-untested error arms in `bairstow_deflate` (POLY-07 non-convergence at lines 329 + 362), `solve_quadratic` / `find_roots` (degenerate-leading-coefficient at lines 232 / 240 / 262), `infer_degree` (degenerate all-zero at line 218), and `submit_step` (`Ready` rejection at line 526; out-of-range index at line 475)."
    3. **File-scope clippy-allow** (line ~16): `#![allow(clippy::unwrap_used)]`
    4. **Imports** — mirror math1_poly.rs (use the imports listed in the `<interfaces>` block above).
    5. **Helpers** — copy `make_state()` and `set_reg(state, idx, val: f64)` verbatim from math1_poly.rs (lines 23–30). Add a `set_all_zero_regs(state)` helper that calls `set_reg(state, i, 0.0)` for `i in 0..=5`.
    6. **Tests** — 10 mandatory tests, each carrying a `// Catches: <bug class>` doc comment (D-27.1) AND an OM citation or D-XX rationale where applicable:

       a) `infer_degree_all_zero_returns_domain` — `set_all_zero_regs(&mut s); let r = dispatch(&mut s, Op::Roots); assert!(matches!(r, Err(HpError::Domain)));` — covers poly.rs:218.

       b) `find_roots_constant_poly_returns_domain` — exercise the n=0 branch in `find_roots`. Read poly.rs::op_roots to confirm how `op_roots` slices the coefficient registers and ensure the slice passed to `find_roots` has length 1 (degree 0). Covers poly.rs:232.

       c) `linear_zero_leading_returns_domain` — exercise the linear `a.abs() < 1e-300` branch. Read poly.rs::op_roots to determine the register-slot setup that produces a degree-1 polynomial whose leading coefficient (after `find_roots`'s slice indexing) is zero. Covers poly.rs:239–240.

       d) `quadratic_zero_leading_returns_domain` — exercise the `solve_quadratic` degenerate-leading-coefficient branch at line 261–262. The setup needs `infer_degree` to return 2 but the first coefficient passed to `solve_quadratic` to be zero. Read poly.rs::op_roots and `find_roots(n=2)` to determine the correct register pattern (e.g., R03=2, R02=3, R01=0, R00=0 produces highest non-zero at index 3 so degree=3 — not what we want; the actual reachable pattern needs `infer_degree=2` AND `coeffs[0]=0` in the slice). If no real-world setup reaches this branch (because `infer_degree` always returns the index of the highest non-zero), document the unreachability and remove this test from the count (it would be dead defensive code). Adjust target count accordingly.

       e) `bairstow_poly07_p_overflow_returns_domain` — set degree-5 coefficients designed to make Bairstow diverge: e.g., R00=1, R01=1e9, R02=-1e9, R03=1e9, R04=-1e9, R05=1e9. Dispatch Op::Roots; assert `matches!(r, Err(HpError::Domain))`. If those specific coefficients converge instead, iterate the experiment: try R00=1, R05=-1e10, others = 0; OR R00=1, R01=1e15. The branch IS reachable per the existing POLY-D5 cases in numerical_accuracy.rs that sometimes return Err. Covers poly.rs:329–330 (POLY-07 p/q-overflow).

       f) `bairstow_poly07_residual_overflow_returns_domain` — set degree-3+ coefficients designed to make `r`/`s` residuals explode (different pattern from e; if e already covers both lines 329 and 362, mark this test with `// Catches: poly.rs:362 residual-overflow path explicitly` and use a different polynomial). Covers poly.rs:362.

       g) `submit_step_ready_returns_invalid_op` — `let r = submit_step(&mut s, PolyInputStep::Ready); assert_eq!(r, Err(HpError::InvalidOp));`. Covers poly.rs:526.

       h) `submit_step_coefficient_idx_out_of_range_returns_invalid_op` — call `submit_step(&mut s, PolyInputStep::CoefficientPrompt(5, 200))` — idx=200 exceeds `state.regs.len()` (normal CalcState carries 100 registers). Assert `Err(HpError::InvalidOp)` AND verify state was NOT mutated (regs unchanged). Covers poly.rs:488 (the `idx >= regs.len()` branch).

       i) `submit_step_degree_prompt_clamp_to_5` — set X=10 (above clamp ceiling) and call `submit_step(&mut s, PolyInputStep::DegreePrompt)`; assert R06 holds 5 (clamped) and `modal_program` advanced to `CoefficientPrompt(5, 0)`. Covers the `clamp(2,5)` upper-bound path at poly.rs:472.

       j) `submit_step_degree_prompt_clamp_to_2` — set X=1 (below clamp floor) and call `submit_step(&mut s, PolyInputStep::DegreePrompt)`; assert R06 holds 2 (clamped). Covers the lower-clamp branch.

    7. (Optional 11th–14th tests) — exercise additional underconstrained arms surfaced by Task 1 reconnaissance. Up to 4 more tests, each MUST carry `// Catches:` + serve a real coverage purpose (no padding per D-27.3).

    **Strict assertion discipline (per Pitfall 14 / 17 — `lint_math1_assertions.rs` will judge this file):**
    - For decimal/HpNum comparisons: use `approx::assert_relative_eq!(actual.to_f64().unwrap(), expected, max_relative = 1e-7)`.
    - For integer/error-variant equality: `assert_eq!` is fine (no `HpNum`/`Decimal`/`.to_f64()` on the line).
    - For error-variant matching: `assert!(matches!(r, Err(HpError::Domain)), "<message>");` (no decimal on the line — safe).
    - **No** `assert_eq!(x.inner(), y.inner())` patterns — that trips line_is_forbidden_assert_eq.
    - **No** `(a - b).abs() < EPSILON` patterns — that trips line_is_forbidden_manual_tolerance. If a manual-tolerance pattern is genuinely needed, add a preceding `// LINT-EXEMPT: <reason>` comment.

    Use `dispatch(&mut s, Op::Roots)` (NOT direct `op_roots(&mut s)`) for the path tests so the dispatch-chain exhaustive-match invariant is exercised end-to-end.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_poly_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_poly_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_poly_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>New test file exists, all tests pass, disclaim header present, `// Catches:` count >= 10, lint passes against the new file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Measure per-file coverage delta and confirm poly.rs reached >= 90 %</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md (the per-file coverage table at the top of the file)
  </read_first>
  <files>(no file writes — measurement only)</files>
  <action>
    Run `cargo llvm-cov --package hp41-core --summary-only` and inspect the `ops/math1/poly.rs` line. Verify:
    - `ops/math1/poly.rs` line coverage >= 90.0 % (currently 76.37 %).
    - `TOTAL` lines is >= 92.0 % (this single plan moves the workspace by ~0.5 pts; the >= 95 % gate closes after 32-04..32-08 all ship; Plan 32-10 is the final verification).

    If poly.rs is still below 90 %:
    - Re-read poly.rs to identify which sub-branches remain unreached.
    - Add 1–3 more tests targeting them.
    - Re-run the coverage measurement.
    - DO NOT lower the per-file floor — that would force a follow-up plan.

    If poly.rs is at >= 90 %, the plan is complete. Document the achieved per-file percentage in the SUMMARY.
  </action>
  <verify>
    <automated>cargo llvm-cov --package hp41-core --summary-only | grep "ops/math1/poly.rs"</automated>
  </verify>
  <done>cargo llvm-cov reports `ops/math1/poly.rs` >= 90 % lines. Per-file floor met for poly.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. No new code in `hp41-core/src/`. No new dependencies. No CI workflow changes. No script execution outside `cargo test`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-01 | Tampering | New test file's assertion discipline | mitigate | Every `// Catches:` comment names a specific bug class; assertions use `matches!(r, Err(HpError::Domain))` (specific) NOT `r.is_ok() \|\| r.is_err()` (tautological). Lint `lint_math1_assertions.rs` continues to gate against `assert_eq!(decimal, decimal)`. |
| T-32-04-02 | Information Disclosure | Free42 GPL contamination via copy-paste | mitigate | Verbatim disclaim header on lines 1–2 per ADR-002. Algorithm tests are pure call-and-assert against existing dispatch chain; no algorithm code copied from Free42 reference. `scripts/check-free42-contamination.sh` (in CI per Plan 32-03) verifies no contamination markers in `hp41-core/src/` — the test file lives in `hp41-core/tests/` which is OUT of the script's grep scope (safe by design). |
| T-32-04-03 | Repudiation | Test pads coverage without regression signal (D-27.3 violation) | mitigate | Each test targets a specific line range named in the `<error_branches_target_list>` table; no test passes by side effect or merely-executes-code. Every test asserts a specific error variant OR a specific state transition. |
</threat_model>

<verification>
- `cargo test -p hp41-core --test math1_poly_error_branches` passes with >= 10 tests
- `cargo test -p hp41-core --test lint_math1_assertions` continues to pass (new file does NOT trip Pitfall 14 / 17)
- `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/poly.rs` at >= 90.0 % lines
- `head -2 hp41-core/tests/math1_poly_error_branches.rs` shows the disclaim header
- `grep -c "// Catches:" hp41-core/tests/math1_poly_error_branches.rs` returns >= 10
- `bash scripts/check-free42-contamination.sh` continues to exit 0
</verification>

<success_criteria>
- `ops/math1/poly.rs` line coverage >= 90.0 % (up from 76.37 %) — primary measurable goal.
- New test file `hp41-core/tests/math1_poly_error_branches.rs` exists with >= 10 passing tests, each carrying `// Catches:` doc-comment.
- All frozen invariants preserved: no `hp41-core/src/` changes, no new runtime deps, `#![deny(clippy::unwrap_used)]` continues to apply, MSRV 1.88 unchanged.
- File-scope `#![allow(clippy::unwrap_used)]` present.
- Free42 disclaim header verbatim on lines 1–2.
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-04-SUMMARY.md` when done with:
- Per-task completion table
- Final poly.rs coverage percentage (achieved)
- Total `// Catches:` count
- List of error branches now exercised (line numbers + brief description)
- Any tests added beyond the 10 mandatory (with rationale)
</output>
