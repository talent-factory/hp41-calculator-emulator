---
plan_id: "32-02"
title: "numerical_accuracy.rs extension 566 -> ~700+ cases with OM citations"
phase: 32
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/numerical_accuracy.rs
autonomous: true
requirements_addressed: ["QUAL-02", "QUAL-06"]
threat_model_refs: []
---

<objective>
Extend `hp41-core/tests/numerical_accuracy.rs` from the v2.2 baseline of 566 cases to ~700+ cases by adding ~134 Math Pac I cases distributed across 11 families per D-32.9 risk-weighting, with each new case carrying an Owner's Manual page+example citation per D-27.7 (or an emulator-extension marker per D-28.3/4/5), and using `approx::assert_relative_eq!(max_relative = 1e-7)` discipline per Pitfall 14 (with one documented exception for the POLY multiplicity cluster per D-32.10).
</objective>

<must_haves>
- `hp41-core/tests/numerical_accuracy.rs` grows to ≥ 700 `case!()` invocations total (566 baseline + ~134 new); `cargo test -p hp41-core --test numerical_accuracy` reports `test result: ok` with combined pass rate ≥ 98.0 % per QUAL-02.
- The independent `baseline_passes >= 498` floor on the v1.x 503-case subset (lines 4313–4330) continues to pass per D-27.6 (NOT altered — case-IDs 1–503 stay unchanged).
- Every new case carries a `// Source: HP 00041-90034 p.<n>, ex.<m> — <description>` doc comment per D-27.7, OR an emulator-extension marker `// Source: D-28.3 emulator extension` for the `XEQ "REAL"` cases per D-32.9 family weighting.
- Every new case carries a `// Catches: <bug class>` doc comment per D-27.1 (e.g., "POLY multiplicity-as-cluster", "CMPLX (0,0) boundary", "INTG subdivision-cap").
- POLY multiplicity-as-cluster cases use the two-part assertion `mean(roots).re ≈ 1.0 ± 1e-4` AND `max(|roots[i].imag|) < 1e-3` per D-32.10 (carried as a non-`case!()` block with an inline `// POLY cluster assertion per D-32.10` rationale comment).
- INTG and SOLVE error-path cases (~3 each per D-32.11) assert `Err(HpError::Domain("DATA ERROR"))` and document the trigger condition (subdivision cap, non-convergence).
- All new cases use `approx::assert_relative_eq!(actual, expected, max_relative = 1e-7)` discipline UNLESS a documented exception applies (POLY cluster per D-32.10; INTG/SOLVE error-path which assert `Err(...)` not a numeric value).
</must_haves>

<threat_model>
No `high` severity threats — test-only phase, case-additions to an existing accuracy suite.

This plan does NOT modify the existing v1.x baseline assertions (case-IDs 1–503) — only appends new cases starting at the next available monotonic ID after the v3.0 Phase 28 first wave (ids 567+). The D-27.6 baseline-floor and `EXPECTED_BASELINE_FAILURES` assertions at lines 4313–4330 remain bit-for-bit untouched, preserving the independent regression-detection mechanism.
</threat_model>

<tasks>

<task id="32-02-01">
<action>
Read the current state of `hp41-core/tests/numerical_accuracy.rs` and confirm the integration points: (1) the `case!` macro declaration at lines 100–123 with its 4-arg + 5-arg `wide` overload form, (2) the v3.0 Math Pac I extension block starting at line 3287+ (hyperbolic cases — the per-family extension precedent), (3) the EXPECTED_BASELINE_FAILURES + baseline_passes assertions at lines 4313–4330 (D-27.6 floor — DO NOT alter), (4) the `get_x(state)` Decimal-to-f64 helper at line ~24 (Pitfall 14 bridge for `approx::assert_relative_eq!`). Also confirm `approx = "0.5.1"` is already in `hp41-core/Cargo.toml` `[dev-dependencies]` per RESEARCH.md HIGH-confidence correction (line 18) — Plan 32-02 has ZERO dep changes.

Determine the next available monotonic case ID by reading the `id += 1` increments through the file's tail; the new ~134 cases will be appended starting at that ID. Document the starting ID in the working notes; record the projected ID range for each of the 11 families per D-32.9.

This task is reconnaissance + planning. Do NOT yet add any cases. Token-thrift note: invoke as `rtk grep` for targeted searches; read targeted ranges with `Read offset/limit` rather than full-file reads.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/numerical_accuracy.rs (read offset=1, limit=130 for header + helpers + `case!` macro; offset=3280, limit=120 for the hyperbolic-extension precedent; offset=4290, limit=50 for the baseline-floor assertions)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/Cargo.toml (lines 1–40 — confirm `approx = "0.5.1"` is at line 18 of `[dev-dependencies]`)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"hp41-core/tests/numerical_accuracy.rs (MODIFIED ...)" — full pattern map with verbatim `case!` macro shape, the 5-line Math Pac I case pattern, the POLY cluster shape from D-32.10, the D-27.6 floor preservation rule)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-RESEARCH.md (§"Plan 32-02" lines 221–268 — distribution table, POLY cluster pattern, `approx 0.5.1` API surface)
</read_first>
<acceptance_criteria>
- Working notes capture: (a) current total `case!()` count in the file (should match the 566 v2.2 baseline plus any v3.0 Phase 28 additions); (b) the next available case ID for the first new Phase 32 case; (c) the projected ID ranges for each of the 11 D-32.9 families (POLY ~25 cases, CMPLX ~20, MAT ~18, INTG ~15, SOLVE ~15, DIFEQ ~12, HYP ~10, TRI ~8, FOUR ~6, TRANS ~3, REAL ~2).
- Confirmation in writing that `approx = "0.5.1"` is at `hp41-core/Cargo.toml` line 18 (no dep changes needed in any subsequent task).
- Confirmation in writing that the EXPECTED_BASELINE_FAILURES + baseline_passes assertions at lines 4313–4330 will NOT be modified by any subsequent task.
- Working notes record the pre-task `// Source: HP 00041-90034` count (`rtk grep -c "// Source: HP 00041-90034" hp41-core/tests/numerical_accuracy.rs`) and the pre-task `// Catches:` count (`rtk grep -c "// Catches:" hp41-core/tests/numerical_accuracy.rs`) as named baselines `SOURCE_BASELINE` and `CATCHES_BASELINE`. These values are the explicit reference points for the "≥ 70 increase" delta checks in tasks 32-02-02 and 32-02-03 — without them the delta assertion is ambiguous (V-04 follow-up).
</acceptance_criteria>
</task>

<task id="32-02-02">
<action>
Add the ~75 cases covering CMPLX (~20), MAT (~18), HYP (~10), TRI (~8), FOUR (~6), TRANS (~3), REAL (~2), and any DIFEQ headroom (~8 of the 12 DIFEQ cases here; the other ~4 stiff-system / 2nd-order ODE cases land in task 32-02-03 alongside the harder INTG/SOLVE families) per D-32.9 risk-weighting. Each case follows the exact `case!()` pattern from `32-PATTERNS.md` §"The Math Pac I case shape — copy this 5-line pattern verbatim" (Op::Sinh sinh(1) example at lines 3296–3308 of `numerical_accuracy.rs`):

```
{
    // Source: HP 00041-90034 p.<n>, ex.<m> — <description>
    // Free42 v3.0.5: <value> — agrees with OM    (optional cross-check)
    // Catches: <bug-class wording per planner's discretion>
    let mut s = CalcState::new();
    /* setup */
    dispatch(&mut s, Op::<Variant>).unwrap();
    case!(
        "<domain>",
        "<description with OM citation>",
        <expected_value>,
        get_x(&s)
    );
}
```

Per-family case selection (planner-chosen specific OM examples within the D-32.9 totals):
- **CMPLX (~20)**: Euler identity `e^(iπ) + 1 = 0` (split into real and imaginary parts), `Cinv` of (0,0) per Pitfall 6 (assert `Err(HpError::DivideByZero)`), `LnZ(0,0)` per Pitfall 6, `ExpZ(0,2π)` round-trip, `SinZ`/`CosZ` of `(0,1)` = `(0, sinh(1))`/`(cosh(1), 0)`, `Magz` of (3,4) = 5, `ApowZ(2, (0,π))` Euler-form, `LogZ(10)` = 1, `ZpowN((1,0), 5)` = (1,0), `Zpow1N((1,0), 3)` = (1,0), `ZpowW((1,0), (0,0))` per Pitfall 6 boundary. Cite the CMPLX sections of OM 00041-90034 (pp. 24–28 per Math Pac I structure). Use `math1_complex_functions.rs` as pattern reservoir.
- **MAT (~18)**: small 2×2 / 3×3 / 4×4 DET examples from OM pp. 10–15; SIMEQ with EPSILON threshold edge per ADR-003; INV of identity = identity (round-trip); INV of singular asserts `NO SOLUTION` modal_prompt; MatSize after INPUT phase. Cite OM page-and-example per case.
- **HYP (~10)**: domain guards `Acosh(0.5)` → `Err(HpError::Domain)`, `Atanh(1.0)` → `Err(HpError::Domain)`; identity `cosh²(x) - sinh²(x) = 1` for x ∈ {0.5, 1.0, 2.0}; `Tanh(0)` = 0; `Sinh(0)` = 0; `Cosh(0)` = 1. Many may deepen existing 9 cases at line 3287–3510; only ADD if a hardware behavior is not yet asserted.
- **TRI (~8)**: SSA ambiguous case = 0 / 1 / 2 solutions (3 cases — distinguish via the modal_prompt or output convention); SSS sanity (3-4-5 right triangle → 90° angle); ASA / SAA / SAS single-solution sanity. Cite OM TRI section.
- **FOUR (~6)**: orthogonality of sine + cosine basis (DFT of `[1,0,0,...]` → first cosine coeff = 1, all others = 0); rect↔polar conversion round-trip; `Op::Four` with USER-mode E-key evaluation at t = 0 returns the DC component. Cite OM FOUR section.
- **TRANS (~3)**: 2D translate-rotate-untranslate round-trip identity = original point; 3D Rodrigues rotation around z-axis by 90° (x,y,z) = (1,0,0) → (0,1,0); 3D round-trip identity = original point.
- **REAL (~2)**: `XEQ "REAL"` clears `complex_mode` (assert `state.complex_mode == false` post-dispatch); `XEQ "REAL"` is a no-op when `complex_mode` is already false (assert no panic, no stack mutation). Cite `// Source: D-28.3 emulator extension` (not in Math Pac I OM 1979 — derived requirement CMPLX-18 per the REQUIREMENTS table line 59).
- **DIFEQ partial (~8)**: RK4 1st-order ODE `y' = y` from x0=0, y0=1, step=0.1, target x=1 → y ≈ e ≈ 2.71828 (relative tolerance 1e-7 may need widening for accumulated RK4 error — use `max_relative = 1e-5` per family floor if needed, document the exception per-case); RK4 simple harmonic `y'' = -y` from x0=0, y0=1, y'0=0, target x=π/2 → y ≈ 0 (within 1e-3 — RK4 stiff floor, document); pendulum / simple oscillator examples from OM DIFEQ section. Cite OM pages.

Each case carries `// Source:` AND `// Catches:` comments per D-27.7 / D-27.1. Use the `approx` library for any non-`case!()` assertions (e.g., the `Err(HpError::DivideByZero)` checks use `assert!(matches!(result, Err(HpError::DivideByZero)))`).

The `case!()` macro auto-increments the monotonic ID and applies its internal `TOLERANCE = 1e-9` (the default for the macro). For cases that need looser tolerance (e.g., DIFEQ accumulated error), use the `case!(..., wide)` 5-arg form which applies `WIDE_TOL`. For cases that need `1e-7` specifically (the Math Pac I floor per Pitfall 14), use the 4-arg form — the default 1e-9 is tighter than 1e-7 so passing 1e-9 also passes 1e-7.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/numerical_accuracy.rs (offset=3287, limit=250 — read the v3.0 hyperbolic extension precedent in detail, especially the case structure, the OM citation pattern, the Free42 cross-check comment style)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_complex_functions.rs (read first 60 lines — pattern reservoir for CMPLX dispatch + extract patterns)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_matrix.rs (read first 80 lines + 200–250 — pattern reservoir for MAT cases incl. small-order DET / SIMEQ setup)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_four_tri_trans.rs (read first 80 lines — pattern reservoir for the combined FOUR / TRI / TRANS file conventions)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"Math Pac I case shape" and §"Shared Patterns" — the 3-line doc-comment header convention)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-RESEARCH.md (§"Plan 32-02" distribution per D-32.9 + §"approx 0.5.1 API surface" lines 270–289)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-CONTEXT.md (D-32.9 family weighting; D-28.3/4/5 emulator extension markers)
</read_first>
<acceptance_criteria>
- `cargo test -p hp41-core --test numerical_accuracy` reports `test result: ok` with combined pass rate ≥ 98.0 % (≥ ~615 of ~628 expected after this task's additions).
- `cargo test -p hp41-core --test numerical_accuracy 2>&1 | grep -c 'Source: HP 00041-90034'` increases by ≥ 70 vs the pre-task baseline (the ~75 new cases minus REAL's 2 D-28.3-cited cases, modulo lines counted vs cases counted).
- The `EXPECTED_BASELINE_FAILURES` + `baseline_passes >= 498` assertions at lines 4313–4330 continue to pass unchanged (`grep -n 'EXPECTED_BASELINE_FAILURES' hp41-core/tests/numerical_accuracy.rs` returns the same line numbers as before the task; no edits to that block).
- The 75 added cases each carry a `// Source:` comment AND a `// Catches:` comment (`grep -c '// Catches:' hp41-core/tests/numerical_accuracy.rs` increases by ≥ 70 vs pre-task baseline).
- The 2 REAL cases cite `D-28.3` (verify: `grep -c 'D-28.3' hp41-core/tests/numerical_accuracy.rs` ≥ 2).
- Hyperbolic domain-error cases (`Acosh(0.5)`, `Atanh(1.0)`) assert `Err(HpError::Domain(...))` and pass.
- `cargo test -p hp41-core --test lint_math1_assertions` continues to pass (the new assertions follow the `approx::assert_relative_eq!` discipline; manual-tolerance patterns are documented exceptions).
</acceptance_criteria>
</task>

<task id="32-02-03">
<action>
Add the remaining ~59 cases covering POLY (~25), INTG (~15), SOLVE (~15), and the residual DIFEQ headroom (~4 of the 12 DIFEQ cases — the stiff-system + 2nd-order ODE reduction cases) per D-32.9 risk-weighting. These four families carry the bulk of v3.0's bug-class density per RESEARCH.md (Pitfalls 2, 5, 6, 7) so they get the most attention.

**POLY (~25 cases):** Degree 2-5 OM examples from `hp41-core/tests/math1_poly.rs` p.30-32 references as starting point. Distribute:
- 5 degree-2 cases (quadratics with various discriminant signs — real roots, complex pair, double root)
- 5 degree-3 cases (cubic with 1 real + complex pair, cubic with 3 real, cubic with real triple root)
- 5 degree-4 cases (quartic with 4 real, quartic with 2 real + complex pair, biquadratic with z^2 substitution)
- 5 degree-5 cases (quintic with 5 real distinct roots, quintic with 1 real + 2 complex pairs, quintic with multiplicity-3 root + 2 simple roots)
- **MULTIPLICITY-CLUSTER CASE per D-32.10**: `(x-1)^5` polynomial (coefficients A=1, B=-5, C=10, D=-10, E=5, F=-1). This case uses a NON-`case!()` block per RESEARCH.md POLY cluster pattern — it asserts the two-part `mean(roots).re ≈ 1.0 ± 1e-4` AND `max(|roots[i].imag|) < 1e-3`. The block carries the dedicated `// POLY cluster assertion per D-32.10` comment. The shape is given in `32-PATTERNS.md` §"Non-`case!` POLY cluster assertion shape".
- 4 non-convergence cases per POLY-07: polynomials with `|imag| > 10⁹` during real-polynomial iteration — assert `Err(HpError::Domain("DATA ERROR"))`. Cite the POLY-07 requirement.

**INTG (~15 cases):** Distribute:
- 4 simple integrand cases (e.g., `f(x) = x²` on [0, 1] = 1/3; `f(x) = sin(x)` on [0, π] = 2; `f(x) = e^x` on [0, 1] = e - 1) with various subdivision counts. Cite OM INTG section.
- 4 Simpson-convergence cases tied to `DisplayMode` per ADR-004 (`threshold = 10^(-decimals - 1)`): set FIX 4 → convergence at 1e-5; FIX 9 → convergence at 1e-10. Verify the threshold is honored. Cite ADR-004.
- 4 discrete-mode (sample-based trapezoidal + Simpson, even-n check returning `N NOT EVEN` for odd n).
- **3 error-path cases per D-32.11**: subdivision-cap 2^15 (e.g., highly oscillatory `sin(1/x)` near 0 with explicit-mode `n = 100`) — assert `Err(HpError::Domain("DATA ERROR"))`. Each carries `// Catches: INTG subdivision cap 2^15 per INTG-07`.

**SOLVE (~15 cases):** Distribute:
- 4 well-conditioned single-root cases (e.g., `f(x) = x² - 2` near guesses 1 and 2 → √2 ≈ 1.41421356; `f(x) = sin(x) - 0.5` near guesses 0 and 1 → π/6).
- 4 multi-root cases where guess selection determines which root is found (cite OM SOLVE section on guess-dependent selection).
- 4 ROOT IS BETWEEN cases (where the modified secant brackets a sign change but doesn't converge within the 100-iter cap — assert the output convention from SOLV-04).
- **3 error-path cases per D-32.11**: non-convergence (e.g., `tan(x) - 10` with bad guesses far from the actual root; `f(x) = e^x` with no zero crossing on the guess interval) — assert either `NO ROOT FOUND` modal_prompt OR `Err(HpError::Domain("DATA ERROR"))` per the actual SOLV-04 behavior. Each carries `// Catches: SOLVE non-convergence per SOLV-04`.

**DIFEQ residual (~4 cases):** Stiff-system case (e.g., `y' = -100·y`, step=0.01, target x=1 → y ≈ e^(-100) ≈ 3.72e-44 — likely large RK4 accumulated error, document the tolerance widening); 2nd-order ODE reduction (e.g., `y'' = -y` from y0=1, y'0=0, target x=π/2 → y ≈ 0). Cite OM DIFEQ section.

All ~59 cases carry `// Source:` AND `// Catches:` comments. POLY cluster gets its dedicated `// POLY cluster assertion per D-32.10` comment in addition. Error-path cases use `assert!(matches!(result, Err(HpError::Domain(_))))` not `case!()` macro (no expected numeric value).

After landing, run `just coverage` to confirm the gate still holds and the per-`ops/math1/*.rs` files (especially `poly.rs`, `integ.rs`, `solve.rs`, `difeq.rs`) are now ≥ 90 %.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_poly.rs (read first 100 lines + the POLY-07 non-convergence section if present — pattern reservoir for POLY dispatch + extract_roots helper)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_integ.rs (read first 100 lines — pattern reservoir for INTG setup with user-program LBL + the subdivision-cap behavior reference)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_solve.rs (read first 100 lines — pattern reservoir for SOLVE guess setup + the modified-secant assertion shape)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_difeq.rs (read first 80 lines — pattern reservoir for DIFEQ RK4 setup)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"Non-`case!` POLY cluster assertion shape" — exact dual-assertion form for `(x-1)^5`; §"D-27.6 baseline-floor" — the lines NOT to touch)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-CONTEXT.md (D-32.10 strictness + D-32.11 error-path counts)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-RESEARCH.md (§"Plan 32-02" POLY cluster pseudo-code at lines 250–263)
- /Users/daniel/GitRepository/hp41-calculator-emulator/docs/adr/v3.0-003-inv-epsilon.md (MAT EPSILON edge cases — only if Plan 32-02 task 2 didn't already land them; cross-check)
- /Users/daniel/GitRepository/hp41-calculator-emulator/docs/adr/v3.0-004-intg-threshold.md (INTG convergence threshold tied to DisplayMode — Phase 32 cites this)
</read_first>
<acceptance_criteria>
- `cargo test -p hp41-core --test numerical_accuracy` reports `test result: ok` with combined pass rate ≥ 98.0 % (≥ ~686 of ~700 expected after this task's additions).
- Total `case!()` invocations in the file ≥ 700 (verify: `grep -c 'case!' hp41-core/tests/numerical_accuracy.rs` should be ≥ 700 + however many lines `case!` macro declaration spans — adjust the count if the macro definition itself contributes false matches).
- The POLY multiplicity-cluster block exists and contains both assertions: `(mean_re - 1.0).abs() < 1e-4` AND `max_imag < 1e-3` (verify: `grep -c 'POLY cluster assertion per D-32.10' hp41-core/tests/numerical_accuracy.rs` returns 1; `grep -c '1e-4' hp41-core/tests/numerical_accuracy.rs` ≥ 1 in cluster context; `grep -c '1e-3' hp41-core/tests/numerical_accuracy.rs` ≥ 1 in cluster context).
- INTG and SOLVE error-path cases (≥ 3 each per D-32.11) assert `Err(HpError::Domain("DATA ERROR"))` — verify by `grep -nE 'HpError::Domain\("DATA ERROR"\)' hp41-core/tests/numerical_accuracy.rs` returns ≥ 6.
- POLY non-convergence cases (≥ 4 per POLY-07) assert `Err(HpError::Domain(...))` — verify these are present in the POLY block.
- `EXPECTED_BASELINE_FAILURES` + `baseline_passes >= 498` assertions remain at lines 4313–4330 unchanged.
- `just coverage` reports `hp41-core` line coverage ≥ 95.0 % AND regions ≥ 93.0 % (the bulk of this plan's case additions sweep the residual gaps surfaced by Plan 32-01 task 32-01-01).
- `cargo test -p hp41-core --test lint_math1_assertions` continues to pass (note: this lint scopes only `tests/math1_*.rs`, NOT `numerical_accuracy.rs` — so it ignores the new cases regardless of their style, but the discipline is still followed manually).
</acceptance_criteria>
</task>

</tasks>

<verification>
After all 3 tasks land:

```bash
rtk cargo test -p hp41-core --test numerical_accuracy                            # PASS; combined ≥ 98 %; baseline_passes ≥ 498
rtk grep -c 'case!' hp41-core/tests/numerical_accuracy.rs                        # ≥ 700
rtk grep -c '// Source: HP 00041-90034' hp41-core/tests/numerical_accuracy.rs    # ≥ 700 (each new case has at least one)
rtk grep -c '// Catches:' hp41-core/tests/numerical_accuracy.rs                  # ≥ 134 new entries
rtk grep -c 'POLY cluster assertion per D-32.10' hp41-core/tests/numerical_accuracy.rs   # = 1
rtk grep -nE 'HpError::Domain\("DATA ERROR"\)' hp41-core/tests/numerical_accuracy.rs     # ≥ 6 (3 INTG + 3 SOLVE per D-32.11)
rtk just coverage                                                                # lines ≥ 95.0 %, regions ≥ 93.0 %
```

The D-27.6 floor block at lines 4313–4330 must remain untouched — verify the literal `baseline_passes >= 498` and `EXPECTED_BASELINE_FAILURES` declarations are intact via `grep -nE 'baseline_passes >= 498|EXPECTED_BASELINE_FAILURES' hp41-core/tests/numerical_accuracy.rs`.
</verification>
</content>
</invoke>