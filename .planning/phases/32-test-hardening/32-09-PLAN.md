---
phase: 32
plan: "32-09"
type: execute
wave: 2
depends_on:
  - 32-04
  - 32-05
  - 32-06
  - 32-07
  - 32-08
files_modified:
  - hp41-core/tests/numerical_accuracy.rs
  - scripts/check-free42-contamination.sh
  - hp41-core/tests/lint_math1_assertions.rs
  - hp41-core/tests/math1_op_test_count.rs
  - hp41-gui/e2e/smoke.spec.js
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06
gap_closure: true
tags:
  - cleanup
  - cr-01
  - wr-01
  - wr-02
  - wr-03
  - wr-04
  - wr-05
  - wr-06
  - wr-07
must_haves:
  truths:
    - "CR-01 tautological cases at numerical_accuracy.rs lines 5867,5885,5903,5921,5944,5962,5980,5999,6018,6041,6060,6078,6097,6116,6266 are EITHER replaced with meaningful regression sentinels OR deleted. Either path is acceptable per the gap_closure_directive CR-01 fix recommendation."
    - "WR-01: scripts/check-free42-contamination.sh has an explicit `[[ ! -d $MATH1_DIR ]]` existence check that exits non-zero with FAIL message if the directory is missing."
    - "WR-02: lint_math1_assertions.rs heuristic extended to detect multi-line `assert_eq!(decimal, decimal)` invocations OR has an explicit documentation block in the file-level `//!` declaring the multi-line blind spot as known."
    - "WR-03: math1_op_test_count.rs uses word-boundary matching (not naked `contains`) so `Sol` does not match `Solve`. Preflight (Task 3a) confirms all 45 variants still meet >= 5 test mentions BEFORE the heuristic is swapped (Task 3b); if preflight fails, a hard STOP surfaces to the user."
    - "WR-04: 7 redundant sentinel cases at numerical_accuracy.rs lines 4344-4346, 4361-4363, 4962-4964, 4980-4982, 5680-5682, 6180-6182, 6803-6805 deleted (real `assert!` already protects the contract)."
    - "WR-05: smoke.spec.js `browser.pause(N)` time-based waits replaced with predicate-driven `browser.waitUntil(...)` patterns (or have an explicit comment justifying why the pause is necessary)."
    - "WR-06: smoke.spec.js has a `beforeEach` hook that resets CalcState between `it()` blocks."
    - "WR-07: smoke.spec.js error extraction logic mirrors `extractErrMessage` helper pattern (handles falsy non-Error values cleanly)."
    - "All existing QUAL-01..08 gates continue to pass after the cleanup. The widened lint from Task 2 validates against the FULL post-32-04..32-08 test corpus (this plan is Wave 2 to encode that ordering constraint)."
  artifacts:
    - path: "hp41-core/tests/numerical_accuracy.rs"
      provides: "Cleaned-up POLY block + redundant sentinel removal"
      contains: "case!\\(.*matches!|matches!"
    - path: "scripts/check-free42-contamination.sh"
      provides: "Existence-checked Free42 contamination guard"
      contains: "\\[\\[ ! -d"
    - path: "hp41-core/tests/lint_math1_assertions.rs"
      provides: "Multi-line assert_eq blind spot closed or documented"
      contains: "multi-line"
    - path: "hp41-core/tests/math1_op_test_count.rs"
      provides: "Word-boundary mention counting"
      contains: "fn line_mentions_variant|word.boundary"
    - path: "hp41-gui/e2e/smoke.spec.js"
      provides: "Predicate-driven E2E + beforeEach reset + extractErrMessage shape"
      contains: "waitUntil|beforeEach"
  key_links:
    - from: "numerical_accuracy.rs cleanup"
      to: "QUAL-02 gate"
      via: "case!() denominator reduced; gate still passes >= 98 %"
      pattern: "passes >= ceiling"
    - from: "smoke.spec.js cleanup"
      to: "ci-gui.yml e2e-linux job"
      via: "WebDriver assertions stay green under mochaOpts.retries: 1"
      pattern: "describe\\|it\\("
    - from: "Task 2 lint widening (multi-line assert_eq detection)"
      to: "Wave-1 plans 32-04..32-08 new test files"
      via: "Wave-2 ordering — widened lint validates against the FULL final corpus, not a mid-run snapshot"
      pattern: "wave: 2 + depends_on: 32-04..32-08"
---

<objective>
Land the seven outstanding code-review findings from `.planning/phases/32-test-hardening/32-REVIEW.md` (CR-01 + WR-01..07) as a bundled cleanup. The CR-01 critical and WR-01..04 warnings live in test/lint/script files; WR-05..07 live in the E2E smoke spec. None requires touching `hp41-core/src/`.

Purpose: The cleanup is gap_closure_directive secondary scope. Per the directive, bundling these into 1–2 plans is recommended because:
1. CR-01 fixes the T-32-04 meta-test-gaming threat that Plan 32-02 introduced.
2. WR-01 closes a Free42 contamination guard silent-pass bug (high-leverage, one-line fix).
3. WR-02..04 close audit-trail blind spots that future-contributor drift could exploit.
4. WR-05..07 reduce E2E flake risk and clarify error handling.

**Wave-2 sequencing:** 32-09 runs AFTER 32-04..32-08 ship (Wave 2 in the gap-closure run) because Task 2 widens `lint_math1_assertions.rs` to detect multi-line `assert_eq!(decimal, decimal)`. Plans 32-04..32-08 each create new `math1_*_error_branches.rs` files that the lint scans. Wave-1 parallel execution offers no guarantee the widened lint validates against all new test files. Moving 32-09 to Wave 2 encodes the constraint and lets the widened lint validate the full final corpus.

Output: 5 modified files. No new test files. No `hp41-core/src/` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md
@.planning/phases/32-test-hardening/32-REVIEW.md

# Files being modified
@hp41-core/tests/numerical_accuracy.rs
@scripts/check-free42-contamination.sh
@hp41-core/tests/lint_math1_assertions.rs
@hp41-core/tests/math1_op_test_count.rs
@hp41-gui/e2e/smoke.spec.js

# Existing patterns for reference
@hp41-gui/src/App.tsx

# Wave-1 outputs that this plan validates against
@.planning/phases/32-test-hardening/32-04-SUMMARY.md
@.planning/phases/32-test-hardening/32-05-SUMMARY.md
@.planning/phases/32-test-hardening/32-06-SUMMARY.md
@.planning/phases/32-test-hardening/32-07-SUMMARY.md
@.planning/phases/32-test-hardening/32-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WR-01 — Add MATH1_DIR existence check to check-free42-contamination.sh</name>
  <read_first>
    - scripts/check-free42-contamination.sh (current 27-line shape)
    - 32-REVIEW.md WR-01 finding (fix block at lines 89–98 of the review)
  </read_first>
  <files>scripts/check-free42-contamination.sh</files>
  <action>
    Edit `scripts/check-free42-contamination.sh` to insert an explicit existence check after `set -euo pipefail` and the MATH1_DIR / DISCLAIM_LINE variable declarations. Insert these 4 lines BEFORE the `PATTERN=` declaration:

    ```bash
    if [[ ! -d "$MATH1_DIR" ]]; then
        echo "FAIL: $MATH1_DIR does not exist — license guard cannot run." >&2
        exit 2
    fi
    ```

    The script's exit-code semantics become:
    - 0: clean (no contamination)
    - 1: contamination detected
    - 2: directory missing (new failure mode)

    Verify the existing `bash scripts/check-free42-contamination.sh` continues to exit 0 against the current codebase (the directory exists today).

    Verify the new path: copy the script to a temp directory that does NOT contain the `hp41-core/src/ops/math1` relative path (so `MATH1_DIR` cannot be resolved from the script's resolved cwd), run it, and expect exit 2 + "FAIL: ... does not exist" stderr. The verify block below uses a `$REPO` capture (not `$OLDPWD`) to avoid the cwd-drift bug noted in the original WR-04 review.
  </action>
  <verify>
    <automated>grep -F "[[ ! -d \"\$MATH1_DIR\" ]]" scripts/check-free42-contamination.sh</automated>
    <automated>bash scripts/check-free42-contamination.sh; echo "exit=$?"</automated>
    <automated>REPO=$(pwd); mkdir -p /tmp/test-fake-cwd-32-09; cp "$REPO/scripts/check-free42-contamination.sh" /tmp/test-fake-cwd-32-09/run.sh; (cd /tmp/test-fake-cwd-32-09 && bash run.sh; echo "exit-when-missing=$?")</automated>
  </verify>
  <done>Script has `[[ ! -d "$MATH1_DIR" ]]` existence check at top. Normal run exits 0. Run-from-wrong-cwd (the `/tmp/test-fake-cwd-32-09` harness) emits `exit-when-missing=2` (not 0 — silent-pass bug closed).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-02 — Close lint_math1_assertions.rs multi-line blind spot</name>
  <read_first>
    - hp41-core/tests/lint_math1_assertions.rs (lines 161–183 — `line_is_forbidden_assert_eq` heuristic)
    - 32-REVIEW.md WR-02 finding (fix block at lines 128–134)
    - All Wave-1 new test files (math1_poly_error_branches.rs, math1_trans_error_branches.rs, math1_four_error_branches.rs, math1_solve_error_branches.rs, math1_difeq_error_branches.rs, math1_matrix_error_branches.rs, math1_mod_extra_coverage.rs, math1_integ_error_branches.rs) — these are now part of the corpus the widened lint must accept
  </read_first>
  <files>hp41-core/tests/lint_math1_assertions.rs</files>
  <action>
    Choose ONE of the three fix options from 32-REVIEW.md WR-02:

    **Option (c) - minimal change (recommended)**: extend `line_is_forbidden_assert_eq` to scan the next 3 lines after a bare `assert_eq!` for the decimal-token markers. Modify the function so that when it sees `assert_eq!` on a line, it joins that line with up to 3 following lines and checks the joined string for `.to_f64()` / `HpNum` / `Decimal` / `.inner()`. If found, flag the original line as forbidden.

    Implementation sketch:
    ```rust
    fn line_is_forbidden_assert_eq(line: &str, lines: &[&str], idx: usize) -> bool {
        if line.contains(LINT_EXEMPT_TOKEN) {
            return false;
        }
        let trimmed = line.trim_start();
        if trimmed.starts_with("//") {
            return false;
        }
        if !line.contains("assert_eq!") {
            return false;
        }
        // Check the line itself AND up to 3 following lines (multi-line invocation)
        let next_3: String = lines.iter()
            .skip(idx)
            .take(4)  // current + 3 following
            .copied()
            .collect::<Vec<_>>()
            .join("\n");
        let is_decimal = next_3.contains(".to_f64()")
            || next_3.contains("HpNum")
            || next_3.contains("Decimal")
            || next_3.contains(".inner()");
        if !is_decimal {
            return false;
        }
        if preceding_block_has_lint_exempt(lines, idx) {
            return false;
        }
        true
    }
    ```

    Update the file-level `//!` block to document the new behavior: "Both single-line and multi-line `assert_eq!(decimal, decimal)` invocations are detected (lookahead window: 3 lines after the `assert_eq!` token)." Remove or update any prior documentation that claimed only single-line was detected.

    **Verify the new heuristic does NOT trip on the existing corpus** — which now INCLUDES the Wave-1 new test files. The lint must pass cleanly against the FULL post-32-04..32-08 corpus. Specifically:
    - The multi-line `assert_eq!` patterns in `math1_poly.rs` lines 80–86 (`assert_eq!(s.stack.x.inner(), x_before, ...)`) WILL trip the new lint because they contain `.inner()` on a following line. Decision: add `// LINT-EXEMPT: integer-equality preserves stack via Decimal copy` comment OR refactor to `assert_eq!(s.stack.x, stack_clone)` (compares HpNum directly, no `.inner()`).
    - The Wave-1 new test files (math1_*_error_branches.rs) follow the `approx::assert_relative_eq!(..., max_relative = 1e-7)` discipline per Plan 32-04..32-07 plan instructions — they should not trip the new lint. If any do, that's a Wave-1 plan instruction that wasn't followed; surface to the user as a follow-up.

    Wave-2 ordering is the key safeguard here: by running 32-09 AFTER 32-04..32-08 ship, the widened lint validates against the FULL final corpus, not a mid-run snapshot.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
    <automated>cargo test -p hp41-core --tests</automated>
    <automated>grep -F "multi-line" hp41-core/tests/lint_math1_assertions.rs</automated>
  </verify>
  <done>Lint heuristic detects multi-line `assert_eq!(decimal, decimal)`. All existing tests pass against the FULL post-32-04..32-08 corpus (any pre-existing offenders carry LINT-EXEMPT annotations; Wave-1 new files clean by `approx::assert_relative_eq!` discipline). File-level docstring documents the multi-line behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3a: WR-03 PREFLIGHT — measure word-boundary counts against current corpus BEFORE swapping the heuristic</name>
  <read_first>
    - hp41-core/tests/math1_op_test_count.rs (lines 75–110 — `count_test_mentions`)
    - 32-REVIEW.md WR-03 finding (fix block at lines 144–168)
    - All Wave-1 new test files (the new corpus the gate now scans)
  </read_first>
  <files>(no file writes in Task 3a — measurement only; the actual heuristic swap happens in Task 3b)</files>
  <action>
    **Preflight contingency** per the gap_closure_directive WR-03 fix recommendation (Option b — restructure to preflight + swap):

    1. Write a STANDALONE measurement script (or use a single shell invocation) that applies the proposed word-boundary counting logic to the CURRENT `hp41-core/tests/math1_*.rs` corpus (including the Wave-1 new files added by plans 32-04..32-08) WITHOUT yet modifying `math1_op_test_count.rs`. The logic mirrors the proposed Task 3b sketch:

       - For each of the 45 Math Pac I `Op` variants, search all `hp41-core/tests/math1_*.rs` files for the pattern `Op::<VariantName>` where the character immediately following the variant name is NOT `[A-Za-z0-9_]` (word-boundary check). Count distinct `#[test]` blocks whose body contains at least one such match.

       Example shell idiom:

       ```bash
       # For each variant in a hardcoded list, count word-bounded mentions per test function.
       # Pseudocode — implement in Rust within math1_op_test_count.rs's existing harness OR as a standalone scratch script.
       cargo run --example op_test_count_preflight  # (if implemented as an example)
       # OR inline via the existing test, with a debug-print mode that does not yet enforce the gate.
       ```

       Simpler alternative: temporarily duplicate `math1_op_test_count.rs::count_test_mentions` as `count_test_mentions_v2_word_boundary` in a new test function (NOT marked `#[test]`, called as a debug helper), have it report per-variant counts to stdout, then `cargo test -p hp41-core --test math1_op_test_count -- --nocapture`.

    2. Read the per-variant counts emitted.

    3. **GATE**: if any of the 45 Math Pac I `Op` variants reports < 5 word-boundary mentions on the CURRENT corpus, **STOP**. Surface a structured report to the user listing:
       - Which variant(s) drop below 5 under word-boundary counting.
       - Which test files would need additional tests to restore the floor.
       - A recommendation: author a follow-up plan (e.g., 32-09.1) that adds the needed tests BEFORE this plan's Task 3b ships.

       The plan's `files_modified` deliberately does NOT include `math1_<area>.rs` files — the structural decision per the gap_closure_directive (Option b) is that Task 3b's heuristic swap is gated on Task 3a's preflight, and any insufficiency surfaces as a STOP rather than as a silent corpus mutation in this plan.

       Acceptable alternative: if exactly one variant drops below 5 AND the missing test is a trivial single-test addition (e.g., one more `Op::TriSaa` reference in `math1_four_tri_trans.rs`), AMEND this plan in revision-mode to add that file to `files_modified` and add the test in Task 3a's body. Anything beyond a trivial single-test addition warrants a follow-up plan, not an in-place plan amendment.

    4. If ALL 45 variants meet >= 5 word-boundary mentions, Task 3b can run safely. Document the per-variant table in the SUMMARY.

    **Established floor per RESEARCH §"Per-Op Test Count Audit":** TriSaa=6, TriSas=6, and a handful at exactly 5. The Wave-1 new test files added in 32-04..32-08 add mentions to (at minimum) the variants targeted by error-branch tests, which only RAISES the floor. The most likely "drop below 5" candidate is a variant whose previous count was inflated by the substring-matching bug (e.g., `Op::Sol` borrowing mentions from `Solve` references) — Task 3a's preflight surfaces that.
  </action>
  <verify>
    <automated>echo "Task 3a is a preflight measurement; verify the per-variant table is captured in the executor's stdout or scratch file before proceeding to Task 3b. The gate is enforced by the explicit STOP in the action body — there is no automated cargo test for Task 3a alone (the heuristic has not been swapped yet)."</automated>
  </verify>
  <done>Per-variant word-boundary count table captured for all 45 Math Pac I `Op` variants. EITHER all 45 meet >= 5 (Task 3b proceeds) OR a structured STOP surfaced with the deficient variant list and a recommendation to author a follow-up plan (Task 3b does NOT run in this plan).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3b: WR-03 SWAP — Word-boundary mention counting in math1_op_test_count.rs (runs only if Task 3a's gate passes)</name>
  <read_first>
    - hp41-core/tests/math1_op_test_count.rs (lines 75–110 — `count_test_mentions`)
    - 32-REVIEW.md WR-03 finding (fix block at lines 144–168)
    - Task 3a's preflight output (per-variant count table)
  </read_first>
  <files>hp41-core/tests/math1_op_test_count.rs</files>
  <action>
    **Precondition:** Task 3a's preflight gate passed (all 45 Math Pac I `Op` variants meet >= 5 word-boundary mentions on the current corpus). If Task 3a surfaced a STOP, this task does NOT run.

    Replace `count_test_mentions` with a word-boundary version that:
    1. Tokenizes the search for `Op::VariantName` (uses prefix `Op::` to anchor).
    2. After matching `Op::VariantName`, verifies the following character is NOT alphanumeric or underscore (so `Op::Sol` does not match `Op::Solve`).
    3. Tracks `#[test]` -> closing `}` blocks, counts test functions whose body mentions the variant (per the docstring's promise).

    Use the implementation sketch from 32-REVIEW.md WR-03 fix block (lines 145–168 of the review). Add a `// Catches:` comment to the modified `each_math1_op_has_at_least_5_tests` test naming the WR-03 bug class: "Catches: WR-03 substring inflation (`Sol` matching `Solve`) - now uses word-boundary matching".

    Update the file-level docstring to reflect the new semantics: "Counts test FUNCTIONS (`#[test] fn ...` blocks whose body mentions the Op variant), not bare substring occurrences."

    Re-run the test gate after the swap. Per Task 3a's preflight, all 45 variants should still meet >= 5 — the swap is non-functional from a gate perspective; it tightens the audit semantics without changing the pass/fail outcome.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_op_test_count</automated>
    <automated>grep -F "word-boundary\|word_boundary\|line_mentions_variant" hp41-core/tests/math1_op_test_count.rs</automated>
  </verify>
  <done>Gate uses word-boundary matching. All 45 Math Pac I variants still meet the >= 5 threshold (test passes). File-level docstring updated. The `each_math1_op_has_at_least_5_tests` test carries a `// Catches: WR-03 substring inflation` doc-comment.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: CR-01 + WR-04 — Replace tautological cases and delete redundant sentinels in numerical_accuracy.rs</name>
  <read_first>
    - hp41-core/tests/numerical_accuracy.rs (lines 5840–6120 — the CR-01 POLY block; lines 4344-4346, 4361-4363, 4962-4964, 4980-4982, 5680-5682, 6180-6182, 6803-6805 — the WR-04 redundant sentinels)
    - 32-REVIEW.md CR-01 (lines 43–76) + WR-04 (lines 170–178)
  </read_first>
  <files>hp41-core/tests/numerical_accuracy.rs</files>
  <action>
    **CR-01 (15 tautological `r.is_ok() || r.is_err()` cases):**

    For each of the 15 cases at lines 5867, 5885, 5903, 5921, 5944, 5962, 5980, 5999, 6018, 6041, 6060, 6078, 6097, 6116, 6266:

    Replace the tautological predicate `if r.is_ok() || r.is_err() { 1.0 } else { 0.0 }` with EITHER:

    **(a) A meaningful regression sentinel**: tighten the predicate to match a specific enumerated outcome:
    ```rust
    if r.is_ok() || matches!(r, Err(hp41_core::HpError::Domain)) { 1.0 } else { 0.0 }
    ```
    This matches line 5845's pattern (`Ok || Err(Domain)`) and would catch `Err(Overflow)`, `Err(CallDepth)`, `Err(InvalidOp)`, or a dispatcher panic — which the tautology cannot. Use this form for the cases where Bairstow legitimately may return either Ok (convergence) or Err(Domain) (POLY-07 reject).

    **OR**

    **(b) Delete the case** if the contract is genuinely unverifiable from the dispatch path (e.g., the polynomial behavior is implementation-dependent and either branch is acceptable). A deleted case cannot game the 98 % gate.

    **Decision rule**: prefer (a) for polynomials with a documented OM example (the 15 cases all carry OM page references at p.31 and p.32); use (b) only if the polynomial has no specific OM-cited expected behavior. Apply (a) to ALL 15 cases unless one specifically has no expectations.

    **WR-04 (7 redundant sentinel cases):**

    For each of the 7 sentinel cases at lines 4344-4346, 4361-4363, 4962-4964, 4980-4982, 5680-5682, 6180-6182, 6803-6805:

    DELETE the `case!("name", "desc", 1.0, 1.0)` invocation (3 lines per location). The preceding `assert!` already protects the contract — the sentinel is pure denominator inflation.

    **Re-run the gate after the changes**:
    - Total `case!()` count drops from 768 to (768 - 7) = 761 (WR-04 deletions).
    - 15 CR-01 cases now use `Ok || Err(Domain)` predicate (no count change for those).
    - The 98 % gate still passes (763 - 7 - up-to-15 if any of the 15 fail under the tightened predicate). If any fail, that's a REAL regression the new predicate caught — DO NOT loosen back to tautology. Either delete those specific cases (option b) or investigate whether the dispatch path is producing an unexpected error variant.
    - The `baseline_passes >= 498` floor (D-27.6) is UNAFFECTED (the 15+7 changes are all in the v3.0 extension range, ids > 503).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test numerical_accuracy</automated>
    <automated>grep -c "if r.is_ok() || r.is_err()" hp41-core/tests/numerical_accuracy.rs</automated>
    <automated>grep -c "case!(\"[^\"]*\", \"[^\"]*\", 1.0, 1.0)" hp41-core/tests/numerical_accuracy.rs</automated>
  </verify>
  <done>0 tautological `r.is_ok() || r.is_err()` predicates (count = 0). 0 redundant `case!(name, desc, 1.0, 1.0)` sentinels. `numerical_accuracy` test continues to pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: WR-05 + WR-06 + WR-07 — E2E smoke.spec.js cleanup</name>
  <read_first>
    - hp41-gui/e2e/smoke.spec.js (entire file, ~245 lines)
    - 32-REVIEW.md WR-05 (lines 180–186), WR-06 (lines 190–197), WR-07 (lines 199–210)
    - hp41-gui/src/App.tsx (the `extractErrMessage` helper — locate via grep; pattern is the source-of-truth for WR-07's fix)
  </read_first>
  <files>hp41-gui/e2e/smoke.spec.js</files>
  <action>
    Apply three cleanups to `hp41-gui/e2e/smoke.spec.js`:

    **WR-05: Replace `browser.pause(N)` with `browser.waitUntil`**.

    For each of the 9 `browser.pause(N)` calls (lines 110, 159, 202, 209, 215, 218, 221, 224, 230):

    Replace with `browser.waitUntil(async () => (await display.getAttribute('data-text')) === expectedText, { timeout: 5000, timeoutMsg: '<describe-step>' })` where `expectedText` is the LCD output expected AFTER the click sequence completes.

    For pauses BETWEEN clicks (mid-sequence, before the final display read), use `await browser.waitUntil(async () => { const text = await display.getAttribute('data-text'); return text !== priorText; }, { timeout: 5000 })` to wait for ANY display change. Capture `priorText` BEFORE the click.

    Cases where a pause is genuinely needed (e.g., waiting for an animation to complete with no observable side-effect): add an explicit comment `// HOLD: <reason>` and keep `browser.pause` — but examine each case carefully; for IPC round-trips a predicate-driven wait is correct.

    **WR-06: Add `beforeEach` state reset**.

    Insert a `beforeEach(async () => { ... })` block at the top of the existing `describe(...)` block. The block should:
    1. Read the current `[data-testid="lcd-display"]` element.
    2. Dispatch a `CLX` operation via the existing `clickKey('clx_or_a')` (or `clickKey('clx')` — check which key id exists in `Keyboard.tsx`).
    3. Wait briefly for the state to flush (use `browser.waitUntil` for confirmation).
    4. Reset the program (dispatch `Op::Clp` if reachable, or skip if not needed for the 3 current it() blocks).

    Cleaner alternative: invoke a backend reset directly via `browser.execute(() => window.__TAURI_INTERNALS__.invoke('dispatch_op', { keyId: 'on' }))` — the `ON` key clears the calculator state (verify in `Keyboard.tsx` / `key_map.rs`). If no clean reset op exists, the minimum acceptable is `clickKey('clx')` + `clickKey('clst')` (clear stack).

    Document the choice in a leading comment on the beforeEach block.

    **WR-07: Tighten error extraction**.

    Find the existing `String(err && err.message ? err.message : err)` (line ~84) and replace with the `extractErrMessage`-style helper from `App.tsx`. Define a `function extractErrMessage(err) { ... }` near the top of the spec file (mirroring App.tsx's helper bit-for-bit), then call it at line 84 (and any other places that handle err). The helper should handle: object-with-message, plain string, null/undefined, falsy non-Error (0, "", false).

    Implementation pattern (copy from App.tsx):
    ```js
    function extractErrMessage(err) {
        if (err && typeof err === 'object' && typeof err.message === 'string') {
            return err.message;
        }
        if (typeof err === 'string') return err;
        if (err === null || err === undefined) return String(err);
        try {
            return JSON.stringify(err);
        } catch {
            return String(err);
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "browser\.pause(" hp41-gui/e2e/smoke.spec.js</automated>
    <automated>grep -c "browser\.waitUntil(" hp41-gui/e2e/smoke.spec.js</automated>
    <automated>grep -c "beforeEach" hp41-gui/e2e/smoke.spec.js</automated>
    <automated>grep -c "extractErrMessage" hp41-gui/e2e/smoke.spec.js</automated>
  </verify>
  <done>`browser.pause` count <= 2 (any remaining are explicitly commented HOLDs). `browser.waitUntil` count >= 9. `beforeEach` block present. `extractErrMessage` helper defined and used. Cannot run `just gui-e2e` locally (per Plan 32-03 SUMMARY note — webkit2gtk-driver / xvfb not installed); CI will validate on next push.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Verify all preceding QUAL-* gates still pass after cleanup</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md (the QUAL-01..08 verification table)
  </read_first>
  <files>(no file writes)</files>
  <action>
    Run the full hp41-core test suite + coverage gate to confirm the cleanup did not regress any QUAL gate:
    - `cargo test -p hp41-core --tests` — all 1627+ tests pass.
    - `cargo test -p hp41-core --test numerical_accuracy` — combined gate >= 98 % AND baseline_passes >= 498.
    - `cargo test -p hp41-core --test lint_math1_assertions` — both `no_decimal_assert_eq` and `no_manual_tolerance` pass after WR-02 widening.
    - `cargo test -p hp41-core --test math1_op_test_count` — gate passes after WR-03 word-boundary tightening (all 45 variants >= 5).
    - `cargo test -p hp41-core --test xrom_shadowing` — still passes.
    - `cargo test -p hp41-core --test math1_user_callback` — still passes.
    - `bash scripts/check-free42-contamination.sh` — exits 0 (WR-01 existence check + no contamination).
    - `cargo llvm-cov --package hp41-core --summary-only | tail -5` — workspace lines >= the post-32-04..32-08 baseline (this plan does not regress coverage; the 15 CR-01 changes and 7 WR-04 deletions remove 22 case! invocations from numerical_accuracy.rs but those are TEST CODE so source-coverage is unaffected).

    If any gate fails, identify the cause and fix BEFORE committing.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --tests 2>&1 | tail -3</automated>
    <automated>bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <done>All hp41-core tests pass. QUAL-02..QUAL-08 gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/check-free42-contamination.sh` | Bash CLI gate — runs in `just ci` and `.github/workflows/ci.yml`. New exit-2 code on missing directory adds a new behavior — verify CI doesn't break. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09-01 | Tampering | CR-01 tautology replacement weakens the test suite | mitigate | Each tautology is replaced with `matches!(Ok | Err(Domain))` predicate — strictly tighter than `is_ok() || is_err()`. Cases where the new predicate fails reveal a REAL bug (not a regression in the test). |
| T-32-09-02 | Information Disclosure | WR-01 fix accidentally introduces a path that exposes filesystem layout in CI error messages | accept | Error message format is `"FAIL: <MATH1_DIR> does not exist"` where MATH1_DIR is a hardcoded relative path. No untrusted-input interpolation. |
| T-32-09-03 | DoS | E2E test predicate-driven waits create infinite hangs on broken builds | mitigate | Every `browser.waitUntil` has explicit `{ timeout: 5000, timeoutMsg: '...' }` — test fails at 5 seconds rather than hanging. |
| T-32-09-04 | Tampering | WR-02 lint widening may flag legitimate multi-line assertions that pre-existed Plan 32-09 OR were added by Wave-1 plans 32-04..32-08 | mitigate | Wave-2 ordering means the widened lint validates against the FULL final corpus. Any legitimate offenders are annotated with `// LINT-EXEMPT: <reason>` per the established pattern. Wave-1 plans 32-04..32-07 explicitly instruct executors to use `approx::assert_relative_eq!` for decimal compares — those new files are clean by construction. |
| T-32-09-05 | Repudiation | WR-03 word-boundary tightening lowers an existing variant below the >= 5 gate without warning | mitigate | Task 3a preflight measures the per-variant word-boundary counts BEFORE Task 3b swaps the heuristic. If any variant drops below 5, Task 3a STOPS and surfaces a structured report; Task 3b does not run. The plan does NOT silently mutate `math1_<area>.rs` files — they are deliberately excluded from `files_modified` to keep this contract explicit. |
</threat_model>

<verification>
- `cargo test -p hp41-core --tests` passes (no test regressions)
- `cargo test -p hp41-core --test numerical_accuracy` passes; `case!()` count = 761 (768 - 7 WR-04 deletions); `grep -c 'r.is_ok() || r.is_err()' = 0`
- `bash scripts/check-free42-contamination.sh` exits 0 normally; exits 2 if MATH1_DIR is missing (validated via `/tmp/test-fake-cwd-32-09` harness)
- `cargo test -p hp41-core --test lint_math1_assertions` passes; multi-line `assert_eq` no longer hidden from gate; widened lint validates against FULL post-32-04..32-08 corpus
- `cargo test -p hp41-core --test math1_op_test_count` passes; word-boundary matching active; Task 3a preflight confirmed all 45 variants >= 5 BEFORE swap
- `smoke.spec.js`: `browser.pause` count <= 2; `browser.waitUntil` count >= 9; `beforeEach` block present; `extractErrMessage` helper present
- `cargo llvm-cov --package hp41-core --summary-only` total lines >= post-32-04..32-08 baseline (cleanup does not regress coverage)
</verification>

<success_criteria>
- CR-01: 0 tautological predicates remain in numerical_accuracy.rs.
- WR-01: contamination guard has explicit existence check; missing-dir scenario validated via `/tmp/test-fake-cwd-32-09` harness (exit 2).
- WR-02: multi-line `assert_eq!(decimal, decimal)` heuristic active in lint; validated against FULL post-32-04..32-08 corpus.
- WR-03: word-boundary matching in math1_op_test_count.rs; Task 3a preflight confirmed all 45 variants >= 5 BEFORE Task 3b swap.
- WR-04: 0 redundant `case!(_, _, 1.0, 1.0)` sentinels.
- WR-05: `browser.pause` replaced with `browser.waitUntil` in smoke.spec.js.
- WR-06: `beforeEach` state reset in smoke.spec.js.
- WR-07: `extractErrMessage` helper used in smoke.spec.js.
- All QUAL-01..08 gates continue to pass.
- Wave-2 ordering preserved (32-09 depends_on 32-04..32-08).
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-09-SUMMARY.md` with:
- Per-finding (CR-01, WR-01..07) closure status
- numerical_accuracy.rs case count delta (768 -> 761)
- Task 3a preflight per-variant table (all 45 variants with word-boundary counts)
- Whether Task 3b ran (gate passed) or surfaced a STOP (gate failed and follow-up plan recommended)
- E2E smoke.spec.js metrics (pause count, waitUntil count, beforeEach presence)
- Final post-cleanup coverage measurement (should match Plan 32-08 baseline)
</output>
</content>
</invoke>