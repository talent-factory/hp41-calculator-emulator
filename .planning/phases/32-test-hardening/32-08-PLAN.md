---
phase: 32
plan: "32-08"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/program_error_branches.rs
autonomous: true
requirements:
  - QUAL-01
gap_closure: true
tags:
  - gap-closure
  - coverage
  - program
  - error-branches
must_haves:
  truths:
    - "After this plan ships, `cargo llvm-cov --package hp41-core --summary-only` shows `ops/program.rs` at >= 90 % line coverage (currently 86.42 %, 141 missed lines)."
    - "Free42 disclaim header on lines 1–2 (cosmetic for non-math1 file, but consistent with ADR-002 + the contamination guard's allowlist mechanic)."
    - "Every new `#[test]` carries `// Catches:` doc comment per D-27.1."
    - "Tests respect Pitfall 14 / 17 assertion discipline."
    - "`cargo test -p hp41-core --test program_error_branches` reports all-passing."
  artifacts:
    - path: "hp41-core/tests/program_error_branches.rs"
      provides: "Error-branch coverage for ops/program.rs uncovered arms (label-resolution failures, SIZE-shrink rejection, ProgrammableSize edge cases, run-loop guards)"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/program_error_branches.rs"
      to: "hp41-core/src/ops/program.rs"
      via: "dispatch + run_program + execute_op + run_loop direct calls"
      pattern: "fn run_program|fn execute_op|fn builtin_card_op"
---

<objective>
Close the largest non-math1 coverage gap: `ops/program.rs` (86.42 % -> >= 90 %, 141 missed lines / 1038 total -> cover >= 935). This file is the v1.0 program-execution engine that v3.0 extended for user-callback re-entrancy. Its error branches include label-resolution failures, SIZE register-shrink guards, ProgrammableSize edge cases, CallDepth guards (Phase 28-added), and the run-loop infinite-loop guard.

Purpose: program.rs is the second-biggest contributor to the workspace coverage shortfall after POLY. Closing it lifts the workspace total by ~0.4 pts. Combined with 32-04..32-07 this puts the workspace at or above the 95 % gate.

Output: One new test file `hp41-core/tests/program_error_branches.rs` (~10–14 tests). Test-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md

# Source file
@hp41-core/src/ops/program.rs

# Existing analog test file (Phase 27 test that already covers some happy paths)
@hp41-core/tests/program_execution_coverage.rs

# Lint
@hp41-core/tests/lint_math1_assertions.rs

<interfaces>
<!-- Key public surface from program.rs. Extracted via grep. -->

`hp41-core/src/ops/program.rs` is a v1.0 module — it was NOT frozen at Plan 25-01, only `hp41-core/src/ops/math1/` was frozen since the Plan 25-03 surgical extension. However for THIS gap-closure plan we still treat it as frozen (test-only changes).

Error-return lines identified via `grep -nE "return Err|^pub fn"`:
- Line 49 (InvalidOp in `run_program`): early-return guard
- Line 83 (InvalidOp): another early-return in run_program
- Line 87 (InvalidOp final): function-level return
- Line 115 (InvalidOp): a sub-function early-return
- Line 128 (InvalidOp): another
- Line 159 (InvalidOp in label resolution): missing label
- Line 165 (`.ok_or(HpError::InvalidOp)?`): label-table lookup failure
- Line 189, 213 (InvalidOp): more label/program-counter guards
- Line 271 (InvalidOp in SIZE): n out-of-range guard
- Line 287 (header comment: "n == 0 OR n >= 5")
- Line 303 (InvalidOp in ProgrammableSize): SIZE n=0 OR n>=5
- Line 360 (InvalidOp `_ =>` defensive arm in dispatch)
- Line 420 (InvalidOp)
- Line 450 (InvalidOp): "nothing to resume" guard
- Line 469 (Overflow): infinite-loop guard
- Line 506 (CallDepth): call-stack-full guard
- Line 520 (CallDepth): "D-13/D-14: error before mutation"
- Line 550 (InvalidOp)
- Line 761, 769, 777 (InvalidOp): three adjacent error returns in some sub-function
- Line 831 (InvalidOp): another function-level return

Plus the `builtin_card_op` resolver (lines 1–50 of program.rs's `pub(super) fn` per CLAUDE.md note) which is already tested via Phase 27's resolver-chain tests.

Imports:
- `use hp41_core::ops::{dispatch, Op};`
- `use hp41_core::ops::program::run_program;` (if `pub`) or via dispatch chain
- `use hp41_core::{CalcState, HpError, HpNum};`
- `use std::sync::atomic::Ordering;` (for cancel tests)
</interfaces>

<error_branches_target_list>

Risk-weighted target list (focus on branches with real bug-class signal, NOT defensive `_ =>` arms):

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| program.rs:159, 165 | Missing label / label-table lookup failure -> `Err(HpError::InvalidOp)` | `gto_missing_label_returns_invalid_op` + `xeq_missing_label_returns_invalid_op` |
| program.rs:271 | SIZE n out-of-range guard -> `Err(HpError::InvalidOp)` | `size_n_above_max_returns_invalid_op` |
| program.rs:303 | ProgrammableSize n=0 or n>=5 -> `Err(HpError::InvalidOp)` | `programmable_size_zero_returns_invalid_op` + `programmable_size_above_4_returns_invalid_op` |
| program.rs:450 | RTN with no call frame -> `Err(HpError::InvalidOp)` ("nothing to resume") | `rtn_outside_subroutine_returns_invalid_op` |
| program.rs:469 | Infinite-loop guard -> `Err(HpError::Overflow)` (CR-01 mitigation) | `infinite_loop_guard_overflow` (build a program with no RTN and MAX_STEPS exceeded) |
| program.rs:506 | Call-stack-full -> `Err(HpError::CallDepth)` | `call_stack_full_returns_call_depth` (deeply nested XEQ chain) |
| program.rs:520 | CallDepth before mutation (D-13/14) | `call_depth_error_before_mutation` (verify state unchanged after failed call) |
| program.rs:761, 769, 777 | Three adjacent InvalidOp arms (identify in Task 1 — likely a single function with 3 guards) | 3 arm tests |
| program.rs:831 | Function-level InvalidOp | identify in Task 1 |

</error_branches_target_list>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconnaissance — read program.rs error arms and identify reach-paths</name>
  <read_first>
    - hp41-core/src/ops/program.rs (entire file is 1500+ lines; focus on the sections around the line numbers in the target table)
    - hp41-core/tests/program_execution_coverage.rs (existing Phase 27 test — identify what's already covered)
    - hp41-core/tests/math1_user_callback.rs (for `call_stack_full` patterns)
  </read_first>
  <files>(read-only)</files>
  <action>
    For each line in the `<error_branches_target_list>`, identify:
    1. The function containing it (e.g., `run_program`, `op_xeq`, `op_size`, `op_rtn`).
    2. Whether it's reachable via a normal `dispatch(&mut state, Op::*)` call OR requires direct invocation of a private function (in which case test via the dispatch chain).
    3. The minimum state setup needed.

    For program.rs:761, 769, 777 (three adjacent InvalidOp returns): these are likely in a single sub-function — identify which one and which 3 paths reach them.

    For program.rs:469 (infinite-loop guard): the existing `program_execution_coverage.rs` may have a test that builds an infinite-loop program — check. If not, the new test builds:
    ```
    LBL "A"
    GTO "A"
    ```
    Run it via `run_program`; assert `Err(HpError::Overflow)` after MAX_STEPS exhaustion.

    For program.rs:506, 520 (CallDepth): build a recursive program that XEQs itself >= 7 levels (the v1.0 CALL_DEPTH_MAX). Assert `Err(HpError::CallDepth)`.

    Output: a reach-path mapping for each target line.
  </action>
  <verify>
    <automated>grep -nE "return Err|HpError::CallDepth|HpError::Overflow" hp41-core/src/ops/program.rs | head -40</automated>
  </verify>
  <done>Mapping table complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author hp41-core/tests/program_error_branches.rs (10–14 tests)</name>
  <read_first>
    - hp41-core/src/ops/program.rs (the sections around target lines)
    - hp41-core/tests/program_execution_coverage.rs (avoid duplication; mirror state-setup patterns)
    - hp41-core/tests/math1_user_callback.rs (`call_stack_full` test for recursion pattern)
    - hp41-core/tests/lint_math1_assertions.rs (lint scope is `tests/math1_*.rs` — this file is `program_error_branches.rs`, NOT in scope, so the lint will not gate it; still follow the discipline as a quality-bar matter)
  </read_first>
  <files>hp41-core/tests/program_error_branches.rs</files>
  <action>
    Create new file:

    1. **Lines 1–2** (Free42 disclaim — even though program.rs is NOT a math1 file, including the header is cosmetic-cost-zero and keeps the contamination guard's allowlist consistent if the script's scope ever widens):
       Line 1: `// Algorithm independently re-derived from HP-41C Owner's Manual; Free42 source consulted only as sanity-check oracle, not copied.`

       (NOTE: program.rs predates Math Pac I by 30+ phases. The HP-41C OM citation is more accurate than the Math Pac I OM. The script's allowlist matches the substring `'Free42 source consulted only as sanity-check oracle'` — both header forms satisfy it.)

    2. **File-doc** (`//!`): "Plan 32-08 gap-closure for `hp41-core/src/ops/program.rs` — closes the 86.42 % per-file coverage gap by exercising error branches in label resolution (lines 159, 165), SIZE / ProgrammableSize guards (lines 271, 303), RTN-outside-subroutine (line 450), infinite-loop / call-stack guards (lines 469, 506, 520), and the three adjacent InvalidOp arms at 761/769/777. Each test follows the D-27.1 risk-weighted discipline with a `// Catches:` doc comment naming the bug class. The lint `lint_math1_assertions.rs` scopes only `tests/math1_*.rs` — this file is OUT of scope but follows the same assertion discipline (no `assert_eq!(decimal, decimal)`, use `approx::assert_relative_eq!` if comparing decimals)."

    3. `#![allow(clippy::unwrap_used)]`

    4. Imports per `<interfaces>`.

    5. **Tests** (10 mandatory + up to 4 optional):
       a) `gto_missing_label_returns_invalid_op` — build program `[Op::Gto("MISSING".to_string())]`, dispatch; assert `Err(HpError::InvalidOp)`. Covers program.rs:159.

       b) `xeq_missing_label_returns_invalid_op` — `[Op::Xeq("MISSING".to_string())]`, dispatch; `Err(InvalidOp)`. Covers 165.

       c) `size_n_above_max_returns_invalid_op` — dispatch `Op::Size(n)` where n exceeds the max register count (typically 320 or similar — check `crate::ops::program::MAX_REGS` constant). Assert `Err(InvalidOp)`. Covers 271.

       d) `programmable_size_zero_returns_invalid_op` — `Op::ProgrammableSize(0)`; `Err(InvalidOp)`. Covers 303 (lower bound).

       e) `programmable_size_above_4_returns_invalid_op` — `Op::ProgrammableSize(5)` (the docstring says "n >= 5"); `Err(InvalidOp)`. Covers 303 (upper bound).

       f) `rtn_outside_subroutine_returns_invalid_op` — `Op::Rtn` dispatched on a fresh state with no call frame; `Err(InvalidOp)`. Covers 450.

       g) `infinite_loop_guard_overflow` — build `[Op::Lbl("A"), Op::Gto("A"), Op::Rtn]`; `run_program(&mut state, &program)`; assert `Err(HpError::Overflow)` after MAX_STEPS exhaustion (the constant is around 1_000_000 — test will take ~0.5s).

       h) `call_stack_full_returns_call_depth` — build `[Op::Lbl("A"), Op::Xeq("A"), Op::Rtn]`; `run_program`; assert `Err(HpError::CallDepth)` after ~7 levels (CALL_DEPTH_MAX).

       i) `call_depth_error_before_mutation` — verify that after a CallDepth error, the state's stack/regs are UNCHANGED from pre-call (D-13/14 contract). Set up the recursion + capture stack-snapshot pre-run, run, assert error, compare post-state.

       j) Cover one of program.rs:761, 769, 777 (Task 1 identifies the function and reach-path).

       Optional k–n) Cover the remaining 2 lines of (761, 769, 777) + program.rs:831.

    **Assertion discipline**: even though this file is not `math1_*.rs`, follow `lint_math1_assertions.rs`'s rules (no `assert_eq!(decimal, decimal)`). Use `matches!` and `approx::assert_relative_eq!` as needed.

    Use `dispatch(&mut state, Op::*)` for normal-path tests; use `run_program(&mut state, &program)` for run-loop tests if `run_program` is `pub`. If `run_program` is private, use `dispatch(&mut state, Op::Run)` which invokes it.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test program_error_branches</automated>
    <automated>head -1 hp41-core/tests/program_error_branches.rs | grep -q "Free42 source consulted only as sanity-check oracle"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/program_error_branches.rs</automated>
  </verify>
  <done>>= 10 tests passing, disclaim sentence on line 1 (so the contamination guard allowlist matches if its scope ever expands), `// Catches:` count >= 10.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Measure program.rs coverage delta</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md
  </read_first>
  <files>(no file writes)</files>
  <action>
    Run `cargo llvm-cov --package hp41-core --summary-only` and inspect `ops/program.rs`. Verify >= 90.0 % lines. If below, identify uncovered branches via `cargo llvm-cov --html -p hp41-core` and add 1–3 more targeted tests.

    Also opportunistically inspect `ops/math.rs` (91.65 % currently) and `ops/stats.rs` (86.26 %) — if either is dragging the workspace below 95 %, document in the SUMMARY whether Plan 32-09 should pick them up OR whether the existing coverage from 32-04..32-07 already lifted them sufficiently via dispatch-chain side-effects.
  </action>
  <verify>
    <automated>cargo llvm-cov --package hp41-core --summary-only | grep -E "ops/program\.rs|ops/math\.rs|ops/stats\.rs"</automated>
  </verify>
  <done>`ops/program.rs` >= 90.0 % lines. math.rs and stats.rs measured and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08-01 | DoS | `infinite_loop_guard_overflow` test exhausts MAX_STEPS in CI runtime | mitigate | MAX_STEPS is ~1_000_000 ops; the test completes in < 1 second per existing `run_program` benchmarks. Total test-runtime budget for this file < 5 seconds. |
| T-32-08-02 | Repudiation | Coverage padding | mitigate | Each test cites the source line in `// Catches:`; tests assert specific error variants AND (for call_depth_error_before_mutation) state-non-mutation contracts. |
| T-32-08-03 | Information Disclosure | Free42 GPL contamination | mitigate | Header sentence on line 1 satisfies the allowlist substring even though program.rs is a v1.0 module pre-dating Math Pac I. |
</threat_model>

<verification>
- `cargo test -p hp41-core --test program_error_branches` passes with >= 10 tests
- `cargo llvm-cov --package hp41-core --summary-only` shows `ops/program.rs` >= 90.0 % lines
- `bash scripts/check-free42-contamination.sh` continues to exit 0 (tests are out of scope)
- File-scope `#![allow(clippy::unwrap_used)]` present
</verification>

<success_criteria>
- `ops/program.rs` line coverage >= 90.0 % (up from 86.42 %).
- New test file exists with all-passing tests.
- All frozen invariants preserved.
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-08-SUMMARY.md` with per-task completion, final program.rs / math.rs / stats.rs percentages.
</output>
