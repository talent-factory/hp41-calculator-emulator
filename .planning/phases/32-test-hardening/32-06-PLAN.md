---
phase: 32
plan: "32-06"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/math1_solve_error_branches.rs
  - hp41-core/tests/math1_difeq_error_branches.rs
autonomous: true
requirements:
  - QUAL-01
gap_closure: true
tags:
  - gap-closure
  - coverage
  - solve
  - difeq
  - error-branches
  - user-callback
must_haves:
  truths:
    - "After this plan ships, `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/solve.rs` >= 90 % line coverage (currently 85.77 %) AND `ops/math1/difeq.rs` >= 90 % line coverage (currently 85.76 %)."
    - "All new tests carry the verbatim Free42 disclaim header on lines 1–2 per ADR-002."
    - "Every new `#[test]` carries a `// Catches: <bug class>` doc comment per D-27.1."
    - "Tests respect the v2.2 D-27.6 / Pitfall 14/17 assertion discipline (`approx::assert_relative_eq!` / `matches!`)."
    - "`cargo test -p hp41-core --test math1_solve_error_branches` AND `--test math1_difeq_error_branches` both report all-passing."
  artifacts:
    - path: "hp41-core/tests/math1_solve_error_branches.rs"
      provides: "Error-branch coverage for ops/math1/solve.rs uncovered arms (iteration cap, non-convergence, missing-label paths)"
      contains: "#[test]"
    - path: "hp41-core/tests/math1_difeq_error_branches.rs"
      provides: "Error-branch coverage for ops/math1/difeq.rs uncovered arms (Order validation, overflow guard, submit_step Ready arms)"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/math1_solve_error_branches.rs"
      to: "hp41-core/src/ops/math1/solve.rs"
      via: "dispatch(&mut s, Op::Solve | Op::Sol) + submit_step direct + run_loop helper"
      pattern: "dispatch\\(.*Op::(Solve|Sol)\\)|SolveInputStep::"
    - from: "hp41-core/tests/math1_difeq_error_branches.rs"
      to: "hp41-core/src/ops/math1/difeq.rs"
      via: "dispatch(&mut s, Op::Difeq) + submit_step direct + run_loop helper"
      pattern: "dispatch\\(.*Op::Difeq|DifeqInputStep::"
---

<objective>
Close two user-callback program coverage gaps in tandem: `ops/math1/solve.rs` (85.77 % -> >= 90 %, +67 missed lines / 471 -> cover >= 424) AND `ops/math1/difeq.rs` (85.76 % -> >= 90 %, +95 missed lines / 667 -> cover >= 600). Both files share the same control-flow shape — `submit_step` Err-arms, run-loop infinite-loop guards (`HpError::Overflow`), iteration-cap branches, and `Order` / `FunctionName` validation.

Purpose: Plans 32-04..32-08 collectively close the QUAL-01 gap. This plan focuses on the two computationally heaviest math1 surfaces (the user-callback programs INTG/SOLVE/DIFEQ share infrastructure). INTG sits at 90.86 % already — no plan needed for it directly, but adjacent SOLVE / DIFEQ tests may incidentally lift it further.

Output: TWO new test files, each ~8–12 tests. Test-only. No `hp41-core/src/` modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md
@.planning/phases/32-test-hardening/32-CONTEXT.md

# Source files
@hp41-core/src/ops/math1/solve.rs
@hp41-core/src/ops/math1/difeq.rs

# Analog test files (existing happy-path tests for both)
@hp41-core/tests/math1_solve.rs
@hp41-core/tests/math1_solve_paths.rs
@hp41-core/tests/math1_difeq.rs
@hp41-core/tests/math1_user_callback.rs

# Lint
@hp41-core/tests/lint_math1_assertions.rs

<interfaces>
<!-- Key public surface from solve.rs and difeq.rs. Extracted via grep. -->

From `hp41-core/src/ops/math1/solve.rs`:
- `pub fn submit_step(state: &mut CalcState, step: SolveInputStep) -> Result<(), HpError>` (around line 565)
- `pub fn op_solve_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>` (already used by `math1_user_callback.rs`)
- `pub fn op_solve(state: &mut CalcState) -> Result<(), HpError>` (master entry, opens modal)
- Error returns at lines: 137, 149 (InvalidOp unconditional from `op_sol` short-circuit paths), 367 (run-loop error propagation), 1013, 1121 (unreachable arms)

From `hp41-core/src/ops/math1/difeq.rs`:
- `pub fn submit_step(state: &mut CalcState, step: DifeqInputStep) -> Result<(), HpError>` (around line 774)
- `pub fn op_difeq_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>`
- `pub fn op_difeq(state: &mut CalcState) -> Result<(), HpError>` (master)
- Error returns at lines: 160, 211 (InvalidOp), 215 (CallDepth), 282 (InvalidOp), 315 (Canceled), 414 (run-loop error), 670 (Overflow infinite-loop guard), 789, 798, 807, 816, 832, 841 (submit_step InvalidOp arms — 6 arms), 1272, 1290, 1304 (probably unreachable defensive arms)

Variants needed (from `hp41-core/src/ops/math1/modal.rs`):
- `pub enum SolveInputStep { ... }` — verify variants
- `pub enum DifeqInputStep { ... }` — verify variants

Helpers from `hp41-core/tests/math1_user_callback.rs`:
- `make_identity_fn_state()` (line ~41)
- `make_nested_integ_state()`
- pattern: build CalcState, set program (LBL + RTN), set alpha_reg, set regs, call `op_*_run_loop`

Imports:
- `use hp41_core::ops::math1::modal::{ModalProgram, SolveInputStep, DifeqInputStep};`
- `use hp41_core::ops::math1::solve::{submit_step as solve_submit_step, op_solve_run_loop};`
- `use hp41_core::ops::math1::difeq::{submit_step as difeq_submit_step, op_difeq_run_loop};`
- `use hp41_core::ops::{dispatch, Op};`
- `use hp41_core::{CalcState, HpError, HpNum};`
</interfaces>

<error_branches_target_list>

### solve.rs (currently 85.77 %, 67 missed lines)

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| solve.rs:137 | `op_sol` short-circuit InvalidOp (no setup) | `op_sol_no_setup_returns_invalid_op` (mirror `math1_solve.rs::no_setup_rejected`) |
| solve.rs:149 | second InvalidOp short-circuit | `op_sol_secondary_path_returns_invalid_op` |
| solve.rs:367 | `run_secant_loop` error propagation | `run_secant_propagates_user_fn_err` (mirror `math1_user_callback.rs::user_fn_stops_aborts_integ` pattern for SOLVE) |
| solve.rs submit_step Err-arms | `Err(HpError::InvalidOp)` per state | `submit_step_arm_*` (one per arm — identify via grep in Task 1) |
| solve.rs run-loop non-convergence | iteration-cap reached -> `NO ROOT FOUND` print + Err? | `solve_iteration_cap_reached` (mirror `math1_solve.rs::iteration_cap` but assert print_buffer wording) |
| solve.rs:1013, 1121 | unreachable defensive arms (if reachable, cover; if not, mark as comment-only via `_ =>` arm exercise) | (omit if unreachable; document in SUMMARY) |

### difeq.rs (currently 85.76 %, 95 missed lines — biggest of the two)

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| difeq.rs:160 | `op_difeq` unconditional InvalidOp (defensive) | `op_difeq_unconditional_invalid_op` (if reachable) |
| difeq.rs:211 | InvalidOp BEFORE state change | `nested_difeq_rejected_pre_mutation` (already covered by `math1_user_callback.rs::nested_difeq_*`? verify) |
| difeq.rs:215 | CallDepth error | `difeq_call_depth_exhausted` (mirror `math1_difeq.rs::call_stack_full` but with deeper recursion) |
| difeq.rs:282 | InvalidOp arm in mid-flow | identify in Task 1 |
| difeq.rs:315 | Canceled error path | `difeq_cancel_propagates` (set `cancel_requested.store(true)`, run loop, assert `Err(HpError::Canceled)` — mirror `math1_difeq.rs::cancel_per_64_steps` but assert the return value not just side-effect) |
| difeq.rs:414 | run-loop error propagation | `run_loop_propagates_user_fn_err` |
| difeq.rs:670 | Overflow infinite-loop guard | `difeq_infinite_loop_guard_overflow` |
| difeq.rs:789, 798, 807, 816, 832, 841 | 6 `submit_step` InvalidOp arms | 6 `submit_step_arm_<line>_returns_invalid_op` tests |
| difeq.rs:1272, 1290, 1304 | defensive `_ =>` arms | document unreachable in SUMMARY if so |

</error_branches_target_list>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconnaissance — enumerate submit_step Err-arms and confirm reachability</name>
  <read_first>
    - hp41-core/src/ops/math1/solve.rs (lines 100–160, 300–400, 560–650)
    - hp41-core/src/ops/math1/difeq.rs (lines 130–220, 280–320, 400–420, 660–680, 770–845, 1260–1310)
    - hp41-core/src/ops/math1/modal.rs (`SolveInputStep` + `DifeqInputStep` enum definitions)
    - hp41-core/tests/math1_user_callback.rs (lines 41–80 for helpers; lines 350–470 for STOP / GTO patterns; lines 480–600 for new QUAL-08 categories)
    - hp41-core/tests/math1_solve.rs and math1_difeq.rs (existing test patterns + state-setup helpers)
  </read_first>
  <files>(read-only)</files>
  <action>
    For each Err-arm line in the target table, identify:
    1. Which `pub fn` it lives in (`op_*`, `submit_step`, `run_loop`).
    2. Which state setup reaches it (which `SolveInputStep` / `DifeqInputStep` variant, what regs/alpha_reg/cancel_requested values).
    3. Whether the arm is REACHABLE in any normal state machine OR is an UNREACHABLE defensive `_ =>` arm.

    **Arm reachability classification (per the 32-04 quadratic_zero_leading_returns_domain pattern):** for each target arm, classify it as REACHABLE (a normal state-machine path lands on it; test will be authored) or UNREACHABLE (the arm is a defensive `_ =>` that the type system prevents from being hit in normal state). Unreachable arms are documented in the SUMMARY and removed from the test target count — they are NOT covered by `#[allow(dead_code)]` test stubs.

    For the `Cancel` path (difeq.rs:315): the existing `math1_difeq.rs::cancel_per_64_steps` test calls `state.cancel_requested.store(true, Ordering::SeqCst)` BEFORE running the loop. Confirm via reading that test that the asserted return is `Err(HpError::Canceled)` not just a side-effect. If so, we don't need to repeat — but we DO need to confirm coverage hits line 315 today. If the existing test doesn't reach 315 (e.g., it runs through normal completion before checking cancel), our new test should.

    For the `Overflow` path (difeq.rs:670 — infinite-loop guard): the existing `math1_difeq.rs::call_stack_full` may already exercise this OR may exercise a different overflow. Confirm.

    For the 6 `submit_step` arms in difeq (lines 789, 798, 807, 816, 832, 841): each corresponds to a specific `DifeqInputStep` variant. Map them one-to-one. Each REACHABLE arm will produce one test in Task 3.

    Output a table mapping each target line -> (function, state setup, expected error, REACHABLE/UNREACHABLE) for use by Tasks 2 and 3.
  </action>
  <verify>
    <automated>grep -nE "HpError::(Domain|InvalidOp|CallDepth|Canceled|Overflow)" hp41-core/src/ops/math1/solve.rs | wc -l</automated>
    <automated>grep -nE "HpError::(Domain|InvalidOp|CallDepth|Canceled|Overflow)" hp41-core/src/ops/math1/difeq.rs | wc -l</automated>
  </verify>
  <done>Mapping table for both files complete. Each target line has either a confirmed reach-path or a documented UNREACHABLE marker.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author hp41-core/tests/math1_solve_error_branches.rs (8–10 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/solve.rs
    - hp41-core/tests/math1_solve.rs + math1_solve_paths.rs + math1_user_callback.rs (for helpers + assertion patterns)
    - hp41-core/tests/lint_math1_assertions.rs
  </read_first>
  <files>hp41-core/tests/math1_solve_error_branches.rs</files>
  <action>
    Create new file with:
    1. Free42 disclaim header on lines 1–2.
    2. File-doc citing the gap-closure scope, the 85.77 % current coverage, and the targeted branches.
    3. `#![allow(clippy::unwrap_used)]`.
    4. Imports per `<interfaces>`.
    5. Helpers: copy `make_identity_fn_state()` from `math1_user_callback.rs` (or import via `use` if it's `pub` — it's not; copy verbatim with a comment "// Mirrors `math1_user_callback.rs::make_identity_fn_state` per gap-closure scope. Update both if the helper changes."). Adapt for SOLVE: instead of building an INTG-ready state, build a SOLVE-ready state with `state.regs[0] = guess_1`, `state.regs[1] = guess_2`, `state.alpha_reg = "<label>"`.

    6. **Tests** (8 mandatory — only REACHABLE arms per Task 1 classification):
       a) `op_sol_no_setup_returns_invalid_op` — call `dispatch(&mut s, Op::Sol)` on fresh `CalcState::new()` without setting alpha_reg / regs; assert `Err(HpError::InvalidOp)`. Mirrors `math1_solve.rs::no_setup_rejected` but exercises the dispatch path.

       b) `solve_run_loop_iteration_cap_returns_no_root_found_print` — build state with 100-iteration-cap-triggering function (e.g., `tan(x) - 10` with bad guesses far from root); run; assert either `Err(HpError::Domain)` OR `print_buffer.iter().any(|l| l.contains("NO ROOT"))` per SOLV-04 OM convention.

       c) `solve_run_loop_propagates_user_fn_overflow` — user function programmed to STO into R99 with overflow value triggering `HpError::Overflow` in the body; run loop should propagate. Mirror `math1_user_callback.rs::user_fn_stops_aborts_integ` shape but trigger Overflow not Stop.

       d) `solve_cancel_propagates` — set `cancel_requested.store(true, Ordering::SeqCst)` BEFORE running; assert `Err(HpError::Canceled)`.

       e–h) `submit_step_arm_<line>_returns_invalid_op` — one test per REACHABLE `submit_step` Err-arm identified in Task 1.

       Optional i–j) — exercise additional `op_solve_*` entries OR the secant-loop edge cases that current tests miss.

    **Assertion discipline**: `approx::assert_relative_eq!` for any decimal compare. `matches!` for error matching.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_solve_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_solve_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_solve_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 8 tests passing for REACHABLE error arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count. Disclaim present, lint passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author hp41-core/tests/math1_difeq_error_branches.rs (10–14 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/difeq.rs
    - hp41-core/tests/math1_difeq.rs + math1_user_callback.rs (helpers + DIFEQ-specific patterns)
    - hp41-core/tests/lint_math1_assertions.rs
  </read_first>
  <files>hp41-core/tests/math1_difeq_error_branches.rs</files>
  <action>
    Same structure as Task 2 but for DIFEQ. Mandatory tests cover (only REACHABLE arms per Task 1 classification):
    - `op_difeq_unconditional_invalid_op` (line 160 — defensive; SKIP if Task 1 marks UNREACHABLE)
    - `difeq_run_loop_call_depth_exhausted` (line 215 — `Err(HpError::CallDepth)` via deeply nested call stack)
    - `difeq_cancel_propagates` (line 315 — `Err(HpError::Canceled)`)
    - `difeq_run_loop_propagates_user_fn_err` (line 414)
    - `difeq_infinite_loop_guard_overflow` (line 670 — `Err(HpError::Overflow)` via maliciously crafted RK4 step that triggers the guard; one approach: very small step_size combined with very large max-steps budget that exhausts before convergence)
    - **6 `submit_step_arm_<line>_returns_invalid_op`** tests covering REACHABLE difeq.rs lines among 789, 798, 807, 816, 832, 841 (one per `DifeqInputStep` variant identified in Task 1)
    - `difeq_order2_specific_path` — exercise one untested ORDER=2 coupled-RK4 path surfaced in Task 1

    **Mirror existing patterns**: `math1_difeq.rs::call_stack_full` and `math1_user_callback.rs::nested_difeq_*` are the closest analogs. Reuse the program-building idiom (`vec![Op::Lbl("X".to_string()), ..., Op::Rtn]`).

    **Assertion discipline**: same rules. `approx::assert_relative_eq!` for decimals; `assert_eq!(r, Err(HpError::CallDepth))` is fine because `HpError::CallDepth` is unit (no decimal token on the line); `matches!` for any error variant if a wildcard match is needed.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_difeq_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_difeq_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_difeq_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 10 tests passing for REACHABLE error arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count. Disclaim present, lint passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Measure per-file coverage delta on solve.rs and difeq.rs</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md
  </read_first>
  <files>(no file writes)</files>
  <action>
    Run `cargo llvm-cov --package hp41-core --summary-only` and inspect both `ops/math1/solve.rs` and `ops/math1/difeq.rs` lines. Verify both >= 90.0 %. If either is below, identify uncovered branches via `cargo llvm-cov --html -p hp41-core` and add 1–3 more targeted tests to that file (do NOT exceed 14 tests per file to avoid bloat).
  </action>
  <verify>
    <automated>cargo llvm-cov --package hp41-core --summary-only | grep -E "ops/math1/(solve|difeq)\.rs"</automated>
  </verify>
  <done>Both `ops/math1/solve.rs` and `ops/math1/difeq.rs` line coverage >= 90.0 %.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06-01 | Tampering | New test files' assertion discipline | mitigate | `// Catches:` names specific bug class; assertions use `matches!` or `approx::assert_relative_eq!`. |
| T-32-06-02 | Information Disclosure | Free42 GPL contamination | mitigate | Verbatim disclaim header lines 1–2 per ADR-002. Tests build their own user-callback programs from scratch — no algorithm code copied. |
| T-32-06-03 | Repudiation | Coverage padding without regression signal | mitigate | Each test cites the source line range it covers (in `// Catches:` or surrounding comment); user-callback tests follow the established `math1_user_callback.rs` regression-class shape per QUAL-08 categories. UNREACHABLE arms are documented in SUMMARY, NOT padded with stubs. |
| T-32-06-04 | DoS | User-callback recursion test triggers an infinite loop in the test runner | mitigate | All run-loop tests cap at the established `MAX_STEPS` / `USER_CALLBACK_MAX_STEPS` budgets which already protect production; reproducible exit within < 1 second per test. |
</threat_model>

<verification>
- `cargo test -p hp41-core --test math1_solve_error_branches` passes with >= 8 reach-confirmed tests
- `cargo test -p hp41-core --test math1_difeq_error_branches` passes with >= 10 reach-confirmed tests
- `cargo test -p hp41-core --test lint_math1_assertions` continues to pass
- `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/solve.rs` >= 90.0 % AND `ops/math1/difeq.rs` >= 90.0 %
- Both new files carry the disclaim header on lines 1–2
- `// Catches:` count appropriate per file (matches reach-confirmed count documented in SUMMARY)
</verification>

<success_criteria>
- `ops/math1/solve.rs` line coverage >= 90.0 % (up from 85.77 %).
- `ops/math1/difeq.rs` line coverage >= 90.0 % (up from 85.76 %).
- Two new test files exist with all-passing tests.
- Reach-confirmed test count documented in SUMMARY (REACHABLE arms covered, UNREACHABLE arms catalogued).
- All frozen invariants preserved.
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-06-SUMMARY.md` when done, including the REACHABLE-vs-UNREACHABLE arm classification table.
</output>
</content>
</invoke>