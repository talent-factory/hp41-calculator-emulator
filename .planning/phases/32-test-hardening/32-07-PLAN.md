---
phase: 32
plan: "32-07"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/math1_matrix_error_branches.rs
  - hp41-core/tests/math1_mod_extra_coverage.rs
  - hp41-core/tests/math1_integ_error_branches.rs
autonomous: true
requirements:
  - QUAL-01
gap_closure: true
tags:
  - gap-closure
  - coverage
  - matrix
  - mod
  - integ
  - error-branches
must_haves:
  truths:
    - "After this plan ships, `cargo llvm-cov --package hp41-core --summary-only` shows `ops/math1/matrix.rs` >= 90 % AND `ops/math1/mod.rs` >= 80 % (raised from 56.25 %) AND `ops/math1/integ.rs` stays >= 90 % (currently 90.86 % — adjacent error-branch tests may lift it further)."
    - "Free42 disclaim header on lines 1–2 of every new file per ADR-002."
    - "Every new `#[test]` carries `// Catches:` doc comment per D-27.1."
    - "Tests respect Pitfall 14 / 17 assertion discipline."
    - "All three new tests files report all-passing."
  artifacts:
    - path: "hp41-core/tests/math1_matrix_error_branches.rs"
      provides: "Error-branch coverage for matrix.rs uncovered arms (singular detection, out-of-range index, submit_step Ready)"
      contains: "#[test]"
    - path: "hp41-core/tests/math1_mod_extra_coverage.rs"
      provides: "Additional coverage for ops/math1/mod.rs (the remaining ~14 missed lines beyond Plan 32-01's 5 tests)"
      contains: "#[test]"
    - path: "hp41-core/tests/math1_integ_error_branches.rs"
      provides: "Error-branch coverage for integ.rs (Overflow guard, CallDepth, submit_step Ready)"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/math1_matrix_error_branches.rs"
      to: "hp41-core/src/ops/math1/matrix.rs"
      via: "dispatch(Op::MatDet | MatInv | MatSimeq) + submit_step + matrix setup helpers"
      pattern: "dispatch\\(.*Op::Mat|MatrixInputStep::"
    - from: "hp41-core/tests/math1_mod_extra_coverage.rs"
      to: "hp41-core/src/ops/math1/mod.rs"
      via: "submit_modal / cancel_modal / submit_modal_with_label entrypoints"
      pattern: "submit_modal|cancel_modal|submit_modal_with_label"
    - from: "hp41-core/tests/math1_integ_error_branches.rs"
      to: "hp41-core/src/ops/math1/integ.rs"
      via: "dispatch(Op::Integ) + submit_step + op_integ_run_loop"
      pattern: "dispatch\\(.*Op::Integ\\)|IntegInputStep::"
---

<objective>
Close three remaining math1 coverage gaps: `ops/math1/matrix.rs` (89.68 % -> >= 90 %, 55 missed lines — marginal), `ops/math1/mod.rs` (56.25 % -> >= 80 %, 14 missed lines — small file, big percentage), AND surface-cover any `ops/math1/integ.rs` adjacent error branches (currently 90.86 %, marginally above floor but lifting it improves workspace total).

Purpose: This is the catch-up plan that closes the last three math1 files below or near the 90 % per-file floor. The mod.rs file is small (only 32 lines total) — Plan 32-01 already lifted it 0% -> 56.25% with 5 tests; closing the remaining 14 lines requires ~3 more targeted tests covering the public-surface arms Plan 32-01 didn't reach.

Output: THREE new test files (matrix, mod-extra, integ), each ~5–10 tests. Test-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md
@.planning/phases/32-test-hardening/32-CONTEXT.md

# Source files
@hp41-core/src/ops/math1/matrix.rs
@hp41-core/src/ops/math1/mod.rs
@hp41-core/src/ops/math1/integ.rs

# Existing test analogs
@hp41-core/tests/math1_matrix.rs
@hp41-core/tests/math1_matrix_flow.rs
@hp41-core/tests/math1_mod_entry_points.rs
@hp41-core/tests/math1_integ.rs
@hp41-core/tests/math1_user_callback.rs

# Lint
@hp41-core/tests/lint_math1_assertions.rs

<interfaces>
<!-- Key public surface. -->

From `hp41-core/src/ops/math1/matrix.rs`:
- `pub fn op_matrix_workflow / op_mat_size / op_mat_vmat / op_mat_edit / op_mat_det / op_mat_inv / op_mat_simeq / op_mat_vcol` (verify via grep)
- `pub fn submit_step(state: &mut CalcState, step: MatrixInputStep) -> Result<(), HpError>` (around line 349+)
- Error returns at lines 88, 101 (InvalidOp from index out-of-range), 216, 286 (Domain from singular pivot), 361, 408, 419, 460, 478 (InvalidOp), 482 (OutOfRange)

From `hp41-core/src/ops/math1/mod.rs`:
- `pub fn submit_modal(state: &mut CalcState) -> Result<(), HpError>`
- `pub fn cancel_modal(state: &mut CalcState)` (no Result — never errs)
- `pub fn submit_modal_with_label(state: &mut CalcState, label: &str) -> Result<(), HpError>`
- Error returns at lines 65 (InvalidOp no-modal), 119, 120 (InvalidOp on label-prompt-step rejection), 133 (`_ =>` catchall)
- Plan 32-01 covered the 5 most-direct error paths via `math1_mod_entry_points.rs`. The remaining ~14 missed lines are likely:
  - The 3 dispatch arms that route to specific per-program `submit_step` (the happy-path branches for each `ModalProgram` variant — Matrix, Solve, Poly, Integ, Difeq, Four, Trans). Each routing arm has ~1–3 lines.
  - Run-on `submit_modal_with_label` for the THREE user-callback programs (INTG, SOLVE, DIFEQ).

From `hp41-core/src/ops/math1/integ.rs`:
- `pub fn op_integ(state: &mut CalcState) -> Result<(), HpError>`
- `pub fn op_integ_run_loop(state: &mut CalcState, program: &[Op]) -> Result<(), HpError>`
- `pub fn submit_step(state: &mut CalcState, step: IntegInputStep) -> Result<(), HpError>`
- Error returns at lines 225 (CallDepth), 229 (CallDepth), 254 (Domain — subdivision cap), 308, 315 (Overflow), 331 (Canceled), 346 (Overflow), 391 (Overflow), 409 (InvalidOp), 456 (Overflow infinite-loop guard), 562 (InvalidOp), 577 (InvalidOp Ready)

Imports for new test files:
- `use hp41_core::ops::math1::modal::{ModalProgram, MatrixInputStep, IntegInputStep, ...};`
- `use hp41_core::ops::math1::matrix::submit_step as matrix_submit_step;`
- `use hp41_core::ops::math1::{submit_modal, cancel_modal, submit_modal_with_label};`
- `use hp41_core::ops::math1::integ::{submit_step as integ_submit_step, op_integ_run_loop};`
- `use hp41_core::ops::{dispatch, Op};`
- `use hp41_core::{CalcState, HpError, HpNum};`
</interfaces>

<error_branches_target_list>

### matrix.rs (89.68 %, 55 missed lines — marginal but the per-file floor is 90)

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| matrix.rs:88, 101 | Index out-of-range -> `Err(HpError::InvalidOp)` (set_matrix_element + get_matrix_element with invalid i/j) | `mat_index_out_of_range_returns_invalid_op` |
| matrix.rs:216, 286 | Singular pivot in `op_mat_det` / `op_mat_inv` -> `Err(HpError::Domain)` (already covered by `math1_matrix.rs::inv_singular_2x2_sets_no_solution` — verify it reaches both lines) | `mat_det_singular_returns_domain` (additional test if needed) |
| matrix.rs:361 | `submit_step Ready -> Err(HpError::InvalidOp)` | `submit_step_ready_returns_invalid_op` |
| matrix.rs:408, 419, 460, 478 | `submit_step` arms -> `Err(HpError::InvalidOp)` per state | 4 arm tests if reachable |
| matrix.rs:482 | `Err(HpError::OutOfRange)` — e.g., MAT ORDER > 14 | `mat_order_above_14_returns_out_of_range` (mirror `math1_matrix.rs::order_cap_15_returns_out_of_range`) |

### mod.rs (56.25 %, 14 missed lines)

Plan 32-01 added 5 tests covering: `submit_modal` no-modal-active guard, `cancel_modal` field-clear, `submit_modal_with_label` no-modal + wrong-step + FunctionNamePrompt happy-path. The remaining ~14 missed lines likely cover the dispatch routing arms for each `ModalProgram::*` variant in `submit_modal`. We need tests that:

| Source area | Branch | Test name |
|-------------|--------|-----------|
| `submit_modal` Solve routing | flushes entry_buf, dispatches to `solve::submit_step` | `submit_modal_dispatches_solve` |
| `submit_modal` Poly routing | flushes, dispatches to `poly::submit_step` | `submit_modal_dispatches_poly` |
| `submit_modal` Difeq routing | flushes, dispatches | `submit_modal_dispatches_difeq` |
| `submit_modal` Four routing | flushes, dispatches | `submit_modal_dispatches_four` |
| `submit_modal` Trans routing | flushes, dispatches | `submit_modal_dispatches_trans` |
| `submit_modal_with_label` Solve label | uppercases + dispatches | `submit_modal_with_label_solve` |
| `submit_modal_with_label` Difeq label | uppercases + dispatches | `submit_modal_with_label_difeq` |

### integ.rs (90.86 %, 32 missed lines — already above floor; opportunistic uplift)

| Source line(s) | Branch | Test name |
|----------------|--------|-----------|
| integ.rs:254 | Subdivision cap n > 32768 -> `Err(HpError::Domain)` (covered by `math1_integ.rs::integ_subdivision_cap_32768`? verify) | additional if needed |
| integ.rs:315 | Overflow at numerical step | `integ_step_overflow_returns_overflow` |
| integ.rs:331 | Canceled propagation | `integ_cancel_propagates` |
| integ.rs:391 | Overflow at sum accumulator | `integ_accumulator_overflow_returns_overflow` |
| integ.rs:409 | InvalidOp arm | identify in Task 1 |
| integ.rs:456 | Overflow infinite-loop guard | `integ_infinite_loop_guard_overflow` |
| integ.rs:562, 577 | submit_step Err-arms | 2 tests if reachable |

</error_branches_target_list>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconnaissance — confirm Plan 32-01's mod.rs coverage gap source and matrix/integ Err-arms</name>
  <read_first>
    - hp41-core/src/ops/math1/mod.rs (entire file; only 135 LOC)
    - hp41-core/src/ops/math1/matrix.rs (lines 80–105, 210–290, 350–500)
    - hp41-core/src/ops/math1/integ.rs (lines 220–260, 300–350, 380–410, 450–470, 555–585)
    - hp41-core/tests/math1_mod_entry_points.rs (entire file — to avoid duplicating tests Plan 32-01 already added)
    - hp41-core/tests/math1_matrix.rs (existing tests — avoid duplication)
    - hp41-core/tests/math1_integ.rs (existing tests — avoid duplication, especially `integ_subdivision_cap_32768`)
  </read_first>
  <files>(read-only)</files>
  <action>
    1. Read `mod.rs` end-to-end. Plan 32-01 covered ~18 lines (56.25 % * 32 lines ~= 18); the remaining ~14 lines are the per-program dispatch routing arms (probably one `match` block in `submit_modal` and another in `submit_modal_with_label`). Confirm by reading the match arms.

    2. For each `ModalProgram` variant (Matrix, Solve, Poly, Integ, Difeq, Four, Trans), determine whether `submit_modal` routes to a per-program `submit_step` AND whether the routing is exercised by existing tests in `math1_matrix.rs`, `math1_solve.rs`, etc. The routing IS exercised in passing by every happy-path test — BUT coverage may miss certain arms if the match arm is in a guarded path. Read the actual match block to identify which arms are likely uncovered.

    3. For `submit_modal_with_label`: this calls `requires_alpha_label` on the current step (per the helper docs). Only INTG / SOLVE / DIFEQ have `FunctionNamePrompt` steps. Confirm the routing arms for SOLVE / DIFEQ (Plan 32-01 covered INTG).

    4. For matrix.rs index out-of-range (lines 88, 101): identify which `pub fn` exposes this. Likely `set_matrix_element(state, i, j, val)` or `get_matrix_element(state, i, j)` (depending on what's `pub`). If those are private and only reached via the `op_mat_edit` flow, the test must use the modal submit path.

    5. For integ.rs Overflow arms: identify the specific computation that triggers them (e.g., very large `f(x)` values).

    **Arm reachability classification (per the 32-04 quadratic_zero_leading_returns_domain pattern):** for each target arm in matrix.rs and integ.rs, classify it as REACHABLE (a normal state-machine path lands on it; test will be authored) or UNREACHABLE (the arm is a defensive `_ =>` that the type system prevents from being hit in normal state). Unreachable arms are documented in the SUMMARY and removed from the test target count.

    Output: a mapping table for each target line and the reach-path / REACHABLE-vs-UNREACHABLE classification.
  </action>
  <verify>
    <automated>grep -nE "match.*modal_program|ModalProgram::" hp41-core/src/ops/math1/mod.rs</automated>
  </verify>
  <done>Mapping table complete; routing arms in mod.rs identified; each matrix/integ arm classified REACHABLE or UNREACHABLE.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author hp41-core/tests/math1_mod_extra_coverage.rs (5–7 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/mod.rs (the routing arms identified in Task 1)
    - hp41-core/tests/math1_mod_entry_points.rs (avoid duplication)
    - hp41-core/tests/lint_math1_assertions.rs
  </read_first>
  <files>hp41-core/tests/math1_mod_extra_coverage.rs</files>
  <action>
    Create new file with disclaim header lines 1–2, file-doc, `#![allow(clippy::unwrap_used)]`, and imports per `<interfaces>`.

    **Tests** (5 mandatory, up to 7):
    - `submit_modal_dispatches_solve` — set `state.modal_program = Some(ModalProgram::Solve(SolveInputStep::Guess1Prompt))`, set entry_buf = "1", call `submit_modal(&mut state)`; assert that the state advanced to Guess2Prompt (or whichever next step) — this exercises the Solve routing arm in `submit_modal`.
    - `submit_modal_dispatches_poly` — similar for `ModalProgram::Poly(PolyInputStep::DegreePrompt)`.
    - `submit_modal_dispatches_difeq` — similar for `ModalProgram::Difeq(DifeqInputStep::OrderPrompt)`.
    - `submit_modal_dispatches_four` — similar for `ModalProgram::Four(FourInputStep::NoSamplesPrompt)` (or first numeric step).
    - `submit_modal_dispatches_trans` — similar for `ModalProgram::Trans(TransInputStep::*)`.
    - (Optional) `submit_modal_with_label_solve` — set up SOLVE `FunctionNamePrompt`, call with label "X", assert dispatch.
    - (Optional) `submit_modal_with_label_difeq` — set up DIFEQ `FunctionNamePrompt`, call with label, assert.

    Each test only needs to verify the routing fired (state advanced OR returned Ok) — NOT verify the per-program logic (that's covered by `math1_<area>.rs` tests). The goal is to cover the match arm in `mod.rs`, not the downstream behavior.

    **Assertion discipline**: `matches!(state.modal_program, Some(ModalProgram::<X>(<NextStep>)))` is fine — no decimal token on the line.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_mod_extra_coverage</automated>
    <automated>head -2 hp41-core/tests/math1_mod_extra_coverage.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_mod_extra_coverage.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 5 tests passing for REACHABLE routing arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count. Disclaim present, lint passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author hp41-core/tests/math1_matrix_error_branches.rs (6–8 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/matrix.rs (the lines named in the matrix target table)
    - hp41-core/tests/math1_matrix.rs + math1_matrix_flow.rs (existing patterns)
    - hp41-core/tests/lint_math1_assertions.rs
  </read_first>
  <files>hp41-core/tests/math1_matrix_error_branches.rs</files>
  <action>
    Create new file with the standard preamble. Tests cover (only REACHABLE arms per Task 1 classification):
    - `mat_index_out_of_range_returns_invalid_op` — set up a 2x2 matrix via dispatch, then attempt to read/write an element at index (5, 5) using whatever pub helper exposes this (identify in Task 1). Assert `Err(HpError::InvalidOp)`. Covers matrix.rs:88 + 101.
    - `mat_det_singular_returns_domain` — additional test for a 3x3 singular matrix if `math1_matrix.rs::inv_singular_2x2_sets_no_solution` only covers INV not DET.
    - `submit_step_ready_returns_invalid_op` — `matrix_submit_step(&mut s, MatrixInputStep::Ready)`; assert `Err(HpError::InvalidOp)`.
    - 4 additional `submit_step` arm tests covering REACHABLE lines among 408, 419, 460, 478 (one per arm identified in Task 1).
    - `mat_order_above_14_returns_out_of_range` — call `op_matrix_workflow`, set X=15, call `matrix_submit_step(&mut s, MatrixInputStep::OrderPrompt)`; assert `Err(HpError::OutOfRange)`. Mirrors `math1_matrix.rs::order_cap_15_returns_out_of_range` but exercises the submit_step path explicitly.

    **Assertion discipline**: same rules.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_matrix_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_matrix_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_matrix_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 6 tests passing for REACHABLE error arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author hp41-core/tests/math1_integ_error_branches.rs (6–8 tests)</name>
  <read_first>
    - hp41-core/src/ops/math1/integ.rs (lines named in integ target table)
    - hp41-core/tests/math1_integ.rs (avoid duplicating `integ_subdivision_cap_32768`)
    - hp41-core/tests/math1_user_callback.rs (overflow + cancel patterns)
    - hp41-core/tests/lint_math1_assertions.rs
  </read_first>
  <files>hp41-core/tests/math1_integ_error_branches.rs</files>
  <action>
    Create new file. Tests cover (only REACHABLE arms per Task 1 classification):
    - `integ_cancel_propagates` — set `state.cancel_requested.store(true, Ordering::SeqCst)` before running `op_integ_run_loop`; assert `Err(HpError::Canceled)`. Covers integ.rs:331.
    - `integ_step_overflow_returns_overflow` — user function returns extremely large value (e.g., set R0 = 1e308 and have program multiply); run loop; assert `Err(HpError::Overflow)`. Covers integ.rs:315 or 346.
    - `integ_accumulator_overflow_returns_overflow` — design a function whose Simpson partial sums exceed f64 range; assert Overflow. Covers integ.rs:391.
    - `integ_infinite_loop_guard_overflow` — pathological setup that triggers the infinite-loop guard at line 456.
    - `submit_step_ready_returns_invalid_op` — `integ_submit_step(&mut s, IntegInputStep::Ready)`; assert `Err(HpError::InvalidOp)`. Covers integ.rs:577.
    - `submit_step_invalid_arm` — exercise the InvalidOp arm at line 562 (Task 1 identifies the variant).
    - (Optional) `integ_call_depth_returns_call_depth` — deeply nested INTG calls; assert `Err(HpError::CallDepth)`. Covers integ.rs:225 / 229.

    **Assertion discipline**: same.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test math1_integ_error_branches</automated>
    <automated>head -2 hp41-core/tests/math1_integ_error_branches.rs | grep -q "independently re-derived"</automated>
    <automated>grep -c "// Catches:" hp41-core/tests/math1_integ_error_branches.rs</automated>
    <automated>cargo test -p hp41-core --test lint_math1_assertions</automated>
  </verify>
  <done>>= 6 tests passing for REACHABLE error arms; arms identified as UNREACHABLE (defensive `_ =>`) documented in SUMMARY.md per the 32-04 `quadratic_zero_leading_returns_domain` pattern. SUMMARY must list which arm IDs were covered, which were unreachable, and the resulting reach-confirmed test count.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Measure per-file coverage delta for matrix.rs, mod.rs, integ.rs</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md
  </read_first>
  <files>(no file writes)</files>
  <action>
    Run `cargo llvm-cov --package hp41-core --summary-only` and inspect all three target files. Verify:
    - `ops/math1/matrix.rs` >= 90.0 %
    - `ops/math1/mod.rs` >= 80.0 % (the 56.25 % -> 80 % is the realistic floor for a 32-LOC file; perfect 100 % may require touching defensive `_ =>` arms — settle for >= 80 % per this plan's `must_haves`; note that this is BELOW the ROADMAP success criterion 1 90 % math1 floor)
    - `ops/math1/integ.rs` >= 90.0 % (already above floor; this plan opportunistically lifts it).

    If any file is below its target, add 1–3 targeted tests OR (for mod.rs) document the remaining defensive arms as unreachable in the SUMMARY.

    **mod.rs explicit handling (per INFO 8 plan-checker feedback):** if `mod.rs` cannot reach the ROADMAP-locked 90 % through behavioral testing alone (some defensive `_ =>` arms genuinely unreachable from the state machine), document in SUMMARY as a known limitation. 32-10 Task 1's programmatic coverage gate will surface the gap explicitly; the team can then choose either:
      (a) author a follow-up plan that adds tighter routing-layer tests (e.g., direct invocation of the routing helper with synthetic `ModalProgram` enum values that bypass the normal state machine), or
      (b) author an ADR raising a per-file floor exception for `mod.rs` (e.g., "mod.rs floor at 80 % due to N defensive `_ =>` arms with no constructible reach-path; UNREACHABLE arms catalogued in 32-07-SUMMARY.md").

    Either path is acceptable; the choice belongs to the team AFTER 32-10's gate surfaces the precise gap. This plan ships at >= 80 % per its locked `must_haves` and does NOT block on the 90 % ROADMAP floor.
  </action>
  <verify>
    <automated>cargo llvm-cov --package hp41-core --summary-only | grep -E "ops/math1/(matrix|mod|integ)\.rs"</automated>
  </verify>
  <done>matrix.rs >= 90.0 %, mod.rs >= 80.0 % (or documented as `mod.rs floor at <pct> %` with the choice between follow-up plan vs. floor-exception ADR deferred to 32-10's gate-surfaced decision), integ.rs >= 90.0 %.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07-01 | Tampering | New test files' assertion discipline | mitigate | Each `// Catches:` names a bug class; assertions use `matches!` or `approx::assert_relative_eq!`. |
| T-32-07-02 | Information Disclosure | Free42 GPL contamination | mitigate | Verbatim disclaim header lines 1–2. |
| T-32-07-03 | Repudiation | Coverage padding | mitigate | Each test targets a specific source line range; tests assert error variants or state transitions, not coverage-by-execution. UNREACHABLE arms are documented in SUMMARY, NOT padded with stubs. |
| T-32-07-04 | Repudiation | `mod.rs` per-file floor deviation (80 % plan target vs 90 % ROADMAP SC-1 floor) is silently absorbed | mitigate | Task 5 explicitly surfaces the deviation in SUMMARY; 32-10 Task 1's programmatic gate will catch the gap and force the team to choose between follow-up plan vs. floor-exception ADR. No silent absorption. |
</threat_model>

<verification>
- `cargo test -p hp41-core --test math1_mod_extra_coverage` passes with >= 5 reach-confirmed tests
- `cargo test -p hp41-core --test math1_matrix_error_branches` passes with >= 6 reach-confirmed tests
- `cargo test -p hp41-core --test math1_integ_error_branches` passes with >= 6 reach-confirmed tests
- `cargo test -p hp41-core --test lint_math1_assertions` continues to pass
- All three new files carry the disclaim header
- `mod.rs` per-file coverage documented; deviation from 90 % ROADMAP floor (if any) surfaced for 32-10 Task 1 gate decision
</verification>

<success_criteria>
- `ops/math1/matrix.rs` line coverage >= 90.0 % (up from 89.68 %).
- `ops/math1/mod.rs` line coverage >= 80.0 % (up from 56.25 %; mod.rs is a 32-LOC file with defensive arms — 80 % is the realistic target for the routing layer; deviation from ROADMAP 90 % SC-1 floor surfaced for 32-10 gate decision).
- `ops/math1/integ.rs` line coverage stays >= 90.0 % (currently 90.86 %; this plan opportunistically lifts).
- Three new test files exist with all-passing tests.
- Reach-confirmed test count documented in SUMMARY (REACHABLE arms covered, UNREACHABLE arms catalogued).
- All frozen invariants preserved.
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-07-SUMMARY.md` with per-task completion, final coverage percentages, REACHABLE-vs-UNREACHABLE arm classification table, and any defensive arms documented as unreachable. If `mod.rs` lands below 90 %, document the deviation explicitly so 32-10 Task 1's gate decision is informed.
</output>
</content>
</invoke>