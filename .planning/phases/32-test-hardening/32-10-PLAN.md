---
phase: 32
plan: "32-10"
type: execute
wave: 2
depends_on:
  - 32-04
  - 32-05
  - 32-06
  - 32-07
  - 32-08
  - 32-09
files_modified:
  - README.md
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: false
requirements:
  - QUAL-01
gap_closure: true
tags:
  - graduation
  - coverage-gate
  - readme
  - claude-md
  - project-md
must_haves:
  truths:
    - "`cargo llvm-cov --package hp41-core --summary-only` reports lines >= 95.0 % AND regions >= 93.0 % on the post-32-04..32-09 codebase."
    - "Every `ops/math1/*.rs` source file reports lines >= 90.0 % per ROADMAP success criterion 1 (with documented exceptions if any defensive arms remain unreachable)."
    - "README.md v3.0 line graduated from soft-claim to OM-cited hard-claim: 'v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034' (per D-32.5)."
    - "CLAUDE.md `### v3.0 additions` header line removed the `Phases 28–32 — README hard-claim DEFERRED pending coverage gate` parenthetical."
    - "CLAUDE.md Phase 32 subsection's `Coverage gate >= 95 % UNMET — README hard-claim graduation DEFERRED` paragraph replaced with a `Coverage gate >= 95 % MET — README hard-claim graduated via gap-closure plans 32-04..32-09` paragraph citing the new plan IDs."
    - "PROJECT.md Active block coverage-gap note removed; v3.0 marked as fully shipped."
    - "Final ship commit embeds the `just coverage` output per D-32.6 provenance discipline."
    - "Coverage gate is enforced programmatically: Task 1's `<verify>` command exits non-zero when lines < 95.0 OR regions < 93.0, HARD-BLOCKING the Task 2 human checkpoint."
  artifacts:
    - path: "README.md"
      provides: "v3.0 OM-cited hard claim graduation"
      contains: "feature-complete per Owner's Manual 00041-90034"
    - path: "CLAUDE.md"
      provides: "Phase 32 closure narrative (DEFERRED -> MET)"
      contains: "Coverage gate >= 95 % MET"
    - path: ".planning/PROJECT.md"
      provides: "v3.0 fully shipped"
      contains: "v3.0.*shipped"
  key_links:
    - from: "README.md line 50"
      to: "docs/hp41-math1-divergences.md"
      via: "follow-up link in the v3.0 bullet"
      pattern: "documented divergences"
    - from: "CLAUDE.md Phase 32 subsection"
      to: "32-04-SUMMARY.md..32-09-SUMMARY.md"
      via: "plan-ID citations in the new MET paragraph"
      pattern: "Plan 32-(04|05|06|07|08|09)"
---

<objective>
Graduate the README v3.0 soft-claim to the OM-cited hard claim and update CLAUDE.md + PROJECT.md to reflect the closure of the QUAL-01 gap that Plan 32-03 Rule 4 had deferred. This plan is the SHIP commit for the gap-closure run — it depends on plans 32-04..32-09 having all shipped green.

Purpose: Per the gap_closure_directive, the user has explicitly REVERSED the Plan 32-03 Rule 4 deferral. After plans 32-04..32-09 close the coverage gap empirically, this plan:
1. Re-measures coverage with `cargo llvm-cov` AND enforces the >= 95 % / >= 93 % gate programmatically (Task 1 verify command exits non-zero when unmet).
2. Graduates the README hard-claim per D-32.5 (mirrors v2.2 HP-41CV pattern from D-30.9).
3. Updates CLAUDE.md and PROJECT.md to reflect the closure.
4. Commits via `/git-workflow:commit --with-skills` (per CLAUDE.md "Git Workflow" section) with the gate-verified coverage output embedded (D-32.6 provenance).

This is the FINAL commit for the gap-closure run. A blocking human checkpoint (`checkpoint:human-verify`) gates the README graduation — the user must confirm the OM-cited hard claim is appropriate before the commit lands. **The programmatic gate in Task 1 hard-blocks the human checkpoint when coverage is unmet** — closing the soft-gate hole that allowed Plan 32-03 Rule 4 to ship without explicit failure.

Output: 3 modified files (README.md, CLAUDE.md, .planning/PROJECT.md), 1 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-test-hardening/32-VERIFICATION.md
@.planning/phases/32-test-hardening/32-CONTEXT.md
@.planning/phases/32-test-hardening/32-03-SUMMARY.md
@.planning/phases/32-test-hardening/32-04-PLAN.md
@.planning/phases/32-test-hardening/32-05-PLAN.md
@.planning/phases/32-test-hardening/32-06-PLAN.md
@.planning/phases/32-test-hardening/32-07-PLAN.md
@.planning/phases/32-test-hardening/32-08-PLAN.md
@.planning/phases/32-test-hardening/32-09-PLAN.md

# Files being modified
@README.md
@CLAUDE.md

# The divergence catalog referenced by the new README claim
@docs/hp41-math1-divergences.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Measure final coverage and HARD-FAIL the gate when unmet</name>
  <read_first>
    - .planning/phases/32-test-hardening/32-VERIFICATION.md (the original gap measurement)
    - .planning/phases/32-test-hardening/32-04-SUMMARY.md through 32-09-SUMMARY.md (post-each-plan coverage)
  </read_first>
  <files>(no file writes)</files>
  <action>
    Run the full coverage gate to confirm the preconditions for README graduation per D-32.5 / D-32.6. **The gate MUST be enforced programmatically — a non-zero exit code from the verify command HARD-BLOCKS Task 2 (the human checkpoint). This closes the soft-gate hole that allowed Plan 32-03 Rule 4 to ship without explicit failure.**

    1. `cargo llvm-cov --package hp41-core --summary-only > /tmp/coverage_report.txt 2>&1` (retained for the commit body in Task 4).

    2. **PREFERRED PATH (single-command native gate)**: if the cargo-llvm-cov version pinned in `ci.yml` supports `--fail-under-lines` and `--fail-under-regions` flags (cargo-llvm-cov >= 0.5.34 per the upstream changelog), the gate is a single command:

       ```bash
       cargo llvm-cov --package hp41-core --fail-under-lines 95 --fail-under-regions 93 --summary-only
       ```

       Exits non-zero when either threshold is unmet. No awk needed.

    3. **FALLBACK PATH (awk on the TOTAL row)**: if the pinned cargo-llvm-cov version lacks the `--fail-under-*` flags, the verify block uses awk against the `cargo llvm-cov --summary-only` TOTAL row:

       ```bash
       cargo llvm-cov --package hp41-core --summary-only > /tmp/llvm-cov-32-10.txt
       LINES=$(awk '/^TOTAL/ { for (i=1;i<=NF;i++) if ($i ~ /%$/ && c++==1) { gsub("%","",$i); print $i; exit } }' /tmp/llvm-cov-32-10.txt)
       REGIONS=$(awk '/^TOTAL/ { for (i=1;i<=NF;i++) if ($i ~ /%$/ && c++==0) { gsub("%","",$i); print $i; exit } }' /tmp/llvm-cov-32-10.txt)
       awk -v L="$LINES" -v R="$REGIONS" 'BEGIN { if (L+0 < 95.0 || R+0 < 93.0) { print "GATE UNMET: lines=" L " regions=" R; exit 1 } else print "GATE MET: lines=" L " regions=" R }'
       ```

       (Column order: the `cargo llvm-cov --summary-only` TOTAL row prints Regions% before Lines% — `c++==0` matches the first `%` token = Regions, `c++==1` matches the second = Functions OR Lines depending on the version's column count. Verify the captured pair against the visual report once before the run and swap selectors if needed.)

    4. The verify block below combines both paths via shell `||` so the executor does not have to decide upfront — the native flag attempt runs first; if cargo-llvm-cov rejects the unknown flag with a non-zero exit, the awk fallback runs and produces the gate decision.

    5. Parse all `ops/math1/*.rs` rows from `/tmp/coverage_report.txt` — verify each >= 90.0 %.
    6. Parse `ops/program.rs` — verify >= 90.0 % (Plan 32-08 target).
    7. Capture the exact percentages for use in the commit message body (D-32.6 embed-coverage-output).

    **If the gate is NOT met** (lines < 95.0 OR regions < 93.0):
    - The verify command exits non-zero — `<automated>` block FAILS — execute-plan hard-blocks Task 2 (the human checkpoint).
    - Output a structured "Gate Unmet" report listing which files are below floor.
    - STOP and surface the issue to the user — this means an earlier plan (32-04..32-08) under-delivered and needs a follow-up task. The plan does NOT lower the gate.

    **If the gate IS met**: `/tmp/coverage_report.txt` is used in Task 4's commit body. Task 2 proceeds.
  </action>
  <verify>
    <automated>cargo llvm-cov --package hp41-core --fail-under-lines 95 --fail-under-regions 93 --summary-only || (cargo llvm-cov --package hp41-core --summary-only > /tmp/llvm-cov-32-10.txt && LINES=$(awk '/^TOTAL/ { for (i=1;i<=NF;i++) if ($i ~ /%$/ && c++==1) { gsub("%","",$i); print $i; exit } }' /tmp/llvm-cov-32-10.txt) && REGIONS=$(awk '/^TOTAL/ { for (i=1;i<=NF;i++) if ($i ~ /%$/ && c++==0) { gsub("%","",$i); print $i; exit } }' /tmp/llvm-cov-32-10.txt) && awk -v L="$LINES" -v R="$REGIONS" 'BEGIN { if (L+0 < 95.0 || R+0 < 93.0) { print "GATE UNMET: lines=" L " regions=" R; exit 1 } else print "GATE MET: lines=" L " regions=" R }')</automated>
  </verify>
  <done>Automated gate command exited 0 AND printed either native cargo-llvm-cov success output OR the awk-fallback line `GATE MET: lines=<L> regions=<R>` (NOT `GATE UNMET: ...`). Lines >= 95.0 %, regions >= 93.0 %, all math1 files >= 90 %, program.rs >= 90 %. Numeric figures captured for the commit body. **Verify failure HARD-BLOCKS Task 2.**</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The QUAL-01 / ROADMAP SC-1 coverage gate has been measured at >= 95 % lines and >= 93 % regions across `hp41-core`. Plans 32-04..32-09 collectively added ~50–80 risk-weighted tests across 9 new test files (poly_error_branches, trans_error_branches, four_error_branches, solve_error_branches, difeq_error_branches, matrix_error_branches, mod_extra_coverage, integ_error_branches, program_error_branches) plus the CR-01 + WR-01..07 cleanups. The Plan 32-03 Rule 4 deferral has been REVERSED per the user's explicit instruction; we are now ready to graduate the README v3.0 line.

    **Task 1's automated gate already enforced lines >= 95.0 % AND regions >= 93.0 %.** This checkpoint is for the documentation graduation, NOT the gate decision (the gate has already passed by definition — otherwise Task 1 would have failed and you would not see this checkpoint).
  </what-built>
  <how-to-verify>
    1. Confirm the coverage measurement from Task 1: `cargo llvm-cov --package hp41-core --summary-only | tail -5` — verify lines >= 95.0 % and regions >= 93.0 %.
    2. Review the proposed README change (Task 3 below will write the diff):
       - REMOVE: `- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry points, documented divergences)`
       - ADD: `- v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034 ([documented divergences](docs/hp41-math1-divergences.md))`
    3. Confirm the OM-cited "feature-complete per Owner's Manual 00041-90034" wording is appropriate. This is the hard claim that Plan 32-03 deferred — by approving here you confirm the claim is grounded in the just-measured coverage gate AND in the existing documented divergences in `docs/hp41-math1-divergences.md`.
    4. Confirm that "feature-complete" is the right word vs. "behaviorally complete" / "OM-compliant" / "behavioral emulation" alone. The CONTEXT.md D-32.5 locks the wording but if the user wants a more conservative graduation (e.g., "behaviorally complete per Owner's Manual 00041-90034") they can adjust here.
    5. Confirm that the deferral REVERSAL is correct — Plan 32-03 SUMMARY documented the Rule 4 architectural decision in detail; by approving here you confirm that the architectural decision is now superseded by the empirical gate closure.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the graduation. Type any other text to abort and surface concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md, CLAUDE.md, .planning/PROJECT.md to reflect closure</name>
  <read_first>
    - README.md (find the v3.0 line — per Plan 32-03 SUMMARY it's around line 50)
    - CLAUDE.md (find the `### v3.0 additions` header and the Phase 32 subsection — per Plan 32-03 SUMMARY the subsection is around line 155)
    - .planning/PROJECT.md (find the Active block referencing the coverage gap)
  </read_first>
  <files>
    - README.md
    - CLAUDE.md
    - .planning/PROJECT.md
  </files>
  <action>
    **README.md edit**:
    Find the line (currently around line 50): `- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry points, documented divergences)` (the wrapped form may span 2 lines — read 50–53 to grab the full bullet).

    Replace with (per D-32.5):
    ```markdown
    - v3.0 ships Math Pac I behavioral emulation, feature-complete per
      Owner's Manual 00041-90034
      ([documented divergences](docs/hp41-math1-divergences.md))
    ```

    Use the exact wording approved in Task 2 (if the user adjusted the wording during the checkpoint, use that instead of D-32.5's locked form).

    **CLAUDE.md edits** (two edits):

    1. Header line — find the `### v3.0 additions` block header (per Plan 32-03 SUMMARY it reads `Phases 28–32 — README hard-claim DEFERRED pending coverage gate`). Replace the `— README hard-claim DEFERRED pending coverage gate` parenthetical with nothing (just `### v3.0 additions` on its own line, OR `### v3.0 additions (Math Pac I Emulation, Phases 28–32)` — match the v2.2 header form).

    2. Phase 32 subsection — find the paragraph that begins with `**Coverage gate >= 95 % UNMET — README hard-claim graduation DEFERRED:**` (per Plan 32-03 SUMMARY this is the documented Rule 4 architectural decision). Replace with:

    ```
    **Coverage gate >= 95 % MET — README hard-claim graduated (post-Phase-32 gap-closure run, 2026-05-18):** the original Phase 32 ship measured 91.74 % lines / 92.14 % regions and deferred the README hard-claim to a v3.0.1 follow-up milestone per Rule 4. The user explicitly reversed that deferral and authorized an in-Phase-32 gap-closure run: Plans 32-04..32-09 added ~50–80 risk-weighted error-branch tests across 9 new `hp41-core/tests/math1_*_error_branches.rs` + `program_error_branches.rs` files plus the CR-01 + WR-01..07 cleanups from `32-REVIEW.md`. Final measurement (post-32-09): {LINES_PCT} % lines / {REGIONS_PCT} % regions on `hp41-core`, with all `ops/math1/*.rs` files >= 90 % per ROADMAP SC-1. The README v3.0 line graduated to the OM-cited hard claim "v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034" per D-32.5. The Plan 32-03 Rule 4 architectural decision is documented as superseded; no v3.0.1 follow-up needed.
    ```

    Substitute the actual {LINES_PCT} and {REGIONS_PCT} figures captured in Task 1.

    **PROJECT.md edits**:

    Find the Active block (around line 47–50 per Plan 32-03 SUMMARY) that notes "Coverage gate >= 95 % UNMET (91.74 % lines / 92.14 % regions) — README v3.0 hard-claim graduation DEFERRED to v3.0.1 per D-30.9 gate-conditional discipline" or similar. Replace with:

    ```
    v3.0 shipped 2026-05-18. Test/CI infrastructure plus README hard-claim
    graduation closed via post-Phase-32 gap-closure run (Plans 32-04..32-10).
    Final coverage: {LINES_PCT} % lines / {REGIONS_PCT} % regions.
    ```

    Update any `Last updated:` dateline to today's date.

    Cross-check that all 3 files build / lint cleanly:
    - README.md: no broken markdown links (the `docs/hp41-math1-divergences.md` link must resolve).
    - CLAUDE.md: not a build-checked file but follow the existing `### vX.Y additions` block structure.
    - PROJECT.md: same — follow the existing Active/Shipped/Inactive block structure.
  </action>
  <verify>
    <automated>grep -F "feature-complete per Owner's Manual 00041-90034" README.md</automated>
    <automated>grep -F "Coverage gate >= 95 % MET" CLAUDE.md || grep -F "Coverage gate ≥ 95 % MET" CLAUDE.md</automated>
    <automated>grep -F "README hard-claim DEFERRED" CLAUDE.md</automated>
    <automated>grep -F "DEFERRED to v3.0.1" .planning/PROJECT.md</automated>
    <automated>test -f docs/hp41-math1-divergences.md</automated>
  </verify>
  <done>README.md graduated to hard claim. CLAUDE.md DEFERRED markers replaced with MET. PROJECT.md Active block updated. All 3 files modified. The DEFERRED grep returns 0 (no stale deferral references). The divergence catalog link resolves.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final ship commit via /git-workflow:commit --with-skills (D-32.6 provenance)</name>
  <read_first>
    - /tmp/coverage_report.txt (captured in Task 1)
    - .planning/phases/32-test-hardening/32-04-SUMMARY.md through 32-09-SUMMARY.md (for the commit message body's plan-ID enumeration)
    - CLAUDE.md "Git Workflow" section: "Always use `/git-workflow:commit --with-skills` — never commit directly via `git commit`." Also "Commit language: English only."
  </read_first>
  <files>(no file writes — commits to git only)</files>
  <action>
    Stage README.md, CLAUDE.md, .planning/PROJECT.md, the 9 new test files created by plans 32-04..32-09, the 5 modified files from plan 32-09 (numerical_accuracy.rs cleanup, check-free42-contamination.sh fix, lint_math1_assertions.rs widening, math1_op_test_count.rs tightening, smoke.spec.js cleanup), and the 7 new SUMMARY files (32-04..32-10).

    **Run `/git-workflow:commit --with-skills` as the SOLE commit path** — this is mandated by CLAUDE.md "Git Workflow" section and is non-negotiable. No `git commit` direct invocation. No `gsd-sdk query commit` alternative. The `/git-workflow:commit --with-skills` skill handles the German Emoji Conventional Commits format AND ensures the commit hooks run AND records the commit in MEMORY.md project state.

    Commit subject template (English per CLAUDE.md `Commit language: English only`):

    ```
    docs(32): close QUAL-01 — README v3.0 hard-claim graduates
    ```

    Commit body template (English per CLAUDE.md — compose during execution; the skill will format the final body):

    ```
    Plan 32-03 Rule 4 deferred QUAL-01 / ROADMAP SC-1 to a v3.0.1 follow-up
    milestone (coverage was 91.74 % lines / 92.14 % regions). The user
    explicitly reversed that deferral and authorized an in-Phase-32
    gap-closure run.

    Plans 32-04..32-09 added ~50–80 risk-weighted error-branch tests:
    - 32-04: ops/math1/poly.rs 76.37% -> X% (math1_poly_error_branches.rs)
    - 32-05: ops/math1/{trans,four}.rs 81% -> X% (2 new test files)
    - 32-06: ops/math1/{solve,difeq}.rs 85% -> X% (2 new test files)
    - 32-07: ops/math1/{matrix,mod,integ}.rs -> X% (3 new test files)
    - 32-08: ops/program.rs 86.42% -> X% (program_error_branches.rs)
    - 32-09: CR-01 + WR-01..07 cleanup (review findings closed)
    - 32-10 (this plan): README graduation + CLAUDE.md/PROJECT.md update

    Per D-32.6, the gate-verified coverage output:

    ```
    {paste the TOTAL row + the per-math1-file rows from `cargo llvm-cov --package hp41-core --summary-only`}
    ```

    README v3.0 line graduated per D-32.5:
    - was: "Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ
      entry points, documented divergences)"
    - now: "v3.0 ships Math Pac I behavioral emulation, feature-complete per
      Owner's Manual 00041-90034"

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```

    The body MUST embed the `cargo llvm-cov --package hp41-core --summary-only` TOTAL row + the per-math1-file rows in a fenced code block per D-32.6 provenance discipline.

    To stage the files before invoking the skill, list each path explicitly (no `git add -A`):

    ```bash
    git add README.md CLAUDE.md .planning/PROJECT.md \
      hp41-core/tests/math1_poly_error_branches.rs \
      hp41-core/tests/math1_trans_error_branches.rs \
      hp41-core/tests/math1_four_error_branches.rs \
      hp41-core/tests/math1_solve_error_branches.rs \
      hp41-core/tests/math1_difeq_error_branches.rs \
      hp41-core/tests/math1_matrix_error_branches.rs \
      hp41-core/tests/math1_mod_extra_coverage.rs \
      hp41-core/tests/math1_integ_error_branches.rs \
      hp41-core/tests/program_error_branches.rs \
      hp41-core/tests/numerical_accuracy.rs \
      hp41-core/tests/lint_math1_assertions.rs \
      hp41-core/tests/math1_op_test_count.rs \
      scripts/check-free42-contamination.sh \
      hp41-gui/e2e/smoke.spec.js \
      .planning/phases/32-test-hardening/32-04-PLAN.md \
      .planning/phases/32-test-hardening/32-04-SUMMARY.md \
      .planning/phases/32-test-hardening/32-05-PLAN.md \
      .planning/phases/32-test-hardening/32-05-SUMMARY.md \
      .planning/phases/32-test-hardening/32-06-PLAN.md \
      .planning/phases/32-test-hardening/32-06-SUMMARY.md \
      .planning/phases/32-test-hardening/32-07-PLAN.md \
      .planning/phases/32-test-hardening/32-07-SUMMARY.md \
      .planning/phases/32-test-hardening/32-08-PLAN.md \
      .planning/phases/32-test-hardening/32-08-SUMMARY.md \
      .planning/phases/32-test-hardening/32-09-PLAN.md \
      .planning/phases/32-test-hardening/32-09-SUMMARY.md \
      .planning/phases/32-test-hardening/32-10-PLAN.md \
      .planning/phases/32-test-hardening/32-10-SUMMARY.md
    ```

    Then invoke `/git-workflow:commit --with-skills`, paste the subject template and the body template above into the skill's prompt. The skill emits the final commit with the German Emoji Conventional Commits prefix + the Co-Authored-By footer.
  </action>
  <verify>
    <automated>git log -1 --format="%s"</automated>
    <automated>git show --stat HEAD | head -30</automated>
    <automated>git log -1 --format="%b" | grep -F "feature-complete per Owner's Manual 00041-90034"</automated>
    <automated>git log -1 --format="%b" | grep -F "Co-Authored-By: Claude"</automated>
  </verify>
  <done>Final commit landed via /git-workflow:commit --with-skills. Subject contains "close QUAL-01". Body embeds the coverage output. README hard claim and divergence catalog link visible in the diff. Co-Authored-By footer present. Commit subject and body are in English per CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md graduation | Public-facing claim — touchable by any future contributor. Mitigated by the programmatic coverage gate in Task 1 AND the blocking human checkpoint in Task 2 (defense in depth — the user explicitly approves the wording AFTER the gate passes). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10-01 | Repudiation | README graduates to hard claim without verified gate | mitigate | Task 1's `<verify>` runs a programmatic gate command that exits non-zero when lines < 95.0 OR regions < 93.0. The failure HARD-BLOCKS Task 2's human checkpoint — the user never sees the graduation prompt if the gate is unmet. This closes the soft-gate hole that allowed Plan 32-03 Rule 4 to ship without explicit failure. |
| T-32-10-02 | Tampering | Graduation locks in incorrect documentation | mitigate | Blocking human checkpoint in Task 2 — the user reviews the exact wording before Task 3 writes the edit. Task 2's `<resume-signal>` requires explicit "approved" text. |
| T-32-10-03 | Information Disclosure | Commit message embeds coverage details that may reveal internal architecture | accept | Coverage percentages and file paths are already public via the open-source codebase; embedding the measurement output is D-32.6 provenance discipline, not a leak. |
| T-32-10-04 | Repudiation | Stale `DEFERRED` strings remain in CLAUDE.md / PROJECT.md after the graduation | mitigate | Task 3 verify step grep's for the DEFERRED marker — must return 0 in CLAUDE.md and PROJECT.md (no stale deferral references) after the edits. Plan 32-03 SUMMARY's DEFERRED references are preserved (they're historical) but the LIVE deferral markers in the v3.0 narrative are removed. |
| T-32-10-05 | Tampering | Commit bypasses CLAUDE.md "Git Workflow" mandate (`/git-workflow:commit --with-skills` only) | mitigate | Task 4 mandates the skill as the SOLE commit path; no `git commit` direct invocation; no `gsd-sdk query commit` alternative. Skill enforces German Emoji Conventional Commits format + hooks + MEMORY.md state. |
</threat_model>

<verification>
- `cargo llvm-cov --package hp41-core --summary-only` confirms lines >= 95.0 % AND regions >= 93.0 %
- Task 1's automated gate command exits 0 (non-zero would hard-block Task 2)
- `grep -F "feature-complete per Owner's Manual 00041-90034" README.md` matches
- `grep -F "Coverage gate >= 95 % MET" CLAUDE.md` (or the `>=` Unicode form) matches
- `grep -F "DEFERRED" CLAUDE.md | grep -v "Plan 32-03\|SUMMARY"` returns 0 (no live deferral markers; historical mentions in plan-citations preserved)
- `git log -1 --format="%b"` shows the embedded coverage output and the Co-Authored-By footer
- Human checkpoint in Task 2 was approved with explicit "approved" signal
- Commit landed via `/git-workflow:commit --with-skills` (per CLAUDE.md Git Workflow), subject + body in English
</verification>

<success_criteria>
- QUAL-01 / ROADMAP SC-1: `hp41-core` line coverage >= 95.0 % AND regions >= 93.0 % AND every `ops/math1/*.rs` file >= 90 %.
- Task 1's programmatic gate is the enforcement mechanism (NOT only the human checkpoint).
- README.md v3.0 line graduated to OM-cited hard claim per D-32.5.
- CLAUDE.md `### v3.0 additions` block reflects the closure (DEFERRED -> MET).
- .planning/PROJECT.md Active block reflects v3.0 fully shipped.
- Final commit message embeds the coverage output per D-32.6 provenance.
- Final commit landed via `/git-workflow:commit --with-skills` per CLAUDE.md mandate.
- All frozen invariants preserved across plans 32-04..32-10.
- Blocking human checkpoint approved before the graduation commit landed.
</success_criteria>

<output>
Create `.planning/phases/32-test-hardening/32-10-SUMMARY.md` with:
- Which gate path was used (native `--fail-under-*` flags vs awk fallback) and why
- Final coverage measurement (lines + regions + per-math1-file table)
- Confirmation that the human checkpoint was approved
- Diff summary of README.md / CLAUDE.md / PROJECT.md edits
- The final commit SHA + subject line
- Verification that no `DEFERRED` markers remain in the v3.0 narrative (CLAUDE.md / PROJECT.md live blocks)
- Confirmation that the commit was authored via `/git-workflow:commit --with-skills` (per CLAUDE.md)
</output>
</content>
</invoke>