---
plan_id: "32-03"
title: "E2E smoke extension + Free42 GPL-contamination guard + README hard-claim graduation"
phase: 32
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - hp41-gui/e2e/smoke.spec.js
  - scripts/check-free42-contamination.sh
  - justfile
  - .github/workflows/ci.yml
  - README.md
  - PROJECT.md
  - CLAUDE.md
autonomous: true
requirements_addressed: ["QUAL-03", "QUAL-05"]
threat_model_refs: ["T-32-01", "T-32-02", "T-32-03"]
---

<objective>
Extend `hp41-gui/e2e/smoke.spec.js` with two new `it()` blocks exercising Math Pac I workflows (`XEQ "SINH" 1 ENTER` → `1.1752` AND `XEQ "MATRIX" 2x2 DET` → `-2.0000`) per D-32.2/D-32.4, ship `scripts/check-free42-contamination.sh` per D-32.7 wired into both `just ci` and a separate `ci.yml::license-audit` parallel job per D-32.8, and graduate the README v3.0 line from soft-claim to OM-cited hard claim per D-32.5/D-32.6 in the final gate-verified ship commit.
</objective>

<must_haves>
- `hp41-gui/e2e/smoke.spec.js` contains 3 `it()` blocks total: the original `2 ENTER 3 + displays 5.0000` (unchanged per D-32.4), the new sinh(1) test (`XEQ "SINH" 1` → `1.1752`), and the new MATRIX 2x2 DET test (`XEQ "MATRIX" 2 R/S 1 R/S 2 R/S 3 R/S 4 R/S XEQ "DET"` → `-2.0000`). All 3 pass in `just gui-e2e` on Ubuntu within the `mochaOpts.retries: 1` budget per QUAL-03.
- `scripts/check-free42-contamination.sh` exists, is executable, runs `set -euo pipefail`, greps `hp41-core/src/ops/math1/` for the 12-symbol D-32.7 pattern with the disclaim allowlist, exits 0 on no matches and 1 on any match outside the allowlist per QUAL-05.
- `justfile` carries a `license-audit` recipe in the `[group('ci')]` group; the `ci` recipe is extended to `ci: lint test coverage license-audit` per D-32.8.
- `.github/workflows/ci.yml` carries a `license-audit` parallel job (sibling to `lint` / `test` / `coverage` / `msrv`) running the same script per D-32.8 (belt + suspenders).
- The 13 `hp41-core/src/ops/math1/*.rs` files each carry the verbatim disclaim header sentence "Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979); Free42 source consulted only as sanity-check oracle, not copied." (already in place per RESEARCH.md HIGH-confidence; this plan verifies; the script's allowlist preserves them).
- Final ship commit (after all 3 plans complete and `just coverage` confirms ≥ 95 %) updates `README.md` from soft-claim to the D-32.5 hard claim, updates `PROJECT.md` `Active` line + adds Phase 32 to the `Shipped` block, updates `CLAUDE.md` `### v3.0 additions` Phase 32 subsection (replacing the `(in progress)` stub from Phase 30), and embeds the `just coverage` output in the commit body per D-32.6.
</must_haves>

<threat_model>
**T-32-01: CI script supply-chain (severity: low)** — `scripts/check-free42-contamination.sh` runs in both `just ci` and `.github/workflows/ci.yml`. Mitigated by: bash-only (no curl/network), no external dependencies, `set -euo pipefail` hardens against pipeline silent failures, and `exit 1` on any unexpected condition. The script is checked into git so any modification surfaces in PR diff review.

**T-32-02: False-negative on Free42 contamination (severity: low)** — The 12-symbol allowlist might miss future Free42-specific identifiers added to the codebase. Mitigated by: D-32.7 locks the 12 symbols verbatim (`phloat|Phloat|bid128_|decNumber|decContext|vartype|arg_struct|prgm_lines|bcd_t|Thomas Okken|AGPL|GNU General Public License`); expansion requires an ADR per the discipline established for ADR-001/002/005; PR review remains the last line of defense per CLAUDE.md.

**T-32-03: E2E test bypass via `browser.execute()` fallback (severity: low)** — The documented fallback path (`browser.execute(() => window.__TAURI__.invoke('dispatch_op', ...))`) per RESEARCH.md Open Question #1 could in principle be abused to skip UI verification. Mitigated by: limit fallback to the single XEQ-by-name submit-key workflow per RESEARCH.md guidance — the primary click-sequence path (real-key simulation via `clickKey` helper) is the canonical path; the fallback applies only when XEQ-modal submit-key turns out to be brittle in Wave 0 reconnaissance. Document the choice (real-click vs `browser.execute`) in a leading comment on each new `it()` block so the test author's intent is preserved across maintenance.

No `high` severity threats — test-only phase, CI gate hardening, doc updates.
</threat_model>

<tasks>

<task id="32-03-01">
<action>
Wave 0 reconnaissance for the XEQ-by-name modal submit-key: read `hp41-gui/src/pending_input.ts` `handleModalKey` for the `'xeq_name'` case (line ~339 per RESEARCH.md grep) and identify which on-screen key id submits the modal once the label `acc` is fully accumulated. The candidates are:

1. **`enter`** — but `enter`'s `alphaChar='N'` means clicking it while accumulating `SINH` would APPEND `N` first (making the acc `SINHN`), then submit. Need to confirm whether `handleModalKey` distinguishes submission from alphaChar appending for the `enter` key when in `xeq_name` mode.
2. **A separate key** (e.g., a dedicated XEQ-confirm button or the secondary chord on `enter`) — if this is the case, the test sequence is unambiguous.
3. **No clean click path** — the modal expects a programmatic Tauri `invoke('dispatch_op', { keyId: 'xeq_SINH' })` for submission, in which case the E2E test uses the `browser.execute()` fallback per RESEARCH.md Open Question #1.

Document the findings in working notes. The decision tree:
- If candidate 1 with conditional logic distinguishing submit from append: the test uses real-click `clickKey('enter')` after the label is complete (the modal's submit handler runs before the alphaChar append).
- If candidate 2: the test uses `clickKey('<submit_key_id>')`.
- If candidate 3 (fallback): the test uses `browser.execute(() => window.__TAURI__.invoke('dispatch_op', { keyId: 'xeq_SINH' }))` per QUAL-03's "one Math Pac I workflow" wording — the workflow IS xeq_SINH → Op::Sinh → display 1.1752, regardless of which click sequence triggers it.

Repeat the analysis for the MATRIX flow: `XEQ "MATRIX"` submit + the R/S key role in the matrix modal (RESEARCH.md notes that R/S submits modal numeric input per D-28.5). Confirm the R/S key id in `hp41-gui/src/Keyboard.tsx` `KEY_DEFS` (likely `r_s` per the v2.1 keyboard layout documented in CLAUDE.md). The MATRIX 2x2 DET sequence: `clickKey('xeq_prompt')` → accumulate `M A T R I X` via alphaChar routing → submit → `clickKey('2')` `clickKey('r_s')` → accumulate the four values 1, 2, 3, 4 each followed by R/S → `clickKey('xeq_prompt')` → accumulate `D E T` → submit → assert LCD reads `-2.0000`.

Also re-confirm via grep that the alphaChar routing mapping for letters S/I/N/H/M/A/T/R/X/D/E/T is as RESEARCH.md sketches: S → key id '8'; I → 'cos'; N → 'enter'; H → 'sin'; M → ?; A → ?; T → ?; R → ?; X → ?; D → ?; E → ? — the X/M/A/T/R/D/E need to be read from `KEY_DEFS` in `hp41-gui/src/Keyboard.tsx` since RESEARCH.md only listed the SINH letters.

**Additionally, resolve the MATRIX input-order convention** (V-02 follow-up — prevents iteration delay in task 32-03-02). Read `hp41-core/tests/math1_matrix.rs` MAT-02 fixtures (the 2×2 SIMEQ / DET test setup) and confirm whether MAT-02 stores elements column-major (R15 = a11, R16 = a21, R17 = a12, R18 = a22) or row-major (R15 = a11, R16 = a12, R17 = a21, R18 = a22). Document the resolved convention so task 32-03-02's `[[1, 2], [3, 4]]` click sequence is unambiguous on first commit. (DET = -2 is invariant under transpose so the assertion passes either way — but the entry sequence in the test should match the actual MAT-02 convention to keep the test honest about what it exercises.)

This task is reconnaissance only — no code changes. Document a decision: "real-click path" or "browser.execute fallback" for each of the two new tests, AND the resolved MAT-02 input-order convention.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src/pending_input.ts (search for `handleModalKey` and the `'xeq_name'` case branch, likely line ~339)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src/Keyboard.tsx (read the `KEY_DEFS` block — confirm alphaChar mappings for M/A/T/R/X/D/E and the R/S key id)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src/App.tsx (search for the `handleClick` and `MODAL_OPENERS.xeq_prompt` references — confirm the xeq_name modal opening shape)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/e2e/smoke.spec.js (full 86-line file — confirm the existing `clickKey` helper and the assertion pattern)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_matrix.rs (read first 100 lines + the MAT-02 / SIMEQ / DET fixtures — confirm the column-major-vs-row-major input convention to resolve the V-02 follow-up)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-RESEARCH.md (§"Research priority #3" lines 306–370 — full reconnaissance discussion + fallback pattern; §"Open Questions Q1" lines 522–525)
</read_first>
<acceptance_criteria>
- Working notes capture: (a) the resolved submit-key strategy for the XEQ-by-name modal (real-click `clickKey('<id>')` or `browser.execute()` fallback), with the file:line evidence from `pending_input.ts` justifying the choice; (b) the complete alphaChar map for letters S, I, N, H, M, A, T, R, X, D, E (each letter → on-screen key id), referenced by file:line from `Keyboard.tsx`; (c) the R/S key id (likely `r_s` per CLAUDE.md v2.1 notes); (d) the modal-cancel key (if needed for graceful failure recovery); (e) the MAT-02 input-order convention (column-major or row-major) with file:line evidence from `math1_matrix.rs`.
- The decision is documented per-test (sinh(1) and MATRIX DET may pick different strategies independently).
- No code files modified in this task.
</acceptance_criteria>
</task>

<task id="32-03-02">
<action>
Add two new `it()` blocks to `hp41-gui/e2e/smoke.spec.js` inside the existing `describe('HP-41 GUI smoke (FN-QUAL-05, D-27.13 literal ROADMAP scope)', ...)` block per D-32.2. DO NOT modify the existing `it('2 ENTER 3 + displays 5.0000', ...)` block (D-32.4 — preserve bit-for-bit). DO NOT create a separate `describe` block. DO NOT modify `wdio.conf.cjs`. DO NOT modify `Display14Seg.tsx` (the `data-testid="lcd-display"` selector is already in place from Plan 27-04 per RESEARCH.md verification).

**Test 1: sinh(1) → 1.1752 (XEQ-by-name path)**

```
it('XEQ "SINH" 1 displays 1.1752 (Math Pac I via xrom_resolve)', async () => { ... })
```

Per task 32-03-01's resolved strategy:
- If real-click path: `clickKey('xeq_prompt')` → click each letter key with the alphaChar map (S/I/N/H) → click the resolved submit key → `clickKey('1')` → `clickKey('enter')` → `await browser.pause(250)` → assert `display.getAttribute('data-text') === '1.1752'`.
- If `browser.execute()` fallback: `clickKey('1')` → `clickKey('enter')` → `await browser.execute(() => window.__TAURI__.invoke('dispatch_op', { keyId: 'xeq_SINH' }))` → `await browser.pause(250)` → assert.

Mirror the exact `clickKey` invocation + `await browser.pause(250)` round-trip pause + explicit `null` / `!== expected` error branches from the existing `2 ENTER 3 +` test at lines 50–84. Include a leading comment on the `it()` block documenting the strategy choice (real-click vs fallback) and citing RESEARCH.md Open Question #1 + D-32.2.

**Test 2: MATRIX 2x2 DET → -2.0000 (modal pipeline path)**

```
it('XEQ "MATRIX" 2x2 DET displays -2.0000 (Math Pac I modal pipeline)', async () => { ... })
```

The matrix `[[1, 2], [3, 4]]` has det = `1·4 − 2·3 = −2`. Display reads `-2.0000` (FIX 4 default). Per task 32-03-01's resolved strategy and the R/S submit semantics from D-28.5:

- Open MATRIX modal: `clickKey('xeq_prompt')` → accumulate `MATRIX` via alphaChar routing → submit.
- Enter order 2: `clickKey('2')` → `clickKey('r_s')` (R/S submits modal numeric input per D-28.5).
- Enter four matrix values column-major (per MAT-02: order N in R14, elements column-major from R15) — for `[[1, 2], [3, 4]]` the column-major sequence is 1, 3, 2, 4: `clickKey('1')` `clickKey('r_s')` `clickKey('3')` `clickKey('r_s')` `clickKey('2')` `clickKey('r_s')` `clickKey('4')` `clickKey('r_s')`. (NOTE: re-confirm column-major order against the MAT-02 requirement and the `math1_matrix.rs` test fixtures before committing — the displayed convention may differ from internal storage; the test's expected output is `-2.0000` regardless of input order since det is invariant under transpose.)
- Open DET modal: `clickKey('xeq_prompt')` → accumulate `DET` via alphaChar routing → submit.
- `await browser.pause(500)` (give the determinant calculation extra time vs the 250ms used for atomic ops).
- Assert `display.getAttribute('data-text') === '-2.0000'`.

Per D-32.3: do NOT add an Esc-cancel verification step at the end. The natural workflow completion (modal_program clears when DET fires) is sufficient. Per RESEARCH.md, fallback to `browser.execute()` is acceptable if click sequence is brittle.

Total CI time added: ~40s per RESEARCH.md estimate. Both tests run only in the `e2e-linux` job on Ubuntu per D-27.15 AMENDED.

After landing, run `just gui-e2e` (or the equivalent CI invocation) locally to confirm both tests pass. Token-thrift note: `rtk just gui-e2e`.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/e2e/smoke.spec.js (full 86-line file — mirror the existing `it()` block shape bit-for-bit, including the assertion error branches at lines 70–80)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"hp41-gui/e2e/smoke.spec.js (MODIFIED — add 2 it() blocks inside existing describe)" — the 5 important-to-mirror rules)
- Working notes from task 32-03-01 (the resolved submit-key strategy + alphaChar map)
- /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/math1_matrix.rs (read first 100 lines + the SIMEQ section — confirm column-major input order convention against the MAT-02 requirement before committing the matrix entry sequence)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-CONTEXT.md (D-32.2 / D-32.3 / D-32.4 for the test scope; D-28.5 for R/S submit semantics; specifics block at lines 256–263 for the explicit expected values)
</read_first>
<acceptance_criteria>
- `hp41-gui/e2e/smoke.spec.js` contains exactly 3 `it()` blocks (verify: `grep -c "^    it(" hp41-gui/e2e/smoke.spec.js` returns 3, or equivalent count accounting for indentation).
- The existing `2 ENTER 3 + displays 5.0000` `it()` block is unchanged (verify: `grep -c '5.0000' hp41-gui/e2e/smoke.spec.js` returns ≥ 1; `grep -A 30 '2 ENTER 3' hp41-gui/e2e/smoke.spec.js` shows the same body as before this task).
- The sinh test `it()` block contains both `XEQ` and `SINH` in its description AND the literal assertion target `1.1752` (verify: `grep -c '1.1752' hp41-gui/e2e/smoke.spec.js` returns ≥ 1; `grep -c 'SINH' hp41-gui/e2e/smoke.spec.js` returns ≥ 1).
- The MATRIX test `it()` block contains `MATRIX` and `DET` in its description AND the literal assertion target `-2.0000` (verify: `grep -c '\-2.0000' hp41-gui/e2e/smoke.spec.js` returns ≥ 1; `grep -c 'MATRIX' hp41-gui/e2e/smoke.spec.js` returns ≥ 1).
- Each new `it()` block carries a leading comment documenting the click-strategy choice (real-click vs `browser.execute()` fallback) per T-32-03 mitigation.
- `just gui-e2e` (or `wdio run hp41-gui/wdio.conf.cjs`) passes all 3 `it()` blocks within the `mochaOpts.retries: 1` budget on the local Ubuntu CI surface (verify locally OR trust the CI invocation from a PR push if local Ubuntu is unavailable).
- `wdio.conf.cjs` is NOT modified (verify: `git diff --name-only -- hp41-gui/wdio.conf.cjs` returns empty).
- `Display14Seg.tsx` is NOT modified (verify: `git diff --name-only -- hp41-gui/src/Display14Seg.tsx` returns empty).
</acceptance_criteria>
</task>

<task id="32-03-03">
<action>
Create the NEW file `scripts/check-free42-contamination.sh` implementing the D-32.7 12-symbol grep policy with the disclaim allowlist. The script body (per the exact recipe in 32-PATTERNS.md §"scripts/check-free42-contamination.sh (NEW — CI script)" and 32-RESEARCH.md §"Free42 Contamination Guard Implementation Pattern"):

1. `#!/usr/bin/env bash` shebang at line 1.
2. Self-documenting header comment block (lines 2–9): script path + one-line purpose + Phase/Plan reference (Phase 32 Plan 32-03) + D-32.7 + D-32.8 + Pitfall 19 names.
3. `set -euo pipefail` (line 10).
4. Variables: `MATH1_DIR="hp41-core/src/ops/math1"` and `DISCLAIM_LINE='Free42 source consulted only as sanity-check oracle'`.
5. The PATTERN variable: `PATTERN='phloat|Phloat|bid128_|decNumber|decContext|vartype|arg_struct|prgm_lines|bcd_t|Thomas Okken|AGPL|GNU General Public License'` — the verbatim 12 symbols from D-32.7. The bare string `Free42` MUST NOT be in this pattern (RESEARCH.md HIGH-confidence: 122 legitimate `Free42 v3.0.5: …` cross-check references exist; the 12 listed symbols are tight enough to never match these).
6. The grep + allowlist: `if matches=$(grep -rn -E "$PATTERN" "$MATH1_DIR" | grep -v "$DISCLAIM_LINE"); then echo "FAIL: ..."; echo "$matches"; exit 1; fi`.
7. Success branch: `echo "OK: no Free42 contamination detected in $MATH1_DIR/"; exit 0`.

The script must be executable: `chmod +x scripts/check-free42-contamination.sh`.

Per Claude's Discretion in 32-CONTEXT.md: `set -euo pipefail` is recommended (hardening clause); printing line context via `grep -n` is already included (the `-n` flag is in the recipe).

After landing, verify against the current source tree that the script exits 0 (zero matches) — RESEARCH.md HIGH-confidence asserts the current `hp41-core/src/ops/math1/` source has zero matches against the 12-symbol pattern; if any match surfaces, treat it as either a real contamination (rare — should be triaged immediately) or a refinement needed in the PATTERN/allowlist (consider whether the matched symbol should be allowlisted via the DISCLAIM_LINE convention or by adding a second allowlist line for the specific case).

ALSO verify (manual + grep) that all 13 `hp41-core/src/ops/math1/*.rs` files carry the verbatim disclaim header sentence per RESEARCH.md HIGH-confidence ("Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979); Free42 source consulted only as sanity-check oracle, not copied."). The discovery grep is `grep -L 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/math1/*.rs` — this should return empty (i.e., zero files MISSING the header). If any file is missing the header, add it (verbatim sentence) before this plan ships.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/scripts/check-tauri-permissions.sh (full 32-line file — the exact template to adapt; matches D-32.7 + D-32.8 script architecture)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"scripts/check-free42-contamination.sh (NEW)" — full pattern map; the 4 important things to mirror; the gotcha about bare `Free42`)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-RESEARCH.md (§"Plan 32-03 / Free42 Contamination Guard Implementation Pattern" lines 372–438 — verified PATTERN, verified zero-match against current source)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-CONTEXT.md (D-32.7 — exact 12 symbols + allowlist sentence; D-32.8 — dual invocation rationale)
</read_first>
<acceptance_criteria>
- `scripts/check-free42-contamination.sh` exists and is executable (`test -x scripts/check-free42-contamination.sh` exits 0).
- The script's line 1 is `#!/usr/bin/env bash`.
- The script contains `set -euo pipefail` (verify: `grep -c 'set -euo pipefail' scripts/check-free42-contamination.sh` returns 1).
- The PATTERN line contains all 12 D-32.7 symbols (verify: `grep -cE 'phloat\|Phloat\|bid128_\|decNumber\|decContext\|vartype\|arg_struct\|prgm_lines\|bcd_t\|Thomas Okken\|AGPL\|GNU General Public License' scripts/check-free42-contamination.sh` returns ≥ 1 — single line containing all 12).
- The PATTERN line does NOT contain the bare string `Free42` as an alternation token (verify: `grep -E 'phloat\|Phloat\|.*\|Free42\|' scripts/check-free42-contamination.sh` returns nothing — the bare-Free42-as-alternation check fails).
- The script's allowlist references the disclaim sentence (verify: `grep -c 'Free42 source consulted only as sanity-check oracle' scripts/check-free42-contamination.sh` returns ≥ 1).
- Running `bash scripts/check-free42-contamination.sh` exits 0 against the current source tree (zero matches outside allowlist).
- Manual verification that all 13 math1 source files carry the verbatim disclaim header: `grep -L 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/math1/*.rs` returns empty.
</acceptance_criteria>
</task>

<task id="32-03-04">
<action>
Wire the contamination guard into both `justfile` and `.github/workflows/ci.yml` per D-32.8 (belt + suspenders).

**`justfile` modifications:**

Add a new `license-audit` recipe in the `[group('ci')]` group, placed right after the existing `coverage` recipe (around line 64 per RESEARCH.md). The recipe shape (per 32-PATTERNS.md §"justfile (MODIFIED)"):

```
# Phase 32 Plan 32-03 (D-32.7 + D-32.8 + Pitfall 19). Greps hp41-core/src/ops/math1/
# for distinctive Free42 identifiers (Intel BID, decNumber, Free42 internals + GPL/AGPL
# copyright markers) with allowlist for the legitimate per-file disclaim header.
# The matching CI job in ci.yml is named "License audit (Free42 contamination)" — keep
# script invocation in sync if the path ever changes.
[group('ci')]
license-audit:
	bash scripts/check-free42-contamination.sh
```

(Note: the recipe body MUST be TAB-indented per the rest of the justfile — strict TAB requirement per RESEARCH.md.)

Extend the existing `ci` recipe (currently `ci: lint test coverage` at line 67) to `ci: lint test coverage license-audit`. The doc-comment above the `ci` recipe should be updated to mention the Phase 32 D-32.8 belt+suspenders rationale, e.g., `# Full CI gate: lint → test → coverage → license-audit (Phase 32 D-32.8 belt+suspenders)`.

Do NOT modify the existing `ci-msrv` recipe (it stays as an MSRV-only smoke per RESEARCH.md).

**`.github/workflows/ci.yml` modifications:**

Add a new `license-audit` parallel job per D-32.8 (sibling to `lint` / `test` / `coverage` / `msrv` — no `needs:` dependency, runs in parallel). The shape (per 32-PATTERNS.md §".github/workflows/ci.yml (MODIFIED)" recommended-shape option B with `just` invocation for parity with local-dev):

```
  license-audit:
    name: License audit (Free42 contamination)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - run: just license-audit
```

The `taiki-e/install-action@v2 tool: just` step is included for parity with how the local-dev path invokes the script (via `just license-audit`) — keeps the CI invocation textually identical to the local invocation, making future debugging easier. The simpler bash-only option (skipping `just` install and running `bash scripts/check-free42-contamination.sh` directly) is also acceptable per 32-PATTERNS.md if speed is preferred over parity.

Placement in ci.yml: as a separate parallel job, not a step inside `lint` or `test` (visible audit trail in the GitHub PR checks panel — the D-32.8 rationale).

After landing, run `just ci` locally to confirm the full chain passes (lint + test + coverage + license-audit). Token-thrift note: `rtk just ci`.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/justfile (lines 56–70 — confirm the existing `coverage` and `ci` recipes; identify the exact insertion point for `license-audit`)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.github/workflows/ci.yml (full 90 lines — confirm the existing job structure; the `lint` job at lines 14–27 is the closest shape analog for a no-rust-needed job)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"justfile (MODIFIED — add license-audit recipe + extend ci)" and §".github/workflows/ci.yml (MODIFIED — add license-audit parallel job)" — full templates)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-RESEARCH.md (§"Justfile integration" + §"ci.yml integration" lines 440–461)
</read_first>
<acceptance_criteria>
- `justfile` contains a `license-audit:` recipe in the `[group('ci')]` group (verify: `grep -B 1 'license-audit:' justfile` shows the `[group('ci')]` attribute on the line above).
- The recipe body is `bash scripts/check-free42-contamination.sh` with TAB indentation (verify: `grep -A 1 'license-audit:' justfile | grep -c 'bash scripts/check-free42-contamination.sh'` returns 1).
- The `ci` recipe ends with `license-audit` (verify: `grep -E '^ci:' justfile` matches `ci: lint test coverage license-audit`).
- `.github/workflows/ci.yml` contains a `license-audit:` job (verify: `grep -c 'license-audit:' .github/workflows/ci.yml` returns ≥ 1; the job is at the top-level `jobs:` indentation, not nested inside another job).
- The job's `name:` is `License audit (Free42 contamination)` (verify: `grep -c 'License audit (Free42 contamination)' .github/workflows/ci.yml` returns 1).
- The job's `runs-on:` is `ubuntu-latest` (verify by reading the job body).
- The job has no `needs:` clause (verify: `grep -A 5 'license-audit:' .github/workflows/ci.yml | grep -c 'needs:'` returns 0).
- `just license-audit` exits 0 against the current source tree.
- `just ci` exits 0 (the full lint + test + coverage + license-audit chain passes locally).
</acceptance_criteria>
</task>

<task id="32-03-05">
<action>
**FINAL SHIP COMMIT — execute ONLY after all preceding tasks (32-01-*, 32-02-*, 32-03-01..04) have landed and verified green per CONTEXT.md D-32.6.**

Verify the gate one more time: run `just coverage` and capture the output (specifically the "Lines: 95.X%" + "Regions: 93.X%" lines). If line coverage is < 95.0 % OR regions < 93.0 %, STOP — surface this to the user and do not proceed with the README hard-claim graduation. The hard claim is gate-conditional per D-30.9 / D-32.5.

If the gate holds, perform 3 file edits in a single commit:

**1. `README.md` — graduate v3.0 line from soft-claim to hard claim per D-32.5.**

Find the current v3.0 soft-claim line under `## Features` (line ~50 per RESEARCH.md; the line wraps so read lines 50–52 to grab the complete bullet):

```
- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry points, documented divergences)
```

Replace with the D-32.5 OM-cited hard claim, moving the "documented divergences" reference to a follow-up link line:

```
- v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034
  ([documented divergences](docs/hp41-math1-divergences.md))
```

(Wrap the second line at the existing line-length convention; check the surrounding bullets for the prevailing wrap column — typically 80 or 100. Use the existing convention.)

**2. `PROJECT.md` — update Active line + add Phase 32 to Shipped block per D-32.6.**

Find the `Active` line (likely "Current focus: Phase 32 — test hardening" or similar in the milestone block) and graduate it to the Shipped block with the format established by Phase 27-31 entries. Example new line in Shipped: `- ✅ Phase 32 — Test Hardening (shipped <date>) — coverage gate held ≥ 95 % / numerical accuracy 700+ cases / E2E Math Pac I smoke / Free42 GPL-contamination guard`. Update the `Active` line to either point at the next milestone (v3.1 if a roadmap entry exists) or to mark v3.0 as fully shipped.

**3. `CLAUDE.md` — final population of Phase 32 subsection in `### v3.0 additions` block per D-32.6.**

Find the `### v3.0 additions` block (after the `### v2.2 additions` section per RESEARCH.md). The `(in progress)` stub for "Phase 32 (Test Hardening & Quality Gates)" left by Phase 30 (per CLAUDE.md `### v3.0 additions` section "Phase 32 (Test Hardening & Quality Gates) — (in progress)") is replaced by the full Phase 32 subsection. Mirror the structure of the existing `### v2.2 additions (Test Hardening, Phase 27)` subsection in CLAUDE.md: coverage-gate-held bullet, numerical accuracy bullet (566 → ~700), E2E smoke extension bullet, Free42 contamination guard bullet, lint_math1_assertions bullet, math1_op_test_count graduation bullet, xrom_shadowing graduation bullet. Each bullet cites the locked decision (D-32.X) and the relevant Pitfall (e.g., Pitfall 14, Pitfall 16, Pitfall 17, Pitfall 19) and the locked frozen-invariants line at the end (no `hp41-core/src/` changes, no `hp41-gui/src-tauri/src/` changes, MSRV 1.88 unchanged, save-file backward compat preserved).

Also update the "Status:" block at the top of CLAUDE.md to mark v3.0 as shipped (or to reflect Phase 32 as the last shipped phase, depending on prior conventions used for v1.0–v2.2 ship lines).

**Commit message** per D-32.6 — embed the `just coverage` output in the commit body. Use German Emoji Conventional Commits per CLAUDE.md (sticker emoji + type + scope + body in English per the "Commit language: English only" extension). Example shape:

```
✅ feat(32): ship v3.0 Math Pac I emulation behind ≥95% coverage gate

Graduates the README v3.0 line from soft-claim to OM-cited hard claim
per D-32.5. Updates PROJECT.md Active line + CLAUDE.md v3.0 additions
Phase 32 subsection per D-32.6. Embeds just coverage output below as
the gate-conditional proof.

Coverage gate (just coverage):

```
<paste the relevant lines from `just coverage` output here, specifically the Lines: 95.X% and Regions: 93.X% lines>
```

Refs: D-32.5, D-32.6, D-30.9, QUAL-01..08, v3.0 ship.
```

Use the `/git-workflow:commit --with-skills` workflow per CLAUDE.md (NEVER commit directly via `git commit`) and per the user's auto-memory ("Always use /git-workflow:commit --with-skills"). The commit is gate-verified per D-32.6 — the embedded coverage output IS the provenance link the discipline requires.

Token-thrift note: invoke as `rtk just coverage` to keep the verification cheap; the commit itself is one shot.
</action>
<read_first>
- /Users/daniel/GitRepository/hp41-calculator-emulator/README.md (read first 60 lines — locate the current v3.0 soft-claim bullet under `## Features` and its surrounding context; identify the line-length convention used for bullets)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/PROJECT.md (full file — locate the `Active` / `Shipped` block; observe how Phase 27, 28, 29, 30, 31 entries are formatted; mirror that shape for the new Phase 32 entry)
- /Users/daniel/GitRepository/hp41-calculator-emulator/CLAUDE.md (full file — locate `### v2.2 additions (Test Hardening, Phase 27)` as the structural template; locate the `### v3.0 additions` block with the `(in progress)` stub for Phase 32; observe the bullet style + decision-citation discipline in the v2.2 block for the mirror)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-PATTERNS.md (§"README.md (FINAL-COMMIT MODIFY ...)" — exact hard-claim wording per D-32.5)
- /Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/32-test-hardening/32-CONTEXT.md (D-32.5 + D-32.6 — full rationale + commit-message embed instruction)
</read_first>
<acceptance_criteria>
- `just coverage` exits 0 with line coverage ≥ 95.0 % AND regions ≥ 93.0 % (the gate-conditional precondition for this task) — captured output embedded in the commit body.
- `README.md` no longer contains the literal string `Math Pac I behavioral emulation (10 top-level programs` (verify: `grep -c 'Math Pac I behavioral emulation (10 top-level programs' README.md` returns 0).
- `README.md` now contains the literal string `v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034` (verify: `grep -c 'v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner.s Manual 00041-90034' README.md` returns 1 — note the `.s` to match either `'s` straight quote or curly).
- `README.md` contains a markdown link to `docs/hp41-math1-divergences.md` near the new claim (verify: `grep -c 'docs/hp41-math1-divergences.md' README.md` returns ≥ 1).
- `PROJECT.md` has graduated Phase 32 into the Shipped block (verify: `grep -c 'Phase 32' .planning/PROJECT.md` returns ≥ 1 and the line is in a Shipped/Complete section, not an Active section).
- `CLAUDE.md` has replaced the `Phase 32 (Test Hardening & Quality Gates) — (in progress)` stub with the full Phase 32 subsection (verify: `grep -c 'Phase 32 (Test Hardening' CLAUDE.md` returns ≥ 1; `grep -c 'Phase 32 (Test Hardening.*in progress' CLAUDE.md` returns 0 — i.e., the in-progress marker is gone).
- The CLAUDE.md Phase 32 subsection cites at least D-32.5, D-32.6, D-32.7, and Pitfall 19 (verify: `grep -c 'D-32.5\|D-32.6\|D-32.7\|Pitfall 19' CLAUDE.md` returns ≥ 4).
- The commit message body contains the verbatim `just coverage` Lines + Regions output (verify by running `git log -1 --pretty=%B` after the commit lands — the embedded coverage output is visible).
- The commit is created via `/git-workflow:commit --with-skills`, NOT via direct `git commit` (per CLAUDE.md mandate).
- Final post-commit check: `just ci` passes (lint + test + coverage + license-audit chain) AND `just gui-e2e` passes (on Ubuntu) AND the README claim is now hard, signaling v3.0 ship-ready.
</acceptance_criteria>
</task>

</tasks>

<verification>
After all 5 tasks land:

```bash
# E2E smoke (QUAL-03)
rtk just gui-e2e                                                                        # 3 of 3 it() blocks PASS (existing + sinh + matrix-det) on Ubuntu
rtk grep -c "^    it(" hp41-gui/e2e/smoke.spec.js                                       # ≥ 3
rtk grep -c '5.0000\|1.1752\|-2.0000' hp41-gui/e2e/smoke.spec.js                        # ≥ 3 (one per test)

# Free42 contamination guard (QUAL-05)
rtk just license-audit                                                                  # exit 0; "OK: no Free42 contamination detected"
rtk grep -c 'phloat\|Phloat\|bid128_\|decNumber\|decContext\|vartype\|arg_struct\|prgm_lines\|bcd_t\|Thomas Okken\|AGPL\|GNU General Public License' scripts/check-free42-contamination.sh   # ≥ 1 (single PATTERN line)
rtk grep -L 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/math1/*.rs   # empty (every math1 source file carries header)

# Justfile + CI wiring (D-32.8)
rtk grep -E '^ci:' justfile                                                             # matches "ci: lint test coverage license-audit"
rtk grep -A 5 'license-audit:' .github/workflows/ci.yml | grep -c 'ubuntu-latest'       # ≥ 1

# README + PROJECT.md + CLAUDE.md hard-claim graduation (D-32.5 / D-32.6)
rtk grep -c 'feature-complete per Owner.s Manual 00041-90034' README.md                 # = 1
rtk grep -c 'Phase 32 (Test Hardening.*in progress' CLAUDE.md                           # = 0 (stub replaced)
rtk grep -c 'Phase 32' .planning/PROJECT.md                                             # ≥ 1 (in Shipped block)

# Full chain
rtk just ci                                                                             # lint + test + coverage + license-audit all green
```

Final ship validation per D-32.6: the commit body embeds the `just coverage` Lines + Regions output as gate-conditional proof; `git log -1 --pretty=%B` shows the embedded output.
</verification>
</content>
</invoke>