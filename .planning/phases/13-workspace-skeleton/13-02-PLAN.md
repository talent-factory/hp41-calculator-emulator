---
phase: 13-workspace-skeleton
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/package.json
  - hp41-gui/vite.config.ts
  - hp41-gui/tsconfig.json
  - hp41-gui/index.html
  - hp41-gui/src/main.tsx
  - hp41-gui/src/App.tsx
  - hp41-gui/src/index.css
autonomous: true
requirements:
  - WSPC-01

must_haves:
  truths:
    - "hp41-gui/package.json exists with all npm dependencies listed per D-08"
    - "vite.config.ts includes both @vitejs/plugin-react and @tailwindcss/vite plugins with strictPort: true"
    - "index.css contains @import tailwindcss (Tailwind v4 CSS-only approach per D-10)"
    - "App.tsx renders an empty <div className=app> (no visible content, per D-09)"
    - "main.tsx uses ReactDOM.createRoot with React.StrictMode (per D-11)"
  artifacts:
    - path: "hp41-gui/package.json"
      provides: "npm manifest with react 19.2, @tauri-apps/api 2.11, and all dev deps"
      contains: "@tauri-apps/api"
    - path: "hp41-gui/vite.config.ts"
      provides: "Vite config with strictPort: true and Tailwind v4 plugin"
      contains: "strictPort: true"
    - path: "hp41-gui/tsconfig.json"
      provides: "Strict TypeScript config for Vite + React"
      contains: "moduleResolution"
    - path: "hp41-gui/index.html"
      provides: "Vite HTML entry with root div and main.tsx module import"
      contains: "id=\"root\""
    - path: "hp41-gui/src/main.tsx"
      provides: "React 19 entry point with StrictMode"
      contains: "ReactDOM.createRoot"
    - path: "hp41-gui/src/App.tsx"
      provides: "Minimal App component with empty app div"
      contains: "className=\"app\""
    - path: "hp41-gui/src/index.css"
      provides: "Tailwind v4 import"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "hp41-gui/index.html"
      to: "hp41-gui/src/main.tsx"
      via: "<script type=module src=/src/main.tsx>"
      pattern: "src/main.tsx"
    - from: "hp41-gui/src/main.tsx"
      to: "hp41-gui/src/App.tsx"
      via: "import App from './App'"
      pattern: "import App"
    - from: "hp41-gui/vite.config.ts"
      to: "hp41-gui/src-tauri/tauri.conf.json"
      via: "port 5173 must match devUrl in tauri.conf.json"
      pattern: "5173"
---

<objective>
Create the complete frontend scaffold for hp41-gui: npm manifest, Vite config, TypeScript
config, HTML entry point, and the three React source files (main.tsx, App.tsx, index.css).

All npm packages are installed in Phase 13 so Phase 14 (IPC) can immediately write React
components without a separate npm setup step (decision D-08).

Tailwind v4 is wired from day one (D-10): index.css contains @import "tailwindcss" and
vite.config.ts includes the @tailwindcss/vite plugin. No tailwind.config.js needed.

Purpose: The frontend scaffold enables `just gui-dev` to launch the Vite dev server and
Tauri window after npm dependencies are installed. It also establishes the React + TypeScript
conventions for all subsequent GUI phases.

Output: Seven files created. The files form a complete, buildable Vite + React frontend stub.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-workspace-skeleton/13-CONTEXT.md
@.planning/phases/13-workspace-skeleton/13-PATTERNS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create npm manifest and build tooling config (package.json, vite.config.ts, tsconfig.json)</name>

  <read_first>
    - .planning/phases/13-workspace-skeleton/13-PATTERNS.md — exact content for package.json, vite.config.ts, tsconfig.json sections
    - .planning/phases/13-workspace-skeleton/13-CONTEXT.md — D-08 (npm packages), D-10 (Tailwind), D-12 (gui-dev recipe calls npm run tauri dev)
    - .planning/research/STACK.md — version pinning for all npm packages
  </read_first>

  <files>
    hp41-gui/package.json
    hp41-gui/vite.config.ts
    hp41-gui/tsconfig.json
  </files>

  <action>
Create the directory hp41-gui/src/ first, then create three config files.

**Step 1: Create directories**
```
mkdir -p hp41-gui/src
```

**Step 2: Create hp41-gui/package.json**

Exact content (D-08, D-10, PATTERNS.md package.json section):

```json
{
  "name": "hp41-gui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11",
    "react": "^19.2",
    "react-dom": "^19.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11",
    "@tailwindcss/vite": "^4.3",
    "@types/react": "^19.2",
    "@types/react-dom": "^19.2",
    "@vitejs/plugin-react": "^6.0",
    "tailwindcss": "^4.3",
    "typescript": "^6.0",
    "vite": "^8.0"
  }
}
```

Key constraints:
- `"type": "module"` — required for ESM imports with Vite
- `"tauri": "tauri"` script — resolves to node_modules/.bin/tauri; enables `npm run tauri dev`
- `@tauri-apps/cli` and `@tauri-apps/api` both on `^2.11` — same minor line as Rust crate
  (Pitfall #1 prevention)
- `@tailwindcss/vite` and `tailwindcss` both on `^4.3` — required pair for Tailwind v4

**Step 3: Create hp41-gui/vite.config.ts**

Exact content (PATTERNS.md vite.config.ts section, STACK.md Integration Point #6):

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    port: 5173,
    strictPort: true,
    host: process.env.TAURI_DEV_HOST || 'localhost',
  },
  build: {
    outDir: 'dist',
  },
})
```

Key constraints:
- `strictPort: true` REQUIRED — tauri.conf.json hardcodes `http://localhost:5173`; if port is
  busy the dev server must fail rather than silently switching ports
- `process.env.TAURI_DEV_HOST` — allows iOS device testing if needed in future
- `outDir: 'dist'` — must match `frontendDist: "../dist"` in tauri.conf.json (one level up
  from src-tauri/ lands in hp41-gui/dist/)
- Both `react()` and `tailwindcss()` plugins required — Tailwind v4 uses Vite plugin approach

**Step 4: Create hp41-gui/tsconfig.json**

Exact content (PATTERNS.md tsconfig.json section):

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
```

Key constraints:
- `"moduleResolution": "bundler"` — correct for TypeScript 5+/6 with Vite; NOT "node" or "node16"
- `"jsx": "react-jsx"` — React 19 JSX transform; no `import React` needed in components
- `"strict": true` — enforces strict mode matching the Rust lint philosophy
- `"noEmit": true` — TypeScript only type-checks; Vite handles transpilation
- `"include": ["src"]` — scope to frontend sources only
  </action>

  <verify>
    <automated>test -f hp41-gui/package.json &amp;&amp; test -f hp41-gui/vite.config.ts &amp;&amp; test -f hp41-gui/tsconfig.json &amp;&amp; grep -c "strictPort" hp41-gui/vite.config.ts &amp;&amp; grep -c "@tauri-apps/api" hp41-gui/package.json</automated>
  </verify>

  <acceptance_criteria>
    - hp41-gui/package.json exists and contains `"@tauri-apps/api": "^2.11"`
    - hp41-gui/package.json contains `"@tauri-apps/cli": "^2.11"`
    - hp41-gui/package.json contains `"react": "^19.2"`
    - hp41-gui/package.json contains `"vite": "^8.0"`
    - hp41-gui/package.json contains `"tailwindcss": "^4.3"`
    - hp41-gui/package.json contains `"type": "module"`
    - hp41-gui/package.json contains `"tauri": "tauri"` (in scripts)
    - hp41-gui/vite.config.ts contains `strictPort: true`
    - hp41-gui/vite.config.ts contains `port: 5173`
    - hp41-gui/vite.config.ts contains `tailwindcss()` plugin import and usage
    - hp41-gui/tsconfig.json contains `"moduleResolution": "bundler"`
    - hp41-gui/tsconfig.json contains `"jsx": "react-jsx"`
    - hp41-gui/tsconfig.json contains `"strict": true`
  </acceptance_criteria>

  <done>
    Three config files created with correct content. strictPort: true present in vite.config.ts.
    @tauri-apps/api and @tauri-apps/cli on same minor version ^2.11.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create HTML entry point and React source files (index.html, src/main.tsx, src/App.tsx, src/index.css)</name>

  <read_first>
    - .planning/phases/13-workspace-skeleton/13-PATTERNS.md — exact content for index.html, main.tsx, App.tsx, index.css sections
    - .planning/phases/13-workspace-skeleton/13-CONTEXT.md — D-09 (empty App div), D-10 (Tailwind import), D-11 (React.StrictMode)
  </read_first>

  <files>
    hp41-gui/index.html
    hp41-gui/src/main.tsx
    hp41-gui/src/App.tsx
    hp41-gui/src/index.css
  </files>

  <action>
Create four files that form the React frontend entry point.

**Step 1: Create hp41-gui/index.html**

Exact content (PATTERNS.md index.html section):

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HP-41 Calculator</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Key constraints:
- `<div id="root">` — React mounts here; matches `document.getElementById('root')` in main.tsx
- `<title>HP-41 Calculator</title>` — matches tauri.conf.json window title (D-03)
- `type="module"` on script tag — required for ESM imports in Vite
- `/src/main.tsx` path — Vite resolves from the hp41-gui/ root

**Step 2: Create hp41-gui/src/main.tsx**

Exact content (PATTERNS.md main.tsx section, D-11):

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

Key constraints:
- `React.StrictMode` included — double-invokes effects in development to expose cleanup
  failures (directly prevents Pitfall #11: duplicate keyboard event listeners)
- `import './index.css'` — loads Tailwind styles from the CSS entry point
- `document.getElementById('root')!` — non-null assertion valid because index.html
  always has this element

**Step 3: Create hp41-gui/src/App.tsx**

Exact content (PATTERNS.md App.tsx section, D-09 — no visible content in Phase 13):

```tsx
function App() {
  return (
    <div className="app">
    </div>
  )
}

export default App
```

Key constraints:
- `className="app"` — CSS hook for Phase 15 (Display & Keyboard)
- No imports needed at Phase 13 — no state, no Tauri IPC, no child components
- Empty div satisfies success criterion SC-1 ("blank Tauri window")
- No `React` import needed — tsconfig "jsx": "react-jsx" handles the JSX transform

**Step 4: Create hp41-gui/src/index.css**

Exact content (PATTERNS.md index.css section, D-10 — Tailwind v4 CSS-only approach):

```css
@import "tailwindcss";
```

Key constraints:
- This single line is the complete Tailwind v4 setup when @tailwindcss/vite plugin is
  registered in vite.config.ts (no tailwind.config.js needed)
- No additional CSS rules in Phase 13 — layout and colors are Phase 15/16 responsibility
  </action>

  <verify>
    <automated>test -f hp41-gui/index.html &amp;&amp; test -f hp41-gui/src/main.tsx &amp;&amp; test -f hp41-gui/src/App.tsx &amp;&amp; test -f hp41-gui/src/index.css &amp;&amp; grep -c "createRoot" hp41-gui/src/main.tsx &amp;&amp; grep -c "@import" hp41-gui/src/index.css</automated>
  </verify>

  <acceptance_criteria>
    - hp41-gui/index.html contains `<div id="root"></div>`
    - hp41-gui/index.html contains `<script type="module" src="/src/main.tsx"></script>`
    - hp41-gui/index.html contains `<title>HP-41 Calculator</title>`
    - hp41-gui/src/main.tsx contains `ReactDOM.createRoot`
    - hp41-gui/src/main.tsx contains `React.StrictMode`
    - hp41-gui/src/main.tsx contains `import './index.css'`
    - hp41-gui/src/App.tsx contains `className="app"`
    - hp41-gui/src/App.tsx contains `export default App`
    - hp41-gui/src/index.css contains exactly `@import "tailwindcss";` (and nothing else)
  </acceptance_criteria>

  <done>
    Four frontend source files created. React StrictMode enabled. App renders an empty div.
    Tailwind v4 import in index.css. index.html title matches tauri.conf.json window title.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite dev server → Tauri webview | Port 5173 is hardcoded in both vite.config.ts and tauri.conf.json; mismatch causes silent blank window |
| npm scripts → @tauri-apps/cli | "tauri" script must resolve to node_modules/.bin/tauri; version must match Rust crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | Bundle identifier (tauri.conf.json) | mitigate | Plan 03 acceptance_criteria checks identifier = "ch.talent-factory.hp41"; wrong identifier causes silent runtime failure (Pitfall #4) — verified in Plan 03 |
| T-13-02-02 | Denial of Service | Vite port mismatch | mitigate | `strictPort: true` in vite.config.ts; acceptance_criteria confirms `strictPort: true` present; if port 5173 is busy the dev server fails fast (not silently switches) |
| T-13-02-03 | Tampering | npm package version drift | mitigate | @tauri-apps/api and @tauri-apps/cli both pinned to "^2.11"; acceptance_criteria grep checks confirm both present at same minor version |
| T-13-02-04 | Denial of Service | React StrictMode double-invoke | accept | StrictMode intentionally double-invokes effects to expose cleanup bugs (Pitfall #11); this is the correct prevention mechanism for duplicate IPC calls |
| T-13-02-05 | Information Disclosure | Tailwind class scanning | accept | Tailwind v4 with @tailwindcss/vite scans only files in the project; no global CSS leakage risk for a local desktop app |
</threat_model>

<verification>
After both tasks complete:

1. All seven files exist:
   `test -f hp41-gui/package.json hp41-gui/vite.config.ts hp41-gui/tsconfig.json hp41-gui/index.html hp41-gui/src/main.tsx hp41-gui/src/App.tsx hp41-gui/src/index.css`

2. Tailwind v4 wired:
   `grep "@import" hp41-gui/src/index.css` → `@import "tailwindcss";`
   `grep "tailwindcss" hp41-gui/vite.config.ts` → shows plugin import and usage

3. Port consistency:
   `grep "5173" hp41-gui/vite.config.ts` → confirms port 5173 configured

4. React StrictMode:
   `grep "StrictMode" hp41-gui/src/main.tsx` → confirms StrictMode wrapping

5. Version alignment:
   `grep "@tauri-apps" hp41-gui/package.json` → both api and cli show ^2.11
</verification>

<success_criteria>
- package.json lists all 11 npm packages from D-08 at correct version ranges
- vite.config.ts has strictPort: true and port 5173 (matches tauri.conf.json devUrl)
- vite.config.ts includes both @vitejs/plugin-react and @tailwindcss/vite plugins
- tsconfig.json uses moduleResolution: "bundler" (correct for TypeScript 6 + Vite)
- index.html has div#root and module script pointing to /src/main.tsx
- main.tsx wraps App in React.StrictMode (Pitfall #11 prevention for duplicate IPC calls)
- App.tsx renders empty div.app (satisfies SC-1: blank window; CSS hook for Phase 15)
- index.css contains only @import "tailwindcss" (Tailwind v4 CSS-only approach per D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/13-workspace-skeleton/13-02-SUMMARY.md` using the
template at `@/Users/daniel/.claude/get-shit-done/templates/summary.md`.

Key items to record in SUMMARY.md:
- All npm package versions as written in package.json (for Phase 14/15 reference)
- vite.config.ts port and outDir configuration (for Phase 14 frontend development)
- tsconfig.json compilerOptions summary (conventions for all future .tsx files)
- React.StrictMode is enabled — Phase 15 keyboard event handlers MUST include cleanup
  functions in useEffect (Pitfall #11)
</output>
