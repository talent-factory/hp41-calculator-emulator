---
phase: 07-hardening
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - .github/workflows/ci.yml
  - Justfile
autonomous: true
requirements: [QUAL-01, QUAL-05]
must_haves:
  truths:
    - "The CI test matrix runs `cargo build --release` on ubuntu-latest, macos-latest, and windows-latest"
    - "The CI matrix also runs `just test` on all three platforms"
    - "CI triggers on push and pull_request to main and develop branches"
    - "Coverage and lint jobs remain separate from the test matrix"
    - "The `bench-startup` recipe exists in the Justfile and documents the hyperfine command"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Cross-platform build gate for QUAL-01 and QUAL-05"
      contains: "cargo build --release"
    - path: "Justfile"
      provides: "bench-startup recipe for cold-start measurement"
      contains: "bench-startup"
  key_links:
    - from: ".github/workflows/ci.yml test matrix"
      to: "cargo build --release"
      via: "run step in matrix job"
      pattern: "cargo build --release"
    - from: "Justfile bench-startup"
      to: "hyperfine cold-start command"
      via: "just recipe"
      pattern: "hyperfine"
---

<objective>
Add `cargo build --release` to the CI test matrix (D-02) so cross-platform build
verification is enforced on every push and PR. Also add the `bench-startup` Justfile
recipe (D-09) that documents the manual pre-release cold-start measurement command.

Purpose: QUAL-01 (cold-start ≤ 0.5s) requires a release binary to exist and be measured.
QUAL-05 (single codebase on Windows/macOS/Ubuntu) requires the CI matrix to build the
release binary on all three platforms — not just run tests in debug mode.

Output: Updated .github/workflows/ci.yml with release build step in test matrix;
updated Justfile with bench-startup recipe.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-hardening/07-CONTEXT.md
</context>

<interfaces>
<!-- Current .github/workflows/ci.yml test job (from codebase read) -->
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - run: just test          # ← only step currently; needs build --release added

<!-- Current Justfile (from codebase read) -->
build:
    cargo build --workspace    # ← debug build; bench-startup needs release binary
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add cargo build --release to CI test matrix (D-02)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — understand current structure before modifying)
  </read_first>
  <action>
    In the `test` job, add a `cargo build --release` step BEFORE `just test`.
    The step must be a direct `cargo` call (not `just build`) because `just build`
    compiles in debug mode — we need the release binary to verify it links and compiles
    successfully on all three platforms.

    Replace the current steps section of the `test` job:

    ```yaml
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - run: just test
    ```

    With:

    ```yaml
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - name: Build release binary
        run: cargo build --release
      - name: Run tests
        run: just test
    ```

    Do NOT change:
    - The `lint` job (stays as-is)
    - The `coverage` job (stays as-is, stays on ubuntu-latest only per D-04)
    - The `on:` triggers (push/pull_request to main/develop)
    - The matrix OS list (ubuntu-latest, macos-latest, windows-latest)
    - The `fail-fast: false` setting

    The full updated file must be valid YAML. Double-check indentation (2-space
    throughout) and that the `name:` fields on the new steps are indented correctly
    under `steps:`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML valid')"` prints "YAML valid"
    - `grep -c "cargo build --release" .github/workflows/ci.yml` returns 1
    - `grep -c "just test" .github/workflows/ci.yml` returns 1
    - `grep -c "ubuntu-latest" .github/workflows/ci.yml` returns >= 3 (test matrix + lint job + coverage job)
    - `grep -c "macos-latest" .github/workflows/ci.yml` returns >= 1 (test matrix)
    - `grep -c "windows-latest" .github/workflows/ci.yml` returns >= 1 (test matrix)
    - The `coverage` job still has `needs: test` (verify with grep)
  </acceptance_criteria>
  <done>
    .github/workflows/ci.yml is valid YAML. The test matrix job runs both
    `cargo build --release` and `just test` on all three platforms.
    Coverage and lint jobs are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bench and bench-startup recipes to Justfile (D-09, D-12)</name>
  <files>Justfile</files>
  <read_first>
    - Justfile (full file — understand current recipe list and formatting style before appending)
  </read_first>
  <action>
    Append two new recipes to the end of the Justfile. Match the existing style exactly:
    one blank line between recipes, tab-indented recipe body, `# Comment` above each recipe.

    ```just
    # Run criterion benchmarks for hp41-core dispatch latency (advisory — does not gate CI)
    bench:
    	cargo bench -p hp41-core

    # Measure cold-start latency with hyperfine (manual pre-release step — not a CI gate)
    # Usage: just bench-startup
    # Prerequisite: just build-release (or cargo build --release) must be run first
    bench-startup:
    	hyperfine --runs 10 ./target/release/hp41-cli
    ```

    Note: The Justfile uses tab indentation for recipe bodies (not spaces). Ensure the
    recipe body lines use a real tab character, not spaces.

    Also add a `build-release` recipe immediately after the existing `build` recipe,
    so `bench-startup` has a clear dependency path:

    ```just
    # Build release binary (required before bench-startup)
    build-release:
    	cargo build --release
    ```

    Insert `build-release` after the existing `build:` recipe, before `test:`.

    The final recipe order in the Justfile should be:
      default → build → build-release → test → lint → run → coverage → ci → bench → bench-startup
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && just --list 2>&1 | grep -E "bench|build-release"</automated>
  </verify>
  <acceptance_criteria>
    - `just --list` shows `bench`, `bench-startup`, and `build-release` in the recipe list
    - `grep -c "bench:" Justfile` returns 1
    - `grep -c "bench-startup:" Justfile` returns 1
    - `grep -c "build-release:" Justfile` returns 1
    - `grep -c "hyperfine" Justfile` returns 1
    - `grep -c "cargo bench -p hp41-core" Justfile` returns 1
    - `just build-release` exits 0 (confirm the recipe is syntactically valid by running it)
  </acceptance_criteria>
  <done>
    Justfile has bench, bench-startup, and build-release recipes. `just --list` shows all
    three. `just build-release` exits 0 producing the release binary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow | GitHub Actions executes on push/PR — untrusted input (branch names, PR titles) must not reach shell commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | CI workflow YAML | mitigate | `cargo build --release` uses no user-controlled inputs; Swatinem/rust-cache is pinned via @v2 tag |
| T-07-02-02 | Denial of Service | bench-startup on missing binary | accept | Recipe documents prerequisite (`just build-release` first); user is the only operator of this recipe |
</threat_model>

<verification>
After completing both tasks:

1. Validate ci.yml YAML syntax:
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML valid')"
   Expected: "YAML valid"

2. Confirm release build step in matrix:
   grep "cargo build --release" .github/workflows/ci.yml
   Expected: one match

3. Confirm Justfile recipes:
   just --list
   Expected: bench, bench-startup, build-release all appear

4. Smoke test build-release recipe:
   just build-release
   Expected: exits 0, target/release/hp41-cli binary exists
</verification>

<success_criteria>
- .github/workflows/ci.yml passes YAML validation
- CI test matrix runs `cargo build --release` on all 3 platforms (per D-02)
- CI coverage job stays on ubuntu-latest only (per D-04, unchanged)
- Justfile has `bench`, `bench-startup`, and `build-release` recipes
- `just bench-startup` documents `hyperfine --runs 10 ./target/release/hp41-cli` (per D-09)
- `just bench` runs `cargo bench -p hp41-core` (per D-12)
- `just build-release` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening/07-02-SUMMARY.md`
using the template at @/Users/daniel/.claude/get-shit-done/templates/summary.md.
</output>
