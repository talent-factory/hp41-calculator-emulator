---
phase: 17-persistence-and-print-output
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified: []
autonomous: false
requirements:
  - PERS-01
  - PERS-02

must_haves:
  truths:
    - "Closing and reopening the GUI app restores stack/register state from ~/.hp41/autosave.json"
    - "A CLI v1.x save file with no print_buffer field loads in GUI without panic or error"
    - "The auto-save Rust thread runs independently of UI thread with std::thread::sleep(30s)"
    - "Both hp41-cli and hp41-gui read/write ~/.hp41/autosave.json"
    - "Executing PRX, PRA, or PRSTK causes formatted lines to appear in the scrollable print panel; the panel retains previous output"
  artifacts:
    - path: "hp41-gui/src-tauri/src/persistence.rs"
      provides: "All 6 persistence tests pass"
      contains: "test_roundtrip_fresh_state"
    - path: "hp41-gui/src/App.tsx"
      provides: "Print panel JSX renders when printPanelOpen is true"
      contains: "print-panel"
    - path: "hp41-gui/src/App.css"
      provides: "Print panel CSS rules"
      contains: "print-panel-content"
  key_links:
    - from: "cargo test (hp41-gui)"
      to: "persistence tests"
      via: "cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml"
      pattern: "test result: ok"
    - from: "npm run build (hp41-gui)"
      to: "TypeScript compilation"
      via: "cd hp41-gui && npm run build"
      pattern: "built in"
---

<objective>
Verify that Plans 17-01 and 17-02 are fully functional: all persistence tests pass, CLI pipeline does not regress, TypeScript compiles cleanly, and a live human-driven test confirms all 5 Phase 17 success criteria.

Purpose: Integration gate — no shipping until SC-1 through SC-5 are confirmed by a human interacting with the running app.

Output: Human approval of SC-1..SC-5; phase marked complete.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-persistence-and-print-output/17-CONTEXT.md
@.planning/phases/17-persistence-and-print-output/17-01-SUMMARY.md
@.planning/phases/17-persistence-and-print-output/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated test suite — Rust persistence + CLI pipeline + TypeScript build</name>
  <read_first>
    - .planning/phases/17-persistence-and-print-output/17-01-SUMMARY.md (confirm persistence.rs was created)
    - .planning/phases/17-persistence-and-print-output/17-02-SUMMARY.md (confirm App.tsx/App.css were updated)
  </read_first>
  <files></files>
  <action>
Run the following three commands in sequence. Each must succeed before proceeding to the next. Report the output of all three.

**Command 1 — GUI Rust tests (persistence module):**
```bash
cargo test --manifest-path /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src-tauri/Cargo.toml 2>&1
```

Expected: All 6 tests pass. The test output must contain:
- `test persistence::tests::test_roundtrip_fresh_state ... ok`
- `test persistence::tests::test_missing_file_returns_err ... ok`
- `test persistence::tests::test_corrupt_json_returns_err ... ok`
- `test persistence::tests::test_is_running_reset_on_load ... ok`
- `test persistence::tests::test_user_mode_roundtrip ... ok`
- `test persistence::tests::test_version_field_in_json ... ok`
- `test result: ok. 6 passed; 0 failed`

If any test fails: read the failure message, diagnose root cause (do not proceed to Command 2 until fixed).

**Command 2 — CLI pipeline (regression guard):**
```bash
cd /Users/daniel/GitRepository/hp41-calculator-emulator && just ci 2>&1
```

Expected: exits 0, same result as before Phase 17. The GUI persistence module must not affect the CLI pipeline.

If `just ci` fails: check whether the failure is pre-existing (compare with STATE.md known status) or Phase 17-introduced.

**Command 3 — TypeScript compilation:**
```bash
cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npm run build 2>&1
```

Expected: exits 0, output contains `built in` (Vite build success). Zero TypeScript errors.

If TypeScript errors appear, the most likely causes are:
- `printEndRef` typed incorrectly — must be `useRef<HTMLDivElement>(null)`
- `calcState.print_lines` not recognized — check that `CalcStateView` interface still has `print_lines: string[]`
- Missing import (check that `useRef` is still in the react import line)
  </action>
  <verify>
    <automated>cargo test --manifest-path /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src-tauri/Cargo.toml 2>&1 | grep "test result"</automated>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && just ci 2>&1 | tail -5</automated>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml` output contains `test result: ok. 6 passed; 0 failed`
    - `just ci` exits with code 0 (no regressions in CLI pipeline)
    - `cd hp41-gui && npm run build` exits with code 0 and output contains "built in" (Vite success message)
    - No "error[E...]" lines appear in any of the three command outputs
  </acceptance_criteria>
  <done>All automated gates pass: 6 Rust persistence tests OK, just ci clean, TypeScript compiles without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of SC-1 through SC-5</name>
  <what-built>
Plan 17-01 added: persistence.rs (state load on startup + 30s auto-save thread) to the GUI Rust backend.
Plan 17-02 added: collapsible print panel with auto-show, history accumulation, and auto-scroll to App.tsx and App.css.
  </what-built>
  <how-to-verify>
Start the GUI dev server:
```bash
cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npm run tauri dev
```

Wait for the window to open (takes ~10-20 seconds on first compile). Then verify each success criterion:

**SC-1: State persists across restarts**
1. In the GUI, press `3`, `ENTER`, `7`, `ENTER`, `+` — the display should show `10`
2. Close the app window (⌘Q or close button)
3. Reopen: `npm run tauri dev` again
4. PASS if: the display shows `10` and the stack X register shows `10`

**SC-2: CLI v1.x save file compatibility**
1. Run in terminal: `cargo run --bin hp41-cli -- 2>&1 | head -1` to confirm CLI launches
2. In hp41-cli: press `5`, ENTER, `3`, ENTER (stack has 3 in X, 5 in Y); press Ctrl+S or wait for auto-save; then Ctrl+C to exit
3. Reopen the GUI (`npm run tauri dev`)
4. PASS if: the GUI loads with `3` in X and `5` in Y (shared autosave.json read by GUI)
   Alternative SC-2 check (simpler): Look at `~/.hp41/autosave.json` in a text editor — it must NOT contain a `print_buffer` key (CLI saves don't have it). If the GUI loaded without error, SC-2 passes.

**SC-3: Auto-save is non-blocking**
1. Perform rapid key sequences in the GUI (e.g., hold down number keys, press +/- repeatedly)
2. Wait 30+ seconds while continuing to use the GUI
3. PASS if: the GUI remains responsive throughout (no freezes, no "Application Not Responding")

**SC-4: Shared autosave path**
1. After using the GUI, check: `cat ~/.hp41/autosave.json | head -5`
2. PASS if: the file exists and contains `"version": 1` and `"state":` fields

**SC-5: Print panel shows PRX/PRA/PRSTK output**
1. Type `3.14` in the GUI and press the key mapped to PRX (physical keyboard: check hp41-cli key bindings for PRX — it may require a multi-key sequence; alternatively type a value and use the GUI key for PRX if visible)
2. PASS if: a panel labeled "PRINT" appears below the keyboard showing the formatted value
3. Click the × button — PASS if the panel collapses (disappears) but the close button click does NOT clear the print history
4. Type another value and press PRX again — PASS if the panel re-opens showing BOTH the previous line and the new line
  </how-to-verify>
  <resume-signal>
Type "approved SC-1 SC-2 SC-3 SC-4 SC-5" if all pass, or describe which SCs failed with what was observed (e.g., "SC-1 FAIL: display shows 0 after restart, not 10").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester → running app | Verification of live behavior; no code changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Repudiation | SC verification | accept | Human approval is captured in resume-signal; SUMMARY.md records which SCs passed |
</threat_model>

<verification>
All verification is performed in Task 1 (automated) and Task 2 (human checkpoint). No additional steps required after checkpoint approval.
</verification>

<success_criteria>
Phase 17 is complete when:
- [ ] SC-1: State persists across GUI restarts (stack/registers restored from autosave.json)
- [ ] SC-2: CLI v1.x save files load in GUI without parse error or panic
- [ ] SC-3: GUI remains responsive during and after auto-save fires (30s background thread)
- [ ] SC-4: `~/.hp41/autosave.json` contains `"version": 1` and `"state":` — shared path confirmed
- [ ] SC-5: PRX/PRA/PRSTK output appears in the print panel; panel re-opens with full history after dismissal
- [ ] All 6 Rust persistence tests pass
- [ ] `just ci` exits 0 (no CLI regressions)
- [ ] `npm run build` exits 0 (TypeScript clean)
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/17-persistence-and-print-output/17-03-SUMMARY.md` with:
- Which SCs passed / failed (and resolution if any failed)
- Automated test results (6/6 persistence tests, just ci status, npm build status)
- Any issues encountered and how they were resolved
- Phase 17 completion declaration

Also update `.planning/STATE.md`:
- `current_phase: 18`
- `current_plan: "not-started"`
- `status: planning`
- `completed_phases: 5`
- `completed_plans: 12`
- Add Phase 17 to the completion record
</output>
