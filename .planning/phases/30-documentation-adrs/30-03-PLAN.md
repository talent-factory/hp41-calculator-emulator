---
phase: 30-documentation-adrs
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
  - 30-02
files_modified:
  - README.md
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-05
  - DOC-06
tags:
  - docs
  - readme
  - claude-md
  - project-md
  - v3-additions-block
must_haves:
  truths:
    - "`README.md` carries the verbatim-locked D-30.9 soft-claim line under `## Features`: `- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry points, documented divergences)`. The link `docs/hp41-math1-function-matrix.md` appears in the `## Documentation` table next to the existing hp41cv matrix link."
    - "`.planning/PROJECT.md` gains a Phase 28 + Phase 29 'Shipped' line and a 'Current focus: Phase 30 — Documentation & ADRs' update under the `## Project History` / Shipped milestones block per D-30.8 Claude's Discretion recommendation (b)."
    - "`CLAUDE.md` gains a new `### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)` section immediately after the existing `### v2.2 additions (Test Hardening, Phase 27)` block (ends at line 112) and before the `## Tech Stack` header (currently line 114). The new block fully populates Phase 28 + Phase 29 + Phase 30 subsections and carries stub headers `#### Phase 31 (GUI Integration) — (in progress)` and `#### Phase 32 (Test Hardening & Quality Gates) — (in progress)`."
    - "The hard claim 'feature-complete Math Pac I behavioral emulation' does NOT appear in README or CLAUDE.md (deferred to Phase 32 conditional on QUAL-01 coverage gate per D-30.9). Only the soft claim ships."
    - "Every link target referenced in the README v3.0 lines exists: `docs/hp41-math1-function-matrix.md` (created in Plan 30-01) is a live file path."
  artifacts:
    - path: "README.md"
      provides: "v3.0 soft-claim + matrix link"
      contains: "Math Pac I behavioral emulation"
    - path: ".planning/PROJECT.md"
      provides: "v3.0 milestone progress lines"
      contains: "Phase 28"
    - path: "CLAUDE.md"
      provides: "v3.0 additions block (Phases 28–30 populated, 31–32 stub)"
      contains: "### v3.0 additions (Math Pac I Emulation"
  key_links:
    - from: "README.md::## Features"
      to: "docs/hp41-math1-function-matrix.md"
      via: "Documentation table row"
      pattern: "hp41-math1-function-matrix"
    - from: "CLAUDE.md::### v3.0 additions"
      to: ".planning/phases/28-…/28-CONTEXT.md, …/29-CONTEXT.md"
      via: "Synthesized decision summaries (D-28.x, D-29.x references)"
      pattern: "D-28\\.|D-29\\."
    - from: "CLAUDE.md::### v3.0 additions Phase 30 subsection"
      to: "docs/adr/v3.0-{001,002,005}-*.md + docs/hp41-math1-divergences.md"
      via: "Cross-references within the subsection body"
      pattern: "v3\\.0-00[125]\\|hp41-math1-divergences"
---

<objective>
Land the user-facing v3.0 narrative in README + PROJECT.md + CLAUDE.md. The soft-claim wording is locked verbatim by D-30.9; the v3.0 additions block heading and ordering are locked by D-30.8. This is the lock-in moment for the v3.0 milestone story across all three briefing files.

Purpose: a reader landing on README sees "Math Pac I behavioral emulation included" with a link to the function matrix. A developer landing on CLAUDE.md sees the v3.0 additions block alongside the existing v1.x / v2.x blocks — same structural shape, same depth, lock-in moments captured. The hard claim "feature-complete Math Pac I" is deferred to Phase 32 conditional on the coverage gate.

Output:
- `README.md` (modified: v3.0 feature bullet + Documentation table row)
- `.planning/PROJECT.md` (modified: Shipped milestones + Current focus lines)
- `CLAUDE.md` (modified: new `### v3.0 additions` block inserted after the v2.2/Phase 27 block)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-documentation-adrs/30-CONTEXT.md
@.planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md
@.planning/phases/29-cli-integration/29-CONTEXT.md
@CLAUDE.md
@README.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- README.md current structure (insertion targets): -->
- `## Features` starts at line 37
- `## Documentation` table starts at line 110, with entries: `[Function Matrix](docs/hp41cv-function-matrix.md)` at line 116
- `## Documented Divergences from HP-41 Hardware` at line 121

<!-- CLAUDE.md current structure: -->
- `### v2.2 additions (HP-41CV Feature Completeness, Phases 20–25)` block: lines 89–101
- `### v2.2 additions (Test Hardening, Phase 27)` block: lines 103–112
- `## Tech Stack` header: line 114
- INSERTION POINT for `### v3.0 additions`: BETWEEN line 112 and line 114 (after the Test Hardening block, before Tech Stack)

<!-- PROJECT.md current structure: -->
- `## Project History` header: line 34
- "Shipped milestones:" sub-header: line 36
- Existing shipped entries: lines 37–41 (v1.0 through v2.2)
- "Active (v3.0 — Math 1 Pac Emulation)": line 163
- INSERTION POINTS: (a) lines 41–42 for two new Shipped entries (Phase 28 + Phase 29) and (b) line 164 area for "Current focus" update

<!-- Verbatim-locked D-30.9 README soft-claim wording: -->

```
- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry
  points, documented divergences)
```

<!-- Verbatim-locked D-30.8 CLAUDE.md heading: -->

```
### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v3.0 soft-claim and matrix link to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file — small, ~160 lines — to see the exact "Calculator engine (`hp41-core`)" feature-list shape and the Documentation table shape)
    - .planning/phases/30-documentation-adrs/30-CONTEXT.md (D-30.9 verbatim soft-claim wording)
  </read_first>
  <action>
Edit `README.md` to add two pieces:

**Insertion 1 — v3.0 soft-claim under `## Features`:**

Locate the `**Calculator engine (`hp41-core`)**` sub-list (starts at line 41). Add a new bullet at the END of that sub-list (just before the next bold sub-header `**Terminal UI (`hp41-cli`)**`). The new bullet uses the D-30.9 VERBATIM-LOCKED wording (no paraphrasing — the wording is fixed):

```
- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry
  points, documented divergences) — see [Math Pac I Function Matrix](docs/hp41-math1-function-matrix.md)
```

The inline matrix link goes on the same bullet (per D-30.9 + Claude's Discretion in CONTEXT.md). The line wrap matches the surrounding bullet style (existing bullets in this section run to ~80 chars per line; the wrap puts "points, documented divergences" on the second line).

Place this bullet AFTER the existing v2.2 "Persistent state via JSON" bullet (which is the last bullet in the `hp41-core` sub-list) and BEFORE the next bold header. This keeps the feature progression chronological within the section.

**Insertion 2 — Matrix link in `## Documentation` table:**

The current Documentation table has an entry at line 116:

```
| [Function Matrix](docs/hp41cv-function-matrix.md) | Per-op status, keyboard path, divergences |
```

Add a NEW row immediately after this one (before the "Keyboard Layout" row at line 117):

```
| [Math Pac I Function Matrix](docs/hp41-math1-function-matrix.md) | Math Pac I XROM entries with module/function IDs |
```

This puts the v3.0 matrix link directly alongside the v2.2 matrix link in the user-facing documentation index.

**Do NOT touch:**
- The `## Documented Divergences from HP-41 Hardware` section (line 121+) — its v2.2 content stays unchanged; Math Pac I divergences are documented in the new `docs/hp41-math1-divergences.md` (Plan 30-02) which is reachable via the matrix link — no need to inline them in README.
- Any v2.x feature bullets — they remain accurate.
- The `## Variants Emulated` table (lines 71–80) — Math Pac I is software-emulated, not a variant difference.

**Do NOT add the hard claim** "feature-complete Math Pac I behavioral emulation" anywhere in README — D-30.9 explicitly defers the hard claim to Phase 32 conditional on QUAL-01 coverage gate.

After both insertions, run `git diff README.md | head -40` to sanity-check that only ~3 lines were added (the bullet + the table row).
  </action>
  <verify>
    <automated>grep -qF "Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry" README.md && grep -qF "docs/hp41-math1-function-matrix.md" README.md && ! grep -qF "feature-complete Math Pac I" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry" README.md` returns 1 match (verbatim-locked D-30.9 soft-claim wording present)
    - `grep -cF "docs/hp41-math1-function-matrix.md" README.md` returns ≥ 2 (link appears in BOTH the Features bullet AND the Documentation table)
    - `grep -cF "docs/hp41cv-function-matrix.md" README.md` returns 1 (existing v2.2 link UNCHANGED — count from HEAD must be preserved)
    - `grep -cF "feature-complete Math Pac I" README.md` returns 0 (hard claim NOT present — deferred per D-30.9)
    - `git diff --stat README.md` shows `+3` or `+4` insertions (single bullet that wraps + single table row); 0 deletions
  </acceptance_criteria>
  <done>Soft-claim bullet present under `## Features` with inline matrix link; matrix link also appears in Documentation table; hard claim deferred per D-30.9.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md Shipped milestones and Current focus lines</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 34–46 (the Shipped milestones block) and lines 160–170 (the `### Active (v3.0 — Math 1 Pac Emulation)` block)
    - .planning/phases/30-documentation-adrs/30-CONTEXT.md (D-30.8 Claude's Discretion bullet — recommendation (b) for PROJECT.md: concise milestone lines, not the full additions block)
  </read_first>
  <action>
Edit `.planning/PROJECT.md` per D-30.8 Claude's Discretion recommendation (b): high-level milestone-progress lines only, NOT the full additions block (that lands in CLAUDE.md in Task 3).

**Insertion 1 — Two new Shipped entries** (after the v2.2 entry at line 41):

```
- v3.0 Math Pac I Emulation — IN PROGRESS (Phases 28–32)
  - Phase 28 XROM Framework + Math Pac I Core Ops (2026-05-16) — `hp41-core` only; 10 plans; ~40 new Op variants; XROM resolver chain fires LAST; modal-workflow state machine; user-callback re-entrancy infrastructure; 5 irreversible decisions locked (ADR-001 through ADR-005)
  - Phase 29 CLI Integration (2026-05-17) — `hp41-cli` only; 3 plans; `xeq_by_name_local_resolve` wired to `xrom_resolve`; second `OnceLock<Vec<HelpEntry>>` for Math Pac I JSON; ~40 new `op_display_name` arms; modal-prompt routing through `print_buffer`
  - Phase 30 Documentation & ADRs — IN PROGRESS — `docs/` + tooling only; 3 plans; matrix-renderer two-input extension; 3 new ADRs; divergence catalog expansion; v3.0 narrative across README/PROJECT.md/CLAUDE.md
```

Use real ship dates from STATE.md (Phase 28 = 2026-05-16; Phase 29 = 2026-05-17). The Phase 30 line shows "IN PROGRESS" because Phase 30 itself is what's shipping; mark it complete only when this plan is verified and committed.

**Insertion 2 — Update Current focus / Active block:**

The existing `### Active (v3.0 — Math 1 Pac Emulation)` block at line 163 currently reads:

```
*Requirements werden in dieser Milestone-Phase via Research → REQUIREMENTS.md generiert. Stand 2026-05-16: Scope ist Math 1 Pac only; XROM-Framework + Math-1-Funktionsbibliothek (Matrix/Komplex/Polynom/Integration/Solver/Vektor). Stat 1 → v3.1.*
```

Replace this italicized line with an updated status reflecting Phase 30 IN PROGRESS:

```
*Stand 2026-05-17: Phase 28 + Phase 29 shipped (13/13 plans, 13/13 success criteria verified). Phase 30 (Documentation & ADRs) IN PROGRESS — landing now. Phase 31 (GUI Integration) and Phase 32 (Test Hardening) planned. Scope is Math 1 Pac only; XROM-Framework + Math-1-Funktionsbibliothek (Matrix/Komplex/Polynom/Integration/Solver/Vektor). Stat 1 → v3.1.*
```

**Do NOT touch:**
- The `## Requirements` section (lines 51+) — Phase 28/29/30 requirements live in REQUIREMENTS.md; PROJECT.md tracks high-level shipped state, not per-phase requirement detail.
- The `## What This Is` description (line 43+) — accurate; mentions "v2.0 will add a Tauri-based graphical desktop app" which is HISTORICAL (v2.0 shipped); leave as-is unless explicitly asked to refresh historical narrative.
- The `### Out of Scope` block (line 167+) — v3.x scope locks are still accurate.
- The `## Context` / `## Constraints` / `## Key Decisions` / `## Evolution` sections — none need updating for Phase 30 documentation.

**Date selection:** use `2026-05-17` (today) for the "Stand" line — matches the date the STATE.md `last_updated` field carries, and matches the date Phase 30 is being planned.
  </action>
  <verify>
    <automated>grep -qF "v3.0 Math Pac I Emulation" .planning/PROJECT.md && grep -qF "Phase 28 XROM Framework + Math Pac I Core Ops (2026-05-16)" .planning/PROJECT.md && grep -qF "Phase 29 CLI Integration (2026-05-17)" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "v3.0 Math Pac I Emulation" .planning/PROJECT.md` returns ≥ 1 (new milestone line present)
    - `grep -cF "Phase 28 XROM Framework + Math Pac I Core Ops (2026-05-16)" .planning/PROJECT.md` returns 1 (Phase 28 ship line with real date)
    - `grep -cF "Phase 29 CLI Integration (2026-05-17)" .planning/PROJECT.md` returns 1
    - `grep -cF "Phase 30 Documentation & ADRs — IN PROGRESS" .planning/PROJECT.md` returns 1
    - `grep -cF "Stand 2026-05-17" .planning/PROJECT.md` returns 1 (Active block date refreshed)
    - `grep -cF "v2.2 HP-41CV Feature Completeness (2026-05-15)" .planning/PROJECT.md` returns 1 (existing v2.2 milestone line UNCHANGED)
    - W3 git provenance check: `[ -n "$(git log --before=2026-05-18 -- .planning/phases/28-xrom-framework-math-pac-i-core-ops | head -1)" ]` succeeds — confirms Phase 28 directory existed by 2026-05-17, supporting the (2026-05-16) ship-date claim.
    - `git diff --stat .planning/PROJECT.md` shows ≤ 8 line insertions + ≤ 2 line deletions (single Active-block line replaced; 4 lines added to Shipped milestones)
  </acceptance_criteria>
  <done>Shipped milestones block gains v3.0 entries for Phase 28 / Phase 29 / Phase 30 IN PROGRESS; Active v3.0 block date refreshed to 2026-05-17.</done>
</task>

<task type="auto">
  <name>Task 3: Insert v3.0 additions block into CLAUDE.md after the v2.2 Phase 27 block</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md lines 89–114 (BOTH v2.2 additions blocks — Feature Completeness AND Test Hardening — these are the structural templates D-30.8 mandates parallel-shape with)
    - .planning/phases/28-xrom-framework-math-pac-i-core-ops/28-CONTEXT.md (decisions C-28.1..C-28.4 + D-28.1..D-28.9 — synthesized into Phase 28 subsection)
    - .planning/phases/29-cli-integration/29-CONTEXT.md (D-29.1 and any other locks — synthesized into Phase 29 subsection)
    - .planning/phases/30-documentation-adrs/30-CONTEXT.md (D-30.1..D-30.9 — Phase 30 subsection body)
  </read_first>
  <action>
Insert a new `### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)` block into `CLAUDE.md`. Insertion point: AFTER line 112 (end of the existing `### v2.2 additions (Test Hardening, Phase 27)` block — the last line is `- **`// Catches: <bug class>` rationale (D-27.1):** ...`) and BEFORE line 114 (`## Tech Stack` header).

The block follows the v2.2 sub-section-per-phase structural shape. Use these EXACT subsection headers (the wording is fixed by D-30.8 + the heading-suffix convention):

```
### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)

#### Phase 28 (XROM Framework + Math Pac I Core Ops, shipped 2026-05-16)

- **Op-strategy A locked (ADR-001 / C-28.1):** ...
- **User-callback strict-reject policy locked (ADR-002 / C-28.2):** ...
- **JSON-pipeline separate-file shape locked (ADR-005 / C-28.3):** ...
- **`xrom_resolve` fires LAST in resolver chain (C-28.4):** ...
- **ComplexStack overlay X/Y/Z/T (D-28.1, D-28.2):** ...
- **XEQ "REAL" derived entry point (D-28.3):** ...
- **`modal_prompt: Option<String>` dedicated field (D-28.4):** ...
- **R/S submits modal numeric input (D-28.5):** ...
- **Hyperbolics XEQ-only — no dedicated keys (D-28.6):** ...
- **Cancellation plumbing in Phase 28; wiring in Phase 31 (D-28.7, D-28.8):** ...
- **`HpError::Canceled` variant (D-28.9):** ...
- **5 new `CalcState` fields** (`xrom_modules`, `complex_mode`, `matrix_dim`, `matrix_active_reg`, `modal_program`, `integ_state`, `solve_state`) carry `#[serde(default)]` / `#[serde(skip)]` per v2.2 backward-compat invariant.
- **~40 new `Op` variants** in `dispatch()` (ops/mod.rs), `execute_op()` (ops/program.rs), AND both `prgm_display.rs` copies — 4-way exhaustive-match invariant trivially preserved per CLAUDE.md "Every new Op variant must be added to 4 places".

#### Phase 29 (CLI Integration, shipped 2026-05-17)

- **`xeq_by_name_local_resolve` → `xrom_resolve` (D-29.1 derivative):** ...
- **`hp41-cli/src/help_data.rs` second `OnceLock<Vec<HelpEntry>>`:** ...
- **`docs/hp41-math1-functions.json` authored (D-29.1):** pulled forward from Phase 30 / DOC-01 to Phase 29 because the CLI parity tests (`tests/function_matrix_parity.rs`) need the file present to validate `xrom_resolve` against. Phase 30 consumes the file read-only as input to the matrix-renderer two-input extension.
- **~40 new `op_display_name` arms in `hp41-cli/src/prgm_display.rs`:** exhaustive match grows; no `_ =>` catch-all per the SC-4-display-formatter exception convention.
- **`KEY_REF_TABLE` derived from JSON (D-25.18 pattern continues):** Math Pac I entries appear in the right-panel discoverability listing automatically once their JSON entries land.
- **Modal-prompt routing through `state.print_buffer`:** `ORDER=?`, `A1,1=?`, `FUNCTION NAME?`, `GUESS 1=?` flow through the existing v1.1 print-buffer channel — no new IPC surface.
- **No core/GUI changes in Phase 29:** SC-4 invariant trivially preserved; v2.2 surface unchanged.

#### Phase 30 (Documentation & ADRs, shipped 2026-05-17 — in this plan)

- **`scripts/docs-matrix` two-input extension (D-30.1, D-30.2, D-30.3):** justfile invokes the renderer twice (once per JSON source); binary signature stays 1-in/1-out; surgical `Entry` struct widening adds optional `xrom: Option<XromRef>` field with `#[serde(default)]`; conditional column emission keys off `has_xrom = entries.iter().any(|e| e.xrom.is_some())`; hp41cv path bit-for-bit unchanged.
- **`docs/hp41-math1-function-matrix.md` generated (~55 entries):** new file at sibling path; carries an XROM column showing `Math 1 / 7-N` per entry; reachable via README v3.0 soft-claim link.
- **Three new ADRs (D-30.6, D-30.7):** `v3.0-001-op-strategy.md` (Op-strategy A vs B — locked A), `v3.0-002-user-callback-policy.md` (strict-reject nested INTG/SOLVE/DIFEQ; Pitfall 19 Free42 disclaim verbatim), `v3.0-005-json-pipeline.md` (separate `hp41-math1-functions.json` file — locked); each ~6–7 KB long-form mirroring `v3.0-003`/`v3.0-004`; `## Alternatives Considered` quotes Phase 28 CONTEXT.md verbatim per D-30.7.
- **`docs/hp41-math1-divergences.md` expanded (D-30.4, D-30.5):** three-bucket numbered catalog (OM Divergences / Emulator Extensions / Behavioral Policies) with ≥ 6 `D-30-NN` entries in the five-field shape (OM citation / Our behavior / OM behavior / Rationale / See). Citation provenance per Pitfall 18 enforced.
- **README v3.0 soft-claim (D-30.9):** `- Math Pac I behavioral emulation (10 top-level programs, ~55 XEQ entry points, documented divergences)` under `## Features`. Hard claim "feature-complete Math Pac I" deferred to Phase 32 conditional on QUAL-01 coverage gate ≥ 95%.
- **PROJECT.md milestone progress lines (D-30.8 (b)):** Shipped block gains Phase 28 + Phase 29 + Phase 30 entries; Active block date refreshed to 2026-05-17.
- **Free42 contamination guard policy documented (Pitfall 19):** ADR-002 carries the verbatim disclaim sentence `"Algorithm independently re-derived from HP Math Pac I Owner's Manual 00041-90034 (1979); Free42 source consulted only as sanity-check oracle, not copied."` The CI enforcement script `scripts/check-free42-contamination.sh` is deferred to Phase 32 / QUAL-05.

#### Phase 31 (GUI Integration) — (in progress)

#### Phase 32 (Test Hardening & Quality Gates) — (in progress)

**Frozen invariants preserved across v3.0 (so far):**
- SC-4 invariant: every Phase 28–30 change respects the stricter grep `grep -rn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum) " hp41-gui/src-tauri/src/` (returns nothing). Math Pac I math lives in `hp41-core/src/ops/math1/`.
- 4-exhaustive-match invariant: every new `Op` variant landed in `dispatch()` + `execute_op()` + both `prgm_display.rs` copies before any caller could compile.
- `#![deny(clippy::unwrap_used)]` continues to apply in `hp41-core`; new test files in v3.0 carry `#[allow]` at file scope per the established pattern.
- Save-file backward compat: every new `CalcState` field added in Phase 28 carries `#[serde(default)]`; transient fields (`integ_state`, `solve_state`, `modal_program`, `cancel_requested`) additionally carry `#[serde(skip)]`. v1.0–v2.2 save files continue to load without migration.
- MSRV 1.88 unchanged through Phase 28–30. Phase 32 plans to add `approx 0.5.1` as the only new dev-dep (relative-tolerance assertion macros) — not landed yet.
```

**Synthesis discipline:**

The Phase 28 and Phase 29 subsection bodies above use ellipses (`...`) as placeholders where the agent must synthesize the actual decision-summary one-liner from the corresponding CONTEXT.md decision blocks. Read each CONTEXT.md decision, then write a one-sentence summary that fits the v2.2 block's voice (terse, technical, lock-mode-stamped). Use the v2.2 additions block at lines 89–112 as the tonal reference — every bullet there starts with a bolded short title `**...:**` followed by terse declarative prose.

For Phase 28 bullets that reference ADRs (Op-strategy A / user-callback / JSON-pipeline), reference the ADR file path (`docs/adr/v3.0-001-op-strategy.md`, etc.). Those files land in Plan 30-02 — by the time this plan runs (Wave 2), the ADRs from Plan 30-02 are committed (Plan 30-02 is Wave 1, this is Wave 2 with `depends_on: [30-01, 30-02]` (the prose now aligns with the frontmatter; the dependency on 30-02 is REAL because Task 3 of this plan cross-references ADR file paths created in Plan 30-02). Plan 30-02 also runs in Wave 1 and completes before Wave 2 starts, so the ADR files will exist by the time Task 3 of this plan runs).

For Phase 30 bullets, the content is already specific to this plan's deliverables (matrix file, ADRs, README claim, etc.) — no synthesis required, just adapt the wording to match the v2.2 block voice.

Phase 31 and Phase 32 subsections are stub headers only — D-30.8 specifies "(in progress)" markers; the body fills in at each phase's ship-time. Do NOT add body content to these subsections.

**Tail section:** the "Frozen invariants preserved across v3.0 (so far)" block is the v3.0 analog of the v2.2 "Frozen invariants preserved" line (CLAUDE.md line 111). It explicitly notes which invariants are preserved through v3.0 so far — covers SC-4, 4-exhaustive-match, `#![deny(clippy::unwrap_used)]`, save-file backward compat, MSRV.

**Do NOT touch:**
- Any pre-existing CLAUDE.md content above line 112 or below line 114 (insertion is BETWEEN those two lines).
- Quality Gates table at line 133+ — Phase 32 will update the v3.0 column when it ships.
- Key Files table at line 145+ — Phase 28/29 file additions are deferred to a future maintenance pass (out of scope for Phase 30 per CONTEXT.md "Out of scope" line 29: "any `hp41-core/src/` source changes" — but the Key Files table is documentation about existing files, not source. Decision: leave for Phase 32 ship-time when the full file inventory stabilizes).
  </action>
  <verify>
    <automated>grep -qF "### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)" CLAUDE.md && grep -qF "#### Phase 28 (XROM Framework" CLAUDE.md && grep -qF "#### Phase 29 (CLI Integration" CLAUDE.md && grep -qF "#### Phase 30 (Documentation & ADRs" CLAUDE.md && grep -qF "#### Phase 31 (GUI Integration) — (in progress)" CLAUDE.md && grep -qF "#### Phase 32 (Test Hardening" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)" CLAUDE.md` returns 1
    - `grep -c "^#### Phase 2[89]\\|^#### Phase 30\\|^#### Phase 31\\|^#### Phase 32" CLAUDE.md` returns 5 (Phase 28 + 29 + 30 + 31 + 32)
    - `grep -cF "(in progress)" CLAUDE.md` returns ≥ 2 (Phase 31 + Phase 32 stub markers)
    - The v3.0 block is positioned BETWEEN the v2.2 Phase 27 block and the `## Tech Stack` header: `awk '/### v2.2 additions \\(Test Hardening, Phase 27\\)/{f=1} /## Tech Stack/{print NR; exit} f' CLAUDE.md` shows the v3.0 block within this range (heuristic: line number of "## Tech Stack" grew by ~80 lines from HEAD's 114 to ~190+)
    - `grep -cE "(C-28|D-28)\\.[1-9]" CLAUDE.md` returns ≥ 10 (Phase 28 decisions referenced in body; portable ERE alternation, not BRE)
    - `grep -cE "D-30\\.[1-9]" CLAUDE.md` returns ≥ 6 (Phase 30 decisions referenced in body; portable ERE)
    - `grep -qE "ADR-001|v3\\.0-001-op-strategy" CLAUDE.md` succeeds (portable ERE alternation, dot escaped)
    - `grep -qE "ADR-002|v3\\.0-002-user-callback-policy" CLAUDE.md` succeeds (portable ERE alternation, dot escaped)
    - `grep -qE "ADR-005|v3\\.0-005-json-pipeline" CLAUDE.md` succeeds (portable ERE alternation, dot escaped)
    - `wc -l CLAUDE.md` returns ≥ 280 (HEAD is ~270 lines including v2.2 blocks; the v3.0 block adds ≥ 60 lines of body)
    - `! grep -qF "feature-complete Math Pac I" CLAUDE.md` succeeds (i.e. the hard claim is ABSENT; deferred to Phase 32 per D-30.9). Equivalent form: `grep -cF "feature-complete Math Pac I" CLAUDE.md` returns 0. The plan adopts the negated-q form as the canonical CI check.
    - The existing v2.2 blocks remain intact: `grep -c "^### v2\\.2 additions" CLAUDE.md` returns 2 (both v2.2 sections preserved — HP-41CV Feature Completeness + Test Hardening)
  </acceptance_criteria>
  <done>v3.0 additions block inserted between v2.2 Phase 27 block and Tech Stack header; Phase 28/29/30 subsections populated; Phase 31/32 stub headers present with `(in progress)` markers; Frozen invariants tail summary present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → users / public web | README is published in the GitHub repo; soft-claim is a public assertion about emulator capabilities. |
| CLAUDE.md → AI coding assistant | CLAUDE.md drives AI behavior on future coding sessions; the v3.0 additions block becomes the canonical reference for v3.0 decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Information Disclosure | README v3.0 soft-claim | accept | D-30.9 explicitly defers the hard claim ("feature-complete") to Phase 32 conditional on QUAL-01; the soft-claim wording is conservative and accurate. |
| T-30-08 | Tampering | CLAUDE.md v3.0 block ordering | mitigate | Insertion point is explicitly between the v2.2 Phase 27 block and `## Tech Stack` header — verified by `awk` line-range check in the acceptance criteria. The block heading "Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS" is verbatim-locked by D-30.8. |
| T-30-09 | Repudiation | Phase 28/29 ship dates in PROJECT.md and CLAUDE.md | mitigate | Dates are git-verifiable (`git log --before=2026-05-18 -- .planning/phases/28-*/ .planning/phases/29-*/` confirms the ship dates 2026-05-16 and 2026-05-17). |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies — documentation-only plan touches `README.md`, `.planning/PROJECT.md`, `CLAUDE.md`. No package legitimacy gate required. |
</threat_model>

<verification>
After all three tasks complete:

1. README soft-claim: present verbatim per D-30.9; hard claim absent; matrix link reachable.
2. PROJECT.md: v3.0 milestone block reflects Phase 28 + Phase 29 shipped + Phase 30 in progress; Active block date refreshed.
3. CLAUDE.md: new `### v3.0 additions` block positioned correctly; Phase 28/29/30 subsections populated; Phase 31/32 stub headers present.
4. SC-4 invariant trivially preserved (plan touches `README.md`, `.planning/PROJECT.md`, `CLAUDE.md` only); MSRV unchanged; zero new dependencies.
5. The matrix link in README (`docs/hp41-math1-function-matrix.md`) resolves to a real file created by Plan 30-01 (Wave 1 dependency satisfied).
</verification>

<success_criteria>
- README.md carries the D-30.9 verbatim soft-claim under `## Features` AND a matrix link in `## Documentation` table
- README.md does NOT carry any "feature-complete Math Pac I" hard claim (deferred to Phase 32)
- PROJECT.md `## Project History` lists Phase 28 (2026-05-16), Phase 29 (2026-05-17), Phase 30 (IN PROGRESS); Active block date refreshed to 2026-05-17
- CLAUDE.md gains the `### v3.0 additions (Math Pac I Emulation, Phases 28–30 — 31–32 IN PROGRESS)` block between the v2.2 Phase 27 block and `## Tech Stack`
- CLAUDE.md v3.0 block has fully populated Phase 28/29/30 subsections and stub `(in progress)` markers for Phase 31/32
- CLAUDE.md v3.0 block references ADR-001, ADR-002, ADR-005 file paths (which exist after Plan 30-02 ships in Wave 1)
</success_criteria>

<output>
Create `.planning/phases/30-documentation-adrs/30-03-SUMMARY.md` when done, summarizing:
- The exact text of the README soft-claim bullet (proof it matches D-30.9 verbatim)
- The exact line number range where the CLAUDE.md v3.0 additions block sits (before/after which existing sections)
- The PROJECT.md "Stand" date used (should be 2026-05-17)
- Confirmation that no hard claim ("feature-complete Math Pac I") appears in any of the three files
- Output of `git diff --stat README.md .planning/PROJECT.md CLAUDE.md` showing the net line additions per file
</output>
