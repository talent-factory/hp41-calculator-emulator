---
phase: 31-gui-integration
plan: 04
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/HelpOverlay.tsx
  - hp41-gui/src/HelpOverlay.test.tsx
  - hp41-gui/src/App.css
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/op_catalog_xrom.rs
requirements: [GUI-03, GUI-04]
autonomous: true
must_haves:
  truths:
    - "`?`-overlay loads docs/hp41-math1-functions.json via Vite static JSON-import (build-time validation; malformed JSON fails the Vite build)"
    - "`?`-overlay renders two top-level collapsible sections: 'HP-41CV (built-in)' and 'Math 1 Pac (XROM 7)', both expanded by default"
    - "JSON's per-program categories (Math1 Hyperbolics, Math1 Complex, Math1 Matrix, etc.) preserve as 2nd-level headers inside the Math 1 Pac section"
    - "Op::Catalog(2) enumerates loaded XROM modules: emits a header line 'XROM 7 Math 1' followed by every name in MATH_1.ops to state.print_buffer (instant scroll — D-31.12/D-31.14 PSE-step deferred per W1 fix)"
    - "If xrom_modules bit 0 is unset, Op::Catalog(2) emits 'NO XROM' instead"
    - "SC-4 invariant preserved (surgical change lives in hp41-core/src/ops/program.rs, NOT hp41-gui/src-tauri/)"
  artifacts:
    - path: "hp41-gui/src/help_data.ts"
      provides: "import math1Functions + helpEntriesMath1() + helpEntriesAll() + optional xrom?: XromEntry field on HelpEntry"
      contains: "hp41-math1-functions.json"
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "Two-section wrapper around existing per-category rendering keyed on entry.xrom?.module === 'Math 1'"
      contains: "Math 1 Pac"
    - path: "hp41-gui/src/App.css"
      provides: ".help-overlay-section-heading + .help-overlay-section-body selectors per UI-SPEC §Typography"
      contains: "help-overlay-section-heading"
    - path: "hp41-core/src/ops/program.rs"
      provides: "op_catalog n==2 arm replaced from stub with XROM enumeration loop (instant push — no PSE-step)"
      contains: "MATH_1.ops"
    - path: "hp41-core/tests/op_catalog_xrom.rs"
      provides: "Test asserting CAT 2 lists module header + every function name + handles xrom_modules == 0 fallback"
  key_links:
    - from: "hp41-gui/src/help_data.ts"
      to: "docs/hp41-math1-functions.json"
      via: "Vite static import"
      pattern: "import.*hp41-math1-functions"
    - from: "hp41-gui/src/HelpOverlay.tsx"
      to: "hp41-gui/src/help_data.ts::helpEntriesAll"
      via: "function call to merged accessor"
      pattern: "helpEntriesAll"
    - from: "hp41-core/src/ops/program.rs::op_catalog"
      to: "hp41-core/src/ops/math1/xrom.rs::MATH_1"
      via: "use statement + iteration over MATH_1.ops"
      pattern: "use crate::ops::math1::xrom::MATH_1"
---

<objective>
Land two GUI-03/GUI-04 surface deliverables independent of cancellation: (a) extend `hp41-gui/src/help_data.ts` and `HelpOverlay.tsx` to parallel-load `docs/hp41-math1-functions.json` via Vite static JSON-import and render the contents as a second top-level collapsible section ("Math 1 Pac (XROM 7)") alongside the existing "HP-41CV (built-in)" section; (b) take the surgical `hp41-core` exception (analogous to v2.2 Plan 25-03's `builtin_card_op` 4→12 extension) to replace the `Op::Catalog(2)` "NOT AVAILABLE" stub with a real XROM enumeration that writes the module header line followed by every name in `MATH_1.ops` into `state.print_buffer`.

**Scope deviation surfaced (W1 fix): instant-scroll for CAT 2; D-31.12/D-31.14 PSE-step + R/S pause/resume DEFERRED to v3.1 polish.**

D-31.12 (locked in 31-CONTEXT.md line 136) and D-31.14 (locked in 31-CONTEXT.md line 142) call for "Hardware-faithful PSE scroll via `state.print_buffer`" with "~500ms PSE delay between lines; R/S pauses/resumes; any other key cancels the scroll mid-listing". Plan 31-04 ships **instant-scroll** instead, per RESEARCH Open Q2 (lines 82, 395, 836 in 31-RESEARCH.md): **v2.2 CAT 1 has NO PSE-step infrastructure** — `op_catalog` is a single-pass loop that pushes ALL lines synchronously into `print_buffer`. D-31.12/D-31.14's reference to "the v2.2 PSE-step infrastructure already handles this" (CONTEXT line 142) is documentation drift — that infrastructure does not exist. Introducing PSE-step infrastructure is out of Phase 31 scope (new abstraction, broader hp41-core surgery, would violate the Phase 28 freeze beyond the surgical 1-arm `op_catalog` extension this plan already takes). The instant-scroll behavior matches v2.2 CAT 1's actual behavior bit-for-bit (consistency over net-new). D-31.12/D-31.14 PSE-step + R/S pause/resume + key-cancel are recorded as a v3.1 polish item.

Purpose: GUI-03 + GUI-04 acceptance. Both deliverables are independent of the cancellation channel and the modal-rendering surface; this plan can ship in parallel to 31-05 if execution windows allow (but waits on Plan 31-02 because the SC-4 grep test must already exist).

Output: 2 modified TypeScript files, 1 modified CSS file, 1 new vitest test extension, 1 surgical 4-line change inside `hp41-core/src/ops/program.rs::op_catalog`, 1 new hp41-core test file.
</objective>

<context_amendment>
**Required executor action (W1 fix):** Before completing Plan 31-04, append the following bullet to the `## Deferred Ideas` section of `.planning/phases/31-gui-integration/31-CONTEXT.md` (insert after the existing "CAT 2 module-header verbosity" bullet around line 313):

```
- **CAT 2 PSE-step infrastructure (D-31.12, D-31.14)** — v3.1 polish. Phase 31-04 ships instant-scroll per RESEARCH Open Q2 (verified v2.2 CAT 1 has NO PSE-step infrastructure — `op_catalog` is a single-pass synchronous loop). D-31.12's "~500ms PSE delay between lines" and D-31.14's "R/S pauses/resumes; other keys cancel" require introducing a new abstraction (PSE-step iterator + per-line tick state on `CalcState` + frontend cooperation) that is out of Phase 31 scope (Phase 28 freeze + risk of broader hp41-core surgery). Defer to v3.1 polish; revisit if user feedback demands hardware-faithful scroll timing.
```

This amendment is a documentation-only follow-up to keep `## Deferred Ideas` synchronized with Plan 31-04's actual ship behavior. It is NOT a code change. Include the amendment as part of the same commit that lands the Plan 31-04 artifacts.
</context_amendment>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gui-integration/31-CONTEXT.md
@.planning/phases/31-gui-integration/31-RESEARCH.md
@.planning/phases/31-gui-integration/31-PATTERNS.md
@.planning/phases/31-gui-integration/31-UI-SPEC.md

# Existing files this plan reads and modifies
@hp41-gui/src/help_data.ts
@hp41-gui/src/HelpOverlay.tsx
@hp41-gui/src/HelpOverlay.test.tsx
@hp41-gui/src/App.css
@hp41-gui/vite.config.ts
@hp41-core/src/ops/program.rs
@hp41-core/src/ops/math1/xrom.rs
@hp41-core/src/state.rs
@docs/hp41-math1-functions.json
@docs/hp41cv-functions.json

<interfaces>
<!-- Existing TS HelpEntry interface in hp41-gui/src/help_data.ts: -->
<!--   export interface HelpEntry { op_variant; display_name; category; status; phase; key_path; description; divergences? } -->
<!-- Phase 31 extends with optional xrom?: { module; module_id; function_id } -->

<!-- Vite import pattern already established at hp41-gui/src/help_data.ts line 13: -->
<!--   import functions from '../../docs/hp41cv-functions.json'; -->
<!-- vite.config.ts fs.allow already covers '..' since Phase 26 W8 — NO config change needed -->

<!-- hp41-math1-functions.json schema (per Phase 29 D-29.1 + ADR-005): -->
<!-- Each entry mirrors HelpEntry shape PLUS an xrom object: -->
<!--   { module: "Math 1", module_id: 7, function_id: <1-based-index> } -->
<!-- Categories used: "Math1 Hyperbolics", "Math1 Complex", "Math1 Matrix", "Math1 Solver", "Math1 Integration", -->
<!-- "Math1 DIFEQ", "Math1 Fourier", "Math1 Triangles", "Math1 Coord Transform", "Math1 Polynomial" -->

<!-- Existing op_catalog stub in hp41-core/src/ops/program.rs (verified lines 335-339 in 31-RESEARCH.md): -->
<!--   2..=4 => { state.print_buffer.push(format!("{:<24}", "NOT AVAILABLE")); } -->

<!-- MATH_1 struct in hp41-core/src/ops/math1/xrom.rs: -->
<!--   pub const MATH_1: XromModule { id: 7, name: "Math 1", ops: &[(&str, Op), ...] } -->

<!-- Existing HelpOverlay grouping logic (hp41-gui/src/HelpOverlay.tsx around lines 28-39): -->
<!--   const grouped = useMemo(() => { ... Map<category, HelpEntry[]> ... }, [filtered]); -->

<!-- W1 deviation context: v2.2 op_catalog in hp41-core/src/ops/program.rs is a SINGLE-PASS -->
<!-- synchronous loop — NO setInterval / NO PSE-step state / NO per-tick yield. Phase 31-04 -->
<!-- CAT 2 mirrors this exact shape (instant push of header + all function names into -->
<!-- print_buffer). D-31.12/D-31.14 PSE-step semantics are deferred to v3.1. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgical Op::Catalog(2) extension in hp41-core/src/ops/program.rs + regression test</name>
  <files>
    hp41-core/src/ops/program.rs
    hp41-core/tests/op_catalog_xrom.rs
  </files>
  <read_first>
    - hp41-core/src/ops/program.rs around lines 300-345 (existing op_catalog function body; line 335-339 is the "NOT AVAILABLE" stub to replace)
    - hp41-core/src/ops/math1/xrom.rs (full file — MATH_1.ops static slice; id=7; name="Math 1"; iteration order is registration order)
    - hp41-core/src/state.rs (confirm xrom_modules field type u8 and default 0b0000_0001 for v3.0)
    - .planning/phases/31-gui-integration/31-RESEARCH.md §"Pitfall 2" lines 458-486 (verbatim replacement sketch) + §"Pitfall 3" lines 488-496 (SC-4 trivially preserved because change lives in hp41-core) + §"Open Q2" lines 82, 395, 836 (v2.2 CAT 1 has NO PSE-step — confirms instant-scroll path)
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-04 — hp41-core/src/ops/program.rs::op_catalog" lines 527-567 (surgical-exception precedent: v2.2 Plan 25-03 `builtin_card_op` 4→12)
  </read_first>
  <action>
    In `hp41-core/src/ops/program.rs::op_catalog`, locate the existing arm matching `2..=4 =>` (around line 335-339 per RESEARCH). Split this arm into a dedicated `2 =>` arm and a residual `3..=4 =>` arm. The new `2 =>` arm: check `state.xrom_modules & 0b0000_0001 != 0`; if set, add `use crate::ops::math1::xrom::MATH_1;` at the top of `program.rs` (if not already imported), push a header line `format!("{:<24}", format!("XROM {} {}", MATH_1.id, MATH_1.name))` into `state.print_buffer`, then iterate `for (name, _op) in MATH_1.ops` pushing `format!("{:<24}", name)` per entry. **All pushes happen in a single synchronous pass — NO PSE-step, NO per-line yield, NO setInterval (per W1 deviation; matches v2.2 CAT 1 shape per Open Q2).** If `xrom_modules & 0b0000_0001 == 0`, push `format!("{:<24}", "NO XROM")` instead. The `3..=4 =>` arm preserves the existing `"NOT AVAILABLE"` payload-line behavior (HP-IL and peripherals stay unavailable). The function signature, visibility, and surrounding arms are NOT touched. Author `hp41-core/tests/op_catalog_xrom.rs` with three `#[test]` functions: (a) `catalog_2_with_math1_loaded_lists_header_and_functions` — build a default CalcState (`xrom_modules` defaults to 0b0000_0001), call `op_catalog(&mut state, 2)`, assert `state.print_buffer[0]` contains both `"XROM 7"` and `"Math 1"`, and assert `state.print_buffer.len() == 1 + MATH_1.ops.len()`; (b) `catalog_2_without_xrom_emits_no_xrom` — clear `state.xrom_modules = 0`, call `op_catalog(&mut state, 2)`, assert `state.print_buffer.len() == 1` and the line contains `"NO XROM"`; (c) `catalog_3_and_4_still_not_available` — call `op_catalog(&mut state, 3)` and `op_catalog(&mut state, 4)`, assert each appended line contains `"NOT AVAILABLE"`. File carries `#![allow(clippy::unwrap_used)]` at file scope per the test-convention.
  </action>
  <verify>
    <automated>cargo test --package hp41-core --test op_catalog_xrom</automated>
  </verify>
  <acceptance_criteria>
    - Source: `hp41-core/src/ops/program.rs` `op_catalog` body contains a dedicated `2 =>` arm distinct from `3..=4 =>`
    - Source: the new `2 =>` arm references `MATH_1.id`, `MATH_1.name`, and iterates `MATH_1.ops`
    - Source: `op_catalog` function signature and visibility (`pub fn`) is unchanged
    - Source: the `3..=4 =>` arm still pushes `"NOT AVAILABLE"`
    - Source: NO `std::thread::sleep`, NO `setInterval`-equivalent, NO PSE-step state mutation inside the `2 =>` arm (instant-scroll confirmed)
    - File exists at `hp41-core/tests/op_catalog_xrom.rs` with the three named `#[test]` functions
    - CLI: `cargo test --package hp41-core --test op_catalog_xrom` exits 0
    - CLI: `cargo test --package hp41-core --test xrom_shadowing` (existing Phase 28 gate) still exits 0 (Plan 31-04 does NOT add new MATH_1.ops entries)
    - CLI: `grep -rn -E "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\(" hp41-gui/src-tauri/src/` returns nothing (SC-4 trivially preserved — change is in hp41-core only)
    - Behavior: in a fresh CalcState, calling CAT 2 produces a header `"XROM 7 Math 1"` and at least 40 function-name lines, ALL appearing in a single `get_state` response (no progressive reveal)
  </acceptance_criteria>
  <done>The Op::Catalog(2) stub is replaced with a working XROM enumeration; the surgical hp41-core exception is documented in SUMMARY.md and the change preserves the FROZEN-since-Phase-28 family invariant by adding NO new Op variants, NO new MATH_1.ops entries, and NO API widening. Instant-scroll behavior matches v2.2 CAT 1 per W1 fix.</done>
</task>

<task type="auto">
  <name>Task 2: Parallel-load hp41-math1-functions.json in help_data.ts + extend HelpEntry with optional xrom field</name>
  <files>
    hp41-gui/src/help_data.ts
  </files>
  <read_first>
    - hp41-gui/src/help_data.ts (full file — existing import of hp41cv-functions.json at line 13, HelpEntry interface, helpEntries() accessor)
    - hp41-gui/vite.config.ts (around lines 18-19 — `fs.allow` already set to `path.resolve(__dirname, '..')`; no edit needed but confirm the existing setting)
    - docs/hp41-math1-functions.json (skim shape — verify the file has an `xrom` object per entry and that categories use the "Math1 *" naming)
    - hp41-cli/src/help_data.rs around lines 100-135 (Phase 29 D-29.2 Rust mirror: second OnceLock + merged accessor — port the same shape to TypeScript)
    - .planning/phases/31-gui-integration/31-RESEARCH.md §"Pattern 3" lines 332-344 + §"Pitfall 7" lines 540-548
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-04 — hp41-gui/src/help_data.ts" lines 359-424
  </read_first>
  <action>
    In `hp41-gui/src/help_data.ts`: add a second Vite static JSON-import at module top: `import math1Functions from '../../docs/hp41-math1-functions.json';`. Extend the existing `HelpEntry` interface with an optional field `xrom?: XromEntry` where `XromEntry` is a new exported interface defined just above `HelpEntry` carrying three fields `module: string`, `module_id: number`, `function_id: number` (matches the JSON schema per ADR-005 / Phase 29 D-29.1). Export two new accessor functions: `helpEntriesMath1(): readonly HelpEntry[]` returning `math1Functions as readonly HelpEntry[]`, and `helpEntriesAll(): readonly HelpEntry[]` returning the concatenation `[...helpEntries(), ...helpEntriesMath1()]`. Preserve the existing `helpEntries()` function unchanged so existing call sites are backward-compatible. NO change to `vite.config.ts` (the existing `fs.allow` already covers `../../docs/`). The Vite static-import is a hard-build-blocker: a malformed `hp41-math1-functions.json` fails `npm run build` — parallel to the Rust `.expect("...malformed")` discipline in hp41-cli/src/help_data.rs.
  </action>
  <verify>
    <automated>cd hp41-gui && npx tsc --noEmit -p . && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `hp41-gui/src/help_data.ts` contains a top-level `import math1Functions from '../../docs/hp41-math1-functions.json';`
    - Source: file exports `interface XromEntry` with three fields `module`, `module_id`, `function_id`
    - Source: `HelpEntry` interface has an optional `xrom?: XromEntry` field
    - Source: file exports both `helpEntriesMath1()` and `helpEntriesAll()` functions; `helpEntriesAll` returns a concatenation of `helpEntries()` and `helpEntriesMath1()`
    - Source: file does NOT remove or rename existing `helpEntries()` (backward compatibility)
    - CLI: `cd hp41-gui && npx tsc --noEmit -p .` exits 0
    - CLI: `cd hp41-gui && npm run build` exits 0 (Vite static import succeeds; JSON is well-formed)
    - Behavior: corrupting `hp41-math1-functions.json` (e.g., trailing comma) causes `npm run build` to fail with a JSON parse error — verifies hard-build-blocker
  </acceptance_criteria>
  <done>The merged JSON pool is available to `HelpOverlay.tsx`; the build is fail-fast on malformed Math Pac I JSON, mirroring CLI's `.expect("...malformed")` pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Two-section wrapper in HelpOverlay.tsx + CSS additions + vitest assertions</name>
  <files>
    hp41-gui/src/HelpOverlay.tsx
    hp41-gui/src/HelpOverlay.test.tsx
    hp41-gui/src/App.css
  </files>
  <read_first>
    - hp41-gui/src/HelpOverlay.tsx (full file — current rendering structure around lines 22-98; identifies the entry source it iterates and the per-category grouping pattern)
    - hp41-gui/src/HelpOverlay.test.tsx (existing tests — extend by adding new test cases without breaking existing ones)
    - hp41-gui/src/App.css (existing `.help-overlay-*` selectors around lines 325-335 — add new selectors alongside; do not modify existing styles)
    - .planning/phases/31-gui-integration/31-UI-SPEC.md (lines 65-90 Typography table; especially the new `.help-overlay-section-heading` 14px/700 role)
    - .planning/phases/31-gui-integration/31-RESEARCH.md §"Pattern 3 / D-31.8 / D-31.9" + 31-CONTEXT.md D-31.8 / D-31.9 (sectioning + collapse-state contract)
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-04 — hp41-gui/src/HelpOverlay.tsx" lines 427-477 + §"Plan 31-04 — hp41-gui/src/App.css" lines 480-525
  </read_first>
  <action>
    In `hp41-gui/src/HelpOverlay.tsx`: replace the call to `helpEntries()` (or equivalent) with `helpEntriesAll()` from `help_data.ts` so the overlay's data pool now includes Math Pac I entries. Introduce a SECTIONS constant at module scope: a typed array of `{ id: 'hp41cv' | 'math1'; heading: string; predicate: (e: HelpEntry) => boolean }` with two entries — `{ id: 'hp41cv', heading: 'HP-41CV (built-in)', predicate: e => !e.xrom }` and `{ id: 'math1', heading: 'Math 1 Pac (XROM 7)', predicate: e => e.xrom?.module === 'Math 1' }`. Restructure the JSX output to render each section as a wrapping element with a focusable collapsible header (`<button>` per UI-SPEC §Accessibility with `aria-expanded` reflecting collapse state) and a body containing the existing per-category grouping logic — ONLY applied to entries matching the section's predicate. Both sections expanded by default per D-31.8; collapse state managed via local `useState<{ hp41cv: boolean; math1: boolean }>({ hp41cv: true, math1: true })`. Within each section body, preserve the existing per-category second-level grouping (categories like "Math1 Hyperbolics" etc. appear as 2nd-level headers via the existing `.help-overlay-category-heading` selector per D-31.9). Sort entries alphabetically within each category per CONTEXT line 159. In `hp41-gui/src/App.css`: append two new CSS rules (do not modify existing ones): `.help-overlay-section-heading` with `margin: 16px 0 8px 0; color: #f5a423; font-size: 14px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; border-bottom: 1px solid #3a3a3a; padding-bottom: 4px; background: transparent; border-left/right/top: none; cursor: pointer; width: 100%; text-align: left;` per UI-SPEC §Typography line 78 + Spacing line 53. Add `.help-overlay-section-body {}` as a placeholder selector for future style hooks. In `hp41-gui/src/HelpOverlay.test.tsx`: add `test('renders two top-level sections with HP-41CV and Math 1 Pac headings')` and `test('Math 1 Pac section contains a Math1 Hyperbolics category')` and `test('clicking section heading toggles aria-expanded')`. The first test renders `<HelpOverlay />` and asserts both heading strings appear via `screen.getByText`. The second asserts that within the Math 1 Pac section, at least one row's category is rendered as a 2nd-level "MATH1 HYPERBOLICS" heading (uppercase via CSS). The third uses `fireEvent.click` on the heading button and asserts `aria-expanded` flips.
  </action>
  <verify>
    <automated>cd hp41-gui && npm test -- HelpOverlay</automated>
  </verify>
  <acceptance_criteria>
    - Source: `hp41-gui/src/HelpOverlay.tsx` imports `helpEntriesAll` from `./help_data` and uses it (not `helpEntries` alone)
    - Source: file contains a `SECTIONS` constant with at least two entries having predicates `!e.xrom` and `e.xrom?.module === 'Math 1'`
    - Source: each section heading is rendered as a `<button>` element (or `role="button"`) with `aria-expanded`
    - Source: the section body is rendered conditionally based on collapse state
    - Source: `hp41-gui/src/App.css` contains a `.help-overlay-section-heading` selector with `font-size: 14px` and `font-weight: 700`
    - Source: `hp41-gui/src/HelpOverlay.test.tsx` contains at least 3 new test cases covering: two-section heading rendering, Math 1 Pac category rendering, and collapse-toggle aria-expanded behavior
    - CLI: `cd hp41-gui && npm test -- HelpOverlay` exits 0 (existing tests + the 3 new tests all pass)
    - CLI: `cd hp41-gui && npx tsc --noEmit -p .` exits 0
    - Behavior: opening the `?`-overlay in `just gui-dev` shows two collapsible headers "HP-41CV (BUILT-IN)" and "MATH 1 PAC (XROM 7)" both expanded by default (visual smoke; not in automated suite)
  </acceptance_criteria>
  <done>`?`-overlay surfaces Math Pac I as a clearly-labeled XROM-7 section with collapsible UX; vitest locks the contract.</done>
</task>

</tasks>

<verification>
- `cargo test --package hp41-core --test op_catalog_xrom` exits 0
- `cargo test --package hp41-core --test xrom_shadowing` exits 0 (existing Phase 28 gate unchanged)
- `cd hp41-gui && npm run build` exits 0 (Vite parallel-load of math1 JSON succeeds)
- `cd hp41-gui && npm test -- HelpOverlay` exits 0
- `grep -rn -E "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\(" hp41-gui/src-tauri/src/` returns nothing
- `bash scripts/check-tauri-permissions.sh` exits 0 (no new Tauri commands in this plan)
- `just gui-ci` exits 0
- `.planning/phases/31-gui-integration/31-CONTEXT.md` `## Deferred Ideas` contains the new CAT 2 PSE-step bullet per `<context_amendment>`
</verification>

<success_criteria>
- `Op::Catalog(2)` emits real XROM module enumeration in `state.print_buffer` for the default v3.0 configuration; SC-4 trivially preserved
- CAT 2 ships INSTANT-SCROLL (single-pass synchronous push) per W1 fix — matches v2.2 CAT 1 behavior bit-for-bit
- D-31.12/D-31.14 PSE-step + R/S pause/resume DEFERRED to v3.1; deferment recorded in 31-CONTEXT.md `## Deferred Ideas`
- `?`-overlay renders two top-level collapsible sections with Math Pac I as its own section
- JSON parallel-load via Vite static import is a hard-build-blocker for malformed Math Pac I JSON
- Per-category groupings inside Math 1 Pac are preserved as 2nd-level headers
- Phase 28 frozen invariants preserved (no new Op variants, no new MATH_1.ops entries, surgical change documented in SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/31-gui-integration/31-04-SUMMARY.md` when done — record:
- Number of Math Pac I entries loaded from JSON (should be ~45-55)
- Number of distinct categories rendered in the Math 1 Pac section (should be ~10-11)
- Output of `XEQ "CATALOG" 2` from a default CalcState (capture the print_buffer contents)
- Surgical hp41-core exception ledger entry — pattern matches v2.2 Plan 25-03 `builtin_card_op` 4→12
- **D-31.x divergence entry naming D-31.12 and D-31.14 explicitly (W1 fix):** record under a new `## Divergences` heading in the SUMMARY: "**D-31-04-01:** Plan 31-04 ships instant-scroll for CAT 2; D-31.12 (~500ms PSE delay per line) and D-31.14 (R/S pauses/resumes; other keys cancel) are DEFERRED to v3.1 polish. Rationale: v2.2 CAT 1 has no PSE-step infrastructure (verified RESEARCH Open Q2); introducing it is out of Phase 31 scope. Instant-scroll matches v2.2 CAT 1 behavior bit-for-bit (consistency over net-new)."
- Confirmation that the `<context_amendment>` bullet was appended to 31-CONTEXT.md `## Deferred Ideas`
</output>
