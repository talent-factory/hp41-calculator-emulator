---
phase: 31-gui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src-tauri/tests/sc4_invariant.rs
  - hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs
requirements: [GUI-01]
autonomous: true
must_haves:
  truths:
    - "SC-4 stricter grep returns zero matches in hp41-gui/src-tauri/src/ (op_display_name remains the documented exception)"
    - "Every Phase 28 Math Pac I Op variant name from MATH_1.ops appears as a string match in hp41-gui/src-tauri/src/prgm_display.rs"
    - "hp41-gui/src-tauri/src/prgm_display.rs::op_display_name body contains NO `_ =>` wildcard catch-all"
  artifacts:
    - path: "hp41-gui/src-tauri/tests/sc4_invariant.rs"
      provides: "CI-gate-equivalent integration test asserting SC-4 grep returns nothing"
    - path: "hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs"
      provides: "File-text coverage test asserting every MATH_1.ops Op variant appears in prgm_display.rs and no wildcard catch-all exists"
  key_links:
    - from: "hp41-gui/src-tauri/tests/sc4_invariant.rs"
      to: "hp41-gui/src-tauri/src/**"
      via: "subprocess grep -rn -E 'fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\('"
      pattern: "Test asserts grep exit code is 1 (no matches)"
    - from: "hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs"
      to: "hp41-gui/src-tauri/src/prgm_display.rs (file text)"
      via: "include_str!(\"../src/prgm_display.rs\") + substring search for each Op variant identifier from MATH_1.ops"
      pattern: "src.contains(\"Op::Sinh\") && !src.contains(\"_ =>\")"
---

<objective>
Verify that Plan 31-01's scope from the ROADMAP is already satisfied by Phase 28's earlier ships and lock that state with a regression test. Phase 28 plans 28-02..28-10 already shipped ~40 new `op_display_name` arms in `hp41-gui/src-tauri/src/prgm_display.rs` (per Phase 29 CONTEXT line 73 + 31-CONTEXT.md line 31). This plan therefore (a) writes a regression test asserting every Phase 28 Math Pac I Op variant has a corresponding text occurrence in `hp41-gui/src-tauri/src/prgm_display.rs`, and (b) authors a CI-gate-equivalent integration test asserting the SC-4 stricter grep returns nothing in `hp41-gui/src-tauri/src/`. No production code edits — both tests are read-only file-text scans, preserving the "zero source edits in `hp41-gui/src-tauri/src/`" pledge.

Purpose: GUI-01 acceptance is "all ~40 new arms exist AND SC-4 invariant holds". The Op variants land via compile-time exhaustive match in Phase 28 — Plan 31-01 turns that compile-time guarantee into a runtime-asserted regression suite (via file-text scan, NOT Rust API access, because `prgm_display` is a private module in `hp41-gui/src-tauri/src/lib.rs:10`) and an SC-4 grep gate, locking the state for future phases.

Output: Two test files under `hp41-gui/src-tauri/tests/`. Zero source-code edits in `hp41-gui/src-tauri/src/`. Zero Tauri command additions. Zero new `pub` exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gui-integration/31-CONTEXT.md
@.planning/phases/31-gui-integration/31-RESEARCH.md
@.planning/phases/31-gui-integration/31-PATTERNS.md
@.planning/phases/31-gui-integration/31-UI-SPEC.md

# Source files relevant to verification (read-only)
@hp41-gui/src-tauri/src/prgm_display.rs
@hp41-gui/src-tauri/src/lib.rs
@hp41-core/src/ops/math1/xrom.rs

<interfaces>
<!-- Op variants whose op_display_name arms must be present in hp41-gui/src-tauri/src/prgm_display.rs. -->
<!-- Sourced from MATH_1.ops static slice in hp41-core/src/ops/math1/xrom.rs. -->
<!-- The full ~40-variant set spans: Sinh, Cosh, Tanh, Asinh, Acosh, Atanh, CPlus, CMinus, CTimes, CDiv, Magz, Cinv, ZpowN, Zpow1N, ExpZ, LnZ, SinZ, CosZ, TanZ, ApowZ, LogZ, ZpowW, Real, PolyWorkflow, Roots, MatrixWorkflow, MatSize, MatVmat, MatEdit, MatDet, MatInv, MatSimeq, MatVcol, Integ, Solve, Sol, Difeq, Four, TriSss, TriAsa, TriSaa, TriSas, TriSsa, Trans2d, Trans3d. -->

<!-- CRITICAL: `prgm_display` is declared `mod prgm_display;` (NOT `pub mod`) in -->
<!-- hp41-gui/src-tauri/src/lib.rs:10. Test code CANNOT call -->
<!-- `hp41_gui_lib::prgm_display::op_display_name(...)` — the function is not -->
<!-- crate-public. Plan 31-01 must NOT widen visibility (the "zero source edits in -->
<!-- hp41-gui/src-tauri/src/" pledge). Therefore the regression test is a FILE-TEXT -->
<!-- SCAN of `prgm_display.rs`, not a Rust API invocation. -->
<!-- Crate library name (for completeness; not used by Plan 31-01): `hp41_gui_lib`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SC-4 invariant integration test</name>
  <files>hp41-gui/src-tauri/tests/sc4_invariant.rs</files>
  <read_first>
    - hp41-gui/src-tauri/src/prgm_display.rs (current contents — confirm Phase 28 arms present and only `op_display_name` matches the lax `op_*` grep)
    - CLAUDE.md "SC-4 invariant" block (root of project) — confirms the stricter pattern is `grep -rn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/`
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-01" lines 12-19 (analog: any existing tests file under hp41-gui/src-tauri/tests/ for test crate structure)
  </read_first>
  <action>
    Create `hp41-gui/src-tauri/tests/sc4_invariant.rs` as a stand-alone integration test that asserts the SC-4 stricter grep returns zero matches inside `hp41-gui/src-tauri/src/`. Implement the test using `std::process::Command` to invoke `grep` with the stricter alternation regex (function-name list per CLAUDE.md SC-4 invariant). The test passes when grep exits with status code 1 (no matches) and fails when it exits 0 (matches found) — invert the standard assert. Path argument to grep must be `hp41-gui/src-tauri/src` resolved relative to `env!("CARGO_MANIFEST_DIR")` parent so the test runs correctly from CI working directory. File header carries `#![allow(clippy::unwrap_used)]` per the established test-file convention. Single `#[test] fn sc4_grep_returns_no_matches()`. No fenced code in this action — the test uses Command::new("grep").args(&["-rn", "-E", "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\(", &path]).output().
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test sc4_invariant sc4_grep_returns_no_matches -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `hp41-gui/src-tauri/tests/sc4_invariant.rs`
    - Source: file contains exactly one `#[test]` attribute and at least one `Command::new("grep")` invocation
    - Source: regex string in the test literally contains all 11 forbidden function-name fragments (`add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum`)
    - Test command exits 0 (grep matched nothing in current tree); if the test exits non-zero the planner has detected a real SC-4 violation
    - CLI: running `grep -rn -E "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\(" hp41-gui/src-tauri/src/` from the repo root prints nothing and exits 1
  </acceptance_criteria>
  <done>SC-4 grep gate is a runtime-asserted test, not just a CLAUDE.md narrative reminder.</done>
</task>

<task type="auto">
  <name>Task 2: Author Math Pac I op_display_name file-text coverage regression test</name>
  <files>hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs</files>
  <read_first>
    - hp41-gui/src-tauri/src/prgm_display.rs (existing arms — confirm the file body literally contains `Op::Sinh`, `Op::Cosh`, etc.; this is the ground truth the test scans)
    - hp41-gui/src-tauri/src/lib.rs lines 6-11 (confirm `prgm_display` is declared as a PRIVATE `mod prgm_display;` — NOT `pub mod`; the test therefore CANNOT call `hp41_gui_lib::prgm_display::op_display_name`)
    - hp41-core/src/ops/math1/xrom.rs (`MATH_1.ops` static slice — source of truth for the set of Math Pac I Op variant identifiers)
    - hp41-core/src/ops/mod.rs (Op enum declaration so the test knows the exact variant identifier strings to scan for)
    - hp41-gui/src-tauri/tests/card_io_tests.rs (precedent for a test crate accessing the GUI library — note it imports `hp41_gui_lib::cards::...` because `cards` is the ONE `pub mod` in lib.rs; `prgm_display` is NOT)
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-01" line 18 (analog: any existing test under hp41-gui/src-tauri/tests/)
  </read_first>
  <action>
    Create `hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs` containing a single `#[test] fn every_math1_op_appears_in_prgm_display()`. The test is a PURE FILE-TEXT SCAN — it does NOT call any function in `hp41-gui/src-tauri/src/prgm_display.rs`. Rationale: `prgm_display` is declared `mod prgm_display;` (private) in `hp41-gui/src-tauri/src/lib.rs:10`, so `op_display_name` is NOT crate-public. Widening its visibility would violate Plan 31-01's "zero source edits in hp41-gui/src-tauri/src/" pledge. Implementation: (1) load the file body once at test-compile-time via `const PRGM_DISPLAY_SRC: &str = include_str!("../src/prgm_display.rs");` (NOTE: integration tests under `hp41-gui/src-tauri/tests/` resolve relative to that directory, so `../src/prgm_display.rs` reaches `hp41-gui/src-tauri/src/prgm_display.rs`). (2) Assert `PRGM_DISPLAY_SRC.trim().is_empty()` is FALSE (sanity: file is non-empty). (3) For each `(name, op)` tuple in `hp41_core::ops::math1::xrom::MATH_1.ops`, extract the Op variant's identifier by formatting via `format!("Op::{:?}", op)` with care — Op is a non-Copy enum; if `:?` is not available or differs in path, the test instead uses a hard-coded `MATH1_VARIANT_IDS: &[&str]` slice mirroring the ~40-variant set listed in the `<interfaces>` comment above (e.g. `["Sinh", "Cosh", "Tanh", "Asinh", "Acosh", "Atanh", "CPlus", "CMinus", "CTimes", "CDiv", "Magz", "Cinv", "ZpowN", "Zpow1N", "ExpZ", "LnZ", "SinZ", "CosZ", "TanZ", "ApowZ", "LogZ", "ZpowW", "Real", "PolyWorkflow", "Roots", "MatrixWorkflow", "MatSize", "MatVmat", "MatEdit", "MatDet", "MatInv", "MatSimeq", "MatVcol", "Integ", "Solve", "Sol", "Difeq", "Four", "TriSss", "TriAsa", "TriSaa", "TriSas", "TriSsa", "Trans2d", "Trans3d"]`). For each id, assert `PRGM_DISPLAY_SRC.contains(&format!("Op::{}", id))` — fail with `panic!("missing arm: Op::{} not found in prgm_display.rs", id)` if absent. (4) Assert NO `_ =>` wildcard exists at the bottom of the exhaustive match: read the file lines, locate the `op_display_name` function body via simple textual heuristic (find the line containing `fn op_display_name`, then scan forward until the matching closing brace at column 0, OR scan a fixed window of N lines after the fn declaration), and assert no line within that span contains the regex `_\s*=>` (use a simple substring check on `"_ =>"` and `"_=>"` if regex is unavailable — both forms must be absent). Use `std::fs::read_to_string` instead of `include_str!` for this scan ONLY if line-by-line iteration is needed; otherwise `include_str!` suffices. File header carries `#![allow(clippy::unwrap_used)]`. No `pub use` modifications, no `hp41_gui_lib::prgm_display::...` imports, no Rust API calls on `op_display_name` — file-text only.
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test prgm_display_math1_arms -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs`
    - Source: file uses `include_str!("../src/prgm_display.rs")` (or `std::fs::read_to_string` resolved against `CARGO_MANIFEST_DIR`) to load the file text — NO `hp41_gui_lib::prgm_display::*` import, NO direct call to `op_display_name`
    - Source: file iterates a list of Math Pac I variant identifiers (either `MATH_1.ops` from `hp41-core` or a hard-coded slice mirroring the ~40-variant set) and asserts each appears as `Op::<Id>` substring in the loaded source
    - Source: file asserts the literal substrings `"_ =>"` AND `"_=>"` do NOT appear in the slice of `prgm_display.rs` text between the `fn op_display_name` declaration line and the matching closing brace
    - Source: file does NOT contain any `pub use` or other source mutation of `hp41-gui/src-tauri/src/`
    - Test exits 0 when run via `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test prgm_display_math1_arms`
    - Behavior: if a future Phase 28 Op variant is added to MATH_1 without a matching `Op::<Id>` arm in `op_display_name`, the test fails with a clear `missing arm: Op::<Id>` panic message
    - Behavior: if a future commit introduces a `_ =>` wildcard in `op_display_name`, the test fails
  </acceptance_criteria>
  <done>The Phase 28 op_display_name shipment is verified by a file-text regression test that future plans can rely on as a baseline; SC-4 + zero-source-edit invariants both preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → grep subprocess | Task 1 invokes `std::process::Command::new("grep")` against the source tree — read-only, no user input |
| Test process → prgm_display.rs file text | Task 2 reads the file via `include_str!` at compile time — read-only, no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | `prgm_display.rs` could grow a `_ =>` catch-all over time, hiding missed Op-variant arms | mitigate | Task 2 reads the source file text and asserts no `_ =>` exists inside `op_display_name` |
| T-31-01-02 | Tampering | New core/math1 logic could leak into `hp41-gui/src-tauri/src/` (SC-4 violation), bypassing review | mitigate | Task 1's SC-4 grep test runs in CI on every PR; identical pattern to CLAUDE.md narrative |
</threat_model>

<verification>
- `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test sc4_invariant` passes
- `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test prgm_display_math1_arms` passes
- `grep -rn -E "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\(" hp41-gui/src-tauri/src/` exits 1 (no matches)
- `just gui-ci` exits 0
- No production source file in `hp41-gui/src-tauri/src/` is modified
</verification>

<success_criteria>
- Two new tests under `hp41-gui/src-tauri/tests/` exist and pass
- SC-4 grep gate is asserted at test-time, locking the v3.0 baseline
- Every Math Pac I Op variant name has been verified to appear in `prgm_display.rs` via file-text scan
- No `_ =>` catch-all introduced or already present in `op_display_name`
- The "zero source edits in hp41-gui/src-tauri/src/" pledge holds: `prgm_display` stays a private module; no `pub use` widening.
</success_criteria>

<output>
Create `.planning/phases/31-gui-integration/31-01-SUMMARY.md` when done — record:
- Confirmation that SC-4 grep returns nothing
- Count of Math Pac I Op variant identifiers verified by file-text scan (target ~40)
- Confirmation that `prgm_display` module visibility was NOT widened (zero source edits in hp41-gui/src-tauri/src/)
- Any drift discovered (should be none; if any, escalate)
</output>
