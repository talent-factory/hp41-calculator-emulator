---
phase: 31-gui-integration
plan: 05
type: execute
wave: 3
depends_on: ["31-03"]
files_modified:
  - hp41-gui/src-tauri/src/types.rs
  - hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs
  - hp41-gui/src/Display14Seg.tsx
  - hp41-gui/src/Display14Seg.test.tsx
  - hp41-gui/src/pending_input.ts
  - hp41-gui/src/pending_input.test.ts
  - hp41-gui/src/App.tsx
  - hp41-gui/src/App.test.tsx
  - hp41-gui/src-tauri/tests/key_map_stub_error_arms.rs
requirements: [GUI-06, GUI-07]
autonomous: true
must_haves:
  truths:
    - "LCD-alternation: when modal_program.is_some() && entry_buf.is_empty(), CalcStateView.display_str renders state.modal_prompt truncated to 12 chars; when entry_buf is non-empty, it renders entry_buf live"
    - "Prompts longer than 12 chars render with `≡` (U+2261) as the 12th char and the first 11 chars preceding"
    - "Display14Seg SEGMENT_MAP contains an entry for `≡` (U+2261) rendering as a three-bar shape"
    - "R/S key in App.tsx routes 3-way: modal_program.is_some() → submit_modal; is_running → request_cancel; else → existing run_stop"
    - "Esc key routes 4-way: modal_program.is_some() → cancel_modal; is_running → request_cancel; shiftActive → clear; else no-op"
    - "Post-dispatch auto-open useEffect opens PendingInput::XeqByName { mode: 'collect-for-modal' } when modal_program.is_some() && modal_requires_alpha_label && pending_input === null"
    - "PendingInput::XeqByName variant extended with optional mode field ('normal' | 'collect-for-modal'); existing call sites continue to work with default 'normal'"
    - "key_map.rs file-text count of the substring 'is planned for a future phase' is exactly the v2.1 baseline N (locked by the regression test; does NOT shrink AND does NOT silently grow in v3.0 — GUI-07)"
    - "R/S 3-way insertion site is BELOW the pending_input routing block and ABOVE the MODAL_OPENERS intercept (Pitfall 5 — D-07 never-discard invariant)"
  artifacts:
    - path: "hp41-gui/src-tauri/src/types.rs"
      provides: "CalcStateView::from_state extended with a 4th branch at the TOP of the display_str priority chain implementing LCD-alternation (modal_prompt routing); uses truncate_with_continuation helper"
      contains: "truncate_with_continuation"
    - path: "hp41-gui/src/Display14Seg.tsx"
      provides: "SEGMENT_MAP entry for '\\u{2261}' rendering as three-bar [0, 6, 7, 3]"
      contains: "\\u2261"
    - path: "hp41-gui/src/pending_input.ts"
      provides: "XeqByName variant gains optional mode?: 'normal' | 'collect-for-modal'; Enter-handling switches on mode"
      contains: "collect-for-modal"
    - path: "hp41-gui/src/App.tsx"
      provides: "R/S 3-way + Esc cascade + post-dispatch auto-open useEffect + magic-prefix routing for submit_modal_with_label"
      contains: "submit_modal"
    - path: "hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs"
      provides: "CalcStateView::from_state with modal_program.is_some() + entry_buf empty + modal_prompt set produces truncated display_str"
    - path: "hp41-gui/src-tauri/tests/key_map_stub_error_arms.rs"
      provides: "Stub-error message-count regression test locking exact baseline N (GUI-07 preserved); file-text scan only"
  key_links:
    - from: "hp41-gui/src-tauri/src/types.rs::CalcStateView::from_state"
      to: "CalcState.modal_prompt + entry_buf + modal_program"
      via: "field reads + truncate_with_continuation transformation; new branch sits at TOP of the existing display_str priority chain"
      pattern: "modal_program.is_some\\(\\) && .* entry_buf.is_empty"
    - from: "hp41-gui/src/App.tsx::invokeForKey"
      to: "hp41-gui/src-tauri/src/commands.rs (request_cancel + submit_modal + run_stop thunks)"
      via: "@tauri-apps/api/core::invoke"
      pattern: "invoke<.*>\\('(request_cancel|submit_modal|run_stop|cancel_modal|submit_modal_with_label)'\\)"
    - from: "hp41-gui/src/App.tsx::useEffect (post-dispatch auto-open)"
      to: "calcState.modal_program_active + modal_requires_alpha_label"
      via: "useEffect dep array [calcState, pendingInput]"
      pattern: "modal_requires_alpha_label"
---

<objective>
Land the user-facing GUI behaviors that complete Phase 31: (a) LCD-alternation rendering of Math Pac I modal prompts via a NEW 4th branch at the TOP of `CalcStateView::from_state`'s `display_str` priority chain in `hp41-gui/src-tauri/src/types.rs` + `truncate_with_continuation` helper + `≡` (U+2261) glyph added to Display14Seg SEGMENT_MAP (per D-31.5 / D-31.6 / Pitfall 6); (b) R/S 3-way state-routed dispatch in `App.tsx` per D-31.1 (modal_program → submit_modal; is_running → request_cancel; else run_stop); (c) Esc cascade per D-31.2 (modal_program → cancel_modal; is_running → request_cancel; shiftActive → clear; else no-op); (d) post-dispatch auto-open `useEffect` per D-29.9 GUI mirror that opens `PendingInput::XeqByName { mode: 'collect-for-modal' }` when a modal is active AND requires an alpha label AND no pending input is currently open; (e) `pending_input.ts` variant extension with the `mode` discriminator + magic-prefix routing to `submit_modal_with_label` for the CollectForModal Enter branch (Pitfall 15); (f) GUI-07 stub-arm-policy file-text regression test asserting `key_map.rs` substring count is exactly the v2.1 baseline N (no shrink, no silent growth).

Routing-site decision (W2 fix): the LCD-alternation routing lives in `hp41-gui/src-tauri/src/types.rs::CalcStateView::from_state` (the 4th branch at the TOP of the existing display_str priority chain), NOT in `hp41-gui/src-tauri/src/commands.rs::handle_get_state`. Rationale: `state.display_override` is reserved for Phase 21 VIEW/AVIEW/PROMPT/CLD semantics per `hp41-core/src/state.rs:142-143` and is cleared at the top of dispatch — routing modal_prompt through `display_override` would collide with the Phase 21 contract. Placing the new branch in `from_state` keeps modal_prompt handling separate from Phase 21 `display_override` and isolates the GUI-only change to one file. `commands.rs::handle_get_state` is NOT modified for LCD-alternation in this plan.

Purpose: GUI-06 + GUI-07 acceptance. This is the final user-facing surface — after this plan, a user can `XEQ "MATRIX" 2 ENTER 1 ENTER 2 ENTER 3 ENTER 4 ENTER XEQ "DET"` and see `ORDER=?` → `A1,1=?` → ... → `-2.0000` round-trip through the GUI exactly as a real HP-41C with Math Pac I would behave.

Output: 5 modified production files (types.rs, Display14Seg.tsx, pending_input.ts, App.tsx + its tests), 4 new tests (LCD-alternation Rust test, Display14Seg ≡ snapshot, App.tsx R/S 3-way + Esc cascade vitest cases, key_map stub-arm file-text regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gui-integration/31-CONTEXT.md
@.planning/phases/31-gui-integration/31-RESEARCH.md
@.planning/phases/31-gui-integration/31-PATTERNS.md
@.planning/phases/31-gui-integration/31-UI-SPEC.md
@.planning/phases/31-gui-integration/31-VALIDATION.md

# Existing files this plan reads or modifies
@hp41-gui/src-tauri/src/commands.rs
@hp41-gui/src-tauri/src/types.rs
@hp41-gui/src-tauri/src/lib.rs
@hp41-gui/src/App.tsx
@hp41-gui/src/Display14Seg.tsx
@hp41-gui/src/pending_input.ts
@hp41-gui/src-tauri/src/key_map.rs
@hp41-cli/src/app.rs
@hp41-core/src/ops/math1/modal.rs
@hp41-core/src/state.rs

<interfaces>
<!-- Existing CalcStateView::from_state in hp41-gui/src-tauri/src/types.rs (around lines 75-81): -->
<!--   let display_str = if !state.entry_buf.is_empty() { state.entry_buf.clone() } -->
<!--   else if state.alpha_mode { format_alpha(&state.alpha_reg) } -->
<!--   else { format_hpnum(&state.stack.x, &state.display_mode) }; -->
<!-- Plan 31-05 (W2 fix — committed routing path) adds a new TOP-priority branch: -->
<!--   if state.modal_program.is_some() && state.entry_buf.is_empty() && state.modal_prompt.is_some() -->
<!--     { display_str = truncate_with_continuation(state.modal_prompt.as_ref().unwrap()) } -->
<!--   else if !state.entry_buf.is_empty() { ... existing chain unchanged ... } -->

<!-- display_override is RESERVED for Phase 21 VIEW/AVIEW/PROMPT/CLD per hp41-core/src/state.rs:142-143; -->
<!-- it is cleared at the top of dispatch. Plan 31-05 must NOT route modal_prompt through display_override -->
<!-- (collision risk). The 4th-branch-at-top-of-from_state path avoids the collision entirely. -->

<!-- Existing handle_get_state in hp41-gui/src-tauri/src/commands.rs ~line 228-232 (NOT modified by 31-05): -->
<!--   pub fn handle_get_state(calc: &mut CalcState) -> Result<CalcStateView, GuiError> { -->
<!--       let print_lines = calc.print_buffer.drain(..).collect(); -->
<!--       let event_lines = calc.event_buffer.drain(..).collect(); -->
<!--       Ok(CalcStateView::from_state(calc, print_lines, event_lines)) -->
<!--   } -->

<!-- Existing PendingInput::XeqByName variant in pending_input.ts ~line 58: -->
<!--   | { kind: 'xeq_name'; acc: string; dispatchPrefix: 'xeq' | 'gto' | 'lbl' } -->
<!-- Plan 31-05 extends with optional mode?: 'normal' | 'collect-for-modal' -->

<!-- Existing invokeForKey in App.tsx ~lines 62-67: -->
<!--   async function invokeForKey(effectiveId: string): Promise<CalcStateView> { -->
<!--     if (effectiveId === 'sst') return invoke<CalcStateView>('sst_step'); -->
<!--     if (effectiveId === 'bst') return invoke<CalcStateView>('bst_step'); -->
<!--     if (effectiveId === 'r_s') return invoke<CalcStateView>('run_stop'); -->
<!--     return invoke<CalcStateView>('dispatch_op', { keyId: effectiveId }); -->
<!--   } -->

<!-- Display14Seg SEGMENT_MAP currently has entries like (around lines 131-184): -->
<!--   '=': [6, 7, 3], -->
<!-- Plan 31-05 adds: '\u{2261}': [0, 6, 7, 3] for three-bar -->

<!-- CLI ground truth for R/S submit at hp41-cli/src/app.rs lines 664-683 -->
<!-- CLI ground truth for Esc cancel at hp41-cli/src/app.rs lines 685-698 -->
<!-- CLI ground truth for post-dispatch auto-open at hp41-cli/src/app.rs lines 1782-1795 -->

<!-- CalcStateView fields from Plan 31-03: is_running, modal_program_active, modal_requires_alpha_label, modal_prompt -->

<!-- key_map.rs visibility (for Task 4): `mod key_map;` (PRIVATE) in hp41-gui/src-tauri/src/lib.rs:8 — -->
<!-- NOT pub. Test code therefore CANNOT call `hp41_gui_lib::key_map::resolve(...)`. -->
<!-- Task 4 is a PURE FILE-TEXT SCAN (no Rust API access; no visibility widening). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LCD-alternation routing in types.rs::from_state + ≡ glyph in Display14Seg SEGMENT_MAP + regression tests</name>
  <files>
    hp41-gui/src-tauri/src/types.rs
    hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs
    hp41-gui/src/Display14Seg.tsx
    hp41-gui/src/Display14Seg.test.tsx
  </files>
  <read_first>
    - hp41-gui/src-tauri/src/types.rs around lines 75-81 (display_str priority chain inside `CalcStateView::from_state`) — confirm exact line numbers and the chain ordering
    - hp41-gui/src-tauri/src/commands.rs around lines 228-232 (handle_get_state — NOT modified by this plan; reading only to confirm the call site of from_state)
    - hp41-gui/src-tauri/src/Display14Seg.tsx (no — actually `hp41-gui/src/Display14Seg.tsx`) around lines 131-184 (SEGMENT_MAP definition; specifically the `=` entry as a structural sibling for `≡`)
    - hp41-gui/src/Display14Seg.test.tsx (existing snapshot tests as test-shape analog)
    - hp41-core/src/state.rs (confirm `modal_prompt: Option<String>`, `modal_program: Option<ModalProgram>`, and `display_override: Option<String>` field types; lines 142-143 confirm `display_override` is reserved for Phase 21 VIEW/AVIEW/PROMPT/CLD and cleared at top of dispatch)
    - .planning/phases/31-gui-integration/31-UI-SPEC.md (Typography table line 71 — modal prompt at LCD 22px; line 78 ≡ continuation marker)
    - .planning/phases/31-gui-integration/31-RESEARCH.md §"Code Examples / Example 8" lines 757-777 (truncate_with_continuation helper sketch) + §"Pitfall 6" lines 530-538 (SEGMENT_MAP gap)
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-05 — handle_get_state" lines 571-643 (pattern; the W2 fix relocates the routing from handle_get_state to from_state but the helper shape is the same) + §"Plan 31-05 — Display14Seg" lines 849-876
  </read_first>
  <action>
    Commit the LCD-alternation routing to the `types.rs::from_state` path (W2 fix — `display_override` route rejected: reserved for Phase 21 per `hp41-core/src/state.rs:142-143`, cleared at top of dispatch; routing modal_prompt through it would collide). In `hp41-gui/src-tauri/src/types.rs`: add a module-level const pair near the top of the file: `const LCD_WIDTH: usize = 12;` and `const CONTINUATION: char = '\u{2261}';`. Add a private helper `fn truncate_with_continuation(s: &str) -> String` that collects chars via `s.chars().collect::<Vec<char>>()`, returns `s.to_string()` if `chars.len() <= LCD_WIDTH`, else takes the first `LCD_WIDTH - 1` chars and appends `CONTINUATION`. Use Unicode-correct char iteration (NOT byte indexing) so multi-byte chars are handled. In `CalcStateView::from_state`, add a NEW 4th branch at the TOP of the existing display_str priority chain — placed BEFORE the existing `if !state.entry_buf.is_empty()` branch — checking `state.modal_program.is_some() && state.entry_buf.is_empty() && state.modal_prompt.is_some()` and emitting `truncate_with_continuation(state.modal_prompt.as_ref().expect("modal_prompt set in guard above"))`. The existing display_str fallthroughs (entry_buf live, alpha_mode, format_hpnum) remain unchanged and still apply when the new top branch's predicate is false. `hp41-gui/src-tauri/src/commands.rs::handle_get_state` is NOT modified. Author `hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs` with five `#[test]` functions exercising the new from_state branch directly via `CalcStateView::from_state(&mut calc, vec![], vec![])`: (a) `prompt_under_12_chars_no_truncation` (set modal_program + modal_prompt = `"ORDER=?"`, leave entry_buf empty → resulting `view.display_str == "ORDER=?"` verbatim); (b) `prompt_at_12_chars_no_truncation` (prompt `"DEGREE=?ABCD"` → `"DEGREE=?ABCD"` verbatim); (c) `prompt_at_13_chars_truncated_with_marker` (prompt `"NO. SAMPLES=?"` → first 11 chars + `≡`); (d) `prompt_at_14_chars_truncated` (prompt `"FUNCTION NAME?"` → `"FUNCTION NAM\u{2261}"`); (e) `entry_buf_nonempty_overrides_prompt` (set modal_program + modal_prompt AND `entry_buf = "123"` → `display_str == "123"`, NOT the prompt). All five tests construct a fresh `CalcState`, set fields by direct field assignment (no need to actually run an op), and call `CalcStateView::from_state`. In `hp41-gui/src/Display14Seg.tsx`: locate the SEGMENT_MAP literal around lines 131-184 and add a new entry `'\u{2261}': [0, 6, 7, 3]` (top + middle + bottom three-bar shape per Assumption A4 in RESEARCH). Place near the `'='` entry for spatial cohesion. In `hp41-gui/src/Display14Seg.test.tsx`: add `test('renders ≡ (U+2261) HP-41 continuation marker as three-bar')` rendering `<Display14Seg text="FUNCTION NAM\u{2261}" />` and asserting that the SVG cell at position 11 has the expected segment data. Use the assertion style of existing SEGMENT_MAP tests in the file.
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test lcd_alternation_modal_prompt && cd hp41-gui && npm test -- Display14Seg</automated>
  </verify>
  <acceptance_criteria>
    - Source: `hp41-gui/src-tauri/src/types.rs` contains the constants `LCD_WIDTH = 12` AND `CONTINUATION = '\u{2261}'`
    - Source: `hp41-gui/src-tauri/src/types.rs` contains a `truncate_with_continuation` helper using `s.chars()` for Unicode-correct iteration
    - Source: `CalcStateView::from_state` contains the new top branch BEFORE the existing `display_str` fallthrough; the branch references `state.modal_program.is_some()`, `state.entry_buf.is_empty()`, AND `state.modal_prompt.is_some()`
    - Source: `hp41-gui/src-tauri/src/commands.rs::handle_get_state` is NOT modified for LCD-alternation (search the file diff for changes to handle_get_state and assert none beyond what other Phase 31 plans introduced for non-LCD reasons)
    - Source: NO new reference to `display_override` is introduced in `types.rs` or `commands.rs` by this plan (display_override remains a Phase 21 channel, untouched)
    - Source: `hp41-gui/src/Display14Seg.tsx` SEGMENT_MAP contains a literal `'\u{2261}'` (or its escaped form) mapped to a segments array
    - File exists at `hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs` with the 5 named test functions
    - File exists at `hp41-gui/src/Display14Seg.test.tsx` (modified) with the new `≡` rendering test case
    - CLI: `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test lcd_alternation_modal_prompt` exits 0
    - CLI: `cd hp41-gui && npm test -- Display14Seg` exits 0 (existing snapshots + new ≡ test pass)
    - Behavior: in `just gui-dev`, after `XEQ "MATRIX" Enter` the LCD shows `ORDER=?`; after the user enters `3 Enter` the LCD shows `A1,1=?` (manual verification per VALIDATION.md "Matrix-entry modal UX")
  </acceptance_criteria>
  <done>LCD-alternation is wired end-to-end via `types.rs::from_state` (not `handle_get_state` and not `display_override`): long prompts truncate with the HP-41 ≡ marker; entry_buf overrides; the routing is automated-test-locked.</done>
</task>

<task type="auto">
  <name>Task 2: PendingInput::XeqByName mode extension + magic-prefix routing + exhaustive switch preservation</name>
  <files>
    hp41-gui/src/pending_input.ts
    hp41-gui/src/pending_input.test.ts
  </files>
  <read_first>
    - hp41-gui/src/pending_input.ts (full file — discriminated-union declaration ~line 58, handleModalKey function with the `xeq_name` case ~lines 335-363, renderModalLcd function)
    - hp41-gui/src/pending_input.test.ts (existing tests — extend by adding new test cases without breaking existing ones)
    - hp41-cli/src/app.rs ~lines 44-50 (XeqByNameMode enum source) + ~lines 134-138 (PendingInput::XeqByName variant with `mode` field — port verbatim shape to TypeScript)
    - .planning/phases/31-gui-integration/31-RESEARCH.md §"Pattern 4" lines 354-363 + §"Pitfall 13" lines 1085-1097 (backward-compat optional field) + §"Pitfall 15" lines 1112-1138 (magic-prefix routing for submit_modal_with_label)
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-05 — hp41-gui/src/pending_input.ts" lines 774-846
  </read_first>
  <action>
    In `hp41-gui/src/pending_input.ts`: extend the `XeqByName` variant in the discriminated union (around line 58) by adding an optional `mode?: 'normal' | 'collect-for-modal'` field. Existing call sites that omit `mode` continue to work (treated as 'normal' by default). Locate the existing `case 'xeq_name':` arm inside `handleModalKey` (around lines 335-363) and, within the Enter-handling branch, add a check: if `pending.mode === 'collect-for-modal'` AND `pending.acc.length > 0`, return `{ nextPending: null, dispatchId: '__submit_modal_with_label__' + pending.acc, consumesShift: false }`. Otherwise (mode is 'normal' or undefined), preserve the existing behavior of returning `dispatchId: pending.dispatchPrefix + '_' + pending.acc`. Magic-prefix pattern precedent: existing `makeKeyCodeMagic` ASN flow uses the same `__<prefix>__` shape. Ensure every existing switch / match over `pending.kind` remains exhaustive (no `default:` per FN-GUI-04 invariant per CLAUDE.md / 31-CONTEXT.md line 52). If the planner introduces a sub-switch over `pending.mode`, it MUST exhaust `'normal' | 'collect-for-modal'` with explicit arms (no `default:`). In `hp41-gui/src/pending_input.test.ts`: add at least 3 new test cases: (a) `xeq_name normal mode Enter returns xeq_<acc> dispatchId` (preserves existing behavior), (b) `xeq_name collect-for-modal Enter returns __submit_modal_with_label__<acc> magic-prefix dispatchId`, (c) `xeq_name collect-for-modal Backspace removes last char without changing mode`. File header carries any existing setup imports.
  </action>
  <verify>
    <automated>cd hp41-gui && npm test -- pending_input</automated>
  </verify>
  <acceptance_criteria>
    - Source: `hp41-gui/src/pending_input.ts` `XeqByName` variant declaration contains `mode?: 'normal' | 'collect-for-modal'`
    - Source: `handleModalKey` `case 'xeq_name':` Enter branch contains a conditional referencing `pending.mode === 'collect-for-modal'`
    - Source: magic-prefix string `__submit_modal_with_label__` appears in the file
    - Source: no `default:` catch-all appears in any switch over `pending.kind` or `pending.mode`
    - Source: `hp41-gui/src/pending_input.test.ts` contains the 3 new test cases (matched by descriptive names)
    - CLI: `cd hp41-gui && npm test -- pending_input` exits 0 (existing tests + new ones all pass)
    - CLI: `cd hp41-gui && npx tsc --noEmit -p .` exits 0
    - Behavior: a CollectForModal `PendingInput::XeqByName { acc: "F", mode: "collect-for-modal" }` with Enter pressed yields the magic-prefix dispatchId
  </acceptance_criteria>
  <done>PendingInput.ts is the TypeScript mirror of Phase 29's Rust XeqByNameMode enum; existing call sites remain compatible; FN-GUI-04 exhaustive-match invariant preserved.</done>
</task>

<task type="auto">
  <name>Task 3: R/S 3-way + Esc cascade + post-dispatch auto-open + magic-prefix invokeForKey routing in App.tsx</name>
  <files>
    hp41-gui/src/App.tsx
    hp41-gui/src/App.test.tsx
  </files>
  <read_first>
    - hp41-gui/src/App.tsx full file — focus on: CalcStateView interface mirror (~lines 23-40), invokeForKey function (~lines 62-67), handleClick (~lines 311-374), handleKeyDown / Esc-handling (~lines 414-486), toast machinery (~lines 201-214), busyRef/pendingActionRef debounce (~lines 50-60), MODAL_OPENERS table (~line 152)
    - hp41-gui/src/App.test.tsx (existing tests — extend with new test cases; understand the test setup harness)
    - hp41-cli/src/app.rs lines 664-683 (CLI R/S submit ground truth) + lines 685-698 (CLI Esc cancel) + lines 1782-1795 (CLI maybe_auto_open_collect_for_modal)
    - hp41-gui/src/pending_input.ts (extended in Task 2 — confirm magic-prefix shape)
    - .planning/phases/31-gui-integration/31-RESEARCH.md §"Pattern 5" lines 365-388 (post-dispatch auto-open verbatim template) + §"Pitfall 5" lines 520-528 (R/S 3-way insertion-site requirement) + §"Pitfall 15" lines 1110-1138 (magic-prefix routing)
    - .planning/phases/31-gui-integration/31-PATTERNS.md §"Plan 31-05 — App.tsx" lines 646-770
  </read_first>
  <action>
    In `hp41-gui/src/App.tsx`: refactor `invokeForKey` to accept the current CalcStateView as a parameter (or close-over the latest calcState via a ref). Signature becomes `async function invokeForKey(effectiveId: string, state: CalcStateView | null): Promise<CalcStateView>`. Extend the `if (effectiveId === 'r_s')` branch with state-aware routing per D-31.1 priority: (1) if `state?.modal_program_active`, `return invoke<CalcStateView>('submit_modal')`; (2) else if `state?.is_running`, `await invoke<void>('request_cancel'); return invoke<CalcStateView>('get_state')`; (3) else `return invoke<CalcStateView>('run_stop')`. Add a magic-prefix route at the TOP of invokeForKey: if `effectiveId.startsWith('__submit_modal_with_label__')`, extract the label after the prefix, call `return invoke<CalcStateView>('submit_modal_with_label', { label })`. Update every existing call site of `invokeForKey` to pass the current `calcState`. In `handleClick`, locate the existing `pending_input !== null` block (~line 312-372) and the MODAL_OPENERS intercept (~line 374). Per Pitfall 5: the new R/S 3-way state-aware dispatch MUST be inserted BETWEEN these two — AFTER pending_input handling, BEFORE MODAL_OPENERS — so an active CollectForModal modal cannot be silently discarded by an R/S press (D-07 never-discard invariant). In `handleKeyDown`, locate the existing Esc handler (~lines 414-486) which currently routes: helpOpen-close → pendingInput-clear-and-shiftActive-clear → shiftActive-clear. Insert TWO new priority arms between the pendingInput-clear branch and the shiftActive-clear branch, per D-31.2: (a) if `calcState?.modal_program_active`, dispatch `invoke<CalcStateView>('cancel_modal')` and refresh state (then return); (b) else if `calcState?.is_running`, dispatch `invoke<void>('request_cancel')` then `invoke<CalcStateView>('get_state')` and update state (then return). Both new branches use the existing `extractErrMessage` + `showToast` pattern on rejection. Add a new `useEffect` for the post-dispatch auto-open per D-29.9 (verbatim template from RESEARCH Pattern 5 / Example 6): dependencies `[calcState, pendingInput]`; early-return if `!calcState` or `pendingInput !== null` or `!calcState.modal_program_active` or `!calcState.modal_requires_alpha_label`; otherwise `setPendingInput({ kind: 'xeq_name', dispatchPrefix: 'xeq', acc: '', mode: 'collect-for-modal' })`. In `hp41-gui/src/App.test.tsx`: add at least 5 new test cases: (a) `R/S with modal_program_active calls submit_modal` — mock calcState with `modal_program_active: true`, simulate R/S click, assert `invoke` was called with `'submit_modal'`; (b) `R/S with is_running calls request_cancel then get_state` — mock with `is_running: true, modal_program_active: false`; (c) `R/S with neither flag calls run_stop` (existing behavior baseline); (d) `Esc with modal_program_active calls cancel_modal`; (e) `useEffect auto-opens xeq_name CollectForModal when modal_requires_alpha_label is true and no pending_input`. Use the existing vitest mock-invoke harness from `App.test.tsx`.
  </action>
  <verify>
    <automated>cd hp41-gui && npm test -- App.test</automated>
  </verify>
  <acceptance_criteria>
    - Source: `hp41-gui/src/App.tsx` `invokeForKey` signature has been extended with a `state: CalcStateView | null` parameter
    - Source: `invokeForKey` body contains conditional routes for `state?.modal_program_active` AND `state?.is_running` AND a `__submit_modal_with_label__` prefix check
    - Source: `invokeForKey` body calls `invoke<*>('submit_modal')`, `invoke<*>('request_cancel')`, `invoke<*>('submit_modal_with_label', { label })`, AND `invoke<*>('run_stop')` (the existing call)
    - Source: `handleClick` inserts the R/S 3-way state-aware path AFTER the existing `pending_input !== null` block (Pitfall 5 — D-07 never-discard); confirm by reading the surrounding lines
    - Source: `handleKeyDown` Esc branch contains conditional routes for `calcState?.modal_program_active` AND `calcState?.is_running` between the pendingInput-clear and shiftActive-clear branches
    - Source: a new `useEffect` exists with dependencies `[calcState, pendingInput]` whose body calls `setPendingInput` with `mode: 'collect-for-modal'`
    - Source: `hp41-gui/src/App.test.tsx` has at least the 5 new test cases for R/S 3-way + Esc cascade + auto-open
    - CLI: `cd hp41-gui && npm test -- App.test` exits 0
    - CLI: `cd hp41-gui && npx tsc --noEmit -p .` exits 0
    - Behavior (manual): pressing R/S while a MATRIX prompt is open advances the modal step; pressing Esc cancels; long INTG run cancellable within 100ms via R/S or Esc
  </acceptance_criteria>
  <done>The user-facing keyboard contract is wired end-to-end with vitest coverage; D-31.1/D-31.2/D-29.9 + Pitfall 5 insertion-site invariant + Pitfall 15 magic-prefix routing all preserved.</done>
</task>

<task type="auto">
  <name>Task 4: GUI-07 stub-arm file-text count regression test for key_map.rs (exact baseline N lock)</name>
  <files>
    hp41-gui/src-tauri/tests/key_map_stub_error_arms.rs
  </files>
  <read_first>
    - hp41-gui/src-tauri/src/key_map.rs (full file — locate the v2.1 stub-error arm that returns `GuiError { message: format!("'{key_id}' is planned for a future phase") }` for the alternation of ~20 ids: `asn`, `catalog`, `view`, `xeq_prompt`, `gto_prompt`, `lbl_prompt`, `sto_prompt`, `rcl_prompt`, `isg_prompt`, `sf_prompt`, `cf_prompt`, `fs_prompt`, `fix_prompt`, `sci_prompt`, `eng_prompt`, `x_eq_y_prompt`, `x_le_y_prompt`, `x_gt_y_prompt`, `x_eq_0_prompt`, `tone`)
    - hp41-gui/src-tauri/src/lib.rs lines 6-11 (confirm `mod key_map;` declared PRIVATE — NOT `pub mod`; test CANNOT call `hp41_gui_lib::key_map::resolve(...)`)
    - .planning/phases/31-gui-integration/31-VALIDATION.md task 31-05-03 (grep-based verification expectation)
    - .planning/phases/31-gui-integration/31-CONTEXT.md lines 64-66 (D-28.6 + GUI-07 contract: Math Pac I uses XEQ-by-name only; key_map stub arms do NOT shrink in v3.0)
    - CLAUDE.md v2.1 additions block (stub-error pattern D-5 description)
  </read_first>
  <action>
    B2 + W6 fix — file-text scan only, exact-baseline-N lock, no Rust API access. RATIONALE: `key_map` is declared `mod key_map;` (private) in `hp41-gui/src-tauri/src/lib.rs:8`, so `hp41_gui_lib::key_map::resolve(...)` is NOT available to test code. Calling it would require widening visibility, which Plan 31-05 must not do. The file-text grep-count test is sufficient by itself — it catches both deletion AND silent addition of stub-arm ids. STEP A — capture the baseline: run `grep -c "is planned for a future phase" hp41-gui/src-tauri/src/key_map.rs` against the current tree to obtain the exact baseline count N (the format! template `'{key_id}' is planned for a future phase` appears once in the v2.1 file → N = 1; verify against the current tree immediately before authoring the test). STEP B — author `hp41-gui/src-tauri/tests/key_map_stub_error_arms.rs` with one `#[test] fn stub_error_message_count_locked_to_v21_baseline()`. The test uses `include_str!("../src/key_map.rs")` (integration tests under `hp41-gui/src-tauri/tests/` resolve relative to that directory, so `../src/key_map.rs` reaches the production file) to load the file text at test-compile-time. Define `const BASELINE_N: usize = <N captured in STEP A>;` near the top of the test with a doc comment: `/// Baseline locked v3.0 — exactly N occurrences of the format! template. /// Increase ONLY after explicit Phase 31+ review approving a new stub arm. /// Decrease is REGRESSION — Math Pac I uses XEQ-by-name only (D-28.6 + GUI-07).` Then assert `KEY_MAP_SRC.matches("is planned for a future phase").count() == BASELINE_N` with `assert_eq!` (NOT `>=`; exact equality). The exact-match assertion catches BOTH shrinkage (regression: an arm removed by mistake — would reduce count below baseline) AND silent growth (regression: a new stub arm added without explicit Phase 31 review — would push count above baseline). Add a second `#[test] fn key_map_file_contains_v21_baseline_ids()` that asserts each of the v2.1 id literals appears in the file text via `include_str!`: iterate over a hard-coded `BASELINE_IDS: &[&str] = &["\"asn\"", "\"catalog\"", "\"view\"", "\"xeq_prompt\"", "\"gto_prompt\"", "\"lbl_prompt\"", "\"sto_prompt\"", "\"rcl_prompt\"", "\"isg_prompt\"", "\"sf_prompt\"", "\"cf_prompt\"", "\"fs_prompt\"", "\"fix_prompt\"", "\"sci_prompt\"", "\"eng_prompt\"", "\"x_eq_y_prompt\"", "\"x_le_y_prompt\"", "\"x_gt_y_prompt\"", "\"x_eq_0_prompt\"", "\"tone\""]` and for each, assert `KEY_MAP_SRC.contains(id)`. This second test asserts ID-set fidelity in addition to the format!-template count lock. File header carries `#![allow(clippy::unwrap_used)]`. NO `hp41_gui_lib::key_map::*` imports, NO `pub mod` widening of `key_map`, NO calls to `resolve()` — file-text only.
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test key_map_stub_error_arms</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `hp41-gui/src-tauri/tests/key_map_stub_error_arms.rs`
    - Source: file uses `include_str!("../src/key_map.rs")` to load the file text — NO `hp41_gui_lib::key_map::*` import, NO call to `resolve()`
    - Source: file declares `const BASELINE_N: usize = <captured N>;` with a doc comment explaining the lock semantics
    - Source: `stub_error_message_count_locked_to_v21_baseline` uses `assert_eq!(..., BASELINE_N)` (NOT `>=`) on `KEY_MAP_SRC.matches("is planned for a future phase").count()`
    - Source: `key_map_file_contains_v21_baseline_ids` iterates a hard-coded slice of v2.1 id literals (at least the 20 ids enumerated above) and asserts each appears in the file text
    - Source: NO modification to `hp41-gui/src-tauri/src/key_map.rs` (no visibility widening; no production-source changes)
    - CLI: `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test key_map_stub_error_arms` exits 0
    - CLI: `grep -c "is planned for a future phase" hp41-gui/src-tauri/src/key_map.rs` returns exactly BASELINE_N (consistent across both the grep and the test)
    - Behavior: if a future plan tries to shrink the stub-arm policy by removing an id, both tests fail with a count mismatch and a missing-id panic respectively
    - Behavior: if a future plan adds a new stub arm without updating BASELINE_N, the count test fails — forcing explicit Phase 31+ review
  </acceptance_criteria>
  <done>GUI-07 stub-arm policy is locked behind a file-text regression test that asserts the v2.1 baseline format!-template count is preserved EXACTLY (no shrink AND no silent growth); the 20 v2.1 id literals are asserted present by name; NO production source mutation; `key_map` stays a private module.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test lcd_alternation_modal_prompt` exits 0
- `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --test key_map_stub_error_arms` exits 0
- `cd hp41-gui && npm test -- Display14Seg` exits 0
- `cd hp41-gui && npm test -- pending_input` exits 0
- `cd hp41-gui && npm test -- App.test` exits 0
- `cd hp41-gui && npx tsc --noEmit -p .` exits 0
- `cd hp41-gui && npm run build` exits 0
- `bash scripts/check-tauri-permissions.sh` exits 0 (no new commands in this plan)
- `grep -rn -E "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)\\(" hp41-gui/src-tauri/src/` returns nothing
- `just gui-ci` exits 0
- Plan 31-03's D-25.6 parity test still passes (no regression introduced)
</verification>

<success_criteria>
- LCD-alternation: modal_prompt renders on the LCD when entry_buf is empty; truncates with `≡` for >12-char prompts; routing lives in `types.rs::from_state` (NOT `handle_get_state`, NOT `display_override`)
- R/S 3-way (modal/cancel/run-stop) and Esc 4-way (modal/cancel/shift/no-op) routing is wired in App.tsx with vitest coverage
- Post-dispatch auto-open useEffect mirrors CLI's maybe_auto_open_collect_for_modal verbatim
- PendingInput::XeqByName variant extended with optional mode discriminator; FN-GUI-04 exhaustive-match invariant preserved
- Magic-prefix __submit_modal_with_label__<label> routes through invokeForKey to the submit_modal_with_label Tauri thunk
- GUI-07 stub-arm policy preserved (regression test asserts exact baseline count match; no shrink, no silent growth)
- Display14Seg renders ≡ (U+2261) as three-bar SEGMENT_MAP entry
- D-07 never-discard invariant preserved by inserting R/S 3-way handler BELOW pending_input routing
- `key_map` and `prgm_display` remain private modules (no `pub mod` widening anywhere in Phase 31)
</success_criteria>

<output>
Create `.planning/phases/31-gui-integration/31-05-SUMMARY.md` when done — record:
- Final placement of the LCD-alternation routing — `types.rs::from_state` (W2 fix committed); confirm `handle_get_state` and `display_override` both UNCHANGED for LCD-alternation
- Baseline count BASELINE_N captured for key_map.rs `"is planned for a future phase"` substring (locked by the regression test)
- Pitfall 5 (R/S insertion-site) verification — confirm by quoting the surrounding 5 lines of context in App.tsx around the new R/S branch
- Manual verification of MATRIX-entry modal UX per VALIDATION.md "Manual-Only Verifications"
- Wave 3 ship gates green: full `just gui-ci` + WebdriverIO smoke (Ubuntu) green
</output>
