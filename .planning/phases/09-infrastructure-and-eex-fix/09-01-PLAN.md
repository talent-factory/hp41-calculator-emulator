---
phase: 09-infrastructure-and-eex-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Workspace Cargo.toml declares rust-version = \"1.85\""
    - "Workspace dependency rust_decimal is pinned to 1.42 (was 1.41)"
    - "CI runs an MSRV verification job pinned to Rust 1.85 that executes just ci"
    - "MSRV CI job runs in parallel with existing test job (no needs: dependency)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace MSRV declaration and rust_decimal version pin"
      contains: "rust-version = \"1.85\""
    - path: ".github/workflows/ci.yml"
      provides: "CI matrix including MSRV verification job"
      contains: "dtolnay/rust-toolchain@1.85"
  key_links:
    - from: "Cargo.toml [workspace.package]"
      to: "rust-version field"
      via: "workspace inheritance"
      pattern: "rust-version\\s*=\\s*\"1\\.85\""
    - from: ".github/workflows/ci.yml jobs.msrv"
      to: "just ci"
      via: "GitHub Actions step"
      pattern: "just ci"
---

<objective>
Declare and enforce MSRV 1.85 for the HP-41 workspace and bump rust_decimal from 1.41 to 1.42.

Purpose: Resolves the implicit MSRV discrepancy (clap 4.6.1 requires Rust 1.74+, but no explicit MSRV is declared, leaving us vulnerable to silent dependency-driven version creep). Adds a CI gate that verifies the declared MSRV is real, not aspirational.

Output:
- `Cargo.toml` updated with `rust-version = "1.85"` at `[workspace.package]` level and `rust_decimal = "1.42"` in `[workspace.dependencies]`
- `.github/workflows/ci.yml` updated with a new `msrv` job that pins `dtolnay/rust-toolchain@1.85` and runs `just ci`
- `cargo build --workspace` succeeds on Rust 1.85 toolchain
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-infrastructure-and-eex-fix/09-CONTEXT.md
@Cargo.toml
@.github/workflows/ci.yml
@justfile

<interfaces>
<!-- Existing workspace manifest layout (from Cargo.toml) -->

```toml
[workspace]
resolver = "2"
members = ["hp41-core", "hp41-cli"]

[workspace.dependencies]
rust_decimal = "1.41"
thiserror = "2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

<!-- Existing CI job pattern (from .github/workflows/ci.yml) -->

The `lint`, `test`, and `coverage` jobs all use `dtolnay/rust-toolchain@stable`.
The new `msrv` job MUST use `dtolnay/rust-toolchain@1.85` instead.
The `coverage` job uses `needs: test`. The new `msrv` job MUST have NO `needs:` field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump MSRV declaration and rust_decimal version in workspace Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current workspace manifest — must understand existing layout before editing)
    - .planning/phases/09-infrastructure-and-eex-fix/09-CONTEXT.md (D-11 and D-12 for exact values)
  </read_first>
  <action>
Edit `Cargo.toml` (the workspace root manifest) to make exactly two changes:

1. Add a new `[workspace.package]` table immediately after the `[workspace]` table that contains:

```toml
[workspace.package]
rust-version = "1.85"
```

This is per D-11 (CONTEXT.md). Place it BEFORE `[workspace.dependencies]`.

2. In `[workspace.dependencies]`, change the `rust_decimal` line from:

```toml
rust_decimal = "1.41"
```

to:

```toml
rust_decimal = "1.42"
```

This is per D-12 (CONTEXT.md).

DO NOT modify `thiserror`, `serde`, `serde_json`, the `[workspace]` table contents (resolver, members), or any other field.

After the edits, the resulting `Cargo.toml` MUST contain (in this order): `[workspace]` table, `[workspace.package]` table with `rust-version = "1.85"`, `[workspace.dependencies]` table with `rust_decimal = "1.42"` (and the other deps unchanged).

Note: We do NOT add `rust-version.workspace = true` to the per-crate `hp41-core/Cargo.toml` and `hp41-cli/Cargo.toml` in this task — workspace-level declaration is sufficient for `cargo build` MSRV enforcement at the workspace level. (If a future phase needs per-crate inheritance, that is out of scope for INFRA-01.)
  </action>
  <verify>
    <automated>grep -E '^rust-version\s*=\s*"1\.85"' Cargo.toml &amp;&amp; grep -E '^rust_decimal\s*=\s*"1\.42"' Cargo.toml &amp;&amp; cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^rust-version = "1.85"' Cargo.toml` returns at least 1
    - `grep -c '^rust_decimal = "1.42"' Cargo.toml` returns exactly 1
    - `grep -c '^rust_decimal = "1.41"' Cargo.toml` returns exactly 0 (old version removed)
    - `grep -c '^\[workspace.package\]' Cargo.toml` returns exactly 1
    - `cargo build --workspace` exits 0
    - `cargo tree -p hp41-core | grep rust_decimal` shows version 1.42.x
  </acceptance_criteria>
  <done>
    Cargo.toml declares MSRV 1.85 at workspace level, rust_decimal pinned to 1.42, workspace builds clean on the host toolchain (which must be ≥ 1.85).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add MSRV verification CI job pinned to Rust 1.85</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current CI matrix — must understand existing job layout)
    - .planning/phases/09-infrastructure-and-eex-fix/09-CONTEXT.md (D-13 and § specifics: "MSRV job placement")
    - justfile (to confirm `just ci` recipe exists and is the canonical full-gate command)
  </read_first>
  <action>
Edit `.github/workflows/ci.yml` to add a new top-level job named `msrv` to the `jobs:` map. The new job MUST be added as a sibling to `lint`, `test`, and `coverage` (not nested under any of them).

Insert the new job AFTER the existing `coverage` job at the end of the file. The new job must have these exact properties:

- Job key: `msrv`
- `name: MSRV (Rust 1.85)`
- `runs-on: ubuntu-latest`
- NO `needs:` field (runs in parallel with `lint`, `test`, `coverage` — per D-13 specifics in CONTEXT.md)
- Steps (in this order):
  1. `uses: actions/checkout@v4`
  2. `uses: dtolnay/rust-toolchain@1.85` (the version 1.85 here is the toolchain, NOT @stable — this is the whole point of the job per D-13)
  3. `uses: Swatinem/rust-cache@v2`
  4. `uses: taiki-e/install-action@v2` with `with: tool: just`
  5. `run: just ci`

The exact YAML to append (matching indentation of existing jobs which use 2-space indentation under `jobs:`):

```yaml

  msrv:
    name: MSRV (Rust 1.85)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - run: just ci
```

DO NOT modify the `lint`, `test`, or `coverage` jobs. DO NOT add `needs:` to the `msrv` job. DO NOT change any other workflow file.

Validate the final YAML with a parser-equivalent check: the file must contain exactly 4 top-level jobs under `jobs:` (`lint`, `test`, `coverage`, `msrv`) and no syntax errors.
  </action>
  <verify>
    <automated>grep -c '^  msrv:' .github/workflows/ci.yml | grep -q '^1$' &amp;&amp; grep -c 'dtolnay/rust-toolchain@1\.85' .github/workflows/ci.yml | grep -q '^1$' &amp;&amp; python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=list(d['jobs'].keys()); assert set(jobs)=={'lint','test','coverage','msrv'}, jobs; assert 'needs' not in d['jobs']['msrv'], 'msrv must not have needs'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  msrv:' .github/workflows/ci.yml` returns exactly 1
    - `grep -c 'dtolnay/rust-toolchain@1\.85' .github/workflows/ci.yml` returns exactly 1
    - `grep -c 'just ci' .github/workflows/ci.yml` returns at least 1 (in the new msrv job)
    - The Python yaml parse assertion above succeeds (4 jobs, msrv has no `needs:`)
    - `grep -c 'name: MSRV (Rust 1.85)' .github/workflows/ci.yml` returns exactly 1
  </acceptance_criteria>
  <done>
    CI workflow has a fourth job `msrv` that pins Rust 1.85 and runs `just ci`. The job runs in parallel with existing jobs. Existing `lint`, `test`, `coverage` jobs are unchanged.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `cargo build --workspace` succeeds locally
- `just ci` succeeds locally (verifies fmt-check + lint + test + coverage all still green)
- `git diff Cargo.toml` shows only the rust-version addition and the rust_decimal version bump
- `git diff .github/workflows/ci.yml` shows only the new `msrv` job appended

Note: We cannot run the GitHub Actions msrv job locally — it will be verified on the next push/PR. The local equivalent is running `just ci` after `rustup default 1.85` if that toolchain is installed.
</verification>

<success_criteria>
- Cargo.toml declares `rust-version = "1.85"` at workspace level
- rust_decimal bumped to 1.42 (and `cargo tree` confirms resolved version is 1.42.x)
- `.github/workflows/ci.yml` has a new `msrv` job pinned to Rust 1.85 that runs `just ci`
- The msrv job has no `needs:` (runs in parallel with `test`)
- Existing CI jobs (`lint`, `test`, `coverage`) are unchanged
- `cargo build --workspace` and `just ci` pass on the local machine
</success_criteria>

<output>
After completion, create `.planning/phases/09-infrastructure-and-eex-fix/09-01-SUMMARY.md` describing:
- Final `Cargo.toml` MSRV and rust_decimal values
- Diff of the new `msrv` CI job
- Result of local `just ci` run
- Confirmation that no other crate manifest or workflow file was modified
</output>
