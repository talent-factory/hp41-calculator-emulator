---
phase: 14-ipc-layer
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - hp41-gui/src-tauri/capabilities/default.json
autonomous: true
requirements:
  - IPC-01

must_haves:
  truths:
    - "capabilities/default.json explicitly lists the auto-generated permission IDs for both Tauri commands (D-12)"
    - "The permission IDs in capabilities/default.json match EXACTLY the IDs emitted by tauri-build into hp41-gui/src-tauri/gen/schemas/desktop-schema.json (no guessing)"
    - "windows: ['main'] is preserved — no wildcard window scoping (T-14-01 mitigation)"
    - "core:default permission is preserved (Phase 13 baseline)"
    - "cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml exits 0 after the JSON edit (no 'unknown permission identifier' build error — Pitfall 2 avoided by sequencing this plan AFTER Plan 02 completes)"
  artifacts:
    - path: "hp41-gui/src-tauri/capabilities/default.json"
      provides: "Tauri v2 capability JSON with explicit IPC permissions for dispatch_op and get_state"
      contains: "allow-dispatch-op"
  key_links:
    - from: "capabilities/default.json"
      to: "tauri::generate_handler! in lib.rs"
      via: "auto-generated permission IDs (allow-<kebab-command-name>)"
      pattern: "allow-(dispatch-op|get-state)"
    - from: "capabilities/default.json windows scope"
      to: "Tauri main window"
      via: "explicit windows array"
      pattern: '"windows"'
---

<objective>
Wave 2 (after Plan 02 build emits permission IDs): finalize the Tauri ACL by adding the
two app-command permissions to `capabilities/default.json`. This is a JSON edit with one
upstream prerequisite — the `gen/schemas/desktop-schema.json` file written by tauri-build
during Plan 02's `cargo check`.

D-12 requires explicit permission entries even though Tauri v2 allows registered commands
by default. The explicit entries make the security posture auditable.

Why this is a separate plan (not folded into Plan 02):
- Tauri's auto-generated `allow-<command>` permissions appear in
  `hp41-gui/src-tauri/gen/schemas/` ONLY after the FIRST successful `cargo check` that
  includes the registered commands.
- Adding the entries to `capabilities/default.json` BEFORE that build causes a build
  error: `unknown permission identifier` (Pitfall 2 in 14-RESEARCH.md).
- Sequencing this as a separate plan with `depends_on: ["14-02"]` and `wave: 2` is the
  only safe order.

The plan has ONE task — a JSON edit and a verifying build.

Output: 1 file modified. SC-5 fully closed (capability scoping prevents privilege
escalation per T-14-01). All five Phase 14 success criteria verified end-to-end.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-ipc-layer/14-CONTEXT.md
@.planning/phases/14-ipc-layer/14-RESEARCH.md
@.planning/phases/14-ipc-layer/14-PATTERNS.md
@hp41-gui/src-tauri/capabilities/default.json

<interfaces>
<!-- The single source of truth for permission IDs is the generated schema file. -->

Generated by tauri-build during the `cargo check` after Plan 02 lands at:
  hp41-gui/src-tauri/gen/schemas/desktop-schema.json
  (also possibly: macos-schema.json, windows-schema.json, linux-schema.json depending on
  the host platform. Tauri generates the schema for the current host's target.)

Predicted permission IDs (from RESEARCH.md A1):
  "allow-dispatch-op"
  "allow-get-state"

Confirmation procedure (this plan executes it before editing JSON):
  jq -r '.. | objects | .identifier? // empty' hp41-gui/src-tauri/gen/schemas/*.json | sort -u | grep -E '^(allow|deny)-'
  -- or equivalently --
  grep -oE '"(allow|deny)-[a-z-]+"' hp41-gui/src-tauri/gen/schemas/*.json | sort -u

Current capabilities/default.json (Phase 13 baseline):
  {
    "identifier": "default",
    "description": "Default capability for hp41-gui",
    "windows": ["main"],
    "permissions": ["core:default"]
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirm auto-generated permission IDs and update capabilities/default.json</name>

  <read_first>
    - hp41-gui/src-tauri/capabilities/default.json (CURRENT — Phase 13 baseline; only the `permissions` array changes)
    - hp41-gui/src-tauri/gen/schemas/desktop-schema.json (auto-generated by Plan 02 cargo check; contains the literal permission IDs to use)
    - .planning/phases/14-ipc-layer/14-RESEARCH.md (Pattern 7 — Capability JSON for App Commands)
    - .planning/phases/14-ipc-layer/14-PATTERNS.md (capabilities/default.json section — exact diff)
    - .planning/phases/14-ipc-layer/14-VALIDATION.md (Manual-Only Verifications row — capability permission inspection)
  </read_first>

  <files>
    hp41-gui/src-tauri/capabilities/default.json
  </files>

  <action>
This is a 3-step task: discover, edit, verify.

**Step 1: Discover the auto-generated permission IDs.**

Run these commands and inspect the output. The first command lists every schema file
emitted by tauri-build. The second extracts the permission identifiers.

```bash
# Confirm the schema files exist (Plan 02 must have run cargo check successfully).
ls -la hp41-gui/src-tauri/gen/schemas/

# Extract the literal permission IDs related to our two commands.
# The pattern matches both kebab-case (allow-dispatch-op) and snake-case
# (allow-dispatch_op) — Tauri's exact convention is unverified per A1.
grep -hoE '"(allow|deny)-[a-z_-]+"' hp41-gui/src-tauri/gen/schemas/*.json | sort -u
```

The expected output includes (among others) two lines matching `dispatch.op` and
`get.state`. The exact characters between `allow-` and the end-quote are the IDs to
use. Most likely:
  - `"allow-dispatch-op"`
  - `"allow-get-state"`

But if the build emits underscores instead of dashes (e.g. `"allow-dispatch_op"`), USE
THE EXACT STRINGS THE SCHEMA EMITS — do not transform.

If `gen/schemas/` does not exist or is empty:
  - Run `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` once to populate it.
  - This indicates Plan 02 was incomplete; do not proceed until the schema is present.

**Step 2: Edit capabilities/default.json.**

REPLACE the file content with EXACTLY this JSON, substituting the two permission IDs
discovered in Step 1 if they differ from the predicted `"allow-dispatch-op"` /
`"allow-get-state"` forms:

```json
{
  "identifier": "default",
  "description": "Default capability for hp41-gui — core + Phase 14 IPC commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-dispatch-op",
    "allow-get-state"
  ]
}
```

CRITICAL CONSTRAINTS:
- The `windows: ["main"]` array MUST stay exactly as `["main"]` — no `"*"` wildcard.
  This is the T-14-01 mitigation: only the main app window can invoke these commands,
  preventing arbitrary command invocation from injected scripts in (hypothetical) other
  windows.
- The `core:default` entry MUST be preserved — it provides Tauri's baseline permissions
  set up in Phase 13.
- Permission IDs are NOT prefixed with a plugin namespace (Tauri v2 docs:
  `${permission-name}` is sufficient for app-command permissions, NOT
  `app:allow-dispatch-op`). Verified in 14-RESEARCH.md Pattern 7 + sources section.
- The `description` field is updated to mention Phase 14 — this is a documentation
  improvement; if the executor disagrees with the wording, leave it as the original
  Phase 13 string `"Default capability for hp41-gui"`. The JSON parses regardless.
- Use 2-space indentation matching the existing file style. JSON formatting matters for
  the `git diff` to be readable in the commit.
- DO NOT add a `remote` block, additional capability identifiers, or platform-specific
  conditions. Phase 14 has only the desktop default.

**Step 3: Verify the build still passes.**

Run `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml`. If it fails with
`unknown permission identifier: allow-dispatch-op` (or similar), the IDs in the JSON do
not match what the schema generated. Re-run Step 1 and use the exact strings.

This MUST exit 0 before the task is complete.
  </action>

  <verify>
    <automated>cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&amp;1 | tail -10 &amp;&amp; cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - File `hp41-gui/src-tauri/capabilities/default.json` exists.
    - `grep -c '"core:default"' hp41-gui/src-tauri/capabilities/default.json` returns 1 (Phase 13 baseline preserved).
    - `grep -cE '"allow-(dispatch[-_]op|get[-_]state)"' hp41-gui/src-tauri/capabilities/default.json` returns 2 (both auto-generated IDs present, regardless of dash/underscore form).
    - `grep -c '"windows": \["main"\]' hp41-gui/src-tauri/capabilities/default.json` returns 1 (window scoping preserved).
    - `grep -c '"\\*"' hp41-gui/src-tauri/capabilities/default.json` returns 0 (no wildcard window).
    - `python3 -c "import json; json.load(open('hp41-gui/src-tauri/capabilities/default.json'))"` exits 0 (valid JSON).
    - `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` exits 0 (no `unknown permission identifier` error).
    - `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml` reports `9 passed; 0 failed` (no regression in unit tests).
    - `grep -rn "fn op_\|fn flush_entry\|fn format_hpnum" hp41-gui/src-tauri/src/` returns nothing (SC-4 final check across whole tree).
  </acceptance_criteria>

  <done>
    capabilities/default.json lists the two auto-generated IPC permissions plus
    core:default, scoped to the main window. cargo check passes. All 9 unit tests still
    GREEN. Phase 14 IPC layer is end-to-end functional and audit-ready.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend window → Tauri command bus | Only the window listed in `capabilities/default.json` `windows` array can invoke the registered commands. Currently `["main"]` — no other windows exist in Phase 14. |
| capabilities/default.json → tauri-build | The build script validates every permission identifier against `hp41-gui/src-tauri/gen/schemas/`. Missing IDs → build error (loud failure, no silent permission grant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | Arbitrary command invocation from injected scripts in non-main windows | mitigate | `windows: ["main"]` array — explicit allow-list, no wildcard. Verified by acceptance grep `"\\*"` returns 0 and `windows": ["main"]` returns 1. |
| T-14-02 | Tampering | Permission identifier drift between handler registration and capability file | mitigate | Plan execution sequence: Plan 02 lands first, generates the IDs, Plan 03 reads them from `gen/schemas/` before editing the JSON. The `cargo check` at task end confirms the IDs match. |
| T-14-03 | Denial of Service | tauri-build rejects unknown permission identifier | accept | This is the loud-failure mode we WANT — if a typo enters the JSON, the build fails immediately rather than silently granting/denying. The task verify step catches it before commit. |
| T-14-04 | Information Disclosure | Description field leaks internal phase numbering | accept | The string `"Default capability for hp41-gui — core + Phase 14 IPC commands"` is part of the source repo, which is already public. No new disclosure. |
</threat_model>

<verification>
After the single task completes:

1. `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` exits 0.
2. `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml` reports `9 passed; 0 failed`.
3. `python3 -c "import json; json.load(open('hp41-gui/src-tauri/capabilities/default.json'))"` exits 0 — JSON is valid.
4. `just gui-check` exits 0 — phase quality gate green.
5. `just ci` (CLI pipeline) still exits with the same pass/fail outcome as before — Phase 14 has not regressed `hp41-cli` or `hp41-core` (SC-2 of Phase 13 carries forward).
6. The Phase 14 IPC layer is feature-complete and ready for Phase 15 (React display + keyboard).
</verification>

<success_criteria>
- capabilities/default.json explicitly lists `core:default` plus the two auto-generated
  app-command permissions discovered from `gen/schemas/`.
- `windows: ["main"]` enforces window scoping.
- `cargo check` passes with no `unknown permission identifier` errors.
- All 9 unit tests remain GREEN.
- Phase 14 SC-1, SC-2, SC-3, SC-4, SC-5 are ALL verified end-to-end (SC-1 by Plan 01,
  SC-2/SC-3 by Plan 02, SC-4 by static grep across whole tree, SC-5 by AppState type
  alias compile-enforcement throughout commands.rs).
</success_criteria>

<output>
After completion, create `.planning/phases/14-ipc-layer/14-03-SUMMARY.md` using the template
at `@/Users/daniel/.claude/get-shit-done/templates/summary.md`.

Key items to record:
- Exact permission ID strings used in capabilities/default.json (verbatim from the
  generated schema — confirms or refutes the predicted naming).
- Path to the generated schema file consulted (e.g. `gen/schemas/desktop-schema.json`).
- Confirmation that all 5 Phase 14 success criteria are met.
- Note for Phase 15: when the React frontend invokes commands, the auto-generated
  TypeScript types for `CalcStateView` and `GuiError` are deferred (RESEARCH.md). Phase
  15 plans will introduce a TypeScript shim that mirrors the Rust struct shapes manually
  OR add a tauri-bindgen-style step.
- Note for Phase 18 CI/CD: `just gui-check` should be added to the new GUI CI job
  (NOT to existing `just ci` per Phase 13 D-13).
</output>
</content>
</invoke>