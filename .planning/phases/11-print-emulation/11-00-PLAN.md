---
phase: 11-print-emulation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - hp41-core/tests/print_tests.rs
autonomous: true
requirements:
  - PRNT-01
  - PRNT-02
  - PRNT-03
  - PRNT-04

must_haves:
  truths:
    - "hp41-core/tests/print_tests.rs exists with failing skeleton tests for PRX, PRA, PRSTK, and program execution"
    - "All test functions compile but fail (RED state) because Op::PRX/PRA/PRSTK do not yet exist"
  artifacts:
    - path: "hp41-core/tests/print_tests.rs"
      provides: "Test scaffold for print operations"
      min_lines: 60
  key_links:
    - from: "hp41-core/tests/print_tests.rs"
      to: "hp41_core::ops::Op"
      via: "use hp41_core::ops::{dispatch, Op};"
      pattern: "Op::PRX|Op::PRA|Op::PRSTK"
---

<objective>
Create the Wave 0 test scaffold for print operations.

Purpose: Nyquist rule — automated verify commands must reference real test files before implementation begins. Tests written first (RED) define the contract; Wave 1 makes them GREEN.
Output: hp41-core/tests/print_tests.rs with failing skeleton tests for PRNT-01/02/03 and program execution.
</objective>

<execution_context>
@/Users/daniel/GitRepository/hp41-calculator-emulator/.claude/workflows/execute-plan.md
@/Users/daniel/GitRepository/hp41-calculator-emulator/.claude/templates/summary.md
</execution_context>

<context>
@/Users/daniel/GitRepository/hp41-calculator-emulator/.planning/ROADMAP.md
@/Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/11-print-emulation/11-CONTEXT.md
@/Users/daniel/GitRepository/hp41-calculator-emulator/.planning/phases/11-print-emulation/11-PATTERNS.md

<interfaces>
<!-- Exact analog pattern from hp41-core/tests/register_tests.rs -->

```rust
use hp41_core::ops::{dispatch, Op};
use hp41_core::{CalcState, HpNum};
use rust_decimal::Decimal;

fn push(state: &mut CalcState, n: i32) {
    dispatch(state, Op::PushNum(HpNum::from(n))).unwrap();
}

#[test]
fn test_sto_rcl_round_trip() {
    let mut s = CalcState::new();
    push(&mut s, 42);
    dispatch(&mut s, Op::StoReg(5)).unwrap();
    assert_eq!(s.stack.x.inner(), Decimal::from(42));
}
```

CalcState fields accessible in tests:
- s.print_buffer: Vec<String>  ← will be added by Plan 11-01
- s.stack.x / .y / .z / .t / .lastx: HpNum
- s.alpha_reg: String
- s.display_mode: DisplayMode
- s.program: Vec<Op>

DisplayMode variants: DisplayMode::Fix(u8), DisplayMode::Sci(u8), DisplayMode::Eng(u8)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create print_tests.rs scaffold with failing tests</name>
  <files>hp41-core/tests/print_tests.rs</files>
  <read_first>
    - hp41-core/tests/register_tests.rs (analog — exact import pattern and test structure to replicate)
    - hp41-core/src/lib.rs (verify which types are re-exported for test use)
  </read_first>
  <action>
Create `hp41-core/tests/print_tests.rs` with the following content. Tests reference `Op::PRX`, `Op::PRA`, `Op::PRSTK` which do not yet exist — the file must compile ONLY after Plan 11-01 ships the Op variants. Use `compile_error!` or `todo!()` stubs; do NOT use `#[ignore]` (tests must be RED, not skipped).

Write the file with these exact test stubs:

```rust
//! Integration tests for print operations: PRX, PRA, PRSTK.
//! PRNT-01, PRNT-02, PRNT-03 — hp41-core side.
//! These tests are RED until Plan 11-01 ships Op::PRX/PRA/PRSTK and print_buffer.

#![allow(clippy::unwrap_used)]

use hp41_core::ops::{dispatch, Op};
use hp41_core::{CalcState, DisplayMode, HpNum};

fn push_val(state: &mut CalcState, n: i32) {
    dispatch(state, Op::PushNum(HpNum::from(n))).unwrap();
}

// ── PRNT-01: PRX ─────────────────────────────────────────────────────────────

/// PRX pushes exactly one line to print_buffer.
#[test]
fn test_prx_pushes_one_line_to_buffer() {
    let mut s = CalcState::new();
    push_val(&mut s, 42);
    dispatch(&mut s, Op::PRX).unwrap();
    assert_eq!(s.print_buffer.len(), 1, "PRX must push exactly 1 line");
}

/// PRX output line is exactly 24 chars wide (right-aligned).
#[test]
fn test_prx_output_is_24_chars() {
    let mut s = CalcState::new();
    push_val(&mut s, 42);
    dispatch(&mut s, Op::PRX).unwrap();
    let line = &s.print_buffer[0];
    assert_eq!(
        line.len(),
        24,
        "PRX output must be exactly 24 chars, got: {:?}",
        line
    );
}

/// PRX output is right-aligned (leading spaces for short values).
#[test]
fn test_prx_output_is_right_aligned() {
    let mut s = CalcState::new();
    push_val(&mut s, 1); // short value → many leading spaces
    dispatch(&mut s, Op::PRX).unwrap();
    let line = &s.print_buffer[0];
    // Right-aligned: the string ends with the value, not spaces
    assert!(
        line.starts_with(' '),
        "PRX output of small value must have leading spaces: {:?}",
        line
    );
}

/// PRX respects SCI display mode.
#[test]
fn test_prx_respects_display_mode_sci() {
    let mut s = CalcState::new();
    s.display_mode = DisplayMode::Sci(4);
    push_val(&mut s, 12345);
    dispatch(&mut s, Op::PRX).unwrap();
    let line = &s.print_buffer[0];
    assert_eq!(line.len(), 24, "PRX SCI output must be 24 chars");
    // SCI 4 of 12345 contains 'E' or 'e' notation
    let trimmed = line.trim();
    assert!(
        trimmed.to_ascii_uppercase().contains('E'),
        "PRX in SCI mode must produce scientific notation: {:?}",
        line
    );
}

/// PRX has LiftEffect::Neutral — stack unchanged after PRX.
#[test]
fn test_prx_lift_effect_neutral() {
    let mut s = CalcState::new();
    push_val(&mut s, 7);
    let x_before = s.stack.x.clone();
    dispatch(&mut s, Op::PRX).unwrap();
    assert_eq!(
        s.stack.x, x_before,
        "PRX must not modify the stack (LiftEffect::Neutral)"
    );
}

// ── PRNT-02: PRA ─────────────────────────────────────────────────────────────

/// PRA pushes exactly one line to print_buffer.
#[test]
fn test_pra_pushes_one_line_to_buffer() {
    let mut s = CalcState::new();
    s.alpha_reg = "HELLO".to_string();
    dispatch(&mut s, Op::PRA).unwrap();
    assert_eq!(s.print_buffer.len(), 1, "PRA must push exactly 1 line");
}

/// PRA output line is exactly 24 chars wide (left-aligned).
#[test]
fn test_pra_output_is_24_chars() {
    let mut s = CalcState::new();
    s.alpha_reg = "HI".to_string();
    dispatch(&mut s, Op::PRA).unwrap();
    let line = &s.print_buffer[0];
    assert_eq!(
        line.len(),
        24,
        "PRA output must be exactly 24 chars, got: {:?}",
        line
    );
}

/// PRA output is left-aligned (trailing spaces for short values).
#[test]
fn test_pra_output_is_left_aligned() {
    let mut s = CalcState::new();
    s.alpha_reg = "HI".to_string();
    dispatch(&mut s, Op::PRA).unwrap();
    let line = &s.print_buffer[0];
    // Left-aligned: starts with "HI", ends with spaces
    assert!(
        line.starts_with("HI"),
        "PRA output must start with alpha content: {:?}",
        line
    );
    assert!(
        line.ends_with(' '),
        "PRA output of short value must have trailing spaces: {:?}",
        line
    );
}

/// PRA with empty alpha_reg produces 24 spaces.
#[test]
fn test_pra_empty_alpha_is_24_spaces() {
    let mut s = CalcState::new();
    s.alpha_reg = String::new();
    dispatch(&mut s, Op::PRA).unwrap();
    let line = &s.print_buffer[0];
    assert_eq!(
        line.as_str(),
        "                        ", // 24 spaces
        "PRA with empty alpha_reg must produce 24 spaces"
    );
}

/// PRA truncates alpha_reg longer than 24 chars to 24 chars.
#[test]
fn test_pra_truncates_long_alpha_to_24_chars() {
    let mut s = CalcState::new();
    s.alpha_reg = "A".repeat(30);
    dispatch(&mut s, Op::PRA).unwrap();
    let line = &s.print_buffer[0];
    assert_eq!(line.len(), 24, "PRA must truncate to 24 chars");
}

// ── PRNT-03: PRSTK ───────────────────────────────────────────────────────────

/// PRSTK pushes exactly 6 lines to print_buffer.
#[test]
fn test_prstk_produces_six_lines() {
    let mut s = CalcState::new();
    dispatch(&mut s, Op::PRSTK).unwrap();
    assert_eq!(s.print_buffer.len(), 6, "PRSTK must push exactly 6 lines");
}

/// All 6 PRSTK lines are exactly 24 chars wide.
#[test]
fn test_prstk_all_lines_are_24_chars() {
    let mut s = CalcState::new();
    push_val(&mut s, 1);
    dispatch(&mut s, Op::PRSTK).unwrap();
    for (i, line) in s.print_buffer.iter().enumerate() {
        assert_eq!(
            line.len(),
            24,
            "PRSTK line {} must be 24 chars, got {:?}",
            i,
            line
        );
    }
}

/// PRSTK lines appear in T/Z/Y/X/LASTX/ALPHA order.
#[test]
fn test_prstk_line_order_and_labels() {
    let mut s = CalcState::new();
    dispatch(&mut s, Op::PRSTK).unwrap();
    assert!(s.print_buffer[0].starts_with("T:"), "Line 0 must start with T:");
    assert!(s.print_buffer[1].starts_with("Z:"), "Line 1 must start with Z:");
    assert!(s.print_buffer[2].starts_with("Y:"), "Line 2 must start with Y:");
    assert!(s.print_buffer[3].starts_with("X:"), "Line 3 must start with X:");
    assert!(s.print_buffer[4].starts_with("LASTX:"), "Line 4 must start with LASTX:");
    assert!(s.print_buffer[5].starts_with("ALPHA:"), "Line 5 must start with ALPHA:");
}

/// PRSTK ALPHA line when alpha_reg is empty: "ALPHA:                  " ("ALPHA:" (6) + 18 spaces = 24).
#[test]
fn test_prstk_alpha_empty_line_format() {
    let mut s = CalcState::new();
    s.alpha_reg = String::new();
    dispatch(&mut s, Op::PRSTK).unwrap();
    let alpha_line = &s.print_buffer[5];
    assert_eq!(
        alpha_line.as_str(),
        "ALPHA:                  ", // "ALPHA:" (6) + 1 pad (to 7-char label field) + 17 spaces (content field) = 24
        "PRSTK ALPHA line with empty alpha_reg must be 'ALPHA:' padded to 24 chars"
    );
}

/// PRSTK ALPHA line with content: left-aligned in 17-char field after 7-char label.
#[test]
fn test_prstk_alpha_nonempty_line_format() {
    let mut s = CalcState::new();
    s.alpha_reg = "HP41".to_string();
    dispatch(&mut s, Op::PRSTK).unwrap();
    let alpha_line = &s.print_buffer[5];
    assert_eq!(alpha_line.len(), 24, "PRSTK ALPHA line must be 24 chars");
    assert!(
        alpha_line.starts_with("ALPHA:"),
        "PRSTK ALPHA line must start with 'ALPHA:'"
    );
    assert!(
        alpha_line.contains("HP41"),
        "PRSTK ALPHA line must contain alpha content"
    );
}

// ── PRNT-01/02/03: program execution (execute_op arms) ───────────────────────

/// PRX inside a running program fills print_buffer (execute_op arm must exist).
#[test]
fn test_prx_in_program() {
    let mut s = CalcState::new();
    push_val(&mut s, 99);
    // Build a minimal program: LBL "P", PRX, RTN
    s.program = vec![Op::Lbl("P".to_string()), Op::PRX, Op::Rtn];
    hp41_core::run_program(&mut s, "P").unwrap();
    assert_eq!(
        s.print_buffer.len(),
        1,
        "PRX in a program must push 1 line to print_buffer"
    );
}

/// PRA inside a running program fills print_buffer (execute_op arm must exist).
#[test]
fn test_pra_in_program() {
    let mut s = CalcState::new();
    s.alpha_reg = "TEST".to_string();
    s.program = vec![Op::Lbl("Q".to_string()), Op::PRA, Op::Rtn];
    hp41_core::run_program(&mut s, "Q").unwrap();
    assert_eq!(
        s.print_buffer.len(),
        1,
        "PRA in a program must push 1 line to print_buffer"
    );
}

/// PRSTK inside a running program fills print_buffer with 6 lines.
#[test]
fn test_prstk_in_program() {
    let mut s = CalcState::new();
    s.program = vec![Op::Lbl("R".to_string()), Op::PRSTK, Op::Rtn];
    hp41_core::run_program(&mut s, "R").unwrap();
    assert_eq!(
        s.print_buffer.len(),
        6,
        "PRSTK in a program must push 6 lines to print_buffer"
    );
}
```

Note: This file references `Op::PRX`, `Op::PRA`, `Op::PRSTK`, and `s.print_buffer` — none of which exist yet. The file will NOT compile until Plan 11-01 is executed. This is intentional (Wave 0 RED state).
  </action>
  <verify>
    <automated>
      # File must exist with expected test function names
      grep -c "fn test_prx_pushes_one_line_to_buffer\|fn test_prstk_produces_six_lines\|fn test_pra_pushes_one_line_to_buffer\|fn test_prx_in_program" /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-core/tests/print_tests.rs
      # Expected output: 4
      # Note: cargo test will FAIL to compile until Plan 11-01 ships Op::PRX/PRA/PRSTK
    </automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/tests/print_tests.rs` exists
    - `grep -c "^#\[test\]" hp41-core/tests/print_tests.rs` returns 18 or more
    - File contains `use hp41_core::ops::{dispatch, Op};`
    - File contains `use hp41_core::{CalcState, DisplayMode, HpNum};`
    - File contains `Op::PRX`, `Op::PRA`, `Op::PRSTK` references
    - File contains `s.print_buffer` references
    - File contains `run_program` reference (program execution coverage)
    - File does NOT contain `#[ignore]` (tests must be RED, not silently skipped)
  </acceptance_criteria>
  <done>print_tests.rs exists with 18+ test functions covering PRX (5 tests), PRA (5 tests), PRSTK (4 tests), and program execution (3 tests). File is in RED state (does not compile yet).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Wave 0 creates only test code; no trust boundaries cross this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W0-01 | Information Disclosure | test scaffold | accept | Test file contains no secrets; uses only synthetic CalcState values |
</threat_model>

<verification>
After this plan executes:
- `ls hp41-core/tests/print_tests.rs` exits 0
- `grep -c "^#\[test\]" hp41-core/tests/print_tests.rs` returns >= 18
- `cargo build -p hp41-core` still succeeds (test file is not compiled with `build`, only with `test`)
- `cargo test -p hp41-core --test print_tests 2>&1 | head -5` shows compile error referencing Op::PRX (confirms RED state)
</verification>

<success_criteria>
- print_tests.rs exists with 18+ failing test stubs
- Tests reference Op::PRX, Op::PRA, Op::PRSTK, print_buffer, and run_program
- No `#[ignore]` attributes — all tests are active RED checks
- cargo build (not test) still passes — test scaffold does not break the library build
</success_criteria>

<output>
After completion, create `.planning/phases/11-print-emulation/11-00-SUMMARY.md`
</output>
