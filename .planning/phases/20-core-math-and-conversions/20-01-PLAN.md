---
phase: 20-core-math-and-conversions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/error.rs
  - hp41-core/src/format.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/math.rs
  - hp41-core/src/ops/stack_ops.rs
  - hp41-core/src/ops/program.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
  - hp41-core/tests/phase20_math.rs
autonomous: true
requirements:
  - FN-MATH-01
  - FN-MATH-02
  - FN-MATH-03
  - FN-MATH-04
  - FN-MATH-05
  - FN-MATH-06
  - FN-MATH-07
  - FN-MATH-08
  - FN-MATH-09
  - FN-STACK-01
tags:
  - hp41
  - rust
  - rpn
  - math
  - stack
  - bcd-decimal

must_haves:
  truths:
    - "From the CLI, dispatching Op::Pi pushes 3.141592654 (10-digit rounded HP-41 hardware value) onto X and lifts the stack (FN-MATH-01, SC-1)"
    - "In DEG mode, with Y=3 and X=53.13010235, dispatching Op::PolarToRect produces Y≈3 (x-coord, the cos component) and X≈4 (y-coord, the sin component); in RAD mode the same op honors radians (FN-MATH-02, D-11, D-13)"
    - "In DEG mode, with Y=3 and X=4, dispatching Op::RectToPolar produces Y=5 (magnitude) and X≈53.13010235 (angle in degrees); in RAD mode the same inputs return the angle in radians (FN-MATH-03, SC-2, D-11, D-13)"
    - "With X=-5.7 and DisplayMode::Fix(1), Op::Rnd produces X=-5.7; with DisplayMode::Fix(0), Op::Rnd produces X=-6 (FN-MATH-04, SC-3 part-a, D-01–D-03)"
    - "Op::Frc applied to X=-3.7 produces X=-0.7 (sign-preserving, complement of INT) (FN-MATH-05, D-15)"
    - "Op::Mod with Y=7 X=-3 produces X=+1 (sign follows Y per HP-41 trunc-toward-zero convention: result = Y - X * trunc(Y/X) = 7 - (-3)*(-2) = 1); Op::Mod with Y=-7 X=3 produces X=-1 (sign follows Y); Op::Mod with X=0 returns HpError::Domain (FN-MATH-06, D-14)"
    - "Op::Abs applied to X=-5 produces X=5; LASTX = -5 (FN-MATH-07, D-16)"
    - "Op::Fact applied to X=5 produces X=120; X=27 succeeds; X=28 returns HpError::Overflow; X=70 returns HpError::OutOfRange (pre-flight, SC-3 part-b); non-integer X returns HpError::Domain; negative integer X returns HpError::Domain (FN-MATH-08, D-04–D-07)"
    - "Op::Sign returns -1 for X<0, 0 for X=0, +1 for X>0; ALPHA divergence is documented but Phase 20 always returns numeric (FN-MATH-09, D-17, D-18)"
    - "Starting from stack {X=1, Y=2, Z=3, T=4}, Op::Rup produces {X=4, Y=1, Z=2, T=3} — mirror of op_rdn; LASTX is unchanged; LiftEffect::Neutral (FN-STACK-01, SC-4, D-19, D-20)"
    - "All 10 new Op variants land in 4 places (dispatch + execute_op + both prgm_display.rs copies); compile-time exhaustive matches confirm coverage (SC-5, D-22)"
    - "round_to_display_precision(x, mode) is the single source of truth shared by format_hpnum() (display path) and op_rnd (value-mutation path) (D-01, D-02)"
    - "The 500-case hp41-core/tests/numerical_accuracy.rs suite remains ≥490/500 passing after Phase 20 lands (no regression; Phase 27 extends the suite — Phase 20 does NOT add cases there)"
    - "`just coverage` reports hp41-core line coverage ≥ 92.5% (non-regression from v2.1 baseline noted in CLAUDE.md §Quality Gates; Phase 27 will raise the floor to 95%, but Phase 20 must not slip below 92.5%)"
    - "SC-4 invariant preserved: NO new op_* / flush_entry_* / format_hpnum body added under hp41-gui/src-tauri/ — only the prgm_display.rs exhaustive-match arms are touched there (D-24)"
    - "#![deny(clippy::unwrap_used)] gate at hp41-core/src/lib.rs:1 still passes; every new line of production code uses ?-propagation or .expect(\"reason\") (D-23)"

  artifacts:
    - path: "hp41-core/src/error.rs"
      provides: "New HpError::OutOfRange variant (preserves SC-3 wording for FACT X>69 pre-flight per D-06)"
      contains: "OutOfRange"
    - path: "hp41-core/src/format.rs"
      provides: "round_to_display_precision(x: &HpNum, mode: &DisplayMode) -> HpNum helper; refactored format_hpnum() to call it (D-01, D-02)"
      contains: "pub fn round_to_display_precision"
    - path: "hp41-core/src/ops/mod.rs"
      provides: "10 new Op enum variants (Pi, PolarToRect, RectToPolar, Rnd, Frc, Mod, Abs, Fact, Sign, Rup) + matching dispatch() arms"
      contains: "Op::Pi"
    - path: "hp41-core/src/ops/math.rs"
      provides: "op_pi, op_polar_to_rect, op_rect_to_polar, op_rnd, op_frc, op_mod, op_abs, op_fact, op_sign function bodies (D-08, D-11, D-14–D-17)"
      contains: "pub fn op_pi"
    - path: "hp41-core/src/ops/stack_ops.rs"
      provides: "op_r_up function body — mirror of op_rdn, LiftEffect::Neutral, does NOT update LASTX (D-19, D-20)"
      contains: "pub fn op_r_up"
    - path: "hp41-core/src/ops/program.rs"
      provides: "10 new execute_op() arms (mirrors dispatch()) so the ops are reachable inside running programs"
      contains: "Op::Pi"
    - path: "hp41-cli/src/prgm_display.rs"
      provides: "10 new op_display_name() arms returning HP-41 mnemonics: \"PI\", \"P\u{2192}R\", \"R\u{2192}P\", \"RND\", \"FRC\", \"MOD\", \"ABS\", \"FACT\", \"SIGN\", \"R\u{2191}\""
      contains: "Op::Pi"
    - path: "hp41-gui/src-tauri/src/prgm_display.rs"
      provides: "Same 10 op_display_name() arms (parallel exhaustive match per CLAUDE.md 4-place rule; SC-4 exception)"
      contains: "Op::Pi"
    - path: "hp41-core/tests/phase20_math.rs"
      provides: "Integration test file covering the 4 ROADMAP success criteria (PI lift, R→P in DEG and RAD, RND FIX 1 / FIX 0, R↑ mirror); plus per-op happy-path + error-path tests for FACT (X=5, X=27, X=28, X=70, X=-1, X=2.5), MOD (sign-follows-Y deterministic asserts per D-14, divide-by-zero), FRC (sign-preserving), ABS, SIGN (-1/0/+1)"
      contains: "fn test_pi_pushes_ten_digit_rounded"
      min_tests: 14

  key_links:
    - from: "hp41-core/src/ops/math.rs::op_pi"
      to: "hp41-core/src/stack.rs::{enter_number, apply_lift_effect, LiftEffect::Enable}"
      via: "PI pushes a literal Decimal-parsed HpNum onto the stack with stack-lift Enable"
      pattern: "enter_number\\(state.*\\)|apply_lift_effect.*LiftEffect::Enable"
    - from: "hp41-core/src/ops/math.rs::op_polar_to_rect"
      to: "hp41-core/src/ops/math.rs::to_radians_f64 + std::f64::sin/cos"
      via: "f64 bridge: HpNum -> f64 -> radians -> sin/cos -> Decimal::from_f64 -> HpNum::rounded"
      pattern: "to_radians_f64\\(.*state\\.angle_mode\\)"
    - from: "hp41-core/src/ops/math.rs::op_rect_to_polar"
      to: "hp41-core/src/ops/math.rs::f64_from_radians + std::f64::sqrt/atan2"
      via: "f64 bridge: hypot via sqrt(x*x+y*y), angle via atan2(y, x) -> f64_from_radians -> Decimal::from_f64 -> HpNum::rounded"
      pattern: "f64_from_radians\\(.*state\\.angle_mode\\)"
    - from: "hp41-core/src/ops/math.rs::op_rnd"
      to: "hp41-core/src/format.rs::round_to_display_precision + stack::unary_result"
      via: "value-mutation path consumes the same helper as the display path"
      pattern: "round_to_display_precision\\(.*&state\\.display_mode\\)"
    - from: "hp41-core/src/ops/math.rs::op_fact"
      to: "rust_decimal::Decimal::from_f64 + HpError::{Overflow, OutOfRange, Domain}"
      via: "pre-flight OutOfRange (X>69 per SC-3), then integer/sign check via x.trunc_int + sign; magnitude via f64 factorial → Decimal::from_f64 → Overflow if None"
      pattern: "if x_int > 69|HpError::OutOfRange|Decimal::from_f64"
    - from: "hp41-core/src/ops/stack_ops.rs::op_r_up"
      to: "hp41-core/src/stack.rs::apply_lift_effect with LiftEffect::Neutral"
      via: "stack reorganization mirror of op_rdn; assignment chain old_x = X; X = T; T = Z; Z = Y; Y = old_x"
      pattern: "apply_lift_effect\\(state, LiftEffect::Neutral\\)"
    - from: "hp41-core/src/ops/mod.rs::dispatch"
      to: "hp41-core/src/ops/program.rs::execute_op AND hp41-cli/src/prgm_display.rs::op_display_name AND hp41-gui/src-tauri/src/prgm_display.rs::op_display_name"
      via: "4-place Op-variant rule (D-22, CLAUDE.md §Critical Implementation Traps) — exhaustive matches will fail to compile if any of the 4 places is missed"
      pattern: "Op::Pi|Op::PolarToRect|Op::RectToPolar|Op::Rnd|Op::Frc|Op::Mod|Op::Abs|Op::Fact|Op::Sign|Op::Rup"
---

<objective>
Land the 10 missing HP-41CV ROM math/stack operations (PI, P→R, R→P, RND, FRC, MOD, ABS, FACT, SIGN, R↑) in `hp41-core` with hardware-faithful semantics. Every new `Op` variant must reach 4 places (dispatch + execute_op + both `prgm_display.rs` copies) and respect every locked decision D-01 through D-25 in `.planning/phases/20-core-math-and-conversions/20-CONTEXT.md`.

This is a SINGLE plan covering all 10 ops per locked decision **D-21** (single-plan structure). Internal task order is Wave-0 (RND helper extraction → must pass the 500-case suite before any new ops land) → Wave-1 (Op variants + dispatch + execute_op + implementations + prgm_display in BOTH copies) → Wave-2 (integration tests in `hp41-core/tests/phase20_math.rs`).

Purpose: Restore HP-41CV ROM behavioral parity for the 10 named ops that were stubbed out of v1.0/v1.1. They are programmable, dispatchable from the CLI/GUI (CLI keyboard wiring lands in Phase 25, GUI key_map in Phase 26 — those phases consume the Ops added here).

Output: 9 modified files (listed in `files_modified`), including the new `hp41-core/tests/phase20_math.rs`. Net new line count target ≈ 600 LOC including tests. All CI gates (`just lint`, `just test`, `just coverage`) stay green; coverage stays ≥ 92.5% (current v2.1 baseline per CLAUDE.md §Quality Gates).

**Editorial note (D-09):** ROADMAP.md success criterion SC-1 currently reads "pushes 3.1415926536 (10-digit rounded)" — that string is 11 sig digits. Correct HP-41 hardware value at 10 sig digits is **3.141592654**. This plan implements 3.141592654 per D-08 and flags the ROADMAP wording for editorial correction when the phase commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-core-math-and-conversions/20-CONTEXT.md
@CLAUDE.md

# Source files the executor will modify
@hp41-core/src/format.rs
@hp41-core/src/num.rs
@hp41-core/src/stack.rs
@hp41-core/src/state.rs
@hp41-core/src/error.rs
@hp41-core/src/ops/mod.rs
@hp41-core/src/ops/math.rs
@hp41-core/src/ops/stack_ops.rs
@hp41-core/src/ops/program.rs
@hp41-cli/src/prgm_display.rs
@hp41-gui/src-tauri/src/prgm_display.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase via Read on 2026-05-13. -->
<!-- Use these directly — no codebase exploration needed before starting. -->

From `hp41-core/src/error.rs`:
- `pub enum HpError { Overflow, DivideByZero, InvalidOp, Domain, CallDepth, InvalidInput, AlphaData, CardData(String) }`
- **NOTE:** `HpError::OutOfRange` is referenced in CONTEXT.md D-06 but **does not currently exist** in `error.rs`. Phase 20 must add it as a new variant `OutOfRange` with `#[error("out of range")]` (preserves the SC-3 wording for FACT X>69 pre-flight). All existing error sites continue to work because exhaustive matches on `HpError` are limited to a small number of internal sites; adding a variant is backwards-compatible.

From `hp41-core/src/state.rs`:
- `pub struct CalcState { pub stack: Stack, pub regs: Vec<HpNum>, pub alpha_reg: String, pub alpha_mode: bool, pub angle_mode: AngleMode, pub display_mode: DisplayMode, pub entry_buf: String, pub program: Vec<Op>, pub prgm_mode: bool, pub pc: usize, pub call_stack: Vec<usize>, pub is_running: bool, pub user_mode: bool, pub key_assignments: BTreeMap<char, String>, pub print_buffer: Vec<String>, pub last_key_code: u8, pub reg_m: HpNum, pub reg_n: HpNum, pub reg_o: HpNum, pub pending_card_op: Option<CardOpRequest> }`
- `pub enum AngleMode { Deg, Rad, Grad }`
- `pub enum DisplayMode { Fix(u8), Sci(u8), Eng(u8) }` (u8 digit count 0–9)
- `pub struct Stack { pub x: HpNum, pub y: HpNum, pub z: HpNum, pub t: HpNum, pub lastx: HpNum, pub lift_enabled: bool }`

From `hp41-core/src/num.rs`:
- `pub struct HpNum(pub(crate) Decimal)`
- `impl HpNum { pub fn rounded(d: Decimal) -> Self; pub fn zero() -> Self; pub fn is_zero(&self) -> bool; pub fn checked_mul(...) -> Result<HpNum, HpError>; pub fn checked_div(...) -> Result<HpNum, HpError>; pub fn checked_sub(...) -> Result<HpNum, HpError>; pub fn negate(&self) -> Self; pub fn inner(&self) -> Decimal; pub fn trunc_int(&self) -> HpNum }`
- `impl From<i32> for HpNum`; `impl From<Decimal> for HpNum`
- **Established `.expect(...)` idioms** for the zero-panic policy (`#![deny(clippy::unwrap_used)]`):
  - `hp41-core/src/num.rs:18` — `.expect("round_sf_with_strategy(10) must succeed for valid finite Decimal")` — the precedent for `Decimal::round_sf_with_strategy(...)` return-type handling. Mirror this exact form when rounding to a fixed sig-digit count on a known-finite Decimal.
  - `hp41-core/src/format.rs:~161` — `.to_f64().expect("HpNum is always within f64 range")` — the precedent for `Decimal::to_f64()` return-type handling on an in-range HpNum. Mirror this exact form (NOT `.ok_or(HpError::Overflow)?`) when reading an HpNum's inner Decimal into f64 for an f64-bridge computation. The Overflow path here is unreachable for finite HpNum; introducing it would add a spurious error branch that cannot be exercised by tests.

From `hp41-core/src/stack.rs`:
- `pub enum LiftEffect { Enable, Disable, Neutral }`
- `pub fn apply_lift_effect(state: &mut CalcState, effect: LiftEffect)`
- `pub fn enter_number(state: &mut CalcState, value: HpNum)`
- `pub fn unary_result(state: &mut CalcState, result: HpNum)` — saves X→LASTX, X←result, Y/Z/T unchanged, sets lift_enabled=true
- `pub fn binary_result(state: &mut CalcState, result: HpNum)` — saves X→LASTX, X←result, Y←Z, Z←T, T preserved, sets lift_enabled=true

From `hp41-core/src/format.rs` (current state — Wave-0 refactors this):
- `pub fn format_hpnum(n: &HpNum, mode: &DisplayMode) -> String` — top-level display formatter; dispatches to private `format_fix(d, digits)`, `format_sci(d, digits)`, `format_eng(d, digits)`.
- Internal: `compute_sci_exp(abs_d) -> i32`, `floor_to_multiple_of_3(exp) -> i32`, `decimal_pow10(exp) -> Decimal`, `scale_decimal(d, exp_shift) -> Decimal`. Rounding strategy used throughout: `RoundingStrategy::MidpointAwayFromZero`.

From `hp41-core/src/ops/math.rs` (precedent for the new ops):
- `fn to_radians_f64(v: f64, mode: AngleMode) -> f64` — internal helper, **make `pub(crate)`** if you need to call from another file in the same crate; otherwise add a thin re-export. Maps Rad→v, Deg→v.to_radians(), Grad→v*PI/200.
- `fn f64_from_radians(rad: f64, mode: AngleMode) -> f64` — same idea, reverse direction.
- Existing pattern (lines 158–168, `op_sin`): `let v = state.stack.x.inner().to_f64().ok_or(HpError::Overflow)?; let rad = to_radians_f64(v, state.angle_mode); let result = Decimal::from_f64(rad.sin()).map(HpNum::rounded).ok_or(HpError::Domain)?; unary_result(state, result); Ok(())`
  - **Note:** the existing `op_sin` precedent uses `.to_f64().ok_or(HpError::Overflow)?` for the X-read. Phase 20 new ops should prefer the **format.rs:161 precedent** — `.to_f64().expect("HpNum is always within f64 range")` — which eliminates the unreachable Overflow branch. The trig precedent is older; the format.rs precedent is what the zero-panic policy converged on for in-range-by-construction values.
- Existing pattern (lines 220–232, `op_asin`): domain guard, f64 compute, f64_from_radians, Decimal::from_f64 → HpNum::rounded.

From `hp41-core/src/ops/stack_ops.rs` (precedent for op_r_up):
- `pub fn op_rdn(state: &mut CalcState) -> Result<(), HpError>` at lines 49–57:
  ```
  let old_x = state.stack.x.clone();
  state.stack.x = state.stack.y.clone();
  state.stack.y = state.stack.z.clone();
  state.stack.z = state.stack.t.clone();
  state.stack.t = old_x;
  apply_lift_effect(state, LiftEffect::Neutral);
  Ok(())
  ```
  `op_r_up` is the same structure with reversed assignment chain: `X = T; T = Z; Z = Y; Y = old_x`.

From `hp41-core/src/ops/mod.rs`:
- `pub enum Op { /* variants */ }` at line 81; current variant count = 57 (Add through Rdprgm). Add 10 new variants — preferred locations in the enum:
  - **Pi** — after `Lastx`, in the stack-section (it's a literal-push; matches LastX semantics).
  - **PolarToRect, RectToPolar** — after `Atan` in the trig section.
  - **Rup** — after `Rdn` in the stack section (mirror sibling).
  - **Rnd, Frc, Abs, Sign** — after `Int` in the unary-math section.
  - **Mod** — after `YPow` in the binary section.
  - **Fact** — after `TenPow` in the unary-math section.
- `pub fn dispatch(state: &mut CalcState, op: Op) -> Result<(), HpError>` at line 307 — exhaustive `match op { ... }` from line 322 onwards. Add 10 new arms.
- The dispatch arms forward to `op_*` functions imported via the `use math::{...}` block at line 23 — extend that import list with the new math functions.

From `hp41-core/src/ops/program.rs`:
- `fn execute_op(state: &mut CalcState, op: Op) -> Result<(), HpError>` at line 262 — exhaustive `match op { ... }` mirroring dispatch(). Add 10 new arms. The inner `use crate::ops::math::{...}` block at line 265 must be extended.

From `hp41-cli/src/prgm_display.rs` and `hp41-gui/src-tauri/src/prgm_display.rs`:
- `fn op_display_name(op: &Op) -> String` — exhaustive `match op { ... }`. Add 10 new arms (parallel in both files).
- Display strings (D-22 dictates parity between copies):
  - `Op::Pi => "PI"`
  - `Op::PolarToRect => "P\u{2192}R"` (P→R, using the same `\u{2192}` codepoint pattern already present for HMS→H at line 121/141)
  - `Op::RectToPolar => "R\u{2192}P"`
  - `Op::Rnd => "RND"`
  - `Op::Frc => "FRC"`
  - `Op::Mod => "MOD"`
  - `Op::Abs => "ABS"`
  - `Op::Fact => "FACT"` (HP-41 hardware also accepts `N!` — `FACT` is the FOCAL mnemonic and is what `op_display_name` returns)
  - `Op::Sign => "SIGN"`
  - `Op::Rup => "R\u{2191}"` (R↑, mirror of `R\u{2193}` used for Op::Rdn at line 38/58)

From `just` recipes (justfile lines 18–36):
- `just test` runs `cargo test --workspace`
- `just lint` runs `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `just coverage` runs `cargo llvm-cov --fail-under-lines 80 -p hp41-core` (clean first). **Phase 20 enforces a stricter local target of 92.5% (non-regression from v2.1 baseline per CLAUDE.md §Quality Gates).** The `--fail-under-lines 80` arg in the justfile recipe is the global floor; Phase 20 verifies the stricter 92.5% threshold via the explicit `cargo llvm-cov` invocation in Task 6's acceptance criteria.
- `just ci` runs lint → test → coverage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave-0 prep): Extract round_to_display_precision helper in format.rs and refactor format_hpnum to call it</name>
  <files>hp41-core/src/format.rs</files>
  <read_first>
    - hp41-core/src/format.rs (entire file — current home of format_hpnum, format_fix, format_sci, format_eng; rounding strategy and digit-count semantics live here)
    - hp41-core/src/state.rs lines 39–47 (DisplayMode::Fix/Sci/Eng with u8 digit count)
    - hp41-core/src/num.rs lines 12–28 (HpNum::rounded, HpNum::inner, HpNum::zero) — note especially line 18's `.expect("round_sf_with_strategy(10) must succeed for valid finite Decimal")` idiom; the new helper MUST mirror this exact form
    - hp41-core/tests/format_tests.rs (the FIX/SCI/ENG display tests that must remain green — read at least the test names to know what behavior is locked)
    - hp41-core/tests/numerical_accuracy.rs first 100 lines (test_numerical_accuracy_suite — must remain ≥490/500 passing; do NOT touch this file)
    - CLAUDE.md §Settled Architecture Decisions / Core engine — 10-significant-digit rounding contract; the helper must preserve it
    - .planning/phases/20-core-math-and-conversions/20-CONTEXT.md lines 22–28 (D-01, D-02, D-03)
  </read_first>
  <action>
    Add a NEW public function `pub fn round_to_display_precision(n: &HpNum, mode: &DisplayMode) -> HpNum` to `hp41-core/src/format.rs`. Semantics per **D-01/D-02/D-03**:

    - **Fix(digits):** Round the inner Decimal to `digits` decimal places with `RoundingStrategy::MidpointAwayFromZero` (matches the rounding in `format_fix` at line 53–54), then wrap in `HpNum::rounded` (re-applies the 10-sig-digit gate as a final safety pass; the round_dp output is already at ≤10 sig digits for non-overflow cases but `HpNum::rounded` is idempotent for in-range values).
    - **Sci(digits):** Round to `digits + 1` significant digits total (the SCI mantissa carries 1 leading digit + `digits` fractional digits). Use the form `n.inner().round_sf_with_strategy((digits + 1) as u32, RoundingStrategy::MidpointAwayFromZero).expect("round_sf_with_strategy(<= 10) cannot fail for finite Decimal")` — this mirrors the `hp41-core/src/num.rs:18` precedent exactly. Do NOT use `.unwrap()` (the deny gate forbids it); do NOT use `.ok_or(...)?` (the error branch is unreachable for finite HpNum and adds dead code). Wrap the resulting Decimal with `HpNum::rounded`.
    - **Eng(digits):** Round mantissa to `digits + 1` significant digits with the engineering exponent constraint (multiples of 3). Compute `sci_exp = compute_sci_exp(abs_d)`, then `eng_exp = floor_to_multiple_of_3(sci_exp)`, then mantissa = `scale_decimal(abs_d, -eng_exp)`, round mantissa to `digits` decimal places, handle the carry (mantissa ≥ 10^(sci_exp - eng_exp + 1)) like `format_eng` does at lines 138–143. Re-scale back to the original magnitude and wrap with `HpNum::rounded`. Any internal `round_sf_with_strategy` call uses the same `.expect("round_sf_with_strategy(<= 10) cannot fail for finite Decimal")` idiom.
    - **Zero handling:** if `n.is_zero()`, return `HpNum::zero()` regardless of mode (skip rounding to avoid division-by-zero in log10).

    Then **refactor `format_hpnum`** so the same rounding logic is shared. Two acceptable shapes — pick whichever produces the smaller diff:
    - **Shape A (recommended):** leave `format_fix/sci/eng` as-is (they already do `round_dp_with_strategy` / `round_sf` inline) and make `round_to_display_precision` call into the same primitives or extracted private helpers. This duplicates the formula but is the smallest behavioral risk. The executor MAY refactor the inner rounding formula freely — the only hard requirement is that the `.expect("round_sf_with_strategy(<= 10) cannot fail for finite Decimal")` idiom (mirroring num.rs:18) is the named expectation under `#![deny(clippy::unwrap_used)]`.
    - Shape B: refactor `format_fix/sci/eng` so each starts with `let rounded = round_to_display_precision(&HpNum(d), mode); let d = rounded.inner(); /* existing string-formatting code */`. This is more aggressive — verify `numerical_accuracy.rs` and `format_tests.rs` still pass.

    Document the helper's contract in a `///` doc comment that explicitly references **D-01/D-02/D-03** and the upcoming `op_rnd` consumer.

    Make `compute_sci_exp`, `decimal_pow10`, `scale_decimal`, `floor_to_multiple_of_3` `pub(crate)` if they are not already, OR re-export the rounding logic inside `round_to_display_precision` without leaking them outside `format.rs`. Prefer the second option — keep `format.rs` internals private.

    Re-export from `hp41-core/src/lib.rs:14` if the helper needs to be reachable from `hp41-core/src/ops/math.rs::op_rnd`. Inside the same crate it's reachable as `crate::format::round_to_display_precision` without changing `lib.rs`. Default: do NOT touch `lib.rs` re-exports — use the `crate::format::` path from inside `op_rnd`.

    `#![deny(clippy::unwrap_used)]` applies: any `Decimal::from_str(...)` literal use `.expect("compile-time literal Decimal must parse")` per existing convention. Any `Decimal::round_sf_with_strategy(...)` call uses `.expect("round_sf_with_strategy(<= 10) cannot fail for finite Decimal")` per num.rs:18 precedent.

    Do NOT touch `hp41-core/tests/numerical_accuracy.rs` (Phase 27 owns extensions; Phase 20 must not regress).

    Do NOT add the `op_rnd` body in this task — Task 4 wires it. Task 1's deliverable is only the helper + format_hpnum parity preservation.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "pub fn round_to_display_precision" hp41-core/src/format.rs` returns exactly 1 line.
    - Source assertion: `grep -n "pub fn format_hpnum" hp41-core/src/format.rs` still returns exactly 1 line (the signature is unchanged).
    - Source assertion (idiom compliance): `grep -n 'round_sf_with_strategy' hp41-core/src/format.rs | grep -v '\.expect('` returns 0 lines — every `round_sf_with_strategy` call in the new helper is followed by `.expect("...")`, never by `.unwrap()` or `.ok_or(...)?`.
    - Test command: `just test -p hp41-core --test format_tests` exits 0 — all 15 existing FIX/SCI/ENG display tests pass unchanged.
    - Test command: `just test -p hp41-core --test numerical_accuracy` exits 0 — the 500-case suite passes (≥490/500 per D-08 in the suite header).
    - Lint command: `cargo clippy -p hp41-core -- -D warnings` exits 0 — no new clippy warnings, no `unwrap_used` violations.
    - Behavior assertion: `round_to_display_precision(&HpNum::from(...), &DisplayMode::Fix(1))` applied to a Decimal parsed from `"-5.65"` returns an HpNum whose inner Decimal equals `-5.7` (the round-half-away-from-zero target). Verify this in a smoke test inside `#[cfg(test)] mod tests { ... }` at the bottom of `format.rs` (the only inline test module pattern, per existing convention — but `format.rs` currently has no test module; the integration test in `tests/format_tests.rs` is sufficient if you prefer not to add an inline module).
  </acceptance_criteria>
  <verify>
    <automated>just test -p hp41-core --test format_tests &amp;&amp; just test -p hp41-core --test numerical_accuracy &amp;&amp; cargo clippy -p hp41-core -- -D warnings</automated>
  </verify>
  <done>round_to_display_precision exists as a pub fn in hp41-core/src/format.rs with documented Fix/Sci/Eng semantics; all `round_sf_with_strategy(...)` calls use the `.expect("round_sf_with_strategy(<= 10) cannot fail for finite Decimal")` idiom (mirrors num.rs:18); format_hpnum is unchanged (Shape A) or shares the helper internally (Shape B); all existing format tests pass; numerical_accuracy suite passes; clippy is green.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave-1): Add HpError::OutOfRange and 10 new Op enum variants + dispatch() arms in ops/mod.rs</name>
  <files>hp41-core/src/error.rs, hp41-core/src/ops/mod.rs</files>
  <read_first>
    - hp41-core/src/error.rs (current HpError variants — 8 today)
    - hp41-core/src/ops/mod.rs lines 1–510 (Op enum definition, dispatch function, the `use math::{...}` import list)
    - hp41-core/src/ops/math.rs (precedent for the `op_*` functions that will be referenced by the new dispatch arms — Task 4 implements them, but the dispatch arm signature must match)
    - hp41-core/src/ops/stack_ops.rs (precedent for op_rdn that op_r_up will mirror)
    - .planning/phases/20-core-math-and-conversions/20-CONTEXT.md (D-08 PI display value, D-10 PI LiftEffect, D-19/D-20 R↑ home/name, D-22 4-place rule, D-25 LiftEffect summary)
    - CLAUDE.md §Settled Architecture Decisions and §Critical Implementation Traps (4-place Op-variant rule)
  </read_first>
  <action>
    **Step 1 — add `HpError::OutOfRange` variant** in `hp41-core/src/error.rs`:

    Add a new variant `OutOfRange` between `Domain` and `CallDepth` (preserving discriminant ordering for any latent matches; new variants at the end of the enum are also acceptable). Use `#[error("out of range")]` to follow the existing thiserror message pattern. Update the `Clone, Debug, PartialEq` derive — no change needed; they already cover new variants.

    Rationale per **D-06:** FACT X > 69 must return `OutOfRange` (the hardware-spec threshold from SC-3), distinct from `Overflow` (returned by `Decimal::from_f64` failure for X ≥ 28 per D-05). Both errors are surfaced to the CLI display by existing error-routing code in `hp41-cli/src/app.rs` (no changes needed there in Phase 20).

    **Step 2 — add 10 new `Op` variants** to `pub enum Op` in `hp41-core/src/ops/mod.rs` (around line 81–259), with doc comments noting LiftEffect per **D-25**. Place each variant in its semantic section (see `<interfaces>` block above for the section guidance):

    - `Pi` — after `Lastx` (stack-literal section). Doc: `/// PI — push the constant π (3.141592654, 10-digit rounded HP-41 hardware value). LiftEffect: Enable.`
    - `Rup` — after `Rdn`. Doc: `/// R↑ — roll stack up (mirror of Rdn). LiftEffect: Neutral. Does NOT update LASTX.`
    - `Rnd` — after `Int`. Doc: `/// RND — round X to the precision of the current display mode (FIX/SCI/ENG n). LiftEffect: Enable (via unary_result).`
    - `Frc` — after `Rnd`. Doc: `/// FRC — fractional part of X (sign-preserving, complement of INT). LiftEffect: Enable.`
    - `Abs` — after `Frc`. Doc: `/// ABS — absolute value of X. LiftEffect: Enable.`
    - `Sign` — after `Abs`. Doc: `/// SIGN — sign of X: -1 / 0 / +1. LiftEffect: Enable. (Phase 20 always returns numeric; SIGN-on-ALPHA divergence is documented in Phase 25 docs per D-18.)`
    - `Fact` — after `Sign`. Doc: `/// FACT — factorial of integer X. Domain error for non-integer or negative X. OutOfRange for X > 69 (hardware spec, D-06). Overflow for X ≥ 28 (Decimal range cap, D-05). LiftEffect: Enable.`
    - `Mod` — after `YPow` (binary section). Doc: `/// MOD — Y mod X with HP-41 trunc-toward-zero convention (D-14): result = Y - X * trunc(Y/X). Sign follows Y (matches HP-41C Owner's Manual + Free42 source). Examples: 7 MOD -3 = 1; -7 MOD 3 = -1. Domain error if X = 0. LiftEffect: Enable (via binary_result).`
    - `PolarToRect` — after `Atan` (trig section). Doc: `/// P→R — convert polar (Y=magnitude, X=angle in current angle_mode) to rectangular (Y=x-coord, X=y-coord). LiftEffect: Enable. Direct stack assignment; LASTX ← consumed X (D-11, D-12, D-13).`
    - `RectToPolar` — after `PolarToRect`. Doc: `/// R→P — convert rectangular (Y=x-coord, X=y-coord) to polar (Y=magnitude, X=angle in current angle_mode). LiftEffect: Enable. Direct stack assignment; LASTX ← consumed X.`

    Variant naming locked per CONTEXT D-20 (`Rup` not `RollUp`) and Claude's-Discretion note (`Mod` not `ModOp` — it is a variant identifier in scope `Op::`, not a module name, so no shadow conflict).

    **Step 3 — extend the `use math::{...}` import block** at line 23 to include `op_pi, op_polar_to_rect, op_rect_to_polar, op_rnd, op_frc, op_mod, op_abs, op_fact, op_sign`. Extend the `use stack_ops::{...}` import at line 31 to include `op_r_up`.

    **Step 4 — add 10 new arms to the `match op { ... }` block inside `dispatch()`** (line 322). Each arm is a one-liner forwarding to the corresponding `op_*` function:

    - `Op::Pi => math::op_pi(state),` (placed in or near the stack section — after `Op::Lastx => op_lastx(state),` at line 333)
    - `Op::Rup => op_r_up(state),` (after `Op::Rdn => op_rdn(state),` at line 331)
    - `Op::Rnd => op_rnd(state),` (after `Op::Int => op_int(state),` at line 339)
    - `Op::Frc => op_frc(state),`
    - `Op::Abs => op_abs(state),`
    - `Op::Sign => op_sign(state),`
    - `Op::Fact => op_fact(state),`
    - `Op::Mod => op_mod(state),` (after `Op::YPow => op_ypow(state),` at line 343)
    - `Op::PolarToRect => op_polar_to_rect(state),` (after `Op::Atan => op_atan(state),` at line 354)
    - `Op::RectToPolar => op_rect_to_polar(state),`

    **Important: stubs allowed for compile-only landing.** It is acceptable for Task 2 to ship the `op_*` functions as stubs returning `Err(HpError::InvalidOp)` (or `unimplemented!()` IS NOT acceptable — it panics, violating D-23 `#![deny(clippy::unwrap_used)]` policy). Task 4 fills them in. Recommended stub body:
    ```
    pub fn op_pi(state: &mut CalcState) -> Result<(), HpError> {
        let _ = state;
        Err(HpError::InvalidOp)
    }
    ```
    OR (preferred for compile-only landing) leave the stubs as a placeholder that the next task replaces inline. The build MUST compile cleanly with `just lint` after Task 2.

    Do NOT touch `synthetic_byte_to_op()` at line 475 — none of the 10 new ops have HP-41 NUT byte codes in the curated subset (Phase 12 froze the safe subset).

    Do NOT touch `execute_op()` in `program.rs` — that's Task 3.
    Do NOT touch the two `prgm_display.rs` files — that's Task 5.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -E "^\s*OutOfRange" hp41-core/src/error.rs` returns 1 match.
    - Source assertion: `grep -E "^\s*(Pi|Rup|Rnd|Frc|Abs|Sign|Fact|Mod|PolarToRect|RectToPolar)," hp41-core/src/ops/mod.rs` returns 10 matches (the 10 new Op enum variants).
    - Source assertion: `grep -E "Op::(Pi|Rup|Rnd|Frc|Abs|Sign|Fact|Mod|PolarToRect|RectToPolar) =>" hp41-core/src/ops/mod.rs` returns at least 10 matches (the dispatch arms).
    - Test command: `cargo build -p hp41-core` exits 0 — exhaustive match in `Op` and `dispatch()` compiles. The build will fail if a variant is added to `Op` but not handled in `dispatch()` (Rust's exhaustive match enforcement) — that's how D-22 is mechanically enforced.
    - Lint command: `cargo clippy -p hp41-core -- -D warnings` exits 0 — stub functions are non-panicking (`Err(HpError::InvalidOp)` only).
    - Test command: `just test -p hp41-core --test numerical_accuracy` exits 0 — still no regression (Task 2 doesn't touch behavior of existing ops).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo clippy -p hp41-core -- -D warnings &amp;&amp; just test -p hp41-core --test numerical_accuracy</automated>
  </verify>
  <done>HpError::OutOfRange exists; 10 new Op variants exist in hp41-core/src/ops/mod.rs::Op; 10 new dispatch() arms exist; stub op_* functions in math.rs/stack_ops.rs let the crate compile cleanly with -D warnings.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave-1): Add 10 new execute_op() arms in ops/program.rs (mirror of dispatch)</name>
  <files>hp41-core/src/ops/program.rs</files>
  <read_first>
    - hp41-core/src/ops/program.rs lines 258–406 (the entire execute_op function — the second of the 4 places per CLAUDE.md §Critical Implementation Traps)
    - hp41-core/src/ops/mod.rs (the dispatch() arms added in Task 2 — execute_op mirrors them exactly)
    - .planning/phases/20-core-math-and-conversions/20-CONTEXT.md D-22 (the 4-place rule)
  </read_first>
  <action>
    Extend the `use crate::ops::math::{...}` import block at line 265 to include the same 9 new math symbols added in Task 2 (`op_pi, op_polar_to_rect, op_rect_to_polar, op_rnd, op_frc, op_mod, op_abs, op_fact, op_sign`).

    Extend the `use crate::ops::stack_ops::{...}` import at line 270 to include `op_r_up`.

    Add 10 new arms to the `match op { ... }` block inside `execute_op()` (line 273), one per new Op variant. Each arm is a one-liner forwarding to the corresponding `op_*` function — identical shape to the dispatch arms added in Task 2:

    - `Op::Pi => op_pi(state),`
    - `Op::Rup => op_r_up(state),`
    - `Op::Rnd => op_rnd(state),`
    - `Op::Frc => op_frc(state),`
    - `Op::Abs => op_abs(state),`
    - `Op::Sign => op_sign(state),`
    - `Op::Fact => op_fact(state),`
    - `Op::Mod => op_mod(state),`
    - `Op::PolarToRect => op_polar_to_rect(state),`
    - `Op::RectToPolar => op_rect_to_polar(state),`

    The compile-time exhaustive match on `Op` will FAIL with a "non-exhaustive patterns" error if any of the 10 variants is missing here — that's the second compile-time gate from D-22.

    Do NOT touch the trailing `Op::Lbl(_) | Op::Gto(_) | ... => Err(HpError::InvalidOp)` arm at lines 397–404 — those are programming control ops handled by `run_loop` separately, and none of the new Phase 20 ops fall into that category.

    None of the 10 new ops trigger a synthetic byte code (Phase 12 froze the safe subset to 24 entries; Phase 20 does NOT extend it).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -E "Op::(Pi|Rup|Rnd|Frc|Abs|Sign|Fact|Mod|PolarToRect|RectToPolar) =>" hp41-core/src/ops/program.rs` returns at least 10 matches.
    - Source assertion: `grep -c "Op::Pi" hp41-core/src/ops/program.rs` returns ≥ 1 (the execute_op arm).
    - Test command: `cargo build -p hp41-core` exits 0 — the second exhaustive-match gate is satisfied. Critically: temporarily DELETE one of the 10 new arms locally and re-run `cargo build -p hp41-core` to confirm Rust reports "non-exhaustive patterns" — restore the arm before committing. (This is a manual sanity check, not a CI step.)
    - Lint command: `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - Test command: `just test -p hp41-core --test program_tests` exits 0 — existing programming-engine tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo clippy -p hp41-core -- -D warnings &amp;&amp; just test -p hp41-core --test program_tests</automated>
  </verify>
  <done>10 new execute_op() arms exist in hp41-core/src/ops/program.rs; compile-time exhaustive match passes; existing program_tests stay green.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave-1): Implement op_pi, op_polar_to_rect, op_rect_to_polar, op_rnd, op_frc, op_mod, op_abs, op_fact, op_sign in math.rs and op_r_up in stack_ops.rs</name>
  <files>hp41-core/src/ops/math.rs, hp41-core/src/ops/stack_ops.rs</files>
  <read_first>
    - hp41-core/src/ops/math.rs (entire file — every precedent op lives here: `op_sin` lines 158–168 for the f64 bridge, `op_asin` lines 220–232 for inverse trig f64 bridge, `op_int` lines 87–91 for trunc_int usage, `op_ypow` lines 129–133 for binary_result, `op_pct_change` lines 142–146 for unary_result on a "logically binary" op, `to_radians_f64` line 50, `f64_from_radians` line 210)
    - hp41-core/src/ops/stack_ops.rs lines 45–57 (op_rdn — the exact mirror template for op_r_up; LiftEffect::Neutral, does NOT update LASTX)
    - hp41-core/src/num.rs lines 17–18 (the established `.expect("round_sf_with_strategy(10) must succeed for valid finite Decimal")` idiom — Task 4 follows this precedent for any f64-to-Decimal conversion on known-finite values)
    - hp41-core/src/num.rs lines 213–226 (HpNum::trunc_int — used by FRC for `x - x.trunc_int()` and FACT for `x == x.trunc_int()`; HpNum::negate; HpNum::inner; HpNum::rounded)
    - hp41-core/src/format.rs around line 161 (the established `.to_f64().expect("HpNum is always within f64 range")` idiom — Task 4 mirrors this for X/Y reads in P→R, R→P, FACT)
    - hp41-core/src/stack.rs (apply_lift_effect, unary_result, binary_result, enter_number, LiftEffect — full file)
    - hp41-core/src/format.rs (round_to_display_precision added in Task 1 — op_rnd calls this)
    - hp41-core/src/error.rs (HpError variants including OutOfRange added in Task 2)
    - .planning/phases/20-core-math-and-conversions/20-CONTEXT.md (D-04 through D-20 — all per-op decisions; note D-14 was UPDATED 2026-05-13 to fix the MOD semantic to "sign follows Y" via trunc-toward-zero)
    - CLAUDE.md §Settled Architecture Decisions (BCD/f64 bridge, LiftEffect contract, #![deny(unwrap_used)])
  </read_first>
  <action>
    Replace the Task 2 stubs with real implementations. Every function signature is `pub fn op_NAME(state: &mut CalcState) -> Result<(), HpError>` to match the existing convention. Add `///` doc comments naming the LiftEffect and any error conditions, following the precedent of `op_sin` (lines 158–168) and `op_ypow` (lines 129–133).

    **Idiom guidance for f64 round-trips under `#![deny(clippy::unwrap_used)]`:** Two precedents apply.
    - For HpNum-X-to-f64 **reads** (input to an f64 computation): use `state.stack.x.inner().to_f64().expect("HpNum is always within f64 range")` — mirrors **`hp41-core/src/format.rs:161`** precedent. The Overflow branch is unreachable for finite HpNum (HpNum is constructed via `HpNum::rounded` and is always within Decimal range ⊂ f64 range), so introducing a `.ok_or(HpError::Overflow)?` here would create a spurious dead error path. The legacy trig precedent (`op_sin` line 158, `.to_f64().ok_or(HpError::Overflow)?`) is acceptable for already-existing ops but Phase 20 new ops should follow the format.rs idiom.
    - For f64-to-Decimal **conversion of the final result** (output): KEEP the `Decimal::from_f64(...).map(HpNum::rounded).ok_or(HpError::Overflow)` form — this path IS reachable (e.g., FACT(28) overflows Decimal range; `f64::sin` of an extreme angle could in theory produce NaN). The Overflow error on the result-conversion side is a legitimate, exercisable branch.

    **op_pi (math.rs, near top of file under a new `// ── Constant push ops ──` section):** Parse the literal Decimal `"3.141592653589793"` via `Decimal::from_str(...).expect("PI literal must parse")`, apply `HpNum::rounded(...)` to enforce the 10-sig-digit gate (result = `3.141592654` per D-08), then push onto the stack respecting lift_enabled. Implementation pattern mirrors `op_lastx` in `stack_ops.rs` lines 76–83 — force `state.stack.lift_enabled = true` before calling `enter_number(state, pi_value)`, then `apply_lift_effect(state, LiftEffect::Enable)`. Do NOT update LASTX (PI is a constant push, not arithmetic — same convention as `op_lastx`). LiftEffect: Enable per **D-10**.

    **op_polar_to_rect (math.rs, in the trig section after op_atan around line 261):** Per **D-11/D-12/D-13** — f64 bridge with direct stack assignment.
    - Read Y (magnitude) and X (angle) into f64 using the format.rs:161 idiom: `let r = state.stack.y.inner().to_f64().expect("HpNum is always within f64 range"); let theta = state.stack.x.inner().to_f64().expect("HpNum is always within f64 range");`
    - Convert angle to radians: `let rad = to_radians_f64(theta, state.angle_mode);`
    - Compute Cartesian: `let new_y = r * rad.cos(); let new_x = r * rad.sin();` (HP-41 convention: P→R puts the cos-projection in Y and sin-projection in X — i.e. (magnitude, angle) → (x-coord, y-coord) with Y=x-coord, X=y-coord per FN-MATH-02).
    - Convert back (final-result path, Overflow IS reachable): `let new_y_d = Decimal::from_f64(new_y).map(HpNum::rounded).ok_or(HpError::Overflow)?; let new_x_d = Decimal::from_f64(new_x).map(HpNum::rounded).ok_or(HpError::Overflow)?;`
    - Direct stack assignment (per D-12, `unary_result`/`binary_result` don't fit the binary-out shape): `state.stack.lastx = state.stack.x.clone(); state.stack.y = new_y_d; state.stack.x = new_x_d;`
    - Apply lift: `apply_lift_effect(state, LiftEffect::Enable);`

    **op_rect_to_polar (math.rs, after op_polar_to_rect):** Per **D-11/D-12/D-13** — f64 bridge with direct stack assignment.
    - Read Y (x-coord) and X (y-coord) into f64 using the format.rs:161 idiom: `let yc = state.stack.y.inner().to_f64().expect("HpNum is always within f64 range"); let xc = state.stack.x.inner().to_f64().expect("HpNum is always within f64 range");`
    - Magnitude: `let r = (yc * yc + xc * xc).sqrt();` (or `f64::hypot(yc, xc)` which is numerically more accurate — prefer hypot).
    - Angle: `let rad = xc.atan2(yc);` (NOTE the argument order: `atan2(y, x)` in standard math gives the angle; here Y register holds x-coord and X register holds y-coord per FN-MATH-03, so the call is `(stack_x).atan2(stack_y)`).
    - Convert angle from radians: `let angle = f64_from_radians(rad, state.angle_mode);`
    - Convert back to HpNum and direct stack assignment (final-result path, Overflow IS reachable): `state.stack.lastx = state.stack.x.clone(); state.stack.y = Decimal::from_f64(r).map(HpNum::rounded).ok_or(HpError::Overflow)?; state.stack.x = Decimal::from_f64(angle).map(HpNum::rounded).ok_or(HpError::Overflow)?;`
    - `apply_lift_effect(state, LiftEffect::Enable);`

    **op_rnd (math.rs, in the unary-math section after op_int around line 91):** Per **D-01/D-02/D-03** — value-rounding to current display precision.
    - `let rounded = crate::format::round_to_display_precision(&state.stack.x, &state.display_mode);`
    - `unary_result(state, rounded);` (this saves X→LASTX, writes the rounded value to X, and sets lift_enabled=true per D-25 RND=Enable).
    - `Ok(())`

    **op_frc (math.rs, after op_rnd):** Per **D-15** — sign-preserving fractional part.
    - `let int_part = state.stack.x.trunc_int();`
    - `let frac = state.stack.x.checked_sub(&int_part)?;` (checked_sub returns Result; for "well-bounded" sub it can't realistically overflow on values within HpNum range, but propagate via `?` to keep the function infallible-looking for clippy)
    - `unary_result(state, frac); Ok(())`
    - This gives `FRC(-3.7) = -0.7` because `trunc_int(-3.7) = -3` and `-3.7 - (-3) = -0.7`. Sign-preserving per FN-MATH-05.

    **op_abs (math.rs, after op_frc):** Per **D-16**.
    - `let result = HpNum(state.stack.x.inner().abs());` — direct construction via `pub(crate)` inner field; OR `let result = if state.stack.x.inner().is_sign_negative() { state.stack.x.negate() } else { state.stack.x.clone() };` if you prefer not to touch the `pub(crate)` field. The second form is preferred since `negate()` is already public.
    - `unary_result(state, result); Ok(())`

    **op_sign (math.rs, after op_abs):** Per **D-17/D-18**.
    - `let v = state.stack.x.inner();`
    - `let result = if v.is_zero() { HpNum::zero() } else if v.is_sign_negative() { HpNum::from(-1i32) } else { HpNum::from(1i32) };`
    - `unary_result(state, result); Ok(())`
    - Note: Phase 20 always returns numeric (HP-41 hardware's ALPHA-typed-X divergence is documented but not modeled here; the divergence note ships in Phase 25 docs per **D-18**).

    **op_fact (math.rs, after op_sign):** Per **D-04/D-05/D-06/D-07** — strict order of checks:
    1. Read X to f64 for the magnitude check using the format.rs:161 idiom: `let v = state.stack.x.inner().to_f64().expect("HpNum is always within f64 range");`
    2. Pre-flight hardware-spec **OutOfRange** check (D-06): if `v > 69.0`, return `Err(HpError::OutOfRange)`. This MUST come before the integer check so X=70.5 still returns OutOfRange (not Domain), matching the spirit of SC-3.
    3. Integer check (D-07): `let int_x = state.stack.x.trunc_int(); if state.stack.x != int_x { return Err(HpError::Domain); }` (using `PartialEq` on HpNum which compares the inner Decimal).
    4. Negative check (D-07): if `v < 0.0`, return `Err(HpError::Domain)`.
    5. Compute factorial in f64 (D-04): `let n = v as u64;` then iterative product `let mut acc: f64 = 1.0; for k in 1..=n { acc *= k as f64; }`. For n ≤ 27 this fits in Decimal range; for n = 28..=69 the f64 product is finite but `Decimal::from_f64` returns None.
    6. Convert back, final-result path (D-05, Overflow IS reachable): `let result = Decimal::from_f64(acc).map(HpNum::rounded).ok_or(HpError::Overflow)?;`
    7. `unary_result(state, result); Ok(())`
    - Edge cases: `0! = 1` (the loop range `1..=0` is empty so acc remains 1.0 — correct). `1! = 1` (loop runs once, acc = 1.0). Both pass through `unary_result` cleanly.

    **op_mod (math.rs, in the binary section after op_ypow around line 133):** Per **D-14** (UPDATED 2026-05-13) — `Y - X * trunc(Y/X)` with `trunc` = truncate-toward-zero. **Sign follows Y** (HP-41 truncate-toward-zero convention; matches HP-41C Owner's Manual + Free42 source).

    Doc comment must explicitly cite the corrected D-14 wording: ```/// MOD — Y mod X with HP-41 trunc-toward-zero convention (D-14). /// Result = Y - X * trunc(Y/X). Sign follows Y (matches HP-41C Owner's Manual + Free42 source). /// Examples: 7 MOD -3 = 1 (sign of Y); -7 MOD 3 = -1 (sign of Y). /// Domain error if X = 0. LiftEffect: Enable (via binary_result).```

    Implementation:
    - Domain guard: if `state.stack.x.is_zero()`, return `Err(HpError::Domain)`.
    - Compute quotient: `let q = state.stack.y.checked_div(&state.stack.x)?;`
    - Truncate toward zero: `let q_trunc = q.trunc_int();` (HpNum::trunc_int from num.rs:213–226 truncates toward zero, matching the corrected D-14 formula).
    - Compute: `let product = state.stack.x.checked_mul(&q_trunc)?; let result = state.stack.y.checked_sub(&product)?;`
    - `binary_result(state, result); Ok(())`

    Worked example for the doc comment (verify against the corrected D-14):
    - `7 MOD -3`: `q = 7/-3 = -2.333...`; `trunc_int(-2.333) = -2`; `result = 7 - (-3)*(-2) = 7 - 6 = 1` ✓ sign of Y (positive Y → positive result).
    - `-7 MOD 3`: `q = -7/3 = -2.333...`; `trunc_int(-2.333) = -2`; `result = -7 - 3*(-2) = -7 + 6 = -1` ✓ sign of Y (negative Y → negative result).
    - `7 MOD 3`: `q = 2.333...`; `trunc_int = 2`; `result = 7 - 3*2 = 1` ✓.
    - `-7 MOD -3`: `q = 2.333...`; `trunc_int = 2`; `result = -7 - (-3)*2 = -7 + 6 = -1` ✓.

    **op_r_up (stack_ops.rs, immediately after op_rdn at line 57):** Per **D-19/D-20** — exact mirror of op_rdn with reversed assignment chain.
    ```
    /// R↑ (Roll Up): Rotate the stack so X←T, T←Z, Z←Y, Y←X (mirror of Rdn).
    ///
    /// Does NOT update LASTX (R↑ is a stack reorganization, not an arithmetic result —
    /// same convention as op_rdn, D-19).
    /// LiftEffect: Neutral (D-25)
    pub fn op_r_up(state: &mut CalcState) -> Result<(), HpError> {
        let old_x = state.stack.x.clone();
        state.stack.x = state.stack.t.clone();
        state.stack.t = state.stack.z.clone();
        state.stack.z = state.stack.y.clone();
        state.stack.y = old_x;
        apply_lift_effect(state, LiftEffect::Neutral);
        Ok(())
    }
    ```

    **#![deny(clippy::unwrap_used)] compliance (D-23):** Every production-code call site uses `?`-propagation (for `Result<...>` returns), `.expect("reason")` (for literal `Decimal::from_str` parses, `to_f64()` on in-range HpNum), or `.map(...).ok_or(HpError::...)` (for `Decimal::from_f64` final-result conversion where Overflow is reachable). The integer cast `v as u64` in op_fact is fine — the bounds (0..=69) are already checked.

    **No new tests in this task — Task 6 owns the integration tests in `tests/phase20_math.rs`.** Existing `cargo test` runs MUST stay green (Task 4 just turns the stubs from Task 2 into real implementations; the dispatch wiring is already in place).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -E "^pub fn (op_pi|op_polar_to_rect|op_rect_to_polar|op_rnd|op_frc|op_mod|op_abs|op_fact|op_sign)" hp41-core/src/ops/math.rs` returns exactly 9 matches.
    - Source assertion: `grep -E "^pub fn op_r_up" hp41-core/src/ops/stack_ops.rs` returns exactly 1 match.
    - Source assertion: `grep -n "to_radians_f64\|f64_from_radians" hp41-core/src/ops/math.rs` returns matches both for the existing trig ops AND for the new op_polar_to_rect / op_rect_to_polar (confirming reuse of the angle-mode helpers per D-13).
    - Source assertion: `grep -n "round_to_display_precision" hp41-core/src/ops/math.rs` returns at least 1 match (op_rnd consumes the Task 1 helper).
    - Source assertion (idiom compliance — read side): `grep -n 'inner()\.to_f64()' hp41-core/src/ops/math.rs | grep -v '\.expect(' | grep -v '\.ok_or('` returns 0 lines, AND for the three new ops the form must be `.expect("HpNum is always within f64 range")` (per format.rs:161 precedent) — verify by `grep -B1 'HpNum is always within f64 range' hp41-core/src/ops/math.rs` returning a non-empty result that references op_polar_to_rect / op_rect_to_polar / op_fact.
    - Source assertion (MOD doc-comment cites corrected D-14): `grep -A2 '^/// MOD' hp41-core/src/ops/math.rs | grep -E 'sign follows Y|trunc-toward-zero'` returns at least 1 match.
    - Source assertion: `grep -c "\.unwrap()" hp41-core/src/ops/math.rs` returns 0 within production code; any test modules carry `#[allow(clippy::unwrap_used)]`.
    - Test command: `cargo build -p hp41-core` exits 0 — no more stub bodies; everything compiles cleanly.
    - Lint command: `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - Test command: `just test -p hp41-core --test math_tests` exits 0 — existing math_tests still pass (op_sin/op_cos/etc. precedent paths are unchanged).
    - Test command: `just test -p hp41-core --test stack_tests` exits 0 — existing stack_tests still pass (op_rdn precedent path is unchanged).
    - Test command: `just test -p hp41-core --test numerical_accuracy` exits 0 — the 500-case suite still passes (Phase 20 must NOT regress this).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p hp41-core &amp;&amp; cargo clippy -p hp41-core -- -D warnings &amp;&amp; just test -p hp41-core --test math_tests &amp;&amp; just test -p hp41-core --test stack_tests &amp;&amp; just test -p hp41-core --test numerical_accuracy</automated>
  </verify>
  <done>All 9 new math op_* functions implemented in math.rs; op_r_up implemented in stack_ops.rs; LiftEffect declared per D-25; f64-bridge pattern followed for P→R/R→P/FACT/PI; HpNum-native path for RND/FRC/MOD/ABS/SIGN/R↑; the `.to_f64().expect("HpNum is always within f64 range")` idiom (format.rs:161 precedent) used for X/Y reads; `Decimal::from_f64(...).map(HpNum::rounded).ok_or(HpError::Overflow)` used for final-result conversion (reachable Overflow); op_mod doc-comment cites the corrected D-14 ("sign follows Y" via trunc-toward-zero); #![deny(unwrap_used)] gate still green; all existing test suites still pass.</done>
</task>

<task type="auto">
  <name>Task 5 (Wave-1): Add 10 op_display_name arms to BOTH prgm_display.rs copies (CLI + GUI)</name>
  <files>hp41-cli/src/prgm_display.rs, hp41-gui/src-tauri/src/prgm_display.rs</files>
  <read_first>
    - hp41-cli/src/prgm_display.rs (entire file — 167 lines; contains the `op_display_name(op: &Op) -> String` function with one arm per Op variant)
    - hp41-gui/src-tauri/src/prgm_display.rs (entire file — 216 lines; parallel structure, also contains `format_all_steps` which Phase 20 does NOT touch)
    - .planning/phases/20-core-math-and-conversions/20-CONTEXT.md D-22, D-24 (4-place rule + SC-4 invariant — prgm_display.rs is the ONLY allowed touch under hp41-gui/src-tauri/)
    - CLAUDE.md §"Settled Architecture Decisions / v2.0 additions" line about op_display_name being the intentional duplication exception under SC-4
  </read_first>
  <action>
    Add 10 new arms to the `match op { ... }` block in `op_display_name(op: &Op)` in BOTH files. The arms must be **byte-identical** in both copies (the duplication is intentional per CLAUDE.md SC-4 invariant note — `op_display_name` in `hp41-gui/src-tauri/src/prgm_display.rs` is the documented exception to SC-4).

    Insert the new arms in their semantic sections, matching the section markers already used in the files. Suggested arms (must be byte-identical across both files):

    - In or near the Phase 1 stack section (after `Op::Lastx => "LASTX".to_string(),`):
      - `Op::Rup => "R\u{2191}".to_string(),` (R↑, parallel to `Op::Rdn => "R\u{2193}".to_string()` for R↓)
    - In a new section `// ── Phase 20: Math constants ──` (or grouped with existing math):
      - `Op::Pi => "PI".to_string(),`
    - In the Phase 2 unary-math section (after `Op::Int => "INT".to_string(),`):
      - `Op::Rnd => "RND".to_string(),`
      - `Op::Frc => "FRC".to_string(),`
      - `Op::Abs => "ABS".to_string(),`
      - `Op::Sign => "SIGN".to_string(),`
      - `Op::Fact => "FACT".to_string(),`
    - In the binary section (after `Op::YPow => "Y^X".to_string(),`):
      - `Op::Mod => "MOD".to_string(),`
    - In the Phase 2 trig section (after `Op::Atan => "ATAN".to_string(),`):
      - `Op::PolarToRect => "P\u{2192}R".to_string(),` (P→R; reuse the `\u{2192}` codepoint already used by HMS→H at line 121 of hp41-cli copy / line 141 of hp41-gui copy)
      - `Op::RectToPolar => "R\u{2192}P".to_string(),`

    The exhaustive match on `Op` enum will FAIL to compile in BOTH `hp41-cli` and `hp41-gui/src-tauri` if any of the 10 arms is missing — that's the third and fourth compile-time gates from D-22.

    Do NOT touch `format_step()` (line 13 in CLI, line 17 in GUI) — its logic is generic over `op_display_name` so it picks up the new arms automatically.

    Do NOT touch `format_all_steps()` in the GUI copy (lines 31–41) — same reason.

    Update the inline test module in `hp41-gui/src-tauri/src/prgm_display.rs` (lines 167–216) IF appropriate. The existing test `test_format_all_steps_nonempty` could optionally be extended with one new Op variant to demonstrate Phase 20 coverage — but it is not required. Recommended: add one tiny test in each file's test module:

    In `hp41-cli/src/prgm_display.rs` `#[cfg(test)] mod tests`:
    ```
    #[test]
    fn test_display_phase20_op_labels() {
        assert_eq!(op_display_name(&Op::Pi), "PI");
        assert_eq!(op_display_name(&Op::Rup), "R\u{2191}");
        assert_eq!(op_display_name(&Op::PolarToRect), "P\u{2192}R");
        assert_eq!(op_display_name(&Op::RectToPolar), "R\u{2192}P");
        assert_eq!(op_display_name(&Op::Rnd), "RND");
        assert_eq!(op_display_name(&Op::Frc), "FRC");
        assert_eq!(op_display_name(&Op::Mod), "MOD");
        assert_eq!(op_display_name(&Op::Abs), "ABS");
        assert_eq!(op_display_name(&Op::Fact), "FACT");
        assert_eq!(op_display_name(&Op::Sign), "SIGN");
    }
    ```

    Add an equivalent `test_display_phase20_op_labels` in `hp41-gui/src-tauri/src/prgm_display.rs` (under the existing `#[allow(clippy::unwrap_used)] mod tests` at line 167).

    **SC-4 invariant gate (D-24):** Do NOT add any new `op_*` / `flush_entry_*` / `format_hpnum` function bodies anywhere under `hp41-gui/src-tauri/src/` — only `op_display_name` arms are permitted. Verify with the stricter grep in CLAUDE.md §"SC-4 invariant":
    `grep -rn "fn op_\(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum\)" hp41-gui/src-tauri/src/`
    The output of that grep must remain empty (or unchanged from the pre-Phase-20 baseline).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -E "Op::(Pi|Rup|Rnd|Frc|Abs|Sign|Fact|Mod|PolarToRect|RectToPolar) =>" hp41-cli/src/prgm_display.rs` returns exactly 10 matches.
    - Source assertion: `grep -E "Op::(Pi|Rup|Rnd|Frc|Abs|Sign|Fact|Mod|PolarToRect|RectToPolar) =>" hp41-gui/src-tauri/src/prgm_display.rs` returns exactly 10 matches.
    - SC-4 invariant assertion: `grep -rn 'fn op_\(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum\)' hp41-gui/src-tauri/src/` returns 0 matches (no calculator/math logic duplicated; D-24).
    - Behavior assertion (CLI test): `op_display_name(&Op::Pi)` returns `"PI"`; `op_display_name(&Op::Rup)` returns `"R↑"` (the U+2191 codepoint); `op_display_name(&Op::PolarToRect)` returns `"P→R"`.
    - Test command: `cargo build -p hp41-cli` exits 0 — fourth exhaustive-match gate passes.
    - Test command: `cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml` exits 0 — fifth exhaustive-match gate passes.
    - Test command: `cargo test -p hp41-cli --test '*'` exits 0; if you added the optional `test_display_phase20_op_labels` test, it passes.
    - Test command: `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p hp41-cli &amp;&amp; cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml &amp;&amp; cargo test -p hp41-cli &amp;&amp; cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml</automated>
  </verify>
  <done>10 op_display_name arms exist in both prgm_display.rs copies (byte-identical mnemonics); SC-4 invariant grep returns nothing; both hp41-cli and hp41-gui/src-tauri compile and test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6 (Wave-2): Create hp41-core/tests/phase20_math.rs covering all 4 ROADMAP SCs + per-op happy/error paths</name>
  <files>hp41-core/tests/phase20_math.rs</files>
  <read_first>
    - hp41-core/tests/math_tests.rs (precedent — uses `hp41_core::ops::{dispatch, Op}` and `hp41_core::CalcState`, with `#![allow(clippy::unwrap_used)]` at the file level OR `#[allow(clippy::unwrap_used)]` on the module)
    - hp41-core/tests/stack_tests.rs (precedent for stack-state assertions)
    - hp41-core/tests/format_tests.rs (precedent for FIX/SCI/ENG behaviour in tests — useful for op_rnd assertions)
    - hp41-core/src/ops/mod.rs (Op enum, the 10 new variants from Task 2)
    - hp41-core/src/state.rs (CalcState::new() — starting state for tests)
    - hp41-core/src/num.rs (HpNum::from(i32), HpNum constructors useful in tests)
    - .planning/ROADMAP.md lines 35–50 (Phase 20 Success Criteria — SC-1 through SC-5)
    - .planning/phases/20-core-math-and-conversions/20-CONTEXT.md (all D-XX decisions for the assertions — note D-14 was UPDATED 2026-05-13 to "sign follows Y" trunc-based convention)
    - .planning/REQUIREMENTS.md FN-MATH-06 (UPDATED 2026-05-13 with explicit examples `7 MOD -3 = 1`, `-7 MOD 3 = -1`)
  </read_first>
  <behavior>
    The test file `hp41-core/tests/phase20_math.rs` MUST cover the 4 ROADMAP success criteria SC-1, SC-2, SC-3, SC-4 (SC-5 is enforced at compile time by the exhaustive matches in Tasks 2/3/5 — no runtime test needed). It also covers per-op happy paths and the FACT error-path matrix per D-04 through D-07.

    Test list (each `#[test]` is one item; minimum 14 tests):

    1. `test_pi_pushes_ten_digit_rounded_and_lifts_stack` — Empty state, dispatch `Op::Pi`. Assert `state.stack.x.inner() == Decimal::from_str("3.141592654").unwrap()`. Then dispatch `Op::Pi` again and assert the previous X has rolled into Y (stack lifted). **Covers SC-1, FN-MATH-01, D-08, D-10.**

    2. `test_polar_to_rect_deg_mode_three_four_at_5313` — Set angle_mode = Deg; push Y=3.0 (magnitude), X=53.13010235 (degrees). Dispatch `Op::PolarToRect`. Assert `state.stack.y` ≈ 1.8 (x-coord = 3·cos(53.13°), but actually `r·cos(angle)` ≈ 3·0.6 = 1.8) and `state.stack.x` ≈ 2.4 (y-coord = 3·sin(53.13°) ≈ 3·0.8 = 2.4). **NOTE:** The ROADMAP wording in SC-2 ("entering `3 ENTER 4 ENTER 5` and pressing `R→P` produces magnitude ≈ 5 in Y and angle ≈ 53.13°") is about R→P not P→R; the test name reflects the actual operation under test. Use a tolerance of `1e-9` on `f64` round-trip comparisons. **Covers FN-MATH-02, D-11, D-13.**

    3. `test_rect_to_polar_deg_mode_three_four_to_5_at_5313` — Set angle_mode = Deg; push Y=3.0 (x-coord), X=4.0 (y-coord). Dispatch `Op::RectToPolar`. Assert `state.stack.y.inner()` rounds to 5.0 (magnitude = sqrt(9+16) = 5) and `state.stack.x.inner()` ≈ 53.13010235 (angle in degrees = atan2(4,3) ≈ 53.13°). Tolerance ≤ 1e-7. **Covers SC-2 part-a, FN-MATH-03, D-11, D-13.**

    4. `test_rect_to_polar_rad_mode` — Same as test 3 but with `state.angle_mode = AngleMode::Rad`. Assert angle is in radians: ≈ 0.927295218 (atan2(4,3) in radians). **Covers SC-2 part-b (RAD mode).**

    5. `test_rnd_fix_one_keeps_one_decimal_for_negative_value` — Set `state.display_mode = DisplayMode::Fix(1)`, push X = -5.7 (from `Decimal::from_str("-5.7")`). Dispatch `Op::Chs` (to ensure CHS path is exercised — optional; can also start with -5.7 directly via `HpNum::from(Decimal::from_str("-5.7").unwrap())`). Dispatch `Op::Rnd`. Assert `state.stack.x.inner() == Decimal::from_str("-5.7").unwrap()`. **Covers SC-3 part-a (FIX 1), FN-MATH-04, D-01–D-03.**

    6. `test_rnd_fix_zero_rounds_to_integer_minus_six` — Set `state.display_mode = DisplayMode::Fix(0)`, push X = -5.7. Dispatch `Op::Rnd`. Assert `state.stack.x.inner() == Decimal::from(-6)` (round-half-away-from-zero: -5.7 → -6, not -5). **Covers SC-3 part-a (FIX 0).**

    7. `test_fact_five_equals_120` — Push X = 5. Dispatch `Op::Fact`. Assert `state.stack.x.inner() == Decimal::from(120)`. Assert `state.stack.lastx.inner() == Decimal::from(5)` (LASTX captured). **Covers FN-MATH-08 happy path.**

    8. `test_fact_twenty_seven_succeeds` — Push X = 27. Dispatch `Op::Fact`. Assert returns `Ok(())` and X is non-zero (27! ≈ 1.08889e28, within Decimal range). **Covers D-05 magnitude wall edge.**

    9. `test_fact_twenty_eight_returns_overflow` — Push X = 28. Dispatch `Op::Fact`. Assert returns `Err(HpError::Overflow)` (D-05: 28! ≈ 3.05e29 exceeds Decimal range). **Covers D-05.**

    10. `test_fact_seventy_returns_out_of_range` — Push X = 70. Dispatch `Op::Fact`. Assert returns `Err(HpError::OutOfRange)` (D-06: hardware-spec pre-flight). **Covers SC-3 part-b, D-06.**

    11. `test_fact_negative_returns_domain` — Push X = -1. Dispatch `Op::Fact`. Assert returns `Err(HpError::Domain)`. **Covers D-07.**

    12. `test_fact_non_integer_returns_domain` — Push X = 2.5. Dispatch `Op::Fact`. Assert returns `Err(HpError::Domain)`. **Covers D-07.**

    13. `test_mod_seven_mod_three_is_one` — Push Y=7, X=3. Dispatch `Op::Mod`. Assert `state.stack.x.inner() == Decimal::from(1)` (7 mod 3 = 1; trivially positive). **Covers FN-MATH-06 happy path.**

    14. `test_mod_seven_mod_neg_three` — Push Y=7, X=-3. Dispatch `Op::Mod`. Assert `state.stack.x.inner() == Decimal::from(1)` (sign follows Y per D-14: `q = -2.333…`; `trunc(q) = -2`; `result = 7 - (-3)*(-2) = 7 - 6 = 1`). The result is +1 because Y is positive. **Covers FN-MATH-06 (REQUIREMENTS.md example #1), D-14.**

    15. `test_mod_neg_seven_mod_three` — Push Y=-7, X=3. Dispatch `Op::Mod`. Assert `state.stack.x.inner() == Decimal::from(-1)` (sign follows Y per D-14: `q = -2.333…`; `trunc(q) = -2`; `result = -7 - 3*(-2) = -7 + 6 = -1`). The result is -1 because Y is negative. **Covers FN-MATH-06 (REQUIREMENTS.md example #2), D-14.**

    16. `test_mod_div_by_zero` — Push Y=5, X=0. Dispatch `Op::Mod`. Assert returns `Err(HpError::Domain)` per D-14. **Covers FN-MATH-06 error path.**

    17. `test_frc_negative_three_seven_is_minus_zero_seven` — Push X = -3.7. Dispatch `Op::Frc`. Assert `state.stack.x.inner() == Decimal::from_str("-0.7").unwrap()`. **Covers FN-MATH-05, D-15.**

    18. `test_abs_negative_five_is_five` — Push X = -5. Dispatch `Op::Abs`. Assert `state.stack.x.inner() == Decimal::from(5)` and `state.stack.lastx.inner() == Decimal::from(-5)`. **Covers FN-MATH-07, D-16.**

    19. `test_sign_negative_zero_positive` — Three sub-cases: (a) X = -5 → SIGN → -1; (b) X = 0 → SIGN → 0; (c) X = 5 → SIGN → 1. **Covers FN-MATH-09, D-17.**

    20. `test_rup_mirrors_rdn` — Push T=4, Z=3, Y=2, X=1 (via four ENTERs or direct field assignment in test setup). Dispatch `Op::Rup`. Assert `state.stack.x.inner() == Decimal::from(4)`, `state.stack.y.inner() == Decimal::from(1)`, `state.stack.z.inner() == Decimal::from(2)`, `state.stack.t.inner() == Decimal::from(3)`. Also assert LASTX is unchanged from before (D-19). **Covers SC-4, FN-STACK-01, D-19, D-20.**

    Carry `#![allow(clippy::unwrap_used)]` at the file head (integration tests opt out of the deny gate per D-23). Use `use hp41_core::{CalcState, HpNum, HpError, AngleMode, DisplayMode}; use hp41_core::ops::{dispatch, Op}; use rust_decimal::Decimal; use std::str::FromStr;` — mirrors the import pattern of `hp41-core/tests/math_tests.rs`.
  </behavior>
  <action>
    Create `hp41-core/tests/phase20_math.rs` with ≥14 `#[test]` functions covering the test list in `<behavior>` (the list contains 20 tests; the minimum gate is 14). Use the integration-test pattern (file directly under `hp41-core/tests/`, NOT inside `#[cfg(test)] mod` in a source file — that's the precedent set by `math_tests.rs`, `stack_tests.rs`, etc.).

    Helpers (define at the top of the file):
    - `fn push_x(state: &mut CalcState, v: &str) { state.stack.x = HpNum::from(Decimal::from_str(v).unwrap()); state.stack.lift_enabled = true; }` — or push via dispatch(Op::PushNum(...)) if you want to exercise the public API more thoroughly. Prefer dispatch for behavior coverage.
    - `fn push_y_then_x(state: &mut CalcState, y: &str, x: &str)` — for binary ops, calls dispatch(Op::PushNum(y)) + dispatch(Op::Enter) + dispatch(Op::PushNum(x)).

    Use `assert!((a.inner() - b).abs() < tolerance)` for f64-bridge ops (PolarToRect/RectToPolar/Fact), where tolerance is `Decimal::from_str("0.0000001").unwrap()` (1e-7) — tighter than the 1e-9 used in some f64 assertions, because HpNum::rounded only keeps 10 sig digits.

    For exact-value ops (Frc, Abs, Sign, Mod with integer args, Rnd, Rup), use exact `==` comparison on `Decimal::from_str(...)` literals.

    **MOD semantic is locked per D-14 (UPDATED 2026-05-13):** Result = `Y - X * trunc(Y/X)` where `trunc` = truncate-toward-zero (= `HpNum::trunc_int()`). Sign follows Y. The three MOD tests (Tests 14, 15, 16 in the list above) are deterministic — no executor research required. The expected values are:
    - Test 14 `test_mod_seven_mod_neg_three`: Y=7, X=-3 → X=+1 (sign of Y).
    - Test 15 `test_mod_neg_seven_mod_three`: Y=-7, X=3 → X=-1 (sign of Y).
    - Test 16 `test_mod_div_by_zero`: Y=5, X=0 → returns `HpError::Domain`.

    The op_mod implementation in Task 4 already uses the corrected formula; do NOT switch op_mod to floor-based or to a different sign convention.
  </action>
  <acceptance_criteria>
    - File exists: `hp41-core/tests/phase20_math.rs` is present.
    - Source assertion: `grep -c '^#\[test\]' hp41-core/tests/phase20_math.rs` returns ≥ 14.
    - Source assertion: file contains test names matching all required tests above (`test_pi_pushes_`, `test_polar_to_rect_`, `test_rect_to_polar_`, `test_rnd_fix_one_`, `test_rnd_fix_zero_`, `test_fact_five_`, `test_fact_twenty_seven_`, `test_fact_twenty_eight_`, `test_fact_seventy_`, `test_fact_negative_`, `test_fact_non_integer_`, `test_mod_seven_mod_three`, `test_mod_seven_mod_neg_three`, `test_mod_neg_seven_mod_three`, `test_mod_div_by_zero`, `test_frc_`, `test_abs_`, `test_sign_`, `test_rup_`).
    - Test command: `just test -p hp41-core --test phase20_math` exits 0 — every test passes.
    - Test command: `just test -p hp41-core --test numerical_accuracy` exits 0 — still ≥ 490/500, no regression (Phase 20 does NOT add cases to this suite per CONTEXT Out-of-scope; Phase 27 owns extensions).
    - Test command: `just ci` exits 0 — full lint + test + coverage gate green.
    - Coverage assertion (non-regression from v2.1 baseline per CLAUDE.md §Quality Gates): `cargo llvm-cov clean --workspace && cargo llvm-cov --fail-under-lines 92.5 -p hp41-core` exits 0 — hp41-core line coverage ≥ 92.5%. This is stricter than the global `just coverage` floor of 80%; Phase 27 will raise the floor to 95%, but Phase 20 must not slip below the current 92.5% baseline. If coverage drops below 92.5%, add tests until it recovers (most likely in `tests/phase20_math.rs` itself, or by expanding the Task 4 op_* test coverage).
    - Behavior assertion: every test name matches a specific FN-MATH-XX / FN-STACK-01 requirement and a CONTEXT D-XX decision; the test body asserts the exact outcome named in that decision.
  </acceptance_criteria>
  <verify>
    <automated>just test -p hp41-core --test phase20_math &amp;&amp; just test -p hp41-core --test numerical_accuracy &amp;&amp; just ci &amp;&amp; cargo llvm-cov clean --workspace &amp;&amp; cargo llvm-cov --fail-under-lines 92.5 -p hp41-core</automated>
  </verify>
  <done>hp41-core/tests/phase20_math.rs covers all 4 ROADMAP success criteria + the FACT error-path matrix + per-op happy paths (incl. the three deterministic MOD tests per corrected D-14: 7 MOD -3 = 1, -7 MOD 3 = -1, MOD-by-zero = Domain); all tests pass; just ci is green; coverage ≥ 92.5% (non-regression from v2.1 baseline).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 20 touches **hp41-core only**. No new untrusted-input surface, no I/O, no parser, no network. The only mutable state crossing a boundary is `CalcState`, which is already a hp41-core-internal value (callers in `hp41-cli` and `hp41-gui` pass it through unchanged).

| Boundary | Description |
|----------|-------------|
| Programmer-supplied X register value → op_fact | Bounded numeric input; X is always `HpNum`, no string-typed surface. Magnitude cap enforced by D-05/D-06 pre-flight. |
| f64 round-trip in op_polar_to_rect / op_rect_to_polar / op_fact | NaN/Inf return values from `f64::sin/cos/sqrt/atan2`/factorial product are filtered by `Decimal::from_f64(...).ok_or(HpError::Overflow)?`. No panics propagate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `op_fact` magnitude limit | mitigate | Two-layer guard: D-06 pre-flight `if v > 69.0 return OutOfRange` + D-05 `Decimal::from_f64(...).ok_or(Overflow)?`. Resource-exhaustion vector (the only one in the phase): unbounded iterative product of factorials. Mitigated by hard cap at 69 in the pre-flight; loop is `for k in 1..=n` with n ≤ 69, so worst-case 69 multiplications — sub-microsecond. |
| T-20-02 | Denial of Service | `op_polar_to_rect` / `op_rect_to_polar` f64 round-trip | accept | NaN/Inf from `f64::sin`/`cos`/`sqrt`/`atan2` are caught by `Decimal::from_f64(...).ok_or(HpError::Overflow)?` on the result side. No panic path. Already-bounded HpNum input (≤ ~7.9e28) yields well-defined f64 output. The X/Y read side uses `.to_f64().expect("HpNum is always within f64 range")` per format.rs:161 precedent — this is panicking by construction but unreachable because HpNum is in-range by invariant; if it ever panicked it would indicate a corrupted HpNum constructed outside the `HpNum::rounded` discipline, which is itself a bug. |
| T-20-03 | Information Disclosure | None | n/a | Phase 20 operates on numeric values only. No PII, no secrets, no filesystem, no network. |
| T-20-04 | Repudiation | None | n/a | hp41-core is single-user, single-process. No audit trail required. |
| T-20-05 | Spoofing | None | n/a | No authentication surface. |
| T-20-06 | Elevation of Privilege | None | n/a | hp41-core has no privilege model. |

**No new attack surface introduced.** All failure modes are bounded by `HpError` (Overflow / Domain / OutOfRange / InvalidOp / DivideByZero); zero-panic policy (`#![deny(clippy::unwrap_used)]`) prevents process termination on any input that respects the HpNum-range invariant.
</threat_model>

<verification>
## Phase-Level Verification (the 4 ROADMAP Success Criteria)

These are the **whole-phase** acceptance gates, derived directly from `.planning/ROADMAP.md` §"Phase 20: Core Math & Conversions / Success Criteria":

| SC | Wording (ROADMAP) | Test command |
|----|--------------------|--------------|
| SC-1 | "typing the keystroke for `PI` followed by ENTER pushes 3.141592654 [editorial correction of ROADMAP wording per D-09] onto X and lifts the stack" | `cargo test --test phase20_math test_pi_pushes_ten_digit_rounded_and_lifts_stack` |
| SC-2 | "In DEG mode, entering `3 ENTER 4 ENTER 5` and pressing `R→P` produces magnitude ≈ 5 in Y and angle ≈ 53.13° in X; in RAD mode the same inputs return the angle in radians" | `cargo test --test phase20_math test_rect_to_polar_deg_mode_three_four_to_5_at_5313 test_rect_to_polar_rad_mode` |
| SC-3 | "`5.7 CHS RND` with FIX 1 active produces `-5.7`; with FIX 0 active produces `-6`; `FACT` with X=70 returns `HpError::OutOfRange`" | `cargo test --test phase20_math test_rnd_fix_one_keeps_one_decimal_for_negative_value test_rnd_fix_zero_rounds_to_integer_minus_six test_fact_seventy_returns_out_of_range` |
| SC-4 | "Starting from stack X=1 Y=2 Z=3 T=4, pressing `R↑` produces X=4 Y=1 Z=2 T=3 (mirror of `Rdn`)" | `cargo test --test phase20_math test_rup_mirrors_rdn` |
| SC-5 | "Every one of the 10 new `Op` variants appears in `dispatch()` (ops/mod.rs), `execute_op()` (ops/program.rs), and BOTH `prgm_display.rs` copies (hp41-cli + hp41-gui); compile-time exhaustive matches confirm coverage" | Compile-time enforcement via Rust exhaustive `match` — verified by `cargo build -p hp41-core` AND `cargo build -p hp41-cli` AND `cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml` all exiting 0. Mechanical proof: temporarily comment one arm in any of the 4 locations and observe a "non-exhaustive patterns" compile error. |

## Cross-Cutting Verification

| Gate | Check |
|------|-------|
| Numerical-accuracy non-regression (D-22 spirit) | `just test -p hp41-core --test numerical_accuracy` exits 0; ≥ 490/500 passing as before Phase 20. |
| Zero-panic gate (D-23) | `cargo clippy -p hp41-core -- -D warnings` exits 0; no `.unwrap()` outside `#[allow(clippy::unwrap_used)]` test modules. |
| SC-4 invariant (D-24) | `grep -rn 'fn op_\(add\|sub\|mul\|div\|sin\|cos\|tan\|sto\|rcl\|flush_entry\|format_hpnum\)' hp41-gui/src-tauri/src/` returns 0 matches. |
| LiftEffect contract (D-25) | Each new op_* function body either calls `unary_result(...)` (auto-Enable), `binary_result(...)` (auto-Enable), or explicitly `apply_lift_effect(state, LiftEffect::Enable|Neutral)`. PI=Enable (via enter_number + explicit Enable), P→R / R→P=Enable (direct assignment + explicit Enable), RND/FRC/ABS/SIGN/FACT=Enable (via unary_result), MOD=Enable (via binary_result), R↑=Neutral (via apply_lift_effect Neutral). |
| Coverage gate — global floor (CLAUDE.md §Quality Gates) | `just coverage` exits 0 (the recipe uses `--fail-under-lines 80`). |
| Coverage gate — non-regression (Phase 20 stricter target per CLAUDE.md §Quality Gates v1.1/v2.0 column) | `cargo llvm-cov clean --workspace && cargo llvm-cov --fail-under-lines 92.5 -p hp41-core` exits 0 — line coverage ≥ 92.5% (the v2.1 baseline). Phase 20 must not slip below this; Phase 27 will raise the floor to 95%. |
| Full CI gate | `just ci` exits 0 (lint → test → coverage). |
| GUI CI gate | `just gui-ci` exits 0 (TypeScript type-check, Rust tests, release build for hp41-gui — relevant because prgm_display.rs in hp41-gui is touched). |

## ROADMAP Editorial Correction Pending

Per **D-09**, ROADMAP.md SC-1 reads "pushes 3.1415926536 (10-digit rounded)" — that string is **11** sig digits, not 10. The actual HP-41 hardware value at 10 sig digits is **3.141592654**. Phase 20 implements 3.141592654 per **D-08**. The ROADMAP wording will be corrected as a separate small commit during Phase 20's wrap-up (mention this in the phase SUMMARY).
</verification>

<success_criteria>
Phase 20 is complete when ALL of the following are true:

1. **All 10 new Op variants exist** in `hp41-core/src/ops/mod.rs::Op` (compile-time check).
2. **All 10 new dispatch arms exist** in `hp41-core/src/ops/mod.rs::dispatch()` (compile-time check — exhaustive match).
3. **All 10 new execute_op arms exist** in `hp41-core/src/ops/program.rs::execute_op()` (compile-time check — exhaustive match).
4. **All 10 new op_display_name arms exist in BOTH** `hp41-cli/src/prgm_display.rs` AND `hp41-gui/src-tauri/src/prgm_display.rs` (compile-time check on both crates).
5. **`round_to_display_precision()` helper exists** in `hp41-core/src/format.rs` and `format_hpnum()` continues to work (no display-test regression).
6. **`HpError::OutOfRange` variant exists** in `hp41-core/src/error.rs`.
7. **`just test` passes** — every test suite green, including the new `tests/phase20_math.rs` with ≥ 14 tests (the test plan lists 20).
8. **`just lint` passes** — `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
9. **`just coverage` passes** — `hp41-core` line coverage ≥ 80% (the global floor).
10. **Coverage non-regression passes** — `cargo llvm-cov --fail-under-lines 92.5 -p hp41-core` exits 0 (Phase 20 stricter target; Phase 27 will raise to 95%).
11. **`just gui-ci` passes** — hp41-gui still type-checks and builds (the prgm_display.rs change compiles cleanly there too).
12. **500-case numerical accuracy suite** still reports ≥ 490/500 passing (no regression; Phase 27 extends; Phase 20 only preserves).
13. **SC-4 invariant grep** under `hp41-gui/src-tauri/src/` returns zero `fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)` matches.
14. **All 4 ROADMAP success criteria** have at least one passing test in `hp41-core/tests/phase20_math.rs`.
15. **MOD semantic per corrected D-14** is verified: `7 MOD -3 = 1`, `-7 MOD 3 = -1`, `5 MOD 0 = Err(Domain)` — three dedicated tests assert this.
16. **SUMMARY.md committed** (see `<output>`).
17. **ROADMAP editorial correction** filed: SC-1 wording updated from "3.1415926536" to "3.141592654" per D-09 (small companion commit or noted in SUMMARY for the next milestone-state update).
</success_criteria>

<output>
After completion, create `.planning/phases/20-core-math-and-conversions/20-01-SUMMARY.md` covering:

- **Plan:** 20-01 (single-plan-per-phase per D-21)
- **Status:** Complete | Partial | Blocked
- **Files touched:** the 9 in `files_modified` (including the new `hp41-core/tests/phase20_math.rs`)
- **What landed:** Wave-0 RND helper extraction, Wave-1 Op-variant+dispatch+execute_op+impl+prgm_display, Wave-2 integration tests, HpError::OutOfRange new variant
- **Decisions honored:** D-01 through D-25 — note that D-14 was UPDATED 2026-05-13 to "sign follows Y" via trunc-toward-zero (REQUIREMENTS.md FN-MATH-06 updated in parallel); op_mod implementation follows the corrected formula `Y - X * trunc(Y/X)` using `HpNum::trunc_int()` for the truncation
- **Test results:** count of new tests, pass/fail breakdown of `just ci` and `just gui-ci`, coverage % (target ≥ 92.5%)
- **Editorial corrections filed:** ROADMAP SC-1 wording fix per D-09
- **CLAUDE.md additions:** any new "v2.2 additions" block content for Phase 20 (SIGN-on-ALPHA divergence note per D-18, MOD trunc-toward-zero convention note citing D-14 + Free42 source, RND helper documented)
- **Out-of-scope, deferred:** SIGN-on-ALPHA typing (Phase 23 candidate), numerical-accuracy suite extension (Phase 27), CLI keyboard wiring (Phase 25), GUI key_map (Phase 26)
- **Followups for Phases 25/26:** the 10 new Op variants are awaiting `key_to_op` (Phase 25) and `key_map::resolve` + `KEY_DEFS` (Phase 26) wiring — flag this in the SUMMARY so the next phase pickup is unambiguous.
</output>
