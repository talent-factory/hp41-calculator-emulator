---
phase: 04-tui-and-input
plan: 05
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - hp41-cli/src/app.rs
  - hp41-cli/src/ui.rs
  - hp41-cli/src/keys.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-cli/src/main.rs
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - INPUT-01

must_haves:
  truths:
    - "`just ci` exits 0 — all workspace tests pass including hp41-core and hp41-cli"
    - "hp41-core coverage gate (≥80%) still passes after Phase 4 additions"
    - "No regression in hp41-core test suite from Phase 4 changes"
  artifacts:
    - path: "hp41-cli/src/tests/keys_tests.rs"
      provides: "All keys unit tests pass under just ci"
      contains: "key_to_op"
    - path: "hp41-cli/src/tests/prgm_display_tests.rs"
      provides: "All prgm_display unit tests pass under just ci"
      contains: "format_step"
  key_links:
    - from: "just ci"
      to: "cargo-llvm-cov coverage gate"
      via: "just ci runs clippy + test + coverage"
      pattern: "just ci"
---

<objective>
Run `just ci` to confirm the complete workspace passes all quality gates: lint (clippy),
tests (hp41-core + hp41-cli), and the ≥80% coverage gate on hp41-core. Fix any
compilation warnings that are treated as errors by clippy.

Purpose: Phase 4 is not complete until `just ci` is green. This plan owns the final gate.
No new source files are created here — only fixes to warnings/lint issues uncovered by CI.

Output: A green `just ci` run across the full workspace with all Phase 4 tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-tui-and-input/04-CONTEXT.md
@.planning/phases/04-tui-and-input/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run just ci — fix any lint/warning issues, confirm green</name>
  <read_first>
    - Justfile (to understand what `just ci` runs — read the ci recipe)
    - hp41-cli/src/app.rs (most likely source of unused import warnings)
    - hp41-cli/src/ui.rs (may have unused variable warnings from stub evolution)
    - hp41-cli/src/keys.rs (may have unused _app warning)
    - hp41-cli/src/prgm_display.rs (check for unused imports)
  </read_first>
  <files>
    hp41-cli/src/app.rs,
    hp41-cli/src/ui.rs,
    hp41-cli/src/keys.rs,
    hp41-cli/src/prgm_display.rs,
    hp41-cli/src/main.rs
  </files>
  <action>
Run `just ci` and fix any issues it reports. Common issues to expect and how to fix them:

**1. Clippy lint: `unused import`**
Remove any `use` statements that are not referenced in the file.

**2. Clippy lint: `dead_code` on _app parameter in key_to_op**
The `_app: &App` parameter has a leading underscore — clippy should accept it. If it
complains, rename to `_app` if not already done.

**3. Clippy lint: `needless_return`**
Remove explicit `return` statements where the expression is already the last one.

**4. Clippy lint: `match_single_binding`**
If any single-arm match can be replaced with `let`, do so.

**5. Coverage gate still at ≥80% for hp41-core**
Phase 4 adds zero hp41-core code, so coverage should be unchanged. If it drops, check
whether any new test file accidentally included `#[cfg(not(test))]` guards on core code.

**6. `just ci` recipe** (read from Justfile before assuming):
Typical recipe pattern from Phase 1–3:
```
ci:
    cargo clippy --all-targets -- -D warnings
    cargo test --workspace
    cargo llvm-cov --package hp41-core --fail-under-lines 80
```

Run the full sequence:
```bash
just ci
```

If `just ci` passes on first run with no edits needed, that is acceptable — record the
output in the SUMMARY.

If any errors appear, fix the minimum needed to make `just ci` green. Do NOT suppress
warnings with `#[allow(dead_code)]` — fix or remove the unused code instead.

After fixing, run `just ci` again to confirm green.
  </action>
  <verify>
    <automated>just ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `just ci` exits 0
    - Output contains "test result: ok" for both hp41-cli and hp41-core test suites
    - Output does not contain "FAILED" for any test
    - Coverage gate line appears (e.g., "Lines: XX.XX% (>= 80.00%)") and shows PASS
    - No `error[` lines in clippy output
  </acceptance_criteria>
  <done>`just ci` exits 0 across the full workspace. All tests pass. Coverage gate ≥80% for hp41-core. Zero clippy errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate | Enforces no-panic, no-clippy-error, coverage invariants for the workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | clippy -D warnings enforces no silent errors | accept | This is the intended gate; any suppressed warning would require explicit `#[allow]` with justification |
</threat_model>

<verification>
```bash
just ci
```

Expected final lines:
```
test result: ok. N passed; 0 failed
...
Lines: XX.XX% (>= 80.00%) ... PASS
```

Exit code must be 0. If not 0, fix issues and re-run.
</verification>

<success_criteria>
1. `just ci` exits 0 with no errors
2. All hp41-cli unit tests pass (keys_tests + prgm_display_tests)
3. All hp41-core tests still pass (no regression from Phase 4)
4. hp41-core line coverage is ≥80% (gate passed)
5. Zero clippy errors across the full workspace
</success_criteria>

<output>
After completion, create `.planning/phases/04-tui-and-input/04-05-SUMMARY.md`
</output>
