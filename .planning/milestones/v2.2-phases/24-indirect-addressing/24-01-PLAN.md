---
phase: 24-indirect-addressing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/ops/indirect.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/phase22_program_control.rs
  - hp41-core/tests/phase24_resolve_indirect.rs
autonomous: true
requirements:
  - FN-IND-01
  - FN-IND-02
tags:
  - rust
  - hp41-core
  - refactor
  - indirect-addressing

must_haves:
  truths:
    - "`hp41-core` exposes `pub fn resolve_indirect(state: &CalcState, reg: u8) -> Result<u8, HpError>` and `pub(crate) fn resolve_indirect_decimal(state: &CalcState, reg: u8) -> Result<Decimal, HpError>` from `ops::indirect`"
    - "Calling `resolve_indirect` with R05=42 returns `Ok(42)`; with R05=12.345 returns `Err(InvalidOp)`; with R05=300 (>u8) returns `Err(InvalidOp)`; with R05=2^64 (>i64) returns `Err(InvalidOp)`; with reg=200 (out of regs.len()) returns `Err(InvalidOp)`; with R05=-3 returns `Err(InvalidOp)` via the u8::try_from arm"
    - "`Op::GtoInd` and `Op::XeqInd` arms in `run_loop` no longer contain inline `state.regs.get(...).ok_or(InvalidOp)?.clone(); pointer.trunc_int(); if int_part != pointer { return Err(InvalidOp); }` — they call `resolve_indirect_decimal` instead"
    - "`Op::XeqInd`'s `if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }` pre-mutation guard remains the FIRST executable line of the arm (before any pointer read)"
    - "All 5 pre-existing Phase-22 GTO/XEQ-IND tests in `hp41-core/tests/phase22_program_control.rs` continue to pass byte-for-byte"
    - "`just test -p hp41-core` passes after the refactor"
  artifacts:
    - path: "hp41-core/src/ops/indirect.rs"
      provides: "Two-tier resolver helpers (resolve_indirect_decimal private, resolve_indirect public) + inline #[cfg(test)] mod tests with 7 unit tests"
      min_lines: 100
      contains: "pub fn resolve_indirect"
    - path: "hp41-core/tests/phase24_resolve_indirect.rs"
      provides: "Phase-24 integration test file (currently scaffold for future Phase-24 cross-cutting helper assertions; 24-01 holds the GtoInd/XeqInd regression sentinels in phase22_program_control.rs per RESEARCH §Sentinel Test Design)"
      min_lines: 20
    - path: "hp41-core/src/ops/mod.rs"
      provides: "`pub mod indirect;` declaration alongside other ops/* sibling modules"
      contains: "pub mod indirect"
    - path: "hp41-core/src/ops/program.rs"
      provides: "Refactored Op::GtoInd and Op::XeqInd run_loop arms calling resolve_indirect_decimal"
      contains: "resolve_indirect_decimal"
    - path: "hp41-core/tests/phase22_program_control.rs"
      provides: "4 new Phase-24 sentinel tests appended in a `// ── Phase 24 D-24.5 sentinel ─────` section"
      contains: "phase24_xeq_ind_call_depth_guard_runs_before_pointer_read"
  key_links:
    - from: "hp41-core/src/ops/program.rs::run_loop Op::GtoInd / Op::XeqInd arms"
      to: "hp41-core/src/ops/indirect.rs::resolve_indirect_decimal"
      via: "qualified call `crate::ops::indirect::resolve_indirect_decimal(state, *reg)?`"
      pattern: "crate::ops::indirect::resolve_indirect_decimal"
    - from: "hp41-core/src/ops/indirect.rs::resolve_indirect"
      to: "hp41-core/src/ops/indirect.rs::resolve_indirect_decimal"
      via: "internal call — single source of pointer-validation truth (D-24.1)"
      pattern: "resolve_indirect_decimal"
    - from: "hp41-core/tests/phase22_program_control.rs sentinel tests"
      to: "Phase-22 Op::GtoInd / Op::XeqInd run_loop arms (post-refactor)"
      via: "run_program / resume_program drivers"
      pattern: "run_program|resume_program"
---

<objective>
Phase 24 Plan 01 — Foundation. Create the two-tier indirect-resolution helper (`resolve_indirect_decimal` private inner + `resolve_indirect` public u8-wrapper) in a new `hp41-core/src/ops/indirect.rs` sibling module, then refactor the existing Phase-22 `Op::GtoInd` / `Op::XeqInd` inline trunc-and-validate blocks in `run_loop` onto the new inner helper. Lock down behavior with 7 inline unit tests in `indirect.rs` plus 4 sentinel regression tests appended to `hp41-core/tests/phase22_program_control.rs` proving the refactor is byte-for-byte equivalent (especially the XeqInd pre-mutation call-depth guard, per D-22.15 / D-24.5).

Purpose: Extract the single source of pointer-validation truth (D-24.1) so Plan 24-02's ~11 new `*Ind` Op variants can delegate to one consolidated helper. Refactor without behavior change is the precondition for adding new variants.

Output: New helper module + module wiring + refactored Phase-22 arms + 7 inline unit tests + 4 sentinel regression tests + new (currently nearly-empty) integration file `phase24_resolve_indirect.rs` reserved for future Phase-24 cross-cutting helper assertions per VALIDATION.md row "Wave 1 (24-01)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-indirect-addressing/24-CONTEXT.md
@.planning/phases/24-indirect-addressing/24-RESEARCH.md
@.planning/phases/24-indirect-addressing/24-PATTERNS.md
@.planning/phases/24-indirect-addressing/24-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from CONTEXT.md / RESEARCH.md / existing source. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From hp41-core/src/error.rs (existing):
  pub enum HpError { InvalidOp, CallDepth, OutOfRange, ... }   // InvalidOp + CallDepth are the only variants Phase 24 returns

From hp41-core/src/state.rs (existing):
  pub struct CalcState { pub regs: Vec<HpNum>, pub flags: u64, pub stack: Stack, pub program: Vec<Op>, pub pc: usize, pub call_stack: Vec<usize>, ... }
  CalcState::new() ships 100 regs by default (verified at hp41-core/tests/phase22_program_control.rs:264)

From hp41-core/src/num.rs (existing):
  pub struct HpNum(Decimal);
  impl HpNum {
      pub fn from(_: i32) -> HpNum;                  // integer constructor used in tests
      pub fn rounded(_: Decimal) -> HpNum;           // 10-digit-rounded constructor
      pub fn inner(&self) -> Decimal;                // unwrap to rust_decimal::Decimal
      pub fn trunc_int(&self) -> HpNum;              // truncate to integer (toward zero)
      pub fn zero() -> HpNum;
  }
  // Existing import in num.rs: `use rust_decimal::prelude::ToPrimitive;` — confirms Decimal::to_i64 is reachable

From rust_decimal 1.42 (workspace dep):
  Decimal::to_i64(&self) -> Option<i64>           // via ToPrimitive
  u8::try_from(i64) -> Result<u8, TryFromIntError> // std

Phase 24 NEW contracts (this plan creates):
  // hp41-core/src/ops/indirect.rs
  pub(crate) fn resolve_indirect_decimal(state: &CalcState, reg: u8) -> Result<Decimal, HpError>;
  pub fn resolve_indirect(state: &CalcState, reg: u8) -> Result<u8, HpError>;

  // hp41-core/src/ops/mod.rs additions
  pub mod indirect;   // sibling-module declaration alongside `pub mod flags;`

Phase 22 inline blocks being refactored (verbatim from hp41-core/src/ops/program.rs lines 466-517 — the executor MUST read these before editing):
  // BEFORE — Op::GtoInd(reg) arm with 9-line inline trunc-and-validate
  // BEFORE — Op::XeqInd(reg) arm with 9-line inline trunc-and-validate
  //          (preceded by `if state.call_stack.len() >= 4 { return Err(HpError::CallDepth); }`)
  // AFTER  — both arms call `let i = crate::ops::indirect::resolve_indirect_decimal(state, *reg)?;`
  //          followed by `let label_str = i.to_string();` then the EXISTING find_in_program / pc-advance / call_stack-push lines
  // CRITICAL: the XeqInd `call_stack.len() >= 4 → CallDepth` guard MUST stay as the FIRST line of the arm.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create indirect.rs helper module with two-tier resolver and inline unit tests</name>
  <files>hp41-core/src/ops/indirect.rs (new), hp41-core/src/ops/mod.rs (add `pub mod indirect;`)</files>
  <read_first>
    - hp41-core/src/ops/flags.rs (analog: sibling helper module — exact-match shape per PATTERNS.md §indirect.rs-analog; mirror imports, free-helper section, op-fn section, inline `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests` block at the bottom)
    - hp41-core/src/ops/mod.rs lines 1-25 (existing `pub mod alpha; pub mod arithmetic; pub mod cardreader_ops; pub mod display_ops; pub mod flags; ...` declaration block — Plan adds `pub mod indirect;` here, alphabetical order suggests after `hms` and before `math`, OR at end alongside the most recently added Phase-23 module if any was added there)
    - hp41-core/src/ops/program.rs lines 466-517 (the inline `Op::GtoInd` and `Op::XeqInd` blocks — informs the exact `state.regs.get(reg as usize).ok_or(HpError::InvalidOp)?.clone(); int_part = pointer.trunc_int(); if int_part != pointer { return Err(HpError::InvalidOp); }` pattern that the inner helper consolidates)
    - hp41-core/src/num.rs (HpNum::trunc_int signature; verify `pub fn inner(&self) -> Decimal`; verify the `use rust_decimal::prelude::ToPrimitive;` line at file head)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Inner helper (full body)" lines 575-760 (full reference implementation including the 7 inline unit tests — copy verbatim)
    - .planning/phases/24-indirect-addressing/24-CONTEXT.md §D-24.1, §D-24.2, §D-24.3, §D-24.4 (locked decisions for two-tier shape, public signature, bounds-responsibility-is-caller, sidecar-inheritance-via-delegation)
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"hp41-core/src/ops/indirect.rs (NEW; helper module + delegation shims)" (full pattern map for module header, free-helper, op-fn, and inline test sections)
  </read_first>
  <behavior>
    - resolve_indirect_decimal(state, reg=5) where state.regs[5]=HpNum::from(42i32) returns Ok(Decimal::from(42))
    - resolve_indirect_decimal(state, reg=5) where state.regs[5]=HpNum::rounded(Decimal::from_str("12.345").unwrap()) returns Err(HpError::InvalidOp)
    - resolve_indirect_decimal(state, reg=200) on default-100-regs CalcState returns Err(HpError::InvalidOp)
    - resolve_indirect_decimal preserves sign — for state.regs[5]=HpNum::from(-3i32), returns Ok(Decimal) such that the returned value's `.to_string()` == "-3" (proves no sign-stripping for GTO/XEQ-IND callers, per Pitfall 2)
    - resolve_indirect(state, reg=5) where state.regs[5]=HpNum::from(42i32) returns Ok(42u8)
    - resolve_indirect(state, reg=5) where state.regs[5]=HpNum::from(300i32) returns Err(HpError::InvalidOp) via the u8::try_from arm
    - resolve_indirect(state, reg=5) where state.regs[5]=HpNum::rounded(Decimal::from_str("18446744073709551616").unwrap()) returns Err(HpError::InvalidOp) via the to_i64().ok_or arm (Pitfall 3 — 2^64 exceeds i64::MAX)
    - resolve_indirect(state, reg=5) where state.regs[5]=HpNum::from(-3i32) returns Err(HpError::InvalidOp) via the u8::try_from arm (negative integer doesn't fit u8)
    - resolve_indirect(state, reg=200) on default-100-regs CalcState returns Err(HpError::InvalidOp) via the inner helper's .get().ok_or arm
  </behavior>
  <action>
    Create `hp41-core/src/ops/indirect.rs` with the following structure (copy verbatim from RESEARCH.md §"Inner helper (full body)" lines 575-760):

    1. Module header: `//! Phase 24 indirect-addressing helpers and op shims.` followed by an explanatory paragraph naming D-24.1 (two-tier resolver) and D-24.4 (delegation inheritance). Note that this plan delivers ONLY the two helpers + inline tests; the 11 op_*_ind shim functions land in plan 24-02.
    2. Imports: `use crate::error::HpError;`, `use crate::state::CalcState;`, `use rust_decimal::prelude::ToPrimitive;`, `use rust_decimal::Decimal;`. (Do NOT import the direct ops yet — those are needed only when 24-02 adds the shims.)
    3. Helper #1 (private to crate): `pub(crate) fn resolve_indirect_decimal(state: &CalcState, reg: u8) -> Result<Decimal, HpError>` — body uses `state.regs.get(reg as usize).ok_or(HpError::InvalidOp)?.clone()` then `pointer.trunc_int()` then equality check `if int_part != pointer { return Err(HpError::InvalidOp); }` then `Ok(int_part.inner())`. Add rustdoc per RESEARCH lines 195-204 naming "single source of pointer-validation truth" and listing the two failure modes.
    4. Helper #2 (public): `pub fn resolve_indirect(state: &CalcState, reg: u8) -> Result<u8, HpError>` — body is the locked D-24.2 chain: `let i = resolve_indirect_decimal(state, reg)?; let as_i64 = i.to_i64().ok_or(HpError::InvalidOp)?; u8::try_from(as_i64).map_err(|_| HpError::InvalidOp)`. Add rustdoc per RESEARCH lines 221-230 naming the two cascading rejection paths and the D-24.3 "bounds-is-caller" rule.
    5. Inline `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests { ... }` block at the bottom containing 7 unit tests verbatim from RESEARCH.md §"Inner helper (full body)" lines 680-759:
       - `resolve_indirect_happy_integer_pointer`
       - `resolve_indirect_non_integer_rejects`
       - `resolve_indirect_reg_out_of_range_rejects`
       - `resolve_indirect_pointer_exceeds_u8_range_rejects`
       - `resolve_indirect_pointer_exceeds_i64_range_rejects`
       - `resolve_indirect_negative_integer_pointer_rejects_via_u8`
       - `resolve_indirect_decimal_preserves_sign_for_gto_ind_callers`
       Test imports: `use super::*; use crate::num::HpNum; use rust_decimal::Decimal; use std::str::FromStr;`. Mirror the inline-test pattern from `hp41-core/src/ops/flags.rs` lines 59-92.

    Then add `pub mod indirect;` to `hp41-core/src/ops/mod.rs` in the existing module-declaration block (lines 9-21 region — see current file for exact ordering; alphabetical insertion between `hms` and `math` is preferred, but appending at the end of the block is also acceptable).

    Anti-patterns to avoid (per RESEARCH §"Anti-Patterns"):
    - Do NOT push bounds checks against `regs.len()` or `< 56` into `resolve_indirect` — D-24.3 forbids it.
    - Do NOT introduce a new `is_integer_pointer()` method on `HpNum` — inline `int_part != pointer` matches the existing Phase-22 idiom.
    - Do NOT use `as u8` for the conversion — use `u8::try_from(...).map_err(|_| InvalidOp)` per D-24.2.
    - Do NOT use `.unwrap()` anywhere in production code; the resolver path uses `?` only. Test modules use `.unwrap()` freely under the `#[allow(clippy::unwrap_used)]` mod attribute.

    The file lives in `hp41-core/src/ops/`, not `hp41-core/src/`. The 11 `op_*_ind` shim functions are NOT added in this task — they belong to plan 24-02.
  </action>
  <verify>
    <automated>cargo build -p hp41-core 2>&1 | tail -20 && cargo test -p hp41-core --lib indirect::tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `hp41-core/src/ops/indirect.rs` exists.
    - `grep -c "pub fn resolve_indirect" hp41-core/src/ops/indirect.rs` is at least 1 (the public helper signature is present).
    - `grep -c "pub(crate) fn resolve_indirect_decimal" hp41-core/src/ops/indirect.rs` is at least 1 (the private inner helper is present).
    - `grep -c "pub mod indirect" hp41-core/src/ops/mod.rs` is at least 1 (module wiring is in place).
    - `cargo build -p hp41-core` exits 0 (the new module compiles and is wired into mod.rs).
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_happy_integer_pointer` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_non_integer_rejects` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_reg_out_of_range_rejects` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_pointer_exceeds_u8_range_rejects` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_pointer_exceeds_i64_range_rejects` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_negative_integer_pointer_rejects_via_u8` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests::resolve_indirect_decimal_preserves_sign_for_gto_ind_callers` exits 0.
    - `cargo test -p hp41-core --lib indirect::tests` reports "7 passed" (all 7 unit tests run).
    - `grep -E '\.unwrap\(\)|\bas u8\b' hp41-core/src/ops/indirect.rs | grep -v '^\s*//' | grep -vc 'mod tests'` returns 0 outside the `mod tests` block (zero-panic invariant: no .unwrap() and no `as u8` in production code).
    - `cargo clippy -p hp41-core --lib -- -D warnings` exits 0 (no new clippy warnings; `#![deny(clippy::unwrap_used)]` does not fire because production code is unwrap-free).
  </acceptance_criteria>
  <done>
    The two-tier resolver lives in `hp41-core/src/ops/indirect.rs` with `pub mod indirect;` wired into `ops/mod.rs`. All 7 inline unit tests pass. Production code is `.unwrap()`-free and `as u8`-free. `cargo build -p hp41-core` and `cargo clippy -p hp41-core` are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Phase-22 Op::GtoInd / Op::XeqInd run_loop arms onto resolve_indirect_decimal + add 4 D-24.5 sentinel tests</name>
  <files>hp41-core/src/ops/program.rs (refactor lines ~466-517), hp41-core/tests/phase22_program_control.rs (append 4 new sentinel tests in a `// ── Phase 24 D-24.5 sentinel ─────` section), hp41-core/tests/phase24_resolve_indirect.rs (new placeholder file with module header, reserved per VALIDATION.md "Wave 1 (24-01)")</files>
  <read_first>
    - hp41-core/src/ops/program.rs lines 460-525 (the EXISTING `Op::GtoInd(reg)` block at ~474-487 and `Op::XeqInd(reg)` block at ~500-517 — the executor MUST read the current state before editing; line numbers may have shifted slightly since the research snapshot — use `grep -n "Op::GtoInd\|Op::XeqInd" hp41-core/src/ops/program.rs` to locate the exact arms)
    - hp41-core/src/ops/indirect.rs (the helper landed in Task 1 — confirm `pub(crate) fn resolve_indirect_decimal` is callable via `crate::ops::indirect::resolve_indirect_decimal`)
    - hp41-core/tests/phase22_program_control.rs (the 5 existing GTO/XEQ-IND tests at ~lines 222-350 — analog for the 4 new sentinel tests; mirror the `#![allow(clippy::unwrap_used)]` crate-level attribute, the `use hp41_core::ops::program::{resume_program, run_program};` import block, the section-divider comment style, and the `state.regs[n] = HpNum::from(...)` setup pattern)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Phase-22 Refactor Diff Sketch (D-24.5)" lines 300-385 (BEFORE/AFTER diffs for both arms — copy the AFTER blocks verbatim)
    - .planning/phases/24-indirect-addressing/24-RESEARCH.md §"Sentinel Regression Test Design (D-24.5)" lines 810-835 (4 sentinel tests with exact names, inputs, and expected outcomes)
    - .planning/phases/24-indirect-addressing/24-PATTERNS.md §"hp41-core/tests/phase24_resolve_indirect.rs (NEW)" lines 119-220 (analog patterns from `phase22_program_control.rs` — header imports, GTO IND happy-path skeleton, non-integer-rejection skeleton, the resume_program-driven 4-deep call-stack atomicity skeleton verbatim from lines 302-325 of the analog file)
    - .planning/phases/24-indirect-addressing/24-CONTEXT.md §D-24.5 (Phase-22 refactor decision; preserves all `find_in_program` + `state.pc = target + 1` + call_stack-push semantics)
    - .planning/phases/24-indirect-addressing/24-VALIDATION.md (confirms `phase24_resolve_indirect.rs` is created in Wave 1 / 24-01 even though most of its eventual content lives in plan 24-02)
  </read_first>
  <behavior>
    - phase24_gto_ind_uses_shared_helper: with state.regs[5]=HpNum::from(42i32) and program `[Lbl "A", GtoInd(5), PushNum(HpNum::from(111i32)), Lbl "42", PushNum(HpNum::from(7i32))]`, `run_program(&mut state, "A").unwrap()` leaves state.stack.x == HpNum::from(7i32). (Sanity — proves the refactored arm still routes to find_in_program.)
    - phase24_xeq_ind_uses_shared_helper: with state.regs[3]=HpNum::from(10i32) and a program with XeqInd(3) jumping to LBL "10", PushNum 99, Rtn, then continuing with PushNum 2 after the call returns; assert state.stack.x == HpNum::from(2i32) after RTN unwinding. (Sanity for XeqInd refactor.)
    - phase24_xeq_ind_call_depth_guard_runs_before_pointer_read: drive via `resume_program` with state.regs[3]=HpNum::rounded(Decimal::from_str("12.345").unwrap()) (NON-INTEGER pointer), state.call_stack pre-filled to `vec![999usize; 4]`, state.pc=0, program=`[XeqInd(3), Lbl "12".to_string()]`. Assert `result == Err(HpError::CallDepth)` (NOT `Err(HpError::InvalidOp)`) — this proves the pre-mutation atomicity guard runs FIRST, before any pointer read. Also assert state.call_stack.len() == 4 (no push happened).
    - phase24_gto_ind_negative_pointer_stringifies_with_sign: with state.regs[5]=HpNum::from(-3i32), program=`[Lbl "A", GtoInd(5)]` (no LBL "-3"), assert `run_program(&mut state, "A")` returns `Err(HpError::InvalidOp)` (the error originates from `find_in_program("-3")` failing — NOT from non-integer rejection, because -3 IS an integer). Pitfall 2 sentinel.
    - All 5 pre-existing Phase-22 GTO/XEQ-IND tests in phase22_program_control.rs (test_gto_ind_happy, test_gto_ind_non_integer_rejects, test_xeq_ind_happy, test_xeq_ind_non_integer_rejects, test_xeq_ind_4_deep_call_stack_rejects — exact names verified by `grep -n "fn test_gto_ind\|fn test_xeq_ind" hp41-core/tests/phase22_program_control.rs`) continue to pass after the refactor.
  </behavior>
  <action>
    Step A — Refactor `hp41-core/src/ops/program.rs`:

    1. Locate the `Op::GtoInd(reg) => { ... }` arm in `run_loop` (currently at ~lines 474-487, body containing `state.regs.get(reg as usize).ok_or(HpError::InvalidOp)?.clone(); ... pointer.trunc_int(); ... if int_part != pointer { return Err(HpError::InvalidOp); } ... let label_str = int_part.inner().to_string();`). Replace the body (everything between the `Op::GtoInd(reg) => {` opener and the `}` closer) with EXACTLY this structure (per RESEARCH §"After (program.rs:474-481 — `Op::GtoInd`)" lines 326-334):
       - `// Phase 24 (D-24.5): pointer-validation logic now lives in the shared` (comment line)
       - `// `resolve_indirect_decimal` helper — single source of truth across all` (comment line)
       - `// ~14 indirect-resolving callers (this + Op::XeqInd + 12 Op::*Ind variants).` (comment line)
       - `let i = crate::ops::indirect::resolve_indirect_decimal(state, *reg)?;`
       - `let label_str = i.to_string();`
       - `let target = find_in_program(program, &label_str)?;`
       - `state.pc = target + 1; // mirrors Op::Gto: pc → step AFTER LBL marker`

    2. Locate the `Op::XeqInd(reg) => { ... }` arm (currently at ~lines 500-517). Replace its body with EXACTLY this structure (per RESEARCH §"After (program.rs:500-510 — `Op::XeqInd`)" lines 363-378), preserving the call-depth guard as the FIRST line:
       - `// Pre-mutation atomicity guard (D-22.15) — UNCHANGED, must run before` (comment line)
       - `// any pointer read.` (comment line)
       - `if state.call_stack.len() >= 4 {`
       - `    return Err(HpError::CallDepth);`
       - `}`
       - `// Phase 24 (D-24.5): shared pointer-validation helper. The label-lookup` (comment line)
       - `// path (find_in_program + call_stack.push + pc advance) is NOT modified —` (comment line)
       - `// GTO/XEQ-IND resolve to LABELS, not register addresses, so the inner` (comment line)
       - `// Decimal-returning helper (not the u8 wrapper) is the right consumer.` (comment line)
       - `let i = crate::ops::indirect::resolve_indirect_decimal(state, *reg)?;`
       - `let label_str = i.to_string();`
       - `let target = find_in_program(program, &label_str)?;`
       - `state.call_stack.push(state.pc);`
       - `state.pc = target + 1;`

    Invariants preserved by the refactor (per RESEARCH lines 381-387):
    - `find_in_program(program, &label_str)` call signature, error path, and `state.pc = target + 1` advance UNCHANGED.
    - `call_stack.len() >= 4 → CallDepth` runs BEFORE pointer read in the XeqInd arm (D-22.15 atomicity).
    - `int_part.inner().to_string()` becomes `i.to_string()` because `i` is already a `Decimal` (the helper returns `int_part.inner()`).
    - No `builtin_card_op` fallback in either arm (existing comment at program.rs:497-499 confirms this is intentional).
    - The `Op::GtoInd(_) | Op::XeqInd(_) => Err(HpError::InvalidOp)` catch-all in `dispatch()` (mod.rs:780) and the `Op::GtoInd(_) | Op::XeqInd(_)` entries in the programming-ops `|`-pattern at program.rs:825-840 are UNCHANGED in this task.

    Step B — Append 4 sentinel tests to `hp41-core/tests/phase22_program_control.rs`:

    1. At the bottom of the file, append a section divider comment block per RESEARCH §"Test header" lines 826-836:
       - `// ── Phase 24 D-24.5 sentinel: refactored GtoInd/XeqInd onto shared helper ────`
       - `//`
       - `// These tests exercise the same code paths as the original Phase-22 tests`
       - `// above, but assert specific invariants of the shared-helper refactor:`
       - `// (1) call-depth guard still runs FIRST (pre-mutation atomicity)`
       - `// (2) Decimal::to_string preserves sign for label-lookup callers`
       - `//`
       - `// If any of these tests fail, the refactor regressed Phase-22 behavior.`

    2. Add the 4 sentinel test functions per RESEARCH §"Sentinel Regression Test Design (D-24.5)" lines 818-823. Mirror the existing `test_gto_ind_happy` / `test_xeq_ind_happy` / `test_xeq_ind_4_deep_call_stack_rejects` skeletons from the same file (analog patterns in PATTERNS.md lines 145-213). Function names exactly:
       - `phase24_gto_ind_uses_shared_helper` — copy `test_gto_ind_happy` skeleton, add a docstring noting "sanity — proves refactored arm routes via resolve_indirect_decimal".
       - `phase24_xeq_ind_uses_shared_helper` — copy `test_xeq_ind_happy` skeleton, add docstring noting "sanity for XeqInd refactor".
       - `phase24_xeq_ind_call_depth_guard_runs_before_pointer_read` — driven via `resume_program` (NOT `run_program` — the latter wipes `call_stack` at entry per phase22_program_control.rs:304-305 inline comment); pre-set `state.call_stack = vec![999usize; 4]` and `state.regs[3] = HpNum::rounded(Decimal::from_str("12.345").unwrap())`; expected `Err(HpError::CallDepth)` AND `state.call_stack.len() == 4` (no push happened).
       - `phase24_gto_ind_negative_pointer_stringifies_with_sign` — `state.regs[5] = HpNum::from(-3i32)`, program `[Op::Lbl("A".to_string()), Op::GtoInd(5)]` (no LBL "-3" anywhere); expected `Err(HpError::InvalidOp)` from `find_in_program` (NOT from non-integer rejection — -3 IS an integer; the failure is "label '-3' not found in program").

    Step C — Create scaffolding file `hp41-core/tests/phase24_resolve_indirect.rs`:

    Per VALIDATION.md row "24-01 / new files / hp41-core/tests/phase24_resolve_indirect.rs" — the file is reserved for Wave 1 even though the bulk of its content (cross-cutting helper assertions in addition to the inline unit tests already added in Task 1) is minimal. Create with:
    - `#![allow(clippy::unwrap_used)]` crate-level attribute
    - `//! Phase 24 cross-cutting integration tests for resolve_indirect / resolve_indirect_decimal.` module-level docstring
    - `//!` (blank line)
    - `//! 24-01 scope: scaffolding — the primary unit-test coverage for the helpers lives` (comment)
    - `//! inline in `hp41-core/src/ops/indirect.rs::tests`, and the GtoInd/XeqInd refactor` (comment)
    - `//! regression sentinels live in `hp41-core/tests/phase22_program_control.rs` (D-24.5).` (comment)
    - `//! 24-02 scope: this file gains the per-variant `*_ind` integration tests OR a sibling` (comment)
    - `//! `phase24_ind_variants.rs` is created — see plan 24-02.` (comment)
    - One smoke test `fn resolve_indirect_is_reachable_from_integration_target() { /* compiles → public symbol path resolves */ let _ = hp41_core::ops::indirect::resolve_indirect; }` (asserts the public symbol path exists and is callable from outside the crate — defends against Pitfall 5 "forgetting `pub mod indirect;` re-exports in lib.rs").

    Anti-patterns to avoid:
    - Do NOT touch the `Op::GtoInd(_) | Op::XeqInd(_) => Err(HpError::InvalidOp)` catch-all in `dispatch()` — refactor is body-only.
    - Do NOT remove or relocate the call_stack pre-mutation guard in XeqInd; sentinel test #3 will fail if you do.
    - Do NOT rename `Op::GtoInd` or `Op::XeqInd` — D-24.5 specifies these are NOT renames; the variant names are preserved.
    - Do NOT add a `find_in_program` call site or a `state.pc` advance line that wasn't there before — the refactor is body-only consolidation, not behavior change.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test phase22_program_control 2>&1 | tail -25 &amp;&amp; cargo test -p hp41-core --test phase24_resolve_indirect 2>&1 | tail -10 &amp;&amp; cargo test -p hp41-core 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolve_indirect_decimal" hp41-core/src/ops/program.rs` is at least 2 (one call in the GtoInd arm, one in the XeqInd arm).
    - `grep -c "pointer.trunc_int" hp41-core/src/ops/program.rs` returns 0 (the inline trunc-and-validate is fully extracted; no surviving inline duplicates remain in the GtoInd/XeqInd arms).
    - `grep -A3 "Op::XeqInd(reg) =>" hp41-core/src/ops/program.rs | grep -c "call_stack.len() >= 4"` is at least 1 (the pre-mutation guard remains as the first check inside the XeqInd arm).
    - `grep -c "phase24_gto_ind_uses_shared_helper\|phase24_xeq_ind_uses_shared_helper\|phase24_xeq_ind_call_depth_guard_runs_before_pointer_read\|phase24_gto_ind_negative_pointer_stringifies_with_sign" hp41-core/tests/phase22_program_control.rs` returns 4 (all four sentinel tests are appended).
    - File `hp41-core/tests/phase24_resolve_indirect.rs` exists.
    - `cargo test -p hp41-core --test phase22_program_control test_gto_ind_happy` exits 0 (existing Phase-22 test still passes after refactor).
    - `cargo test -p hp41-core --test phase22_program_control test_gto_ind_non_integer_rejects` exits 0 (existing Phase-22 reject test still passes).
    - `cargo test -p hp41-core --test phase22_program_control test_xeq_ind_happy` exits 0 (existing Phase-22 XeqInd happy test still passes).
    - `cargo test -p hp41-core --test phase22_program_control test_xeq_ind_non_integer_rejects` exits 0.
    - `cargo test -p hp41-core --test phase22_program_control test_xeq_ind_4_deep_call_stack_rejects` exits 0 (existing Phase-22 4-deep guard test still passes — proves D-22.15 atomicity preserved).
    - `cargo test -p hp41-core --test phase22_program_control phase24_gto_ind_uses_shared_helper` exits 0.
    - `cargo test -p hp41-core --test phase22_program_control phase24_xeq_ind_uses_shared_helper` exits 0.
    - `cargo test -p hp41-core --test phase22_program_control phase24_xeq_ind_call_depth_guard_runs_before_pointer_read` exits 0 (proves pre-mutation atomicity preserved through refactor — sentinel returns CallDepth, NOT InvalidOp).
    - `cargo test -p hp41-core --test phase22_program_control phase24_gto_ind_negative_pointer_stringifies_with_sign` exits 0 (Pitfall 2 sentinel — Decimal sign preservation).
    - `cargo test -p hp41-core --test phase24_resolve_indirect resolve_indirect_is_reachable_from_integration_target` exits 0 (smoke test — public symbol path resolves from outside the crate; defends against Pitfall 5).
    - `cargo test -p hp41-core` exits 0 (entire hp41-core test suite green; no regression in any other phase's tests).
    - `cargo clippy -p hp41-core -- -D warnings` exits 0 (no new clippy warnings introduced).
  </acceptance_criteria>
  <done>
    Both `Op::GtoInd` and `Op::XeqInd` arms in `run_loop` consume `crate::ops::indirect::resolve_indirect_decimal`. The `call_stack.len() >= 4 → CallDepth` guard remains the first executable line of the XeqInd arm. All 5 pre-existing Phase-22 GTO/XEQ-IND tests pass byte-for-byte. All 4 new D-24.5 sentinel tests pass. `phase24_resolve_indirect.rs` exists as the Wave-1 scaffolding file with one smoke test confirming the public symbol path resolves. `cargo test -p hp41-core` and `cargo clippy -p hp41-core` are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save-file → `Op` enum deserialization | `serde_json` reads program steps from `~/.hp41/autosave.json`; malformed `Op::*Ind` payloads (or unknown variants) flow into `state.program` and are later dispatched. Phase 24 plan 01 adds NO new Op variants (those land in plan 02), so the attack surface change in this plan is limited to the inner helpers' input space. |
| `state.regs[reg]` content → `resolve_indirect` arithmetic | The pointer Decimal can be any value a deserialized HpNum can hold (any 28-digit Decimal magnitude). The helper must reject all out-of-domain inputs without panicking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | malformed save-file `state.regs[n]` (e.g. Decimal magnitude > i64::MAX) flowing into `resolve_indirect` | mitigate | The `decimal.to_i64().ok_or(InvalidOp)?` arm in `resolve_indirect` collapses any out-of-i64 pointer to `HpError::InvalidOp` without panic. Explicitly tested by `resolve_indirect_pointer_exceeds_i64_range_rejects` (Task 1). |
| T-24-01-02 | Denial of service | `unwrap()`/`expect()` panic on a corrupt pointer crashing the calculator | mitigate | `#![deny(clippy::unwrap_used)]` at `hp41-core/src/lib.rs` is enforced workspace-wide. Production helpers use `?` and `.ok_or(InvalidOp)?` only. Acceptance criterion `grep -E '\.unwrap\(\)|\bas u8\b' ... | grep -vc 'mod tests'` returns 0. |
| T-24-01-03 | Tampering | refactor inadvertently relaxes the pre-mutation atomicity guard on XeqInd, allowing a non-integer pointer to mutate `call_stack` before failing | mitigate | Sentinel test `phase24_xeq_ind_call_depth_guard_runs_before_pointer_read` (Task 2) drives a 4-deep call_stack with a non-integer pointer and asserts `CallDepth` (NOT `InvalidOp`) — proves the guard runs FIRST. Combined with `state.call_stack.len() == 4` post-condition, confirms no partial mutation. |
| T-24-01-04 | Information disclosure | low-value | accept | `hp41-core` is a pure offline library — no network, no logging, no PII. The IND helpers only manipulate numeric register state already trusted as user input. No exfiltration vector. |

(Phase 24 / Plan 01 is local refactor + helper code; the variant-set expansion in Plan 02 will register additional STRIDE entries for the new Op variants' deserialization surface and dispatch behavior.)
</threat_model>

<verification>
- `cargo build -p hp41-core` exits 0
- `cargo test -p hp41-core` exits 0 (all hp41-core tests green — including the 7 new inline `indirect::tests` and the 4 new `phase22_program_control.rs` sentinels)
- `cargo clippy -p hp41-core -- -D warnings` exits 0 (no new warnings; zero-panic invariant preserved)
- `just test -p hp41-core` exits 0 (workspace-runner equivalent — confirms the `just`-only invariant from CLAUDE.md still holds)
- `grep -c "pub fn resolve_indirect" hp41-core/src/ops/indirect.rs` is at least 1
- `grep -c "pointer.trunc_int" hp41-core/src/ops/program.rs` returns 0 (refactor complete; no surviving inline trunc-and-validate)
- `grep -c "resolve_indirect_decimal" hp41-core/src/ops/program.rs` is at least 2 (both arms consume the helper)
- `grep -A3 "Op::XeqInd(reg) =>" hp41-core/src/ops/program.rs | grep -c "call_stack.len() >= 4"` is at least 1 (atomicity guard still in place)
</verification>

<success_criteria>
- Helper file `hp41-core/src/ops/indirect.rs` ships with the two-tier resolver (`resolve_indirect_decimal` private + `resolve_indirect` public) and 7 inline unit tests covering happy path, non-integer rejection, regs-out-of-range, u8-out-of-range, i64-out-of-range, negative-via-u8, and Decimal sign preservation
- `pub mod indirect;` declared in `hp41-core/src/ops/mod.rs` so the helper is reachable as `hp41_core::ops::indirect::resolve_indirect` from integration tests (Pitfall 5 mitigated)
- Phase-22 `Op::GtoInd` and `Op::XeqInd` run_loop arms consume `crate::ops::indirect::resolve_indirect_decimal`; their bodies no longer contain inline `state.regs.get(...)?.clone(); pointer.trunc_int(); int_part != pointer` chains
- The XeqInd `call_stack.len() >= 4 → CallDepth` guard remains the first executable line of the arm — proven by sentinel test driving a 4-deep stack with a non-integer pointer and asserting `CallDepth`, not `InvalidOp`
- All 5 pre-existing Phase-22 GTO/XEQ-IND tests in `phase22_program_control.rs` continue to pass byte-for-byte (refactor is behavior-preserving)
- All 4 new D-24.5 sentinel tests pass
- `phase24_resolve_indirect.rs` exists as the Wave-1 scaffolding file with one smoke test asserting the public symbol path resolves from outside the crate
- Coverage gate (current 92.5%/92.68% baseline on hp41-core) does not regress (verified by Plan 24-02's final `just coverage` run; Plan 24-01 alone cannot raise or lower the gate)
</success_criteria>

<output>
After completion, create `.planning/phases/24-indirect-addressing/24-01-SUMMARY.md` documenting:
- Files created (paths) and modified (paths + line-range deltas)
- The 7 inline unit tests + 4 D-24.5 sentinel tests that now pass
- Confirmation that the 5 pre-existing Phase-22 GTO/XEQ-IND tests still pass (refactor is behavior-preserving)
- Pitfall mitigations exercised (Pitfall 2 negative-pointer sign preservation, Pitfall 3 i64 overflow branch, Pitfall 5 `pub mod indirect;` reachability)
- Hand-off note for plan 24-02: `crate::ops::indirect::resolve_indirect` is the contract Plan 24-02's 11 new `op_*_ind` shims will delegate to
</output>
