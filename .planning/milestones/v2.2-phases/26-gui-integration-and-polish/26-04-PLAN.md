---
phase: 26-gui-integration-and-polish
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: [26-01, 26-02, 26-03]
files_modified:
  - hp41-gui/src/App.tsx
  - hp41-gui/src/pending_input.ts
  - hp41-gui/src/App.test.tsx
  - hp41-gui/src/pending_input.test.ts
autonomous: true
requirements:
  - FN-GUI-01
  - FN-GUI-02
  - FN-GUI-03
  - FN-GUI-04
  - FN-GUI-05
  - FN-POLISH-01
  - FN-POLISH-02
  - FN-POLISH-03
  - FN-POLISH-04
tags: [gap-closure, modal-routing, frontend-projections, integration-tests]

must_haves:
  truths:
    # Mirrors the 5 BLOCKERs in 26-VERIFICATION.md gaps[].
    - "Clicking the ASN flow stores assignments at the canonical HP-41 keyCode (key.keyCode), not at layout-coords; clicking an undefined-keyCode key surfaces a toast (CR-01)"
    - "Search input inside the open `?` help overlay does NOT dispatch ops in the background — typing 's', 'q', 'r', 't' leaves stack and entry_buf unchanged (CR-02)"
    - "Clicking the on-screen ENTER key inside an open assign_label / clp / xeq_name / gto / lbl modal confirms and dispatches the parameterized id (CR-03)"
    - "Clicking the on-screen ← (clx_or_a) inside an open clp / xeq_name / gto / lbl / assign_label modal removes the last accumulator char (CR-03)"
    - "AVIEW / PROMPT / VIEW backend ops produce a user-visible display via calcState.display_override (CR-04)"
    - "BEEP / TONE backend ops produce a user-visible feedback line via calcState.event_buffer drain (CR-04)"
    - "CATALOG modal accepts 1, 2, 3, 4 and rejects 0 + 5..=9 with no dispatch (CR-05)"
    - "USER mode shows the ASN'd label on the keycap after the click-only ASN flow completes — the assignment keyCode and the keycap keyCode match (CR-01 + CR-03 closure)"
    - "Integration test suite covers end-to-end click → modal → dispatch → render against a mocked Tauri invoke (verifier's explicit recommendation)"
  artifacts:
    - path: "hp41-gui/src/App.tsx"
      provides: "CR-01/CR-02/CR-03/CR-04/CR-05 wiring fixes — single edit pass"
      contains: "key.keyCode"
      contains_2: "if (helpOpen) return"
      contains_3: "display_override ?? calcState.display_str"
      contains_4: "max: 4"
    - path: "hp41-gui/src/pending_input.ts"
      provides: "CR-05 single_digit lower bound; CR-03 may stay untouched (translation at click-router)"
      contains: "Catalog allows 1..=max"
    - path: "hp41-gui/src/App.test.tsx"
      provides: "@testing-library/react integration suite with mocked @tauri-apps/api/core invoke"
      min_lines: 200
  key_links:
    - from: "App.tsx handleClick (assign_key branch)"
      to: "makeKeyCodeMagic"
      via: "key.keyCode (canonical, not row*10+(col+1))"
      pattern: "makeKeyCodeMagic\\(key\\.keyCode\\)"
    - from: "App.tsx handleKey"
      to: "helpOpen short-circuit"
      via: "early return before resolveKeyId"
      pattern: "if \\(helpOpen\\) return"
    - from: "App.tsx handleClick effectiveId='enter'/'clx_or_a'"
      to: "handleModalKey"
      via: "translation to 'Enter'/'Backspace' at the click-router boundary"
      pattern: "(effectiveId === 'enter'|effectiveId === 'clx_or_a')"
    - from: "App.tsx displayText derivation"
      to: "calcState.display_override"
      via: "?? fallback to display_str"
      pattern: "display_override \\?\\? calcState\\.display_str"
    - from: "App.tsx useEffect on calcState"
      to: "event_buffer consumer"
      via: "showToast or printLog append"
      pattern: "event_buffer"
    - from: "App.tsx MODAL_OPENERS.catalog"
      to: "single_digit with max=4"
      via: "constructor literal"
      pattern: "Catalog'.*max: 4"
---

<objective>
Close the 5 BLOCKER gaps from `.planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md` (CR-01..CR-05) in a single focused pass and add the missing integration test surface that would have caught them. After this plan ships, Phase 26 re-verification must lift its score from 7/12 to 12/12 must-haves verified.

The 5 BLOCKERs are concentrated in two files (App.tsx, pending_input.ts) and all five are layer-wiring defects — each layer was individually unit-tested and correct in isolation, but the click → modal → dispatch → render pipeline was never exercised end-to-end. This plan fixes the wiring AND adds the @testing-library/react integration suite that exercises the full pipeline with a mocked Tauri `invoke`, so a future regression of the same class fails CI immediately.

Purpose: Restore FN-POLISH-03 (USER-mode relabel) from "dead-on-arrival" to working, restore FN-POLISH-02 (`?` overlay search) from "corrupts state" to "owns focus", restore FN-GUI-05 (`display_override` + `event_buffer`) from "hollow prop" to "visible effect", restore FN-GUI-01 (CATALOG 4) from "unreachable" to "dispatchable", and restore FN-GUI-03 (click-only modal Enter submit) from "broken on the click path" to "working from both keyboard and click".

Output: 2 source files modified (App.tsx, pending_input.ts), 2 test files modified/created (`App.test.tsx` new with the integration suite; `pending_input.test.ts` extended for CR-05 Catalog bounds), 1 SUMMARY in a follow-up commit.

NOT in scope (out of scope, explicit):
- Any `hp41-core` changes (frozen since Plan 25-01 — the surgical 4→12 `builtin_card_op` extension was the documented exception).
- Any `hp41-cli` changes (Phase 25 is shipped).
- Any backend (`hp41-gui/src-tauri/`) changes — the 5 BLOCKERs are all in the React frontend; the Rust projections + drain are correct.
- Actual Web Audio API for BEEP/TONE (v3.x territory per CONTEXT D-26.6 deferral comment). CR-04's `event_buffer` consumer ships as a console.log + toast feedback path — visible but not audible.
- The 8 WARNING / INFO findings from 26-REVIEW.md (WR-02 isPrintableChar widening for `<>?`, IN-01..04 informationals) — those are not BLOCKERs; Phase 27 territory.
- New Playwright E2E (Phase 27 FN-QUAL-05 — this plan ships Vitest+@testing-library/react integration tests, NOT a Tauri-app boot).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

# Phase 26 context (locked decisions + gap report — required reading)
@.planning/phases/26-gui-integration-and-polish/26-CONTEXT.md
@.planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md
@.planning/phases/26-gui-integration-and-polish/26-PATTERNS.md

# Original 26-01 plan + summary (modal architecture this plan repairs)
@.planning/phases/26-gui-integration-and-polish/26-01-PLAN.md
@.planning/phases/26-gui-integration-and-polish/26-01-SUMMARY.md
@.planning/phases/26-gui-integration-and-polish/26-03-SUMMARY.md

# Target source files (CURRENT state — what to fix)
@hp41-gui/src/App.tsx
@hp41-gui/src/pending_input.ts
@hp41-gui/src/Keyboard.tsx
@hp41-gui/src/pending_input.test.ts
@hp41-gui/vite.config.ts

# Backend reference (READ ONLY — no changes here, but executor must understand contract)
@hp41-gui/src-tauri/src/types.rs
@hp41-gui/src-tauri/src/commands.rs

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase so no
     scavenger hunt is required. -->

# Frontend mirror of CalcStateView (App.tsx:23-40):

```typescript
interface CalcStateView {
  display_str: string;
  x_str: string; y_str: string; z_str: string; t_str: string; lastx_str: string;
  in_eex_mode: boolean;
  annunciators: Annunciators;
  print_lines: string[];
  program_steps: string[];
  pc: number;
  user_keymap: Array<[number, string]>;
  flags: number[];
  display_override: string | null;
  event_buffer: string[];
}
```

# PendingInput shape (pending_input.ts:52-69) — DO NOT change the union shape; just CR-05 lower-bound check inside the `single_digit` arm.

# handleModalKey signature (pending_input.ts:166-170) — input alphabet
# documented at line 158: `key` is a digit/letter/special token
# ("Enter", "Escape", "Backspace"). The translation from on-screen ids
# ('enter'/'clx_or_a') happens at the click-router boundary (App.tsx::handleClick).

# KeyDef from Keyboard.tsx (lines 23-53). CRITICAL — read the W9 doc comment
# at lines 36-52 BEFORE touching App.tsx:297. KeyDef.keyCode is a HARDCODED
# CLI-canonical literal (e.g. SIN=25, STO=22, ENTER=84); it is NOT `row*10+col`.
# Some keys (variant 'top'/'shift', empty-id, CHS, CL X/A, xge_y) intentionally
# leave keyCode undefined. The fix must REJECT undefined-keyCode in assign_key
# (surface a toast — "this key cannot be assigned"), NOT silently fall back to
# a computed value.

# Backend CalcStateView fields (types.rs):
#   display_override: Option<String>   — set by Phase 21 AView/Prompt/View; cleared on next op
#   event_buffer: Vec<String>          — drained per-IPC by commands.rs; React must
#                                        consume from CalcStateView.event_buffer
# CR-04 consumer: useEffect dependent on calcState.event_buffer.length > 0,
# similar precedent at App.tsx:421-426 (print_lines accumulation).

# Existing Tauri-invoke pattern (App.tsx:62-67):
#   invokeForKey routes effectiveId → sst_step / bst_step / run_stop / dispatch_op
#   Integration tests MUST mock @tauri-apps/api/core's `invoke` export — there is
#   NO existing mock pattern in this repo's test suite (greppable: zero hits for
#   `vi.mock` in hp41-gui/src/*.test.*). This plan introduces it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-05 — Fix Catalog single_digit lower bound (pending_input.ts) and CR-05 frontend max (App.tsx MODAL_OPENERS.catalog)</name>

  <files>hp41-gui/src/pending_input.ts, hp41-gui/src/App.tsx, hp41-gui/src/pending_input.test.ts</files>

  <read_first>
    - hp41-gui/src/pending_input.ts (the `single_digit` arm at lines 294-309 — the ONLY change in this file for this task)
    - hp41-gui/src/App.tsx (lines 124-162 — `MODAL_OPENERS` table; specifically line 156 `catalog: () => ({ kind: 'single_digit', op: 'Catalog', max: 3 })`)
    - hp41-gui/src/pending_input.test.ts (existing `single_digit` test cases — search for `'catalog'` and `'tone'` substrings; new tests follow the same shape)
    - .planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md (gap CR-05 detail, lines 56-66 of the YAML frontmatter)
    - hp41-core/src/ops/program.rs:294-297 (op_catalog accepts 1..=4; `n==0` or `n>=5` returns InvalidOp — the source-of-truth for the range)
  </read_first>

  <behavior>
    - Test 1 (pending_input.test.ts): a `{ kind: 'single_digit', op: 'Catalog', max: 4 }` modal accepts digit '1' → dispatchId 'catalog_1', closes (nextPending: null).
    - Test 2: same modal accepts digit '4' → dispatchId 'catalog_4', closes.
    - Test 3: same modal REJECTS digit '0' — nextPending unchanged, dispatchId: null (no dispatch).
    - Test 4: same modal REJECTS digit '5' — nextPending unchanged, dispatchId: null.
    - Test 5: `{ kind: 'single_digit', op: 'Tone', max: 9 }` still accepts '0' → 'tone_0' (Tone lower bound is 0, NOT 1 — this asymmetry is the reason the lower bound is op-specific).
    - Test 6: Tone modal accepts '9' → 'tone_9'.
    - Test 7: any non-digit (e.g. 'Enter', 'a', 'Backspace') leaves nextPending unchanged for both Catalog and Tone.
  </behavior>

  <action>
    Implement op-specific lower-bound in `pending_input.ts` `single_digit` arm:

    1. In `pending_input.ts` at the `case 'single_digit':` arm (currently lines 294-309), add an op-specific lower-bound check. Catalog requires `digit >= 1`; Tone allows `digit >= 0`. Reject digits below the op's lower bound silently (mirror the existing > max rejection at line 298-300). Concrete shape: introduce a local `const minDigit = pending.op === 'Catalog' ? 1 : 0;` immediately after `const digit = Number(key);` and add `if (digit < minDigit) return { nextPending: pending, dispatchId: null, consumesShift: false };` BEFORE the existing `if (digit > pending.max)` rejection.

    2. In `App.tsx` at line 156, change `catalog: () => ({ kind: 'single_digit', op: 'Catalog', max: 3 })` to `catalog: () => ({ kind: 'single_digit', op: 'Catalog', max: 4 })`. Backend op_catalog at hp41-core/src/ops/program.rs:295 accepts n in 1..=4 (rejects 0, rejects >=5).

    3. In `pending_input.test.ts`, append a new `describe('handleModalKey — single_digit Catalog bounds (CR-05)')` block with the 7 tests enumerated in `<behavior>` above. Each test uses the existing `handleModalKey(key, pending, false)` pattern (see existing Sto modal tests at lines 20-37 for the call shape).

    4. Do NOT change pending_input.ts's `renderModalLcd` `case 'single_digit'` arm — `CAT _` and `TONE _` remain the LCD previews.

    5. Do NOT touch hp41-core/src/ops/program.rs (FROZEN per CLAUDE.md "v2.2 additions" and the gap_closure_context). The backend already accepts 1..=4 correctly; the bug is exclusively frontend.

    Self-check: `grep -n "max: 3" hp41-gui/src/App.tsx` must return zero matches after this task (the only `max: 3` in the file was Catalog). `grep -n "minDigit\|digit < minDigit" hp41-gui/src/pending_input.ts` must show the new guard.
  </action>

  <verify>
    <automated>cd hp41-gui && npx vitest run src/pending_input.test.ts 2>&1 | tail -30 | grep -E "PASS|FAIL|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c 'max: 4' hp41-gui/src/App.tsx` returns ≥ 1
    - Source assertion: `grep -c 'max: 3' hp41-gui/src/App.tsx` returns 0
    - Source assertion: `grep -n 'minDigit' hp41-gui/src/pending_input.ts` shows a match inside the single_digit arm (within lines 290-320)
    - Test command: `cd hp41-gui && npx vitest run src/pending_input.test.ts` reports all tests passing (the existing 42 tests plus the 7 new CR-05 tests = 49+ tests; exact count may vary if executor groups differently)
    - Behavior assertion: a programmatic call `handleModalKey('0', { kind: 'single_digit', op: 'Catalog', max: 4 }, false)` returns `{ nextPending: <same modal>, dispatchId: null, consumesShift: false }` — verifiable in the new test
    - Behavior assertion: `handleModalKey('4', { kind: 'single_digit', op: 'Catalog', max: 4 }, false)` returns `{ nextPending: null, dispatchId: 'catalog_4', consumesShift: false }`
    - Behavior assertion: `handleModalKey('0', { kind: 'single_digit', op: 'Tone', max: 9 }, false)` returns `{ nextPending: null, dispatchId: 'tone_0', consumesShift: false }` (Tone lower bound is 0, NOT 1)
  </acceptance_criteria>

  <done>
    pending_input.ts has the op-specific lower-bound guard; App.tsx MODAL_OPENERS.catalog has max:4; 7 new Vitest tests covering Catalog 0/1/4/5 + Tone 0/9 + non-digit rejection are added and all green; existing 42 tests still pass; CR-05 closed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-01/CR-02/CR-03/CR-04 — App.tsx wiring fixes (single edit pass)</name>

  <files>hp41-gui/src/App.tsx</files>

  <read_first>
    - hp41-gui/src/App.tsx (the ENTIRE file — every fix in this task is in this one file. Specifically: handleClick lines 263-341 for CR-01 and CR-03, handleKey lines 346-410 for CR-02, displayText lines 463-465 and useEffect lines 418-432 for CR-04)
    - hp41-gui/src/Keyboard.tsx lines 23-53 (the W9 doc comment block — the authoritative contract for KeyDef.keyCode usage; required reading before changing line 297)
    - hp41-gui/src/Keyboard.tsx lines 71-127 (the MAIN_GRID const — verify which keys have hardcoded keyCode literals and which leave it undefined: variant='top', variant='shift', empty-id, xge_y, chs, clx_or_a all skip keyCode)
    - hp41-gui/src/pending_input.ts lines 166-404 (handleModalKey signature + the 'Enter'/'Backspace' alphabet — the translation in CR-03 maps to these tokens)
    - .planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md gaps CR-01, CR-02, CR-03, CR-04 (lines 7-67 of the YAML frontmatter — exact missing items per BLOCKER)
    - hp41-gui/src-tauri/src/types.rs (the `display_override` and `event_buffer` projection — the contract the frontend consumes)
  </read_first>

  <behavior>
    The integration tests in Task 3 lock these behaviors. This task implements them in production code.

    - Test (CR-01): inside an open assign_key modal, clicking a key with hardcoded `keyCode: 25` (e.g. SIN) calls `handleModalKey(makeKeyCodeMagic(25), …)` — the parameter is the canonical CLI literal, NOT `row*10+(col+1)` (which would compute 23 from row 2 col 2). The next pendingInput becomes `{ kind: 'assign_label', keyCode: 25, acc: '' }`.

    - Test (CR-01 toast): inside an open assign_key modal, clicking a key with undefined keyCode (e.g. CHS, xge_y, clx_or_a, SHIFT, ON, USER, PRGM, ALPHA) surfaces a toast with a message containing "cannot be assigned" (or similar — exact string set in implementation; the integration test asserts substring "cannot" + "assign"); pendingInput stays as `{ kind: 'assign_key' }`; no dispatch fires.

    - Test (CR-02 helpOpen short-circuit): with `helpOpen=true`, pressing physical 's' / 'q' / 'r' / 't' / Backspace / '0'..'9' does NOT call `invoke('dispatch_op', …)`. Only Escape (closes overlay) and '?' (no-op while overlay open per existing line 355) reach state. The mocked `invoke` is asserted zero times for these keys.

    - Test (CR-03 on-screen ENTER): inside an open assign_label modal with `acc: 'TEST'`, calling `handleClick({ id: 'enter', label: 'ENTER↑', row: 4, col: 0, … })` results in `invoke('dispatch_op', { keyId: 'asn_22_TEST' })` (assuming keyCode 22 was captured in the assign_key step). Without this fix, the click is ignored because pending_input.ts:372 checks `key === 'Enter'` (capital E) but App.tsx sends lowercase `'enter'`.

    - Test (CR-03 on-screen ←): inside an open assign_label modal with `acc: 'TEST'`, calling `handleClick` with the `clx_or_a` KeyDef pops the last char — pendingInput becomes `{ kind: 'assign_label', keyCode: 22, acc: 'TES' }`. NO Op::Clx is dispatched (the modal swallows the click; the ALPHA-aware `clx | alpha_clear` branch at line 324-329 must NOT fire while a modal is open — but that branch is already gated by line 292 `if (pendingInput !== null) { … return; }`, so this works once we translate `clx_or_a` → `'Backspace'`).

    - Test (CR-04 display_override): after `invoke('dispatch_op', …)` resolves with `{ …, display_override: 'HELLO', display_str: '0.0000' }`, the rendered `displayText` is `'HELLO'`, NOT `'0.0000'`. When pendingInput is non-null, the modal preview still wins (existing precedence preserved at line 463-465).

    - Test (CR-04 event_buffer): after `invoke('dispatch_op', …)` resolves with `event_buffer: ['BEEP', 'TONE 5']`, a toast or visible feedback row surfaces. The integration test asserts: (a) a useEffect-driven side effect runs (we'll verify via a visible element in the DOM containing the event lines, OR via showToast firing with one of the event strings).
  </behavior>

  <action>
    Apply 5 surgical edits to `hp41-gui/src/App.tsx` in a single pass (one commit at end of Task 2 covers all CR-01..CR-04 wiring). The order below is the recommended diff sequence to minimize re-reading. NO new files are created in this task; everything mutates App.tsx in place. Identifier names below (`routedKey`, helper function names) are illustrative; the executor may choose any naming that makes the source-grep acceptance assertions hit.

    1. **CR-01 keycode fix + undefined-keyCode toast + CR-03 click-router translation** — Edit handleClick lines 293-298. The existing block declares `routedKey` via a single ternary that computes `makeKeyCodeMagic(key.row * 10 + (key.col + 1))` for the `assign_key` kind and uses `effectiveId` otherwise. Replace that ternary with a 4-branch routing chain that handles assign_key, on-screen ENTER, on-screen ← (clx_or_a), and the default in that order:
       (a) For `pendingInput.kind === 'assign_key'`: first check `key.keyCode === undefined`. If true, call `showToast('This key cannot be assigned')` and early-return from handleClick — do NOT advance the modal, do NOT consume shift, do NOT dispatch. Otherwise assign `routedKey = makeKeyCodeMagic(key.keyCode)` — passing the canonical CLI literal from `KeyDef.keyCode` (the hardcoded HP-41 hardware code per the W9 doc comment at Keyboard.tsx:36-52), NOT a computed row×10+col value.
       (b) Else-if `effectiveId === 'enter'`: assign `routedKey = 'Enter'` (capital E — the alphabet handleModalKey expects per pending_input.ts:244, 326, 372).
       (c) Else-if `effectiveId === 'clx_or_a'`: assign `routedKey = 'Backspace'` (the alphabet handleModalKey expects per pending_input.ts:254, 336, 379). Note: the existing non-modal `clx_or_a` ALPHA-aware branch at handleClick lines 324-329 is already gated behind `if (pendingInput !== null) { ... return; }` at line 292, so this translation only fires inside an open modal — the non-modal ALPHA-aware path is preserved untouched.
       (d) Default: assign `routedKey = effectiveId` verbatim.

       The 4-branch chain may be expressed as a sequence of if/else-if statements, or extracted into a small helper function (e.g. `translateClickToModalKey(effectiveId, key, kind)`) defined above the component. Whichever form, the source-grep acceptance assertions below must hit (specifically: `key.keyCode` appears in handleClick; `key.row * 10` does NOT appear anywhere in App.tsx; one of `effectiveId === 'enter'` / `effectiveId === 'clx_or_a'` / `translateClickToModalKey` appears in handleClick).

    2. **CR-02 helpOpen short-circuit** — Edit handleKey body. Insert an early-return immediately after the `if (e.key === 'Tab') { e.preventDefault(); setShiftActive(prev => !prev); return; }` block at lines 381-385, AND BEFORE the `if (busyRef.current) return;` busyRef debounce at line 386. The new guard is a single-line `if (helpOpen) return;` preceded by a short comment explaining intent: when the help overlay is open, the React-rendered `<input>` inside HelpOverlay.tsx owns focus; window-level keystrokes must NOT leak to resolveKeyId / dispatchKeyId, or the calculator's stack/entry_buf gets corrupted by every search keystroke. Escape and '?' are already handled above and short-circuit before reaching this point, so this `if (helpOpen) return;` is the third Esc-precedence layer.

       `helpOpen` is already present in the handleKey useCallback dependency array at line 410 (verified during the Read phase), so no array change is needed. If the executor's edit happens to clobber the dep array, restore it.

    3. **CR-04a displayText derivation** — Edit lines 463-465. The current derivation reads `const displayText: string = pendingInput ? renderModalLcd(pendingInput) : calcState.display_str;`. Modify ONLY the else-branch of the ternary: replace `calcState.display_str` with `(calcState.display_override ?? calcState.display_str)`. The pendingInput modal preview still wins (precedence: modal preview > display_override > display_str). This ordering mirrors the hp41-cli equivalent (CLI Phase 21 gives display_override priority over the entry-buf when no modal is open).

    4. **CR-04b event_buffer consumer useEffect** — Insert a new useEffect immediately AFTER the existing print_lines useEffect at lines 421-426 (which appends `calcState.print_lines` into the React `printLog` state). The new effect's dependency list is `[calcState, showToast]`. Its body checks `if (calcState && calcState.event_buffer.length > 0)` and, when true, iterates the `calcState.event_buffer` array calling `showToast(line)` once per line. The single-toast policy at App.tsx:194-207 (the `seq` counter pattern) makes consecutive same-message toasts re-fire by mutating the seq, so a multi-event payload visibly cycles through. Future v3.x can replace the toast with a Web Audio API call without changing the projection contract; the event_buffer projection's drain semantics in commands.rs stay the same. NOTE: `showToast` at line 196-199 is a stable useCallback reference; no further memoization needed.

    5. **CR-03 click-router translation reminder** — Step 1 already implements this as branches (b) and (c) of the 4-branch chain. No separate edit. Verify after Task 2 completes: `grep -n "effectiveId === 'enter'\|translateClickToModalKey" hp41-gui/src/App.tsx` returns at least one match.

    **Self-check after all 5 edits:**
    - `grep -c 'key\.keyCode' hp41-gui/src/App.tsx` returns ≥ 1 (the new fix).
    - `grep -c 'key.row \* 10' hp41-gui/src/App.tsx` returns 0 (the buggy formula is gone).
    - `grep -c 'if (helpOpen) return' hp41-gui/src/App.tsx` returns ≥ 1 (new short-circuit; an additional helpOpen check pre-existed around line 355 for the '?' open-overlay guard and may remain).
    - `grep -c 'display_override ??' hp41-gui/src/App.tsx` returns ≥ 1.
    - `grep -c 'event_buffer' hp41-gui/src/App.tsx` returns ≥ 2 (interface field + new useEffect consumer).
    - `cd hp41-gui && npx tsc --noEmit` returns clean (no TS errors introduced).
    - `cd hp41-gui && npx vitest run` reports the existing 121 + 7 (Task 1) tests still passing; Task 3 will add the integration tests.
  </action>

  <verify>
    <automated>cd hp41-gui && npx tsc --noEmit && npx vitest run 2>&1 | tail -10 | grep -E "PASS|FAIL|passed|failed|Test Files"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -n 'key\.keyCode' hp41-gui/src/App.tsx` shows a match inside handleClick (between lines 263 and 341)
    - Source assertion: `grep -n 'key.row \* 10' hp41-gui/src/App.tsx` returns zero matches (the bug formula is eliminated)
    - Source assertion: `grep -c 'if (helpOpen) return' hp41-gui/src/App.tsx` returns at least 1 (the new short-circuit before resolveKeyId in handleKey, around line 386)
    - Source assertion: `grep -n 'display_override ??' hp41-gui/src/App.tsx` shows a match in the displayText derivation (around line 463-465)
    - Source assertion: `grep -nE 'useEffect\([^)]*\{[^}]*event_buffer' hp41-gui/src/App.tsx` shows a match (the new useEffect consuming event_buffer — exact regex may need slight adjustment for multi-line, but the spirit is "useEffect referencing event_buffer somewhere in App.tsx")
    - Source assertion: `grep -nE "effectiveId === 'enter'|effectiveId === 'clx_or_a'|translateClickToModalKey" hp41-gui/src/App.tsx` shows the CR-03 translation (in any of the 3 idiomatic shapes — branch chain, helper function, or table lookup)
    - Test command: `cd hp41-gui && npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - Test command: `cd hp41-gui && npx vitest run` reports all existing tests still passing (no regressions to the 121-test baseline; Task 3 adds new tests on top)
    - CLI assertion: `cd hp41-gui/src-tauri && grep -rEn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" src/` returns zero matches (SC-4 invariant per CLAUDE.md preserved — no calculator logic added to the backend)
  </acceptance_criteria>

  <done>
    All 4 BLOCKERs in App.tsx are surgically fixed: assign_key uses key.keyCode with undefined-rejection toast (CR-01), helpOpen gates handleKey before resolveKeyId (CR-02), 'enter'/'clx_or_a' on-screen ids translate to 'Enter'/'Backspace' for the modal alphabet (CR-03), displayText prefers display_override over display_str when no modal is open (CR-04a), event_buffer drains into the toast queue (CR-04b). tsc clean. All existing Vitest tests still pass. SC-4 grep still zero.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test suite — @testing-library/react with mocked Tauri invoke (CR-01..CR-05 end-to-end)</name>

  <files>hp41-gui/src/App.test.tsx</files>

  <read_first>
    - hp41-gui/src/App.tsx (the FINAL state after Task 2 — the test asserts the production behavior just written)
    - hp41-gui/src/HelpOverlay.test.tsx lines 1-60 (the existing @testing-library/react pattern in this repo — `import { render, fireEvent } from '@testing-library/react'` plus the call shape `render(<Component ... />)`; this is the closest analog)
    - hp41-gui/src/Keyboard.test.tsx (existing patterns for asserting against rendered SVG keys — useful for the click-target assertions)
    - hp41-gui/src/pending_input.test.ts (the pure-function call shape for handleModalKey — the integration tests use the rendered component path but the underlying transitions match)
    - hp41-gui/vite.config.ts (jsdom environment is already configured at lines 25-32; no vite config changes needed for this task)
    - hp41-gui/package.json (verify `@testing-library/react@^16.3.2` and `vitest@^4.1.6` are already in devDependencies — confirmed in 26-01-SUMMARY; no install needed)
    - hp41-gui/src/Keyboard.tsx KEY_DEFS (the canonical click target list — SIN has keyCode 25, STO has keyCode 22; CHS / xge_y / SHIFT / clx_or_a have undefined keyCode)
    - .planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md (the explicit verifier recommendation: "no integration test exercises the end-to-end click → modal → dispatch → render flow" — this task is the closure for that recommendation)
  </read_first>

  <behavior>
    Create `hp41-gui/src/App.test.tsx` as a new file. Use `vi.mock('@tauri-apps/api/core', ...)` at module top with a configurable mock that returns a controllable `CalcStateView` per call. Each test resets the mock + sets up the next response.

    Test groups (organize via `describe` blocks):

    **Group A — CR-01 ASN canonical keyCode (3 tests):**
    - A1: render `<App />`; mock initial `get_state` → empty CalcStateView. Click SHIFT then XEQ (which has shifted: { id: 'asn' }) to enter ASN flow. Assert pendingInput state by reading the LCD text — should show "ASN __" (per renderModalLcd assign_key case). Then click SIN (keyCode 25). Assert the LCD now shows "ASN _" (assign_label kind, acc empty — per renderModalLcd line 469). No invoke calls happened yet.
    - A2: continue A1, then click 'T', 'E', 'S', 'T' on appropriate alphaChar keys (or simulate physical typing — physical-keyboard path uses key.length === 1 branch at line 395-398). Click ENTER (on-screen). Assert `invoke` was called with `('dispatch_op', { keyId: 'asn_25_TEST' })` — keyCode 25 is the canonical SIN literal, NOT 23 (which would be `row 2 * 10 + (col 2 + 1)` per the bug).
    - A3: render `<App />`, enter ASN flow, click CHS (keyCode undefined). Assert: (a) a toast surfaces containing "cannot be assigned" or similar substring, (b) `invoke` was NOT called with any `asn_*` keyId in this sequence, (c) the pendingInput LCD still shows "ASN __" (modal stayed open).

    **Group B — CR-02 helpOpen gates window keystrokes (2 tests):**
    - B1: render `<App />`; press '?' to open overlay. Reset the invoke mock so we can assert call counts. Fire physical-keyboard `'s'`, `'q'`, `'r'`, `'t'`, Backspace via `fireEvent.keyDown(window, { key: 's' })` etc. Assert `invoke` was NOT called (mock call count is 0 after the reset).
    - B2: same setup, then press Escape. Assert helpOpen closes (the overlay element disappears from the DOM). Then press 's' again; `invoke` SHOULD now be called with `('dispatch_op', { keyId: 'sqrt' })` per the MAP at line 102 (verifies the gate only fires while helpOpen is true; closing the overlay restores normal dispatch).

    **Group C — CR-03 on-screen ENTER/clx_or_a translation (2 tests):**
    - C1: render `<App />`, enter ASN flow + click SIN (keyCode 25) to get into assign_label modal. Type 'T' 'E' 'S' 'T' via physical keys (acc accumulates). Click the on-screen ENTER key — assert `invoke` called with `('dispatch_op', { keyId: 'asn_25_TEST' })`. Without the CR-03 fix the click would be ignored and `invoke` would not have been called for asn_*.
    - C2: render `<App />`, click XEQ directly (xeq_prompt is the primary id at Keyboard.tsx line 89 and is in MODAL_OPENERS at App.tsx:152) to open an xeq_name modal. Type 'A' 'B' 'C' via physical keys. Click on-screen ← (clx_or_a). Assert renderModalLcd output drops the last char (LCD now shows "XEQ AB_" instead of "XEQ ABC_"). No invoke calls for clx during this sequence (the modal swallows the click).

    **Group D — CR-04 display_override + event_buffer wiring (3 tests):**
    - D1 (display_override): mock initial state; click ENTER (Op::Enter). Then mock the NEXT invoke response with `display_override: 'HELLO'` and `display_str: '0.0000'`. Click any dispatching key (e.g. '1' digit) to trigger an invoke that returns the override state. Assert the rendered `.display` element's text content includes 'HELLO'. If reading the SVG Display14Seg's text is too fragile in jsdom, add a `data-displaytext={displayText}` attribute on the `.display` div in App.tsx as a test-only locator (zero behavior change; data attributes are reflected to DOM and queryable via `container.querySelector('[data-displaytext]')`). Executor picks; both routes are acceptable.
    - D2 (display_override falls back to display_str when null): mock response with `display_override: null, display_str: '3.1416'`. Trigger an invoke. Assert displayText reads '3.1416'.
    - D3 (event_buffer toast surfacing): mock response with `event_buffer: ['BEEP', 'TONE 5']`. Trigger an invoke. Assert that the rendered toast element (`.toast` class) eventually contains one of those strings — showToast fires once per line in a for-loop, so the LAST call wins for the visible toast. The integration test asserts the toast surfaces SOMETHING from the event_buffer; an exact "BEEP then TONE 5" sequence is racy and not asserted.

    **Group E — CR-05 CATALOG max=4 lower-bound (2 tests):**
    - E1: render `<App />`; click ENTER+CATALOG to open catalog modal (CATALOG is the shifted of ENTER at row 4 col 0 per Keyboard.tsx line 97 with `shifted: { id: 'catalog' }`). Press '4' via fireEvent.keyDown(window, { key: '4' }). Assert `invoke` called with `('dispatch_op', { keyId: 'catalog_4' })`.
    - E2: same setup, press '0'. Assert `invoke` was NOT called (the lower bound rejects '0'). Press '5'. Assert `invoke` was NOT called (existing > max rejection covers this; redundant but explicit). Modal stays open until user presses Escape.

    **Group F — USER-mode end-to-end (CR-01 + CR-03 closure, 1 test):**
    - F1: render `<App />`, complete the full ASN flow click-only: SHIFT → XEQ (shifted = ASN) → STO key (keyCode 22) → physical 'T' 'E' 'S' 'T' → on-screen ENTER → mock invoke response includes `user_keymap: [[22, 'TEST']]`. Then click USER (top row) to toggle USER mode (mock the response with `annunciators.user: true`). Query the rendered Keyboard SVG and assert that the keycap labeled at row 3 col 2 (STO position) now displays 'TEST' (the relabel logic in Keyboard.tsx:203-208 takes effect because keyCode 22 in user_keymap matches the STO KeyDef's keyCode 22). This single test confirms the two halves of the previously-broken FN-POLISH-03 are now connected.
  </behavior>

  <action>
    Create `hp41-gui/src/App.test.tsx` with the test groups described in `<behavior>`. Strict implementation requirements (the executor follows this prose; no fenced code blocks are embedded here — the test file content itself is left to the executor to draft against the patterns at HelpOverlay.test.tsx and Keyboard.test.tsx).

    1. **Mock setup at module top.** Import `describe, it, expect, beforeEach, vi` from vitest. Import `render, fireEvent, waitFor, screen` from `@testing-library/react`. Import `App` from `./App`. Declare a module-scoped `const mockInvoke = vi.fn();`. Register the Tauri mock via `vi.mock('@tauri-apps/api/core', () => ({ invoke: (...args: unknown[]) => mockInvoke(...args) }));`. The mock SUBSTITUTES the production invoke export — calls flowing through `invokeForKey` at App.tsx:62-67 land in mockInvoke. In a `beforeEach` block, call `mockInvoke.mockReset()` then `mockInvoke.mockResolvedValue(makeEmptyView())` to seed every test with the empty CalcStateView baseline.

    2. **Helper: `makeEmptyView(overrides)`.** Returns a complete CalcStateView object matching the App.tsx interface declaration at lines 23-40. All numeric/string fields default to safe values (`display_str: '0.0000'`, all stack registers `'0.0000'`, `in_eex_mode: false`, annunciators all-false, `print_lines: []`, `program_steps: ['END']`, `pc: 0`, `user_keymap: []`, `flags: []`, `display_override: null`, `event_buffer: []`). The `overrides` parameter is `Partial<CalcStateView>` spread over the defaults so tests can override individual fields. The CalcStateView type can be re-declared as a local `interface` in the test file (acceptable; a future tsc drift will fail-fast — alternative is to export the interface from App.tsx, but the test-local copy is simpler and the cost is one extra block).

    3. **Helper: `renderAppAndWait()`.** Renders `<App />` via `@testing-library/react`'s `render()`, then awaits `waitFor(() => expect(mockInvoke).toHaveBeenCalledWith('get_state'))` to ensure the initial mount `invoke('get_state')` settles before the test's first interaction. Returns the `render()` utils object (with `container`, `rerender`, `unmount`, etc.).

    4. **Helper: `clickKeyById(container, id)`.** Locates the SVG `<g>` element representing the KEY_DEFS entry with the given id, then fires a click. Two locator strategies are acceptable: (a) ADD a test-only `data-key-id={key.id}` attribute to the `<g>` rendering inside Keyboard.tsx (single 1-line change; zero behavior cost — data attributes are not reflected to React listeners), then `container.querySelector(\`[data-key-id="${id}"]\`)` followed by `fireEvent.click(element)`. (b) Locate by the keycap label text via `screen.getByText('SIN')` etc. Strategy (a) is more robust against future label/layout changes; (b) avoids touching production source at all. Executor picks. If (a) is chosen, document the data-key-id attribute with a comment in Keyboard.tsx referencing this plan (26-04 Task 3).

    5. **Test organization.** Each test group A through F from `<behavior>` is a separate `describe(...)` block; each individual test (A1, A2, A3, B1, B2, C1, C2, D1, D2, D3, E1, E2, F1) is an `it(...)` case inside its describe. Total: 6 describes + 13 its (the count is 13 not 11 — A has 3, D has 3, others have 2 or 1).

    6. **Test contract.** Every test asserts via two channels: (a) the mocked invoke's call list (`expect(mockInvoke).toHaveBeenCalledWith('dispatch_op', { keyId: <expected> })` or `expect(mockInvoke).not.toHaveBeenCalledWith(...)` for negative cases), and (b) the rendered DOM (`screen.findByText(...)`, `container.querySelector(...)`, etc.). Tests do NOT introspect React internal state directly — the pattern is "fire event → wait for DOM/invoke side-effect → assert".

    7. **No new production dependencies.** `@testing-library/react@^16.3.2` and `vitest@^4.1.6` are already in hp41-gui/package.json devDependencies (confirmed in 26-01-SUMMARY). The vite config at hp41-gui/vite.config.ts already declares `test: { environment: 'jsdom' }` for the Display14Seg.test.tsx render tests, so this test file inherits jsdom automatically.

    8. **SVG event quirk note for jsdom.** The Keyboard.tsx `<g>` keyboard handler is `onClick={() => handleKeyClick(key)}` — jsdom supports `click` events on SVG elements via `fireEvent.click(element)`. The existing Keyboard.test.tsx already does this successfully; read its test cases for the canonical pattern. If a test exhibits "click does not fire" behavior, fall back to `element.dispatchEvent(new MouseEvent('click', { bubbles: true }))`.

    9. **For tests that need to inspect the rendered displayText (D1 and D2).** If reading the SVG Display14Seg's text is too fragile in jsdom (the Display14Seg renders SVG `<path>` elements per glyph, NOT text nodes), use the test-only `data-displaytext={displayText}` attribute on the `<div className="display">` wrapper in App.tsx (single 1-line change). Then `container.querySelector('[data-displaytext]')?.getAttribute('data-displaytext')` reads the value. This is the same test-attribute convention as `data-key-id`. Document in a comment that the attribute is a test locator; it has no production effect (React serializes it to HTML but the application code does not read it).

    10. **Self-check after writing the test file:**
       - `cd hp41-gui && npx vitest run src/App.test.tsx 2>&1 | tail -20` shows 13 (or however the executor groups them) passing tests.
       - Total `cd hp41-gui && npx vitest run` count: 121 (existing baseline) + 7 (Task 1 CR-05) + 13 (Task 3) = 141 minimum. Exact count may differ if the executor splits/merges; floor is 130+.
       - `cd hp41-gui && npx tsc --noEmit` still clean.
       - Production behavior unchanged: the only production-source mutations from Task 3 are optional test-only data attributes on Keyboard.tsx `<g>` and App.tsx `.display` div; both are inert React-passthrough attributes with no listener attached.
  </action>

  <verify>
    <automated>cd hp41-gui && npx vitest run 2>&1 | tail -15 | grep -E "Test Files|Tests|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f hp41-gui/src/App.test.tsx && echo OK` returns `OK`
    - Source assertion: `grep -c "vi.mock('@tauri-apps/api/core'" hp41-gui/src/App.test.tsx` returns ≥ 1 (the Tauri invoke mock is in place — verifier's explicit recommendation closed)
    - Source assertion: `grep -cE "describe\(" hp41-gui/src/App.test.tsx` returns ≥ 6 (one per group A/B/C/D/E/F)
    - Source assertion: `grep -cE "it\(" hp41-gui/src/App.test.tsx` returns ≥ 11 (one per behavior described in `<behavior>` — 13 is the target but 11 is the floor for partial-merge groupings)
    - Behavior assertion (CR-01): a test inside App.test.tsx asserts `mockInvoke` was called with `('dispatch_op', { keyId: 'asn_25_TEST' })` after clicking SIN+typing TEST+ENTER (NOT asn_23_TEST, which is what the buggy `row*10+(col+1)` would produce for SIN)
    - Behavior assertion (CR-02): a test asserts physical 's' keypress while helpOpen=true does NOT result in any invoke call (uses `expect(mockInvoke).not.toHaveBeenCalledWith('dispatch_op', ...)` or equivalent count assertion)
    - Behavior assertion (CR-03): a test asserts on-screen ENTER click inside an open assign_label modal DOES result in an invoke call with `asn_*` keyId
    - Behavior assertion (CR-04): a test asserts that after an invoke returns `event_buffer: ['BEEP']`, the rendered `.toast` element contains the string `'BEEP'` (or showToast's `seq`-versioned variant — assert via `screen.findByText(/BEEP/)` with a small waitFor)
    - Behavior assertion (CR-05): a test asserts pressing '0' inside an open Catalog modal does NOT result in an invoke call; pressing '4' DOES result in `('dispatch_op', { keyId: 'catalog_4' })`
    - Behavior assertion (USER mode F1): the rendered keyboard SVG, after USER toggle + completed ASN, contains a `<text>` element with content `'TEST'` at the position of the STO key (proves CR-01 + CR-03 closure end-to-end)
    - Test command: `cd hp41-gui && npx vitest run` reports total passing ≥ 130 tests (floor; exact count depends on executor grouping)
    - Test command: `cd hp41-gui && just gui-check` exits with code 0 (the project-level gate from CLAUDE.md "Just task runner")
  </acceptance_criteria>

  <done>
    `hp41-gui/src/App.test.tsx` exists with 11+ integration tests covering each of CR-01..CR-05 end-to-end against a mocked Tauri invoke. The Tauri-invoke mock pattern is established for future tests (first occurrence in the repo). Total Vitest count ≥ 130. `just gui-check` clean. The verifier's explicit recommendation ("no integration test exercises the end-to-end click → modal → dispatch → render flow") is closed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase 26 Plan 04 SUMMARY + final gates</name>

  <files>.planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md</files>

  <read_first>
    - .planning/phases/26-gui-integration-and-polish/26-01-SUMMARY.md (the template — frontmatter shape, section structure)
    - .planning/phases/26-gui-integration-and-polish/26-03-SUMMARY.md (the polish-bundle precedent — shape for multi-fix summaries)
    - .planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md (the gap report — the SUMMARY explicitly references which BLOCKERs each task closed)
    - hp41-gui/src/App.tsx (the FINAL state after Task 2 — measure source changes for the SUMMARY metrics section)
    - hp41-gui/src/pending_input.ts (final state after Task 1 — measure source changes)
    - hp41-gui/src/App.test.tsx (final state after Task 3 — count integration tests for the SUMMARY)
  </read_first>

  <action>
    Create `.planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md` mirroring the frontmatter + section shape of 26-01-SUMMARY.md (read it first; replicate the YAML frontmatter fields: phase, plan, subsystem, tags, dependency_graph, tech_stack, key_files, decisions, metrics).

    SUMMARY sections (mandatory):

    1. **Frontmatter** — phase: 26-gui-integration-and-polish, plan: 04, gap_closure: true (mirror the PLAN's frontmatter), tags: [gap-closure, modal-routing, frontend-projections, integration-tests], dependency_graph.requires lists 26-01/02/03.

    2. **One-liner** — "Closes the 5 BLOCKER gaps from 26-VERIFICATION.md (CR-01..CR-05) with surgical edits to App.tsx and pending_input.ts, and ships the @testing-library/react integration test suite the verifier flagged as missing. Lifts Phase 26 from 7/12 to 12/12 must-haves verified."

    3. **Outcome** — narrative paragraph covering: which BLOCKER each Task closed (Task 1 = CR-05, Task 2 = CR-01+CR-02+CR-03+CR-04, Task 3 = integration test coverage for all five), what's now possible that wasn't before (USER mode relabel works end-to-end, AVIEW/PROMPT visible, BEEP/TONE surface as toasts, on-screen ENTER closes modals, helpOpen owns focus, CATALOG 4 reachable).

    4. **BLOCKER closure table** — one row per CR-01..CR-05 with: BLOCKER ID, file changed, line range, fix one-liner, test that locks it.

    5. **Integration test inventory** — list the test groups A..F + the 11+ it-cases, briefly describing what each asserts.

    6. **Source-grep verification** — paste the 8 acceptance-criteria grep results from Task 2 and Task 3 as inline shell output blocks (proof the wiring fixes landed).

    7. **All gates green table** — `just gui-check`, `cd hp41-gui && npx vitest run` (count), `cd hp41-gui && npx tsc --noEmit`, `cd hp41-gui/src-tauri && cargo test` (61 still pass since this plan touched ZERO backend files), `cd hp41-gui/src-tauri && cargo clippy`, SC-4 grep zero, build artifact size delta (frontend bundle should change by < 5KB — only pure-function fix + new useEffect; no new dependencies).

    8. **No-deviation note** — explicit statement that hp41-core and hp41-cli are untouched (SC-4 + Phase 25 frozen-core invariants preserved). Frontend-only fix.

    9. **Re-verification trigger** — note: "Phase 26 verification should now re-run with `/gsd-verify-phase 26 --re-verify` and lift score from 7/12 to 12/12. The 7 human_verification items in 26-VERIFICATION.md are still pending manual sanity check by the developer (visual LCD, modal preview, etc.) but no longer have any known blockers."

    10. **Cross-references** — back-link to 26-VERIFICATION.md gaps section, forward-pointer to potential 27-future Playwright E2E (FN-QUAL-05) that would catch this class of regression at the booted-app level (Vitest+@testing-library/react catches it at the render-DOM level).

    **Final gate runs** (record in section 7):
    - `cd hp41-gui && just gui-check` → expected clean
    - `cd hp41-gui && npx vitest run` → expected ≥ 130 tests passing
    - `cd hp41-gui && npx tsc --noEmit` → expected clean
    - `cd hp41-gui/src-tauri && cargo test --no-fail-fast` → expected 61 passing (unchanged from 26-VERIFICATION baseline)
    - `grep -rEn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/` → expected zero matches

    Do NOT commit. The orchestrator handles git after plan-checker passes (per the gap_closure_context note).

    Do NOT mark anything in ROADMAP.md or STATE.md — the verifier+orchestrator handle phase status transitions.
  </action>

  <verify>
    <automated>test -f .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md && head -20 .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md | grep -E "phase|plan|gap_closure"</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md`
    - Frontmatter assertion: `head -20 .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md | grep -c "plan: 04"` returns 1
    - Frontmatter assertion: `head -20 .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md | grep -c "gap_closure: true"` returns 1
    - Section assertion: `grep -c "CR-01\|CR-02\|CR-03\|CR-04\|CR-05" .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md` returns ≥ 5 (each BLOCKER ID is mentioned in the closure table)
    - Section assertion: `grep -ci "integration test" .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md` returns ≥ 1 (test inventory section)
    - Section assertion: `grep -c "SC-4" .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md` returns ≥ 1 (SC-4 invariant preservation noted)
    - CLI assertion: `cd hp41-gui && just gui-check` exits with code 0
    - CLI assertion: `cd hp41-gui && npx vitest run` reports ≥ 130 tests passing
    - CLI assertion: `cd hp41-gui/src-tauri && cargo test --no-fail-fast 2>&1 | tail -5 | grep -E "61 passed|test result: ok"` returns ≥ 1 match (Rust test count unchanged at 61)
    - CLI assertion: `grep -rEn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/` returns zero matches (SC-4 preserved)
  </acceptance_criteria>

  <done>
    `26-04-SUMMARY.md` exists with all 10 sections; final gate runs are recorded with concrete pass evidence; SC-4 invariant explicitly verified zero; ≥ 130 Vitest tests passing; Phase 26 ready for `/gsd-verify-phase 26 --re-verify`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React render → window keydown listener | User-controlled keystrokes flow through `handleKey` and can reach `dispatch_op`. CR-02 closes the leak path through helpOpen. |
| On-screen click → modal alphabet | `effectiveId` strings ('enter', 'clx_or_a') from KEY_DEFS are user-controlled via click and must be translated to the modal's expected alphabet ('Enter', 'Backspace') without expanding the attack surface. CR-03. |
| Mocked Tauri invoke in tests → real Tauri invoke in production | The mock is test-only; production `invoke` calls remain unchanged. Mock isolation guaranteed by `vi.mock` scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | App.tsx handleClick assign_key branch | mitigate | CR-01 fix: assignments are stored at canonical CLI literals (KeyDef.keyCode hardcoded from `hp41-cli/src/keys.rs::keycode_to_hp41_code`). A malicious page-injected click cannot rewrite KeyDef.keyCode (const + readonly KEY_DEFS export). Undefined-keyCode keys (variant='top'/'shift', clx_or_a, etc.) reject with toast — no fallback to a computed value an attacker could exploit. |
| T-26-04-02 | Spoofing | Help overlay search input | mitigate | CR-02 fix: window-level handleKey returns early when helpOpen=true, so a JS-injected `dispatchEvent(new KeyboardEvent('keydown', { key: 's' }))` cannot trigger `Op::Sqrt` from inside the overlay. The overlay's `<input>` owns its keystroke processing. |
| T-26-04-03 | Information disclosure | event_buffer toast surfacing | accept | event_buffer carries strings produced exclusively by hp41-core's print_buffer/event_buffer push sites (BEEP/TONE/etc. — no user-influenced content). Existing precedent: print_lines is consumed via the same useEffect pattern at lines 421-426. No new disclosure surface. |
| T-26-04-04 | Denial of service | event_buffer toast queue | accept | The single-toast policy (newest replaces older, no queue) means a malicious program emitting 1000 TONE events would surface only the last as a 2s toast. No memory growth in React state. Existing showToast pattern with `seq` counter is sufficient. |
| T-26-04-05 | Repudiation | ASN keycode mismatch / lost assignments | mitigate | CR-01 fix locks the canonical keyCode contract. The user_keymap projection in CalcStateView reflects the actual backend state, so a successful ASN is observable via toggle USER → keycap relabels. Integration test F1 locks the round-trip. |
| T-26-04-06 | Elevation of privilege | Tauri invoke mock leaks to production | accept | `vi.mock('@tauri-apps/api/core', ...)` is scoped to the vitest runtime. Production `npm run build` / `just gui-build` does not load test files (vite ignores `*.test.tsx` per default config). No risk of mock leak. |
| T-26-04-07 | Tampering | data-key-id / data-displaytext test attributes on rendered DOM | accept | If the executor adds test-only `data-key-id={key.id}` to Keyboard.tsx `<g>` or `data-displaytext={displayText}` to App.tsx `.display`, these are purely informational. The attributes are reflected from readonly KEY_DEFS or computed displayText; user-supplied input cannot inject a value here (no user-controlled string flows into either render path). Equivalent to existing label text. |
</threat_model>

<verification>
After all 4 tasks complete, the entire gap-closure should be verifiable via a single command sequence:

```bash
cd hp41-gui && just gui-check          # tsc + clippy + cargo test + vitest, all clean
cd hp41-gui && npx vitest run | tail   # ≥ 130 passing tests including new App.test.tsx
cd hp41-gui/src-tauri && cargo test    # 61 passing (unchanged from baseline)
grep -rEn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/  # zero
grep -c "key.row \* 10" hp41-gui/src/App.tsx          # 0 (CR-01 bug formula gone)
grep -c "key.keyCode" hp41-gui/src/App.tsx            # ≥ 1 (CR-01 fix in place)
grep -c "if (helpOpen) return" hp41-gui/src/App.tsx   # ≥ 1 (CR-02 short-circuit in place)
grep -c "display_override ??" hp41-gui/src/App.tsx    # ≥ 1 (CR-04a derivation wired)
grep -nE "useEffect.*event_buffer" hp41-gui/src/App.tsx  # match (CR-04b consumer wired)
grep -c "max: 4" hp41-gui/src/App.tsx                 # ≥ 1 (CR-05 catalog ceiling fix)
grep -c "max: 3" hp41-gui/src/App.tsx                 # 0 (old ceiling gone)
grep -c "minDigit" hp41-gui/src/pending_input.ts      # ≥ 1 (CR-05 lower-bound guard)
```

All ten checks must pass. Failure of any one signals the gap-closure is incomplete.

The 7 `human_verification` items in 26-VERIFICATION.md (visual LCD sanity, modal preview rendering, help overlay search behavior, USER mode relabel, 'p'/'P' physical-key remap, AVIEW visible effect, BEEP/TONE feedback) become available for re-execution; the three that previously WOULD HAVE FAILED (helpOpen search, USER mode relabel, AVIEW visible) should now succeed.
</verification>

<success_criteria>
1. All 5 BLOCKERs from `.planning/phases/26-gui-integration-and-polish/26-VERIFICATION.md` are closed:
   - CR-01 (ASN keycode): `key.keyCode` (not `row*10+col`) is the assign target; undefined keyCode rejects with toast.
   - CR-02 (helpOpen leak): `if (helpOpen) return;` short-circuits handleKey before resolveKeyId.
   - CR-03 (on-screen ENTER): `'enter'` / `'clx_or_a'` translated to `'Enter'` / `'Backspace'` at the click-router boundary in App.tsx.
   - CR-04 (display_override + event_buffer): displayText derives `?? display_str`; new useEffect surfaces event_buffer entries as toasts.
   - CR-05 (Catalog bounds): MODAL_OPENERS.catalog max:4; single_digit arm enforces op-specific lower bound (Catalog ≥ 1, Tone ≥ 0).

2. Integration test suite ships at `hp41-gui/src/App.test.tsx` with 11+ @testing-library/react integration tests covering CR-01..CR-05 end-to-end against a mocked Tauri invoke. This is the verifier's explicit recommendation closed.

3. Phase 26 must-haves count rises from 7/12 to 12/12 (5 BLOCKERs closed = 5 must-haves restored). Subject to re-verification by `/gsd-verify-phase 26 --re-verify`.

4. All Vitest tests pass (count ≥ 130 — existing 121 + new ≥ 9 unit + integration tests).

5. `just gui-check` clean. `cargo test -p hp41-gui-tauri` still 61 passing (zero backend changes). SC-4 invariant grep zero matches.

6. ZERO changes to `hp41-core/` or `hp41-cli/` directories — gap-closure stays inside `hp41-gui/` per the SC-4 invariant and the Phase 25 frozen-core constraint per CLAUDE.md.

7. No new production dependencies; only test files added (App.test.tsx) and 7 + 11 = 18 new test cases.

8. No new `Op` variant, no new `CalcState` field, no new IPC command, no new permission TOML — the gap-closure is purely React-side wiring + tests.
</success_criteria>

<output>
After all 4 tasks complete (Task 4 already creates the SUMMARY), the orchestrator will commit the 5 files:
- hp41-gui/src/App.tsx
- hp41-gui/src/pending_input.ts
- hp41-gui/src/pending_input.test.ts
- hp41-gui/src/App.test.tsx
- .planning/phases/26-gui-integration-and-polish/26-04-SUMMARY.md

Plus optional: hp41-gui/src/Keyboard.tsx if Task 3 added a `data-key-id` test-locator attribute.

Commit (executor uses `/git-workflow:commit --with-skills` per CLAUDE.md):
- Suggested subject (English per CLAUDE.md "Commit language: English only"): `fix(26-04): close 5 BLOCKER gaps from Phase 26 verification (CR-01..CR-05) + ship integration test suite`
- Body bullets: one per BLOCKER closed; one for the @testing-library/react integration suite; SC-4 unchanged; hp41-core / hp41-cli untouched.
</output>
