---
phase: 07-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/lib.rs
  - hp41-core/src/ops/math.rs
autonomous: true
requirements: [QUAL-03, QUAL-04]
must_haves:
  truths:
    - "No unwrap() calls exist in production code in hp41-core/src/ops/math.rs"
    - "`#![deny(clippy::unwrap_used)]` is present at the hp41-core crate root"
    - "Phase 5 Plan 11 fixes are confirmed: gcd_ops has Op::Int after Op::Div, stack_stats_ops uses XLtY in max section and XGtY in min section"
    - "`just lint` passes with zero warnings after the deny attribute is added"
  artifacts:
    - path: "hp41-core/src/lib.rs"
      provides: "Crate-level lint deny for clippy::unwrap_used"
      contains: "#![deny(clippy::unwrap_used)]"
    - path: "hp41-core/src/ops/math.rs"
      provides: "Panic-free angle conversion constants"
      contains: ".expect(\"valid constant\")"
  key_links:
    - from: "hp41-core/src/lib.rs"
      to: "hp41-core (all production code)"
      via: "crate-level #![deny]"
      pattern: "deny\\(clippy::unwrap_used\\)"
    - from: "hp41-core/src/ops/math.rs pi_over_180"
      to: "HpNum(raw_decimal)"
      via: "Decimal::from_str(...).expect(...)"
      pattern: "\\.expect\\(\"valid constant\"\\)"
---

<objective>
Verify Phase 5 Plan 11 is complete (gcd_ops + stack_stats_ops fixes) then execute the
Phase 7 panic audit: replace the two `unwrap()` calls in math.rs with `.expect("valid
constant")` and add `#![deny(clippy::unwrap_used)]` at the hp41-core crate root.

Purpose: Establish the zero-panic structural guarantee before any other hardening work.
The `#![deny]` attribute makes the guarantee compile-time enforceable — subsequent plans
cannot inadvertently introduce new panics without breaking the build.

Output: panic-free hp41-core/src/ops/math.rs, updated hp41-core/src/lib.rs, confirmation
that Phase 5 Plan 11 is verified complete.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 5 Plan 11 completion (D-16)</name>
  <files>hp41-cli/src/programs.rs</files>
  <read_first>
    - hp41-cli/src/programs.rs lines 240-260 (gcd_ops modulo step — confirm Op::Int after Op::Div)
    - hp41-cli/src/programs.rs lines 335-365 (stack_stats_ops — confirm XLtY in max section, XGtY in min section)
    - .planning/phases/05-persistence-and-ux/05-11-SUMMARY.md (confirm execution recorded)
  </read_first>
  <action>
    This is a verification-only task. Confirm each of the following invariants by reading
    the relevant lines — do NOT modify programs.rs.

    Invariant 1 — CR-02 fix (gcd_ops):
    In gcd_ops(), the modulo calculation block must contain Op::Int immediately after
    Op::Div. Expected sequence in the vec![...] literal:
      Op::RclReg(0), Op::RclReg(1), Op::Div,
      Op::Int,   // floor-truncate quotient
      Op::RclReg(1), Op::Mul,

    Invariant 2 — CR-03 fix (stack_stats_ops):
    Max-finding section (3 comparisons): each uses TestKind::XLtY
    Min-finding section (3 comparisons): each uses TestKind::XGtY

    Invariant 3 — Tests exist:
    The test module contains test_gcd_correctness and test_stack_stats_correctness.

    If ANY invariant fails: execute the fix as described in
    .planning/phases/05-persistence-and-ux/05-11-PLAN.md Tasks 1–3 before proceeding
    to Task 2 of THIS plan. (Based on the SUMMARY.md, these fixes are expected to already
    be in place — verification should confirm, not repeat work.)

    Run the verification command below and confirm it exits 0 with the expected counts.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-cli -- programs::tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli -- programs::tests` exits 0 with all tests passing
    - grep confirms Op::Int present in gcd_ops: `grep -c "Op::Int" hp41-cli/src/programs.rs` returns >= 2
    - grep confirms max section: `grep -c "TestKind::XLtY" hp41-cli/src/programs.rs` returns >= 3
    - grep confirms min section: `grep -c "TestKind::XGtY" hp41-cli/src/programs.rs` returns >= 3
    - Test names `test_gcd_correctness` and `test_stack_stats_correctness` appear in output with ` ok`
  </acceptance_criteria>
  <done>Phase 5 Plan 11 confirmed complete. All three invariants verified. Programs test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix math.rs unwrap() calls and add deny(clippy::unwrap_used) to lib.rs (D-13, D-14)</name>
  <files>hp41-core/src/ops/math.rs, hp41-core/src/lib.rs</files>
  <read_first>
    - hp41-core/src/ops/math.rs lines 17-30 (full context of pi_over_180 and pi_over_200 including the comment block that explains HpNum(raw_decimal) bypass — MUST be preserved)
    - hp41-core/src/lib.rs (full file — add deny attribute at correct position)
  </read_first>
  <action>
    ## Step A: Fix math.rs (D-13)

    In hp41-core/src/ops/math.rs, change lines 25-28 from:

    ```rust
    fn pi_over_180() -> HpNum {
        HpNum(Decimal::from_str("0.01745329251994329576").unwrap())
    }
    fn pi_over_200() -> HpNum {
        HpNum(Decimal::from_str("0.01570796326794896558").unwrap())
    }
    ```

    to:

    ```rust
    fn pi_over_180() -> HpNum {
        HpNum(Decimal::from_str("0.01745329251994329576").expect("valid constant"))
    }
    fn pi_over_200() -> HpNum {
        HpNum(Decimal::from_str("0.01570796326794896558").expect("valid constant"))
    }
    ```

    CRITICAL: The `HpNum(raw_decimal)` wrapper (using the pub(crate) inner field directly,
    bypassing HpNum::rounded()) MUST be preserved exactly as-is. The fix is ONLY replacing
    `.unwrap()` with `.expect("valid constant")`. Do not change the string literals, do not
    wrap with HpNum::rounded(), do not change the function signatures or the surrounding
    comment block (lines 17-23) that explains the bypass rationale.

    ## Step B: Add deny attribute to lib.rs (D-14)

    Add `#![deny(clippy::unwrap_used)]` as the FIRST line in hp41-core/src/lib.rs,
    before the existing `//!` doc comment:

    ```rust
    #![deny(clippy::unwrap_used)]
    //! hp41-core — HP-41 calculator behavioral emulation library.
    //!
    //! Zero UI/CLI dependencies. All state is in [`state::CalcState`].
    ```

    The deny attribute applies to all production code in hp41-core. Test files that
    legitimately use unwrap (for test conciseness) MUST have `#[allow(clippy::unwrap_used)]`
    at their module level. Check the tests.rs file: if any test uses `.unwrap()`, add
    `#![allow(clippy::unwrap_used)]` at the top of that file.

    ## Step C: Verify no other unwrap() in production code

    After adding the deny attribute, run `just lint` (= `cargo clippy --workspace
    --all-targets --all-features -- -D warnings`). If clippy reports unwrap_used violations
    in any hp41-core source file (not test file), fix each one using `.expect("reason")`.
    Production files are: src/lib.rs, src/error.rs, src/num.rs, src/state.rs, src/stack.rs,
    src/format.rs, src/ops/mod.rs, src/ops/math.rs, src/ops/arithmetic.rs, src/ops/stack_ops.rs,
    src/ops/registers.rs, src/ops/alpha.rs, src/ops/program.rs, src/ops/stats.rs, src/ops/hms.rs.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo clippy -p hp41-core --all-targets --all-features -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '\.expect\|#\[allow' hp41-core/src/ops/math.rs | grep -c '\.unwrap()'` returns 0
    - `grep -c '#!\[deny(clippy::unwrap_used)\]' hp41-core/src/lib.rs` returns 1
    - `cargo clippy -p hp41-core --all-targets --all-features -- -D warnings` exits 0 with no errors
    - `just test` exits 0 (no regressions — the .expect() change is behaviorally identical to .unwrap() for valid string literals)
    - The string literal "0.01745329251994329576" is unchanged in math.rs (verify with grep)
    - The string literal "0.01570796326794896558" is unchanged in math.rs (verify with grep)
  </acceptance_criteria>
  <done>
    Both pi_over_180() and pi_over_200() use .expect("valid constant"). The #![deny] attribute
    is in lib.rs. just lint and just test both exit 0 with no errors or failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hp41-core production code | Library boundary — panics here crash the entire CLI process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Denial of Service | pi_over_180/pi_over_200 panic on invalid string | mitigate | `.expect("valid constant")` documents the invariant; `#![deny(clippy::unwrap_used)]` prevents future panics at compile time |
| T-07-01-02 | Denial of Service | Future unwrap() in production code | mitigate | `#![deny(clippy::unwrap_used)]` in lib.rs — compile-time enforcement for all hp41-core source files |
</threat_model>

<verification>
After completing both tasks:

1. Confirm Phase 5 Plan 11 invariants:
   cd /Users/daniel/GitRepository/hp41-calculator-emulator
   cargo test -p hp41-cli -- programs::tests
   Expected: all tests pass (test_gcd_correctness ok, test_stack_stats_correctness ok)

2. Confirm no unwrap() in math.rs production code:
   grep -v '\.expect\|#\[allow' hp41-core/src/ops/math.rs | grep '\.unwrap()'
   Expected: no output (zero matches)

3. Confirm deny attribute present:
   grep '#!\[deny(clippy::unwrap_used)\]' hp41-core/src/lib.rs
   Expected: one match

4. Confirm clippy and test pass:
   just lint && just test
   Expected: both exit 0
</verification>

<success_criteria>
- Phase 5 Plan 11 confirmed complete (gcd_ops, stack_stats_ops, tests all pass)
- hp41-core/src/ops/math.rs: zero `.unwrap()` calls in production code
- hp41-core/src/lib.rs: `#![deny(clippy::unwrap_used)]` present as first line
- `just lint` exits 0 (clippy clean across workspace)
- `just test` exits 0 (369+ tests, 0 failed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening/07-01-SUMMARY.md`
using the template at @/Users/daniel/.claude/get-shit-done/templates/summary.md.
</output>
