---
quick_id: 260508-06h
slug: fix-sci-eng-digit-input
description: Implement FIX n / SCI n / ENG n digit-count input in the TUI
date: "2026-05-08"
status: planned
must_haves:
  truths:
    - PendingInput::FmtDigits(DisplayMode) variant exists in app.rs
    - 'F' (uppercase) opens the FmtDigits modal for current display mode
    - digit 0-9 in modal dispatches correct FmtFix/Sci/Eng(n) op
    - 'f' in modal cycles the pending format type without dispatching
    - Esc cancels modal (no state change)
    - status bar shows "FIX [_]" / "SCI [_]" / "ENG [_]" while modal is open
    - help_data.rs updated with new 'F' entry and updated 'f' description
    - all existing tests pass (cargo test --workspace)
---

# Quick Task 260508-06h: FIX n / SCI n / ENG n Digit-Count Input

## Goal

Add a modal (using the existing `PendingInput` pattern) so the user can set the
exact digit count for FIX/SCI/ENG display modes. Currently `'f'` cycles with
hardcoded count=4.

**Design:**
- `'f'` (lowercase): unchanged — immediately cycles Fix→Sci→Eng, keeping
  the digit count from the current mode
- `'F'` (uppercase Shift+f): opens `PendingInput::FmtDigits(DisplayMode)`
  modal with the current display mode
  - In modal: digit `0`–`9` → dispatch `FmtFix/Sci/Eng(n)`, close modal
  - In modal: `'f'` → cycle format type (Fix→Sci→Eng→Fix), stay in modal
  - In modal: `Esc` → cancel, close modal, no change

## Tasks

### Task 1: Add PendingInput::FmtDigits and handler logic in app.rs

**Files:** `hp41-cli/src/app.rs`

**Action:**
1. Add `FmtDigits(DisplayMode)` to the `PendingInput` enum (after `ConfirmLoad`)
2. In `handle_key()`, add a handler for `'F'` (before the `key_to_op()` fallthrough)
   that opens `PendingInput::FmtDigits(self.state.display_mode.clone())`
3. In `handle_pending_input()`, add a match arm for `FmtDigits`:
   - digit `0`–`9`: dispatch `Op::FmtFix/Sci/Eng(n)` matching the stored mode, clear pending
   - `'f'`: cycle stored mode Fix→Sci→Eng→Fix, update pending to `FmtDigits(new_mode)`
   - `Esc`: clear pending
   - other keys: restore pending silently

**Verify:** `cargo check -p hp41-cli` passes, no new warnings

### Task 2: Add status bar prompt for FmtDigits in ui.rs

**Files:** `hp41-cli/src/ui.rs`

**Action:**
Add match arm to `pending_prompt()`:
```rust
PendingInput::FmtDigits(mode) => {
    let label = match mode {
        DisplayMode::Fix(_) => "FIX",
        DisplayMode::Sci(_) => "SCI",
        DisplayMode::Eng(_) => "ENG",
    };
    format!("{label} [_]  (0–9 set digits, f cycles, Esc cancel)")
}
```

**Verify:** `cargo check -p hp41-cli` passes

### Task 3: Update help_data.rs

**Files:** `hp41-cli/src/help_data.rs`

**Action:**
1. Update the `'f'` description: `"Cycle display format (FIX/SCI/ENG), keeps current digit count"`
2. Add new entry after `'f'`: `("F", "FIX/SCI/ENG n", "Open digit-count modal: enter 0–9 to set digits; f cycles format")`

**Verify:** `cargo test -p hp41-cli -- test_help_data` passes (existing help tests)

### Task 4: Run full test suite and commit

**Action:**
```bash
cargo test --workspace
```

Commit all changes atomically with message:
`feat(tui): add F modal for FIX/SCI/ENG digit-count input (0–9)`
