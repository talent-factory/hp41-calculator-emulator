---
quick_id: 260508-06h
slug: fix-sci-eng-digit-input
status: complete
date: "2026-05-08"
commit: 7ff792c
duration_minutes: 15
tags: [tui, display-mode, modal, input]
key_files:
  modified:
    - hp41-cli/src/app.rs
    - hp41-cli/src/ui.rs
    - hp41-cli/src/help_data.rs
    - hp41-cli/src/keys.rs
decisions:
  - "Repurposed 'F' from Op::HToHms (→HMS) to FmtDigits modal; HToHms is now unbound — 'h' (HmsToH) remains"
  - "FmtDigits stores DisplayMode (Clone) — digit count in stored mode is carried through 'f' cycling"
  - "Intercept 'F' in handle_key() before key_to_op(), same pattern as 'S'/'R' modals"
---

# Quick Task 260508-06h: FIX n / SCI n / ENG n Digit-Count Input — Summary

One-liner: FmtDigits modal opened by 'F' lets user set exact digit count 0–9 for FIX/SCI/ENG display modes.

## Tasks Completed

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Add PendingInput::FmtDigits + handle_key + handle_pending_input in app.rs | Done | 7ff792c |
| 2 | Add FmtDigits arm in pending_prompt() in ui.rs | Done | 7ff792c |
| 3 | Update help_data.rs (f description + F entry) | Done | 7ff792c |
| 4 | Run full test suite (472 tests, 0 failures) and commit | Done | 7ff792c |

## Must-Have Verification

| Truth | Result |
|-------|--------|
| PendingInput::FmtDigits(DisplayMode) variant exists in app.rs | PASS |
| 'F' (uppercase) opens FmtDigits modal for current display mode | PASS |
| digit 0-9 in modal dispatches correct FmtFix/Sci/Eng(n) op | PASS |
| 'f' in modal cycles the pending format type without dispatching | PASS |
| Esc cancels modal (no state change) | PASS |
| status bar shows "FIX [_]" / "SCI [_]" / "ENG [_]" while modal open | PASS |
| help_data.rs updated with new 'F' entry and updated 'f' description | PASS |
| all existing tests pass (cargo test --workspace) | PASS — 472/472 |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Conflict] 'F' was already bound to Op::HToHms (→HMS) in keys.rs**
- **Found during:** Task 1 (reviewing keys.rs before implementation)
- **Issue:** `key_to_op()` returned `Some(Op::HToHms)` for `KeyCode::Char('F')`. Binding the FmtDigits modal to 'F' requires removing this mapping.
- **Fix:** Removed `KeyCode::Char('F') => Some(Op::HToHms)` from `key_to_op()`. The intercept in `handle_key()` runs before `key_to_op()`, so the old binding was effectively dead anyway. Updated KEY_REF_TABLE and help_data.rs `→HMS` entry to reflect that the key is now unbound.
- **Files modified:** `hp41-cli/src/keys.rs`, `hp41-cli/src/help_data.rs`
- **Commit:** 7ff792c

## Self-Check: PASSED

- hp41-cli/src/app.rs — modified, contains PendingInput::FmtDigits and handlers
- hp41-cli/src/ui.rs — modified, contains FmtDigits arm in pending_prompt()
- hp41-cli/src/help_data.rs — modified, 'f' description updated, 'F' entry added in Display section
- hp41-cli/src/keys.rs — modified, 'F' removed from key_to_op(), KEY_REF_TABLE updated
- Commit 7ff792c exists: `git log --oneline | grep 7ff792c` confirms
- 472 tests pass, 0 failures
