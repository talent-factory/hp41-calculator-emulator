---
quick_id: 260506-a1g
slug: add-gitignore
description: "Add .gitignore for Rust/Cargo workspace"
date: "2026-05-06"
status: planned
must_haves:
  truths:
    - .gitignore exists at repo root
    - /target/ is ignored (Rust build artifacts)
    - .DS_Store is ignored (macOS)
    - Coverage artifacts are ignored (lcov.info, *.profraw, *.profdata)
    - Insta pending snapshots are ignored
  artifacts:
    - /.gitignore
---

# Quick Task 260506-a1g: Add .gitignore

## Goal

Add a proper `.gitignore` for a Rust/Cargo workspace project (hp41-core + hp41-cli + future hp41-gui Tauri app).

## Context

Currently missing completely — `target/` and `.DS_Store` are untracked noise in `git status`.

## Tasks

### Task 1: Create .gitignore at repo root

**Files:** `/.gitignore`
**Action:** Create comprehensive .gitignore covering Rust/Cargo artifacts, macOS/Windows OS files, editor configs, coverage artifacts, and future Tauri build output.
**Verify:** `git status` no longer shows `target/` or `.DS_Store` as untracked
**Done:** `.gitignore` committed

### Sections to cover

1. **Rust/Cargo** — `/target/`, `Cargo.lock` (keep for binary crates; this is a workspace with a binary so keep it)
2. **Coverage** — `lcov.info`, `*.profraw`, `*.profdata`, `tarpaulin-report.html`, `/coverage/`
3. **Insta snapshots** — `**/*.pending-snap`
4. **OS files** — `.DS_Store`, `Thumbs.db`, `Desktop.ini`
5. **Editors** — `.idea/`, `.vscode/` (optional, many teams commit .vscode — add as comment)
6. **Env files** — `.env`, `.env.local`, `.env.*.local`
7. **Tauri (future)** — `/dist/`, `node_modules/`, `src-tauri/target/`, `*.dmg`, `*.deb`, `*.rpm`, `*.msi`, `*.AppImage`
8. **Claude Code local** — `.claude/` (machine-specific agent data)
