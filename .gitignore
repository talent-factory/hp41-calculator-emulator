# Rust / Cargo
/target/
**/*.rs.bk

# Standalone dev-tooling crates — not in workspace
scripts/docs-matrix/target/
scripts/help-aliases/target/

# Coverage (cargo-llvm-cov, tarpaulin)
lcov.info
tarpaulin-report.html
*.profraw
*.profdata
/coverage/

# Insta snapshot testing — pending review files
**/*.pending-snap

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors
.idea/
**/xcuserdata/
# .vscode/ — omitted intentionally; commit shared settings if the team uses them

# Environment / secrets
.env
.env.local
.env.*.local

# Native SwiftUI GUI
/.build/
hp41-gui/.build/
hp41-gui/hp41-app/target/
hp41-gui/hp41-bridge/target/

# Legacy Tauri artifacts (the sources are retained temporarily for migration history)
hp41-gui/dist/
hp41-gui/src-tauri/gen/macos/
node_modules/
src-tauri/target/
*.dmg
*.deb
*.rpm
*.msi
*.AppImage
*.exe

# Claude Code local agent data
.claude/
.clone/

# Graphify (knowledge graph) — keep config + GRAPH_REPORT, ignore derived artifacts
graphify-out/
.planning/graphs/graph.json
.planning/graphs/graph.html
.planning/graphs/.last-build-snapshot.json
.gbrain-source
.grepai/

# Playwright MCP session artifacts (console/page dumps)
.playwright-mcp/

# Headroom + Serena local tool scratch
.headroom/
.serena/
