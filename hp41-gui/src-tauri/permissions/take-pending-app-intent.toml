"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-take-pending-app-intent"
description = "Allows the iOS frontend to consume a pending App Intent request."
commands.allow = ["take_pending_app_intent"]
